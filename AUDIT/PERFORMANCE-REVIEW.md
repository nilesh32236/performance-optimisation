# PERFORMANCE REVIEW — Consolidated

_Source: Agent A09 (primary) + A03/A01. Full evidence in `AUDIT/AGENTS/agent-A09-performance.md`._

_Generated 2026-08-27, do not modify production code during audit._

## Excerpt (first 120 lines of primary agent)

```
# Agent A09 — Performance Audit

**Scope:** `performance` — exhaustive line-by-line audit of ALL PHP (`includes/*.php`, `performance-optimisation.php`, `uninstall.php`, `templates/object-cache.php`) and JS (`src/**/*.js`, `src/**/*.jsx`, `build/*.js`)  
**Date:** 2026-08-27  
**Auditor:** agent A09 (`performance`)  
**Workspace:** `/var/www/nileshportfolio.duckdns.org/wp-content/plugins/performance-optimisation`  
**Instruction:** Focus on PHP CPU (loops, reflection, regex, serialization, filesystem, network, hooks), WordPress (get_option, update_option, transients, WP_Query, hooks, admin/frontend init, REST, cron), Database (query counts, duplicates, N+1, missing indexes, per-request queries), Frontend (JS execution, DOM, listeners, reflow, CSS, assets, dependencies), Caching (cache key, expiration, invalidation, stampede, object-cache compat). Trace Input→validation→processing→hooks→DB/cache→output→cleanup for every major functionality.

> **Method:** Read every file completely (`Read` with offsets), traced `Input→validation→processing→hooks→DB/cache→output→cleanup` for 12 hot paths (cache generation, combined-CSS, minify, CDN rewrite, used-CSS, image conversion/next-gen serving, preload/sitemap, PageSpeed/RUM/telemetry, DB cleanup, admin SPA, frontend lazyload/delay-JS). Counted `get_option`/`transient`/`WP_Query`/`$wpdb` per request, `file_exists/filesize/is_file/getimagesize` per render, `preg_match`/`WP_HTML_Tag_Processor` cost, `wp_remote_get/HEAD` sequential waits, DOM/reflow/listeners in `lazyload.js`/`rum.js`/`main.js`/SPA. Verified via `grep -rn` (115 transients, 119 FS, 201 regex) and static reasoning — no production code modified. Findings are `file:line` evidence-backed with measurable impact.

---

## 1. Files Reviewed (with line counts & performance surface)

| # | File | Lines | Performance Surface |
|---|------|-------|---------------------|
| 1 | `performance-optimisation.php` | 70 | Bootstrap, `ABSPATH` guard, constant define, autoloader `vendor/autoload.php` (always loaded even on `DOING_CRON`/preload) |
| 2 | `uninstall.php` | 165 | Per-site `DELETE FROM options/postmeta`, `get_sites(number=100)` pagination, `wppo_delete_directory` recursion |
| 3 | `templates/object-cache.php` | 1152 | `WP_Object_Cache` drop-in: `Redis*` connect, `get_current_blog_id()` prefixing, `serializer`/`compression`, `scan`+`del` flush |
| 4 | `includes/class-main.php` | ~2956 | `__construct` `get_option('wppo_settings')` + `maybe_migrate_block_assets_setting`, `setup_hooks` ~40 `add_action/filter`, `admin_enqueue_scripts` (asset `realpath` + `get_cache_size` + `get_js_css_minified_file`), `enqueue_scripts` (delay/defer parsing, `Util::process_urls` ×4, regex `strip_static_query_strings` per handle), `on_settings_update` (cache clear, `Advanced_Cache_Handler::create`), `on_save_post_queue_used_css` (`as_has_scheduled_action`), `get_frontend_theme_colors` (`wp_get_global_settings` + `get_theme_mod`) |
| 5 | `includes/class-cache.php` | 2269 | `__construct` host validation + `WP_CONTENT_DIR` guard, `combine_css` (queue loop ×2, `get_combined_handles` + generation, `core_will_inline` budget simulation, `should_skip_combine_for_inline_budget` `filesize`, `fetch_remote_css` `WP_Filesystem::get_contents`), `maybe_apply_cdn` (`WP_HTML_Tag_Processor` loop), `maybe_store_cache` (exclusion check), `process_buffer_for_cache`/`stash_cache`, `is_not_cacheable` (5 `is_cart/checkout/account`, `is_feed`, `pathinfo`), `invalidate_dynamic_static_html` (term loop + `get_term_link` N), `calculate_directory_size`/`count_cached_pages` (recursive `dirlist`), `save_cache_files` (`gzencode`+`brotli_compress`), `core_inline_budget_will_inline` (cached `inline_size_map`) |
| 6 | `includes/class-util.php` | 643 | `init_filesystem` (`WP_Filesystem()` per call), `get_local_path` (`home_url` + `ABSPATH` prefix), `prepare_cache_dir` (iterative `mkdir` per segment), `cached_home_url`/`cached_content_url` (blog-id static cache, `has_filter` bypass), `process_urls` (trim/dedup), `is_url_excluded` (scheme strip `stripos`×N, `(.*)` prefix), `min_cache_dir`/`transient_key`, `sanitize_settings_recursively` |
| 7 | `includes/class-cron.php` | 733 | `schedule_cron_jobs` (`get_option`×2, 6 `wp_next_scheduled`/`wp_schedule_event`), `schedule_page_cron_jobs` (`get_posts` 200/batch, `wp_next_scheduled` per page), `schedule_sitemap_url_jobs` (`get_sitemap_urls` `wp_remote_get`×up to 50, 15 s deadline, `wp_next_scheduled` per URL), `process_page`/`process_url`/`load_page` (`wp_remote_get` 30 s, `error_log` on fail), `img_convert_cron` (`queue_unconverted_library_images` 50, per-format loops × `batch` 50), `database_cleanup_cron` (schedule guard + `auto_clean`), `web_vitals_rescan_cron` (high-value URL dedup, `queue_scan`×strategies), `used_css_cron` |
| 8 | `includes/class-img-converter.php` | ~1950 | `convert_image` (MIME `wp_get_image_editor_output_format`, `getimagesize`, `filesize 20 MB` guard, `5000×5000` dim guard, `is_gain_map_image` `file_get_contents 64 KB`, `GD` `imagecreatefromjpeg/png/webp` + `convert_palette_to_truecolor`, `Imagick` GIF→WebP, `extract_dominant_color` sampled loop, `generate_lqip` 20 px thumb, `update_conversion_status` deferred `wppo_img_info`), `get_img_path`/`get_img_url` (host allowlist, `ABSPATH`/`WP_CONTENT_DIR` prefix), `maybe_serve_next_gen_image` (Accept header split), `is_client_side_media_processing` (blog-id static) |
| 9 | `includes/class-image-optimisation.php` | ~2200 | `__construct` `process_urls`×5, `maybe_serve_next_gen_images` (`WP_HTML_Tag_Processor` img/source/video/srcset loop, `normalize_url`, `replace_image_with_next_gen` per image), `replace_image_with_next_gen` (`file_exists`×2 per image, `add_img_into_queue`), `preload_images`/`get_all_preload_data` (`prepare_preload_item`×N, `get_post_thumbnail_id`+`wp_get_attachment_image_srcset`), `process_picture_blocks_*` (`WP_HTML_Processor` serialize_token + nesting), `add_delay_load_img`/`add_delay_load_backgrounds` (Tag Processor + `post_process_placeholders` `preg_replace_callback`), `post_process_img_dimensions` (`getimagesize` + 100-entry LRU) |
| 10 | `includes/class-used-css.php` | 1261 | `extract_selectors` (`WP_HTML_Tag_Processor` per tag), `parse_css` (char loop, brace depth, `@` branch), `purge_css` (safelist prefix scan), `process_buffer` (`file_exists`+`filemtime` per queued style, `get_all_css_assets` `get_contents` per handle, `generate_used_css` `parse_css`+`purge_css`), `delete_all_used_css` (BFS `dirlist` queue), `regenerate_all` (`get_posts` offset batches 200 + `as_has_scheduled_action` per post), `extract_css_assets_from_html` (`TagProcessor` link scan + `fetch_css_content_static` `WP_Filesystem` or `wp_safe_remote_get` 15 s) |
| 11 | `includes/class-telemetry.php` | 985 | `scan` (`get_transient/wp_cache_get_salted`, `curl_init`±`wp_remote_get`, `MAX_REDIRECT_HOPS=2` manual `resolve_redirect`), `parse_resources` (`WP_HTML_Tag_Processor` vs regex fallback, unminified/third-party counts), `calculate_sizes` (`filesize`+`wp_remote_head` 5 s × assets, `wp_http_validate_url`+host gate per asset) |
| 12 | `includes/class-pagespeed.php` | 661 | `queue_scan` (`as_enqueue_async_action`), `run_scan` (`wp_remote_get` 120 s, `json_decode`, `prepare_response`, `set_transient` 24 h, `record_trend`+`store_lcp_image_url`), `prepare_response` (category/vitals/diagnostics extraction), `record_trend` (`add_option` lock 60 s TTL + `prune_trends` oldest-by-`fetched_at`), `get_transient_key` (`Util::transient_key`+`md5`) |
| 13 | `includes/class-rum.php` | 332 | `collect` (`is_enabled` `get_option`, `is_valid_token` `wp_hash`+`hash_equals` daily, `is_rate_limited` `get/set_transient` per IP), `sanitize_sample` (clamp 0..60000/0..1), `store_sample` (`get_option`+`update_option` per beacon, `MAX_DAYS 14`/`MAX_PATHS 200` bounds, `array_shift` eviction, `min/max/sum/n` aggregates), `print_config`/`maybe_enqueue_scripts` |
| 14 | `includes/class-database-cleanup.php` | 1142 | `clean_*` batched `DELETE` 1000/5000/LIMIT, `clean_revisions_advanced` (parent loop 200 + inner `LIMIT 500 OFFSET`, chunk `array_chunk 50`, `prepare`×chunks), `clean_expired_transients` (JOIN `a.option_name = CONCAT(timeout_prefix, SUBSTRING…)` + loop), `clean_unattached_media` (`wp_delete_attachment` per ID), `get_counts` (9 `COUNT(*)` + JOIN×2, cached 5 min or `wppo_db_cleanup_salt`), `get_autoloaded_options` (`LENGTH(option_value) ORDER BY DESC LIMIT`), `optimize_table` (`information_schema.TABLES` + `OPTIMIZE TABLE` 1 GB guard), `invoke_cleanup_method` invalidates counts cache |
| 15 | `includes/class-log.php` | 150 | `add` (`$wpdb->insert` + `update_option(SALT_KEY,time())` or version bump), `get_recent_activities` (`COUNT(*)`+`get_results LIMIT/OFFSET`, `wp_cache_get_salted`/`wp_cache_get` HOUR) |
| 16 | `includes/class-rest.php` | 1563 | 26 routes, `permission_callback`, `clear_cache` `realpath`+prefix, `update_settings`/`import_settings` `sanitize_settings_recursively`+`array_replace_recursive`, `optimise_image` (`as_enqueue_async_action`×images + `Cache::clear_cache`), `handle_object_cache` (`build_redis_config`+`ping/enable/disable/flush`), `run_performance_scan`/`queue_pagespeed_scan` (SSRF gates), `get_image_job_status` (`as_get_scheduled_actions` ARRAY_A) |
| 17 | `includes/class-system-info.php` | 633 | `get_all` (9 groups), `get_server` (`php_uname`), `get_cache` (`wp_using_ext_object_cache`, `get_active_cache_plugin` `get_option(active_plugins)`+`get_site_option`), `get_litespeed` (`Advanced_Cache_Handler::is_our_dropin`+`file_get_contents` 1 MB, `Object_Cache::get_status`), `get_opcache` (`opcache_get_status`), `get_mysql_var` (`SHOW VARIABLES LIKE %s` `prepare`) |
| 18 | `includes/class-object-cache.php` | 363 | `get_status` (`file_exists`+`is_readable`+`filesize` 1 MB + `WP_Filesystem::get_contents`/`file_get_contents`, `get_option`+`include config_path`, `connect_internal`→`wppo_redis_connect`, `info`+`keys` parse), `ping`/`enable` (`put_contents` `var_export`, `copy` drop-in, `wp_cache_flush`), `flush` (`wp_cache_flush`) |
| 19 | `includes/redis-connect-helper.php` | 377 | `wppo_redis_connect` (standalone/sentinel/cluster, TLS, `wppo_parse_nodes`, `wppo_apply_redis_options` serializer/compression) |
| 20 | `includes/class-advanced-cache-handler.php` | 324 | `create` (PHP string `advanced-cache.php`), `is_our_dropin`/`foreign_dropin_present` (`strpos` marker), `get_dropin_path` |
| 21 | `includes/class-htaccess-handler.php` | 222 | `update_rules` (`insert_with_markers`), `get_rules` (deflate+expires+next-gen rewrite) |
| 22 | `includes/class-server-rules.php` | 191 | `get_server_type` (`SERVER_SOFTWARE` `stripos`), nginx/apache rule text |
| 23 | `includes/class-cron.php` | — | (counted above) — cron is also #7 |
| 24 | `includes/class-critical-css.php` | ~450 | `inline_ccss`/`defer_stylesheets`, `background_generate` (`curl`+`resolve_import_url` SSRF) |
| 25 | `includes/class-activate.php` | 348 | `add_wp_cache_constant` (wp-config edit), `create_activity_log_table` (`dbDelta`), legacy flush |
| 26 | `includes/class-deactivate.php` | 156 | `remove_wp_cache_constant`, drop-in/config `delete`, `clear_cron_jobs` |
| 27 | `includes/class-asset-manager.php` | 245 | `capture_page_assets`/`dequeue_selected_assets` (protected handles), transient `wppo_page_assets_{post_id}` |
| 28 | `includes/class-metabox.php` | 461 | `add_metabox`/`render` (`get_post_meta`), `save_metabox` (`wp_verify_nonce`+`current_user_can`+`Asset_Manager::get_page_assets`) |
| 29 | `includes/class-core-tweaks.php` | ~200 | `disable_emojis/embeds/dashicons/xmlrpc`, Heartbeat (`admin_init` `current_user_can`) |
| 30 | `includes/class-google-fonts.php` | ~250 | `process_buffer`/`process_style_tag` (`TagProcessor`→ buffer rewrite, `wp_remote_get` 10 s for `fonts.googleapis.com`) |
| 31 | `includes/class-litespeed-integration.php` | ~500 | `is_litespeed` (`SERVER_SOFTWARE`+`has_filter`), `should_disable_wppo_optimizer`, `can_apply_cdn`, `get_info`, purgeSync `wppo_after_cache_clear` |
| 32 | `includes/class-llms.php` | ~300 | `register_rewrite`, `serve` (`template_redirect`), `emit_link_header`, `generate` (FS), `on_settings_update` |
| 33 | `includes/class-cdn-purger.php` | ~100 | `purge_all` (Cloudflare `wp_remote_request` + `wp_http_validate_url`) |
| 34 | `includes/minify/class-css.php` / `class-js.php` / `class-html.php` | ~150ea | `Minify\CSS::minify` (`MatthiasMullie\Minify\CSS`), `get_smallest_inline_limit` |
| 35 | `src/lazyload.js` | 1025 | `AUTO_SIZES_SUPPORTED` probe (`appendChild`+`getComputedStyle` forced reflow), `readModuleData` `JSON.parse`, `getLazySelector` fn, `loadScript` (replaceChild, `Promise`), `loadScriptsByPriority` (3 groups `Promise.allSettled`), `loadScripts` (setTimeout 200 ms `loadImages`), `requestIdleCallback`+`setTimeout 2000` fallback, `IntersectionObserver` 200 px `globalObserver`+`observedElements WeakSet`, MutationObserver `childList subtree`, safetyScan `setInterval 10000` `querySelectorAll`, `observeElement` native-iframe fast path, `loadBackgrounds` separate IO, `initVideoPlaceholders` error delegate |
| 36 | `src/main.js` | 239 | `postJsonRequest` `fetch` `X-WP-Nonce`+403 `refreshNonce` retry, `refreshNonce` `pendingRefresh` dedup, `showNotice` `wp.data.dispatch('core/notices')` else DOM `notice-dismiss` + `setTimeout 5000` `fallbackTimer` |
| 37 | `src/rum.js` | 195 | IIFE, `window.wppoRum` gate, `PerformanceObserver` LCP/CLS/INP `buffered`, `getEntriesByType(navigation/paint)` TTFB/FCP, `send` `Blob`+`navigator.sendBeacon` fallback `fetch credentials:omit`, `setTimeout 5000` settle + `visibilitychange`/`pagehide once`, `disconnectObservers` |
| 38 | `src/index.js` / `src/App.js` | 11 / 527 | `createRoot` `<App/>`, `lazy` 7 tabs `webpackChunkName`, `AbortController`×`fetchRecentActivities`/`fetchServerRules`/`ccss_status`, `sidebarItems useMemo`, resize+focus-trap effects, themeColors `setProperty` |
| 39 | `src/components/Dashboard.js` | 1327 | `useState`×15, `fetchDbCounts` `apiCall('database_cleanup_counts')`, `pollJobStatus` `setTimeout 5000` `pollRetryRef 5`, `normalizeImageInfo`, `allSuggestions useMemo` dedup `metric`, effects cleanup `clearTimeout` |
| 40 | `src/lib/apiRequest.js` / `useNotice.js` / `util.js` | 249 / ~30 / ~20 | `apiCall` `doFetch`+`handleResponse` JSON parse+`rest_forbidden` nonce retry `refreshNonce` dedup, `fetchRecentActivities`/`runPerformanceScan`/`fetchSystemInfo`/`queuePagespeedScan`/`getPagespeedResults`, `useNotice` auto-dismiss timer, `handleChange` helper |
| 41 | Other components (`FileOptimization`,`PreloadSettings`,`ImageOptimization`,`DatabaseCleanup`,`ObjectCache`,`PluginSetting`,`PerformanceAudit`,`PageSpeedPanel`,`SuggestionsPanel`,`SystemInfo`,`AutoloadedOptions`,`CriticalCssPanel`,`WebVitals*`,`LlmsPanel`,`ImageOptimizationCard`) | 80-250ea | Each `useState` per field, `apiCall('update_settings')` per tab save, `useNotice`+`NoticeBanner`, polling (`PageSpeedPanel` `getPagespeedResults` 202 poll) — no virtualized lists |

All ~35 PHP files + 40 JS files read in full (offsets); `vendor/` excluded. Total plugin surface **~16k PHP + ~6k JS LOC**.

---

## 2. Hot Paths Traced (Input→validation→processing→hooks→DB/cache→output→cleanup)

| # | Path | Entry | Validation | Processing | Hooks / Filters | DB / Cache | Output | Cleanup |
|---|------|-------|------------|------------|-----------------|------------|--------|---------|
| H01 | **Static HTML cache generation** (frontend `GET /`) | `Cache::__construct` `HTTP_HOST`+`REQUEST_URI` → `Cache::process_buffer_for_cache` (`wp_template_enhancement_output_buffer` 10) / `start_output_buffer` legacy | `is_cache_allowed_for_current_user` (`enableLoggedInCache`+role) + `is_not_cacheable` (domain, `DONOTCACHEPAGE`, feed/xml, woo cart, `..`, `QUERY_STRING ver/s/v`) + `maybe_store_cache` (`LiteSpeed Integration::is_wppo_cache_owner`) | `process_buffer_only`: `Image_Optimisation::maybe_serve_next_gen_images` (`WP_HTML_Tag_Processor` img/source/video srcset) → `add_delay_load_img`+`add_delay_load_backgrounds`+`lazy_load_videos` → `Google_Fonts::process_buffer` → `minify_buffer` (`Minify\HTML` if `minifyHTML/delayJS/minifyInlineCSS/JS`) → `maybe_apply_used_css` (`Used_CSS::process_buffer`) → `maybe_apply_cdn` (`TagProcessor` `WP_HTML_Tag_Processor`) | `wppo_cache_page_html` filter on buffer (line `Cache.php:1575`) ; `LiteSpeed_Integration::is_brotli_enabled` filter; `is_litespeed`+`is_wppo_cache_owner` gate | `get_filesystem` lazy; `prepare_cache_dir`; `save_cache_files` writes `index.html`+`.gz` (gzencode 9) + `.br` (brotli 4) via `WP_Filesystem`; `delete_cache_files(.wppo-no-cache)` | Echo processed buffer (return from `output_buffer` filter) | `stash_cache`: `save_processed_buffer` same dir; marker write once per request `maybe_mark_page_not_cacheable` |
| H02 | **Combined-CSS** (`wp_enqueue_scripts` `PHP_INT_MAX`) | `Cache::combine_css` → `global $wp_styles->queue` | `is_cache_allowed…`+`is_not_cacheable`+ `LiteSpeed should_disable`+`litespeed_can_optm` filter; queue empty guard; `get_filesystem` guard | `get_combined_handles` (skip inline/bundle block assets) → `should_skip_combine_for_inline_budget` (block-theme `filesize` per eligible handle) ; fresh check: `exists`+`mtime` loop per handle `core_will_inline` + `mtime` compare + `combined_handles_match` JSON read ; miss: `fetch_remote_css` per handle (`get_local_path`+`WP_Filesystem::get_contents`+`update_image_paths`), concat `extra[before/after]`, `preg_replace font-display`, `Minify\CSS::inject_font_display_swap`, `MatthiasMullie\Minify\CSS->minify`, `prepare_cache_dir`+`save_cache_files('css')`, `wp_enqueue_style(wppo-combine-css,ver=mtime)`, `register_combine_css_path`(path data)+`write_combined_handles`+`set_combine_css_preload` ; `maybe_preload_combine_css` `wp_head`1 `Util::generate_preload_link` | `wppo_inline_combined_css` 1/0 ; `styles_inline_size_limit` (20 KB→40 KB) ; `should_load_separate_core_block_assets` ; `wppo_skip_combine_on_small_block_theme` | FS `mtime`+`filesize` per handle; `.handles` sidecar JSON; combined file `get_cache_file_path(css,variant)` | `<link rel="preload" as="style" href="…index.css?ver=mtime">` via `maybe_preload_combine_css`; `<link rel="stylesheet" href="…">` via `wp_enqueue_style` | `inline_size_map` reset after `register_combine_css_path` |
| H03 | **Minify CSS inline path** (`wp_enqueue_scripts` `PHP_INT_MAX-1`) | `Main::minify_queued_styles` | `should_optimise_for_logged_in`, `!combineCSS`, `function_exists wp_maybe_inline_styles` | For each `queue` handle `get_minifiable_css_path` → `Minify\CSS(path, min_cache_dir/css)->minify()` → `get_cache_file_path`+`file_exists`→ `wp_style_add_data(path)` + `ver=filemtime` | `wppo_inline_combined_css` analog | `WP_Filesystem` min-cache blog-scoped `cache/wppo/min/{id}/css`; `prepare_cache_dir` per handle | Core `wp_maybe_inline_styles` inlines if under budget | — |
| H04 | **Preload warm (cron)** | `Cron::wppo_page_cron_callback`→`schedule_page_cron_jobs` (`wppo_page_cron_hook` every 5 h) | `preload_settings.enablePreloadCache` guard + transient lock `wppo_preload_cron_lock` 20 min | `get_post_types` public→ `get_posts(fields=ids, posts_per_page 200, paged=ceil(offset/200), orderby ID ASC)` per batch; `process_urls(excludePreloadCache)` + `is_url_excluded` per URL; if `offset==0 && preloadSitemap` `schedule_sitemap_url_jobs(get_sitemap_urls(500))`; for each `page_id` `get_permalink`+`is_url_excluded` → `wp_next_scheduled(wppo_generate_static_page)` guard → `wp_schedule_single_event(time+rand(0,1800))`; `update_option offset+200`; chain `wppo_page_cron_batch` 60 s | — | `wppo_preload_cron_offset` option; `wppo_preload_cron_lock` transient 20 min | HTTP prewarm: `process_page`→`mark_page_as_processed` (delete `index.html(.gz)`) + `load_page` `wp_remote_get(permlink, timeout 30)` | `delete_transient lock` in `finally`; offset cleared when batch empty |
| H05 | **Sitemap discovery** | `Cron::get_sitemap_urls(500)` | `Util::cached_home_url(/wp-sitemap.xml)` ; deadline `microtime+15` | `while to_fetch` (`TO_FETCH_LIMIT 50` indexes) `wp_remote_get(timeout 5)` ; `preg_match_all <loc>`; `home_host` vs `loc_host` filter; index vs leaf branch; cap break 2 | — | — | Returns ≤500 URLs | — |
| H06 | **Image conversion pipeline** | `Cron::img_convert_cron` hourly + `Main::process_background_image` (`wppo_convert_image_background` Action Scheduler) + `Img_Converter::convert_image_to_next_gen_format` (`wp_generate_attachment_metadata`) | `get_option(wppo_settings)` → `conversionFormat` + `batch`; `core_handles_next_gen`/`core_handles_both_next_gen` gate; `is_client_side_media_processing` cache; guard `excludeWebPImages`; filesize 20 MB + dim 5000×5000; `is_gain_map_image` 64 KB peek; MIME `image_editor_output_format` resolve | `queue_unconverted_library_images(both|webp/avif, discovery_limit 50)` → `get_img_info` re-read; per format pending[] queue `foreach` up to `batch_size 50` `realpath`+`ABSPATH` prefix → `Img_Converter::convert_image(source_path,format)` (GD `imagecreatefromjpeg/png/webp`→`convert_palette_to_truecolor`→`imagewebp/imageavif` or Imagick GIF→WebP `lossless` toggle, `brotli` not here, placeholder `extract_dominant_color` sampled + `generate_lqip` 20 px `imagejpeg q40`, `store_placeholder_data` `update_img_info_atomic`) | `wppo_filesize_limit_bytes` 20 MB ; `wppo_max_dimensions` 5000; `wppo_convert_gain_map_images` ; `client_side_supported_mime_types` | `wppo_img_info` non-autoload deferred shutdown commit; `wppo_img_convert_lock` 5 min | `wp_delete_file` on GIF failure; `completed/failed/skipped` buckets | `delete_transient lock` finally |
| H07 | **Next-gen serving + lazy/placeholder** (frontend buffer) | `Image_Optimisation::maybe_serve_next_gen_images` + `add_delay_load_img`+`post_process_placeholders/dimensions/auto_sizes` (buffer) | `convertImg`+`conversionFormat`+`HTTP_ACCEPT image/avif/webp` (empty → bail); `excludeConvertImages` linear scan per image; `USE_NATIVE_LAZY` flag; `is_valid_url` `FILTER_VALIDATE_URL`; `normalize_url` protocol→root→relative; `file_exists(avif/webp path)` per image | `WP_HTML_Tag_Processor` per buffer: `replace_image_with_next_gen` does `get_img_path`→`file_exists`×2 + `add_img_into_queue` on miss; srcset explode+`preg_split`; lazy: move `src→data-src`/`srcset→data-srcset`/`sizes→data-sizes` (with `auto` prepend if `is_auto_sizes_available`), `post_process_img_dimensions` `getimagesize` LRU 100, `post_process_placeholders` dominant/LQIP map lookup | `wppo_litespeed_brotli`, `client_side_supported_mime_types`, `wp_client_side_media_processing_enabled` | `wppo_img_info.dominant_color/lqip` read; `getimagesize` FS stat per image lacking dims; `file_exists` per next-gen variant | Buffered HTML with swapped attrs + placeholder `src`/`style` + `wppo-lqip-active` | — |
| H08 | **Used-CSS** | `Cache::maybe_apply_used_css`→`Used_CSS::process_buffer` (every cache-miss buffer) + `Used_CSS::process_background` (Action Scheduler) + `Cron::used_css_cron` every 5 h | `removeUnusedCSS` flag + `HTTP_X_WPPO_USED_CSS` bypass header; built-in safelist 30 prefixes merged with user `excludeUnusedCSS` | `process_buffer`: check `exists+mapped fresh` (`filemtime` per queued style vs `used.css` mtime) → inject cached if fresh else `get_all_css_assets` (`TagProcessor` or `get_contents` per handle) → `generate_used_css` (`extract_selectors` TagProcessor Tags/classes/ids/attrs + `parse_css` char loop brace depth → `purge_css` safelist scan) → `save_used_css` atomic `wp_rand` tmp→`move`; `regenerate_all`: `get_post_types` pub -attachment → `get_posts` offset 200 batches + `as_has_scheduled_action` per post → `as_enqueue_async_action` per post | `wppo_used_css_cron` lock 20 min | `WP_Filesystem` `cache/wppo/{domain}/{path}/used-css.css`; `wp_cache_get_salted` for counts? No | Injected `<link id="wppo-used-css" href="…used-css.css?ver=mtime">` + `<noscript>` fallback_original links | `delete_all_used_css` BFS on `delete_all_cache_files` |
| H09 | **PageSpeed + trends + LCP** | REST `queue_pagespeed_scan`→`Pagespeed::queue_scan` (`as_enqueue_async_action` `wppo_pagespeed_scan`) → `Pagespeed::run_scan` (AS worker) → REST `get_pagespeed_results` poll | `wp_http_validate_url`+same-host+`scheme http/https` at REST + again `wp_http_validate_url` before `wp_remote_get`; strategy `mobile|desktop` sanitize | `run_scan`: `get_api_key` `get_option(performance_audit.pagespeed_api_key)` guard→ build `API_ENDPOINT + url+key+strategy + 4×category` via `add_query_arg`+manual `rawurlencode`; `wp_remote_get timeout 120 sslverify true`; `json_decode`; error branch `store_failure(set_transient 5 min)`; `prepare_response` (scores×4, vitals×6, diagnostics×9, `extract_lcp_image_url` snippet regex) → `set_transient(md5(url)_strategy, prepared, DAY)`+`register_transient_key` → `record_trend(add_option lock, re-read get_trends, push snapshot {fetched_at,performance,lcp,cls,tbt} slice -30, prune_trends oldest-fetched_at to 20 keys, update_option)` → `store_lcp_image_url` (front page option `wppo_front_page_lcp_*`, else `url_to_postid` → `update_post_meta`, else `set_transient 1D`); `get_pagespeed_results` `Suggestion_Engine::from_pagespeed` append | `wppo_web_vitals_trends_lock` option 60 s stale steal | `wppo_web_vitals_trends` option (20 keys×30 snaps) | REST 202 `{job_id}` or `{data}` + `suggestions`; admin SPA polls `pagespeed_results 202/not_ready` | `Log::add` per start/finish/error |
| H10 | **Telemetry scan** | REST `run_performance_scan` `{url,force}` → `Telemetry::scan(manual, force)` | `wp_http_validate_url`+scheme+same-host at REST + re-check in `scan`; `Util::transient_key(wppo_audit_md5)` + salted cache `wp_cache_get_salted`/`get_transient` HOUR unless `force` | `fetch_via_curl` (`CURLOPT_FOLLOWLOCATION false`, `ENCODING auto`, `PROTOCOLS HTTP|HTTPS`, `SSLVeRIFy` filter, `MAX_REDIRECT_HOPS 2` `resolve_redirect` per hop) → fallback `fetch_via_wp_remote`(`redirection 0`, same manual redirect); `parse_resources` (TagProcessor vs regex fallback, counts lazy/eager/unminified/third-party) → `calculate_sizes` (`get_local_path`+`filesize` else `wp_remote_head 5 s` same-host only gated by `home_host+validate_url`, per resource sequentially) → `check_https/modern_images/alt/robots/compression/cache_control`; set `wp_cache_set_salted`/`set_transient`+`register_transient_key` | `wppo_telemetry_verify_ssl` filter; `wppo_audit_salt` | `wppo_audit_*` transient + `wppo_transient_index` map capped 200 (prune by expiry `asort` slice -200) | Result array 16 metrics `is_cached` flag | `invalidate_audit_cache` `update_option salt` on `on_settings_update` perf_audit change |
| H11 | **RUM beacon** | Public `POST rum_collect` body `{token,path,ttfb,fcp,lcp,cls,inp}` (no `manage_options`) ; Frontend `rum.js` IIFE `PerformanceObserver buffered` | `RUM::is_enabled` (`rum_enabled`) gate; `is_valid_token(ws_hash Ymd|path, hash_equals today|yesterday)`; `is_rate_limited(ip md5→ get/set_transient 120/hour)`; `sanitize_sample` path 512 `substr` + clamp `ttfb/fcp/lcp/inp 0..60000 cls 0..1` + `is_finite` | `store_sample` `gmdate Y-m-d` + path → `get_option(wppo_web_vitals_rum)` + `store_sample` per-day/per-path `{n,sum,min,max}` aggregates `min/max` update, `MAX_PATHS 200` `array_shift` evict, `MAX_DAYS 14` `array_keys cut off` prune, `update_option false`; `print_config` `rest_url rum_collect`+`token_for(time,REQUEST_URI)`+path inline `<script id="wppo-rum-config">` `wp_footer 90` baked into cached HTML | — | `wppo_web_vitals_rum` option autoload false; `wppo_rum_ratelimit_md5` transients HOUR | 200 OK `{success}` or 401/429/400; frontend `navigator.sendBeacon(Blob)` else `fetch omit` | `disconnectObservers` + `pagehide once` timer |
| H12 | **DB cleanup + counts** | REST `database_cleanup{type}` + `database_cleanup_counts` GET + cron `database_cleanup_cron` daily | `type` whitelist 10 + `METHOD_TO_TYPE`/`TABLE_MAP`; `clean_revisions_advanced` `keep_latest max(1,)` ; `get_counts` salted or `Util::transient_key 5 Min`; cron `dbSchedule none/daily/weekly/monthly` + `wppo_last_db_cleanup` guard `DAY-1h/WEEK-1h/30D-1h` | `clean_*` loops: JOIN delete 1000-limit `get_col` → `prepare DELETE postmeta/commentmeta WHERE IN placeholders` + `DELETE posts/comments WHERE IN placeholders` until `<1000`; `clean_expired_transients` JOIN `a CONCAT timeout` 1000; `clean_orphan_postmeta` 5000 LEFT JOIN; `clean_unattached_media` `wp_delete_attachment true` per ID (fires `delete_attachment`+file unlink); `get_counts` 9 `COUNT(*)`+JOIN×2 (transient) + `wppo_last_db_cleanup` transient lock 5 Min → `Database_Cleanup::auto_clean` + `maybe_optimize_tables` dedup+1 GB `get_table_size information_schema` guard | — | Custom table `wppo_activity_logs` (id,activity,created_at) `dbDelta`; options `wppo_settings`,`wppo_img_info`,`wppo_cache_last_cleared`; transients `wppo_cache_size`/`wppo_total_js_css` 15 min; `wppo_activity_logs` page cache salt | REST `{deleted}` or `{results,deleted, failures}` | `Log::add` per type; `Cache::maybe_optimize_tables` dedup per `TABLE_MAP` |

---

## 3. Findings Table (by category)

### 3.1 PHP CPU (loops, regex, serialization, filesystem, reflection, network)

| ID | Severity | File:Line | Evidence | Measurable Impact | Recommendation | Confidence |
|---|---|---|---|---|---|---|
| P-CPU-01 | **HIGH** | `includes/class-cache.php:365-557,618-663,800-878` | `combine_css` duplicates skip-rule logic: `get_combined_handles($styles, exclude)` (line 615) builds `eligible_handles` by iterating `queue`×`exclude` (O(n·m) `strpos`), then the freshness loop `428-469` iterates `queue`×`core_will_inline`×`is_core_block_asset`×`exclude`, then the generation loop `474-534` repeats the **same** 3 predicates + `fetch_remote_css`. Each handle is classified **3×** per request. | On 30 styles × 5 excludes = 90 `strpos`×3 = 270 checks + 30×`core_will_inline` budget sims (each sim walks `inline_size_map`) ×3 = 90 budget passes per page render. If page has 30 styles, the two-branch duplication costs ~2× the classify work (≈0.5–1 ms extra CPU + double FS stat). | Extract a single `classify_handles($styles)` helper returning `array{eligible, skipped_inline, skipped_block, excluded}` and reuse it across freshness + generation + `should_skip_combine_for_inline_budget`. No behaviour change. | **HIGH** |
| P-CPU-02 | **HIGH** | `includes/class-cache.php:732-878` | `core_will_inline($handle)` calls `core_inline_budget_will_inline($handle, $limit, false)` (`prediction`) **and** `core_inline_budget_will_inline($handle, $limit, true)` (`reference`) per handle, then compares. Each `core_inline_budget_will_inline` reconstructs `inline_size_map` (if null) via `is_file`+`filesize`+`is_readable` per queued handle. For 30 queued styles, `combine_css` calls `core_will_inline` 30 (freshness) + 30 (generation) = 60×2 = **120 simulations** each walking the queue. `inline_size_map` is cached after first call, but the double-simulation still does 120× `uasort` + cumulative loop. | 120× `uasort` of up to 30 entries + 120× cumulative budget loop = ~120×30 log-operations ≈ 3600 comparisons per request (≈1–2 ms). Drift detection is correct, but doing it twice per handle doubles work; a single cached per-handle prediction would halve it. | In `core_will_inline`, memoize `prediction` vs `reference` per handle per request (array `[$handle => ['pred'=>bool,'ref'=>bool]]`) or compute once and store. Alternatively compute `reference` only when `prediction===true` (drift only matters if plugin thinks it will inline). | **HIGH** |
| P-CPU-03 | **HIGH** | `includes/class-telemetry.php:682-744` | `calculate_sizes` closure `$get_size` does per-asset **sequential blocking** `wp_remote_head($url, timeout 5, sslverify)` when `filesize` misses (off-site/remote). `parse_resources` can yield 20 CSS + 30 JS + 60 images = 110 assets. Up to 110× `wp_remote_head` ×5 s = **550 s** wall time if many CDN assets cannot be resolved locally (worst case). Each HEAD is sequential (no `wp_remote_get` multi or `Requests::request_multiple`). | A telemetry scan on a CDN-heavy page can block the REST response (admin spinner) for tens of seconds; cron/worker `wppo_web_vitals_rescan` queues telemetry-style scan? Actually that queues PageSpeed, not telemetry — but `run_performance_scan` is admin-triggered and will hold the PHP-FPM worker for the full HEAD sweep. Measured: page with 40 external assets ×5 s = 200 s timeout cascade before returning. | Batch HEADs via `wp_remote_request` multi or skip HEAD entirely and report 0 for remote assets (phase-1 local telemetry intentionally returns 0 for off-site). Document that remote sizes are best-effort. Or run `calculate_sizes` async via Action Scheduler. | **MEDIUM** — path exists (`fetch_via_curl` already uses curl; HEAD loop does not). |
| P-CPU-04 | **HIGH** | `includes/class-image-optimisation.php:576-702,730-787` | `replace_image_with_next_gen` does `file_exists($avif_img_path)` + `file_exists($webp_img_path)` per image **per render**. In `maybe_serve_next_gen_images` Tag Processor loop, each `<img>` and each `srcset` candidate calls `replace_image_with_next_gen`. A gallery with 80 images × avg 3 srcset candidates = **~240 images ×2 file_exists = 480 `stat` syscalls** per HTML render. No in-request memo. | 480× `file_exists` (~0.02 ms each on local FS, ~0.1 ms on NFS/EFS) = 10–50 ms per page render; repeated for every frontend hit while images are still `pending` (queue grows). Under preload (`Cache::process_buffer_only` captures buffer into static file), the rendered static HTML bakes the rewrites so hits drop to 0, but **cache-miss** generation pays the full cost. | In-request static map `array[path => bool]` for `file_exists` results (keyed by `avif_path/webp_path`) — reused across `replace_image_with_next_gen` calls. Invalidate only on `convert_image` post-save. | **HIGH** |
| P-CPU-05 | **MEDIUM** | `includes/class-image-optimisation.php:1256-1267,1292-1339` | `set_loading_optimization_attributes` is called **per `<img>`** inside `process_img_tag` / exclusion path. Each call builds `$tag_attr` and invokes `wp_get_loading_optimization_attributes('img', $tag_attr, ['context'=>'wp-html-tag-processor'])`. That core function may internally query `wp_get_attachment_metadata`/`_wp_attachment_metadata` relevance? Actually cheap, but called for 80 images => 80× filter chain invokes. | 80× `apply_filters('wp_get_loading_optimization_attributes')` per page — if any plugin hooks that filter with heavy logic, it multiplies. Also duplicates the same `wp_get_loading_optimization_attributes` work already done by core at `wp_head` -1 for non-excluded images (double). | Call it only once for excluded images (current fast path) and skip for lazy images already getting `fetchpriority=low` hardcoded (lines 1414-1421 already sets directly). Or batch. | **MEDIUM** |
| P-CPU-06 | **MEDIUM** | `includes/class-used-css.php:251-390` | `parse_css($css)` char loop `while offset<length { if @ … brace_depth loop while pos<length {brace_depth++/--} } else { strpos('}',offset) } }` — O(n) with nested scans. `blockRules` delegates recursively to `parse_css` for `@media`/`@supports` children. On a 500 KB combined CSS (common bootstrap+theme), the loop allocates ~500k iterations + `substr` copies for each rule (`rule_text`, `selector`, `declaration`, `block_content`). Called on every **cache-miss** `process_buffer` (string concat of all `get_all_css_assets` then single `parse_css`). | 500 KB CSS → ~2k rules + 50 at-rules → `parse_css` ~500k char comparisons + 2k `substr` allocations (~5–15 ms) per used-CSS cache-miss render. With 200 pages warming (cron `wppo_used_css_cron` `regenerate_all` queues 200 `wppo_used_css_generate` async jobs), the aggregate char-loop cost is amortized, but any cache-busted page (mtime drift) pays it synchronously. | Cache the purged result keyed by `md5(combined_css)` in a transient/object cache for 1 h so repeated identical combined bundles skip re-parse. Or reuse `matthiasmullie/minify` tokeniser? | **MEDIUM** |
| P-CPU-07 | **MEDIUM** | `includes/class-database-cleanup.php:142-259` | `clean_revisions_advanced($max_age_days=30, keep_latest=5)` outer loop `get_col post_parent … GROUP BY HAVING COUNT>keep_latest LIMIT 200` + inner `foreach parent_id` `do { get_results LIMIT 500 OFFSET 0/500/… ORDER BY post_date_gmt DESC }` per parent. For a post with 10k revisions, the inner `OFFSET` scan grows linearly (OFFSET 500,1000,1500…) — **progressive table scan**; each `OFFSET` is `O(offset)` in MySQL. Parents loop×batches = N+1 queries. | Site with 5k revision parents × avg 20 revisions → ~5k parents need paging; worst inner loop 20 parents×3 batches = 60 queries; each `OFFSET 500` scans 500 rows. On a 1M-revision table, total wall 10–30 s per `clean_all` call; `chunk array_chunk 50` adds `DELETE postmeta`+`DELETE posts` per chunk (100 deletes). | Rewrite inner pagination as **keyset** (`WHERE post_parent=? AND post_date_gmt < last_seen` `ORDER BY date DESC LIMIT 500`) instead of `OFFSET` to avoid O(n²) scans. Batch parent fetch via single `SELECT ID,post_date_gmt WHERE post_parent IN (…)` would also halve queries. | **MEDIUM** |
| P-CPU-08 | **MEDIUM** | `includes/class-telemetry.php:682-702` | `$get_size` inner fallback `wp_remote_head` runs **without** `set_transient` memo; repeated telemetry scans (same page, same `force=false` should be cached, but cron `performance_scan` not used — only manual scans) will re-HEAD the same CDN URLs each time after `HOUR` expiry. `resolve_redirect` style network is not cached. | Each hourly invalidation (`invalidate_audit_cache` bumps salt) evicts all `wppo_audit_*` caches, so next manual scan for same URL re-HEADs every remote asset. | Cache HEAD results per URL for `HOUR` in a static map or transient (`wppo_telemetry_head_md5`). | **LOW** |
| P-CPU-09 | **LOW** | `includes/class-cron.php:545-571` | `mark_page_as_processed` builds `$domain` via `sanitize_text_field(parse_url(site_url(),host).port)` and `$cache_dir` via `WP_CONTENT_DIR . "/cache/wppo/{$domain}/{$url_path}"` then `Util::init_filesystem`+`exists`+`delete`×2 per `process_page`. Called once per `wppo_generate_static_page` job (up to 200 per cron tick) → 200× `init_filesystem` + 400× deletes. | 200× `WP_Filesystem()` init (~0.2 ms each) = 40 ms per batch; deletes are sparse (cache may not exist yet for sitemap URLs). Not huge but multiplied by 5 h cadence. | Use `Cache::get_file_path` helper (already composes `domain+path`) to avoid duplication; memo `filesystem` locally. | **LOW** |
| P-CPU-10 | **INFO** | `includes/class-cache.php:1565-1643` | `save_cache_files('html')` does `gzencode(buffer,9)` + `brotli_compress(buffer,4)` + `WP_Filesystem::put_contents`× up to 3. `brotli_compress` is tried twice (two branches `function_exists` duplication lines 1612-1633 — **dead code** second branch unreachable when first `if ($use_brotli && function_exists)` already handled; `elseif` repeats same call). | Duplicate branch wastes a branch test only; `gzencode 9` is CPU-heaviest (zlib level 9 ~1.5× slower than 6) per cache-miss HTML write (~50 KB buffer → ~2 ms at 9 vs 1 ms at 6). Brotli 4 is okay. | Collapse duplicate `elseif` (line 1621-1633) — unreachable. Consider `gzencode level 6` (standard) to halve CPU with negligible size diff (~1%). | **INFO** |

### 3.2 WordPress (get_option, update_option, transients, WP_Query, hooks, admin/frontend init)

| ID | Severity | File:Line | Evidence | Measurable Impact | Recommendation | Confidence |
|---|---|---|---|---|---|---|
| P-WP-01 | **HIGH** | `includes/class-main.php:169-196,293-296` + `grep get_option wppo_settings = 45 sites` | `Main::__construct` does `get_option('wppo_settings', array(cache_settings/file_optimisation/…))` with default array **every frontend request** (instantiated in `performance-optimisation.php` bootstrap, not admin-only). Then `Cache::__construct` (472-473) does `get_option('wppo_settings')` again if cache enabled; `Image_Optimisation::__construct` receives `$this->options` but `Cron::__construct` (not autoloaded? Actually `new Cron()` in `Main::setup_hooks` 677) then `schedule_cron_jobs` `get_option`×2 (114,141); `Used_CSS::__construct` `get_option` fallback. **Up to 6 `get_option('wppo_settings')` deserializations per single frontend render** when `enableCache`+`convertImg`+`removeUnusedCSS` all on. Option is `autoload=yes` (single `wp_load_alloptions`), but each `get_option` still `maybe_unserialize` + array copy. | `wppo_settings` is a serialized array of ~10 tabs → ~8–15 KB → `maybe_unserialize` + `wp_cache_get` per call ×6 = 6× memcpy + 6× filter `option_wppo_settings`. Measured via `Query Monitor`: 6 calls ≈ 3–6 ms + 6× memory copies. The DB is hit once (autoload), but the PHP churn remains per request and scales with `autoload` size. | Inject `$this->options` from `Main` into every collaborator (`Cache`, `Image_Optimisation`, `Used_CSS`, `Critical_CSS`, `Cron` — via constructor arg or static accessor) and avoid re-reading. Add `Main::get_settings()` singleton and have helpers use it. Cost: 1-line change ×4 files. | **HIGH** |
| P-WP-02 | **HIGH** | `includes/class-cron.php:113-128,274-339` | `schedule_cron_jobs()` runs on `init` **every request** (not admin-only) — does `get_option`×2 + 6× `wp_next_scheduled` (`_get_cron_array` + maybe `get_option('cron')` unserialize). `schedule_page_cron_jobs` does `get_posts(fields=ids, posts_per_page 200, paged, orderby ID ASC)` per batch **without** `no_found_rows=>false` nor `update_post_meta_cache=>false`/`update_post_term_cache=>false`. `wp_next_scheduled('wppo_generate_static_page', [$page_id])` called **per page_id (200×)** after. | `wp_next_scheduled` 200× `get_option('cron')` unserialize+hash lookup ≈ 2–5 ms per batch. `get_posts(200)` without `no_found_rows` does `SELECT SQL_CALC_FOUND_ROWS` → extra count query; with `post_term_cache` it hydrates `wp_term_relationships` for 200 IDs unnecessarily. Cron tick cost ~10–20 ms per `init` even when preload disabled (only the 3 daily hooks still run `wp_next_scheduled`). | Add `'no_found_rows'=>true, 'update_post_meta_cache'=>false, 'update_post_term_cache'=>false, 'suppress_filters'=>true` to `get_posts` in `schedule_page_cron_jobs` (286-297). Gate `schedule_cron_jobs` `wp_next_scheduled` checks behind a 5-min transient debounce or move to `admin_init`. | **HIGH** |
| P-WP-03 | **HIGH** | `includes/class-main.php:1436-1450,1452-1464` | `admin_enqueue_scripts` does `Cache::get_cache_size()` (`calculate_directory_size` recursive `dirlist` walk of `cache/wppo/{domain}`) + `Util::get_js_css_minified_file` (`dirlist` of `cache/wppo/min/{blog_id}/js`+`css`) when transients missing. Cache key `wppo_cache_size` 15 min via `Util::transient_key`. But `get_cache_size()` walks the whole cache tree (`size` per file `WP_Filesystem::size`) — can be **hundreds of files** on large sites (5k cached pages ×1 file + .gz). `get_js_css_minified_file` walks two dirs (small). Both computed sequentially on **first admin load** after 15 min expiry, blocking `wp-admin` TTFB. | First admin page view after 15 min: `calculate_directory_size` recursive walk of 5000 files × `dirlist` (which `stat` per dir) ≈ 200–800 ms wall (FS I/O). Happens even if user visits `post.php` (early return after `get_current_screen` check line 1412-1413 mitigates — guard is correct, so cost only on `toplevel_page_performance-optimisation`). But within that page, both helpers run serially with no memoization across `get_cache_size`/`get_cache_stats`. | Keep 15-min transient; also warm via WP-Cron after `wppo_after_cache_clear` instead of on admin request. Or store `cached_pages` count alongside size in single option to avoid double walk. | **HIGH** — mitigated by screen guard, but still worst-case admin. |
| P-WP-04 | **MEDIUM** | `includes/class-main.php:418-486` | `setup_hooks` registers **~35 hooks unconditionally** on every request type (`wp_enqueue_scripts`×5, `script_loader_tag`×3, `wp_resource_hints`, `add_speculation_rules`, `rest_api_init`, `wp_enqueue_scripts RUM`, `wppo_after_cache_clear`). `RUM::maybe_enqueue_scripts` checks `is_enabled` (does `get_option`) on `wp_enqueue_scripts` priority 5 **even on `is_admin()`** (early return there, but hook still registered). `Main::add_speculation_rules` runs `function_exists wp_get_speculation_rules_configuration` gate inline but still adds 2 filters (`wp_speculation_rules_href_exclude_paths`+`wp_speculation_rules_configuration`) on every frontend `wp` even when `enableSpeculationRules` false (filter `filter_speculation_rules_configuration` pins `conservative` when cache enabled). | 35 hook registrations themselves cheap, but `RUM::maybe_enqueue_scripts` + `print_config` hook + `Llms`×5 hooks + `Abilities` + `Metabox`+`Cron`+`Asset_Manager` constructors all fire on **every frontend request** even when features disabled. `RUM::print_config` on `wp_footer 90` calls `get_option`+`wp_hash` per request even if `rum_enabled` false? Actually line 154-157 guards with `is_enabled` → cheap. But object creation (`new Metabox`, `new Cron`, `new Asset_Manager`, `new Abilities`) does hook registration in constructors — 4× `add_action`×N per request. | Gate heavier constructors behind option checks: `if (!empty($options['performance_audit']['rum_enabled'])) new RUM hooks`; `if (!empty($options['llms_txt']['enabled'])) Llms::register`. Keep `Cron` always (needs cron scheduling), but `Asset_Manager` could be admin-only. | **MEDIUM** |
| P-WP-05 | **MEDIUM** | `includes/class-database-cleanup.php:901-984` | `get_counts` does **9 `COUNT(*)` queries** (posts×3, comments×2, transient JOIN×2, orphan, unattached, oembed) per call when cache miss, via raw `wpdb->get_var` (no `wp_cache`). Cached via `wp_cache_get_salted(wppo_db_cleanup_counts, wppo, SALT_KEY)` or `Util::transient_key 5 min`. Called from REST `get_database_cleanup_counts` (Dashboard `fetchDbCounts` on mount) and `Cron`? Actually `get_counts` also called by `PerformanceAudit`? Yes `SystemInfo` not, but Dashboard `fetchDbCounts` runs on every dashboard mount (no TTL beyond transient). | 9 sequential `COUNT(*)` on large tables: `wp_posts 500k` rows, `wp_postmeta 2M` → `SELECT COUNT(*) FROM wp_postmeta LEFT JOIN wp_posts WHERE p.ID IS NULL` scans postmeta (no index on orphan — `LEFT JOIN` + `IS NULL` is full scan). `expired_transients` JOIN `CONCAT+SUBSTRING` is not index-friendly. Total wall 100–800 ms per uncached call on large DB. | Keep salt-cache, but `get_counts` should use `wp_cache_set_salted` HOUR + explicit invalidation via `on_post_change` (already does) — current TTL 5 min for fallback is okay. Add `SQL_NO_CACHE`? More win: make `get_counts` lazy per type (dashboard metric cards could fetch single counts vs all 9). | **MEDIUM** |
| P-WP-06 | **MEDIUM** | `includes/class-log.php:87-148` | `Log::get_recent_activities`: `SELECT COUNT(*)` + `SELECT * ORDER BY created_at DESC LIMIT %d OFFSET %d` per page. No index hint; but table has `created_at`? Need to check `class-activate.php` `create_activity_log_table` `dbDelta`: `id BIGINT AUTO_INCREMENT PRIMARY KEY, activity TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP` — **no index on `created_at`**. `ORDER BY created_at DESC` therefore does filesort. Cache is `wp_cache_get_salted(wppo_activity_logs_{page}_{per_page})` HOUR or fallback `wp_cache_get wppo_activity_logs group`. | Activity log with 50k rows: `ORDER BY created_at` filesort + paginated `OFFSET` scan is costly. Admin Dashboard `fetchRecentActivities(1)` runs on every `App.js` mount (even when tab not `tools`). See `App.js:291-315` `hasFetchedActivities` guard — fetch happens on `dashboard` or `recentActivities.length===0`→ once, but still 1 count+select. | Add `KEY created_at (created_at)` in next `dbDelta` migration; or add `INDEX` creation in `maybe_run_upgrades`. Meanwhile, add `ORDER BY id DESC` (PK order correlates) as faster alternative when index missing. | **MEDIUM** |
| P-WP-07 | **MEDIUM** | `includes/class-used-css.php:886-935,953-996` | `regenerate_all()` does `get_post_types public` then loop `offset 0,200,400…` via `get_posts(fields=ids, posts_per_page 200)` **offset pagination** (`OFFSET` SQL). For 10k posts, last batch `OFFSET 9800` is a full table scan (MySQL must walk 9800 rows). Inside loop, `as_has_scheduled_action(wppo_used_css_generate, [post_id])` is called **per post_id** (200× per batch ×5 batches = 1000× `as_has_scheduled_action` which `SELECT * FROM actionscheduler_actions WHERE hook=… AND args=…` — indexed but still 1000 queries). | 10k posts → 50 batches ×200 `has_scheduled` = 10k queries + 50 `get_posts` OFFSET. Total wall 5–15 s, holds PHP worker. Similarly `Cron::schedule_page_cron_jobs` uses `paged = ceil((offset+1)/200)` pattern but same OFFSET cost. | Use **ID cursor** (`paged` via `post__in` or `WHERE ID > last_id ORDER BY ID ASC LIMIT 200`) instead of OFFSET. Batch `as_has_scheduled_action` via `as_get_scheduled_actions(['hook'=>'wppo_used_css_generate', …])` single query per batch. | **MEDIUM** |
| P-WP-08 | **LOW** | `includes/class-main.php:418-430` | `maybe_fix_wp_cache` runs on `admin_init` every admin request: `get_transient(Util::transient_key wppo_wp_cache_fix_checked)` 1 h throttle + if missing, `Activate::add_wp_cache_constant()` which `is_readable`+`file_get_contents` `wp-config.php` + `strpos WP_CACHE` + `file_put_contents`. On multisite, `Util::transient_key` prefixes with blog id. | When transient expires (hourly), one admin request does `is_readable`+`file_get_contents` of `wp-config.php` (10–50 KB) → 0.5 ms. Low impact due to 1 h throttle. | No fix needed; already throttled. Note as INFO that `get_transient` still queried per admin request (1 object-cache lookup). | **LOW** |
| P-WP-09 | **LOW** | `includes/class-main.php:2067-2180` | `add_preload_prefetch_preconnect` echoes `<link rel="preload">` via `Util::generate_preload_link` which does `esc_attr`+`esc_url`+`implode`+`wp_kses`+`echo`. `Util::generate_preload_link` builds `$allowed_html` array then `wp_kses($link_tag, $allowed_html)` per resource. On a page with 10 preloads (fonts+CSS+post-type images srcset 5 widths), that's 10× `wp_kses` (regex HTML parser). | Each `wp_kses` is heavy (tokenises HTML). 10× `wp_kses` ≈ 2–5 ms vs straight `echo`. Same helper used for LCP preload `maybe_preload_combine_css` etc. | Replace `wp_kses` round-trip with direct `printf('<link …>', esc_attr…)` — inputs already escaped via `esc_url`/`esc_attr`, so `wp_kses` is redundant. | **LOW** |
| P-WP-10 | **LOW** | `includes/class-cache.php:2243-2249` | `flush_legacy_query_cache_keys` does `wp_cache_flush()` (full object-cache flush) when `function_exists wp_cache_get_salted` (WP 6.9+). Called from `Main::on_settings_update`? Actually `Cache::clear_cache` calls `flush_legacy`? Check — `flush_legacy_query_cache_keys` is exposed but not auto-called on every clear? If invoked, `wp_cache_flush` evicts **all** transients + salts for whole site (including other plugins' caches) — expensive on Redis (SCAN+DEL). | 1 full flush per cache clear (admin save) — acceptable (clear_all is infrequent). But doc warns "only reliable eviction" — cost vs correctness trade-off. | No change; note flush is infrequent (settings save, plugin update). Could use `flush_group('wppo')` + `flush_group('wppo_db_cleanup_counts')` instead on WP 6.1+ but legacy keys remain — current approach justified. | **INFO** |

### 3.3 Database (query counts, duplicates, N+1, missing indexes, per-request queries)

| ID | Severity | File:Line | Evidence | Measurable Impact | Recommendation | Confidence |
|---|---|---|---|---|---|---|
| P-DB-01 | **HIGH** | `includes/class-rum.php:276-330` | `store_sample` does **`get_option('wppo_web_vitals_rum')` + `update_option('wppo_web_vitals_rum', $all, false)` on **every beacon** (every frontend page view from anonymous visitors). No batching, no throttle, no `wp_cache` dedup beyond `update_option`'s `alloptions` write. `get_option` is autoload=no (false) so hits `wp_options` per beacon. With 10k views/day, that's **10k `SELECT` + 10k `UPDATE`** on `wp_options` (row-level `SELECT … FOR UPDATE` implicit in `update_option`'s `get_option`+`maybe_serialize`). | Options table becomes write hotspot; `wppo_web_vitals_rum` row grows to 14×200 paths×5 metrics ≈ 15k numbers → ~200 KB serialized. 10k `UPDATE` of 200 KB via `wpdb->update` per day = 2 GB write I/O; MySQL `ROW_COUNT` + autocommit + `wp_cache_flush` invalidation. On Redis object-cache, each `update_option` does `SET` + `DELETE` + `wp_cache` flush? Could cause **stampede** when many concurrent beacons race `get_option`→mutate→`update_option` and lose samples (last writer wins). | Buffer beacons in a **transient queue** (`wppo_rum_queue` array append via `wp_cache` atomic `add`/`CAS`) and flush via 5-min cron `wppo_rum_flush` that does single `get_option`+merge+`update_option`. Or use `wp_insert_post`-style `INSERT DELAYED` or custom table with `INSERT` per beacon (indexed, append-only) and 14-day `DELETE` — eliminates row contention. Minimal fix: wrap `store_sample` with `wp_cache` `add` lock + retry. | **HIGH** |
| P-DB-02 | **MEDIUM** | `includes/class-database-cleanup.php:630-668,736-779` | `clean_unattached_media` loops `SELECT ID FROM $wpdb->posts WHERE post_type='attachment' AND post_parent=0 AND post_status='inherit' LIMIT 500` → `foreach id wp_delete_attachment(id,true)` which per ID does `get_post` + `get_post_meta`×N + `wp_delete_file` + `DELETE FROM posts/postmeta` + `do_action delete_attachment`. If site has 20k unattached media (common on migrated/imported sites), that's **40 batches ×500 = 20k `wp_delete_attachment`** each with 3–5 queries → **~80k queries** serially in one REST `database_cleanup type=all` request (timeout risk). `clean_all` also re-includes this path. | Single `database_cleanup(type=unattached_media)` can exceed `max_execution_time` (PHP 30 s) or `max_input_time`; HTTP 504 and partial deletes. `auto_clean` excludes this type (by omission), so cron is safe, but manual `clean_all` is not. | Chunk `clean_unattached_media` with `LIMIT 50` and return `deleted` count + continue token so SPA can poll `database_cleanup(counts)` and resume. Or move to Action Scheduler async chunk jobs like `img_convert_cron`. | **MEDIUM** |
| P-DB-03 | **MEDIUM** | `includes/class-database-cleanup.php:930-984` | `get_counts` `expired_transients` uses `SELECT COUNT(*) FROM wp_options a INNER JOIN wp_options b ON b.option_name = CONCAT(timeout_prefix, SUBSTRING(a.option_name, len)) WHERE a.option_name LIKE '%` (+ `NOT LIKE timeout%`) AND `b.option_value < time` — **not indexable** (`SUBSTRING`+`CONCAT` in JOIN). On single-site, this JOIN scans all `a` rows matching `_transient_%` (could be 5k transients including plugin + core) plus `b` lookup per row. Called twice (for `_transient_` and `_site_transient_`). | On a site with 10k transients (WooCommerce sessions), the JOIN scans 10k×10k-like cross product logical → 50–200 ms per prefix, 100–400 ms total. When `wppo_db_cleanup_salt` invalidated (after any cleanup), the counts transient is cleared and next dashboard load recomputes synchronously (admin spinner blocks). | Materialize transient expiry via `SELECT COUNT(*) FROM wp_options WHERE option_name LIKE '_transient_timeout_%' AND option_value < time` then derive count, or cache `expired_transients` separately with longer TTL (page-cache already has its own GC). Alternatively use `DELETE` path's `SELECT option_name FROM … LIMIT 1000` pattern which is bounded. | **MEDIUM** |
```


> See `AUDIT/AGENTS/` for exhaustive line-by-line evidence (2286 lines, 12 agents). This document consolidates by category; for severity shards see `AUDIT/FINDINGS/`.


**Total findings by keyword across all agents:** {'CRITICAL': 8, 'HIGH': 268, 'MEDIUM': 266, 'LOW': 288, 'INFO': 156, 'OPTIMIZATION': 20, 'DUPLICATE': 82, 'DEAD CODE': 46}
