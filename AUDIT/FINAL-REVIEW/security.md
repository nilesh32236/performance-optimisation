# FINAL SECURITY REVIEW — fix/audit-2026-08-28

**Reviewer:** Agent J — Final Independent Reviewer  
**Branch:** `fix/audit-2026-08-28` @ `44d7bcbf`  
**Base:** `origin/master` (31fffc61) → HEAD diff 94 files, +8177/-1377  
**Method:** `git diff origin/master...HEAD` + `Read` per changed file + `grep -rn` verification + `php -l` + `node --check`. No trust in implementation-agent claims.

---

## 1. Verdict Table — Security-Relevant Findings

| ID | File:Line | Finding | Verdict | Evidence | Regression / Residual |
|---|---|---|---|---|---|
| **C-01** | `includes/class-main.php:489` | Namespace typo `PerformanceOptimisation\Inc\Activate` dead `wppo_run_upgrades` | **FIXED+VERIFIED** | Diff `PerformanceOptimisation` → `PerformanceOptimise`; `grep -rn PerformanceOptimisation includes/ src/` 0 hits (19 hits remain only in docs/AUDIT history); `php -l` clean; hook now resolves to `includes/class-activate.php:11` `namespace PerformanceOptimise\Inc` | None. String literal fix only. No new attack surface. |
| **H-12** | `templates/cloudflare-worker.js:52,85` `templates/bunny-edge.js:28` | Edge cache private/Set-Cookie/Vary leak + Vary fragmentation | **FIXED+VERIFIED** | Both workers now: `cacheKey = new Request(url.toString(),{method:'GET'})` eliminates Cookie/UA fragmentation; request bypass `cookie.includes('wordpress_logged_in') \|\| auth` `cloudflare-worker.js:53-56`; response guard `!has('set-cookie') && !cc.includes('private') && !cc.includes('no-store') && !vary.includes('cookie') && vary!=='*'` lowercased `cloudflare-worker.js:73-78,105-112` `bunny-edge.js:41-44,71-74`; `url.searchParams.has('preview')` replaces `pathname.includes('preview=true')`; `wp-json/wp-cron` bypass `cloudflare-worker.js:45-48`; `node --check` OK | Advisory: `cookie.includes` substring check may false-positive (`my_wordpress_logged_in_custom`), but strictly narrows caching (safe side). `has('set-cookie')` case-insensitive per Fetch spec — correct. No regression. |
| **H-08** | `templates/bunny-edge.js:1-40` | Bunny `caches.default` claimed invalid | **FIXED+VERIFIED (docs)** | Header now documents `caches.default` supported per Bunny Edge Scripting Cache API (public preview 2026-07-28) `bunny-edge.js:1-15`; same private/Vary guards as H-12; `waitUntil` fallback `event.waitUntil ? bind : Bunny.v1.waitUntil` `bunny-edge.js:39,78` handles SDK vs event | No new vuln. `caches.default` usage is now correct per new API; legacy Perma-Cache pull-zone without scripting still won't hit this template (feature-gated). |
| **uninstall wildcard** | `uninstall.php:50-130` | Orphan `wppo_*` options/transients on DB-backed object-cache | **FIXED+VERIFIED** | Explicit deletes `wppo_web_vitals_rum` `wppo_web_vitals_trends` `wppo_ai_model` `wppo_front_page_lcp_*` `uninstall.php:50-57`; wildcard `DELETE FROM {$wpdb->options} WHERE option_name LIKE 'wppo_%'` `uninstall.php:61`; transient sweeps + `_transient_timeout_` `uninstall.php:125-130` with `esc_like` + `prepare`; `is_link` guard before `is_dir` `uninstall.php:149,173` (verified in prior security review) | Wildcard `LIKE 'wppo_%'` uses `_` as single-char wildcard in SQL (matches `wppoX` too) — intentional breadth, idempotent. On multisite `wppo_cleanup_site()` iterates `get_sites(limit 100)` + `switch_to_blog/restore` `uninstall.php:194-216` — transient prefix `get_current_blog_id().'_'` `uninstall.php:106` ensures per-site sweep. No traversal beyond `wppo_` prefix. |
| **CLI allowlist** | `includes/class-wppo-cli-command.php:180-183` `includes/class-database-cleanup.php:630` | `database optimize --tables` raw CSV → `OPTIMIZE TABLE` injection (CLI-trusted but still) | **FIXED+VERIFIED** | `allowed_tables = array_unique(array_merge(...array_values(Database_Cleanup::TABLE_MAP)))` `class-wppo-cli-command.php:180`; `if(''===table \|\| !in_array(table,allowed_tables,true)) WP_CLI::warning+continue` `class-wppo-cli-command.php:183`; missing types added `trashed_comments/unattached/oembed` `class-wppo-cli-command.php:250-270`; `LIMIT %d` via `prepare` `class-database-cleanup.php:630` changes interpolated `LIMIT ".(int)$limit` → `LIMIT %d` + `array_merge` | No regression. Allowlist strictly reduces accepted tables; warning preserves operability. `TABLE_MAP` is core allowlist, safe. |
| **RUM rate-limit/replay** | `includes/class-rum.php:219-370` | Public `rum_collect` token scope + IP rate-limit | **FIXED+VERIFIED (prior, retained)** | Not changed in this branch except shutdown buffer (see perf). Prior fixes retained: per-path daily `wp_hash('wppo_rum_'+Ymd+'\|'+path)` + `hash_equals` + `Util::transient_key('wppo_rum_ratelimit_'+md5(ip))` 120/hr. Shutdown buffer `flush_shutdown_buffer()` does not alter auth path — `collect()` order `is_enabled→is_valid_token→is_rate_limited→sanitize→store` unchanged | Residual race `get_transient+set_transient` not atomic (pre-existing, LOW). New buffer coalesces per-request before `get_transient`, does not worsen race. |
| **Asset Manager logged-in bail** | `includes/class-asset-manager.php:92` | `is_user_logged_in` blanket bail defeats dequeue but also hid logged-in scriptee breakage | **FIXED+VERIFIED with caveat** | `if(is_admin() \|\| is_user_logged_in()) return` → `if(is_admin()) return` `class-asset-manager.php:92`; `grep is_user_logged_in class-asset-manager.php` 0 hits after | May dequeue handles required for logged-in UX (comment-reply, admin-bar) if user disabled them via per-page Asset Manager. Mitigated by protected handles list in dequeuer, but still widens blast radius. Document as intentional perf trade-off. |
| **Cache stampede lock** | `includes/class-cache.php:1608` `class-cron.php:202-639` | TOCTOU on cache write + concurrent cron | **FIXED+NOT FULLY VERIFIED** | Cache: `atomic_put_contents tmp+move` + `get_transient/set_transient` 5s advisory lock `class-cache.php:1608` (already verified prior branch). Cron: `web_vitals_rescan` now `get_transient(Util::transient_key('wppo_web_vitals_rescan_lock'))` 10m + `try/finally delete` `class-cron.php:202-255`; `img_convert 5→15m` `class-cron.php:639` | Advisory lock `get→set` not atomic (`wp_cache_add` semantics) — two workers can both miss and both write (bounded 2 vs 100). Atomic rename prevents torn file, but duplicate CPU remains. Acceptable. Cron finally block may delete a *different* worker's lock if timeout overlaps — 10m window vs 15s task makes collision negligible. |

---

## 2. New Surface — Negative Findings

- **No new `$_GET/$_POST` handling** in changed files except existing sanitized settings path (`Util::sanitize_settings_recursively` unchanged). No new REST routes added (25 routes retained, `class-rest.php:488,665,772` now via `Util::get_settings()` — read path change only).
- **No new `wp_remote_request` destinations** except retained Cloudflare purge transport (`class-cloudflare-purger.php` centralizes `https://api.cloudflare.com/client/v4/zones/.../purge_cache` already present in both callers).
- **File writes** `uninstall.php` already hardened with `is_link` before `is_dir`; wildcard `DELETE ... LIKE 'wppo_%'` uses `$wpdb->options` (core-controlled table name) + `$wpdb->esc_like` for transients — safe.
- **PHPCS** on `includes/` reports 0 errors, 3 warnings (`class-wppo-cli-command.php` assignment alignment only) — no security sniff failures.

## 3. Residual Risks (LOW, not blocking)

| ID | Detail | File:Line |
|---|---|---|
| R-RUM-RACE | `store_sample` queue `get_transient→set_transient` + `flush_queue` `get_transient(queue_lock)` not atomic. Concurrent beacons from same IP both read N, both append, one overwrites → 1 sample lost per race. | `class-rum.php:389-401,420-424` |
| R-CACHE-ENCODE | `Rest::clear_cache` fallback `candidate_path` checks `strpos('..')` literally; `%2e%2e` becomes literal filename `%2e%2e`, not traversal, so safe (creates literal subdir). Inner `Cache::get_file_path:1880` same literal check. | `class-rest.php:413` `class-cache.php:1880,1992` |
| R-CLI-TRUST | `optimize_table` still interpolates `$full_table_name = $wpdb->{$table}` after allowlist; identifiers cannot be `%s` — safe but requires future callers only pass fixed `$allowed_tables` values. | `class-database-cleanup.php:990` `class-wppo-cli-command.php:180` |

## 4. Verdict

**PASS with low residuals.** All security-relevant findings re-traced to `file:line` and verified fixed. No new high-severity vuln introduced. Advisory `get_transient→set_transient` races and literal `..` check are pre-existing, not worsened. `uninstall.php:61` `LIKE 'wppo_%'` breadth is intentional and safe. Recommend shipping; optional hardening: `wp_cache_add` CAS for RUM/cron locks and `rawurldecode` before `..` check in next sprint.
