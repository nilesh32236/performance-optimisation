# FINDINGS — LOW

_Consolidated from 12 agents (2286 lines). Each finding below is traceable to `AUDIT/AGENTS/agent-*.md` file:line evidence. This shard groups by severity LOW._

## Index

### agent-A01-php-correctness.md

- > Severities: `CRITICAL` > `HIGH` > `MEDIUM` > `LOW` > `INFO` > `OPTIMIZATION` > `DUPLICATE` > `DEAD CODE`
- | P01 | LOW | `performance-optimisation.php:7` | plugin header | `Tested up to:      7.1` — WP latest stable is 6.9 (Aug 2026); `7.1` does not exist. | Misleading WP.org readme parser & users; may suppress compatibility warnings. | Set to actual tested version (e.g. `6.9` or `6.8.*`) and automate bump in `tri-merge-cycle.yml`. | HIGH |
- | P03 | LOW | `performance-optimisation.php:41` | bootstrap | `require_once WPPO_PLUGIN_PATH . 'vendor/autoload.php';` without `file_exists()` guard. Fatal if `vendor/` missing (fresh git clone without `composer dev-setup`). | White-screen on activation before dependencies installed. | Guard: `if (file_exists(...)) require_once ...; else add admin notice and return`. | MEDIUM |
- | P05 | INFO | `performance-optimisation.php:46-57,59-70` | `wppo_activate` / `wppo_deactivate` | Guard `if ( ! function_exists('wppo_activate'))` — correct for duplicate inclusion, but `register_activation_hook(__FILE__, 'wppo_activate')` outside guard would fatal if guard prevented definition on second load. | Harmless currently (file loaded once), but pattern is fragile. | Keep guard + registration together, or remove guard (activation file included only once). | LOW |
- | U04 | LOW | `uninstall.php:75` | `wppo_cleanup_site()` | `strpos($content,'WPPO_ADVANCED_CACHE_DROPIN') || strpos($content,'is_user_logged_in_without_wp')` — substring match for legacy detection. | False-positive if foreign plugin legitimately contains helper `is_user_logged_in_without_wp`. | Prefer strict marker `WPPO_ADVANCED_CACHE_DROPIN` only for deletion; keep second substring as `INFO` fallback with comment. | LOW |
- | U05 | LOW | `uninstall.php:93` | `wppo_cleanup_site()` | `$transient_prefix = is_multisite() ? get_current_blog_id().'_':''` — transient prefix logic duplicates `Util::transient_key()` but not via that helper (uninstall cannot autoload `Util`). | Drift if `Util::transient_key()` changes format (e.g. adds network prefix). | Add comment referencing `Util::transient_key()` and keep manual duplication (unavoidable at uninstall). | HIGH |
- | U06 | LOW | `uninstall.php:114` | `wppo_delete_directory()` | `$items = scandir($dir)` with `@` ignored return; early `return` on `false` hides I/O errors. | Silent failure leaves leftover cache files; not security-critical. | Log via `error_log` on failure when `WP_DEBUG`. | LOW |
- | A06 | LOW | `advanced-cache-handler.php:231-232` | generated drop-in | `header('ETag: "'. $etag .'"')` — ETag quoted with double quotes; later `trim($_SERVER['HTTP_IF_NONE_MATCH']) === $etag` compares raw header (which includes quotes) to bare hash via `trim` — `trim` does not strip quotes, so `W/"hash"` or `"hash"` never matches. | Conditional GET (`304 Not Modified`) never fires for clients sending quoted ETags (most browsers), so bandwidth wasted. | Compare with `trim($etag_header,'"W/ ')` or generate weak ETag compatible check. | MEDIUM |
- | A07 | LOW | `advanced-cache-handler.php:165-166` | generated drop-in | `if (''===$site_domain \|\| strpos($site_domain,'..')!==false \|\| strpos($request_uri,'..')!==false) return;` — prevents `..` but not encoded `%2e%2e` or `//` normalization earlier? | Encoded traversal bypass possible before `rawurldecode`. | Check occurs after `rawurldecode` (line 161) — correct; note that `%2e%2e` is decoded, so covered. Document. | HIGH |
- | A08 | LOW | `advanced-cache-handler.php:291-293` | `Advanced_Cache_Handler::create()` | `$chmod_file = defined('FS_CHMOD_FILE') ? FS_CHMOD_FILE : 0644;` then `put_contents(..., $chmod_file)` — correct, but no locking/atomic write. | Concurrent `Advanced_Cache_Handler::create()` (e.g. two `on_settings_update` races) may interleave writes. | Use temp file + rename, or `put_contents` with `LOCK_EX` where filesystem supports it. | LOW |
- | H01 | LOW | `htaccess-handler.php:158-175` | `Htaccess_Handler::get_rules()` | Fallback next-gen logic duplicates `Server_Rules::get_nginx_rules()` fallback (reads `wppo_settings` + `Server_Rules::is_litespeed()` + `apply_filters('wppo_litespeed_nextgen_rewrite')`). Same pattern in two files. | Drift if gating changes in one place only. | Extract to `LiteSpeed_Integration::is_nextgen_rewrite_enabled()` (already exists) and remove duplicate fallback once that class is always loaded. | HIGH |
- | H02 | LOW | `htaccess-handler.php:202-208` | `Htaccess_Handler::get_rules()` | DocBlock `@param bool $use_nextgen Whether next-gen block was added.` but `apply_filters('wppo_htaccess_nextgen_rules', $rules)` actually filters `array $rules`. Same mismatch in `Server_Rules.php:156-162`. | Misleads IDE/static analysis (Psalm). | Fix `@param array $rules`. | HIGH |
- | H03 | LOW | `htaccess-handler.php:184-193` | `Htaccess_Handler::get_rules()` | Rewrite rules: `RewriteCond %{HTTP:Accept} image/webp` then `RewriteCond %{REQUEST_FILENAME}.webp -f` then `RewriteRule ... $1.webp`; next rule checks `image/avif` — but after first rewrite, `REQUEST_URI` unchanged while `REQUEST_FILENAME` is filesystem path; on Apache `REQUEST_FILENAME` reflects original before rewrite per `RewriteCond`, so both conditions evaluated against same file; order (webp then avif) means AVIF-preferring clients still could get WebP if both exist due to first rule firing first. Comment says "prefer AVIF over WebP — order after" is contradictory. | AVIF-capable browsers served WebP even when AVIF sibling exists (suboptimal). | Swap order: AVIF before WebP, or document that Apache picks first-match. | MEDIUM |
- | S01 | LOW | `server-rules.php:103` | `Server_Rules::get_nginx_rules()` | `$rules[] = '    text/x-component;';` includes semicolon inside array element; while `gzip_types` header element at line 88 has no semicolon, earlier types have no semicolon. Implode with `"\n"` yields `text/x-component;;` double-semicolon? Actual output: lines 88-103 produce `gzip_types` then each `type` on new line, last line already has `;`. Works but inconsistent. | Extra `;` may be ignored by Nginx or cause `nginx -t` warning. | Normalize: keep semicolon only on last element, remove from `'text/x-component;'` → `'text/x-component'` and add semicolon on join, or keep as-is with comment. | MEDIUM |
- | S03 | LOW | `server-rules.php:156-162` | `Server_Rules::get_nginx_rules()` | DocBlock `@param bool $use_nextgen` mismatches `apply_filters('wppo_nginx_nextgen_rules', $rules)` array. | Same Psalm/doc issue as H02. | Fix docblock. | HIGH |
- | S04 | LOW | `server-rules.php:140-155` | `Server_Rules::get_nginx_rules()` | Nginx next-gen snippet wraps `server { location ~* \.(jpe?g|png)$ { try_files ... } }` — this emits a top-level `server` block inside included config, which may conflict with user's existing `server` context if rules are pasted inside `server` already. Docs say snippet requires manual placement. | Copy-paste without context awareness breaks Nginx. | Document required insertion context (http vs server). Already in docs; add comment in returned string. | LOW |
- | M03 | LOW | `class-main.php:405-407` | `Main::includes()` | `if (defined('WP_CLI') && WP_CLI) \WP_CLI::add_command('wppo','PerformanceOptimise\Inc\WPPO_CLI_Command')` — class string not `::class`, no `require` for `class-wppo-cli-command.php`. Relies on autoloader; autoloader is PSR-4 for `PerformanceOptimise\Inc`? Actually plugin has no PSR-4 autoload, only vendor — so class may not be loaded. | WP-CLI command silently fails to register if file not manually required. | Explicitly `require_once WPPO_PLUGIN_PATH.'includes/class-wppo-cli-command.php'` before `add_command`. | MEDIUM |
- | M05 | LOW | `class-main.php:428-429` | `setup_hooks()` | `add_action('init', set_role_hash_cookie)` runs on every request to set `wppo_role_hash` cookie via `setcookie(..., COOKIEPATH, COOKIE_DOMAIN, is_ssl(), true)`. Uses `Util::get_role_hash()` (12-char hex). No `SameSite` attribute set. | On Chrome/Firefox with strict `SameSite=Lax` defaults, cross-site cookie may be blocked when serving role variants via drop-in (drop-in reads `$_COOKIE['wppo_role_hash']` via raw `preg_replace`). Not critical but modern hardening missing. | Add `SameSite=Lax` via `setcookie` options array on PHP 7.3+ (`['samesite'=>'Lax']`). Check PHP 8.2 supports array syntax. | MEDIUM |
- | M06 | MEDIUM | `class-main.php:351-365` | `set_role_hash_cookie()` | Early returns if `is_admin() \|\| wp_doing_ajax() \|\| REST_REQUEST` — correctly avoids setting cookie in admin/AJAX/REST. But `is_admin()` is true for `admin-ajax.php`? `wp_doing_ajax()` already covers; redundant but safe. However, role hash cookie is `httpOnly=true` (last param), so JS cannot read it — drop-in reads via `$_COOKIE` server-side, so httpOnly is fine. But cache variant file is `index-{hash}.html` — if user changes roles, old variant remains until invalidation. | Role change leaves stale variant files for old hash; not purged on `profile_update`. | Hook `profile_update`/`set_user_role` to purge old variants or delete on login. | LOW |
- | M09 | LOW | `class-main.php:472-486` | `setup_hooks()` cache branch | `if (!empty(cache_settings.enableCache)) { $this->cache=new Cache(); $this->cache->set_image_optimisation/image_optimisation ... if (wp_should_output_buffer_template_for_enhancement && $is_wp69_plus) add_filter(...process_buffer_for_cache) else add_action(template_redirect, start_output_buffer)}` — legacy `template_redirect` fallback still registered on WP 6.9+ if function absent? Actually checks `function_exists` first, so OK. But `$is_wp69_plus` checks `wp_version` string vs `'6.9-alpha'` — alpha/beta/RC builds correctly match due to inclusive floor (comment at 440-442). | Correct. Minor: `$wp_version` from `$GLOBALS` may be string like `6.9-RC1` — `version_compare('6.9-RC1','6.9-alpha','>=')` returns true (RC > alpha). Works. | No change. | HIGH |
- | M10 | LOW | `class-main.php:498-507` | `setup_hooks()` used-CSS standalone | `if (empty(cache_settings.enableCache) && !empty(removeUnusedCSS))` registers `process_used_css_only` (6.9+) or `start_used_css_buffer` (legacy). But `process_used_css_only` checks `should_optimise_for_logged_in()` inside — so logged-in users bypass used-CSS even when cache disabled. | Logged-in pages miss used-CSS optimization when logged-in cache is off but used-CSS is on. Intentional (matches cache behavior), but may surprise. | Document that used-CSS respects logged-in cache eligibility. | MEDIUM |
- | M11 | DUPLICATE | `class-main.php:532-545` | `setup_hooks()` combineCSS | `if (!empty(combineCSS)) { if(!$this->cache) { $this->cache=new Cache(); ... } add_action(wp_enqueue_scripts, combine_css, PHP_INT_MAX); add_action(wp_head, maybe_preload_combine_css,1)}` — Creates Cache instance again if already created for page cache. | Duplicate construction path; two `get_option` calls. | Reuse single instance; lazy init method. | LOW |
- | M13 | LOW | `class-main.php:640-670` | delay strategy parsing | `delayJSDefaultStrategy = sanitize_text_field(...) ?: 'interaction'`; `delayJSIdleList/ViewportList` via `Util::process_urls`; `delayJSPriority` via `explode(':',$line,2)` + `trim` + whitelist `high/normal/low`. Empty lines ignored. | Tiny functionality: idle timeout defaults to 3000ms; site operators can set per-handle priority via `handle:high` syntax. Correct. | No change. | HIGH |
- | M14 | LOW | `class-main.php:672-674` | `setup_hooks()` preload | `add_action(wp_head, add_preload_prefetch_preconnect,1)` + `add_speculation_rules,0` + `add_filter(wp_resource_hints,10,2)` — all correctly documented to keep preload vs resource-hints separation (preload needs `as`/`type`, hints need core dedup). | Correct. | No change. | HIGH |
- | M16 | LOW | `class-main.php:787-826` | `migrate_block_assets_setting()` | Uses `get_option('wppo_block_assets_migrated')` marker + checks `is_array(stored)` then `array_key_exists('blockAssetsOnDemand', $file)`. Fresh installs (no stored option) skip migration and set marker `1`. Correct one-time backfill for WP 6.9 `blockAssetsOnDemand` ON default. | Tiny toggle: on 6.9+, classic themes default to separate block assets; migration opts old installs into `true`. | No change. | HIGH |
- | M17 | LOW | `class-main.php:828-850` | `maybe_fix_wp_cache()` | Throttles via `get_transient(wppo_wp_cache_fix_checked)` 1h; on success always sets 1h throttle even on failure then merges `wppo_activation_notices` transient for 30s. Uses `Activate::add_wp_cache_constant()` which writes `wp-config.php`. | Classic TOCTOU: concurrent admin_init from two admins could double-write `wp-config.php`. Low risk. | Keep throttle; note concurrency is rare. | MEDIUM |
- | M18 | LOW | `class-main.php:911-938` | `maybe_run_version_upgrade()` | Checks `current_user_can(manage_options)` + `version_compare(installed,WPPO_VERSION)>=` bail; then `if (!Advanced_Cache_Handler::create()) return` (leaves version unchanged so retry later); then `if (!foreign_dropin_present()) Cache::clear_cache()` then `update_option(wppo_version, WPPO_VERSION, false)` (autoload `false`). Correct idempotency, handles transient filesystem failure. | One subtle: `Advanced_Cache_Handler::create()` returns `true` when foreign drop-in present (leave alone) — then `foreign_dropin_present()` second call returns true again, so `clear_cache` skipped (correct). | No change. | HIGH |
- | M20 | LOW | `class-main.php:1093-1096` | `on_save_post_invalidate_cache()` | Checks `wp_is_post_revision || wp_is_post_autosave` bail, then `$this->cache->invalidate_dynamic_static_html`. But `$this->cache` may be `null` if `enableCache` off — correctly guarded. However, hook is registered only when cache enabled (`if (!empty(enableCache)) add_action(save_post, on_save_post_invalidate_cache)`) — so null path unreachable. | Correct. | No change. | HIGH |
- | M21 | LOW | `class-main.php:1146-1151` | `process_used_css_only()` | Instantiates `new Used_CSS` per request when cache disabled. No reuse; minor overhead. | — | — | LOW |
- | M22 | MEDIUM | `class-main.php:1197-1231` | `emit_server_timing_header()` | Called on `wp_finalized_template_enhancement_output_buffer` with `$output` param; checks `headers_sent()` bail; computes durations via `System_Info::get_request_start_microtime()` (may be `null` if `REQUEST_TIME_FLOAT` missing). Appends `Server-Timing` via `header('Server-Timing: '.implode(', ',$timings), false)` — `false` preserves coexisting entries, correct. But `header()` with `false` appends, not replace — repeated calls could duplicate header if `emit_server_timing_header` somehow called twice (e.g. both cache and non-cache paths). Also `$timings` values use `round(...,2)` but header spec requires `dur` without quotes — correct. | Duplicate Server-Timing if hooked twice (unlikely). | Idempotency guard: static bool flag. | LOW |
- | M23 | LOW | `class-main.php:1356-1377` | `get_frontend_theme_colors()` | Reads `wp_get_global_settings()['color']['palette']['theme']` — assumes `palette.theme` is array of `['slug','color']`; skips entries where `sanitize_hex_color` fails. Then falls back to `get_theme_mod('primary_color')` etc., then `get_header_textcolor()`. Each `sanitize_hex_color` may return `null` on empty, then filtered via `array_filter`. | Correct. Minor: `sanitize_title` on slug may mangle camelCase. | No change. | HIGH |
- | M24 | LOW | `class-main.php:1419-1432` | `admin_enqueue_scripts()` | `realpath` + `strpos($resolved, WPPO_PLUGIN_PATH)===0` traversal guard before `require`. Correct. But `WPPO_PLUGIN_PATH` is normalized via `wp_normalize_path`, while `realpath` returns OS-native separators; on Windows mismatch possible, but `wp_normalize_path` normalizes both. | Correct. | No change. | HIGH |
- | M26 | LOW | `class-main.php:1488-1495` | `admin_enqueue_scripts()` | `client_side_media_processing_enabled => function_exists('wp_is_client_side_media_processing_enabled') && wp_is_client_side_media_processing_enabled()` — evaluated after `Image_Optimisation` has registered opt-out filter (comment says so). | Correct lazy evaluation. | No change. | HIGH |
- | M27 | LOW | `class-main.php:1553-1604` | `enqueue_scripts()` | Builds `$lazy_config` then registers `wppo-lazyload` as script module on WP 6.5+ via `wp_enqueue_script_module` + `script_module_data_wppo-lazyload` filter; fallback on `<6.5` uses `wp_enqueue_script` + `wp_add_inline_script('window.wppoNativeLazy=true')`. The `add_filter('script_module_data_wppo-lazyload', ...)` is added **every** `enqueue_scripts` call without `remove_filter`, so if `enqueue_scripts` somehow fires twice, filter stacks. | Harmless (single fire per request). | Consider `add_filter` once in `setup_hooks` or use `has_filter` guard. | LOW |
- | M28 | LOW | `class-main.php:1620-1661` | `apply_module_loading_strategies()` | Collects module ids via `get_print_queue()` → `registered` → `all` fallback to tolerate core version differences; then `set_in_footer` + `set_fetchpriority('low')` per id, skipping `excludeDeferJS`. Check uses `Util::process_urls((string)excludeDeferJS)` — but module ids are not URLs, so URL-based `process_urls` may split incorrectly. | Exclusion may not match module ids that look like URLs. | Align exclusion semantics: for modules, match exact id only. | MEDIUM |
- | M30 | MEDIUM | `class-main.php:1836-1880` | `add_defer_attribute()` (delayJS branch) | When `delayJS` active, does `str_replace('<script ','<script fetchpriority="low" ',tag)` then `str_replace(' src',' wppo-src',tag)` then `preg_replace('/type=("|\')text\/javascript...')` — but if `deferJS` also active and tag already has `defer` (via `add_defer_strategy` native), this would still add `wppo-src` after defer guard merging `exclude_delay_js`? Actually `exclude_delay_js` merged with `exclude_defer_js` at lines 632-634, so deferred handles are excluded from this branch. Correct. | Guard prevents corruption. Note `add_defer_attribute` is hooked as `script_loader_tag` priority 10, while `add_fetchpriority_to_deferred` hooks at 11 — but `add_defer_attribute` is **delay** handler, not defer. Name is confusing: `add_defer_attribute` handles **delay**, `add_defer_attribute_legacy` handles **defer** on <6.3. | Rename for clarity or add doc. | LOW |
- | M31 | LOW | `class-main.php:1935-1940` | `matches_delay_pattern()` | `preg_match('/\b'.preg_quote($pattern,'/').'\b/', $handle)` — word-boundary matching prevents `slide`→`slider` false positive; `preg_quote` escapes URL metachars. Empty pattern returns false. | Correct. Tiny functionality: dash-delimited prefix still matches because `\b` before `-`? Actually `-` is non-word, so `\b` between word char and `-` counts as boundary, so `jquery` matches `jquery-core` at start— intended. Documented. | No change. | HIGH |
- | M32 | LOW | `class-main.php:2004-2046` | `apply_per_page_delay_config()` | Reads `_wppo_delay_strategies`/`_wppo_delay_priorities` post meta; merges into in-memory `delay_js_*` lists. No `sanitize_text_field` on meta values beyond exact string checks. | Correct whitelist checks `in_array` for allowed values. | No change; note meta is admin-controlled. | HIGH |
- | M33 | LOW | `class-main.php:2097-2112` | `add_preload_prefetch_preconnect()` | Converts relative font/CSS URLs via `Util::cached_content_url($font_url)` when not `https?://`. Then `Util::generate_preload_link` echoes. Correct. | — | — | HIGH |
- | M34 | LOW | `class-main.php:2135-2173` | `add_resource_hints()` | `preconnect` returns array of `['href'=>origin,'crossorigin'=>'anonymous']`; `dns-prefetch` normalizes bare hostnames to `//host`. Core's `wp_resource_hints` will deduplicate. | Correct. Note `preconnectOrigins` origins are taken raw without `https://` prefix validation — user may enter `https://cdn.example.com` or `cdn.example.com`; both handled (core normalizes). | — | HIGH |
- | M35 | LOW | `class-main.php:2288-2314` | `filter_speculation_rules_configuration()` | Pins `conservative` when cache enabled && `eagerness == 'auto'` && no host constant override. Honors `WP_SPECULATIVE_LOADING_DEFAULT_*` via `get_speculation_default_override`. Correct per WP 7.1 #64066/#65624 handling. | — | — | HIGH |
- | M36 | LOW | `class-main.php:2335-2367` | `get_speculation_default_override()` | Reads `getenv` then `defined(const)` so constant wins over env (matches core precedence). Validates `mode` in `prefetch/prerender` and `eagerness` in `conservative/moderate/eager`. | Correct. Tiny: `getenv` may be disabled via `disable_functions`. Guarded by `function_exists('getenv')`. | No change. | HIGH |
- | M37 | LOW | `class-main.php:2384-2528` | `strip_static_query_strings()` / `is_plugin_cache_url*` | Removes only `ver` keys via `rawurldecode($key)==='ver'`; preserves other keys byte-for-byte (duplicate keys, percent-encoding, ordering). Skips plugin cache URLs (`/cache/wppo/`) origin-aware via `Util::cached_content_url('/')` host check; static per-blog cache with `has_filter('content_url')` bypass. | Correct cache-busting preservation. Static cache keyed by `get_current_blog_id()` but not cleared on `switch_to_blog` outside typical flow — minor staleness but blog_id check covers. | No change. | HIGH |
- | M38 | LOW | `class-main.php:2557-2587` | `minify_queued_styles()` | Early bails if `wp_maybe_inline_styles` missing (pre-5.8), logged-in check, `combineCSS` guard. Loops `wp_styles->queue`, skips non-`all` media, then `get_minifiable_css_path` + `Minify\CSS->minify()`. | Correct. | — | HIGH |
- | M39 | LOW | `class-main.php:2640-2721` | `get_minifiable_css_path()` / `minify_css()` / `minify_js()` | Shared helper checks `is_minified_asset_name` regex `(\.min\|\.bundle\|-min)\.css$` (case-insensitive) + `wppo_exclude_minification` filter + `is_file_minified` heuristic. | Consistent. | No change. | HIGH |
- | M41 | LOW | `class-main.php:2897-2907` | `sanitize_image_info_for_client()` | Replaces path arrays with counts for `wppoSettings.image_info`. Prevents filesystem disclosure via View Source. | Correct. | No change. | HIGH |
- | M42 | LOW | `class-main.php:2918-2954` | `enqueue_admin_bar_script()` | Same `realpath`+`strpos` guard as admin page; `wp_enqueue_script` with `fetchpriority=low` via array args (WP 6.5+ style). | Correct. Note inline `wppoObject` JSON includes `apiUrl`+`nonce` — nonce is per-request `wp_rest` — correct. | — | HIGH |
- | C02 | LOW | `class-cache.php:200-254` | `__construct()` | Domain validation: `sanitize_text_field(wp_unslash(HTTP_HOST))`, `idn_to_ascii` (INTL_IDNA_VARIANT_UTS46), strip port via `explode(':',domain,2)[0]`, regex `^[a-z0-9\.\-]+$`, reject `.. / \`. On failure sets `domain=''`. | Correct IDN + port + traversal handling. Minor: `sanitize_text_field` before `idn_to_ascii` may strip valid IDN; order should be `idn_to_ascii` first? However `sanitize_text_field` preserves UTF-8, so puny conversion after is safe. | Document order rationale. | HIGH |
- | C03 | LOW | `class-cache.php:238-244` | `__construct()` | `url_path = wp_normalize_path(trim(rawurldecode(parse_url(request_uri, PATH)),'/'))` then `if(strpos(url_path,'..')!==false) url_path=''` | Correct normalization + traversal reject. | — | HIGH |
- | C04 | LOW | `class-cache.php:256-272` | `is_cache_allowed_for_current_user()` / `get_logged_in_role_hash()` | Delegates to `Util::is_cache_eligible_for_current_user` / `Util::get_role_hash`. Role hash is 12-char hex. | Correct reuse; centralized. | — | HIGH |
- … +16 more in agent-A01-php-correctness.md

### agent-A02-php-media.md

- Severity legend: **CRITICAL** = exploitable / data-loss; **HIGH** = correctness / security regression; **MEDIUM** = functional bug / measurable perf / a11y; **LOW** = minor edge / style; **INFO** = observation / confirmation; **OPTIMIZATION** = perf improvement opportunity; **DUPLICATE** = copy-pasted logic; **DEAD CODE** = unreachable / never-executed.
- | A02-013 | LOW | `class-image-optimisation.php:299-339` + `1488-1515` | `post_process_img_dimensions` static LRU inside closure duplicated & `getimagesize` uncapped call per image | Two copies of same LRU logic (299-338 and 1488-1515). Both call `getimagesize($local_path)` without `filesize` precheck and without silencing — warning on corrupt file leaks to output (outside `try`). TagProcessor branch silently uses `getimagesize`, regex branch does same at 1704 without `@`. Minor inconsistency. | Consolidate helper `get_image_dimensions_cached($path)`; add `@` or `try` and handle `false` uniformly; centralise LRU. |
- | A02-014 | LOW | `class-img-converter.php:402-410`, `1256-1265`, `1318-1335` | `filesize()` result not validated — `false` compared to int | `if (filesize($source) > $max_bytes)` when `filesize` returns `false` on stat failure, PHP coerces `false` (0) > int => false, so oversized check silently passes. Should be `($size = @filesize(...)) !== false && $size > $max_bytes`. Evidence: `403`, `1261`. | Guard false case, treat stat failure as failure (`return false` + status failed). |
- | A02-015 | LOW | `class-used-css.php:1143-1144` | `preg_quote` + `implode('|')` without length limit — ReDoS / pcre.backtrack | `get_all_css_assets()` could return many handles (30+). Joined quoted srcs with `|` inside single regex may create huge alternation (KBs). PCRE backtrack limit may cause `preg_replace` to return `null` (failure) and leave buffer unchanged. Fallback not checked. | Chunk removal or loop per URL, or switch to `WP_HTML_Tag_Processor` iteration. Check `preg_last_error()` after. |
- | A02-016 | LOW | `class-critical-css.php:283-298`, `308-313` | Archive sample URL uses `setup_postdata(int)` with ID not WP_Post | `setup_postdata($archives[0])` where `$archives[0]` is integer ID (298, 308). `setup_postdata()` expects `WP_Post` object; with ID it triggers `_doing_it_wrong` in WP 6.4+ and `get_the_time()` may use global `$post` incorrectly. Evidence: `278-313`. | Pass `get_post($archives[0])` or use `get_the_time('Y', $postId)` static. |
- | A02-017 | LOW | `class-image-optimisation.php:838-852` | `normalize_url()` uses `add_query_arg(array())` without URL | `wp_parse_url(add_query_arg(array()), PHP_URL_PATH)` (842) relies on `add_query_arg` defaulting to current request — but adds empty query to current URL, which may include_fragment? Works but triggers `_doing_it_wrong` if `$_SERVER['REQUEST_URI']` empty in CLI/cron. Also static cache per blog never invalidated if pretty permalinks change mid-request. | Pass `Util::get_current_url()` explicitly; add fallback `if (empty($path)) $path='/'`. Document CLI behavior. |
- | A02-018 | LOW | `class-img-converter.php:624-651` | `imagewebp`/`imageavif` branches ignore `Util::prepare_cache_dir` failure | `if (!function_exists('imageavif') || !imageavif(...))` at 642: `prepare_cache_dir` result not checked. For `webp` path at 626, `prepare_cache_dir` is inside `||` chain so failure causes `$success=false` (good). AVIF branch at 641 calls `prepare_cache_dir` but discards return, then `imageavif` may fail to write due to missing dir (silent). Evidence: `626` vs `641`. | Add `|| ! Util::prepare_cache_dir(...)` to AVIF branch identically. |
- | A02-019 | LOW | `class-image-optimisation.php:1786` | Video placeholder stores `allowfullscreen` sans value — JSON roundtrip may lose valueless attr | `$attrs_to_store` includes `allowfullscreen` (1785). Value read via `get_attribute('allowfullscreen')` returns empty string for `<iframe allowfullscreen>`. Stored as `""` then `esc_attr(json)` then restored? Client `data-wppo-iframe-attrs` JSON not used to recreate iframe yet — just stored. Not harmful, but may misrepresent. | Either store boolean presence or omit empty values; document. |
- | A02-020 | LOW | `class-google-fonts.php:284-319` | `rename($tmp,$dest)` TOCTOU across filesystems | `rename` fails if `$tmp` on different filesystem (rare with cache dir). Code handles via `wp_delete_file($tmp)` but does not fallback to `copy+unlink`. Also `wp_rand()` for tmp suffix is predictable; use `wp_tempnam`. Evidence: `311-318`. | Use `Util::init_filesystem()->move()` for cross-FS safety or `copy` fallback. Not urgent. |
- | A02-038 | LOW | `class-asset-manager.php:93-95` | `dequeue_selected_assets` bails for logged-in users — per-page asset disabling invisible to admin preview | Early return `is_user_logged_in()` (93) means admin editing post never sees effect. Expected to test as logged-out/incognito; document. Consider allowing `WP_DEBUG` or capability `manage_options` preview bypass via query var. |
- | A02-039 | LOW | `class-image-optimisation.php:128-134` | Backward-compat migration `replacePlaceholderWithSVG → placeholderType` overwrites explicit `none` with `'svg'/'none'` based on truthiness — loose bool | Line 130: `(bool) $opt['replacePlaceholderWithSVG'] ? 'svg':'none'` — if legacy value is string `'0'` or `'false'`, `(bool)'false'` is true → incorrectly maps to `svg`. Legacy stored as bool per old checkbox, so low risk, but sanitise via `filter_var($v, FILTER_VALIDATE_BOOLEAN)`. |
- | A02-040 | LOW | `class-image-optimisation.php:139` | `excludeSize` csv explode+absint yields `[0]` for empty string | `explode(',','')` → `['']` → `trim→'','absint('')→0`, `array_map` yields `[0]`. Later `in_array($width,[0],true)` will exclude width 0 (never) so harmless, but pollutes array. Filter empty: `array_filter(explode(...))` before absint. |
- | A02-041 | LOW | `class-critical-css.php:960-996` | `inline_ccss` synchronous fallback creates dir via `$fs->mkdir` but `generate_and_store` uses `wp_mkdir_p` — inconsistency | Both succeed, but `wp_mkdir_p` respects `FS_METHOD` filters, `mkdir` via `$wp_filesystem` may require credentials on some hosts. Prefer `wp_mkdir_p`. |
- | A02-043 | LOW | `class-used-css.php:806-813` | `save_used_css` tmp file not cleaned on move failure when `file_exists` succeeds but move fails | At 808-815, `$fs->move` failing deletes tmp (814) but if `move` succeeded partially and `$file_path` now exists partially truncated? `move` with `true` overwrite is atomic on same filesystem (rename). Failure leaves original intact, tmp deleted — correct. No issue. |
- | A02-045 | LOW | `class-google-fonts.php:173-248` | `download_and_rewrite` not rate-limited — concurrent frontend requests for same uncached font race | Two parallel requests for same Google Fonts URL both see `!file_exists(css_file)` → both fetch CSS and download same woff2 files concurrently, writing same `$hash.woff2` without lock. Could interleave. `rename` atomic mitigates corruption, but wastes bandwidth. | Add file lock (`GET_LOCK` or `flock`) per hash, or check `file_exists` again after fetch before write. |

### agent-A03-php-infra.md

- | 6 | `includes/class-telemetry.php` | 985 | Local HTTP scan (cURL → wp_remote fallback), manual redirect validation, HTML parse, size calc, transient index | filter `wppo_telemetry_verify_ssl` | `wppo_settings`, salted `wppo_audit_{md5(url)}` (HOUR), `wppo_audit_salt`, `wppo_transient_index` option | cURL raw `CURLOPT_FOLLOWLOCATION=false`, `wp_remote_get/head` — 30s/5s timeouts |
- > Severity: `CRITICAL` > `HIGH` > `MEDIUM` > `LOW` > `INFO` > `OPTIMIZATION` / `DUPLICATE` / `DEAD CODE`
- | A03-016 | LOW | `class-database-cleanup.php:86-130,267-311,321-365,373-417,425-469` batch deletes | Optimization | 5 methods copy-paste 30-line `do{ get_col LIMIT 1000; implode placeholders; DELETE postmeta/commentmeta; DELETE posts/comments }` with only table/where differing. Fix requires 5-file edits. `clean_revisions` uses `post_type='revision'` string literal not `prepare`, but safe because literal. | Maintenance burden; risk of divergent fixes (e.g. A03-006 OPTIMIZE guard fix needs 5 edits). | Extract `delete_posts_by_where(string $whereClause, array $tables)` helper; keep PHPCS ignores centralized. | low |
- | A03-017 | LOW | `class-cron.php:141` + `113` double `get_option` + `class-system-info.php:368-379` double `get_option` | Optimization | `schedule_cron_jobs()` line 114 and 141 re-fetch same option; `get_infrastructure()` and `get_litespeed()` each call `get_option('wppo_settings')` within same `get_all()` request without static memoization. Consent overhead ~2 extra `get_option` DB hits per `init`/`system_info` REST call. | Negligible but measurable on high-traffic `init` (cron schedule runs every page view though short-circuited by `wp_next_scheduled`). | Memoize with `static $opts` inside `Cron::schedule_cron_jobs` or call `Main::get_settings()` single source. | low |
- | A03-018 | LOW | `class-telemetry.php:957-983,68-82,939-944` salt vs transient fallback | Compatibility | `invalidate_audit_cache()` no-ops when `wp_cache_get_salted` absent (fallback transients not invalidated). Conversely `scan()` fallback writes `set_transient` + `register_transient_key` but salted path uses `wp_cache_set_salted` with 1 h expiry while fallback writes with `HOUR_IN_SECONDS` + index. Mixed environments (object cache flushed, function appears/disappears) could leave stale data in one store. | Stale audit results after settings save when running without drop-in; minor UX glitch. | In fallback, mirror `delete` path: `invalidate_audit_cache` should `delete_transient` wildcard via index when salted absent, or always delete index entries. | medium |
- | A03-019 | LOW | `class-pagespeed.php:579-610` `extract_lcp_image_url()` snippet regex | Brittleness | Falls back to `preg_match('/<img\s[^>]*src=["\']([^"\']+)["\']/i', $snippet)` — will capture first src but snippets may contain escaped entities (`&quot;`) or single-quoted mixed case; also matches `data-src`? Actually first alternative prioritizes structured `url` field now, but snippet regex could return CDN-rewritten URL with query params truncated by sanitizer. | Rare LCP preload mis-url; preloading wrong image. | Prefer `DOMDocument` fragment parse over regex for snippet, and pass through `Util::get_local_path` normalization check before store. | low |
- | A03-020 | LOW | `class-litespeed-integration.php:887-926,943-968,980-1005` header emission | Timing / Testing | Uses `has_action('litespeed_control_set_ttl')` runtime check then `header()` immediate. Unit tests need `headers_sent()` stub; currently bypassed by `headers_sent()` early return which test env mocks differently. Duplicate `apply_filters('wppo_litespeed_ttl')` called both in `get_litespeed_ttl()` and again in `send_litespeed_ttl()` — double-filter. | TTL filter invoked twice; test doubles need two expectations. | Cache TTL after filtering once; pass through unfiltered TTL to `send_litespeed_ttl` and document single filter point. | low |
- - **SSRF hardening — Telemetry:** `scan()` validates `wp_http_validate_url` + scheme allowlist + same-host `home_host` before any fetch, `CURLOPT_PROTOCOLS` restricted, `CURLOPT_FOLLOWLOCATION=false`, manual `resolve_redirect` re-validates every hop with `MAX_REDIRECT_HOPS=2` — correctly merged from earlier audit.
- **Polish (LOW/INFO):** A03-016–A03-026

### agent-A04-php-rest-cli.md

- | R04 | LOW | `class-rest.php:389-406` | `clear_cache` action dispatch | `if('clear_single_page_cache'===$action){...} else { Cache::clear_cache(); }` — any non-`clear_single_page_cache` value (typo, `clear`, empty) clears **all** cache. No whitelist error. | Accidental full purge on malformed request (e.g. frontend JS bug sending `action:''`). | Whitelist: `if('clear_single_page_cache'===action) else if('clear_all'===action \|\| ''===action) else return 400 Invalid action`. | HIGH |
- | R06 | MEDIUM | `class-rest.php:423-434,712-728` | `update_settings` / `import_settings` tab allowlist drift | `update_settings` allowed_tabs: `file_optimisation,preload_settings,image_optimisation,database_cleanup,object_cache,performance_audit,cache_settings,litespeed_integration,llms_txt` (9). `import_settings` same 9. CLI `WPPO_CLI_Command::settings` allowed_keys adds `core_tweaks` but omits `litespeed_integration,llms_txt` (8). `Activate::maybe_seed_settings` / `Util::sanitize` not gated. | `core_tweaks` settings saved via CLI `wppo settings update core_tweaks` succeed, but `update_settings` REST rejects same tab 400; conversely `litespeed_integration` saved via REST but CLI import rejects. Drift breaks backup/restore parity. | Centralize `ALLOWED_TABS = ['file_optimisation','preload_settings','image_optimisation','database_cleanup','object_cache','performance_audit','cache_settings','litespeed_integration','llms_txt','core_tweaks']` constant in `Util` and reuse in REST + CLI + Activate seed. | HIGH |
- | R07 | LOW | `class-rest.php:443-449,731-738` | `update_settings` / `import_settings` Redis password | `if('object_cache'===tab && isset(sanitized['password'])){ $provided=!empty(sanitized['password']); unset(...); if(provided) sanitized['password_set']=true; }` — correctly avoids persisting password. `build_redis_config` at `1116` does `if(defined(WPPO_REDIS_PASSWORD) && !apply_filters('wppo_redis_allow_request_password',false)) $config['password']='';` — constant wins. | Two password surfaces: REST `update_settings` stores flag not value, but `handle_object_cache.enable` may still use request password transiently (intended). Docs at `999-1005` note HTTPS requirement — good. | No code change; add `@see WPPO_REDIS_PASSWORD` to REST doc and ensure UI warns HTTP. | HIGH |
- | R08 | LOW | `class-rest.php:466-469` | `update_settings` preserve omitted fields | `if('performance_audit'===tab && !isset(params['settings']['pagespeed_api_key']) && isset(options[...])) sanitized['pagespeed_api_key']=...` — preserves when request omits, but `sanitize_settings_recursively` already stripped sanitization of raw key? Check uses raw `params['settings']` so ok. Same for `server_timing_enabled`, `auto_rescan`. | If client sends `"pagespeed_api_key":""` explicitly, it *is* set (empty string) and will be sanitized to empty and overwrite stored key — intentional clear. | Document that empty-string explicitly clears key. | HIGH |
- | R10 | LOW | `class-rest.php:529-530,580-589` | `optimise_image` sanitize | `$webp_images = array_map('sanitize_text_field', (array)$params['webp'])` — `sanitize_text_field` strips tags but preserves `../` . Path validation later via `realpath`+prefix mitigates traversal, but sanitize does not normalize slashes. | `sanitize_text_field('a/b/../c')` still contains `../` until `realpath` check — validation below catches it, so safe. | Keep as defense-in-depth; consider `sanitize_text_field` + `wp_normalize_path` before validation. | MEDIUM |
- | R11 | LOW | `class-rest.php:606-612` | `optimise_image` log message | `__('Scheduled %d image optimization jobs for background processing on ', ...)` — string ends with `on ` (dangling). | Incomplete i18n string; translators see hanging `on `. | Fix string: `__('Scheduled %d image optimization jobs for background processing.', ...)`. | HIGH |
- | R12 | LOW | `class-rest.php:648-657` | `optimise_image` sync fallback counts | `$sanitized[bucket]=['webp'=>count(...), 'avif'=>count(...)]` counts after sync, but `$response` shape from `Img_Converter::get_img_info()` already counts? Duplicated count logic vs `get_image_job_status` at `959-970`. | Drift if bucket shape changes (e.g. `pending.webp` becomes int). | Extract helper `Img_Converter::get_counts()` and reuse. | MEDIUM |
- | R13 | MEDIUM | `class-rest.php:668-688` | `delete_optimised_image` filesystem | `if(!Util::init_filesystem()) return 500; $wppo_dir=WP_CONTENT_DIR.'/wppo'; if(!$wp_filesystem->is_dir(...)) return 404; if(!$wp_filesystem->delete(...,true)) return 500; Img_Converter::clear_completed_formats(); Cache::clear_cache();` — deletes entire `wp-content/wppo` (images + maybe other plugin data) recursively. No confirmation param. | With `manage_options` an accidental DELETE call wipes all converted images; no undo. `Cache::clear_cache()` after deletion is correct (HTML references broken). | Require explicit `confirm:true` param or use `WP_Filesystem::rmdir` with allowlist; add notice. | LOW |
- | R14 | LOW | `class-rest.php:695-773` | `import_settings` merge | `$merged = array_replace_recursive($existing, $sanitized)` — recursive merge preserves nested keys not in import, but also merges arrays numerically (e.g. `loggedInCacheRoles` arrays merge by index, not replace). Importing `{"cache_settings":{"loggedInCacheRoles":["editor"]}}` over `["administrator"]` yields `["editor"]`? Actually `array_replace_recursive` *does* replace indexed values by index 0, so it replaces — correct for this shape. But deep merge of `high_value_urls` may duplicate? | For list-arrays, `array_replace_recursive` replaces index 0, leaving extra indexes from old array if new list shorter → stale roles persist. | For list fields, use `array_replace` shallow or explicit replace; document merge semantics (import is overlay, not reset). | MEDIUM |
- | R15 | LOW | `class-rest.php:798-862` | `database_cleanup` valid_types vs method_map | `valid_types` includes 10 inc `all`; `method_map` maps 9 types (revisions..oembed_cache). `if(!method) return 400` at `861` catches `oembed_cache`? No, map has `oembed_cache` at `857` (`clean_oembed_cache`) so ok. But `valid_types` includes `trashed_comments` and map includes it — consistent. One subtle: `revisions` uses `clean_revisions_advanced` (advanced) while CLI `database` uses `clean_revisions` (non-advanced) — different retention semantics. | REST respects `dbRevMaxAge/dbRevKeepLatest` settings, CLI `wppo database cleanup --type=revisions` ignores them (or uses defaults inside `clean_revisions`). Divergence. | Make CLI use `invoke_cleanup_method('clean_revisions_advanced', max_age, keep_latest)` same as REST, or document difference. | MEDIUM |
- | R16 | LOW | `class-rest.php:888-894` | `database_cleanup` post-clean optimize | `if((int)result>0 && isset(Database_Cleanup::TABLE_MAP[type])) maybe_optimize_tables(TABLE_MAP[type], true)` — only on success and >0 rows. `TABLE_MAP` not shown but assumed correct. | Optimize may run on every single-type clean, adding `OPTIMIZE TABLE` latency (table lock). Flag as intentional. | Gate optimize behind `Database_Cleanup::should_optimize()` or transient throttle; already `maybe_optimize_tables` likely throttles. | LOW |
- | R18 | LOW | `class-rest.php:1027-1072` | `handle_object_cache` branching | `if(status) return status+compressors; if(ping) build_config→ping; if(enable) build→enable; if(disable) disable; if(flush) flush; return 400 Invalid action` — sequential `if` not `elseif`, but each returns, so safe. `ping`/`enable` build config from raw `$params` via `build_redis_config` (sanitized). | `flush` path does `if(result) log success else 400` — `flush()` may return WP_Error vs bool; code treats falsy as failure, but WP_Error truthy would incorrectly success. Check `Object_Cache::flush()` return type. | Normalize to `is_wp_error($result)` check. | MEDIUM |
- | R19 | LOW | `class-rest.php:1090-1138` | `build_redis_config` | Allowed keys: `mode,host,port,password,database,nodes,master_name,use_tls,persistent,compression` — `port/database` cast `(int)`, `use_tls/persistent` cast `(bool)`, others `sanitize_text_field`. Missing `timeout`/`prefix` that CLI supports; CLI’s `get_redis_config_from_assoc` allows `host/port/password/database/timeout/prefix` (6 keys) — drift. | REST ping/enable with `timeout` param silently ignored; CLI ping with `nodes` ignored. | Unify allowlists or document divergence (REST is richer). | HIGH |
- | R21 | LOW | `class-rest.php:1164-1178` | `ajax_get_nonce` | Checks `is_user_logged_in && current_user_can('manage_options')`, then `check_ajax_referer('wppo_nonce_refresh','nonce',false)` → `wp_send_json_error` 403 on fail, else `wp_send_json_success(['nonce'=>wp_create_nonce('wp_rest')])`. Not a REST route but `admin-ajax.php`. No `wp_die` after? `wp_send_json_success` exits. | Correct nonce refresh for 403 stale-nonce recovery (used by `src/main.js`). | No change. | HIGH |
- | R22 | MEDIUM | `class-rest.php:1201-1236` | `run_performance_scan` SSRF | Order: `wp_http_validate_url` (loopback/private) → scheme allowlist → host equality vs `Util::cached_home_url()`. `queue_pagespeed_scan` at `1258-1274` does scheme→host→`wp_http_validate_url` (different order). Both enforce host equality, but `wp_http_validate_url` rejects loopback/private *including* 127.0.0.1 hosted behind same origin via hosts file? Good. However `Util::cached_home_url()` may be cached per blog_id and may include path (`https://example.com/subdir`) → `wp_parse_url(...PHP_URL_HOST)` only host, so subdir comparison lost — scanning `https://example.com.evil.com/` passes host equality? No, host differs, so blocked. | Scheme check before `wp_http_validate_url` in `queue_pagespeed_scan` means `file://` blocked early, but `gopher://` also blocked via scheme allowlist. Good defense-in-depth. Minor order inconsistency not exploitable. | Normalize order to `scheme → wp_http_validate_url → host equality` in both. | LOW |
- | R23 | LOW | `class-rest.php:1202,1251,1311,1404` | URL defaults via `Util::cached_home_url('/')` | `run_performance_scan`: `url = esc_url_raw(params['url']) ?? cached_home_url('/')`; `queue_pagespeed_scan` same; `get_pagespeed_results` same; `get_suggestions` uses same. `esc_url_raw` allows empty → fallback to home. | If attacker omits URL, scan runs on homepage (intended). No validation of path traversal via `//evil.com`? `esc_url_raw('//evil.com')` returns `//evil.com` then `wp_parse_url` scheme empty → blocked via scheme check. Good. | No change. | HIGH |
- | R24 | LOW | `class-rest.php:1335-1338` | `get_pagespeed_results` retroactive LCP store | `if(!empty(results['lcp_image_url'])) Pagespeed::store_lcp_image_url(url,results,strategy)` — stores on every GET (idempotent, dedup inside). | Extra write on each poll (GET should be side-effect free). Could spam options/transients if polled rapidly. | Guard with transient throttle or move to queue completion hook only; note dedup mitigates. | LOW |
- | R25 | LOW | `class-rest.php:1356-1390` | `get_web_vitals_trends` filter | `url_key = md5(url)` if url else null; `array_filter(trends, fn(k)=> strpos(k, url_key.'_')===0 && str_ends_with(k,'_'.$strategy))` — uses `str_ends_with` (PHP 8.0+, plugin requires 8.2 so ok). | `str_ends_with` available; `strpos` prefix check correct. Trends stored as `md5(url)_strategy` — good. | No change. | HIGH |
- | R26 | LOW | `class-rest.php:1406-1415` | `get_suggestions` | `$transient_key = transient_key('wppo_audit_'.md5(url))` — multisite prefix via `Util::transient_key`; `get_transient` → if false returns `['suggestions'=>[]]` with message `No cached scan...Run a scan first.` | Correct. Note `wppo_audit_*` transients not cleaned in `uninstall.php` (see U03). | Add to uninstall cleanup via `LIKE 'wppo_audit_%'`. | MEDIUM |
- | R27 | LOW | `class-rest.php:1430-1451` | `used_css_regenerate` | Checks `as_enqueue_async_action` exists else 500; if `post_id` enqueues `wppo_used_css_generate` else `Used_CSS::regenerate_all()` (queues per post). Returns 202. | No post_id existence check (trashed? invalid ID still queues). | Validate `get_post(post_id)` exists, else 404. | LOW |
- | R29 | INFO | `class-rest.php:291-318,326` | `get_schema_for_route` + `permission_callback` | Schema static, no args validation; permission uses `manage_options` + nonce. | `args` / `validate_callback` / `sanitize_callback` not declared per-route — relies on handler sanitization. Acceptable, but OpenAPI discoverability limited. | Consider adding `args` declarations for gateway validation and `OPTIONS` preflight docs. | LOW |
- | CLI03 | LOW | `class-wppo-cli-command.php:233-235` | `database` revisions | `cleaned_count = Database_Cleanup::clean_revisions()` — no args, while REST does `invoke_cleanup_method('clean_revisions_advanced', max_age, keep_latest)` with settings. CLI ignores `dbRevMaxAge/dbRevKeepLatest`. | CLI `cleanup --type=revisions` may delete more/less than UI respects settings. | Pass settings as REST does: read `wppo_settings[database_cleanup][dbRevMaxAge/KeepLatest]` and invoke advanced method. | MEDIUM |
- | CLI04 | LOW | `class-wppo-cli-command.php:174-197` | `database optimize` | `foreach table_list as table: result=optimize_table(table); if(result) ++success; WP_CLI::log(' - Optimized table: ...')` — `optimize_table` takes identifier like `posts`, not fully qualified `$wpdb->prefix . 'posts'`. Internals likely map, but CLI accepts arbitrary comma list without allowlist. | `wppo database optimize --tables=options,users` could OPTIMIZE arbitrary tables if identifier not validated. | Allowlist `['posts','postmeta','comments','commentmeta','options']` else warn; delegate mapping to `Database_Cleanup::TABLE_MAP`. | MEDIUM |
- | CLI05 | LOW | `class-wppo-cli-command.php:306-343` | `image convert` | `$conversion_format = format ? format : (options['image_optimisation']['conversionFormat'] ?? 'webp'); if('both'===format) formats=[avif,webp] else if in [avif,webp] single` — `format` values not sanitized beyond `in_array`; `batch = options['image_optimisation']['batch'] ?? 50` not validated (could be 0 or 1M). `total_pending` counts all pending, but `counter>=batch_size` breaks per-format, so `both` processes up to `2*batch` (one batch per format) — exceeds UI batch? | Operator `wppo image convert --format=webp` with batch 5000 could OOM (convert 5000 images synchronously). | Cast `batch = max(1, min(200, (int)batch))`; validate `format in [webp,avif,both]` else error. | MEDIUM |
- | CLI06 | MEDIUM | `class-wppo-cli-command.php:336-341` | `image convert` traversal | `$source_path = ABSPATH . img` (wp_normalize_path); `$resolved=realpath(source_path); if(false===resolved || strpos(normalize(resolved), normalized_abspath)!==0) continue;` — skips traversal attempts (good) but no logging of skipped. If `wppo_img_info` contains poisoned `../etc/passwd` entry, silently skipped — no error surfaced. | Poisoned entry remains pending forever (never converted, never failed). | Log warning for skipped traversal, and consider `clear_completed_formats` for failed? | LOW |
- | CLI09 | LOW | `class-wppo-cli-command.php:661-688` | `settings get` | `if(tab && !isset(options[tab])) error Invalid tab ... Available tabs: implode(keys(options))` — `options` already merged with defaults, so lists all 7 defaults plus any stored, but if `litespeed_integration` stored but not in defaults, it *is* listed (since merged). However error message leaks internal tab names to CLI user (acceptable). | — | No change, but align tab list with central allowlist. | LOW |
- | CLI12 | LOW | `class-wppo-cli-command.php:885-915` | `pagespeed` | `$url = assoc['url'] ?? cached_home_url(); $job_id = Pagespeed::queue_scan(url, strategy)` — no SSRF/host validation as REST does; no strategy whitelist validation before queue. | `wp wppo pagespeed scan --url=https://evil.com` queues scan for external domain (Pagespeed internally may validate, but CLI bypasses REST gate). Low risk (CLI is privileged). | Add same host/scheme validation or delegate to `Pagespeed::queue_scan` (ensure it validates). | MEDIUM |
- | MB02 | LOW | `class-metabox.php:50-53` | `add_metabox` | `$post_types = get_post_types(['public'=>true],'names'); $excluded=['attachment']; $post_types=array_diff(...)` — excludes only attachment, but includes `wp_block`, `wp_template`, etc. if public (custom). | Asset Manager appears on block/template editors where script disabling is irrelevant. | Allowlist via `apply_filters('wppo_asset_manager_post_types', $post_types)` or exclude `wp_block` explicitly. | LOW |
- | MB04 | LOW | `class-metabox.php:118` | `render_asset_manager_metabox` | `wp_nonce_field('wppo_save_asset_manager','wppo_asset_manager_nonce')` — correct. | — | — | HIGH |
- | MB06 | LOW | `class-metabox.php:142-159` | `render_asset_manager_metabox` | `if(false===assets || (empty(scripts)&&empty(styles))) show description + Visit Page button if publish` — `Asset_Manager::get_page_assets` returns false if not captured; message says "Visit this page/post on frontend while logged out" — but `Main::` only captures when `is_cache_allowed`? Logged-in capture depends on `enableLoggedInCache`? Documentation correct? | User with logged-in cache off visiting as logged-in won't capture; message says logged out — correct guidance. | No change. | HIGH |
- | MB08 | LOW | `class-metabox.php:308-321` | `save_metabox` | `if(DOING_AUTOSAVE) return; if(!current_user_can('edit_post',post_id)) return; save_preload...; save_asset...` — capability `edit_post` correct for per-post meta; autosave bypass correct. No `wp_is_post_revision` check — import may trigger on revision save. | Revision `post_id` is revision ID, meta saved on revision not parent. `update_post_meta` on revision ID creates orphaned meta. | Add `if(wp_is_post_revision(post_id)) return;` or map to parent `wp_is_post_revision` then `post_id = parent`. | MEDIUM |
- | MB09 | LOW | `class-metabox.php:329-343` | `save_preload_image_urls` | `if(!isset($_POST[nonce])) return; if(!wp_verify_nonce(...)) return; if(!isset($_POST[preload_url])) return; $preload_urls=sanitize_textarea_field(wp_unslash(...)); update_post_meta(post_id,'_wppo_preload_image_url',preload_urls)` — sanitizes as textarea (preserves newlines). No URL validation per line. | User can save `javascript:alert(1)` in preload; later `Main::add_preload_*` may output raw via `Util::generate_preload_link` which does `esc_url` — so `javascript:` stripped to empty, harmless but no early error. | Validate each line via `esc_url_raw` + `Util::is_url_excluded` style, warn. | LOW |
- | MB11 | LOW | `class-metabox.php:403-406` | `get_raw_post_array` | `return isset($_POST[key]) && is_array($_POST[key]) ? wp_unslash($_POST[key]) : []` — unslashes but not sanitized; sanitization deferred to `process_*`. Correct. `// phpcs:ignore NonceVerification` comment present. | — | No change. | HIGH |
- | ACT03 | LOW | `class-activate.php:112-114` | `maybe_seed_settings` | `existing = get_option('wppo_settings', null); if(null!==existing) return;` — distinguishes "no option" (null) from empty array `[]` (would have returned `[]` if option stored as empty). If option exists as `[]` (wiped), seed not run, leaving empty. | Edge: manual `delete_option` vs `update_option([],[])` vs `null` — seed only on not-found, not on empty wipe. CLI fallback handles empty, but Activate does not. | Check `if(null!==existing && is_array(existing) && !empty(existing)) return;` or treat empty array as missing. | MEDIUM |
- | ACT04 | LOW | `class-activate.php:180` | `maybe_seed_settings` | `add_option('wppo_settings', defaults, '', false)` — autoload `false` (not autoloaded). Correct per `wppo_img_info` pattern (non-autoloading to reduce options bloat). | Settings loaded on every admin + frontend via `get_option` still hits DB (autoload false means extra query). Tradeoff: not autoloaded reduces memory but adds query per request. | Evaluate `autoload=false` vs True for hot path; document rationale (size ~5KB). | LOW |
- | ACT06 | LOW | `class-activate.php:239-243` | `schedule_upgrade_routine` | `if(!wp_next_scheduled('wppo_run_upgrades')) wp_schedule_single_event(time()+delay, 'wppo_run_upgrades')` — dedup via `wp_next_scheduled`. | Multiple concurrent activations only schedule once. | No change. | HIGH |
- | ACT08 | LOW | `class-activate.php:88-93` | `init` server rules | `enable_server_rules = options[file_optimisation][enableServerRules] ?? false; if(enable) { updated=Htaccess_Handler::update_rules(true); if(!updated) notices[]= 'Failed...' ; set_transient(... ) }` — on activation, `.htaccess` update may fail (permissions) and notice added. | Second `set_transient` overwrites earlier foreign-dropin notices? It does `array_unique(notices)` including prior, so merged. | Good merging; keep. | HIGH |
- | DEA01 | LOW | `class-deactivate.php:38-42` | `Deactivate::init` | `unschedule_crons(); unschedule_database_cleanup_cron(); delete_option('wppo_preload_cron_offset');` — deletes offset but not `wppo_version` (kept for upgrade gate). | On deactivate/reactivate cycle, `wppo_version` remains, so `maybe_run_upgrades` sees fresh_install false? Actually init passes `!$has_activation_time` as `is_fresh_install`; deactivation does not delete `wppo_activation_time`, so reactivate is not fresh. Correct (preserve upgrade gate). | Document retention. | HIGH |
- | DEA02 | MEDIUM | `class-deactivate.php:42-61` | `init` drop-in removal | `Advanced_Cache_Handler::remove();` plus manual `object-cache.php` delete if contains `'Redis Object Cache Drop-in for Performance Optimisation'`, and delete `wppo-redis-config.php`. | Selective delete via substring is safe but `object-cache.php` from another plugin containing same substring false-positive? Unlikely. | Tie to `Advanced_Cache_Handler::is_our_dropin()` style check instead of substring. | LOW |
- | DEA03 | LOW | `class-deactivate.php:78-102` | `unschedule_crons` | Unschedules `wppo_page_cron_hook`, `wppo_page_cron_batch`, `wppo_img_conversation` + `wppo_img_conversion`, `wppo_generate_static_page`. Missing `wppo_run_upgrades`, `wppo_web_vitals_rescan`, `wppo_database_cleanup_cron` (separate method handles one), `wppo_used_css_generate`, `wppo_generate_ccss`, `wppo_pagespeed_scan`, `wppo_generate_static_url` (sitemap), `wppo_preload` 5-hourly? `Cron::` hooks may cover some. | Residual cron entries after deactivation (e.g. `wppo_web_vitals_rescan` daily) keep firing via `wp-cron` → PHP errors (class not loaded). | Audit `Cron::` and `Pagespeed::` scheduled hooks and unschedule comprehensively or via `wp_clear_scheduled_hook` wildcard. | MEDIUM |
- | DEA04 | LOW | `class-deactivate.php:126-154` | `remove_wp_cache_constant` | Pattern `/\/\*\*\s*Enables WordPress Cache\s*\*\/\s*\nif...define..true.../` else `/\n?define\(\s*['"]WP_CACHE['"]\s*,\s*true\s*\);/` → `put_contents`. Same atomicity concern as ACT07; `is_writable` checked but `get_contents` may return false on huge file → `preg_match` on false → PHP warning. | On read failure, `preg_replace` on false yields null, then `put_contents(null)` may corrupt `wp-config.php`. | Guard `if(!is_string(content)) return;` before regex. | MEDIUM |
- | UTL02 | LOW | `class-util.php:55-81` | `prepare_cache_dir` | Iteratively builds dirs `explode('/',trim(path,'/'))` → `build='/'.$part` then `if(!is_dir(build)) mkdir(build,FS_CHMOD_DIR)` — creates each ancestor with `FS_CHMOD_DIR` (typically 0755). No `is_dir` race handling; concurrent mkdir may fail harmlessly. | On race, second `mkdir` returns false and function returns false though dir now exists (another process created). | On `!mkdir` re-check `is_dir` before returning false. | LOW |
- | UTL06 | LOW | `class-util.php:199-224` | `get_image_mime_type` | `extension=strtolower(pathinfo(parse_url(url, PHP_URL_PATH), PATHINFO_EXTENSION)); switch(jpg/jpeg→image/jpeg, png→..., webp, gif, svg, avif, heic/heif, default '')` — HEIC/HEIF not needed for preload link `as=image` but present. `default ''` so unknown extension yields empty type → caller likely skips preload type. | Good. No `jxl` / `heic` mime needed? `heic` correct. | Add `jxl`? Keep minimal. | LOW |
- | UTL09 | LOW | `class-util.php:315-365` | `is_url_excluded` | `strip_scheme = fn(u) 0===stripos('https://')?substr(8):stripos('http://')?substr(7):u; normalized_url=strip_scheme(rtrim(url,'/')); foreach(exclude){ rtrim(..., '/'); if(''===rule) continue; if(strpos(rule,'http')!==0) rule=home_base.'/'.ltrim(...); normalized_rule=strip_scheme(rule); if(strpos(rule,'(.*)')!==false) { prefix=rtrim(str_replace('(.*)','',rule),'/').'/'; if(strpos(normalized_url.'/', prefix)===0) return true; } if(normalized_url===normalized_rule) return true; } return false;` — `strip_scheme` case-insensitive, handles `HTTP://`. `(.*)` prefix logic adds `/` boundary so `/cart` rule `https://example.com/cart(.*)` matches `/cart` and `/cart/` and `/cart/item` but not `/cartoon` (good). Root-relative rules resolved against `home_base`. | Empty rule skip handles whitespace-only? `rtrim('   ','/')` leaves `'   '` not empty, then `strip_scheme` leaves `'   '` and exact match fails — whitespace-only rules slip through as non-empty but should be ignored. Original comment says empty+whitespace-only ignored, but code only checks `''===exclude_url` after `rtrim('/'` not `trim`. | Use `trim($exclude_url)` check before `rtrim` to skip whitespace-only. | MEDIUM |
- | UTL10 | LOW | `class-util.php:377-381` | `get_current_url` | `url = cached_home_url((string)add_query_arg([], $wp->request ?? '')); return untrailingslashit(esc_url_raw(url))` — strips query, normalizes via `esc_url_raw`. Uses `cached_home_url` with path = `add_query_arg` result (which encodes). Good. | `add_query_arg([], wp->request)` on multisite subdirectory may double-encode? | Acceptable; matches `store_lcp_image_url` normalization. | LOW |
- | UTL14 | LOW | `class-util.php:589-592` | `is_auto_sizes_available` | `return function_exists('wp_sizes_attribute_includes_valid_auto') || function_exists('wp_img_tag_add_auto_sizes')` — gates WP 6.7 auto-sizes. | Correct version gate. | No change. | HIGH |
- | UTL15 | LOW | `class-util.php:605-641` | `sanitize_settings_recursively` | `foreach settings as key=>value: safe_key=preg_replace('/[^a-zA-Z0-9_\-]/','',key); if(''===safe_key) continue; if('mode'===safe_key && !is_array(value)) raw=sanitize_text_field((string)value) whitelist auto/wppo/litespeed/standalone; if(is_array) recurse; elseif is_bool → bool; elseif is_numeric → (int); elseif safe_key in [pagespeed_api_key,password] → sanitize_text_field; elseif stripos(url|cdn|origin) → esc_url_raw; elseif stripos(exclude|preload|delay|list) → sanitize_textarea_field; else sanitize_text_field` | Numeric cast `(int)` truncates floats (e.g. `delayJSIdleTimeout` 3000 okay, but `0.5` becomes 0). `is_numeric('3.14')` → 3 loses precision. Also `is_numeric` check before url/list checks means numeric-like url? Not. Order: `mode` allowlist before generic handling — good. `stripos` checks case-insensitive, so `CDNURL` would be caught though safe_key regex stripped non-alphanum and is case-sensitive? `safe_key` retains original case? Regex allows A-Z, so `CDNURL` stays uppercase, `stripos` case-insensitive still matches. Good. | For float settings, cast to `(float)` or keep string; currently integer truncation may silently change `auto_sizi`? No floats used. Document. Also `sanitize_settings_recursively` called on both REST + CLI, so truncation affects both. | MEDIUM |
- | CT02 | LOW | `class-core-tweaks.php:108-127` | `disable_emojis` | `remove_action(wp_head,print_emoji_detection_script,7)` + `admin_print_scripts`, `wp_print_styles`, filters `the_content_feed/comment_text_rss/wp_mail`, `tiny_mce_plugins`, `wp_resource_hints`, then `wp_dequeue_script_module('emoji')` if function exists, `wp_dequeue_script('wp-emoji')`. | Duplicates `disable_emojis_script_module` hooked separately at 100; `disable_emojis` runs on `init` (before enqueue) so `wp_dequeue_script_module` there may be too early (scripts not registered yet) — dequeue no-op, but second hook at `wp_enqueue_scripts` 100 catches. Intentional defense-in-depth. | Document dual dequeue timing. | LOW |
- | CT04 | LOW | `class-core-tweaks.php:161-167` | `disable_emojis_remove_dns_prefetch` | `if('dns-prefetch'===relation && is_array(urls)) { emoji_svg_url=apply_filters('emoji_svg_url','https://s.w.org/.../svg/'); urls=array_diff(urls,[emoji_svg_url]); }` — removes only that exact URL, not host variants. | If filter changes emoji URL via `emoji_svg_url`, removal still works via filtered value. Good. | No change. | HIGH |
- | CT06 | LOW | `class-core-tweaks.php:204-215` | `disable_embeds_rewrites` | `if(!is_array(rules)) return rules; foreach(rules as rule=>rewrite) if(false===strpos(rewrite,'embed=true')) new_rules[rule]=rewrite; return new_rules` — drops embed rules. | No flush rewrite rules after removal — rules stay cached until flush. | Call `flush_rewrite_rules()` on settings toggle (once) via `on_settings_update`. | MEDIUM |
- … +12 more in agent-A04-php-rest-cli.md

### agent-A05-js-spa.md

- | 05 | LOW | `src/App.js:211-257` | Mobile focus trap does not handle `Escape` to close drawer | Effect traps `Tab` but no `Escape => setMobileMenuOpen(false)` | Keyboard users must tab to toggle; WCAG 2.1 expectation for drawer | Add `if(e.key==='Escape'){ e.preventDefault(); setMobileMenuOpen(false); toggleBtnRef.current?.focus(); }` inside sidebar keydown handler. | HIGH |
- | 08 | LOW | `src/lib/apiRequest.js:52-54` | `pendingRefresh` guard uses `.finally` on promise then discards rejection swallowing timing | `pendingRefresh = refreshPromise.finally(()=>{pendingRefresh=null})` | If two concurrent 403s both await `refreshNonce()`, second gets the shared promise; `finally` clears ref *after* microtask, but if refresh fails, shared rejection propagates to both – correct. No bug but `finally` on rejected promise still resolves pendingRefresh to rejection then clears; however callers `await refreshNonce()` will throw – okay. Just document expiry | No change; keep. Add comment that concurrent callers share rejection. | MEDIUM |
- | 16 | LOW | `src/components/PreloadSettings.js:52-57` | `speculationRules` read from `wppoSettings.speculation_rules` without null guard on `speculationRules?.eagerness_override` later used as string | Fine but `speculationRules` defaults to `{}` -> `eagernessOverride` nullish | Minor; not a bug | — | LOW |
- | 18 | MEDIUM | `src/components/FileOptimization.js:51-55` | Fallback via `\|\|` instead of `??` masks intentional empty string for `delayJSIdleList` etc. | `delayJSIdleList: options.delayJSIdleList \|\| ''` | Empty string intentional value forced to '' anyway same; low risk. But for `delayJSDefaultStrategy` value `'idle'` falsy? no. For `delayJSIdleTimeout` `options.delayJSIdleTimeout \|\| 3000` treats `0` as `3000` (desired) but also `''` as 3000. Not bug but inconsistent with `??` elsewhere | Use `??` for strings, keep `\|\|` for numeric with comment. | LOW |
- | 20 | MEDIUM | `src/components/PageSpeedPanel.js:125-142` | `isMounted` ref update not tied to `stopPolling` deps could leak poll after strategy switch | `stopPolling` is callback with empty deps (stable), `isMounted.current=false` on unmount only; if user switches `strategy` during polling, prior poll continues with old strategy until next fetch resolves | Poll uses `scanUrl`+`scanStrategy` closed over at call-time, so strategy change mid-poll does not restart correctly; handler starts new poll without cancelling previous if `handleScan` called twice quickly (guard `submittingRef` prevents but strategy toggle disabled while scanning so okay) | Acceptable as-is; document disabled toggle during scan. | LOW |
- | 22 | MEDIUM | `src/components/PluginSetting.js:69-75` | `apiKeyConfigured` state initialized from global once then never synced if global mutated by other flow | `const [apiKeyConfigured,setApiKeyConfigured]=useState(wppoSettings.performance_audit?.pagespeedApiKeyConfigured ?? false)` | After saving API key via `saveApiKey`, local `setApiKeyConfigured(true)` updates, but if user navigates away and returns via full page reload, global re-read is correct; SPA tab switch without reload leaves stale if API key cleared server-side via other method. Minor | Add effect syncing from global as Dashboard does for cacheSettings. | LOW |
- | 24 | LOW | `src/components/DatabaseCleanup.js:279-282` | `parseInt(val)` missing radix | `Object.values(counts).reduce((sum,val)=>sum+(parseInt(val)\|\|0),0)` | In older engines, leading-zero strings interpreted as octal; plus linter warning | Use `parseInt(val,10)` consistently (also `246` in Dashboard). | LOW |
- | 25 | MEDIUM | `src/components/PerformanceAudit.js:360-377` | Second fetch `fetchSuggestions(url, abort.signal)` uses same `AbortController` that may already be aborted if scan fetch threw? Actually controller only aborted on unmount or next scan, not after success – okay. But if suggestions fetch lags and user starts new scan, first suggestions fetch still runs with old signal then aborted, but new scan's abort aborts old request – okay. However suggestions state may be set after component unmounted without isMounted guard | `onSuggestionsReady` may call `setTelemetrySuggestions` after Dashboard unmounted if user switches tab quickly | Add `isMounted` ref similar to PageSpeedPanel, or gate with `!abort.signal.aborted` check already does first half but not mounted guarantee; aborted signal already tied to unmount via effect that aborts on unmount – okay. Keep. | LOW |
- | 26 | LOW | `src/components/CriticalCssPanel.js:14-35` | `STATUS_CONFIG` labels `__(...)` executed at import time, not per-render; if locale changes (admin language switch) without reload, labels stale | Static import-level translation | SPA never changes locale without reload; low impact | Keep or make config a factory `getStatusConfig()` called per render. | LOW |
- | 27 | LOW | `src/components/AutoloadedOptions.js:25-56` | `load` effect runs once, but no retry UI on error, only `<p>` error; duplicate pattern vs WebVitalsTrends which shows retry-less error. Consistent but missing affordance | Users cannot retry without remounting component (tab switch does not refetch because deps `[]` – actually `[load]` where load is stable callback so runs once) | Add retry button invoking `load()` as PageSpeedPanel/SystemInfo do. | LOW |
- | 29 | LOW | `src/components/WebVitalsRum.js:23-39` | `dayAverages` averages weighted by sample count `n` but divides `sum/n` per metric independently, not per page; correct site-wide average, but loses per-path cardinality | Minor accuracy: mixing paths with different decision? Weighting by sample count already correct for site-wide mean | Document as site-wide mean; alternatively show median per metric. | INFO |
- | 30 | MEDIUM | `src/components/common/LoadingSubmitButton.js:17-49` | `children` vs `label` precedence unclear; `label \|\| children` but if both passed, label wins silently, children ignored; several callers pass children incorrectly via `label={<><FontAwesomeIcon/> Save</>}` vs children prop | In `Dashboard:776-798` `label={<><FontAwesomeIcon/> Purge All Cache</>}` – no children; in `PluginSetting:522` `children` as JSX inside button without `label` – works via fallback `label \|\| children` but `isLoading` then shows `loadingLabel \|\| children` which shows same children even while loading (should show loadingLabel) | Normalise API: always use `label` + `loadingLabel`; deprecate `children` path or warn. Current usage in PluginSetting `Load Activity Log` passes children without label, so loadingLabel works but shows children when not loading via fallback – okay but inconsistent. | LOW |
- | 31 | LOW | `src/components/common/Tooltip.js:25-35` | Container `tabIndex="0"` makes tooltip trigger focusable even when `children` is already a focusable control (e.g., wrapping SwitchField) → nested focusable violation | `<span tabIndex=0><SwitchField>...</SwitchField></span>` results in extra tab stop | Only set `tabIndex` when `children` not already focusable; or render as `span` with `role="button"`? Better: render tooltip container as `span` without tabIndex when wrapping interactive child, rely on child's focus. | MEDIUM |
- | 32 | LOW | `src/components/common/ConfirmDialog.js:68-103` | Body scroll lock via `doc.body.style.overflow='hidden'` unconditionally hides, but does not restore original value if it was `overflow:auto` or `scroll` explicitly | `doc.body.style.overflow=''` clears inline style, but if page had `overflow:hidden` via inline style before opening, it gets lost | Store original `body.style.overflow` before hiding: `const prev=doc.body.style.overflow; doc.body.style.overflow='hidden'; return ()=>{doc.body.style.overflow=prev}`. | LOW |
- | 33 | LOW | `src/components/common/ErrorBoundary.js:34-37` | Hard reload via `window.location.reload()` loses WP admin unsaved state without confirm | Okay for boundary | Consider `onReset` prop to allow parent App to reset state without full reload. | INFO |
- | 34 | MEDIUM | `src/setupTests.js:37-55` | Mock of `@wordpress/components` `ToggleControl` renders plain checkbox with `onChange(e.target.checked)` but real component passes `newValue` boolean via prop – mock does `onChange(e.target.checked)` correct but strips other props `disabled`, missing `__nextHasNoMarginBottom` passthrough okay. However mock does not reflect real WP ToggleControl a11y (`role="switch"`), causing false-positive tests for SwitchField | Tests asserting `aria-label` etc may pass differently on real UI (`ToggleControl` renders `<input role="checkbox">`? Actually Toggle renders checkbox) | Mock intentionally minimal; ensure tests use `getByLabelText` not implementation detail. Document drift. | LOW |
- | 37 | DUPLICATE | `src/components/Dashboard.js:246-251` vs `src/components/DatabaseCleanup.js:279-282` | `dbOverhead` total pattern repeated: `Object.values(dbCounts).reduce((s,v)=>s+(parseInt(v,10)\|\|0),0)` | Two copies, minor | Extract helper `sumCounts(counts)` to util. | LOW |
- | 41 | MEDIUM | `src/components/SystemInfo.js:69-107` | `handleLoad` sets `loaded=true` only on success, leaving `loaded` false on error so trigger stays “Load System Info” not “Refresh” even after successful prior load then subsequent failure | Second fetch failure then immediate success should show Refresh – works because loaded already true; but initial failure retains Load label correctly. Minor inconsistency when error recovery | Keep `loaded` true after first success forever, or track `attempted` separately. Already does (`setLoaded(true)` only on success, keeps true after). Okay but upon error `notice` shows, button still says Load vs Refresh – acceptable. | LOW |
- | 42 | MEDIUM | `src/App.js:389-393` | Transition `setTimeout(400)` duration mismatched with CSS `wppo-fadeIn` likely `300ms` animation | If CSS duration changes, JS timeout desync leaves transition class longer/shorter than animation | Use `onTransitionEnd` event instead of fixed timeout, or read CSS variable. | LOW |
- | 43 | MEDIUM | `src/components/FileOptimization.js:309-328` | `handleSubTabKeyDown` wraps index without handling `Home`/`End` keys expected for tablist ARIA pattern | `role="tablist"` expects `ArrowRight/Left`, `Home` → first, `End` → last per WAI-ARIA | Add `Home`/`End` cases per spec. | LOW |
- | 45 | LOW | `src/lib/apiRequest.js:193-195` | `encodeURIComponent` on `url` and `strategy` inside template literal for `getPagespeedResults` vs `fetchSuggestions` etc. direct encode – consistent, safe. No double-encode issue | Query param built via `URLSearchParams` in `fetchWebVitalsTrends:211-221` correctly uses `params.set` + `params.toString()` double-encodes? Actually `fetchWebVitalsTrends` uses URLSearchParams correctly vs manual encode for PageSpeed – two patterns coexist | Standardise on `URLSearchParams` everywhere. Minor style, not bug. | INFO |

### agent-A06-js-vanilla.md

- | 02 | LOW | `lazyload.js:63-76` | AUTO_SIZES_SUPPORTED probe forces reflow at module load | `probe.setAttribute('sizes','auto'); document.documentElement.appendChild(probe); getComputedStyle(probe).contain` | 1 reflow even when no images; wasteful in jsdom | Lazy-evaluate on first restoreSizes('auto') instead of IIFE | MEDIUM |
- | 08 | LOW | `lazyload.js:393-412` | 6 triggerEvents over-trigger | `['mouseenter','mousedown','mouseover','touchstart','scroll','keydown']` with {once:true} + manual remove | First interaction fires 2-3 handlers synchronously; deduped via scriptLoadPromise but wasteful | Reduce to `['pointerdown','keydown','scroll']`; keep once:true only | LOW |
- | 09 | LOW | `lazyload.js:412-415` | Dead else-if branch | `else if (document.querySelector('script[data-wppo-delay-strategy]')) { // nothing }` | Retains query + branch for comment | Remove | HIGH |
- | 14 | LOW | `lazyload.js:711-747` | MutationObserver disconnected too eagerly | observe(document.body,subtree) – checkCleanup disconnects when remaining===0; new HTMX images after disconnect missed | Dynamic content after initial drain not observed | Keep observer alive until pagehide or re-create in observeElement | MEDIUM |
- | 16 | MEDIUM | `lazyload.js:875-891` | Global error handler uses tagName check without instanceof | `e.target.tagName==='IMG' && hasAttribute('data-wppo-fallback')` – window has no tagName | Throws if target is window; fallback loop safe but fragile | Use `e.target instanceof HTMLImageElement` | LOW |
- | 17 | LOW | `lazyload.js:913-918` | iframe.allowFullscreen property only | `iframe.allowFullscreen=true; iframe.loading='lazy'` – property vs attribute | Works in HTML but XHTML strict needs attribute | Also `setAttribute('allowfullscreen','')` | LOW |
- | 18 | LOW | `lazyload.js:924-940` | data-wppo-iframe-attrs allows on* injection | `if(!['src','width','height','style'].includes(k)) setAttribute(k,v)` permits onload/onerror if server allows | Server filters sandbox/referrerpolicy today; client should denylist /^on/i | Add `/^on/i.test(k)` reject + `/^[a-z-]+$/` | MEDIUM |
- | 21 | LOW | `lazyload.js:997-1006` | backgroundObserver queries document each batch | `if(!querySelector('.wppo-lazy-bg')) disconnect()` per intersection | Extra scan | Track pending count decrement instead | INFO |
- | 22 | LOW | `lazyload.js:1015-1025` | loadImages called twice (DOMContentLoaded + loadScripts setTimeout) | Guard `if(!globalObserver)` makes second call no-op; intended rescan after scripts never happens | Confusing comment; scripts-injected images never scanned | After loadScripts call dedicated scanNewLazyElements() iterating observedElements | MEDIUM |
- | 26 | MEDIUM | `main.js:158-163` | pagehide clears timer not DOM | `clearTimeout(fallbackTimer)` leaves .wppo-admin-notice in bfcache | Stale notices on pageshow | Also `querySelectorAll('.wppo-admin-notice').forEach(el=>el.remove())` | LOW |
- | 32 | MEDIUM | `rum.js:128-137` | CLS sum never resets session window | `cls+=entry.value` lifetime sum; spec uses 5s window max | Over-reports vs CrUX | Document lifetime vs spec or implement gap reset | LOW |
- | 33 | LOW | `rum.js:145-158` | INP uses last entry not max | `values.inp=entries[entries.length-1].duration` overwrites per batch | Under-reports worst interaction | Track maxInp across batches | MEDIUM |
- | 35 | MEDIUM | `setupTests.js:37-55` | ToggleControl mock stubs only ToggleControl | `Dashboard` doesn't use Modal/Spinner today; future imports silently undefined | Masked regression | Merge with actual: `...requireActual('@wordpress/components')` | LOW |
- | 40 | LOW | `__tests__/main.test.js:69-88` | Path validation branch uncovered | Only tests path '/' from JSDOM; no cases for '..', 2049 chars, empty, no-slash | Security guard zero coverage | Add param cases for traversal + long path | MEDIUM |

### agent-A07-css.md

- > Severity: **CRITICAL** > **HIGH** > **MEDIUM** > **LOW** > **INFO** > **OPTIMIZATION** > **DUPLICATE** > **DEAD CODE**
- | F-18 | **LOW** | `src/css/components/_video-placeholder.scss:8` | `background-color: var(--wppo-bg-secondary, #f0f0f1);` | `--wppo-bg-secondary` **not defined** in `_variables.scss` (only fallback `#f0f0f1`). Works but inconsistent — `grep` shows `used but not defined` (build scan). | Add `--wppo-bg-secondary: #f0f0f1;` to `:root` in `_variables.scss` and remove fallback param. | 99% |
- | F-19 | **LOW** | `src/css/base/_base.scss:98,74` vs `_variables.scss:18-19` | `background: var(--wppo-border-hover, #cbd5e1)` fallback `#cbd5e1` vs token `#c7d2e0` (`_variables.scss:19`) | Mismatch: actual hover border is `#c7d2e0` but fallback says `#cbd5e1` (Tailwind slate-300). If var fails, color jumps. | Align fallback to `#c7d2e0`. | 97% |
- | F-20 | **LOW** | `src/css/components/_progress.scss:1-18` + `src/css/base/_base.scss:16-23` + others | Hardcoded `border-radius: 4px` in `_base.scss:22,69`, `_progress.scss:4`, `_video-placeholder.scss:5` vs tokens `--wppo-radius:16px / 10px / 6px` | Inconsistent radius scale — `4px` appears 9× but closest token is `6px` (`--wppo-radius-xs`). Breaks radius rhythm. | Map `4px` to `--wppo-radius-xs` (change token to `4px` if needed) or introduce `--wppo-radius-2xs:4px`. | 85% |
- | F-21 | **LOW** | `src/css/components/_tabs.scss:62-65,127-134` + `src/css/components/_forms.scss:255-262` | `@media (max-width: 768px) { min-height:44px }` raw vs `@include respond-to('md')` which is also `768px` | Duplication: raw and mixin produce **identical media query** but generate **separate** blocks in build (contributes to 54 duplicate selectors). | Replace all raw `768px` with `@include respond-to('md')` (or define `$breakpoint-md` constant usage). | 94% |
- | F-22 | **LOW** | `src/css/components/_fields.scss:32-34` | `@media (max-width:400px) { flex-wrap: wrap; }` — breakpoint not in map | Orphan breakpoint (`400px`). Future `sm` change won't affect it. | Add `'xs': 400px` to breakpoint map or inline comment `// bespoke: 400px not in design system`. | 90% |
- | F-23 | **LOW** | `src/css/components/_header.scss:69` | `text-wrap: balance;` (in `h2`) | Baseline 2023+ (Chrome 114+, Safari 17.5+). Unsupported browsers ignore gracefully, but build browserslist `last 1` includes older Safari — no fallback. Not critical (progressive enhancement). | Keep (harmless) but add comment `/* progressive enhancement — ignored where unsupported */`. | 92% |
- | F-24 | **LOW** | `src/css/components/_welcome.scss:107-112` | `.wppo-welcome-step .wppo-button { font-size:12px; padding:4px 14px; min-height:28px }` overrides generic `.wppo-button` inside step | High specificity nesting increases override cost; also `min-height:28px` below a11y minimum 32px (WCAG 2.5.5 target size). | Bump to `32px` min-height; consider `.wppo-button--sm` variant instead of nesting. | 78% |

### agent-A08-security.md

- | A-AUTH-03 | LOW | `includes/class-metabox.php:308-321` | `save_metabox` checks `current_user_can('edit_post', $post_id)` + `wp_verify_nonce` for each sub-save, but `add_metabox` registers asset-manager box for all `public` post types without cap check. | Low — `add_meta_box` visibility is WP core controlled; save is gated. No priv-esc. | No change; note as INFO. | HIGH |
- | A-AUTH-04 | LOW | `includes/class-wppo-cli-command.php:75-955` | All `WP_CLI` commands (`wppo cache/database/image/settings/object-cache`) have no in-code `current_user_can` — rely on CLI's shell access (requires `WP_CLI` constant). | CLI access = server shell = already privileged; no web exposure. | Document that CLI is shell-gated; no fix needed. | HIGH |
- | A-SAN-02 | LOW | `includes/class-rest.php:342-344,528-530` | `optimise_image`: `$webp_images = array_map('sanitize_text_field', (array)$params['webp'])` — image paths are filesystem-relative strings (e.g. `wp-content/uploads/2024/01/img.jpg`), not free text; `sanitize_text_field` strips tags but not `../` (caught later by `realpath` check). | Defense-in-depth holds (realpath gate), but `sanitize_text_field` is not path-aware; edge: `%2e%2e%2f` not decoded before sanitize? Later `realpath` resolves decoded path anyway. | Keep as-is; add comment that path validation is via `realpath` prefix check, not sanitize. | HIGH |
- | A-SAN-03 | LOW | `includes/class-util.php:605-641` | `sanitize_settings_recursively`: `$safe_key = preg_replace('/[^a-zA-Z0-9_\-]/','',$key)` + type-branch (`bool→bool`, `numeric→int`, `pagespeed_api_key|password→sanitize_text_field`, `url/cdn/origin→esc_url_raw`, `exclude/preload/delay/list→sanitize_textarea_field`, else `sanitize_text_field`). | Strong central sanitization shared by REST + CLI. One nuance: `numeric` branch casts to `(int)` — floats truncated (e.g. `delayJSIdleTimeout` float becomes int). Intentional. Keys with empty `$safe_key` skipped (empty-string key prevented). | No change; good centralization. | HIGH |
- | A-SQL-02 | LOW | `uninstall.php:29` | `$wpdb->query("DROP TABLE IF EXISTS {$table_name}")` where `$table_name = $wpdb->prefix . 'wppo_activity_logs'` — direct interpolation, phpcs-ignored. | `$wpdb->prefix` is core-controlled (alphanumeric + `_`), not user input; safe but violates PHPCS. | Keep `// phpcs:ignore` with comment that prefix is core-provided; or use `$wpdb->prepare` with identifier placeholder (not needed). | HIGH |
- | A-SQL-03 | LOW | `includes/class-database-cleanup.php:704-708` | `get_autoloaded_options`: `prepare("SELECT option_name, LENGTH(option_value) AS opt_size FROM {$wpdb->options} WHERE autoload IN ($placeholders) ORDER BY opt_size DESC LIMIT ".(int)$limit, ...$autoload_values)` — `$limit` cast to `(int)` before interpolation, `$placeholders` are `%s` for `autoload` values (`yes/on/auto/auto-on` from `get_autoloadable_values()`). | Safe — limit is int-cast, values are hardcoded. | No change. | HIGH |
- | A-SQL-04 | LOW | `includes/class-database-cleanup.php:1049-1055` | `get_table_size`: `prepare('SELECT (data_length+index_length) FROM information_schema.TABLES WHERE table_schema=%s AND table_name=%s', DB_NAME, $table)` where `$table = $wpdb->{$table_id}` (e.g. `wp_posts`). | `$table` is `$wpdb->{identifier}` resolved from allowlisted `TABLE_MAP` keys; not user input. Safe. | No change. | HIGH |
- | A-SQL-05 | LOW | `includes/class-database-cleanup.php:1093-1094` | `optimize_table`: `$wpdb->query("OPTIMIZE TABLE {$full_table_name}")` where `$full_table_name = $wpdb->{$table}` after 1 GB guard. | `$full_table_name` is allowlisted via `TABLE_MAP` + `wp_posts` etc.; safe. Note `OPTIMIZE TABLE` takes `READ` lock — 1 GB guard + `Database_Cleanup::maybe_optimize_tables` dedup mitigates. | No change. | HIGH |
- | A-XSS-02 | LOW | `includes/class-log.php:51-66` | `Log::add` stores `wp_kses_post($activity)` — `wp_kses_post` allows `<a href>`, `<em>`, etc. `get_recent_activities` returns raw `activity` HTML; React dashboard renders via `dangerouslySetInnerHTML`? Check `src/components/…` — likely escapes. Even if rendered as HTML, `wp_kses_post` limits to safe tags. | Stored XSS via log message that includes user input (e.g. CDN URL, PageSpeed URL). CDN URL is `esc_url_raw` before logging; PageSpeed URL is `esc_url` before logging. So inputs escaped before `wp_kses_post`. | Audit dashboard rendering: ensure `activity` is not `innerHTML` without `wp_kses_post` server-side guarantee. Current `wp_kses_post` is sufficient. | MEDIUM |
- | A-XSS-03 | LOW | `includes/class-main.php:2918-2930` | `enqueue_admin_bar_script`: `wp_json_encode(['apiUrl'=>rest_url(...), 'nonce'=>wp_create_nonce('wp_rest')])` inline via `wp_localize_script` / `wp_add_inline_script` — `wp_create_nonce` is safe, `rest_url` is core-escaped. | No XSS — `wp_localize_script` JSON-encodes. | No change. | HIGH |
- | A-FILE-02 | LOW | `includes/class-cache.php:200-254,592-606,1550-1610` | Cache path construction: `$cache_root_dir = WP_CONTENT_DIR . '/cache/wppo'`; `$file_path = WP_CONTENT_DIR . '/cache/wppo/{domain}/{path}/index.html'` where `{domain}` validated via IDN+regex, `{path}` stripped of `..` and normalized. `Util::prepare_cache_dir` iterates `explode('/',$path)` and `mkdir` per segment via `WP_Filesystem`. | Strong confinement: `domain` regex + `..` reject + `WP_CONTENT_DIR` prefix prevents traversal. `WP_Filesystem` abstraction ensures no direct `fopen` with user path. | No change; note validation in `class-rest.php:371-386` `clear_cache` path also uses `realpath` prefix check — duplicate defense. | HIGH |
- | A-FILE-03 | LOW | `includes/class-img-converter.php:989-1069` | `get_img_path(string $source_image, string $format)`: checks `path_is_absolute` + `strpos(ABSPATH|WP_CONTENT_DIR)===0`, then `Util::get_local_path` + `..` check, off-site host check (`content_host`/`home_host` vs `source_host`), `prepare_cache_dir(dirname($avif_path))` before `imagewebp`/`imageavif`. | Robust — off-site URLs returned unchanged (no filesystem write), traversal blocked at 3 layers. `..` rejected after `rawurldecode`. | No change. | HIGH |
- | A-FILE-04 | LOW | `includes/class-rest.php:340-387,552-563` | `clear_cache`: `$path = wp_normalize_path($path)` → `$real_path = realpath($this->cache_dir . $path)` → `$normalized_cache_dir = wp_normalize_path($this->cache_dir)` → `if (false===$real_path || (!is_exact_match && !is_under_dir)) 400`. `optimise_image`: `realpath($source_path)` + `strpos(normalized_resolved, normalized_abspath)===0`. | Correct realpath+prefix validation. Empty path (clear all) skips realpath (cache dir may not exist yet) — intentional. | Note: `realpath` returns `false` for non-existent sibling paths — correctly returns 400 (no disclosure). | HIGH |
- | A-FILE-05 | LOW | `includes/class-util.php:110-145` | `get_local_path(string $url)`: `wp_parse_url($url, PATH)` → `strpos('..')!==false => ''`, then `home_path` prefix strip, then `full_path = ABSPATH . ltrim(relative,'/')` → `strpos(full_path, normalized_abspath)===0` check. | Enforces ABSPATH-descendant — sibling `/path/abspath2` cannot prefix-match due to trailing `/` on `normalized_abspath` (WP core sets `ABSPATH` with `/`). Prevents `ABSPATH` escape. | No change. | HIGH |
- | A-FILE-06 | LOW | `includes/class-object-cache.php:292-305,336-345` | `enable`: `$wp_filesystem->put_contents($this->config_path, var_export($config_data,true))` then `copy(template, dropin_path)`. `disable`/`uninstall.php:80-87` deletes only if `strpos(content, DROPIN_MARKER) !== false` — prevents foreign drop-in deletion. | Marker-guarded write/delete prevents clobbering third-party `object-cache.php` (e.g. Redis Cache by Till Krüss). | No change. | HIGH |
- | A-FILE-07 | LOW | `includes/class-advanced-cache-handler.php:128-294` | `create`: `var_export(home_url(),true)` for `$site_url_escaped` → generated `advanced-cache.php` uses `preg_replace('/[^a-z0-9.:-]+/i','',$raw_domain)` + `strpos('..')` reject + `WP_CONTENT_DIR` prefix for `$file_path`. `remove` checks `is_our_dropin()` before delete. | Generated drop-in sanitizes `HTTP_HOST`/`REQUEST_URI` before filesystem use; marker guards deletion. | No change. | HIGH |
- | A-PRIV-01 | LOW | `includes/class-rest.php:417-481,696-773` | `update_settings` / `import_settings`: `$allowed_tabs` whitelist enforced; unknown keys rejected with 400. `sanitize_settings_recursively` strips empty keys, type-normalizes. `object_cache.password` never stored — stripped and `password_set` flag set. `performance_audit.pagespeed_api_key` preserved when omitted (not overwritten to empty). | Prevents option injection — cannot write arbitrary `wppo_settings` keys or inject `password` into DB. `password` transient exposure via `wppo-redis-config.php` is explicitly stripped (see `class-object-cache.php:280-289`). | No change. | HIGH |
- | A-PRIV-02 | LOW | `includes/class-util.php:523-538` | `get_role_hash(WP_User $user)`: `substr(md5(implode(',', sorted roles) . wp_salt()),0,12)` — salted with `wp_salt()` (secret). Drop-in computes same hash via same `wp_salt()` (baked at generation). | Prevents cookie forgery `wppo_role_hash` — without `wp_salt`, cannot predict 12-char hex for unauthorized role. `wppo_role_hash` is `httpOnly=true`, `SameSite` not set (see `A-SAN` note). | Consider `SameSite=Lax` via PHP 7.3+ array syntax (minor hardening). | MEDIUM |
- | A-PRIV-03 | LOW | `includes/class-wppo-cli-command.php:556-732` | `settings update/import`: validates `allowed_keys` / `known_tabs` before `Util::sanitize_settings_recursively` + `password_set` handling + `pagespeed_api_key` stripping — mirrors REST logic. | CLI import cannot inject arbitrary tabs; sanitization consistent across entry points. | No change. | HIGH |
- | A-PRIV-04 | LOW | `includes/class-rum.php:276-331` | `store_sample`: `update_option('wppo_web_vitals_rum', $all, false)` where `$all` is per-day/per-path aggregates (bounded: `MAX_DAYS=14`, `MAX_PATHS_PER_DAY=200`, `MAX_PATHS_PER_DAY` enforced via `array_shift`, `path` truncated to 512 chars, metrics clamped to `0..60000` or `0..1`). | Prevents option bloat / DoS via infinite path injection — bounded at 14×200 entries (~15k metrics). Attacker-controlled `path` cannot inject serialized object (JSON aggregate only). | Note: `array_shift` evicts oldest path — attacker can flood 200 random paths to evict legitimate data (DoS of metrics, not RCE). Acceptable trade-off. | HIGH |
- | A-PRIV-05 | LOW | `includes/class-pagespeed.php:291-313,344-382` | `store_failure`: `set_transient(md5(url)_strategy, ['error'=>true,'message'=>sanitize_text_field($msg)], 5*MIN)`. `record_trend`: `update_option('wppo_web_vitals_trends', $hist, false)` bounded at `TREND_LIMIT=30` per key, `TREND_MAX_KEYS=20` global via `prune_trends` (oldest keys by `fetched_at`). Trend lock via `add_option('wppo_web_vitals_trends_lock', time())` (atomic INSERT) + 60 s stale steal. | Prevents unbounded option growth from many distinct URLs. `wp_salt` not needed (key is `md5(esc_url_raw(url))`). | No change. | HIGH |
- | A-SSRF-01 | INFO | `includes/class-telemetry.php:83-98,251-428` | `Telemetry::scan` + `fetch_via_curl`/`fetch_via_wp_remote`: `wp_http_validate_url($url)` + scheme `http|https` + `home_host === parsed_host` before fetch; `CURLOPT_FOLLOWLOCATION=false` + manual `resolve_redirect` re-validates each hop against same SSRF rules; `MAX_REDIRECT_HOPS=2`. `calculate_sizes` HEAD fallback also gates via `home_host===url_host && wp_http_validate_url`. | Strong SSRF defense — DNS rebinding via redirect to `127.0.0.1/0.0.0.0/169.254.169.254` blocked; same-host-only policy. `CURLOPT_PROTOCOLS` restricts to HTTP/HTTPS. | No change. | **HIGH** |

### agent-A09-performance.md

- | H10 | **Telemetry scan** | REST `run_performance_scan` `{url,force}` → `Telemetry::scan(manual, force)` | `wp_http_validate_url`+scheme+same-host at REST + re-check in `scan`; `Util::transient_key(wppo_audit_md5)` + salted cache `wp_cache_get_salted`/`get_transient` HOUR unless `force` | `fetch_via_curl` (`CURLOPT_FOLLOWLOCATION false`, `ENCODING auto`, `PROTOCOLS HTTP|HTTPS`, `SSLVeRIFy` filter, `MAX_REDIRECT_HOPS 2` `resolve_redirect` per hop) → fallback `fetch_via_wp_remote`(`redirection 0`, same manual redirect); `parse_resources` (TagProcessor vs regex fallback, counts lazy/eager/unminified/third-party) → `calculate_sizes` (`get_local_path`+`filesize` else `wp_remote_head 5 s` same-host only gated by `home_host+validate_url`, per resource sequentially) → `check_https/modern_images/alt/robots/compression/cache_control`; set `wp_cache_set_salted`/`set_transient`+`register_transient_key` | `wppo_telemetry_verify_ssl` filter; `wppo_audit_salt` | `wppo_audit_*` transient + `wppo_transient_index` map capped 200 (prune by expiry `asort` slice -200) | Result array 16 metrics `is_cached` flag | `invalidate_audit_cache` `update_option salt` on `on_settings_update` perf_audit change |
- | P-CPU-08 | **MEDIUM** | `includes/class-telemetry.php:682-702` | `$get_size` inner fallback `wp_remote_head` runs **without** `set_transient` memo; repeated telemetry scans (same page, same `force=false` should be cached, but cron `performance_scan` not used — only manual scans) will re-HEAD the same CDN URLs each time after `HOUR` expiry. `resolve_redirect` style network is not cached. | Each hourly invalidation (`invalidate_audit_cache` bumps salt) evicts all `wppo_audit_*` caches, so next manual scan for same URL re-HEADs every remote asset. | Cache HEAD results per URL for `HOUR` in a static map or transient (`wppo_telemetry_head_md5`). | **LOW** |
- | P-CPU-09 | **LOW** | `includes/class-cron.php:545-571` | `mark_page_as_processed` builds `$domain` via `sanitize_text_field(parse_url(site_url(),host).port)` and `$cache_dir` via `WP_CONTENT_DIR . "/cache/wppo/{$domain}/{$url_path}"` then `Util::init_filesystem`+`exists`+`delete`×2 per `process_page`. Called once per `wppo_generate_static_page` job (up to 200 per cron tick) → 200× `init_filesystem` + 400× deletes. | 200× `WP_Filesystem()` init (~0.2 ms each) = 40 ms per batch; deletes are sparse (cache may not exist yet for sitemap URLs). Not huge but multiplied by 5 h cadence. | Use `Cache::get_file_path` helper (already composes `domain+path`) to avoid duplication; memo `filesystem` locally. | **LOW** |
- | P-WP-08 | **LOW** | `includes/class-main.php:418-430` | `maybe_fix_wp_cache` runs on `admin_init` every admin request: `get_transient(Util::transient_key wppo_wp_cache_fix_checked)` 1 h throttle + if missing, `Activate::add_wp_cache_constant()` which `is_readable`+`file_get_contents` `wp-config.php` + `strpos WP_CACHE` + `file_put_contents`. On multisite, `Util::transient_key` prefixes with blog id. | When transient expires (hourly), one admin request does `is_readable`+`file_get_contents` of `wp-config.php` (10–50 KB) → 0.5 ms. Low impact due to 1 h throttle. | No fix needed; already throttled. Note as INFO that `get_transient` still queried per admin request (1 object-cache lookup). | **LOW** |
- | P-WP-09 | **LOW** | `includes/class-main.php:2067-2180` | `add_preload_prefetch_preconnect` echoes `<link rel="preload">` via `Util::generate_preload_link` which does `esc_attr`+`esc_url`+`implode`+`wp_kses`+`echo`. `Util::generate_preload_link` builds `$allowed_html` array then `wp_kses($link_tag, $allowed_html)` per resource. On a page with 10 preloads (fonts+CSS+post-type images srcset 5 widths), that's 10× `wp_kses` (regex HTML parser). | Each `wp_kses` is heavy (tokenises HTML). 10× `wp_kses` ≈ 2–5 ms vs straight `echo`. Same helper used for LCP preload `maybe_preload_combine_css` etc. | Replace `wp_kses` round-trip with direct `printf('<link …>', esc_attr…)` — inputs already escaped via `esc_url`/`esc_attr`, so `wp_kses` is redundant. | **LOW** |
- | P-WP-10 | **LOW** | `includes/class-cache.php:2243-2249` | `flush_legacy_query_cache_keys` does `wp_cache_flush()` (full object-cache flush) when `function_exists wp_cache_get_salted` (WP 6.9+). Called from `Main::on_settings_update`? Actually `Cache::clear_cache` calls `flush_legacy`? Check — `flush_legacy_query_cache_keys` is exposed but not auto-called on every clear? If invoked, `wp_cache_flush` evicts **all** transients + salts for whole site (including other plugins' caches) — expensive on Redis (SCAN+DEL). | 1 full flush per cache clear (admin save) — acceptable (clear_all is infrequent). But doc warns "only reliable eviction" — cost vs correctness trade-off. | No change; note flush is infrequent (settings save, plugin update). Could use `flush_group('wppo')` + `flush_group('wppo_db_cleanup_counts')` instead on WP 6.1+ but legacy keys remain — current approach justified. | **INFO** |
- | P-DB-05 | **LOW** | `includes/class-database-cleanup.php:1045-1094` | `get_table_size` does `SELECT (data_length+index_length) FROM information_schema.TABLES WHERE table_schema=DB_NAME AND table_name=%s` per `optimize_table` call; `maybe_optimize_tables` dedups via `array_unique` but still calls `get_table_size` per unique table (up to 5 tables). `information_schema` query requires `SUPER`/`PROCESSLIST` on some hosts and may be **blocked by `information_schema` privilege** on shared hosts (returns null → skip guard). | Per `optimize_table` 1 extra query; per `clean_all` with 5 tables =5 queries + `OPTIMIZE TABLE` locks. Low cost vs `OPTIMIZE` itself (seconds for large tables). Fallback when `information_schema` blocked still proceeds (guard returns 0 → `size 0 < 1GB` so optimizes anyway — loses safety). | Document that 1 GB guard is best-effort on shared hosts; consider `SHOW TABLE STATUS LIKE %s` as fallback (uses `information_schema` implicitly but may be allowed). | **LOW** |
- | P-DB-06 | **LOW** | `includes/class-system-info.php:548-559` | `get_mysql_var('max_connections')` runs `SHOW VARIABLES LIKE %s` via `wpdb->prepare` per `get_system_info` call (admin SPA `fetchSystemInfo`). `SHOW VARIABLES` is not cached; runs once per admin settings visit but `get_all` calls it via `get_database`. | 1 extra query per `system_info` REST — trivial. | No fix; note `SHOW VARIABLES` could be replaced by `SELECT @@max_connections` (lighter). | **INFO** |
- | P-FE-04 | **MEDIUM** | `src/App.js:284-387` | Initial mount effect `useEffect([activeTab, recentActivities.length, serverRules, rulesRetryTrigger, ccssRefreshTrigger])` runs `fetchRules` + `fetchCcssStatus` **on every `activeTab` change** (dependency includes `activeTab`). `fetchRules` has guard `if (serverRules || hasFetchedRules.current) return;` so second call is cheap, but the effect still creates an `AbortController` and calls async functions + `fetchCcssStatus` checks `if (hasFetchedCcss.current && ccssRefreshTrigger===0) return;`. The effect cleanup `abort()` runs on tab switch, cancelling in-flight but re-triggering. | Tab switching 7 times triggers effect 7×, each constructing `AbortController` + branch checks. No network waste after first fetch (guarded), but still 7× React effect runs with `console.error` potential. Minor TTI cost. | Split into two effects: one `[]` mount-only for `fetchRules`/`fetchCcssStatus`, another `[activeTab]` only for `fetchActivities` (which is tab-dependent). | **LOW** |
- | P-FE-06 | **LOW** | `src/main.js:110-163,168-191` | `showNotice` fallback builds DOM `div.notice is-dismissible wppo-admin-notice` + `button.notice-dismiss` per click, with `setTimeout 5000` `fallbackTimer` single global. Concurrent `Clear All` + `Clear This Page` clicks share `fallbackTimer` (clears previous timer). No `transition` CSS for dismiss (instant `remove()`). | Sub-minor: two notices cannot coexist (second `clearTimeout` cancels first's auto-dismiss). Not a perf issue. | Use per-notice `setTimeout` map (`WeakMap<Element, timer>`) or `wp.data.dispatch('core/notices')` primary path already used. | **INFO** |
- | P-FE-07 | **LOW** | `src/rum.js:98-195` | `rum.js` registers `PerformanceObserver` `largest-contentful-paint`+`layout-shift`+`event durationThreshold 16 buffered:true` unconditionally. `getEntriesByType('navigation')[0]` + `getEntriesByType('paint')` loop on every page. `scheduleSend` 5000 ms + `visibilitychange`+`pagehide once`. Script loaded via `wp_enqueue_script wppo-rum true` (footer, no `defer`/`async` by default, but `wp_enqueue_scripts` with `true` footer should be `defer` if WP 6.3+? Check `class-rum.php:143` `$deps=[] $version=WPPO_VERSION true` — enqueued as footer script, may block parser until footer). | RUM script is ~3 KB; 3 PerformanceObservers add ~1 % CPU overhead per page (LCP/CLS/INP tracking is native). `sendBeacon` fallback `fetch omit` may hold connection. Non-critical but runs on every public page when `rum_enabled` true (opt-in). | Mark `wppo-rum` as `defer`/`async` via `script_loader_tag` filter or `wp_enqueue_script` with `'strategy'=>'defer'` (WP 6.3+). Add `strategy=defer` to `wp_enqueue_script` call. | **LOW** |
- | P-FE-08 | **LOW** | `src/components/Dashboard.js:75-88,805-830` | `allSuggestions useMemo([telemetrySuggestions, pagespeedSuggestions])` builds `seen Set` + `merged[]` per render; `dbOverheadCount useMemo([dbCounts])` `Object.values.reduce`. Lightweight. `WelcomePanel` etc. are static. Bundle size: `src/App.js` imports `FontAwesomeIcon` + 12 icons statically (not dynamic) — icons bundled in `build/index.js` main chunk (≈150 KB including `react`, `wp-element`). `lazy` 7 tab chunks each ~20–50 KB. Total admin SPA ~500 KB transferred (gzip ~120 KB). | Gzip 120 KB SPI still ~200 ms parse on mobile 3G. Icons imported statically in `App.js` (faTachometerAlt…faSpinner 13 icons) + `Dashboard.js` 6 icons → duplication across chunks? `FontAwesome` tree-shaking should deduplicate. | Move icon imports to per-tab lazy chunks or use SVG sprites instead of FA React (smaller). Current `build/index.js` likely >200 KB — consider `dynamic import('@fortawesome…')`. | **INFO** |
- | P-CACHE-05 | **MEDIUM** | `includes/class-pagespeed.php:291-313` + `includes/class-telemetry.php:69-76` | Transient key scheme `Util::transient_key('wppo_pagespeed_'+md5(url)+'_'+strategy)` and `wppo_audit_+md5(url)` share the same `wppo_transient_index` (used by `Telemetry`, `Pagespeed` both via `register_transient_key`). `register_transient_key` writes `get_option('wppo_transient_index')` + `update_option` **per scan** (telemetry) and `store_failure` also registers. `wppo_transient_index` caps at 200 via `prune stale by expiry` + `array_slice -200`. But `record_trend` uses **option** `wppo_web_vitals_trends_lock` via `add_option` atomic (60 s TTL) — good. However `Telemetry::register_transient_key` does **no lock**— concurrent `scan` from two admins for same URL does `get_option`+modify+`update_option` race (lost update but harmless, just index). | `register_transient_key` per `scan`/`run_scan` does 1 `get_option`+`update_option` per scan completion — cheap vs `wp_remote_get` but still 2× options writes per manual audit. On a site where audit is run often (auto-rescan daily via cron `web_vitals_rescan_cron` queues 2×urls×strategies = up to 40 `queue_scan` which each `run_scan` does `set_transient`+`register_transient_key`) → 40× `update_option(wppo_transient_index)` per day (batch writes). Could be coalesced. | Already capped 200 and pruned; acceptable. Consider moving `wppo_transient_index` to `wp_cache` with `add/append` instead of `option` writes for transient-tracked keys. | **LOW** |
- | P-CACHE-07 | **LOW** | `includes/class-object-cache.php:86-185` | `Object_Cache::get_status` checks `file_exists(dropin_path)` + `is_readable` + `filesize <1MB` then `get_contents` via `WP_Filesystem` or `file_get_contents` fallback to detect `DROPIN_MARKER`. Then `get_option('wppo_settings')['object_cache']` + fallback `include(config_path)` then `connect_internal` which actually **opens Redis TCP connection** (`wppo_redis_connect` → `Redis::connect(timeout 1 s)`). Called from REST `object_cache status` and `System_Info::get_litespeed()->Object_Cache` branch. `get_status` does **real TCP connect** per call, not cached. | Each `object_cache status` REST poll (admin SPA tab `objectCache`) or `SystemInfo` panel mount triggers `get_status` which opens a new Redis connection + `info()` + `close()` — ~5–10 ms TCP + `INFO` (holds Redis). If SPA auto-polls on tab switches (no), but manual refresh can spam connections. `System_Info::get_litespeed` also may instantiate `Object_Cache` and call `get_status` → extra connection from dashboard health. | Cache `get_status` result for 30 s in a static or `get_transient(Util::transient_key('wppo_object_cache_status'))` 60 s, especially the Redis `info()` branch. Already `is_readable`+marker check could be memoized per request via static. | **MEDIUM** |
- | P-CACHE-08 | **LOW** | `includes/class-main.php:419-484,806-819` | `maybe_run_version_upgrade` runs on `admin_init`+`upgrader_process_complete` when `get_option('wppo_version') < WPPO_VERSION` then `Advanced_Cache_Handler::create()` (`file_get_contents`+`var_export`+`file_put_contents` `advanced-cache.php`) + `Cache::clear_cache()` (recursive `delete` of `cache/{domain}` + min dir + legacy dirs). The 4 `maybe_*` upgrades (`maybe_fix_wp_cache`, `maybe_run_upgrades`, `maybe_run_version_upgrade`, `maybe_migrate_block_assets_setting`) each do `get_option` checks and run **on every `admin_init`** (even on AJAX? Actually `admin_init` fires on admin only, but `DOING_AJAX` also is admin). With 4 handlers, that's 4× `get_option` per admin request until migrations complete (then early returns). | After initial install, `get_option('wppo_version')` matches and version upgrade short-circuits (`version_compare >=` line 919 → return). `maybe_fix_wp_cache` throttled by 1 h `get_transient`. Net extra cost after warm =2× `get_option`+`has_filter` per admin request (negligible). | No fix; throttles and gates are already sound. Note `add_wp_cache_constant` file check (`is_readable wp-config`) only when not throttled — good. | **INFO** |
- **OPTIMIZATION: 1 | HIGH: 8 | MEDIUM: 12 | LOW: 6 | INFO: 4**
- 9. (**LOW, P-WP-09/P-FE-07**) Replace `Util::generate_preload_link` `wp_kses` with direct `echo` (10× per page); mark `wppo-rum` script `strategy=defer`.

### agent-A10-duplication-deadcode.md

- **Instruction:** Find exact duplicates, near-duplicates, repeated conditions, repeated queries, repeated validation, repeated cache logic, repeated permission checks, repeated hook registrations, similar helpers, copy-pasted functionality, multiple impls of same behaviour; and unused functions/classes/methods/variables/constants/hooks/assets/dependencies/dead branches/unreachable code/commented-out. For each duplicate determine consolidate/abstract/remove/keep. For each dead trace references before declaring unused. Classify DUPLICATE/DEAD CODE/LOW/INFO with file:line evidence, impact, recommendation, confidence. Do not modify production code. Search with grep/rg for duplicates.
- | D-04 | **DUPLICATE** | `update_settings` validation duplicated between `Rest` and `WPPO_CLI_Command` | `includes/class-rest.php:417-481` (`allowed_tabs`, `sanitize_settings_recursively`, password→`password_set`, preservation of `pagespeed_api_key` / `server_timing_enabled` / `auto_rescan`) vs `includes/class-wppo-cli-command.php:556-680` (`settings` subcommand: same `allowed_tabs`, delegates to `Util::sanitize_settings_recursively`, same password flag logic) | Both endpoints maintain their own `allowed_tabs` whitelist and the `object_cache.password → password_set` stripping logic. Drift risk when a new tab is added (`litespeed_integration`, `llms_txt` already required two-file edits). | **Medium.** New settings tabs require two whitelists to be updated; a forgotten CLI entry would allow stale tab values via CLI while REST rejects them (or vice-versa). | **Consolidate**: extract `ALLOWED_SETTINGS_TABS` constant + `sanitize_and_normalise_settings_tab($tab,$settings)` helper to `Util` or a small `Settings_Validator` class, and have both `Rest::update_settings` and `WPPO_CLI_Command::settings` call it. | High |
- | D-05 | **DUPLICATE** | `ALLOWED_IMPORT_KEYS` / `allowed_keys` whitelists triplicated | `src/components/PluginSetting.js:22-32`, `includes/class-rest.php:713-723` (`import_settings`), `includes/class-rest.php:423-433` (`update_settings`) | Three identical allowlists of top-level settings keys (`file_optimisation`, `preload_settings`, `image_optimisation`, `database_cleanup`, `object_cache`, `performance_audit`, `cache_settings`, `litespeed_integration`, `llms_txt`). JS copy is for client-side UX, two PHP copies for two REST endpoints. | **Low.** Same drift risk as D-04; adding a new settings group requires three edits. | **Consolidate** PHP side: single `Util::ALLOWED_SETTINGS_KEYS` / `Util::get_allowed_settings_keys()` used by both REST methods; JS side keep its copy but generate from same source at build or derive from `wppoSettings` shape if feasible. | High |
- | X-01 | **LOW** | `Database_Cleanup::clean_revisions()` (simple `LIMIT 1000` variant) is dead code — never called by `clean_all` or REST/CLI | `includes/class-database-cleanup.php:86-130` | Defined but not referenced by `clean_all` (`788-798` uses `clean_revisions_advanced`), not referenced by `Rest::database_cleanup` (`method_map` at `848` points to `clean_revisions_advanced`), not referenced by `WPPO_CLI_Command` (`234` calls `clean_revisions()` — **actually, CLI does call it** — re-check). **Trace:** `grep -rn clean_revisions includes/` shows `class-wppo-cli-command.php:234` does call `clean_revisions()` (simple variant) when `dbRevMaxAge` path not used? Re-read: CLI `database` subcommand at `:234` does `Database_Cleanup::clean_revisions()` for the non-advanced path. So not dead — CLI uses it. Downgrade. | **Info.** Method is still reachable via CLI `wppo database clean --type=revisions` ? Verify CLI's `$method_map` vs direct calls. | **Keep** — CLI still uses simple variant as fallback; not dead. Add comment clarifying advanced vs simple path. | Medium |
- | X-04 | **LOW** | `src/css/abstracts/_mixins.scss:20-24` (`flex-center`) and `:26-30` (`truncate`) — defined but not `@include`d anywhere | `src/css/abstracts/_mixins.scss:20-30` | `grep -rn "flex-center\|truncate" src/css/` returns only definitions, no usages. Both mixins are dead code. | **Low.** Unused SCSS increases bundle negligible but clutters API. | **Remove** or keep as library mixins with comment “for future use”. Verify via `rg "truncate|flex-center" src/`. | High |
- | X-05 | **LOW** | JS `src/lib/litespeed.js` — helpers may be unused if imported only in one place | `src/lib/litespeed.js` | `grep -rn "from.*litespeed\|import.*litespeed" src/` shows only one consumer (`FileOptimization.js` via `wppoSettings.litespeed`). Check if module is tree-shaken / actually needed. | **Info.** If only one consumer, not dead but single-use. | **Keep** if consumed; verify import graph. | Low |
- | X-09 | **LOW** | `includes/minify/class-css.php` vs `includes/minify/class-js.php` vs `MatthiasMullie\Minify` — two minifier stacks | `includes/minify/class-css.php`, `includes/minify/class-js.php`, `composer.json` (`matthiasmullie/minify` + `voku/html-min`) | Plugin ships both a local CSS/JS minifier wrapper and a vendor minifier; `Cache::combine_css` uses `MatthiasMullie\Minify\CSS` while `Main::minify_queued_styles` / `minify_css` uses `Minify\CSS`. Not dead but dual implementation. | **Low.** Two minification paths for same language (CSS) maintenance burden. | **Keep** if they serve different contexts (combined-file vs per-handle inline budget); document why two paths exist. | Low |
- | X-11 | **LOW** | `performance-optimisation.php` defines `WPPO_VERSION` etc. but `includes/class-activate.php` re-reads plugin header for version | `performance-optimisation.php:20-30` vs `includes/class-activate.php` | Both derive version; not dead, just dual source. | **Info.** Keep single source (`WPPO_VERSION` constant). | **Keep**. | Low |
- | X-13 | **LOW** | `src/App.js` `TabFallback` is a tiny component defined but its usage is trivial | `src/App.js:40-45` | Defined and used in every `Suspense fallback` — not dead. | **None.** | **Keep.** | High |

### agent-A11-compatibility.md

- **Instruction:** Classify findings CRITICAL/HIGH/MEDIUM/LOW/INFO with `file:line` evidence, impact, recommendation, confidence. Do not modify production code.
- | C-04 | **LOW** | Transient-isolation: `uninstall.php` uses inline `is_multisite() ? get_current_blog_id().'_' : ''` prefix instead of `Util::transient_key()` — consistent now but diverges from canonical helper's `try/catch` + `function_exists` hardening | `uninstall.php:93` `$transient_prefix = is_multisite() ? get_current_blog_id() . '_' : ''`, `includes/class-util.php:509-518` `transient_key()` with `!function_exists('is_multisite')` guard and `try/catch \Throwable` | `uninstall.php` runs in `WP_UNINSTALL_PLUGIN` context where `is_multisite()` may be mocked or throw under Brain Monkey / early uninstall; the inline pattern lacks the defensive `function_exists`/`try` that `Util::transient_key` provides (added for Redis shared-cache safety). Deletion set is finite (`wppo_activation_notices`, `wppo_show_welcome_notice`, `wppo_cache_size`, `wppo_total_js_css`, `wppo_wp_cache_fix_checked`) and matches `Main::maybe_fix_wp_cache` etc., so no collision today. | If a persistent object cache still holds a prefixed transient at uninstall on a flaky multisite mock, the non-prefixed delete misses it, leaving stale data. Negligible because uninstall iterates `switch_to_blog` over all sites. | Replace inline prefix in `uninstall.php:93-98` with `PerformanceOptimise\Inc\Util::transient_key()` (or duplicate its `function_exists` guard) for single-source hardening. Alternately document that inline form is intentional for uninstall without autoloader. | High |
- | C-05 | **LOW** | `templates/object-cache.php:69` `blog_prefix = (is_multisite() ? get_current_blog_id() : $table_prefix).':'` — mixes `get_current_blog_id()` with `$table_prefix` on single-site, while WP core memcached uses `table_prefix` consistently on multisite too | `templates/object-cache.php:69`, `vendor/php-stubs...` `get_blog_prefix` | On single-site `$table_prefix` is typically `wp_` → prefix `wp_:`; on multisite it's `1:`. This matches WP's `WP_Object_Cache` expectation and AGENTS.md's `blog_prefix` note. However some multisite installs with custom `table_prefix` per site (HyperDB) rely on `$table_prefix` isolation; using `blog_id` alone conflates `blog_id` reuse across networks? In practice WP core also uses `blog_prefix` via `get_current_blog_id()` for cache key prefix, so this is canonical. No cross-site leak because `WP_CONTENT_DIR . '/wppo-redis-config.php'` is shared — but `blog_prefix` still isolates. | No functional leak; the `:` suffix prevents `1:` matching `10:` (`strpos` not used; prefix is concatenated). Minor documentation gap: reason for ternary not commented. | Add a one-line comment referencing `AGENTS.md` / `Multisite compatibility` explaining the ternary choice and that Redis SCAN flush at `object-cache.php:536-537` uses `$this->blog_prefix` consistently. No code change. | Medium |
- | C-08 | **LOW** | `phpcs.xml` `testVersion 8.2-` vs `composer.json` `platform 8.2` is consistent, but `PHPCompatibility` standard without explicit `testVersion` in CI may not run on 7.2 to verify back-compat of header-declared `Requires at least: 6.2` | `phpcs.xml:13` `<config name="testVersion" value="8.2-"/>`, `.github/workflows/psalm-wpcs-check.yml` (not read but per AGENTS.md runs `parallel-lint` PHP 8.2-8.5) | `testVersion 8.2-` tells `PHPCompatibility` to flag usage incompatible with 8.2+. It does *not* verify compatibility with older WP/PHP — and with `Requires PHP: 8.2` the plugin intentionally drops older PHP. That's correct per declaration, but `psalm-wpcs-check.yml` running `parallel-lint` on 8.2-8.5 without also linting on `wordpress:6.2` + `php:8.2` runtime may miss that `wp_load_classic_theme_block_styles_on_demand` etc. are gated. | CI green does not guarantee WP 6.2 runtime; but manual `function_exists` gates cover it. | Keep `testVersion 8.2-` and add a CI job `wp-env` against `WP 6.2` + `PHP 8.2` (minimum matrix) to smoke-test that all 25 REST routes + cache still boot without the 6.9/7.1 APIs. | Medium |
- | C-09 | **LOW** | `Util::transient_key()` `try/catch \Throwable` around `is_multisite()` + `get_current_blog_id()` is exceptionally defensive — but `get_current_blog_id()` itself can return `0` early in `wp-settings.php` before multisite is bootstrapped, causing transient key `0_wppo_...` at boot | `includes/class-util.php:513-515` `is_multisite() ? (string)get_current_blog_id().'_' : $key` inside `try` | Drop-in `templates/object-cache.php:69` runs before plugins bootstrap; `Util::transient_key` is never called that early. However `class-activate.php:70` and `class-main.php:834-841` call `transient_key` on `admin_init` / `init` where `get_current_blog_id()` is valid. Early-boot transient access via `Main::maybe_fix_wp_cache` on `admin_init` is safe. The `0_` prefix would only affect a transient written before `ms_settings.php` — not a real path. | Transient collision if a plugin called `Util::transient_key` during `muplugins_loaded` before blog ID is set — isolated to that early window. | Consider asserting `get_current_blog_id() > 0` before prefixing, falling back to unprefixed key if `0`. Or document that `transient_key` assumes `after_setup_theme`. Current `try/catch` already falls back to raw key on `Throwable`, but `0` is not a `Throwable`. | Medium |
- | C-11 | **LOW** | wordpress.org compliance: `build/` committed but `.distignore` anchors may silently drop future `build/tab-*.js` chunks if Webpack hashing changes | `.distignore:2-7` `/.git/` `/.github/` etc. never mention `build`, `build/index.asset.php`, `package.json:7` `wp-scripts build src/index.js src/lazyload.js src/main.js src/rum.js` | `.distignore` does *not* list `/build` — so `build/` is shipped (correct per `Build` section: “Build output is committed”). The file also excludes `vendor/squizlabs` etc. separately. Verify: `build/` appears in repo and not in `.distignore`. `scripts/build-release.sh` uses `.distignore` patterns to create zip; since `build/` is absent from `.distignore` it is included. | If a future `.distignore` adds `/build` by mistake, release would ship without JS and plugin would white-screen. Current state is correct; risk is accidental addition. | Add a comment line `# KEEP build/ — plugin ships committed build output` above `.distignore:42` `# Keep build/`, and add a CI check `test -f build/index.js` in `webpack.yml` (already does `npm run build`). | High |
- | C-12 | **LOW** | `composer.json` `autoload.classmap includes/` will generate a classmap for all `includes/*.php` — but the plugin is documented as “manually loaded via `Main::includes()` + `vendor/autoload.php` (Composer for vendor packages only, no PSR-4 autoload for plugin classes)” — classmap is redundant and may autoload classes before `ABSPATH` guard runs | `composer.json:19-23` `autoload.classmap includes/`, `performance-optimisation.php:41` `require_once vendor/autoload.php`, `includes/class-main.php:388-408` `includes()` | Classmap means `new Main()` at `performance-optimisation.php:44` *would* autoload `PerformanceOptimise\Inc\Main` even without `Main::includes()` requiring the file — but `Main::includes()` still re-requires `class-server-rules.php` etc. The `if (!class_exists('PerformanceOptimise\\Inc\\*'))` guard at top of every file prevents double-declare. No functional break; but `composer dump-autoload --optimize` on 8.2 will pre-parse all 25 files — if any file uses syntax unsupported on the *build* PHP it could fatal at autoload generation (unlikely with `platform 8.2`). | Extra autoload map increases `vendor/composer/autoload_classmap.php` size (~25 entries) for no benefit; may mask a missing `require_once` during refactors. | Either remove the `classmap` entry and rely solely on manual requires (as `AGENTS.md` documents), or keep it but remove the manual `require_once` for files that are already classmapped. Document the chosen strategy in `AGENTS.md` `Architecture`. | Medium |
- | C-14 | **LOW** | Caching-plugin detection only covers `advanced-cache.php` / `object-cache.php` drop-ins, not runtime object-cache collision via `wp_cache_set` — multisite with Redis `WP_Object_Cache` from another plugin (e.g. “Redis Object Cache” by Till Krüss) will be overwritten by `Object_Cache::enable()` if user enables WPPO Redis while foreign drop-in exists — but `get_status:107-110` `foreign_dropin` check blocks `enable` with `WP_Error foreign_dropin` | `includes/class-object-cache.php:257-260`, `includes/class-advanced-cache-handler.php:83-110` | `enable()` correctly returns `foreign_dropin` error instead of overwriting — safe. `uninstall.php:80-86` and `deactivate.php:50-56` guard deletion behind marker check. Runtime coexistence with full-page cache plugins (WP Rocket, LSCWP) is handled via `Admin_Notices` warning and `LiteSpeed_Integration::should_disable_wppo_optimizer()` gate that respects `litespeed_can_optm` filter — good. Missing: a warning for `WP Super Cache` / `W3TC` style `CACHE` constants beyond `WP_CACHE`. | No overwrite happens; but the admin notice for competing full-page cache may not cover `WP_CACHE` false-negative where host sets `WP_CACHE` true but serves via Varnish — not a plugin bug. | Keep gates; consider expanding `Admin_Notices` competing-cache detection to also check `defined('W3TC_DIR')` / `WP_CACHE` constant + known constants. | Medium |
- | C-15 | **LOW** | `uninstall.php:142-165` multisite iteration uses `get_sites(['number'=>100,'offset'=>...])` + `switch_to_blog` — correct but `WP_CONTENT_DIR . '/cache/wppo'` and `WP_CONTENT_DIR . '/wppo'` directory deletions are not multisite-isolated by domain, so uninstall on one site deletes cache for all sites (acceptable because uninstall means plugin is removed network-wide) | `uninstall.php:57-63` `$cache_dir = WP_CONTENT_DIR.'/cache/wppo/'`, `62` `$wppo_dir = WP_CONTENT_DIR.'/wppo/'`, `138-165` loop | Domain-based static cache (`{domain}/{path}/index.html`) is shared; `wppo/` converted images are shared. Deleting them once per site would be wasteful; current code deletes them `wppo_cleanup_site()` on every blog iteration — idempotent but does repeated `scandir` + `unlink`. On 1000-site network that's 1000× directory walks. | High-site networks experience slow uninstall (timeouts). Data loss is not an issue because uninstall is intentional wipe. | Guard directory deletion to run only once (first site) or move it outside `switch_to_blog` loop. Alternatively keep as-is and document that repeated deletes are safe/idempotent. | Medium |
- **Verdict:** No CRITICAL compatibility regression. One HIGH (external-services disclosure), remainder MEDIUM/LOW/INFO. Multisite isolation (`transient_key`, `blog_prefix`, domain-based cache, `min_cache_dir` per-blog) is exemplarily implemented; WP version gates for 6.3/6.7/6.9/7.0/7.1 APIs are exhaustive with `function_exists`; object-cache drop-in, hosting (Apache/Nginx/LiteSpeed/OLS), and portability checks are solid. The plugin is correctly declared as PHP 8.2 / WP 6.2 minimum with committed `build/` and no obfuscated code; remaining work is docs/compliance polish.

### agent-A12-quality-architecture.md

- **Instruction:** Check responsibilities, abstractions, coupling, global state, dependency management, lifecycle, naming, overly complex functions, god classes, circular deps, testability, repeated logic, inconsistent patterns, error handling, edge cases, coding standards. Classify CRITICAL/HIGH/MEDIUM/LOW/INFO/OPTIMIZATION/DUPLICATE/DEAD CODE with file:line evidence, impact, recommendation, confidence. Do not modify production code. Be evidence-based, do not invent unnecessary abstraction.
- | A-14 | **LOW** | Naming inconsistencies: package vs slug vs prefix vs textdomain | `composer.json:2` (`name: "performance-optimisation"` hyphen), `performance-optimisation.php:3` (`Plugin Name: Performance Optimisation`), `includes/class-main.php:13` (`package PerformanceOptimise\Inc` — missing `i`), `performance-optimisation.php:18` (`Text Domain: performance-optimisation`), JS `import PerformanceAudit from './PerformanceAudit'` (camelCase), CSS `.wppo-` (4-char), PHP `wppo_` (option `wppo_settings`, transient `wppo_cache_size`, hook `wppo_page_cron_hook`, filter `wppo_exclude_defer_js`), constants `WPPO_VERSION`/`WPPO_PLUGIN_PATH` | Three spellings: `performance-optimisation` (slug, composer, textdomain), `PerformanceOptimise` (namespace, missing `i` vs `PerformanceOptimisation`), `wppo` (prefix). Namespace typo `PerformanceOptimise` (no `i`) is inconsistent with plugin slug `performance-optimisation` (with `i`). Not a bug (autoload is manual) but confusing for contributors. JS components use PascalCase, PHP classes `Main`/`Cache` generic names that collide with common nouns. | **Low.** Confuses `grep` (must search both `performance-optimisation` and `performance-optimise`). Namespace typo would break if PSR-4 were ever enabled. Generic class names `Cache`/`Main` risk collision with other plugins in global `class_exists` check (guarded by namespace, so low risk). | **Keep, document, add alias.** Do not rename namespace (breaking). Add `composer.json: { "autoload": { "psr-4": { "PerformanceOptimise\\Inc\\": "includes/" } } }` plus `class_alias` for `PerformanceOptimisation\Inc\Main` → `PerformanceOptimise\Inc\Main` so both spellings work. Lint new classes against generic names (prefer `Wppo_Cache` if ever adding new). | High |
- | A-15 | **LOW** | Overly complex functions (McCabe >15, 80+ lines) | `includes/class-cache.php:365` (`combine_css` 193 lines, 6 nested loops, 4 early returns, 2 branches for cached vs fresh, inline-budget check), `includes/class-cache.php:800` (`core_inline_budget_will_inline` 78 lines, double simulation, `uasort`, cumulative budget), `includes/class-image-optimisation.php:576` (`maybe_serve_next_gen_images` 126 lines, Tag Processor vs regex dual branches), `includes/class-image-optimisation.php:1368` (`process_img_tag` ~250 lines, Native lazy vs JS lazy vs excluded, 3 `WP_HTML_Tag_Processor` passes), `includes/class-main.php:946` (`on_settings_update` 92 lines, 6 tab comparisons + htaccess + nextgen + google-fonts), `includes/class-cron.php:473` (`get_sitemap_urls` 71 lines, `while to_fetch`, deadline, index vs leaf branching), `includes/class-util.php:605` (`sanitize_settings_recursively` 38 lines, 6 `if/elseif` heuristic branches), `src/components/FileOptimization.js:2024` (single file 2024 lines, `render` with 5 conditional sub-tabs), `src/lazyload.js:1025` (1025 lines, `initLazy` + `observerCallback` + `mutationCallback` + `delayLoad` with 3 strategies) | **Medium.** `combine_css` + `process_img_tag` are hardest to review; bug in one skip rule must be mirrored in `get_combined_handles` (already flagged D-12). `sanitize_settings_recursively` heuristic `stripos('url'|'cdn'|'origin')` could misclassify a future `excludeUrlsThatAreNotReallyUrls`. `lazyload.js` 1025 lines vanilla JS with no tests for idle/viewport branches. | Extract helpers (already partially done: `get_combined_handles`, `is_core_block_asset`, `get_styles_inline_limit`). No further split needed beyond A-01/A-02 facades. Add complexity lint (e.g. `phpmd` cyclomatic) to keep new functions <15. | Medium |
- | Q-08 | **LOW** | `Database_Cleanup` batched `LIMIT 1000` loop duplicates error handling (repeated logic) | `includes/class-database-cleanup.php:86` (`clean_revisions` 44 lines: `do { $post_ids = $wpdb->get_col("SELECT … LIMIT 1000"); if ($wpdb->last_error) return false; if (empty) break; $placeholders=implode(',',array_fill(…)); $wpdb->query("DELETE FROM $wpdb->postmeta … IN ($placeholders)"); $wpdb->query("DELETE FROM $wpdb->posts … IN ($placeholders)"); $deleted+=count } while (count>=1000)`), `267` (`clean_auto_drafts`), `321` (`clean_trashed_posts`), `373` (`clean_spam_comments`), `425` (`clean_trashed_comments`) — same 45-line scaffold, only `WHERE` varies | **Low.** 5× copy of `LIMIT 1000` batch + placeholder generation + `last_error` check. Fix to placeholder generation must be applied 5 times (already flagged as D-07 in A10). Also `clean_expired_transients`/`clean_orphan_postmeta` have bespoke logic not batched. | Keep 5 copies (each has distinct table/where) or extract `batch_delete_by_ids(string $select_sql, string $meta_delete_sql?, string $rows_delete_sql): int|false` helper that takes table/where fragment. Low priority. | Medium |
- | Q-09 | **LOW** | `Main::setup_hooks` registers `wp_resource_hints` + `add_preload_prefetch_preconnect` both for preconnect (inconsistent pattern) | `includes/class-main.php:672` (`add_action('wp_head', add_preload_prefetch_preconnect,1)` + `add_action('wp_head', add_speculation_rules,0)` + `add_filter('wp_resource_hints',10,2)`) vs doc at `672-674` ("keep preload vs resource-hints separation (preload needs `as`/`type`, hints need core dedup)") | `add_preload_prefetch_preconnect` handles `preload fonts/CSS` via `Util::generate_preload_link` (echo), while `add_resource_hints` handles `preconnect`/`dns-prefetch` via `wp_resource_hints` filter. Separation is correct per doc, but reader must understand why `preconnectOrigins` are not in `Util::generate_preload_link`. `wppo_resource_hints` not used. | **Info.** No bug; pattern is intentional. Ensure future `preconnect` added via `wp_resource_hints` not via `generate_preload_link` (which lacks dedup). | Keep with doc; add comment in `add_preload_prefetch_preconnect` "preconnect here is for legacy; prefer wp_resource_hints for new preconnect". | High |
- | Q-10 | **LOW** | `Rest` `VALID_TYPES` + `METHOD_MAP` + `TABLE_MAP` triplication (repeated logic / inconsistent patterns) | `includes/class-rest.php:796` (`$valid_types = ['revisions','auto_drafts','trashed_posts','spam_comments','trashed_comments','expired_transients','orphan_postmeta','unattached_media','oembed_cache','all']`), `847` (`$method_map 9 entries: revisions→clean_revisions_advanced … oembed_cache→clean_oembed_cache`), `includes/class-database-cleanup.php:788` (`$methods` in `clean_all` same 9), `843` (`$methods` in `auto_clean` 7 entries, omits `unattached_media`/`oembed_cache` without comment), `includes/class-abilities.php:466` (same map) | Adding new cleanup type (`NEXT`: `unattached_media`, `oembed_cache`) required editing 4 maps (Rest valid + Rest method + DB clean_all + Abilities). `auto_clean` omits 2 types intentionally? Not documented why daily auto-clean excludes unattached media/oembed cache while `clean_all` includes. `Database_Cleanup::TABLE_MAP`/`METHOD_TO_TYPE` are single-source but not used by Rest/Abilities. | **Low.** Drift if new type added to one map not others (CLI `database` subcommand at `class-wppo-cli-command.php:234` uses simple `clean_revisions` not `clean_revisions_advanced` — intentional?). | **Single source.** Expose `Database_Cleanup::ALL_METHODS`/`VALID_TYPES` constants and have `Rest`/`Abilities`/`WPPO_CLI_Command` derive via `Database_Cleanup::get_method_map()`; document why `auto_clean` excludes 2 types or include them. | Medium |
- | Q-11 | **LOW** | `Performance-optimisation.php` hard-codes `WPPO_VERSION '1.9.0'` duplicate of header (naming / versioning) | `performance-optimisation.php:7` (`Version: 1.9.0` header) vs `28` (`define('WPPO_VERSION','1.9.0')`) + `includes/class-activate.php` re-reads plugin header via `get_file_data` for version? Actually `class-activate.php:??` uses `WPPO_VERSION` constant, not header. `class-main.php:911` (`maybe_run_version_upgrade` compares `get_option('wppo_version','')` vs `WPPO_VERSION`) | Header bump without constant bump → upgrade gate `version_compare(installed, WPPO_VERSION, '>=')` stale; `wppo_version` option never advances despite new code. Already flagged in A01 P02 as INFO. Plugin ships with `Tested up to: 7.1` non-existent WP version (A01 P01). | **Low.** Version drift not caught by CI (no check). | **Source `WPPO_VERSION` from header via `get_file_data(__FILE__)` once, or generate at `npm run build`/`composer release`. Add CI check that `WPPO_VERSION` === header `Version`. | High |
- | Q-12 | **LOW** | CSS/SCSS design tokens partially duplicated (inconsistent pattern) | `src/css/base/_base.scss:27-43` (`.wppo-mt-8`…`wppo-mb-24`, `.wppo-text-small`/`wppo-text-13` both `font-size:13px`), `src/css/components/_card.scss:92` (`.wppo-danger-zone { border-left:4px solid #ef4444; background:#fef2f2 }`) vs `src/components/PluginSetting.js:880` (`style={{borderLeft:'4px solid #ef4444', background:'#fef2f2'}}` hard-coded hex instead of `var(--wppo-danger)`/`var(--wppo-error-bg)`), `src/css/abstracts/_mixins.scss:20-30` (`flex-center`/`truncate` defined but never `@include`d) | `wppo-text-small` vs `wppo-text-13` exact duplicate (13px). Inline `style` bypasses `.wppo-danger-zone` class (defined but not used where it should be). `flex-center`/`truncate` dead mixins. Already flagged X-04/X-06 in A10. Not a behaviour bug but design-system inconsistency. | Replace hard-coded `#fef2f2`/`#ef4444` with `className="wppo-danger-zone"` and remove inline `style` at `PluginSetting.js:880`; deprecate `wppo-text-13` alias or keep with comment. Remove or keep `flex-center`/`truncate` as library (document "for future use"). | Medium |
- | Q-13 | **LOW** | JS `handleChange(setSettings)` factory wiring repeated per field (repeated logic) | `src/lib/util.js:1-33` (`handleChange` factory) used as `onChange={handleChange(setSettings)}` in `src/components/FileOptimization.js` (~15 fields), `ImageOptimization.js`, `DatabaseCleanup.js`, `PreloadSettings.js`, `ObjectCache.js`, `PluginSetting.js` each repeats same import + wiring | Factory is DRY, but per-field wiring `onChange={handleChange(setSettings)}` ×15 in FileOptimization is boilerplate. Not a bug. Common components `SwitchField`/`CheckboxOption` already wrap field UX. | **Info.** Keep factory; per-field wiring is idiomatic. Optional: `useFormState(initial)` hook that returns `[settings, handleChange, setSettings]` would halve boilerplate but not load-bearing. | High |
- | Q-14 | **LOW** | `Cache::count_cached_pages` counts only `index.html` exact, misses variants (edge case) | `includes/class-cache.php:2165-2185` (`count_cached_pages` iterates `dirlist` and `if ('index.html' === file['name']) count++`, not `index-{hash}.html` variants nor `index.html.gz/br`) | When `enableLoggedInCache` on, `index-{hash}.html` variants not counted in `get_cache_stats()['cached_pages']` used by Dashboard stat tile (shows fewer pages than exist). Gzip/br siblings intentionally not counted (they are encodings, not pages) — correct. | **Info.** Dashboard `Cache Size` vs `Cached Pages` mismatch for logged-in cache. | Count `preg_match('/^index(-[a-f0-9]{12})?\.html$/', name)` for `index*.html`. Keep gzip/br excluded. | Medium |
- | Q-15 | **LOW** | `uninstall.php` misses newer option keys (incomplete cleanup) | `uninstall.php:32-48` (deletes 16 options explicitly) but does not delete `wppo_web_vitals_trends`, `wppo_pagespeed_*` transients, `wppo_used_css_*`, `wppo_ccss_*`, `wppo_front_page_lcp_*`, `wppo_block_assets_migrated`, `wppo_web_vitals_last_rescan`, `wppo_last_db_cleanup`, `wppo_preload_cron_offset` etc. introduced in `class-pagespeed.php`/`class-used-css.php`/`class-cron.php`/`class-critical-css.php` | Leaves orphaned `wp_options` rows (autoload `no` but still bloats). `delete_option('wppo_settings')` already clears main option; remaining `wppo_*` options not deleted. Transient sweep at `93` handles transients with `blog_id_` prefix but not options. | Keep explicit list but audit `get_option`/`set_transient`/`update_option` keys across `includes/` and add to cleanup; or iterate `LIKE 'wppo_%'` (but `LIKE` delete is heavy on large multisite). Prefer explicit list + comment referencing `grep -rn wppo_ includes/`. | Medium |


> Source agents: agent-A01-php-correctness.md, agent-A02-php-media.md, agent-A03-php-infra.md, agent-A04-php-rest-cli.md, agent-A05-js-spa.md, agent-A06-js-vanilla.md, agent-A07-css.md, agent-A08-security.md, agent-A09-performance.md, agent-A10-duplication-deadcode.md, agent-A11-compatibility.md, agent-A12-quality-architecture.md — full evidence in each agent file.
