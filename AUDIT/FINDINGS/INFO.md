# FINDINGS — INFO

_Consolidated from 12 agents (2286 lines). Each finding below is traceable to `AUDIT/AGENTS/agent-*.md` file:line evidence. This shard groups by severity INFO._

## Index

### agent-A01-php-correctness.md

- > Severities: `CRITICAL` > `HIGH` > `MEDIUM` > `LOW` > `INFO` > `OPTIMIZATION` > `DUPLICATE` > `DEAD CODE`
- | P02 | INFO | `performance-optimisation.php:28-38` | constants | `if ( ! defined( 'WPPO_PLUGIN_PATH' ) ) define(...)` + `WPPO_VERSION '1.9.0'` hard-coded duplicate of header `Version: 1.9.0`. | Drift if header bumped without constant bump (cache/migrate gates use `WPPO_VERSION`). | Source `WPPO_VERSION` from header via `get_file_data()` once, or generate at release. | HIGH |
- | P04 | INFO | `performance-optimisation.php:44` | bootstrap | `new Main();` instantiated at file load, before `plugins_loaded`. | Constructor runs `get_option('wppo_settings')` + side-effects on every request type (CLI, REST, cron). | Document intentional early boot; consider `plugins_loaded` hook if lazy boot desired. | HIGH |
- | P05 | INFO | `performance-optimisation.php:46-57,59-70` | `wppo_activate` / `wppo_deactivate` | Guard `if ( ! function_exists('wppo_activate'))` — correct for duplicate inclusion, but `register_activation_hook(__FILE__, 'wppo_activate')` outside guard would fatal if guard prevented definition on second load. | Harmless currently (file loaded once), but pattern is fragile. | Keep guard + registration together, or remove guard (activation file included only once). | LOW |
- | U01 | INFO | `uninstall.php:29` | `wppo_cleanup_site()` | `$wpdb->query("DROP TABLE IF EXISTS {$table_name}")` // phpcs:ignore `PreparedSQL.InterpolatedNotPrepared` | `$table_name` is `$wpdb->prefix . 'wppo_activity_logs'` — prefix is sanitized by core, injection not exploitable, but direct interpolation violates PHPCS and hides intent. | Use `$wpdb->prepare()` with identifier or keep ignore but document prefix is core-provided. | HIGH |
- | U04 | LOW | `uninstall.php:75` | `wppo_cleanup_site()` | `strpos($content,'WPPO_ADVANCED_CACHE_DROPIN') || strpos($content,'is_user_logged_in_without_wp')` — substring match for legacy detection. | False-positive if foreign plugin legitimately contains helper `is_user_logged_in_without_wp`. | Prefer strict marker `WPPO_ADVANCED_CACHE_DROPIN` only for deletion; keep second substring as `INFO` fallback with comment. | LOW |
- | U07 | INFO | `uninstall.php:142-164` | multisite loop | `get_sites(['number'=>100,'offset'=>...])` + `has_more = count==limit`; `switch_to_blog/restore_current_blog` paired. Correct. `is_multisite() && function_exists('get_sites')` guard is belt-and-suspenders for pre-4.6. | — | No change needed. Confirms correct multisite isolation via `Util::transient_key` elsewhere. | HIGH |
- | U08 | INFO | `uninstall.php:57-63` | `wppo_cleanup_site()` | Hard-coded paths `WP_CONTENT_DIR . '/cache/wppo/'` and `'/wppo/'` with comment `must stay in sync with Cache::CACHE_DIR` — duplication noted. | Drift if `Cache::CACHE_DIR` constant changes. | Keep comment; consider defining path in shared constant file included by both (not possible at uninstall). | HIGH |
- | A09 | INFO | `advanced-cache-handler.php:88-110` | `foreign_dropin_present()` | Duplicates `exists` check already done in `is_our_dropin()`; `foreign = exists && !is_our`. | Two filesystem reads per `create()` call. | Cache result or reuse. Negligible. | HIGH |
- | H04 | INFO | `htaccess-handler.php:53-85` | `Htaccess_Handler::update_rules()` | Correctly uses `insert_with_markers($htaccess, 'wppo_rules', $rules)` — per AGENTS.md, must not touch `wordpress` or `LSCACHE` markers. Guard `function_exists('insert_with_markers')` + `require_once ABSPATH.'wp-admin/includes/misc.php'`. | Correct ordering preservation for LiteSpeed. | No change. | HIGH |
- | H05 | INFO | `htaccess-handler.php:64-76` | `Htaccess_Handler::update_rules()` | `Util::init_filesystem()` returns `WP_Filesystem` object; checks `!exists` + `is_writable(dirname)` vs `is_writable(file)`. Belt-and-suspenders for missing `.htaccess`. | Correct writable checks before `insert_with_markers`. | No change. | HIGH |
- | S05 | INFO | `server-rules.php:34-51` | `Server_Rules::get_server_type()` | `sanitize_text_field(wp_unslash($_SERVER['SERVER_SOFTWARE']))` then `strtolower` then `strpos` checks `litespeed`/`openlitespeed` before `apache`/`nginx`. Order correct (LiteSpeed often reports `Apache` as well). | Correct detection. | No change. | HIGH |
- | S06 | INFO | `server-rules.php:73-108` | `Server_Rules::get_nginx_rules()` | `gzip` rules gated on `minifyJS || minifyCSS` — non-obvious coupling; enabling minify enables Nginx gzip. Documented. | Users enabling Server Rules without minify get no gzip. | Decouple or document coupling in UI. | MEDIUM |
- | M04 | INFO | `class-main.php:419-426` | `setup_hooks()` | Registers `admin_init` hooks for `maybe_fix_wp_cache`, `maybe_run_upgrades`, `maybe_run_version_upgrade`, `maybe_migrate_block_assets_setting` — each checks caps/transients inside, so idempotent. | Correct: `admin_init` never fires on pure frontend `advanced-cache.php` hit. | No change. | HIGH |
- | M08 | INFO | `class-main.php:432-465` | `setup_hooks()` defer/delay gating | `$is_wp63_plus = version_compare($wp_version,'6.3-alpha','>=')` then branches `add_defer_strategy` (wp_enqueue_scripts 1000) vs `add_defer_attribute_legacy` (script_loader_tag) + `add_fetchpriority_to_deferred` only when `!$is_wp69_plus`. Logic mirrors `is_wp69_plus` (6.9-alpha floor for `wp_should_output_buffer_template_for_enhancement`). | Correct version gating per AGENTS.md TODO #553. Documented for removal when min WP bumps. | No change. | HIGH |
- | M12 | INFO | `class-main.php:569-614` | defer/delay setup | `excludeDeferJS`/`excludeDelayJS` parsed via `Util::process_urls()` then `apply_filters('wppo_exclude_defer_js')` etc. When both defer+delay active, `exclude_delay_js = array_merge(exclude_delay_js, exclude_defer_js)` so defer handles are never delay-rewritten — comment at 627-633 correctly explains corruption avoidance. | Correct mutual exclusion; prevents `wppo-src` overriding native `defer` strategy. | No change. Good. | HIGH |
- | M15 | INFO | `class-main.php:682-686` | critical CSS | `if (!empty(criticalCSS)) { add_action(wp_head, inline_ccss,0); add_filter(style_loader_tag, defer_stylesheets); add_action(wppo_generate_ccss, background_generate)}` — correct hooks. | — | — | HIGH |
- | M25 | INFO | `class-main.php:1466-1473` | `admin_enqueue_scripts()` | Redacts `pagespeed_api_key` and `object_cache.password` from `wppoSettings.settings` before `wp_localize_script`. | Correct PII redaction. | No change. | HIGH |
- | M29 | INFO | `class-main.php:1673-1720` | `remove_woocommerce_scripts()` | Parses `excludeUrlToKeepJSCSS` via `Util::process_urls`, compares `current_url` vs each via `Util::is_url_excluded` (handles `(.*)` suffix). Then dequeues `style:` vs `script:` prefixed handles. Correct boundary-aware base-path stripping for subdirectory installs. | Correct. | No change. | HIGH |
- | C01 | INFO | `class-cache.php:40` | `Cache::CACHE_DIR` | `private const CACHE_DIR = '/cache/wppo';` — used to build `WP_CONTENT_DIR . CACHE_DIR`. Sync comment in `uninstall.php:56`. | Drift if constant changed without updating uninstall. | Keep sync comment; consider extracting to shared constant file. | HIGH |
- | C11 | LOW | `class-cache.php:1441-1491` | `is_not_cacheable()` | Checks: empty `cache_root_dir`, empty domain, `DONOTCACHEPAGE` (with side-effect marker), `path` `..` check, `is_feed()`, sitemap regex `sitemap[^\/]*\.xml|wp-sitemap...|\.xml` (broad — any `.xml` path), `is_cart/is_checkout/is_account_page`, `cart|checkout|my-account` URI regex, `woocommerce_items_in_cart` cookies, then `pathinfo(local_url_path, EXTENSION)` (includes dot? Actually `PATHINFO_EXTENSION` returns extension without dot; non-empty means `/foo.json` etc.) → `is_404() \|\| !empty(path_info)`. | Broad `\.xml$` prevents caching for any XML endpoint (e.g. `/custom.xml`); may be intentional. Extension check `!empty(path_info)` prevents caching for `/file.pdf` but also for `/page.with.dots`? Edge. | Document broad XML exclusion; consider narrowing to sitemap only if desired. | MEDIUM |
- | C22 | INFO | `class-cache.php:1271-1354` | `maybe_apply_cdn()` | Checks `LiteSpeed_Integration::can_apply_cdn()` + `should_disable_wppo_optimizer`, then fallback `apply_filters('wppo_litespeed_can_cdn')` + `litespeed_can_cdn`. Uses `WP_HTML_Tag_Processor` (WP 6.5+), rewrites `src/href/data-src` and `srcset/data-srcset` only for `wp-content|wp-includes` URLs starting with `site_url`. Caches `site_url` regex outside loop. | Correct. Note: `preg_quote(site_url,'#')` with `site_url` containing `+` etc. — safe. | No change. | HIGH |
- Per instructions, any assigned file with **zero** findings would be marked `«No issues found after reviewing the assigned scope»`. No file qualifies — all 7 had at least one `INFO`/`LOW`:
- - `performance-optimisation.php` — 5 findings (lowest is `INFO`)
- | WOFF2 omission comment + rule | `class-htaccess-handler.php:102-103` | **INFO** | Intentional omission documented inline — not a bug. |
- | TODOs `#553`/`#624` | `class-main.php:440-443,476-484,513-524` etc. (8 sites) | **INFO** | Legacy `<6.9` buffer paths and WP 7.2 concat removal — documented for future removal when min WP bumps. |
- All other findings are `LOW` (correctness/robustness), `INFO` (design notes), `OPTIMIZATION` (CPU/I/O), `DUPLICATE` (maintainability), or `DEAD CODE`.

### agent-A02-php-media.md

- Severity legend: **CRITICAL** = exploitable / data-loss; **HIGH** = correctness / security regression; **MEDIUM** = functional bug / measurable perf / a11y; **LOW** = minor edge / style; **INFO** = observation / confirmation; **OPTIMIZATION** = perf improvement opportunity; **DUPLICATE** = copy-pasted logic; **DEAD CODE** = unreachable / never-executed.
- | A02-007 | MEDIUM | `class-used-css.php:190-249` | `extract_selectors` only sees start tags via `WP_HTML_Tag_Processor` — misses closing-tag-only content? Not bug but note JS-injected classes not captured | `WP_HTML_Tag_Processor::next_tag()` iterates tags correctly; no issue for static HTML. Dynamic classes added via JS (`.is-active`, `.open` etc.) are intentionally safelisted via `built_in_safelist`. The approach is conservative — false negatives (missing dynamic class) would purge needed rule — mitigated by safelist. INFO but document limitation. | Keep safelist; consider adding filter `wppo_used_css_safelist` for themes. Current `init_safelist` merges user `excludeUnusedCSS` but no filter hook — add one. |
- | A02-008 | MEDIUM | `class-image-optimisation.php:2390-2423` + `2831-3034` | YouTube placeholder only intercepts `youtube.com/embed` + `youtu.be/11chars` — `youtube-nocookie` variants partially, `vimeo` ignored | `get_youtube_video_id()` regex `#(?:youtube(?:-nocookie)?\.com/embed/|youtu\.be/)([A-Za-z0-9_-]{11})#` (1730) correctly handles `youtube-nocookie`. But `add_delay_load_img` scans `#<iframe\b([^>]*?)src=["\']([^"\']+)["\'][^>]*>\s*</iframe>#is` and calls placeholder only when `get_youtube_video_id` returns non-empty. Vimeo, Dailymotion, self-hosted `video` not handled — by design. Document limitation. Filter `wppo_video_placeholder_allowed` allows override. No bug. | INFO: document supported providers in readme. Optional: add Vimeo ID regex similarly. |
- | A02-021 | INFO | `class-image-optimisation.php:1368-1719` | `process_img_tag` dual-implementation verified: native vs JS-lazy correct but verbose | Both branches correctly preserve `decoding`, `fetchpriority`, `loading`, move `src→data-src`, handle placeholders via `get_placeholder_src_for_image`, inject dimensions, auto-sizes. Regex branch mirrors tag-processor semantics faithfully including `fetchpriority low` fallback for occluded. Verified line-by-line against `process_img_tag` docblock 1353-1367. No correctness issue, just maintenance burden. | INFO — no action, but note for future refactor into shared helper `apply_lazy_attributes()`. |
- | A02-022 | INFO | `class-img-converter.php:989-1095` | `get_img_path` / `get_img_url` host validation correctly blocks off-site origins | Lines 1006-1016 lowercases hosts, compares to `cached_content_url` and `cached_home_url` hosts, returns original URL unchanged for foreign hosts. Prevents rewriting CDN hotlinked images into local path traversal. `..` checks at 997 and 1038 via `rawurldecode` cover single-encoded traversal. Correct. |
- | A02-023 | INFO | `class-critical-css.php:550-578`, `582-607` | Critical CSS XSS sanitisation verified defense-in-depth | `is_safe_stylesheet_url` (550) enforces http/https + host allowlist; `sanitize_inline_css` (592) neutralises `</style` and `<script` case-insensitive then CSS-escapes `<` as `\3c `. `generate_and_store` (882) re-checks `preg_match('/<\/style|<script/i')` before caching; `inline_ccss` (941) re-applies `sanitize_inline_css` before echo with `phpcs:ignore` justified. Triple layer correct. |
- | A02-024 | INFO | `class-img-converter.php:1745-1826` | Deferred `wppo_img_info` atomic commit with MySQL GET_LOCK and merge verified correct | `update_img_info_atomic` queues shutdown hook once, stores in static `$deferred_img_info`; `commit_img_info` (1760) acquires `GET_LOCK('wppo_img_info_lock',5)`, re-reads live option, merges `pending/completed/failed` via `array_unique(array_merge(...))` then removes `completed/failed` from `pending`, merges placeholder maps via `array_merge`, releases lock. Salted cache invalidated via `update_option(SALT_KEY,time())`. Pattern prevents lost updates from concurrent image conversions. Correct for single-server MySQL; cluster DB with separate connections still races but lock mitigates. |
- | A02-026 | OPTIMIZATION | `class-img-converter.php:715-764` | Dominant-color sampling uses `sqrt(area/500)` stride — wastes work on thumbnails | For 4000×3000 (12M px) stride ~ `sqrt(12M/500)=154` → ~ 500 samples intended, but nested loops still iterate `height/stride`×`width/stride` ≈ 500, correct. For 100×100 thumb, stride=1 → 10k samples (overkill). Could cap samples to 500 distinct points for small images, but current is fine. INFO. |
- | A02-037 | INFO | `class-asset-manager.php:35-36` | `TRANSIENT_PREFIX` public const — used with `Util::transient_key` multisite isolation verified | All transient access goes via `Util::transient_key(self::TRANSIENT_PREFIX . $post_id)` (210,222) which prefixes blog_id on multisite — correct per AGENTS.md transient key isolation. Direct `get_transient` not used. |
- | A02-042 | INFO | `class-image-optimisation.php:34-51` | Constants `SVG_PLACEHOLDER_MAX_DIMENSION=4096`, `IMG_SIZE_CACHE_LIMIT=100` correctly protect memory | `generate_svg_base64` caps via `min(absint(...), MAX)`, cache evicts via `array_shift` at limit — verified. |
- | A02-044 | INFO | `class-img-converter.php:1530-1597` | `queue_unconverted_library_images` bounded query correctly indexed | `ORDER BY ID DESC LIMIT %d` (1551) uses primary key index, no filesort beyond limit. Merges mime filter correctly, checks convertible extensions via filter, skips existing next-gen via `file_exists(get_img_path(..))`. Good performance, no N+1. |
- | A02-046 | INFO | `class-image-optimisation.php:2146-2190` | `prioritize_lcp_in_buffer` dual engine (WP 6.9 serialize_token + fallback) correctly handles both WP versions | `method_exists('WP_HTML_Processor','serialize_token') && version_compare(...)` (2254) streams tokens and rebuilds via `serialize_token()`, checks `get_last_error()` (2279) to bail on parse failure. Fallback uses `WP_HTML_Tag_Processor` (2286). Correct graceful degradation. |
- These sections were inspected line-by-line and found correct with no finding above INFO:

### agent-A03-php-infra.md

- > Severity: `CRITICAL` > `HIGH` > `MEDIUM` > `LOW` > `INFO` > `OPTIMIZATION` / `DUPLICATE` / `DEAD CODE`
- | A03-021 | INFO | `class-system-info.php:129-145` `get_database()` | Dead field | `$client_version` is always `null` (initialized null, never assigned). Historically `mysqli_get_client_version()` or `$wpdb->client_version()` but removed. Returned as `'client_version'=>null` in `get_all()` — SPA shows "—". Not harmful but misleading diagnostics. | Admin System Info panel shows blank client version. | Populate via `mysqli_get_client_info()` when `$wpdb->dbh instanceof \mysqli`, or remove field and document intentional omission due to fingerprinting policy (adjacent to version redaction). | low |
- | A03-022 | INFO | `class-suggestion-engine.php:114-116,128-130` raw header override | Clarity | `compression_value`/`cache_control_value` override mutates `$suggestions[count-1]['value']` after `make_boolean()` built `'pass'`→ raw header. Relies on last-index invariant; if additional suggestions inserted later the index breaks. Works now but fragile. | Future edits may silently mis-attribute raw header to wrong metric. | Capture index at push time: `$idx = count($suggestions); $suggestions[] = …; if($comp_pass) $suggestions[$idx]['value']=$comp_value;` | low |
- | A03-023 | INFO | `class-cdn-purger.php:82-104` `purge_litespeed()` path normalization | Edge | `purge_litespeed('single_page', $url_path)` prepends `'/' . ltrim($path,'/')` then `home_url($path_for_url)` inside `sync_purge_url_to_litespeed`. If `$url_path` already contains query string `/page?foo=1`, tag purge will be `home_url('/page?foo=1')` which LSCWP may treat as different tag than `Po.{id}`. Second fallback block duplicates same logic (lines 100-103). | Duplicate code; query-string pages not purged accurately by URL tag fallback. | Deduplicate to single `if (is_string($url_path) && ''!==trim)` branch; strip query via `wp_parse_url($path_for_url,PHP_URL_PATH)` before `home_url`. | low |
- | A03-024 | INFO | `class-rum.php:154-169` `print_config()` | Exposure | Prints `<script id="wppo-rum-config">window.wppoRum={"apiUrl":"…","token":"…","path":"…"}</script>` on `wp_footer` even for cached pages (baked into `index.html`). Token is daily-per-path `wp_hash('wppo_rum_' . Ymd . '|' . path)` — deterministic and brute-forceable if `NONCE_SALT` leaked. No `nonce` attribute on script tag for CSP. | Token replay within day is allowed (by design); exposure is intentional for cache-hit operation. Not a vulnerability per se but CSP strict sites will block inline script. | Consider `wp_add_inline_script('wppo-rum', 'window.wppoRum=…', 'before')` instead of echo, and add `apply_filters('wppo_rum_token_ttl', DAY)` for rotation. Document token is not a secret. | low |
- | A03-031 | INFO | `class-system-info.php:298-322,326-352` `get_litespeed()` drop-in arbitration | FS | Reads both `advanced-cache.php` and `object-cache.php` via `file_get_contents` (<1 M guard) on every `get_all()` REST call, even when not LiteSpeed. `Advanced_Cache_Handler::foreign_dropin_present()` already checks, then file read again for substring `litespeed`/`LSCACHE`. | Extra FS I/O per System Info poll (~2 reads). | Memoize drop-in content per request with `static $adv_content`. | low |
- **Polish (LOW/INFO):** A03-016–A03-026

### agent-A04-php-rest-cli.md

- | R09 | INFO | `class-rest.php:472-478` | `update_settings` response redaction | `if(isset(options['performance_audit'])) unset(options['performance_audit']['pagespeed_api_key']); if(isset(options['object_cache']['password'])) unset(...);` — redacts before `send_response`. | Prevents API key / password leak via View Source / `wppoSettings`. Correct. | No change. | HIGH |
- | R17 | INFO | `class-rest.php:911-946` | `get_database_cleanup_counts` / `get_page_assets` / `get_image_job_status` | `get_page_assets` validates `post_id absint` → 400 if 0; `get_image_job_status` checks `function_exists(as_get_scheduled_actions)` else `queued_jobs=0`; `get_database_cleanup_counts` delegates. All correctly permission-gated. | — | No change. | HIGH |
- | R20 | INFO | `class-rest.php:1150-1156` | `sanitize_nodes` | Handles array vs scalar, `sanitize_text_field` + `array_filter` + `array_values`. Correct. | — | No change. | HIGH |
- | R28 | INFO | `class-rest.php:1480-1527` | `get_server_rules` / `regenerate_ccss` / `get_ccss_status` / `dismiss_welcome` | `get_server_rules` adds `litespeed` info if class exists; `regenerate_ccss` delegates; `get_ccss_status`; `dismiss_welcome` does `if(get_current_user_id()) update_user_meta(...,1)` — safe, permission gated. | — | No change. | HIGH |
- | R29 | INFO | `class-rest.php:291-318,326` | `get_schema_for_route` + `permission_callback` | Schema static, no args validation; permission uses `manage_options` + nonce. | `args` / `validate_callback` / `sanitize_callback` not declared per-route — relies on handler sanitization. Acceptable, but OpenAPI discoverability limited. | Consider adding `args` declarations for gateway validation and `OPTIONS` preflight docs. | LOW |
- | CLI07 | INFO | `class-wppo-cli-command.php:434-505` | `get_default_settings` | Returns 7 tabs with full defaults (cache_settings, file_optimisation 32 keys, preload, image, performance_audit, database_cleanup, object_cache). Matches `Activate::maybe_seed_settings` except Activate missing 7 disable* keys (see ACT02). | Drift noted in ACT02. | Centralize defaults in `Util::get_default_settings()` single source. | HIGH |
- | CLI13 | INFO | `class-wppo-cli-command.php:939-954` | `system_info` | `group = args[0] ?? null; if(group && !isset(all[group])) error Invalid group; else log json` — correctly validates group. | — | No change. | HIGH |
- | MB03 | INFO | `class-metabox.php:88-100` | `render_metabox` | `preload_urls = get_post_meta(post->ID,'_wppo_preload_image_url',true); wp_nonce_field('save_preload_image_url','wppo_preload_image_nonce'); textarea esc_textarea(preload_urls)` — correct nonce, esc. | — | No change. | HIGH |
- | MB05 | INFO | `class-metabox.php:120-136` | `render_asset_manager_metabox` | Ensures `disabled_scripts/styles`, `delay_strategies/priorities` are arrays via `if(!is_array) =[]` — prevents warning on `in_array` / `array_column`. | — | No change. | HIGH |
- | MB07 | INFO | `class-metabox.php:173-254` | `render_asset_manager_metabox` scripts table | `is_protected = in_array(handle, protected_js, true); is_disabled = in_array(handle, disabled_scripts, true); strategy = delay_strategies[handle] ?? '';` renders checkbox `disabled(protected)` and select `disabled(protected)`. `checked/disabled/selected` helpers correctly escaped via `esc_attr`. | Protected handles visually dimmed `opacity:0.5` and disabled — cannot be disabled, correct. | No change. | HIGH |
- | MB12 | INFO | `class-metabox.php:416-458` | `process_disabled_assets` / `process_delay_setting` | Both sanitize, intersect/allowlist, skip empty values. `process_delay_setting` also checks `allowed_values` via `in_array(...,true)` and `valid_handles`. Correct. | — | No change. | HIGH |
- | ACT01 | INFO | `class-activate.php:58-73` | `Activate::init` | `if(foreign_dropin_present()) notices[]=foreign_dropin else create(); add_wp_cache... ; set_transient(wppo_activation_notices, unique, WEEK)` — one-week activation notice transient via `Util::transient_key`. | Correct multisite prefix via `transient_key`. WEEK duration means stale notice persists. | WEEK is intentional per UI dismiss flow; keep. | HIGH |
- | ACT05 | INFO | `class-activate.php:199-226` | `maybe_run_upgrades` | Fresh install `is_fresh_install=true` → `update_option(VERSION, WPPO_VERSION)` and return (no flush). Else `version_compare(stored, LEGACY_FLUSH_FLOOR,'>=')` → return (one-time flush only). Else `flush_legacy_query_cache_keys()`, schedule retry on fail. | Correct one-time eviction gated on fixed floor `1.8.1`, not current version — future bumps no re-flush. Good. | No change. | HIGH |
- | ACT09 | INFO | `class-activate.php:321-346` | `create_activity_log_table` | `SHOW TABLES LIKE` via `$wpdb->prepare`, then `dbDelta` create `wppo_activity_logs (id mediumint(9) AI, activity varchar255, created_at datetime DEFAULT CURRENT_TIMESTAMP)`. | Single table, correct collation via `get_charset_collate`. Activity log not multisite-prefixed? Uses `$wpdb->prefix` so per-site table (correct single-site + per-blog on multisite). | No change. | HIGH |
- | DEA05 | INFO | `class-deactivate.php:65-68` | `init` tail | `Log::add('Plugin deactivated'); Cache::clear_cache();` — clears HTML cache on deactivate (good). | — | No change. | HIGH |
- | UTL01 | INFO | `class-util.php:37-46` | `reset_cached_home_urls` | `self::$home_url_cache=[]` testing isolator. | — | No change. | HIGH |
- | UTL03 | INFO | `class-util.php:89-101` | `init_filesystem` | Requires `ABSPATH.wp-admin/includes/file.php` if `WP_Filesystem` missing, then `WP_Fisisystem()` return `$wp_filesystem` else false. | Correct FS abstraction. | No change. | HIGH |
- | UTL05 | INFO | `class-util.php:153-190` | `get_js_css_minified_file` | Dirlist `min_cache_dir()/js` and `/css`, counts `.js/.css` via `pathinfo`. Uses `Util::init_filesystem` then `min_cache_dir()`. | Correct per-site min cache dir (`cache/wppo/min/{blog_id}`). | No change. | HIGH |
- | UTL06 | LOW | `class-util.php:199-224` | `get_image_mime_type` | `extension=strtolower(pathinfo(parse_url(url, PHP_URL_PATH), PATHINFO_EXTENSION)); switch(jpg/jpeg→image/jpeg, png→..., webp, gif, svg, avif, heic/heif, default '')` — HEIC/HEIF not needed for preload link `as=image` but present. `default ''` so unknown extension yields empty type → caller likely skips preload type. | Good. No `jxl` / `heic` mime needed? `heic` correct. | Add `jxl`? Keep minimal. | LOW |
- | UTL07 | INFO | `class-util.php:243-281` | `generate_preload_link` | Escapes `rel/href/as/crossorigin/type/media/fetchpriority`, builds `<link ...>` then `wp_kses` allowlist then `echo`. | Safe. | No change. | HIGH |
- | UTL08 | INFO | `class-util.php:293-298` | `process_urls` | Array: `trim, array_unique, array_filter, array_values` — dedup + reindex. String: `explode("\n", string)`. | Correct. Note filters empty strings but not whitespace-only? `trim` then `array_filter` without callback removes empty strings correctly (whitespace-only becomes '' after trim → falsy). Good. | No change. | HIGH |
- | UTL11 | INFO | `class-util.php:393-435` | `min_cache_*` / `cached_content_url` | `min_cache_base_dir = WP_CONTENT_DIR/cache/wppo/min`, `min_cache_dir(blog_id/subdir)`, `min_cache_url(blog_id/subdir/filename)` via `cached_content_url`, `cached_content_url` caches per blog_id+path via static `$cache` unless `has_filter('content_url')` then bypass. | Correct multisite isolation. | No change. | HIGH |
- | UTL12 | INFO | `class-util.php:480-518` | `cached_home_url` / `transient_key` | `cached_home_url` caches per blog_id via static, bypasses if `has_filter('home_url')`; `transient_key` prefixes `blog_id_` on multisite with try/catch. | Correct transient isolation for shared Redis. | No change. | HIGH |
- | UTL13 | INFO | `class-util.php:533-578` | `get_role_hash` / `is_cache_eligible_for_current_user` | `get_role_hash` sorts roles, `md5(implode(',',roles).wp_salt())` substr 0,12; eligible checks `is_user_logged_in`→true if anon, else `enableLoggedInCache` flag, empty roles → false, empty allowed_roles → true else intersects. | Correct role-variant caching logic mirroring `advanced-cache.php` drop-in salt. | No change. | HIGH |
- | CT01 | INFO | `class-core-tweaks.php:33-101` | `__construct` | Registers hooks conditionally per settings flags; `disableEmojis` → `init:disable_emojis` + conditional `wp_enqueue_scripts/admin_enqueue_scripts:disable_emojis_script_module` if `wp_dequeue_script_module` exists; `disableEmbeds` → `init:-1000:disable_embeds`; `disableDashicons` → `wp_enqueue_scripts:disable_dashicons`; `disableXMLRPC` → `xmlrpc_methods:__return_false`, `xmlrpc_enabled:__return_false`, `wp_headers:remove_x_pingback`; etc. | Correct conditional hook registration, no overhead when disabled. | No change. | HIGH |
- | CT03 | INFO | `class-core-tweaks.php:135-139` | `disable_emojis_script_module` | Single `wp_dequeue_script_module('emoji')` if exists. | — | No change. | HIGH |
- | CT05 | INFO | `class-core-tweaks.php:174-183` | `disable_embeds` | `remove_action(rest_api_init, wp_oembed_register_route); add_filter(embed_oembed_discover,__return_false); remove_filter(oembed_dataparse, wp_filter_oembed_result,10); remove_action(wp_head, wp_oembed_add_discovery_links/wp_oembed_add_host_js); add_filter(tiny_mce_plugins...); add_filter(rewrite_rules_array...); remove_filter(pre_oembed_result...)` | Comprehensive oembed disable. `remove_action(rest_api_init,...)` priority default 10 — WP registers at 10, so removal at init -1000 before registration? Actually `rest_api_init` action not yet fired at `init`, so `remove_action('rest_api_init', ...)` is valid to deregister before fire. | No change. | HIGH |
- | CT07 | INFO | `class-core-tweaks.php:222-226` | `disable_dashicons` | `if(!is_user_logged_in()) wp_deregister_style('dashicons')` | Logged-in users retain dashicons for admin bar icons. Correct. | No change. | HIGH |
- | CT08 | INFO | `class-core-tweaks.php:234-249` | `remove_x_pingback / block_xmlrpc_methods` | `remove_x_pingback` unsets `X-Pingback`; `block_xmlrpc_methods` returns `[]`. | `xmlrpc_methods` returning empty blocks all methods, but `xmlrpc_enabled` filter already false — defense-in-depth. | No change. | HIGH |
- | CT12 | INFO | `class-core-tweaks.php:324-327` | `redirect_feed_to_home` | `wp_safe_redirect(cached_home_url('/'),301); exit` | Uses `cached_home_url('/')` (blog-id cached) correct. | No change. | HIGH |
- | AN01 | INFO | `class-admin-notices.php:22-39` | `COMPETING_CACHE_PLUGINS` | Map of 10 plugins `file=>label` including `litespeed-cache/litespeed-cache.php=>LiteSpeed Cache`. | Used to warn multiple page-cache plugins. | Keep updated as new cache plugins emerge. | HIGH |
- | AN06 | INFO | `class-admin-notices.php:208-232` | `maybe_competing_plugins_notice` | `if(!Advanced_Cache_Handler::is_our_dropin()) return; active=get_active_plugin_files(); found=[] foreach COMPETING as file=>label if(in_array(file,active,true)) found[file]=label; if(empty) return; names=implode(', ', map esc_html); echo notice-info is-dismissible` — only warns when *our* drop-in is active, not foreign. | When foreign drop-in present (our not active), competing notice suppressed (correct, activation notice already warns). | No change. | HIGH |
- | AN08 | INFO | `class-admin-notices.php:293-314` | `get_active_plugin_files` | `plugins=(array)get_option('active_plugins',[]); if(function_exists(is_multisite)) try is_multisite() catch → false; if(multisite) try get_site_option(active_sitewide_plugins) merge; catch→ignore; return unique values` — multisite-aware, try/catch for Brain Monkey tests. | Correct. | No change. | HIGH |

### agent-A05-js-spa.md

- | 10 | INFO | `src/lib/litespeed.js:19-38` | Pure helper duplicated vs PHP but not imported anywhere in SPA | `getEffectiveMode`, `shouldDisableOptimizer`, `modeLabel` defined and tested, but grep shows zero imports in `src/` | Functionally dead in SPA (Dashboard/FileOptimization re-derive `effectiveMode` inline from `wppoSettings.litespeed`) – logic duplication risk on drift | Import and use `getEffectiveMode` in `Dashboard.js:686` and `FileOptimization.js:98-103` to single-source; or delete module if intentionally example-only. | HIGH |
- | 29 | LOW | `src/components/WebVitalsRum.js:23-39` | `dayAverages` averages weighted by sample count `n` but divides `sum/n` per metric independently, not per page; correct site-wide average, but loses per-path cardinality | Minor accuracy: mixing paths with different decision? Weighting by sample count already correct for site-wide mean | Document as site-wide mean; alternatively show median per metric. | INFO |
- | 33 | LOW | `src/components/common/ErrorBoundary.js:34-37` | Hard reload via `window.location.reload()` loses WP admin unsaved state without confirm | Okay for boundary | Consider `onReset` prop to allow parent App to reset state without full reload. | INFO |
- | 44 | INFO | `src/setupTests.js:6-15` | `sprintf` mock supports `%d`/`%s` with positional `%2$s` but not `%02d` or float formatters used elsewhere – sufficient for current tests | No impact now but may mask future format bugs | Keep; note limitation. | INFO |
- | 45 | LOW | `src/lib/apiRequest.js:193-195` | `encodeURIComponent` on `url` and `strategy` inside template literal for `getPagespeedResults` vs `fetchSuggestions` etc. direct encode – consistent, safe. No double-encode issue | Query param built via `URLSearchParams` in `fetchWebVitalsTrends:211-221` correctly uses `params.set` + `params.toString()` double-encodes? Actually `fetchWebVitalsTrends` uses URLSearchParams correctly vs manual encode for PageSpeed – two patterns coexist | Standardise on `URLSearchParams` everywhere. Minor style, not bug. | INFO |

### agent-A06-js-vanilla.md

- | 05 | INFO | `lazyload.js:126-143` | External replacement preserves CSP nonce | `Array.from(script.attributes).forEach(attr=>replacement.setAttribute(...)); removeAttribute('wppo-src')` retains nonce | Correct CSP passthrough | No change (verified) | HIGH |
- | 21 | LOW | `lazyload.js:997-1006` | backgroundObserver queries document each batch | `if(!querySelector('.wppo-lazy-bg')) disconnect()` per intersection | Extra scan | Track pending count decrement instead | INFO |
- | 28 | INFO | `main.js:1-3` | Keep-in-sync comment drift vs apiRequest.js | postJsonRequest/pendingRefresh mirrored but diverges on AbortSignal, error messages | Future 401 fix may miss admin bar | Extract src/lib/nonce.js or checklist both | INFO |
- | 34 | INFO | `rum.js:178-194` | visibilitychange+pagehide double send guarded by sent flag | Both call send(); second no-op via sent | Not bug; note once:true not needed for visibilitychange | No change | INFO |

### agent-A07-css.md

- > Severity: **CRITICAL** > **HIGH** > **MEDIUM** > **LOW** > **INFO** > **OPTIMIZATION** > **DUPLICATE** > **DEAD CODE**
- | F-25 | **INFO** | `src/css/components/_performance-audit.scss:34,72,84,94` + `_header.scss:21-29` + `_stats.scss:25-32` | Hardcoded gradients/hero backgrounds: `linear-gradient(135deg, #ffffff 0%, #eef2ff 55%, #f0f9ff 100%)` etc. — 6 hardcoded hex fills outside tokens | Not a11y fail (contrast passes), but theming via `wp_localize` `themeColors` won't affect these. Design-system violation — premium palette should be tokenized. | Tokenize hero gradients: `--wppo-gradient-hero: linear-gradient(...)` in `_variables.scss`. | 80% |
- | F-26 | **INFO** | `src/css/components/_notices.scss:49-52` | `@media (max-width: 768px) { .wppo-notice__dismiss { min-width:44px; min-height:44px } }` | Good — meets WCAG 2.5.5 (44×44) on mobile. `_forms.scss:100-102` also bumps button to 44px at `md`. Consistent — **positive**. No action. | Keep; extend audit to verify all icon buttons meet 44px. | 95% |
- | F-27 | **INFO** | `src/css/base/_base.scss:107-126` | `a:focus-visible, button:focus-visible ... { outline: 2px solid var(--wppo-primary); outline-offset:2px; }` + `.wppo-focusable:focus-visible` | Good a11y — focus ring consistent. `.wppo-input:focus-visible` duplicate next block is redundant but harmless. Positive pattern. | Deduplicate second block or merge into first with `&:focus-visible`. | 90% |

### agent-A08-security.md

- | A-AUTH-03 | LOW | `includes/class-metabox.php:308-321` | `save_metabox` checks `current_user_can('edit_post', $post_id)` + `wp_verify_nonce` for each sub-save, but `add_metabox` registers asset-manager box for all `public` post types without cap check. | Low — `add_meta_box` visibility is WP core controlled; save is gated. No priv-esc. | No change; note as INFO. | HIGH |
- | A-AUTH-05 | INFO | `includes/class-rest.php:326-330` | `permission_callback` = `current_user_can('manage_options') && wp_verify_nonce($_SERVER['HTTP_X_WP_NONCE'], 'wp_rest')` — correct dual check for 25 admin routes. | Proper defense-in-depth; REST 403 auto-refresh via `src/main.js` nonce refresh mitigates stale nonce UX. | No change. | HIGH |
- | A-SAN-04 | INFO | `includes/class-metabox.php:88-97,118-122` | `render_metabox`: `esc_textarea($preload_urls)`, `esc_html_e`, `esc_attr($script['handle'])`, `esc_html($script['src'])` — consistent escaping. `save_preload_image_urls`: `sanitize_textarea_field(wp_unslash($_POST['wppo_preload_image_url']))`. | Correct output escaping + input sanitization. | No change. | HIGH |
- | A-SAN-05 | INFO | `includes/class-cache.php:200-244` | `__construct`: `HTTP_HOST` sanitized via `sanitize_text_field`, `idn_to_ascii`, port stripped, regex `^[a-z0-9\.\-]+$`, `..`/`/`/`\` rejection → empty domain → `is_not_cacheable() => true`. | Prevents Host-header poisoning → cache poisoning / directory traversal (`cache/wppo/{domain}/{path}`) | No change. | HIGH |
- | A-SQL-01 | INFO | `includes/class-database-cleanup.php:90-117,150-258,267-365,374-468,481-563,571-618,630-668,736-779,930-984,1045-1094` | All dynamic deletes/counts use `$wpdb->prepare` with `%d/%s` + `...$post_ids` spread, or fixed SQL with `$wpdb->posts`/`$wpdb->postmeta` table names (core-provided, not user input). Example `class-database-cleanup.php:156-162` `prepare("SELECT post_parent FROM $wpdb->posts WHERE post_type='revision' AND post_parent>%d GROUP BY post_parent HAVING COUNT(*)>%d ...", $greatest_parent_id, $keep_latest)`. | No injection — values are ints, strings escaped, table names are core constants. | No change. | **HIGH** |
- | A-SQL-06 | INFO | `includes/class-log.php:58-66,120-127` | `Log::add`: `$wpdb->insert($table_name, ['activity'=>wp_kses_post($activity)], ['%s'])` — uses core insert with placeholder. `get_recent_activities`: `prepare("SELECT * FROM {$wpdb->prefix}wppo_activity_logs ORDER BY created_at DESC LIMIT %d OFFSET %d", $per_page, $offset)` — both ints via `absint`. | Correct parameterization. `wp_kses_post` strips dangerous tags before storage (defense vs stored XSS). | No change. | HIGH |
- | A-SQL-07 | INFO | `includes/class-system-info.php:548-559` | `get_mysql_var`: `prepare('SHOW VARIABLES LIKE %s', $variable)` where `$variable` is hardcoded `'max_connections'` in caller — safe. | Safe. | No change. | HIGH |
- | A-XSS-04 | INFO | `includes/class-metabox.php:95-97` | `esc_textarea($preload_urls)` in textarea; `esc_attr`/`esc_html` in asset manager tables — correct context-aware escaping. | Correct. | No change. | HIGH |
- | A-XSS-05 | INFO | `includes/class-cache.php:1271-1354` | `maybe_apply_cdn`: uses `WP_HTML_Tag_Processor` `set_attribute('src', str_replace(...))` — `Tag_Processor` auto-escapes attribute context; CDN URL is `rtrim($cdn_url,'/')` where `$cdn_url` was `esc_url_raw` at save time. | Correct — CDN rewrite does not introduce URI injection (only rewrites `site_url` prefix to `cdn_url`). | No change. | HIGH |
- | A-CSRF-01 | INFO | `includes/class-rest.php:326-330,788-799` | All 25 admin REST routes: `permission_callback` checks `current_user_can('manage_options') && wp_verify_nonce($_SERVER['HTTP_X_WP_NONCE'], 'wp_rest')` — correct. `update_settings`, `clear_cache`, `database_cleanup`, `object_cache`, `pagespeed_scan`, etc. all state-changing and gated. | No CSRF — REST nonce required; cookie-only replay blocked. | No change. | HIGH |
- | A-CSRF-02 | INFO | `includes/class-rest.php:1164-1178` | `ajax_get_nonce`: `current_user_can('manage_options') && check_ajax_referer('wppo_nonce_refresh','nonce',false)` — correct double check. | No CSRF. | No change. | HIGH |
- | A-CSRF-03 | INFO | `includes/class-metabox.php:328-336,355-362` | `save_preload_image_urls`: `wp_verify_nonce($_POST['wppo_preload_image_nonce'],'save_preload_image_url')`; `save_asset_manager_settings`: `wp_verify_nonce($_POST['wppo_asset_manager_nonce'],'wppo_save_asset_manager')` + `current_user_can('edit_post')` before. | Correct CSRF protection for `save_post`. | No change. | HIGH |
- | A-CSRF-04 | INFO | `includes/class-main.php:428-429,351-377` | `set_role_hash_cookie` / `clear_role_hash_cookie` run on `init`/`wp_logout` — not CSRF-sensitive (cookie set is idempotent; `clear` on logout is logout-CSRF but `wp_logout` already has nonce). | No issue. | No change. | HIGH |
- | A-CSRF-05 | INFO | `includes/class-rum.php:77-103` | `rum_collect` is intentionally unauthenticated (anonymous beacons) — CSRF not applicable (public endpoint). Token is `wp_hash('wppo_rum_'+date+'|'+path)` — knowledge of `wp_salt` required to forge; daily rotation limits replay window. | Token protects against off-site forged beacons (attacker site cannot know per-page token without fetching `wppo-rum-config` from victim site). | No change. | HIGH |
- | A-FILE-08 | INFO | `includes/class-cron.php:582-606` | `mark_page_as_processed`: `$domain = sanitize_text_field(parse_url(site_url(), host))` + `$cache_dir = WP_CONTENT_DIR . "/cache/wppo/{$domain}/{$url_path}"` — `domain` from `site_url()` (not `HTTP_HOST`) so admin-triggered cron not poisonable. | Safe. | No change. | HIGH |
- | A-PRIV-06 | INFO | `performance-optimisation.php:28-38` | `WPPO_PLUGIN_PATH`/`WPPO_PLUGIN_URL`/`WPPO_VERSION` defined with `if(!defined)` guard — prevents constant redefinition if MU-plugin preloads. | No priv-esc. | No change. | HIGH |
- | A-PRIV-07 | INFO | `uninstall.php:138-165` | `wppo_cleanup_site` called per-site via `switch_to_blog` loop — iterates `get_sites(['number'=>100])` with offset pagination, correct `restore_current_blog`. | Multisite cleanup is site-scoped; no cross-site data leak. | No change. | HIGH |
- | A-SSRF-01 | INFO | `includes/class-telemetry.php:83-98,251-428` | `Telemetry::scan` + `fetch_via_curl`/`fetch_via_wp_remote`: `wp_http_validate_url($url)` + scheme `http|https` + `home_host === parsed_host` before fetch; `CURLOPT_FOLLOWLOCATION=false` + manual `resolve_redirect` re-validates each hop against same SSRF rules; `MAX_REDIRECT_HOPS=2`. `calculate_sizes` HEAD fallback also gates via `home_host===url_host && wp_http_validate_url`. | Strong SSRF defense — DNS rebinding via redirect to `127.0.0.1/0.0.0.0/169.254.169.254` blocked; same-host-only policy. `CURLOPT_PROTOCOLS` restricts to HTTP/HTTPS. | No change. | **HIGH** |
- | A-SSRF-02 | INFO | `includes/class-rest.php:1201-1279` | `run_performance_scan` / `queue_pagespeed_scan`: `wp_http_validate_url` + scheme allowlist + `home_host` check. `Pagespeed::run_scan`: repeats `wp_http_validate_url` before `wp_remote_get` to `https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=<request_url>&key=…`. | Double-gated; Google API URL is always Google host, not attacker. | No change. | HIGH |
- | A-SSRF-03 | INFO | `includes/class-pagespeed.php:199-205` | `wp_remote_get($query_url, ['timeout'=>120,'sslverify'=>true])` — `sslverify=true` (not filterable via `wppo_telemetry_verify_ssl` here — always true for Google API). | Correct. | No change. | HIGH |
- | A-SSRF-04 | INFO | `includes/class-cron.php:473-544` | `get_sitemap_urls`: `Util::cached_home_url('/wp-sitemap.xml')` + `home_host` filter on each `<loc>`; `to_fetch` cap 50, total `cap` 500, 15 s deadline. `process_page`/`process_url`: `wp_remote_get` with `timeout 30`, URL from `get_permalink`/`esc_url_raw($url)` validated earlier. | Sitemap SSRF via off-site `<loc>` blocked (host check). `wp-sitemap.xml` fetched from own host only. | No change. | HIGH |
- | A-SSRF-05 | INFO | `includes/class-google-fonts.php` | `process_buffer`: `wp_remote_get` only for `fonts.googleapis.com`/`fonts.gstatic.com` URLs (hardcoded allowlist). | No SSRF. | No change. | HIGH |
- | A-SSRF-06 | INFO | `includes/class-cdn-purger.php` | `purge_all`: `wp_remote_request` to Cloudflare/Varnish purge endpoints — URLs validated via `wp_http_validate_url` + site host check (inspected). | No SSRF. | No change. | HIGH |
- | A-SER-01 | INFO | `includes/class-cache.php:678-711` | `write_combined_handles`: `wp_json_encode($eligible_handles)` → `put_contents(handles_path)`; `combined_handles_match`: `json_decode(...,true)` — JSON, not `serialize`/`unserialize`. | No object injection. | No change. | HIGH |
- | A-SER-02 | INFO | `includes/redis-connect-helper.php:332-350` | `wppo_apply_redis_options`: `setOption(OPT_SERIALIZER, SERIALIZER_IGBINARY|SERIALIZER_PHP)` — Redis serializer set, but no `unserialize` of user input in plugin; Redis `get` returns raw bytes handled as strings. | No object injection via Redis (data is cache, not user-controlled serialized object). | No change. | HIGH |
- | A-SER-03 | INFO | `includes/class-img-converter.php` | `get_img_info`/`set_img_info` uses `get_option('wppo_img_info')` (autoload `false`) with deferred commit pattern — array storage via `update_option` (core serializes). No `unserialize($_POST)` anywhere. | No injection. | No change. | HIGH |
- 4. (**MEDIUM/INFO**) Fix `class-main.php:351` `wppo_role_hash` `SameSite` attribute.
- 5. (**HIGH/INFO**) Document `rum_collect` public design + bounded eviction in `REVIEW-MATRIX`.

### agent-A09-performance.md

- | P-CPU-10 | **INFO** | `includes/class-cache.php:1565-1643` | `save_cache_files('html')` does `gzencode(buffer,9)` + `brotli_compress(buffer,4)` + `WP_Filesystem::put_contents`× up to 3. `brotli_compress` is tried twice (two branches `function_exists` duplication lines 1612-1633 — **dead code** second branch unreachable when first `if ($use_brotli && function_exists)` already handled; `elseif` repeats same call). | Duplicate branch wastes a branch test only; `gzencode 9` is CPU-heaviest (zlib level 9 ~1.5× slower than 6) per cache-miss HTML write (~50 KB buffer → ~2 ms at 9 vs 1 ms at 6). Brotli 4 is okay. | Collapse duplicate `elseif` (line 1621-1633) — unreachable. Consider `gzencode level 6` (standard) to halve CPU with negligible size diff (~1%). | **INFO** |
- | P-WP-08 | **LOW** | `includes/class-main.php:418-430` | `maybe_fix_wp_cache` runs on `admin_init` every admin request: `get_transient(Util::transient_key wppo_wp_cache_fix_checked)` 1 h throttle + if missing, `Activate::add_wp_cache_constant()` which `is_readable`+`file_get_contents` `wp-config.php` + `strpos WP_CACHE` + `file_put_contents`. On multisite, `Util::transient_key` prefixes with blog id. | When transient expires (hourly), one admin request does `is_readable`+`file_get_contents` of `wp-config.php` (10–50 KB) → 0.5 ms. Low impact due to 1 h throttle. | No fix needed; already throttled. Note as INFO that `get_transient` still queried per admin request (1 object-cache lookup). | **LOW** |
- | P-WP-10 | **LOW** | `includes/class-cache.php:2243-2249` | `flush_legacy_query_cache_keys` does `wp_cache_flush()` (full object-cache flush) when `function_exists wp_cache_get_salted` (WP 6.9+). Called from `Main::on_settings_update`? Actually `Cache::clear_cache` calls `flush_legacy`? Check — `flush_legacy_query_cache_keys` is exposed but not auto-called on every clear? If invoked, `wp_cache_flush` evicts **all** transients + salts for whole site (including other plugins' caches) — expensive on Redis (SCAN+DEL). | 1 full flush per cache clear (admin save) — acceptable (clear_all is infrequent). But doc warns "only reliable eviction" — cost vs correctness trade-off. | No change; note flush is infrequent (settings save, plugin update). Could use `flush_group('wppo')` + `flush_group('wppo_db_cleanup_counts')` instead on WP 6.1+ but legacy keys remain — current approach justified. | **INFO** |
- | P-DB-06 | **LOW** | `includes/class-system-info.php:548-559` | `get_mysql_var('max_connections')` runs `SHOW VARIABLES LIKE %s` via `wpdb->prepare` per `get_system_info` call (admin SPA `fetchSystemInfo`). `SHOW VARIABLES` is not cached; runs once per admin settings visit but `get_all` calls it via `get_database`. | 1 extra query per `system_info` REST — trivial. | No fix; note `SHOW VARIABLES` could be replaced by `SELECT @@max_connections` (lighter). | **INFO** |
- | P-FE-06 | **LOW** | `src/main.js:110-163,168-191` | `showNotice` fallback builds DOM `div.notice is-dismissible wppo-admin-notice` + `button.notice-dismiss` per click, with `setTimeout 5000` `fallbackTimer` single global. Concurrent `Clear All` + `Clear This Page` clicks share `fallbackTimer` (clears previous timer). No `transition` CSS for dismiss (instant `remove()`). | Sub-minor: two notices cannot coexist (second `clearTimeout` cancels first's auto-dismiss). Not a perf issue. | Use per-notice `setTimeout` map (`WeakMap<Element, timer>`) or `wp.data.dispatch('core/notices')` primary path already used. | **INFO** |
- | P-FE-08 | **LOW** | `src/components/Dashboard.js:75-88,805-830` | `allSuggestions useMemo([telemetrySuggestions, pagespeedSuggestions])` builds `seen Set` + `merged[]` per render; `dbOverheadCount useMemo([dbCounts])` `Object.values.reduce`. Lightweight. `WelcomePanel` etc. are static. Bundle size: `src/App.js` imports `FontAwesomeIcon` + 12 icons statically (not dynamic) — icons bundled in `build/index.js` main chunk (≈150 KB including `react`, `wp-element`). `lazy` 7 tab chunks each ~20–50 KB. Total admin SPA ~500 KB transferred (gzip ~120 KB). | Gzip 120 KB SPI still ~200 ms parse on mobile 3G. Icons imported statically in `App.js` (faTachometerAlt…faSpinner 13 icons) + `Dashboard.js` 6 icons → duplication across chunks? `FontAwesome` tree-shaking should deduplicate. | Move icon imports to per-tab lazy chunks or use SVG sprites instead of FA React (smaller). Current `build/index.js` likely >200 KB — consider `dynamic import('@fortawesome…')`. | **INFO** |
- | P-CACHE-04 | **MEDIUM** | `includes/class-cache.php:1978-1986,193-195` | `flush_group` vs `flush_legacy_query_cache_keys`: admin path `clear_cache` does `delete_transient(wppo_cache_size)` + `delete_transient(wppo_total_js_css)` **or** `update_option(wppo_cache_last_cleared, salt)` when `wp_cache_get_salted` exists (WP 6.9+). `wppo_cache_last_cleared` salt is **incremented** (`(int)get_option +1`) not `time()` — but `salted cache` expects `time()`-like value? Actually `Cache.php:1980` `salt = (int)get_option('wppo_cache_last_cleared',0)+1` — uses **incremental counter** vs `Log`/`Telemetry` use `time()`. Growth: after many clears, salt value grows monotonic; old salts never GC'd (transient index prunes, but salted cache keys persist until expiry). No stampede on clear because salt bump invalidates all `wppo_cache_*` groups logically, not physically. | Counter-based salt is fine (bounded by clears). Invalidation cost is 1 `update_option` per clear (writes `wp_options` autoload=false). No object-cache flush — correct. But `Cache::flush_legacy_query_cache_keys` does `wp_cache_flush` full flush when 6.9+ salted cache present — this **is** a stampede risk: it invalidates **entire** object cache (including other plugins' transients). Comment in file acknowledges but code still may be called on settings save. | Already correct for `wppo_cache_last_cleared` (counter is fine, optionally use `time()` like others for consistency). Document that `flush_legacy_query_cache_keys` is fallback-only (6.9 only) and infrequent. No fix needed beyond documenting. | **INFO** |
- | P-CACHE-06 | **MEDIUM** | `includes/class-cache.php:1503-1512,1541-1554` | `get_cache_file_path(type, role_hash, variant)` and `get_cache_file_url` compose `cache_root_dir/domain/url_path/index{ -{hash} -{variant} }.type`. `Cache::__construct` sets `request_uri` via `sanitize_text_field(wp_unslash($_SERVER[REQUEST_URI]))` then `url_path = wp_normalize_path(trim(rawurldecode(parse_url(request_uri,PHP_URL_PATH)),'/'))`. `REQUEST_URI` includes **query string** (`?s=foo&ver=1`) stripped by parse_url path, but `is_not_cacheable` also checks `!empty(pathinfo(local_url_path,EXTENSION))` → extension present → not cacheable. However `maybe_store_cache` checks `!empty($_SERVER[QUERY_STRING]) && preg_match(/(?:^|&)(s|ver|v)(?:=|&|$)/, sanitize_text_field(wp_unslash QUERY_STRING))` — so `?s`/`?ver`/`?v` URLs are **query-string rejected** at storage time, but `get_cache_file_path` still built from path-only, so `?ver` URLs map to same `url_path` as bare URL (correct). No per-query-string cache variant — intentional. | Cache key does not include query string — prevents cache poisoning via query-string; correct. Negative cache for `DONOTCACHEPAGE` writes `.wppo-no-cache` marker beside HTML — `is_not_cacheable` fast path `defined(DONOTCACHEPAGE)` writes marker and deletes stale files per request (1× `put_contents`+`delete` per opted-out page). | No fix; confirm `Cache::is_not_cacheable` extension guard prevents caching `file.css?ver` style false positives (cache path `index.css` vs `index.html`). | **INFO** |
- | P-CACHE-07 | **LOW** | `includes/class-object-cache.php:86-185` | `Object_Cache::get_status` checks `file_exists(dropin_path)` + `is_readable` + `filesize <1MB` then `get_contents` via `WP_Filesystem` or `file_get_contents` fallback to detect `DROPIN_MARKER`. Then `get_option('wppo_settings')['object_cache']` + fallback `include(config_path)` then `connect_internal` which actually **opens Redis TCP connection** (`wppo_redis_connect` → `Redis::connect(timeout 1 s)`). Called from REST `object_cache status` and `System_Info::get_litespeed()->Object_Cache` branch. `get_status` does **real TCP connect** per call, not cached. | Each `object_cache status` REST poll (admin SPA tab `objectCache`) or `SystemInfo` panel mount triggers `get_status` which opens a new Redis connection + `info()` + `close()` — ~5–10 ms TCP + `INFO` (holds Redis). If SPA auto-polls on tab switches (no), but manual refresh can spam connections. `System_Info::get_litespeed` also may instantiate `Object_Cache` and call `get_status` → extra connection from dashboard health. | Cache `get_status` result for 30 s in a static or `get_transient(Util::transient_key('wppo_object_cache_status'))` 60 s, especially the Redis `info()` branch. Already `is_readable`+marker check could be memoized per request via static. | **MEDIUM** |
- | P-CACHE-08 | **LOW** | `includes/class-main.php:419-484,806-819` | `maybe_run_version_upgrade` runs on `admin_init`+`upgrader_process_complete` when `get_option('wppo_version') < WPPO_VERSION` then `Advanced_Cache_Handler::create()` (`file_get_contents`+`var_export`+`file_put_contents` `advanced-cache.php`) + `Cache::clear_cache()` (recursive `delete` of `cache/{domain}` + min dir + legacy dirs). The 4 `maybe_*` upgrades (`maybe_fix_wp_cache`, `maybe_run_upgrades`, `maybe_run_version_upgrade`, `maybe_migrate_block_assets_setting`) each do `get_option` checks and run **on every `admin_init`** (even on AJAX? Actually `admin_init` fires on admin only, but `DOING_AJAX` also is admin). With 4 handlers, that's 4× `get_option` per admin request until migrations complete (then early returns). | After initial install, `get_option('wppo_version')` matches and version upgrade short-circuits (`version_compare >=` line 919 → return). `maybe_fix_wp_cache` throttled by 1 h `get_transient`. Net extra cost after warm =2× `get_option`+`has_filter` per admin request (negligible). | No fix; throttles and gates are already sound. Note `add_wp_cache_constant` file check (`is_readable wp-config`) only when not throttled — good. | **INFO** |
- | P-CACHE-09 | **INFO** | `includes/class-cache.php:1555-1581` | `register_combine_css_path` sets `wp_style_add_data('wppo-combine-css','path', $css_file_path)` so `wp_maybe_inline_styles` can inline the combined file if under budget. It resets `inline_size_map=null` to rebuild budget sim after combined handle added. This is correct for `combine_css` regeneration path. No double-write when `inline_drift_detected` true (query `wppo_inline_drift` transient per handle). | Single reset per `combine_css` regeneration (when CSS queue changes) — trivial. | No fix; already lazily rebuilt. | **INFO** |
- **OPTIMIZATION: 1 | HIGH: 8 | MEDIUM: 12 | LOW: 6 | INFO: 4**

### agent-A10-duplication-deadcode.md

- **Instruction:** Find exact duplicates, near-duplicates, repeated conditions, repeated queries, repeated validation, repeated cache logic, repeated permission checks, repeated hook registrations, similar helpers, copy-pasted functionality, multiple impls of same behaviour; and unused functions/classes/methods/variables/constants/hooks/assets/dependencies/dead branches/unreachable code/commented-out. For each duplicate determine consolidate/abstract/remove/keep. For each dead trace references before declaring unused. Classify DUPLICATE/DEAD CODE/LOW/INFO with file:line evidence, impact, recommendation, confidence. Do not modify production code. Search with grep/rg for duplicates.
- | D-19 | **DUPLICATE** | `get_autoloadable_values` / `get_autoloaded_options` vs `write_combined_handles` JSON size-map — no duplication, noted as INFO | — | — | — | **Keep** — no action. | — |
- | X-03 | **INFO** | `TODO(#553)` / `TODO(#624)` comment scaffolding — not dead code but forward-labelled branches | `includes/class-cache.php:373,593`, `includes/class-main.php:438,442,482,504,511,522,533`, `includes/class-image-optimisation.php:357,1907` | Comments mark WP-version-gated branches (`WP 6.9` template-enhancement fallback, `WP 6.3` defer strategy, `WP 7.2` preload). Branches are live today but labelled for future removal when minimum WP is bumped. Not dead — they are active fallbacks. | **Info.** No action now; keep until version floor is raised. | **Keep**. Track in roadmap for when `minimum WP = 6.9` is declared. | High |
- | X-07 | **INFO** | `wppo-text-small` vs `wppo-text-13` exact duplicate definitions | `src/css/base/_base.scss:44-45` | Both set `font-size:13px`. Not dead, but duplicate utility. | **Info.** Keep alias or consolidate. | **Consolidate**: make `wppo-text-13` an alias comment or remove one. | High |
- | X-10 | **INFO** | Unreachable `return false` after `throw` in `Database_Cleanup::invoke_cleanup_method` false→WP_Error branch | `includes/class-database-cleanup.php:994-1000` | `if(false===res) return WP_Error;` then `invalidate_counts_cache(); return res;` — no unreachable code; `return` guards correctly. Verified — not dead. | **None.** | **No action.** | High |

### agent-A11-compatibility.md

- **Instruction:** Classify findings CRITICAL/HIGH/MEDIUM/LOW/INFO with `file:line` evidence, impact, recommendation, confidence. Do not modify production code.
- | C-06 | **INFO** | Domain-based static HTML cache `wp-content/cache/wppo/{domain}/{path}/index.html` is correctly multisite-safe; minor inconsistency between `Cache` domain validation and `Advanced_Cache_Handler` drop-in domain sanitization regex | `includes/class-cache.php:200-227` `preg_match '/^[a-z0-9\.\-]+$/i'` + `..`/`/` checks + `strtolower`, `includes/class-advanced-cache-handler.php:158-165` `preg_replace '/[^a-z0-9.:-]+/i'` + `strtolower` + `..` guard | Both enforce `..` exclusion and domain character whitelist, but `Cache` strips port via `explode(':')` while the drop-in keeps `:` in `$site_domain` (`preg_replace` allows `[:-]` and `strtolower(preg_replace(...))`). Ports are rare but on `example.com:8080` the file cache directory would be `example.com:8080` (Cache after strip) vs `example.com:8080`? Wait Cache strips port → `example.com`; drop-in keeps `example.com:8080` after lower? Actually `$raw_domain` is not port-stripped in drop-in; `$site_domain` lowercases but does not `explode(':')`. On a ported host the drop-in would write to `.../example.com:8080/...` while `Cache` writes to `.../example.com/...`, causing cache miss. | Rare (non-standard port). No cache poisoning because both validate `..`. But ported hosts (e.g. `:8080` dev) would never hit the static file via drop-in. | Align the two: either keep port in both (include port in directory) or strip in both. Add a test for host-with-port. Document that `Cache::__construct` strips port intentionally and that drop-in should do the same. | High |
- | C-07 | **INFO** | `wp_get_loading_optimization_attributes` back-compat is exemplary — every call-site is `function_exists`-gated with a safe fallback, but regex-fallback paths in `class-image-optimisation.php` re-invoke core for `fetchpriority` under different contexts | `includes/class-image-optimisation.php:1296` `if (function_exists('wp_get_loading_optimization_attributes'))`, `1322-1342`, `1425`, `1523,1540,1585,1611,1625`, `includes/class-util.php:589-592` `is_auto_sizes_available()` | All 7 call-sites pass `tag_name='img'`, `attr` from `WP_HTML_Tag_Processor`, `context='wp-html-tag-processor'` vs `'regex-fallback'`. Fallback correctly defaults to `fetchpriority=low`/`high` and `decoding=sync/async` when core absent (WP 6.2 floor). The context string difference is harmless but means regex path receives `'regex-fallback'` while Tag-Processor path receives `'wp-html-tag-processor'` — core's `wp_get_loading_optimization_attributes` may branch on context in future. | None today; future core adding context-specific logic could make the two paths diverge invisibly. | Keep dual-context but document why `'regex-fallback'` vs `'wp-html-tag-processor'` are used, and centralise `set_loading_optimization_attributes` so the context constant is single-sourced. | High |
- | C-10 | **INFO** | `Util::cached_home_url()` / `cached_content_url()` per-blog static caches are correctly gated on `has_filter('home_url')` / `has_filter('content_url')` and `get_current_blog_id()` — avoids stale cache under `switch_to_blog()` | `includes/class-util.php:37` `private static array $home_url_cache`, `:450-496` `cached_content_url()`, `:480-496` `cached_home_url()` | Both bypass cache when a `home_url` / `content_url` filter is present (context-dependent output), otherwise cache per `blog_id`. The per-request static survives `switch_to_blog` because `get_current_blog_id()` is re-read on each call (see `uninstall.php:159-161` loop). `reset_cached_home_urls()` exists for tests. Excellent. | No compatibility issue; this is the reference implementation for multisite safety cited in `AGENTS.md`. | No change; keep as pattern for any future URL helpers. Optionally add `@codeCoverageIgnore` for filter-present branch. | High |
- | C-13 | **INFO** | Hosting: `class-htaccess-handler.php:53-62` correctly uses `insert_with_markers` with `wppo_rules` only — LiteSpeed ordering invariant preserved (LSCACHE block stays above) | `includes/class-htaccess-handler.php:36-44` comment, `53-84` | The comment explicitly states “`# BEGIN LSCACHE must stay above # BEGIN WordPress and # BEGIN wppo_rules`” and the method never calls `insert_with_markers` with `wordpress`. On OLS a restart is required — correctly logged via `LiteSpeed_Integration::is_litespeed()` notice in `class-main.php:1011-1013`. Nginx rules are not auto-written; `Server_Rules::get_nginx_rules()` returns a snippet for manual copy — correct. | No compatibility regression; this is the correct LiteSpeed coexistence pattern per `docs/litespeed-*.md`. | Keep; optionally add integration test that writes a fake `.htaccess` with `LSCACHE` marker and asserts order is preserved. | High |
- | C-16 | **INFO** | Themes/plugins: `is_cart` / `is_checkout` / `is_account_page` guards in `Cache::is_not_cacheable:1472-1479`, plus `DONOTCACHEPAGE` constant honored in `Cache::is_not_cacheable:1450-1453` and `LiteSpeed_Integration::is_request_cacheable` — covers WooCommerce correctly; `Yoast` / `Rank Math` (SEO) have no conflict; Elementor/Divi no special handling needed because minify excludes `jquery` only | `includes/class-cache.php:1441-1491`, `includes/class-main.php:46-48` `exclude_js jquery`, `includes/class-litespeed-integration.php:769` query-param exclude `s|ver|v` | WooCommerce cart/checkout/account bypass is both runtime (`is_cart()` etc.) and cookie-based (`woocommerce_items_in_cart` / `woocommerce_cart_hash` in `class-cache.php:1485-1486` and drop-in `:169-171`). `DONOTCACHEPAGE` covers any plugin (WooCommerce, EDD, etc.) that sets the constant. Minify defer delay excludes `wppo-lazyload` via `exclude_defer_js` default — correct. | No theme/plugin incompatibility observed; page-builders supported as declared in `readme.txt:259-260`. | Consider adding WCs `is_wc_endpoint_url` check for custom endpoints (orders/downloads) if reports arise. | High |
- **Verdict:** No CRITICAL compatibility regression. One HIGH (external-services disclosure), remainder MEDIUM/LOW/INFO. Multisite isolation (`transient_key`, `blog_prefix`, domain-based cache, `min_cache_dir` per-blog) is exemplarily implemented; WP version gates for 6.3/6.7/6.9/7.0/7.1 APIs are exhaustive with `function_exists`; object-cache drop-in, hosting (Apache/Nginx/LiteSpeed/OLS), and portability checks are solid. The plugin is correctly declared as PHP 8.2 / WP 6.2 minimum with committed `build/` and no obfuscated code; remaining work is docs/compliance polish.

### agent-A12-quality-architecture.md

- **Instruction:** Check responsibilities, abstractions, coupling, global state, dependency management, lifecycle, naming, overly complex functions, god classes, circular deps, testability, repeated logic, inconsistent patterns, error handling, edge cases, coding standards. Classify CRITICAL/HIGH/MEDIUM/LOW/INFO/OPTIMIZATION/DUPLICATE/DEAD CODE with file:line evidence, impact, recommendation, confidence. Do not modify production code. Be evidence-based, do not invent unnecessary abstraction.
- | Q-11 | **LOW** | `Performance-optimisation.php` hard-codes `WPPO_VERSION '1.9.0'` duplicate of header (naming / versioning) | `performance-optimisation.php:7` (`Version: 1.9.0` header) vs `28` (`define('WPPO_VERSION','1.9.0')`) + `includes/class-activate.php` re-reads plugin header via `get_file_data` for version? Actually `class-activate.php:??` uses `WPPO_VERSION` constant, not header. `class-main.php:911` (`maybe_run_version_upgrade` compares `get_option('wppo_version','')` vs `WPPO_VERSION`) | Header bump without constant bump → upgrade gate `version_compare(installed, WPPO_VERSION, '>=')` stale; `wppo_version` option never advances despite new code. Already flagged in A01 P02 as INFO. Plugin ships with `Tested up to: 7.1` non-existent WP version (A01 P01). | **Low.** Version drift not caught by CI (no check). | **Source `WPPO_VERSION` from header via `get_file_data(__FILE__)` once, or generate at `npm run build`/`composer release`. Add CI check that `WPPO_VERSION` === header `Version`. | High |
- | Q-16 | **INFO** | `Main::deferred_handles` O(1) map correct but `apply_module_loading_strategies` exclusion uses `process_urls` (stringly) on module ids | `includes/class-main.php:112` (`private array $deferred_handles = array()` keyed by handle for O(1) `isset`), `1620-1661` (`apply_module_loading_strategies` collects `get_print_queue`→`registered`→`all` fallback, then `set_in_footer`+`set_fetchpriority('low')` per id, skipping `excludeDeferJS` via `Util::process_urls((string)excludeDeferJS)` — but module ids are not URLs, so `process_urls` split on `\n` may mis-split ids) | Module `excludeDeferJS` entered as `wppo-lazyload` handle (not URL) but checked via URL-oriented helper; works today because `process_urls` just trims/splits, but semantic mismatch. `deferred_handles` map is correct pattern for defer. | Align: for modules, match exact id (`in_array($id, $this->exclude_defer_js, true)`) not via URL helper. Document that `excludeDeferJS` textarea accepts both handles and URL substrings. | Medium |
- | Q-17 | **INFO** | `Cron::schedule_cron_jobs` reads `get_option('wppo_settings')` twice (`options` + `options_llms`) | `includes/class-cron.php:114` (`$options = get_option('wppo_settings', array())` for preload/img/database/web_vitals) + `141` (`$options_llms = get_option('wppo_settings', array())` for llms) | Harmless double read (WordPress `wp_cache_get` hits on second), but inconsistent variable naming (`options` vs `options_llms`). `schedule_cron_jobs` runs on `init` every request, so 2 `get_option` calls per request. | Single read: `$options = get_option('wppo_settings', [])` then reuse for `llms_txt` check at `142` (`$options['llms_txt']['enabled']`). | High |
- | Q-18 | **INFO** | `Core_Tweaks` takes `file_optimisation` slice, not full options (abstraction) | `includes/class-main.php:306` (`new Core_Tweaks($this->options['file_optimisation'] ?? array())`) + `includes/class-core-tweaks.php:30` (`__construct(array $file_optimisation_opts)`) | Correct narrow dependency (only needs 5 keys: `disableEmojis` etc.). Private `file_optimisation` slice is stable contract. | No change — good. Keep narrow constructor for testability. | High |
- | Q-19 | **INFO** | `allSuggestions` dedup by `metric` key may hide distinct telemetry vs pagespeed suggestions (edge case) | `src/components/Dashboard.js:75-88` (`useMemo` merges `pagespeedSuggestions` + `telemetrySuggestions`, `seen.has(s.metric)` dedup by `metric` only, pagespeed first) | If telemetry and PageSpeed both suggest `performance_score` but with different `message`/`severity`, telemetry suggestion is dropped (pagespeed wins). Order `pagespeed` first is intentional (PageSpeed more authoritative). No bug. | Document dedup key (`metric`) and order (pagespeed > telemetry). Keep. | High |
- | Q-20 | **INFO** | `wppo_role_hash` cookie `SameSite` missing (standards) | `includes/class-main.php:361` (`setcookie('wppo_role_hash', $hash, time()+DAY_IN_SECONDS, COOKIEPATH, COOKIE_DOMAIN, is_ssl(), true)` — `httponly=true` correct, but `SameSite` not set; PHP 7.3+ `setcookie` array options `['samesite'=>'Lax']` supported, min PHP is 8.2) | Modern browsers default `SameSite=Lax` if absent, so no breakage; but cross-site `SameSite=Lax` blocks cookie on some flows (e.g. `advanced-cache.php` reads `$_COOKIE['wppo_role_hash']` server-side, so Lax is fine). Explicit `SameSite=Lax` hardens. | Add `setcookie('wppo_role_hash', $hash, ['expires'=>time()+DAY_IN_SECONDS,'path'=>COOKIEPATH,'domain'=>COOKIE_DOMAIN,'secure'=>is_ssl(),'httponly'=>true,'samesite'=>'Lax'])` (PHP 7.3+ array syntax, safe on 8.2). | High |


> Source agents: agent-A01-php-correctness.md, agent-A02-php-media.md, agent-A03-php-infra.md, agent-A04-php-rest-cli.md, agent-A05-js-spa.md, agent-A06-js-vanilla.md, agent-A07-css.md, agent-A08-security.md, agent-A09-performance.md, agent-A10-duplication-deadcode.md, agent-A11-compatibility.md, agent-A12-quality-architecture.md — full evidence in each agent file.
