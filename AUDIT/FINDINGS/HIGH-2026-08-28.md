# FINDINGS/HIGH — 2026-08-28 (deduped ~12)

**Base:** `master@31fffc61` | **Agents:** A01-A14

| # | File:Line | Category | Problem | Why matters | Evidence | Impact | Fix |
|---|-----------|----------|---------|-------------|----------|--------|-----|
| H-01 | `includes/class-image-optimisation.php:2800` | Correctness | `count($matches)==5` invariant (4 groups → always 5) routes all `<img>`/`<picture>` to iframe lazy path | Lazy-load broken on WP<6.4 regex fallback | `preg_match` 4 groups | WP<6.4 sites lazy broken | Fix `count>=4` or explicit group check | A02 HIGH |
| H-02 | `includes/class-ai-adaptive.php:279` | Correctness | `>3500` and `>2500` both assign `moderate`, `eager` unreachable | Speculation eagerness never eager | `if ($lcp>3500) $e='moderate'; elseif ($lcp>2500) $e='moderate';` | AI never suggests eager | Make second `>1500` eager or adjust thresholds | A05 HIGH |
| H-03 | `includes/class-od-bridge.php:318` | Correctness | non-LCP `else` branch adds non-LCP URLs to LCP list, wrong `fetchpriority` | LCP prediction inverted | `else { $lcp_urls[] = $url; }` | Wrong fetchpriority, LCP regression | Remove else or use `$non_lcp` | A05 HIGH |
| H-04 | `includes/class-bfcache.php:270` | Correctness | `filter_nocache_headers` dead inner `null!==$token` (always true after outer), cookie repair never runs | bfcache token repair dead | `if (null !== $token) { // inner }` after `if ($token)` | bfcache cookie never repaired | Remove dead branch | A05 HIGH |
| H-05 | `includes/class-asset-manager.php:92` | Correctness | `is_user_logged_in` blanket bail defeats logged-in cacheable dequeuing | Logged-in cacheable scripts not dequeued | `if (is_user_logged_in()) return;` | Wasted LCP on logged-in | Gate on `is_admin` only | A05 HIGH |
| H-06 | `includes/class-metabox.php:54` | Correctness | preload metabox screen `''` never displays | Preload UI missing | `add_meta_box(...,'',...)` | Feature invisible | Use `post`+`page` | A05 HIGH |
| H-07 | `includes/class-ai-adaptive.php:246` | Correctness | `asort` picks rarely-disabled scripts (inverted), `exclude_css` never populated | AI learns wrong handles | `asort($freq)` picks lowest freq | Wrong optimization | Use `arsort` | A05 HIGH |
| H-08 | `templates/bunny-edge.js:28` | Perf/Correctness | Uses Cloudflare `caches.default` API invalid for Bunny `storage` | Bunny edge never caches | `caches.default` Bunny | Edge adapter broken | Use Bunny storage API | A07 HIGH |
| H-09 | `includes/class-cache.php:396` | Performance | `combine_css` triple classify: double sim + second filesize loop = 3600 comparisons/30 handles | 3-6ms churn per render | `get_combined_handles`+`core_will_inline`+`should_skip_combine` | Render slow | Single-pass | A10 HIGH |
| H-10 | `includes/class-main.php:285` | Frontend | Shared `AbortController` (`App.js:285`) cancels sibling requests on abort | SPA race cancels wrong fetch | Single AbortController | Lost updates | Per-request controller | A06 HIGH |
| H-11 | `includes/class-main.php:489` (CRITICAL→HIGH) | Arch | God class `Main` 3053 McCabe>30 | Maintainability, testability | 30+ responsibilities | High churn risk | Facade extraction | A14 HIGH |
| H-12 | `templates/cloudflare-worker.js:85` + `:52` | Security/Perf | `private`/`Set-Cookie` leak + `Vary` fragmentation `X-Edge-Cache` | Cache poisoning, variance | `if (private) cache` | Privacy leak | Bypass cache on private | A07 HIGH |

