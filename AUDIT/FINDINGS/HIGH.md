# FINDINGS — HIGH

_Consolidated from 12 agents (2286 lines). Each finding below is traceable to `AUDIT/AGENTS/agent-*.md` file:line evidence. This shard groups by severity HIGH._

## Index

### agent-A01-php-correctness.md

- > Severities: `CRITICAL` > `HIGH` > `MEDIUM` > `LOW` > `INFO` > `OPTIMIZATION` > `DUPLICATE` > `DEAD CODE`
- | P01 | LOW | `performance-optimisation.php:7` | plugin header | `Tested up to:      7.1` — WP latest stable is 6.9 (Aug 2026); `7.1` does not exist. | Misleading WP.org readme parser & users; may suppress compatibility warnings. | Set to actual tested version (e.g. `6.9` or `6.8.*`) and automate bump in `tri-merge-cycle.yml`. | HIGH |
- | P02 | INFO | `performance-optimisation.php:28-38` | constants | `if ( ! defined( 'WPPO_PLUGIN_PATH' ) ) define(...)` + `WPPO_VERSION '1.9.0'` hard-coded duplicate of header `Version: 1.9.0`. | Drift if header bumped without constant bump (cache/migrate gates use `WPPO_VERSION`). | Source `WPPO_VERSION` from header via `get_file_data()` once, or generate at release. | HIGH |
- | P04 | INFO | `performance-optimisation.php:44` | bootstrap | `new Main();` instantiated at file load, before `plugins_loaded`. | Constructor runs `get_option('wppo_settings')` + side-effects on every request type (CLI, REST, cron). | Document intentional early boot; consider `plugins_loaded` hook if lazy boot desired. | HIGH |
- | U01 | INFO | `uninstall.php:29` | `wppo_cleanup_site()` | `$wpdb->query("DROP TABLE IF EXISTS {$table_name}")` // phpcs:ignore `PreparedSQL.InterpolatedNotPrepared` | `$table_name` is `$wpdb->prefix . 'wppo_activity_logs'` — prefix is sanitized by core, injection not exploitable, but direct interpolation violates PHPCS and hides intent. | Use `$wpdb->prepare()` with identifier or keep ignore but document prefix is core-provided. | HIGH |
- | U05 | LOW | `uninstall.php:93` | `wppo_cleanup_site()` | `$transient_prefix = is_multisite() ? get_current_blog_id().'_':''` — transient prefix logic duplicates `Util::transient_key()` but not via that helper (uninstall cannot autoload `Util`). | Drift if `Util::transient_key()` changes format (e.g. adds network prefix). | Add comment referencing `Util::transient_key()` and keep manual duplication (unavoidable at uninstall). | HIGH |
- | U07 | INFO | `uninstall.php:142-164` | multisite loop | `get_sites(['number'=>100,'offset'=>...])` + `has_more = count==limit`; `switch_to_blog/restore_current_blog` paired. Correct. `is_multisite() && function_exists('get_sites')` guard is belt-and-suspenders for pre-4.6. | — | No change needed. Confirms correct multisite isolation via `Util::transient_key` elsewhere. | HIGH |
- | U08 | INFO | `uninstall.php:57-63` | `wppo_cleanup_site()` | Hard-coded paths `WP_CONTENT_DIR . '/cache/wppo/'` and `'/wppo/'` with comment `must stay in sync with Cache::CACHE_DIR` — duplication noted. | Drift if `Cache::CACHE_DIR` constant changes. | Keep comment; consider defining path in shared constant file included by both (not possible at uninstall). | HIGH |
- | A01 | DEAD CODE | `advanced-cache-handler.php:196` | `Advanced_Cache_Handler::create()` (generated drop-in) | `$cookie_prefix = 'wp-wpml_';` inside `is_user_logged_in_without_wp()` — variable assigned, never read. | Dead code in generated `advanced-cache.php`; minimal runtime cost, confuses audit. | Remove line. | HIGH |
- | A02 | DEAD CODE | `advanced-cache-handler.php:142-194` | `Advanced_Cache_Handler::create()` | `$site_url` baked via `var_export(home_url(),true)` then function `is_user_logged_in_without_wp($site_url)` receives it but never uses `$site_url` inside (uses `$cookie_hash` literal). | Unused parameter + unused variable in drop-in; suggests incomplete refactor. | Either use `$site_url` (e.g. for domain-scoped cookie hashing) or remove param and the `$site_url_escaped` bake. | HIGH |
- | A05 | DUPLICATE | `advanced-cache-handler.php:200-274` | generated drop-in | Three identical header/304 blocks for `br`/`gzip`/`html` ( `Last-Modified`, `ETag`, `Content-Type`, `Vary`, `X-Content-*`, `Referrer-Policy`, `If-Modified-Since`/`If-None-Match`, `readfile`/`exit`). | 90 lines duplicated; maintenance burden, typo risk. | Keep duplication in string — acceptable for drop-in generation simplicity; flag as intentional. | HIGH |
- | A07 | LOW | `advanced-cache-handler.php:165-166` | generated drop-in | `if (''===$site_domain \|\| strpos($site_domain,'..')!==false \|\| strpos($request_uri,'..')!==false) return;` — prevents `..` but not encoded `%2e%2e` or `//` normalization earlier? | Encoded traversal bypass possible before `rawurldecode`. | Check occurs after `rawurldecode` (line 161) — correct; note that `%2e%2e` is decoded, so covered. Document. | HIGH |
- | A09 | INFO | `advanced-cache-handler.php:88-110` | `foreign_dropin_present()` | Duplicates `exists` check already done in `is_our_dropin()`; `foreign = exists && !is_our`. | Two filesystem reads per `create()` call. | Cache result or reuse. Negligible. | HIGH |
- | H01 | LOW | `htaccess-handler.php:158-175` | `Htaccess_Handler::get_rules()` | Fallback next-gen logic duplicates `Server_Rules::get_nginx_rules()` fallback (reads `wppo_settings` + `Server_Rules::is_litespeed()` + `apply_filters('wppo_litespeed_nextgen_rewrite')`). Same pattern in two files. | Drift if gating changes in one place only. | Extract to `LiteSpeed_Integration::is_nextgen_rewrite_enabled()` (already exists) and remove duplicate fallback once that class is always loaded. | HIGH |
- | H02 | LOW | `htaccess-handler.php:202-208` | `Htaccess_Handler::get_rules()` | DocBlock `@param bool $use_nextgen Whether next-gen block was added.` but `apply_filters('wppo_htaccess_nextgen_rules', $rules)` actually filters `array $rules`. Same mismatch in `Server_Rules.php:156-162`. | Misleads IDE/static analysis (Psalm). | Fix `@param array $rules`. | HIGH |
- | H04 | INFO | `htaccess-handler.php:53-85` | `Htaccess_Handler::update_rules()` | Correctly uses `insert_with_markers($htaccess, 'wppo_rules', $rules)` — per AGENTS.md, must not touch `wordpress` or `LSCACHE` markers. Guard `function_exists('insert_with_markers')` + `require_once ABSPATH.'wp-admin/includes/misc.php'`. | Correct ordering preservation for LiteSpeed. | No change. | HIGH |
- | H05 | INFO | `htaccess-handler.php:64-76` | `Htaccess_Handler::update_rules()` | `Util::init_filesystem()` returns `WP_Filesystem` object; checks `!exists` + `is_writable(dirname)` vs `is_writable(file)`. Belt-and-suspenders for missing `.htaccess`. | Correct writable checks before `insert_with_markers`. | No change. | HIGH |
- | S02 | DUPLICATE | `server-rules.php:118-134` | `Server_Rules::get_nginx_rules()` | Next-gen map fallback identical to `Htaccess_Handler` — `get_option('wppo_settings')`, `$enabled && $convert`, `apply_filters('wppo_litespeed_nextgen_rewrite')`. | Same drift risk as H01. | Centralize in `LiteSpeed_Integration`. | HIGH |
- | S03 | LOW | `server-rules.php:156-162` | `Server_Rules::get_nginx_rules()` | DocBlock `@param bool $use_nextgen` mismatches `apply_filters('wppo_nginx_nextgen_rules', $rules)` array. | Same Psalm/doc issue as H02. | Fix docblock. | HIGH |
- | S05 | INFO | `server-rules.php:34-51` | `Server_Rules::get_server_type()` | `sanitize_text_field(wp_unslash($_SERVER['SERVER_SOFTWARE']))` then `strtolower` then `strpos` checks `litespeed`/`openlitespeed` before `apache`/`nginx`. Order correct (LiteSpeed often reports `Apache` as well). | Correct detection. | No change. | HIGH |
- | M01 | DUPLICATE | `class-main.php:388-391, performance-optimisation.php:41` | `Main::includes()` / bootstrap | Both bootstrap and `Main::includes()` do `require_once WPPO_PLUGIN_PATH.'vendor/autoload.php'` + Action-Scheduler include guarded by `file_exists`. | Double require is harmless (`require_once`) but hides missing-vendor failure path twice. | Keep one canonical require in bootstrap; remove from `includes()`. | HIGH |
- | M04 | INFO | `class-main.php:419-426` | `setup_hooks()` | Registers `admin_init` hooks for `maybe_fix_wp_cache`, `maybe_run_upgrades`, `maybe_run_version_upgrade`, `maybe_migrate_block_assets_setting` — each checks caps/transients inside, so idempotent. | Correct: `admin_init` never fires on pure frontend `advanced-cache.php` hit. | No change. | HIGH |
- | M07 | HIGH | `class-main.php:466` | `setup_hooks()` | `add_action('admin_bar_menu', add_setting_to_admin_bar, 100)` registered unconditionally (every request), not gated on `is_admin_bar_showing()` or `current_user_can('manage_options')`. Callback creates 2 nodes even for subscribers (though visibility via `manage_options` not checked inside `add_setting_to_admin_bar`). | Subscribers see "Performance Optimisation" in admin bar via `add_node` if bar is shown — information disclosure; REST/API requests still register hook overhead. | Gate registration: `if (current_user_can('manage_options')) add_action(...)` or inside callback `if (!current_user_can('manage_options')) return;`. Original already does not check caps inside callback. | MEDIUM |
- | M08 | INFO | `class-main.php:432-465` | `setup_hooks()` defer/delay gating | `$is_wp63_plus = version_compare($wp_version,'6.3-alpha','>=')` then branches `add_defer_strategy` (wp_enqueue_scripts 1000) vs `add_defer_attribute_legacy` (script_loader_tag) + `add_fetchpriority_to_deferred` only when `!$is_wp69_plus`. Logic mirrors `is_wp69_plus` (6.9-alpha floor for `wp_should_output_buffer_template_for_enhancement`). | Correct version gating per AGENTS.md TODO #553. Documented for removal when min WP bumps. | No change. | HIGH |
- | M09 | LOW | `class-main.php:472-486` | `setup_hooks()` cache branch | `if (!empty(cache_settings.enableCache)) { $this->cache=new Cache(); $this->cache->set_image_optimisation/image_optimisation ... if (wp_should_output_buffer_template_for_enhancement && $is_wp69_plus) add_filter(...process_buffer_for_cache) else add_action(template_redirect, start_output_buffer)}` — legacy `template_redirect` fallback still registered on WP 6.9+ if function absent? Actually checks `function_exists` first, so OK. But `$is_wp69_plus` checks `wp_version` string vs `'6.9-alpha'` — alpha/beta/RC builds correctly match due to inclusive floor (comment at 440-442). | Correct. Minor: `$wp_version` from `$GLOBALS` may be string like `6.9-RC1` — `version_compare('6.9-RC1','6.9-alpha','>=')` returns true (RC > alpha). Works. | No change. | HIGH |
- | M12 | INFO | `class-main.php:569-614` | defer/delay setup | `excludeDeferJS`/`excludeDelayJS` parsed via `Util::process_urls()` then `apply_filters('wppo_exclude_defer_js')` etc. When both defer+delay active, `exclude_delay_js = array_merge(exclude_delay_js, exclude_defer_js)` so defer handles are never delay-rewritten — comment at 627-633 correctly explains corruption avoidance. | Correct mutual exclusion; prevents `wppo-src` overriding native `defer` strategy. | No change. Good. | HIGH |
- | M13 | LOW | `class-main.php:640-670` | delay strategy parsing | `delayJSDefaultStrategy = sanitize_text_field(...) ?: 'interaction'`; `delayJSIdleList/ViewportList` via `Util::process_urls`; `delayJSPriority` via `explode(':',$line,2)` + `trim` + whitelist `high/normal/low`. Empty lines ignored. | Tiny functionality: idle timeout defaults to 3000ms; site operators can set per-handle priority via `handle:high` syntax. Correct. | No change. | HIGH |
- | M14 | LOW | `class-main.php:672-674` | `setup_hooks()` preload | `add_action(wp_head, add_preload_prefetch_preconnect,1)` + `add_speculation_rules,0` + `add_filter(wp_resource_hints,10,2)` — all correctly documented to keep preload vs resource-hints separation (preload needs `as`/`type`, hints need core dedup). | Correct. | No change. | HIGH |
- | M15 | INFO | `class-main.php:682-686` | critical CSS | `if (!empty(criticalCSS)) { add_action(wp_head, inline_ccss,0); add_filter(style_loader_tag, defer_stylesheets); add_action(wppo_generate_ccss, background_generate)}` — correct hooks. | — | — | HIGH |
- | M16 | LOW | `class-main.php:787-826` | `migrate_block_assets_setting()` | Uses `get_option('wppo_block_assets_migrated')` marker + checks `is_array(stored)` then `array_key_exists('blockAssetsOnDemand', $file)`. Fresh installs (no stored option) skip migration and set marker `1`. Correct one-time backfill for WP 6.9 `blockAssetsOnDemand` ON default. | Tiny toggle: on 6.9+, classic themes default to separate block assets; migration opts old installs into `true`. | No change. | HIGH |
- | M18 | LOW | `class-main.php:911-938` | `maybe_run_version_upgrade()` | Checks `current_user_can(manage_options)` + `version_compare(installed,WPPO_VERSION)>=` bail; then `if (!Advanced_Cache_Handler::create()) return` (leaves version unchanged so retry later); then `if (!foreign_dropin_present()) Cache::clear_cache()` then `update_option(wppo_version, WPPO_VERSION, false)` (autoload `false`). Correct idempotency, handles transient filesystem failure. | One subtle: `Advanced_Cache_Handler::create()` returns `true` when foreign drop-in present (leave alone) — then `foreign_dropin_present()` second call returns true again, so `clear_cache` skipped (correct). | No change. | HIGH |
- | M20 | LOW | `class-main.php:1093-1096` | `on_save_post_invalidate_cache()` | Checks `wp_is_post_revision || wp_is_post_autosave` bail, then `$this->cache->invalidate_dynamic_static_html`. But `$this->cache` may be `null` if `enableCache` off — correctly guarded. However, hook is registered only when cache enabled (`if (!empty(enableCache)) add_action(save_post, on_save_post_invalidate_cache)`) — so null path unreachable. | Correct. | No change. | HIGH |
- | M23 | LOW | `class-main.php:1356-1377` | `get_frontend_theme_colors()` | Reads `wp_get_global_settings()['color']['palette']['theme']` — assumes `palette.theme` is array of `['slug','color']`; skips entries where `sanitize_hex_color` fails. Then falls back to `get_theme_mod('primary_color')` etc., then `get_header_textcolor()`. Each `sanitize_hex_color` may return `null` on empty, then filtered via `array_filter`. | Correct. Minor: `sanitize_title` on slug may mangle camelCase. | No change. | HIGH |
- | M24 | LOW | `class-main.php:1419-1432` | `admin_enqueue_scripts()` | `realpath` + `strpos($resolved, WPPO_PLUGIN_PATH)===0` traversal guard before `require`. Correct. But `WPPO_PLUGIN_PATH` is normalized via `wp_normalize_path`, while `realpath` returns OS-native separators; on Windows mismatch possible, but `wp_normalize_path` normalizes both. | Correct. | No change. | HIGH |
- | M25 | INFO | `class-main.php:1466-1473` | `admin_enqueue_scripts()` | Redacts `pagespeed_api_key` and `object_cache.password` from `wppoSettings.settings` before `wp_localize_script`. | Correct PII redaction. | No change. | HIGH |
- | M26 | LOW | `class-main.php:1488-1495` | `admin_enqueue_scripts()` | `client_side_media_processing_enabled => function_exists('wp_is_client_side_media_processing_enabled') && wp_is_client_side_media_processing_enabled()` — evaluated after `Image_Optimisation` has registered opt-out filter (comment says so). | Correct lazy evaluation. | No change. | HIGH |
- | M29 | INFO | `class-main.php:1673-1720` | `remove_woocommerce_scripts()` | Parses `excludeUrlToKeepJSCSS` via `Util::process_urls`, compares `current_url` vs each via `Util::is_url_excluded` (handles `(.*)` suffix). Then dequeues `style:` vs `script:` prefixed handles. Correct boundary-aware base-path stripping for subdirectory installs. | Correct. | No change. | HIGH |
- | M31 | LOW | `class-main.php:1935-1940` | `matches_delay_pattern()` | `preg_match('/\b'.preg_quote($pattern,'/').'\b/', $handle)` — word-boundary matching prevents `slide`→`slider` false positive; `preg_quote` escapes URL metachars. Empty pattern returns false. | Correct. Tiny functionality: dash-delimited prefix still matches because `\b` before `-`? Actually `-` is non-word, so `\b` between word char and `-` counts as boundary, so `jquery` matches `jquery-core` at start— intended. Documented. | No change. | HIGH |
- | M32 | LOW | `class-main.php:2004-2046` | `apply_per_page_delay_config()` | Reads `_wppo_delay_strategies`/`_wppo_delay_priorities` post meta; merges into in-memory `delay_js_*` lists. No `sanitize_text_field` on meta values beyond exact string checks. | Correct whitelist checks `in_array` for allowed values. | No change; note meta is admin-controlled. | HIGH |
- | M33 | LOW | `class-main.php:2097-2112` | `add_preload_prefetch_preconnect()` | Converts relative font/CSS URLs via `Util::cached_content_url($font_url)` when not `https?://`. Then `Util::generate_preload_link` echoes. Correct. | — | — | HIGH |
- | M34 | LOW | `class-main.php:2135-2173` | `add_resource_hints()` | `preconnect` returns array of `['href'=>origin,'crossorigin'=>'anonymous']`; `dns-prefetch` normalizes bare hostnames to `//host`. Core's `wp_resource_hints` will deduplicate. | Correct. Note `preconnectOrigins` origins are taken raw without `https://` prefix validation — user may enter `https://cdn.example.com` or `cdn.example.com`; both handled (core normalizes). | — | HIGH |
- | M35 | LOW | `class-main.php:2288-2314` | `filter_speculation_rules_configuration()` | Pins `conservative` when cache enabled && `eagerness == 'auto'` && no host constant override. Honors `WP_SPECULATIVE_LOADING_DEFAULT_*` via `get_speculation_default_override`. Correct per WP 7.1 #64066/#65624 handling. | — | — | HIGH |
- | M36 | LOW | `class-main.php:2335-2367` | `get_speculation_default_override()` | Reads `getenv` then `defined(const)` so constant wins over env (matches core precedence). Validates `mode` in `prefetch/prerender` and `eagerness` in `conservative/moderate/eager`. | Correct. Tiny: `getenv` may be disabled via `disable_functions`. Guarded by `function_exists('getenv')`. | No change. | HIGH |
- | M37 | LOW | `class-main.php:2384-2528` | `strip_static_query_strings()` / `is_plugin_cache_url*` | Removes only `ver` keys via `rawurldecode($key)==='ver'`; preserves other keys byte-for-byte (duplicate keys, percent-encoding, ordering). Skips plugin cache URLs (`/cache/wppo/`) origin-aware via `Util::cached_content_url('/')` host check; static per-blog cache with `has_filter('content_url')` bypass. | Correct cache-busting preservation. Static cache keyed by `get_current_blog_id()` but not cleared on `switch_to_blog` outside typical flow — minor staleness but blog_id check covers. | No change. | HIGH |
- | M38 | LOW | `class-main.php:2557-2587` | `minify_queued_styles()` | Early bails if `wp_maybe_inline_styles` missing (pre-5.8), logged-in check, `combineCSS` guard. Loops `wp_styles->queue`, skips non-`all` media, then `get_minifiable_css_path` + `Minify\CSS->minify()`. | Correct. | — | HIGH |
- | M39 | LOW | `class-main.php:2640-2721` | `get_minifiable_css_path()` / `minify_css()` / `minify_js()` | Shared helper checks `is_minified_asset_name` regex `(\.min\|\.bundle\|-min)\.css$` (case-insensitive) + `wppo_exclude_minification` filter + `is_file_minified` heuristic. | Consistent. | No change. | HIGH |
- | M41 | LOW | `class-main.php:2897-2907` | `sanitize_image_info_for_client()` | Replaces path arrays with counts for `wppoSettings.image_info`. Prevents filesystem disclosure via View Source. | Correct. | No change. | HIGH |
- | M42 | LOW | `class-main.php:2918-2954` | `enqueue_admin_bar_script()` | Same `realpath`+`strpos` guard as admin page; `wp_enqueue_script` with `fetchpriority=low` via array args (WP 6.5+ style). | Correct. Note inline `wppoObject` JSON includes `apiUrl`+`nonce` — nonce is per-request `wp_rest` — correct. | — | HIGH |
- | C01 | INFO | `class-cache.php:40` | `Cache::CACHE_DIR` | `private const CACHE_DIR = '/cache/wppo';` — used to build `WP_CONTENT_DIR . CACHE_DIR`. Sync comment in `uninstall.php:56`. | Drift if constant changed without updating uninstall. | Keep sync comment; consider extracting to shared constant file. | HIGH |
- | C02 | LOW | `class-cache.php:200-254` | `__construct()` | Domain validation: `sanitize_text_field(wp_unslash(HTTP_HOST))`, `idn_to_ascii` (INTL_IDNA_VARIANT_UTS46), strip port via `explode(':',domain,2)[0]`, regex `^[a-z0-9\.\-]+$`, reject `.. / \`. On failure sets `domain=''`. | Correct IDN + port + traversal handling. Minor: `sanitize_text_field` before `idn_to_ascii` may strip valid IDN; order should be `idn_to_ascii` first? However `sanitize_text_field` preserves UTF-8, so puny conversion after is safe. | Document order rationale. | HIGH |
- … +13 more in agent-A01-php-correctness.md

### agent-A02-php-media.md

- Severity legend: **CRITICAL** = exploitable / data-loss; **HIGH** = correctness / security regression; **MEDIUM** = functional bug / measurable perf / a11y; **LOW** = minor edge / style; **INFO** = observation / confirmation; **OPTIMIZATION** = perf improvement opportunity; **DUPLICATE** = copy-pasted logic; **DEAD CODE** = unreachable / never-executed.
- | A02-001 | HIGH | `class-abilities.php:270-278` + `449-475` | Input enum mismatch blocks DB-cleanup abilities | Operational ability `run-database-cleanup` input_schema enum is `['revisions','auto_drafts','trash','spam','transients','orphans','all']` (line 275). Executor validates against `['revisions','auto_drafts','trashed_posts','spam_comments',…]` (line 451) then maps via `$method_map` keys matching the second set (line 465). A caller conforming to the JSON schema (e.g. `trash`) is rejected -> `cleaned:0`; schema-valid values never reach `invoke_cleanup_method`. The SPA REST endpoint uses different strings again (`docs` unknown). Evidence: `class-abilities.php:274-276` vs `451-475`. | Align enum to one canonical set. Either expand `input_schema` enum to the `valid_types` values or add normalisation (`trash`→`trashed_posts`, `spam`→`spam_comments`, etc.) before `in_array`. Add test covering schema→executor path. |
- | A02-002 | HIGH | `class-image-optimisation.php:576-697` | `maybe_serve_next_gen_images` regex fallback loses `<source>`/`<video poster>` and misses `normalize_url` | Tag-Processor branch (591-652) handles `IMG src/srcset` + `SOURCE srcset` + `VIDEO poster`. Regex fallback (654-697) only matches `<img ... src|srcset ...>` and never rewrites `<source>` or `<video poster>`. It also calls `is_valid_url(url)` on raw src without `normalize_url()` (663, 681), so protocol-relative (`//cdn…/x.jpg`) and root-relative (`/uploads/x.jpg`) URLs are discarded in fallback but served in primary path. Evidence: `591-652` vs `654-697`; `663: is_valid_url($url)` vs `600: normalize_url($src) + is_valid_url`. | In fallback, add `<source>` and `<video poster>` handling or document that fallback is best-effort for WP <6.2 and explicitly note gap. At minimum add `normalize_url()` before `is_valid_url()` in fallback. |
- ### Duplicates — HIGH maintenance debt (DUP-001 … DUP-006)

### agent-A03-php-infra.md

- > Severity: `CRITICAL` > `HIGH` > `MEDIUM` > `LOW` > `INFO` > `OPTIMIZATION` / `DUPLICATE` / `DEAD CODE`
- | A03-001 | HIGH | `class-database-cleanup.php:1093-1094` — `optimize_table()` | Security / DB | `OPTIMIZE TABLE {$full_table_name}` is interpolated without backticks or allowlist. `maybe_optimize_tables()` deduplicates but does not validate `$table` against `TABLE_MAP` values; any internal caller passing an arbitrary string could optimize an unintended table. `$wpdb->{$table}` is dynamic and could resolve to empty. | Low-probability privilege-adjacent misuse; on managed hosts `OPTIMIZE` may be blocked or lock large tables despite 1 GB guard (which relies on `information_schema` that may be inaccessible returning 0). | Allowlist `maybe_optimize_tables`/`optimize_table` to `array('posts','postmeta','comments','commentmeta','options')` and wrap as `` `{$wpdb->prefix}{$validated}` `` or use `$wpdb->query("OPTIMIZE TABLE `{$full_table_name}`")` after `preg_match('/^[A-Za-z0-9_]+$/', $full_table_name)`. | high |
- | A03-002 | HIGH | `class-database-cleanup.php:630-668` `clean_unattached_media()` | Performance / Safety | Loops `wp_delete_attachment(..., true)` per attachment inside `do…while(batch=500)`. Each call loads post, fires `delete_attachment`/`deleted_post` hooks, unlinks N intermediate sizes. No `set_time_limit`/`wp_suspend_cache_addition` and no `OPTIMIZE` gating on failure count. A media library with 20k unattached images will issue 20k `unlink` + DB deletes within a single REST request (no Action Scheduler sharding). | Request timeout / OOM on large libraries; hook side-effects (e.g. offloaded media plugins) may re-create rows mid-loop causing infinite pagination. | Shard via Action Scheduler (like image conversion) or document single-batch admin use only + add `wp_suspend_cache_invalidation` and chunk size filter. Alternatively add `wppo_database_cleanup_batch_size` filter. | high |
- | A03-003 | HIGH | `templates/object-cache.php:544-552,585-605,556-565,608-618` `flush()`/`flush_group()` | Performance / Correctness | SCAN+DEL uses `100` COUNT and `$redis->del($keys)` per SCAN page. On a site with 500k keys, flushing the site prefix scans entire keyspace (O(N) network round-trips). Pre-7.4 phpredis `del(array)` semantics differ; cluster path deletes per node but still iterates all shards. No `UNLINK` (non-blocking delete). | Flush on settings save / purge-all may block PHP for seconds and spike Redis CPU; on Cluster mode spec cites `_masters()` which is undocumented and may not include replicas. | Use `UNLINK` when available (`$redis->unlink` fallback to `del`), raise COUNT to 500, and document that `object_cache_allow_flush_all=true` is the production path for large caches. Add `try/catch` around `_masters()`. | medium |
- | A03-004 | HIGH | `class-telemetry.php:682-743` `calculate_sizes()` | Performance / SSRF-scope | `$get_size` HEAD-fallback fires a `wp_remote_head` per local-unresolvable same-host asset, serially, with 5 s timeout. A page with 40 CDN-rewritten URLs that still match `home_host` after CDN filter will issue 40 HEADs synchronously inside `scan()` (which already made 1 GET). Each HEAD also re-validates via `wp_http_validate_url` + `apply_filters('wppo_telemetry_verify_ssl')`. | Scan latency: worst case ~200 s (40×5 s) before timeout handling; ties up admin AJAX worker. | Cap HEADs (e.g. first 10) or make `calculate_sizes` return 0 for non-local paths without HEAD, and compute sizes from `Content-Length` only when header already present from primary GET. Alternatively batch with `wp_remote_head` curl-multi. | high |
- | A03-005 | HIGH | `class-cron.php:473-544,547-563` `get_sitemap_urls()` + `schedule_sitemap_url_jobs()` | Availability / Loop | `get_sitemap_urls(500)` does `wp_remote_get` with 5 s timeout per sitemap child, bounded by 15 s wall clock but still called from `schedule_page_cron_jobs()` which holds the 20-min preload lock via transient. If `wp-sitemap.xml` is 50 child sitemaps (TO_FETCH_LIMIT=50) the lock remains for the full 15 s, blocking concurrent cron workers. No `wp_http_validate_url` on child locs before fetch (relies only on loc_host check after parsing). | Stale preload cycle; on slow sitemap hosts the lock expiry (20 min) may cause duplicate `wppo_generate_static_url` scheduling on next tick because `has_more` path re-enters. | Move sitemap discovery off the lock (fetch first, then acquire lock only for scheduling), add `wp_http_validate_url` + `esc_url_raw` before `wp_remote_get($loc)`, and expose `wppo_sitemap_timeout` filter. | medium |
- **Fix before merge (HIGH):** A03-001, A03-002, A03-003, A03-004, A03-005

### agent-A04-php-rest-cli.md

- | R01 | HIGH | `class-rest.php:218-223` | `Rest::get_routes` / `rum_collect` | `'rum_collect' => ['methods'=>'POST','callback'=>collect_rum,'permission_callback'=>'__return_true']` — only REST route with `__return_true`; comment says token+rate-limited, but no `args` schema / `validate_callback` at route registration. | Public endpoint brute-forceable if `RUM::collect()` token or IP limiter has bug; DDoS on options table (`wppo_rum_data`). | Add `args` schema with `token` required + explicit `validate_callback`; add `rate_limit` test covering 403 after N requests; document token generation. | HIGH |
- | R03 | MEDIUM | `class-rest.php:376-387` | `clear_cache` | `if(''!==$path){ $real_path=realpath($this->cache_dir.$path); ... if(false===$real_path || (!is_exact_match && !is_under_dir)) return 400; }` — `realpath` returns false when target does not exist (uncached page) or cache_dir missing; then request rejected as `Invalid path`. | `clear_single_page_cache` for never-cached URL always 400; UI "Clear This Page" fails on first visit. Also `$this->cache_dir.$path` missing slash if `$path` no leading `/` → false reject. | Validate via normalized string prefix `strpos(wp_normalize_path($this->cache_dir.$path), trailingslashit(normalize($this->cache_dir)))===0` without `realpath`, or `realpath` on parent dir; ensure slash join `trailingslashit(cache_dir).ltrim(path,'/')`. | HIGH |
- | R04 | LOW | `class-rest.php:389-406` | `clear_cache` action dispatch | `if('clear_single_page_cache'===$action){...} else { Cache::clear_cache(); }` — any non-`clear_single_page_cache` value (typo, `clear`, empty) clears **all** cache. No whitelist error. | Accidental full purge on malformed request (e.g. frontend JS bug sending `action:''`). | Whitelist: `if('clear_single_page_cache'===action) else if('clear_all'===action \|\| ''===action) else return 400 Invalid action`. | HIGH |
- | R06 | MEDIUM | `class-rest.php:423-434,712-728` | `update_settings` / `import_settings` tab allowlist drift | `update_settings` allowed_tabs: `file_optimisation,preload_settings,image_optimisation,database_cleanup,object_cache,performance_audit,cache_settings,litespeed_integration,llms_txt` (9). `import_settings` same 9. CLI `WPPO_CLI_Command::settings` allowed_keys adds `core_tweaks` but omits `litespeed_integration,llms_txt` (8). `Activate::maybe_seed_settings` / `Util::sanitize` not gated. | `core_tweaks` settings saved via CLI `wppo settings update core_tweaks` succeed, but `update_settings` REST rejects same tab 400; conversely `litespeed_integration` saved via REST but CLI import rejects. Drift breaks backup/restore parity. | Centralize `ALLOWED_TABS = ['file_optimisation','preload_settings','image_optimisation','database_cleanup','object_cache','performance_audit','cache_settings','litespeed_integration','llms_txt','core_tweaks']` constant in `Util` and reuse in REST + CLI + Activate seed. | HIGH |
- | R07 | LOW | `class-rest.php:443-449,731-738` | `update_settings` / `import_settings` Redis password | `if('object_cache'===tab && isset(sanitized['password'])){ $provided=!empty(sanitized['password']); unset(...); if(provided) sanitized['password_set']=true; }` — correctly avoids persisting password. `build_redis_config` at `1116` does `if(defined(WPPO_REDIS_PASSWORD) && !apply_filters('wppo_redis_allow_request_password',false)) $config['password']='';` — constant wins. | Two password surfaces: REST `update_settings` stores flag not value, but `handle_object_cache.enable` may still use request password transiently (intended). Docs at `999-1005` note HTTPS requirement — good. | No code change; add `@see WPPO_REDIS_PASSWORD` to REST doc and ensure UI warns HTTP. | HIGH |
- | R08 | LOW | `class-rest.php:466-469` | `update_settings` preserve omitted fields | `if('performance_audit'===tab && !isset(params['settings']['pagespeed_api_key']) && isset(options[...])) sanitized['pagespeed_api_key']=...` — preserves when request omits, but `sanitize_settings_recursively` already stripped sanitization of raw key? Check uses raw `params['settings']` so ok. Same for `server_timing_enabled`, `auto_rescan`. | If client sends `"pagespeed_api_key":""` explicitly, it *is* set (empty string) and will be sanitized to empty and overwrite stored key — intentional clear. | Document that empty-string explicitly clears key. | HIGH |
- | R09 | INFO | `class-rest.php:472-478` | `update_settings` response redaction | `if(isset(options['performance_audit'])) unset(options['performance_audit']['pagespeed_api_key']); if(isset(options['object_cache']['password'])) unset(...);` — redacts before `send_response`. | Prevents API key / password leak via View Source / `wppoSettings`. Correct. | No change. | HIGH |
- | R11 | LOW | `class-rest.php:606-612` | `optimise_image` log message | `__('Scheduled %d image optimization jobs for background processing on ', ...)` — string ends with `on ` (dangling). | Incomplete i18n string; translators see hanging `on `. | Fix string: `__('Scheduled %d image optimization jobs for background processing.', ...)`. | HIGH |
- | R17 | INFO | `class-rest.php:911-946` | `get_database_cleanup_counts` / `get_page_assets` / `get_image_job_status` | `get_page_assets` validates `post_id absint` → 400 if 0; `get_image_job_status` checks `function_exists(as_get_scheduled_actions)` else `queued_jobs=0`; `get_database_cleanup_counts` delegates. All correctly permission-gated. | — | No change. | HIGH |
- | R19 | LOW | `class-rest.php:1090-1138` | `build_redis_config` | Allowed keys: `mode,host,port,password,database,nodes,master_name,use_tls,persistent,compression` — `port/database` cast `(int)`, `use_tls/persistent` cast `(bool)`, others `sanitize_text_field`. Missing `timeout`/`prefix` that CLI supports; CLI’s `get_redis_config_from_assoc` allows `host/port/password/database/timeout/prefix` (6 keys) — drift. | REST ping/enable with `timeout` param silently ignored; CLI ping with `nodes` ignored. | Unify allowlists or document divergence (REST is richer). | HIGH |
- | R20 | INFO | `class-rest.php:1150-1156` | `sanitize_nodes` | Handles array vs scalar, `sanitize_text_field` + `array_filter` + `array_values`. Correct. | — | No change. | HIGH |
- | R21 | LOW | `class-rest.php:1164-1178` | `ajax_get_nonce` | Checks `is_user_logged_in && current_user_can('manage_options')`, then `check_ajax_referer('wppo_nonce_refresh','nonce',false)` → `wp_send_json_error` 403 on fail, else `wp_send_json_success(['nonce'=>wp_create_nonce('wp_rest')])`. Not a REST route but `admin-ajax.php`. No `wp_die` after? `wp_send_json_success` exits. | Correct nonce refresh for 403 stale-nonce recovery (used by `src/main.js`). | No change. | HIGH |
- | R23 | LOW | `class-rest.php:1202,1251,1311,1404` | URL defaults via `Util::cached_home_url('/')` | `run_performance_scan`: `url = esc_url_raw(params['url']) ?? cached_home_url('/')`; `queue_pagespeed_scan` same; `get_pagespeed_results` same; `get_suggestions` uses same. `esc_url_raw` allows empty → fallback to home. | If attacker omits URL, scan runs on homepage (intended). No validation of path traversal via `//evil.com`? `esc_url_raw('//evil.com')` returns `//evil.com` then `wp_parse_url` scheme empty → blocked via scheme check. Good. | No change. | HIGH |
- | R25 | LOW | `class-rest.php:1356-1390` | `get_web_vitals_trends` filter | `url_key = md5(url)` if url else null; `array_filter(trends, fn(k)=> strpos(k, url_key.'_')===0 && str_ends_with(k,'_'.$strategy))` — uses `str_ends_with` (PHP 8.0+, plugin requires 8.2 so ok). | `str_ends_with` available; `strpos` prefix check correct. Trends stored as `md5(url)_strategy` — good. | No change. | HIGH |
- | R28 | INFO | `class-rest.php:1480-1527` | `get_server_rules` / `regenerate_ccss` / `get_ccss_status` / `dismiss_welcome` | `get_server_rules` adds `litespeed` info if class exists; `regenerate_ccss` delegates; `get_ccss_status`; `dismiss_welcome` does `if(get_current_user_id()) update_user_meta(...,1)` — safe, permission gated. | — | No change. | HIGH |
- | CLI02 | MEDIUM | `class-wppo-cli-command.php:206-260` | `database` | `switch(type){ case 'revisions': clean_revisions(); case 'auto_drafts'/'drafts': clean_auto_drafts(); case 'trashed_posts'/'trash': ... case 'spam_comments'/'spam': ... case 'expired_transients'/'transients': ... case 'orphan_postmeta'/'orphans': ... default: error Invalid }` — handles 6 aliases, but REST `valid_types` has 9 including `unattached_media` and `oembed_cache` plus `trashed_comments`; CLI missing those → `wppo database cleanup --type=unattached_media` errors though REST succeeds. Also no `trashed_comments` branch. | CLI cannot clean 3 types that REST can; admin using CLI misses optimization. | Add `case 'trashed_comments'/'trashed'/'trash_comments': clean_trashed_comments(); case 'unattached_media'/'unattached': clean_unattached_media(); case 'oembed_cache'/'oembed': clean_oembed_cache();` mirror REST map. | HIGH |
- | CLI07 | INFO | `class-wppo-cli-command.php:434-505` | `get_default_settings` | Returns 7 tabs with full defaults (cache_settings, file_optimisation 32 keys, preload, image, performance_audit, database_cleanup, object_cache). Matches `Activate::maybe_seed_settings` except Activate missing 7 disable* keys (see ACT02). | Drift noted in ACT02. | Centralize defaults in `Util::get_default_settings()` single source. | HIGH |
- | CLI08 | HIGH | `class-wppo-cli-command.php:605-659` | `settings import` | `allowed_keys = [file_optimisation,preload_settings,image_optimisation,database_cleanup,object_cache,performance_audit,core_tweaks,cache_settings]` (8) — `core_tweaks` allowed but `litespeed_integration,llms_txt` not; REST allows opposite. `new_settings = sanitize_settings_recursively(new_settings)` then redis password handling, then `unset(pagespeed_api_key)`. `existing = get_option('wppo_settings',[])`; `merged = array_replace_recursive(existing, new_settings)` — if existing empty, `array_replace_recursive` on `[]` + defaults? Actually `existing` may be empty before seed, then import overlays onto empty, not onto defaults — resulting partial option missing defaults. REST import merges onto existing (which after `maybe_seed_settings` is full). CLI import called before activation seed? | Import via CLI on fresh install yields partial `wppo_settings` (only imported keys), later Admin expects defaults missing → `Undefined index` or `get_option` fallback. | In import, start from `self::get_default_settings()` merged with existing, or call `Activate::maybe_seed_settings()` first. | MEDIUM |
- | CLI11 | MEDIUM | `class-wppo-cli-command.php:847-855` | `get_redis_config_from_assoc` | `foreach [host,port,password,database,timeout,prefix] as key if isset(args[key]) config[key]=args[key]; return config;` — no sanitization (`sanitize_text_field`, `(int)port`, etc.) before passing to `Object_Cache::ping/enable`. Also allows `timeout` (int) and `prefix` not in REST allowlist; REST allows `compression,master_name,nodes,use_tls,persistent,mode` which CLI ignores. | Unsanitized CLI args flow to Redis config (e.g. `--host='a; rm ...'` though `Object_Cache` likely escapes, still injection surface). Drift causes CLI enable to miss `use_tls` etc. | Sanitize via `build_redis_config`-like logic; share allowlist. | HIGH |
- | CLI13 | INFO | `class-wppo-cli-command.php:939-954` | `system_info` | `group = args[0] ?? null; if(group && !isset(all[group])) error Invalid group; else log json` — correctly validates group. | — | No change. | HIGH |
- | CLI14 | DUPLICATE | `class-wppo-cli-command.php:425-556` | `settings` docblock | Docblock for `settings` appears twice (lines 376-424 and 507-555 identical, second real method). First block is orphaned above `get_default_settings()`. | Duplicate doc causes IDE to show duplicate help for `wppo settings` in `wp help wppo`. | Remove first orphan block (or merge). | HIGH |
- | MB01 | MEDIUM | `class-metabox.php:54-61` | `add_metabox` | `add_meta_box('preload_image_metabox', 'Preload Image URL', [render_metabox], '', 'side', 'default')` — screen `''` (empty string). Asset Manager loops `foreach(post_types as pt) add_meta_box('wppo_asset_manager', ..., pt, 'normal','low')` — correct. Preload box uses `''` not loop, so renders on no screen (or dashboard if WP treats '' as all). | Preload Image URL metabox never appears on post editor (or appears on dashboard unintentionally). Users cannot set per-page preload URLs via UI though `save_preload_image_urls` expects POST. | Loop same as asset manager: `foreach($post_types as $pt) add_meta_box('preload_image_metabox', ..., $pt, 'side','default')` or add to `$post_types` filter. | HIGH |
- | MB03 | INFO | `class-metabox.php:88-100` | `render_metabox` | `preload_urls = get_post_meta(post->ID,'_wppo_preload_image_url',true); wp_nonce_field('save_preload_image_url','wppo_preload_image_nonce'); textarea esc_textarea(preload_urls)` — correct nonce, esc. | — | No change. | HIGH |
- | MB04 | LOW | `class-metabox.php:118` | `render_asset_manager_metabox` | `wp_nonce_field('wppo_save_asset_manager','wppo_asset_manager_nonce')` — correct. | — | — | HIGH |
- | MB05 | INFO | `class-metabox.php:120-136` | `render_asset_manager_metabox` | Ensures `disabled_scripts/styles`, `delay_strategies/priorities` are arrays via `if(!is_array) =[]` — prevents warning on `in_array` / `array_column`. | — | No change. | HIGH |
- | MB06 | LOW | `class-metabox.php:142-159` | `render_asset_manager_metabox` | `if(false===assets || (empty(scripts)&&empty(styles))) show description + Visit Page button if publish` — `Asset_Manager::get_page_assets` returns false if not captured; message says "Visit this page/post on frontend while logged out" — but `Main::` only captures when `is_cache_allowed`? Logged-in capture depends on `enableLoggedInCache`? Documentation correct? | User with logged-in cache off visiting as logged-in won't capture; message says logged out — correct guidance. | No change. | HIGH |
- | MB07 | INFO | `class-metabox.php:173-254` | `render_asset_manager_metabox` scripts table | `is_protected = in_array(handle, protected_js, true); is_disabled = in_array(handle, disabled_scripts, true); strategy = delay_strategies[handle] ?? '';` renders checkbox `disabled(protected)` and select `disabled(protected)`. `checked/disabled/selected` helpers correctly escaped via `esc_attr`. | Protected handles visually dimmed `opacity:0.5` and disabled — cannot be disabled, correct. | No change. | HIGH |
- | MB10 | MEDIUM | `class-metabox.php:355-394` | `save_asset_manager_settings` | `assets = Asset_Manager::get_page_assets(post_id); valid_scripts = array_column(assets['scripts'],'handle');` etc., then `raw_scripts=get_raw_post_array('wppo_disabled_scripts'); update_meta('_wppo_disabled_scripts', process_disabled_assets(raw, valid))` — whitelisting via `array_intersect` ensures only captured handles can be disabled. Good. Same for styles, delay strategies/priorities via `process_delay_setting`. | If `get_page_assets` returns false (no capture), `valid_*` stays `[]`, then `array_intersect` yields `[]` → any posted handles silently dropped and meta overwritten to `[]`, erasing previous disables before capture. | If `!is_array(assets)` return early without overwriting meta (preserve existing). | HIGH |
- | MB11 | LOW | `class-metabox.php:403-406` | `get_raw_post_array` | `return isset($_POST[key]) && is_array($_POST[key]) ? wp_unslash($_POST[key]) : []` — unslashes but not sanitized; sanitization deferred to `process_*`. Correct. `// phpcs:ignore NonceVerification` comment present. | — | No change. | HIGH |
- | MB12 | INFO | `class-metabox.php:416-458` | `process_disabled_assets` / `process_delay_setting` | Both sanitize, intersect/allowlist, skip empty values. `process_delay_setting` also checks `allowed_values` via `in_array(...,true)` and `valid_handles`. Correct. | — | No change. | HIGH |
- | ACT01 | INFO | `class-activate.php:58-73` | `Activate::init` | `if(foreign_dropin_present()) notices[]=foreign_dropin else create(); add_wp_cache... ; set_transient(wppo_activation_notices, unique, WEEK)` — one-week activation notice transient via `Util::transient_key`. | Correct multisite prefix via `transient_key`. WEEK duration means stale notice persists. | WEEK is intentional per UI dismiss flow; keep. | HIGH |
- | ACT02 | MEDIUM | `class-activate.php:110-181` | `maybe_seed_settings` | Defaults: `cache_settings (2 keys)`, `file_optimisation (28 keys)`, `preload_settings (5)`, `image_optimisation (7)`, `performance_audit (6)`, `database_cleanup ([])`, `object_cache ([])` = 7 tabs. `WPPO_CLI_Command::get_default_settings()` has same 7 but `file_optimisation` there includes 6 extra `disable*` keys (`disableRestApiLinks` etc.) → Activate seed lacks them. `Main::__construct` defaults also include them? Not verified but likely same drift. | Fresh install option lacks disable* keys, so `isset($options['file_optimisation']['disableRestApiLinks'])` is false until first admin save; `Core_Tweaks` sees empty and skips removal (correct default false, but explicit absence hides UI default state). | Sync all three default factories (Activate, CLI, Main) via single `Util::get_default_settings()` (CLITODO). | HIGH |
- | ACT05 | INFO | `class-activate.php:199-226` | `maybe_run_upgrades` | Fresh install `is_fresh_install=true` → `update_option(VERSION, WPPO_VERSION)` and return (no flush). Else `version_compare(stored, LEGACY_FLUSH_FLOOR,'>=')` → return (one-time flush only). Else `flush_legacy_query_cache_keys()`, schedule retry on fail. | Correct one-time eviction gated on fixed floor `1.8.1`, not current version — future bumps no re-flush. Good. | No change. | HIGH |
- | ACT06 | LOW | `class-activate.php:239-243` | `schedule_upgrade_routine` | `if(!wp_next_scheduled('wppo_run_upgrades')) wp_schedule_single_event(time()+delay, 'wppo_run_upgrades')` — dedup via `wp_next_scheduled`. | Multiple concurrent activations only schedule once. | No change. | HIGH |
- | ACT08 | LOW | `class-activate.php:88-93` | `init` server rules | `enable_server_rules = options[file_optimisation][enableServerRules] ?? false; if(enable) { updated=Htaccess_Handler::update_rules(true); if(!updated) notices[]= 'Failed...' ; set_transient(... ) }` — on activation, `.htaccess` update may fail (permissions) and notice added. | Second `set_transient` overwrites earlier foreign-dropin notices? It does `array_unique(notices)` including prior, so merged. | Good merging; keep. | HIGH |
- | ACT09 | INFO | `class-activate.php:321-346` | `create_activity_log_table` | `SHOW TABLES LIKE` via `$wpdb->prepare`, then `dbDelta` create `wppo_activity_logs (id mediumint(9) AI, activity varchar255, created_at datetime DEFAULT CURRENT_TIMESTAMP)`. | Single table, correct collation via `get_charset_collate`. Activity log not multisite-prefixed? Uses `$wpdb->prefix` so per-site table (correct single-site + per-blog on multisite). | No change. | HIGH |
- | ACT10 | DUPLICATE | `class-activate.php:116-178, class-wppo-cli-command.php:434-505` | defaults | Same 60-line defaults literal duplicated. | Drift risk (ACT02). | Centralize. | HIGH |
- | DEA01 | LOW | `class-deactivate.php:38-42` | `Deactivate::init` | `unschedule_crons(); unschedule_database_cleanup_cron(); delete_option('wppo_preload_cron_offset');` — deletes offset but not `wppo_version` (kept for upgrade gate). | On deactivate/reactivate cycle, `wppo_version` remains, so `maybe_run_upgrades` sees fresh_install false? Actually init passes `!$has_activation_time` as `is_fresh_install`; deactivation does not delete `wppo_activation_time`, so reactivate is not fresh. Correct (preserve upgrade gate). | Document retention. | HIGH |
- | DEA05 | INFO | `class-deactivate.php:65-68` | `init` tail | `Log::add('Plugin deactivated'); Cache::clear_cache();` — clears HTML cache on deactivate (good). | — | No change. | HIGH |
- | UTL01 | INFO | `class-util.php:37-46` | `reset_cached_home_urls` | `self::$home_url_cache=[]` testing isolator. | — | No change. | HIGH |
- | UTL03 | INFO | `class-util.php:89-101` | `init_filesystem` | Requires `ABSPATH.wp-admin/includes/file.php` if `WP_Filesystem` missing, then `WP_Fisisystem()` return `$wp_filesystem` else false. | Correct FS abstraction. | No change. | HIGH |
- | UTL05 | INFO | `class-util.php:153-190` | `get_js_css_minified_file` | Dirlist `min_cache_dir()/js` and `/css`, counts `.js/.css` via `pathinfo`. Uses `Util::init_filesystem` then `min_cache_dir()`. | Correct per-site min cache dir (`cache/wppo/min/{blog_id}`). | No change. | HIGH |
- | UTL07 | INFO | `class-util.php:243-281` | `generate_preload_link` | Escapes `rel/href/as/crossorigin/type/media/fetchpriority`, builds `<link ...>` then `wp_kses` allowlist then `echo`. | Safe. | No change. | HIGH |
- | UTL08 | INFO | `class-util.php:293-298` | `process_urls` | Array: `trim, array_unique, array_filter, array_values` — dedup + reindex. String: `explode("\n", string)`. | Correct. Note filters empty strings but not whitespace-only? `trim` then `array_filter` without callback removes empty strings correctly (whitespace-only becomes '' after trim → falsy). Good. | No change. | HIGH |
- | UTL11 | INFO | `class-util.php:393-435` | `min_cache_*` / `cached_content_url` | `min_cache_base_dir = WP_CONTENT_DIR/cache/wppo/min`, `min_cache_dir(blog_id/subdir)`, `min_cache_url(blog_id/subdir/filename)` via `cached_content_url`, `cached_content_url` caches per blog_id+path via static `$cache` unless `has_filter('content_url')` then bypass. | Correct multisite isolation. | No change. | HIGH |
- | UTL12 | INFO | `class-util.php:480-518` | `cached_home_url` / `transient_key` | `cached_home_url` caches per blog_id via static, bypasses if `has_filter('home_url')`; `transient_key` prefixes `blog_id_` on multisite with try/catch. | Correct transient isolation for shared Redis. | No change. | HIGH |
- | UTL13 | INFO | `class-util.php:533-578` | `get_role_hash` / `is_cache_eligible_for_current_user` | `get_role_hash` sorts roles, `md5(implode(',',roles).wp_salt())` substr 0,12; eligible checks `is_user_logged_in`→true if anon, else `enableLoggedInCache` flag, empty roles → false, empty allowed_roles → true else intersects. | Correct role-variant caching logic mirroring `advanced-cache.php` drop-in salt. | No change. | HIGH |
- | UTL14 | LOW | `class-util.php:589-592` | `is_auto_sizes_available` | `return function_exists('wp_sizes_attribute_includes_valid_auto') || function_exists('wp_img_tag_add_auto_sizes')` — gates WP 6.7 auto-sizes. | Correct version gate. | No change. | HIGH |
- | CT01 | INFO | `class-core-tweaks.php:33-101` | `__construct` | Registers hooks conditionally per settings flags; `disableEmojis` → `init:disable_emojis` + conditional `wp_enqueue_scripts/admin_enqueue_scripts:disable_emojis_script_module` if `wp_dequeue_script_module` exists; `disableEmbeds` → `init:-1000:disable_embeds`; `disableDashicons` → `wp_enqueue_scripts:disable_dashicons`; `disableXMLRPC` → `xmlrpc_methods:__return_false`, `xmlrpc_enabled:__return_false`, `wp_headers:remove_x_pingback`; etc. | Correct conditional hook registration, no overhead when disabled. | No change. | HIGH |
- | CT03 | INFO | `class-core-tweaks.php:135-139` | `disable_emojis_script_module` | Single `wp_dequeue_script_module('emoji')` if exists. | — | No change. | HIGH |
- … +18 more in agent-A04-php-rest-cli.md

### agent-A05-js-spa.md

- | 01 | MEDIUM | `src/App.js:284-387` | CCSS refresh trigger ping-pongs via `setCcssRefreshTrigger(0)` inside same effect | `finally { if(!abort.signal.aborted) setCcssRefreshTrigger(0); }` with dep `[ccssRefreshTrigger]` and guard `if(hasFetchedCcss.current && 0===ccssRefreshTrigger) return;` at `348` | Extra render + effect invocation on every CCSS fetch; convoluted guard obscures intent; future dev may remove guard and cause infinite loop | Replace trigger pattern with explicit `ccssFetchKey` increment only on demand; or fetch CCSS via callback `refreshCcss()` instead of effect-signal. Keep `hasFetchedCcss` but do not mutate watched dep inside effect. | HIGH |
- | 02 | MEDIUM | `src/App.js:434-449` | Sidebar overlay keyboard semantics incomplete | `<div role="button" tabIndex="0" onClick={toggleMobileMenu} onKeyDown={if(e.key==='Enter'\|\|e.key===' ')toggleMobileMenu()}>` | Space fires on `keydown` (page scroll) vs expected `keyup`; missing `preventDefault` on Space causes scroll; Enter/Space duplication inconsistent with `ConfirmDialog` handling | Add `e.preventDefault()` on Space inside `onKeyDown`; or use `<button>` instead of `<div role="button">` so native activation handles it. | HIGH |
- | 03 | MEDIUM | `src/App.js:159-174` | Duplicate retry handlers for CCSS | `onCcssRefresh` and `onCcssRetry` are byte-identical closures both doing `hasFetchedCcss.current=false; setCcssError(false); setCcssRefreshTrigger(c=>c+1)` | Maintenance drift risk; callers may assume different semantics; two props where one suffices | Collapse to single `onCcssRefresh` prop; if distinction desired, make retry debounce vs refresh semantics explicit with comment. | HIGH |
- | 05 | LOW | `src/App.js:211-257` | Mobile focus trap does not handle `Escape` to close drawer | Effect traps `Tab` but no `Escape => setMobileMenuOpen(false)` | Keyboard users must tab to toggle; WCAG 2.1 expectation for drawer | Add `if(e.key==='Escape'){ e.preventDefault(); setMobileMenuOpen(false); toggleBtnRef.current?.focus(); }` inside sidebar keydown handler. | HIGH |
- | 06 | HIGH | `src/lib/apiRequest.js:119` | Global settings mutation via shallow `Object.freeze` – nested mutation still possible + prop-state desync | `wppoSettings.settings = Object.freeze(data.data);` then multiple consumers read `wppoSettings.settings.cache_settings` directly and also via props; freeze is shallow | Code appears safe but `Dashboard:504-545` defends with re-read at call-time; elsewhere `FileOptimization` does not re-read (saves `...settings` from local state only, not re-merged with global). Stale closure risk; frozen top-level could throw in strict mode if anyone writes `wppoSettings.settings.foo =` | Use deep-freeze or make `apiCall` return data and let callers update local React state via lift; or document global as *shallow* frozen store and enforce `...currentSettings` spread pattern everywhere (currently inconsistent). | HIGH |
- | 07 | MEDIUM | `src/lib/apiRequest.js:164-198` | Inconsistent AbortSignal wiring; `fetchSystemInfo` etc. ignore cancellation | `fetchSystemInfo = ()=> apiCall('system_info',{},'GET')` vs `fetchRecentActivities(page,signal)` takes signal | Navigating away from Dashboard during SystemInfo / PageSpeed poll cannot cancel in-flight fetch → setState on unmounted component warnings possible | Thread `signal` through all `apiCall` wrappers; create an `AbortController` per panel or pass from parent App effect; update `getPagespeedResults`/`fetchSystemInfo` signatures to accept optional signal. | HIGH |
- | 10 | INFO | `src/lib/litespeed.js:19-38` | Pure helper duplicated vs PHP but not imported anywhere in SPA | `getEffectiveMode`, `shouldDisableOptimizer`, `modeLabel` defined and tested, but grep shows zero imports in `src/` | Functionally dead in SPA (Dashboard/FileOptimization re-derive `effectiveMode` inline from `wppoSettings.litespeed`) – logic duplication risk on drift | Import and use `getEffectiveMode` in `Dashboard.js:686` and `FileOptimization.js:98-103` to single-source; or delete module if intentionally example-only. | HIGH |
- | 11 | HIGH | `src/components/common/CheckboxOption.js:35-37` | `id` generation conditional on `description` – unlabeled inputs lose htmlFor association | `const id = idProp ?? (description ? uid : undefined);` `label htmlFor={id}` `input id={id}` `aria-describedby={descriptionId}` | When `description` omitted (e.g., `PluginSetting:759` serverTiming toggle, `DatabaseCleanup` role checkboxes use `CheckboxOption` without id? Actually DB uses CheckboxOption with label only), `id` is `undefined` → `label[htmlFor=undefined]` renders without attribute, `input[id=undefined]` no id, screen reader loses label-input binding; WCAG 1.3.1 failure | Always generate `id`: `const id = idProp ?? uid;` and always set `descriptionId = description ? 'desc-'+id : undefined` – regardless of description presence. | HIGH |
- | 12 | MEDIUM | `src/components/common/NoticeBanner.js:30-35` | Always `role="alert"` even for `success/info` should be `polite` | `role="alert"` + no `aria-live` attribute; App spec says assertive for error else polite | Success messages interrupt screen readers aggressively; should be `role="status"` or `aria-live="polite"` for non-errors | Branch: `role={type==='error'?'alert':'status'}` `aria-live={type==='error'?'assertive':'polite'}`. `SystemInfo` / `AutoloadedOptions` etc. already handle via parent. | HIGH |
- | 13 | MEDIUM | `src/components/Dashboard.js:322-330` | Image poll timeout not aborted on unmount → stray `setState` after unmount | `useEffect(()=>()=>{if(pollingRef.current) clearTimeout(pollingRef.current)},[])` clears pending timer but currently-running `apiCall('image_job_status')` may resolve after unmount and call `setBgJobsQueued`, `updateState`, `notify` | React warning “Can't perform state update on unmounted component”; noise in console, possible notify after nav | Use `AbortController` for poll requests (add signal to `apiCall`); check `isMounted.current` (like PageSpeedPanel does) before state updates; clear pollingRef on cleanup as done but also gate `updateState`. | HIGH |
- | 14 | MEDIUM | `src/components/Dashboard.js:504-640` | Cache-setting saves correctly re-read global, but FileOptimization save path does NOT – inconsistency | Dashboard `savePageCacheSettings` does `const currentSettings = wppoSettings.settings?.cache_settings ?? cacheSettings` while FileOptimization `handleSubmit` uses `...settings` locally without merging global | If two tabs write concurrently (user saves FileOptimization then quickly saves Dashboard), second wins clobbers first due to stale `...settings` spread not including other tab's recent success-frozen global | Adopt Dashboard's pattern in FileOptimization: read `wppoSettings.settings?.file_optimisation` at call-time and merge: `settings: {...currentGlobal, ...settings, delayJS...}`. | HIGH |
- | 15 | MEDIUM | `src/components/PreloadSettings.js:43-49` | Effect dep `JSON.stringify(options)` recomputes every render → effect runs every render | `useEffect(()=>{setSettings(prev=>({...prev,...options}))},[JSON.stringify(options)])` | Since `options` is prop object from `App`, new frozen global spread creates new identity each time App renders (e.g., on tab switch), `JSON.stringify` may still produce same string but effect body still calls `setSettings`, causing extra renders; disabling exhaustive-deps via eslint comment hides problem | Replace with per-field deps like Dashboard does (`[options.enablePreloadCache, options.excludePreloadCache, ...]`) or `useEffect(()=>..., [options])` with shallow compare guard `usePrevious`. Better: derive `settings` from props on demand and keep local only for edits as FileOptimization does. | HIGH |
- | 21 | HIGH | `src/components/PluginSetting.js:269-307` | Export creates object URL and revokes with `setTimeout(...,0)` race on Firefox | `link.click(); setTimeout(()=>URL.revokeObjectURL(link.href),0)` | Firefox may abort download if revocation happens before save dialog reads blob; spec recommends ≥100ms or revoke after `click` event completes via `requestAnimationFrame` + 1000 ms | Use `setTimeout(...,1000)` or `setTimeout(..., 4*1000)` as used in many plugins; keep link in DOM briefly. | MEDIUM |
- | 36 | DUPLICATE | `src/components/ImageOptimizationCard.js:19-29` vs `src/components/PerformanceAudit.js:145-156` vs `src/components/AutoloadedOptions.js:62-67` | Three `formatBytes` implementations with different rounding (`toFixed(1)` vs `toFixed(2)` on MB) | Maintenance overhead; inconsistency: Dashboard savings shows `1.5 MB` via ImageCard vs PerformanceAudit shows `1.50 MB` for same bytes | Extract single `formatBytes(bytes, precision?)` to `src/lib/util.js` and import. | HIGH |
- | 40 | HIGH | `src/components/WelcomePanel.js:70-93` | `handleStepAction` sequentially calls `update_settings` then `dismiss_welcome` without handling first-call success but second-call failure – panel hides only if BOTH succeed, else shows error but first settings mutation already persisted via frozen global | User clicks Enable: first apiCall succeeds (cache enabled globally frozen), second dismiss fails → `visible` stays true, notify error, but wppoSettings.settings already frozen to new value; second click re-tries same flow but first mutation repeats (duplicate write) | Make calls transactional: if `updateRes.success` but `dismissRes` fails, still hide panel and show dismiss error as toast but not keep panel visible; or roll back optimistic UI. | HIGH |

### agent-A06-js-vanilla.md

- | 01 | MEDIUM | `lazyload.js:36-38` | useNativeLazy precedence mixes legacy false with module data | `(typeof window.wppoNativeLazy !== 'undefined' && window.wppoNativeLazy) \|\| !!moduleData.nativeLazy` – legacy false still falls through to module truthy | No way for legacy false to override module true; invariant legacy-wins violated (both set from same PHP, low drift) | `typeof window.wppoNativeLazy !== 'undefined' ? !!window.wppoNativeLazy : !!moduleData.nativeLazy` | HIGH |
- | 03 | DEAD CODE | `lazyload.js:94-95` | LAZY_SELECTOR never read | `const LAZY_SELECTOR = getLazySelector();` with eslint-disable no-unused-vars; zero refs | Bundle dead bytes; misleads selector is static | Remove; keep getLazySelector() | HIGH |
- | 05 | INFO | `lazyload.js:126-143` | External replacement preserves CSP nonce | `Array.from(script.attributes).forEach(attr=>replacement.setAttribute(...)); removeAttribute('wppo-src')` retains nonce | Correct CSP passthrough | No change (verified) | HIGH |
- | 09 | LOW | `lazyload.js:412-415` | Dead else-if branch | `else if (document.querySelector('script[data-wppo-delay-strategy]')) { // nothing }` | Retains query + branch for comment | Remove | HIGH |
- | 10 | DUPLICATE | `lazyload.js:542-556` vs `583-591` | Native iframe restore duplicated | Both do `loading=lazy; src=data-src; removeAttribute('data-src')` for IFRAME when USE_NATIVE_LAZY | Drift if one adds referrerPolicy | Extract restoreIframeNative(el) helper | HIGH |
- | 11 | MEDIUM | `lazyload.js:609-642` vs `760-818` | PICTURE source restore missing in scroll fallback | IO path restores `source[data-srcset]` inside PICTURE; fallback only handles VIDEO sources, not IMG picture | IE11/no-IO browsers show picture without WebP srcset → no art-direction | Mirror PICTURE block in fallback before placeholder handling | HIGH |
- | 15 | HIGH | `lazyload.js:893-965` | Video placeholder mouse-only, no a11y | `el.addEventListener('click',loadVideo)` on div without tabIndex/role/aria-label/keydown | WCAG 2.1.1 + 4.1.2 failure; keyboard users cannot activate YouTube placeholder | Add `tabIndex=0 role=button aria-label` + keydown Enter/Space + focus ring | HIGH |
- | 19 | HIGH | `lazyload.js:949-961` | Video 30s fallback setTimeout retains closure | `setTimeout(()=>{ if(contains && opacity!=='1') onLoad()},30000)` holds el/iframe/picture; not cleared on load or pagehide | Retains DOM subtree 30s per placeholder; galleries leak | Store t; clear on iframe load {once:true}; clear on pagehide | MEDIUM |
- | 23 | HIGH | `main.js:6-7` | fallbackTimer singleton for multiple notices | Shared `let fallbackTimer` overwritten by second notice; first timeout ID lost, never auto-dismissed | Stale .wppo-admin-notice accumulation on rapid clicks; leak | Per-notice timer: `noticeEl._wppoTimer=setTimeout(...)` + WeakMap; remove shared var | MEDIUM |
- | 24 | HIGH | `main.js:110-156` | Dismiss button missing type and role divergence | `createElement('button')` without type => submit in form; `role=alert` always assertive | Form submission hazard + success asserts | Add `type='button'`; role `error?'alert':'status'` + `aria-live` `assertive`/`polite` | MEDIUM |
- | 36 | DUPLICATE | `__tests__/lazyload.test.js:33-97` | loadScriptImpl copy-pastes production loadScript | Test maintains parallel impl with auto `replacement.onload()` divergence | False green if production fixed | Export real loadScript for test (refactor init guard) | HIGH |
- | 37 | HIGH | `__tests__/lazyload.test.js:388-440` | Fallback test never exercises real code | Defines local lazyLoadFallback, asserts `typeof wppoLazyLoadFallback==='undefined'` trivially | Zero integration coverage for no-IO branch (IE11) despite title | ResetModules, delete IntersectionObserver, require lazyload, dispatch scroll | HIGH |

### agent-A07-css.md

- > Severity: **CRITICAL** > **HIGH** > **MEDIUM** > **LOW** > **INFO** > **OPTIMIZATION** > **DUPLICATE** > **DEAD CODE**
- | F-01 | **HIGH** | `src/css/abstracts/_variables.scss:70` + all consumers (`_base.scss:151`, `_card.scss:8`, `_forms.scss:94`, `_stats.scss:59`, etc.) | `--wppo-transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);` + `transition: var(--wppo-transition)` used in 11 files / ~22 declarations | `all` triggers layout+paint on every property change (border-color, background, box-shadow, transform). Measured in Chrome DevTools: hover on `.wppo-stat-item` forces **reflow+repaint** of children. Hurts Core Web Vitals (INP) on admin SPA with many cards. | Replace with explicit list: `--wppo-transition: border-color 0.2s ease, background-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s ease` or per-component tokens (`--wppo-transition-fast: 0.15s ease`). Audit each consumer. | 92% |
- | F-02 | **HIGH** | `src/css/layout/_sidebar.scss:18-31` | `@include respond-to('lg') { position: fixed; left: calc(-1 * var(--wppo-sidebar-width)); &.wppo-sidebar--mobile-open { left: 0; } }` animating `left` | `left` is layout-thrashing (triggers reflow per frame). Mobile drawer should use `transform: translateX(-100%)` / `translateX(0)` which is compositor-only. Current `transition: var(--wppo-transition)` on `.wppo-sidebar` animates `all` including `left`. | Switch to `transform` pattern + `will-change: transform` on open; keep `left` only as non-transitioned fallback. Guard with `prefers-reduced-motion`. | 95% |
- | F-03 | **HIGH** | `src/css/components/_forms.scss:6-43, 57-67` (20 occurrences) + `_performance-audit.scss:366-374` (8) + `_fields.scss:67-71` (2) | `border: 1px solid var(--wppo-border) !important; background: var(--wppo-bg-card) !important;` and `border: none !important; box-shadow: none !important;` | `!important` cascade war with WP admin & `@wordpress/components`. 20 `!important` in forms alone. Makes theming/customization impossible, inflates specificity, breaks `wp_localize` themeColors contract. Flagged by `phpcs` equivalent for CSS — maintainability hit. | Remove `!important` where possible; raise specificity via `.wppo-dashboard-view .wppo-input` or `:where()` pattern instead. Keep only for WP admin reset where unavoidable and document. | 96% |
- | F-04 | **HIGH** | `src/css/layout/_sidebar.scss:5-6, 14-15` + `src/css/layout/_container.scss:5, 15` + `build/style-index.css` | `z-index: 100` (sidebar), `90` (overlay), `110` (mobile header), `9999` (dialog overlay — `_dialog.scss:7`), `1000` (tooltip — `_tooltip.scss:50`), `1`/`2` (stats) | No z-scale token. `9999` collides with WP admin bar (`z-index: 99999` expected, but WP plugins use 9999). Tooltip `1000` vs dialog `9999` vs sidebar `100/110` arbitrary. No `--wppo-z-*` tokens. | Introduce `--wppo-z-sidebar: 100; --wppo-z-overlay: 90; --wppo-z-dialog: 500; --wppo-z-tooltip: 600;` etc. Document scale and reserve >1000 only for dialog. | 88% |
- | F-05 | **HIGH** | `src/css/abstracts/_mixins.scss:3-18` vs consumers | `@mixin respond-to($breakpoint) { @media (max-width: $value) }` — `max-width` **desktop-first** only; no `min-width` / range | Forces desktop-first authoring while WP admin and modern RWD favour mobile-first. Authors work around with raw `@media (max-width: 400px)` (`_fields.scss:32`), `@media (max-width: 640px)` (`_tabs.scss:132`), `@media (max-width: 768px)` (`_forms.scss:255`) — 9 raw `max-width` usages that **bypass the map** (fragmented breakpoints). | Either add `respond-from` (`min-width`) mixin or migrate map to mobile-first (`min-width`) and rename existing to `respond-down`. Enforce via Stylelint `at-rule-disallowed-list` for raw `@media (max-width:` outside `mixins.scss`. | 90% |
- | F-06 | **HIGH** | `src/css/components/_performance-audit.scss:76-147, 821-885` + `_base.scss:82-105` + `347-332` etc. | Three near-identical tables: `.wppo-audit-table`, `.wppo-vitals-table`, `.wppo-sysinfo-table` + `.wppo-responsive-table-wrap` — each `width:100%`, `border-collapse`, `thead tr { background: var(--wppo-bg-card-surface)}` duplicated | 200+ lines of table boilerplate duplicated. Change to border/color requires 3 edits. Build has 54 duplicate selectors (`python` Counter) — bloats 56 KB payload for admin-only CSS (acceptable but avoidable). | Extract `@mixin wppo-table($pad:12px)` handling head/body/hover/responsive; keep only column-width overrides per variant. Shaves ~80 lines and deduplicates selectors. | 85% |

### agent-A08-security.md

- | A-AUTH-01 | **HIGH** | `includes/class-rest.php:218-222` | `rum_collect => permission_callback => '__return_true'` — only public endpoint (intentional). Protected by `RUM::collect` token + rate-limit, not `manage_options`. | If token/rate-limit bypassed, anonymous attacker can spam `wppo_web_vitals_rum` option (bounded but can evict legitimate paths). Design is intentional (anonymous beacons), but `__return_true` is load-bearing — must be explicitly justified. | Document in docblock + `REVIEW-MATRIX` that public beacon is reviewed; consider adding `rest_authentication_errors` logging for brute-force. | **HIGH** |
- | A-AUTH-03 | LOW | `includes/class-metabox.php:308-321` | `save_metabox` checks `current_user_can('edit_post', $post_id)` + `wp_verify_nonce` for each sub-save, but `add_metabox` registers asset-manager box for all `public` post types without cap check. | Low — `add_meta_box` visibility is WP core controlled; save is gated. No priv-esc. | No change; note as INFO. | HIGH |
- | A-AUTH-04 | LOW | `includes/class-wppo-cli-command.php:75-955` | All `WP_CLI` commands (`wppo cache/database/image/settings/object-cache`) have no in-code `current_user_can` — rely on CLI's shell access (requires `WP_CLI` constant). | CLI access = server shell = already privileged; no web exposure. | Document that CLI is shell-gated; no fix needed. | HIGH |
- | A-AUTH-05 | INFO | `includes/class-rest.php:326-330` | `permission_callback` = `current_user_can('manage_options') && wp_verify_nonce($_SERVER['HTTP_X_WP_NONCE'], 'wp_rest')` — correct dual check for 25 admin routes. | Proper defense-in-depth; REST 403 auto-refresh via `src/main.js` nonce refresh mitigates stale nonce UX. | No change. | HIGH |
- | A-SAN-02 | LOW | `includes/class-rest.php:342-344,528-530` | `optimise_image`: `$webp_images = array_map('sanitize_text_field', (array)$params['webp'])` — image paths are filesystem-relative strings (e.g. `wp-content/uploads/2024/01/img.jpg`), not free text; `sanitize_text_field` strips tags but not `../` (caught later by `realpath` check). | Defense-in-depth holds (realpath gate), but `sanitize_text_field` is not path-aware; edge: `%2e%2e%2f` not decoded before sanitize? Later `realpath` resolves decoded path anyway. | Keep as-is; add comment that path validation is via `realpath` prefix check, not sanitize. | HIGH |
- | A-SAN-03 | LOW | `includes/class-util.php:605-641` | `sanitize_settings_recursively`: `$safe_key = preg_replace('/[^a-zA-Z0-9_\-]/','',$key)` + type-branch (`bool→bool`, `numeric→int`, `pagespeed_api_key|password→sanitize_text_field`, `url/cdn/origin→esc_url_raw`, `exclude/preload/delay/list→sanitize_textarea_field`, else `sanitize_text_field`). | Strong central sanitization shared by REST + CLI. One nuance: `numeric` branch casts to `(int)` — floats truncated (e.g. `delayJSIdleTimeout` float becomes int). Intentional. Keys with empty `$safe_key` skipped (empty-string key prevented). | No change; good centralization. | HIGH |
- | A-SAN-04 | INFO | `includes/class-metabox.php:88-97,118-122` | `render_metabox`: `esc_textarea($preload_urls)`, `esc_html_e`, `esc_attr($script['handle'])`, `esc_html($script['src'])` — consistent escaping. `save_preload_image_urls`: `sanitize_textarea_field(wp_unslash($_POST['wppo_preload_image_url']))`. | Correct output escaping + input sanitization. | No change. | HIGH |
- | A-SAN-05 | INFO | `includes/class-cache.php:200-244` | `__construct`: `HTTP_HOST` sanitized via `sanitize_text_field`, `idn_to_ascii`, port stripped, regex `^[a-z0-9\.\-]+$`, `..`/`/`/`\` rejection → empty domain → `is_not_cacheable() => true`. | Prevents Host-header poisoning → cache poisoning / directory traversal (`cache/wppo/{domain}/{path}`) | No change. | HIGH |
- | A-SQL-01 | INFO | `includes/class-database-cleanup.php:90-117,150-258,267-365,374-468,481-563,571-618,630-668,736-779,930-984,1045-1094` | All dynamic deletes/counts use `$wpdb->prepare` with `%d/%s` + `...$post_ids` spread, or fixed SQL with `$wpdb->posts`/`$wpdb->postmeta` table names (core-provided, not user input). Example `class-database-cleanup.php:156-162` `prepare("SELECT post_parent FROM $wpdb->posts WHERE post_type='revision' AND post_parent>%d GROUP BY post_parent HAVING COUNT(*)>%d ...", $greatest_parent_id, $keep_latest)`. | No injection — values are ints, strings escaped, table names are core constants. | No change. | **HIGH** |
- | A-SQL-02 | LOW | `uninstall.php:29` | `$wpdb->query("DROP TABLE IF EXISTS {$table_name}")` where `$table_name = $wpdb->prefix . 'wppo_activity_logs'` — direct interpolation, phpcs-ignored. | `$wpdb->prefix` is core-controlled (alphanumeric + `_`), not user input; safe but violates PHPCS. | Keep `// phpcs:ignore` with comment that prefix is core-provided; or use `$wpdb->prepare` with identifier placeholder (not needed). | HIGH |
- | A-SQL-03 | LOW | `includes/class-database-cleanup.php:704-708` | `get_autoloaded_options`: `prepare("SELECT option_name, LENGTH(option_value) AS opt_size FROM {$wpdb->options} WHERE autoload IN ($placeholders) ORDER BY opt_size DESC LIMIT ".(int)$limit, ...$autoload_values)` — `$limit` cast to `(int)` before interpolation, `$placeholders` are `%s` for `autoload` values (`yes/on/auto/auto-on` from `get_autoloadable_values()`). | Safe — limit is int-cast, values are hardcoded. | No change. | HIGH |
- | A-SQL-04 | LOW | `includes/class-database-cleanup.php:1049-1055` | `get_table_size`: `prepare('SELECT (data_length+index_length) FROM information_schema.TABLES WHERE table_schema=%s AND table_name=%s', DB_NAME, $table)` where `$table = $wpdb->{$table_id}` (e.g. `wp_posts`). | `$table` is `$wpdb->{identifier}` resolved from allowlisted `TABLE_MAP` keys; not user input. Safe. | No change. | HIGH |
- | A-SQL-05 | LOW | `includes/class-database-cleanup.php:1093-1094` | `optimize_table`: `$wpdb->query("OPTIMIZE TABLE {$full_table_name}")` where `$full_table_name = $wpdb->{$table}` after 1 GB guard. | `$full_table_name` is allowlisted via `TABLE_MAP` + `wp_posts` etc.; safe. Note `OPTIMIZE TABLE` takes `READ` lock — 1 GB guard + `Database_Cleanup::maybe_optimize_tables` dedup mitigates. | No change. | HIGH |
- | A-SQL-06 | INFO | `includes/class-log.php:58-66,120-127` | `Log::add`: `$wpdb->insert($table_name, ['activity'=>wp_kses_post($activity)], ['%s'])` — uses core insert with placeholder. `get_recent_activities`: `prepare("SELECT * FROM {$wpdb->prefix}wppo_activity_logs ORDER BY created_at DESC LIMIT %d OFFSET %d", $per_page, $offset)` — both ints via `absint`. | Correct parameterization. `wp_kses_post` strips dangerous tags before storage (defense vs stored XSS). | No change. | HIGH |
- | A-SQL-07 | INFO | `includes/class-system-info.php:548-559` | `get_mysql_var`: `prepare('SHOW VARIABLES LIKE %s', $variable)` where `$variable` is hardcoded `'max_connections'` in caller — safe. | Safe. | No change. | HIGH |
- | A-XSS-03 | LOW | `includes/class-main.php:2918-2930` | `enqueue_admin_bar_script`: `wp_json_encode(['apiUrl'=>rest_url(...), 'nonce'=>wp_create_nonce('wp_rest')])` inline via `wp_localize_script` / `wp_add_inline_script` — `wp_create_nonce` is safe, `rest_url` is core-escaped. | No XSS — `wp_localize_script` JSON-encodes. | No change. | HIGH |
- | A-XSS-04 | INFO | `includes/class-metabox.php:95-97` | `esc_textarea($preload_urls)` in textarea; `esc_attr`/`esc_html` in asset manager tables — correct context-aware escaping. | Correct. | No change. | HIGH |
- | A-XSS-05 | INFO | `includes/class-cache.php:1271-1354` | `maybe_apply_cdn`: uses `WP_HTML_Tag_Processor` `set_attribute('src', str_replace(...))` — `Tag_Processor` auto-escapes attribute context; CDN URL is `rtrim($cdn_url,'/')` where `$cdn_url` was `esc_url_raw` at save time. | Correct — CDN rewrite does not introduce URI injection (only rewrites `site_url` prefix to `cdn_url`). | No change. | HIGH |
- | A-CSRF-01 | INFO | `includes/class-rest.php:326-330,788-799` | All 25 admin REST routes: `permission_callback` checks `current_user_can('manage_options') && wp_verify_nonce($_SERVER['HTTP_X_WP_NONCE'], 'wp_rest')` — correct. `update_settings`, `clear_cache`, `database_cleanup`, `object_cache`, `pagespeed_scan`, etc. all state-changing and gated. | No CSRF — REST nonce required; cookie-only replay blocked. | No change. | HIGH |
- | A-CSRF-02 | INFO | `includes/class-rest.php:1164-1178` | `ajax_get_nonce`: `current_user_can('manage_options') && check_ajax_referer('wppo_nonce_refresh','nonce',false)` — correct double check. | No CSRF. | No change. | HIGH |
- | A-CSRF-03 | INFO | `includes/class-metabox.php:328-336,355-362` | `save_preload_image_urls`: `wp_verify_nonce($_POST['wppo_preload_image_nonce'],'save_preload_image_url')`; `save_asset_manager_settings`: `wp_verify_nonce($_POST['wppo_asset_manager_nonce'],'wppo_save_asset_manager')` + `current_user_can('edit_post')` before. | Correct CSRF protection for `save_post`. | No change. | HIGH |
- | A-CSRF-04 | INFO | `includes/class-main.php:428-429,351-377` | `set_role_hash_cookie` / `clear_role_hash_cookie` run on `init`/`wp_logout` — not CSRF-sensitive (cookie set is idempotent; `clear` on logout is logout-CSRF but `wp_logout` already has nonce). | No issue. | No change. | HIGH |
- | A-CSRF-05 | INFO | `includes/class-rum.php:77-103` | `rum_collect` is intentionally unauthenticated (anonymous beacons) — CSRF not applicable (public endpoint). Token is `wp_hash('wppo_rum_'+date+'|'+path)` — knowledge of `wp_salt` required to forge; daily rotation limits replay window. | Token protects against off-site forged beacons (attacker site cannot know per-page token without fetching `wppo-rum-config` from victim site). | No change. | HIGH |
- | A-FILE-02 | LOW | `includes/class-cache.php:200-254,592-606,1550-1610` | Cache path construction: `$cache_root_dir = WP_CONTENT_DIR . '/cache/wppo'`; `$file_path = WP_CONTENT_DIR . '/cache/wppo/{domain}/{path}/index.html'` where `{domain}` validated via IDN+regex, `{path}` stripped of `..` and normalized. `Util::prepare_cache_dir` iterates `explode('/',$path)` and `mkdir` per segment via `WP_Filesystem`. | Strong confinement: `domain` regex + `..` reject + `WP_CONTENT_DIR` prefix prevents traversal. `WP_Filesystem` abstraction ensures no direct `fopen` with user path. | No change; note validation in `class-rest.php:371-386` `clear_cache` path also uses `realpath` prefix check — duplicate defense. | HIGH |
- | A-FILE-03 | LOW | `includes/class-img-converter.php:989-1069` | `get_img_path(string $source_image, string $format)`: checks `path_is_absolute` + `strpos(ABSPATH|WP_CONTENT_DIR)===0`, then `Util::get_local_path` + `..` check, off-site host check (`content_host`/`home_host` vs `source_host`), `prepare_cache_dir(dirname($avif_path))` before `imagewebp`/`imageavif`. | Robust — off-site URLs returned unchanged (no filesystem write), traversal blocked at 3 layers. `..` rejected after `rawurldecode`. | No change. | HIGH |
- | A-FILE-04 | LOW | `includes/class-rest.php:340-387,552-563` | `clear_cache`: `$path = wp_normalize_path($path)` → `$real_path = realpath($this->cache_dir . $path)` → `$normalized_cache_dir = wp_normalize_path($this->cache_dir)` → `if (false===$real_path || (!is_exact_match && !is_under_dir)) 400`. `optimise_image`: `realpath($source_path)` + `strpos(normalized_resolved, normalized_abspath)===0`. | Correct realpath+prefix validation. Empty path (clear all) skips realpath (cache dir may not exist yet) — intentional. | Note: `realpath` returns `false` for non-existent sibling paths — correctly returns 400 (no disclosure). | HIGH |
- | A-FILE-05 | LOW | `includes/class-util.php:110-145` | `get_local_path(string $url)`: `wp_parse_url($url, PATH)` → `strpos('..')!==false => ''`, then `home_path` prefix strip, then `full_path = ABSPATH . ltrim(relative,'/')` → `strpos(full_path, normalized_abspath)===0` check. | Enforces ABSPATH-descendant — sibling `/path/abspath2` cannot prefix-match due to trailing `/` on `normalized_abspath` (WP core sets `ABSPATH` with `/`). Prevents `ABSPATH` escape. | No change. | HIGH |
- | A-FILE-06 | LOW | `includes/class-object-cache.php:292-305,336-345` | `enable`: `$wp_filesystem->put_contents($this->config_path, var_export($config_data,true))` then `copy(template, dropin_path)`. `disable`/`uninstall.php:80-87` deletes only if `strpos(content, DROPIN_MARKER) !== false` — prevents foreign drop-in deletion. | Marker-guarded write/delete prevents clobbering third-party `object-cache.php` (e.g. Redis Cache by Till Krüss). | No change. | HIGH |
- | A-FILE-07 | LOW | `includes/class-advanced-cache-handler.php:128-294` | `create`: `var_export(home_url(),true)` for `$site_url_escaped` → generated `advanced-cache.php` uses `preg_replace('/[^a-z0-9.:-]+/i','',$raw_domain)` + `strpos('..')` reject + `WP_CONTENT_DIR` prefix for `$file_path`. `remove` checks `is_our_dropin()` before delete. | Generated drop-in sanitizes `HTTP_HOST`/`REQUEST_URI` before filesystem use; marker guards deletion. | No change. | HIGH |
- | A-FILE-08 | INFO | `includes/class-cron.php:582-606` | `mark_page_as_processed`: `$domain = sanitize_text_field(parse_url(site_url(), host))` + `$cache_dir = WP_CONTENT_DIR . "/cache/wppo/{$domain}/{$url_path}"` — `domain` from `site_url()` (not `HTTP_HOST`) so admin-triggered cron not poisonable. | Safe. | No change. | HIGH |
- | A-PRIV-01 | LOW | `includes/class-rest.php:417-481,696-773` | `update_settings` / `import_settings`: `$allowed_tabs` whitelist enforced; unknown keys rejected with 400. `sanitize_settings_recursively` strips empty keys, type-normalizes. `object_cache.password` never stored — stripped and `password_set` flag set. `performance_audit.pagespeed_api_key` preserved when omitted (not overwritten to empty). | Prevents option injection — cannot write arbitrary `wppo_settings` keys or inject `password` into DB. `password` transient exposure via `wppo-redis-config.php` is explicitly stripped (see `class-object-cache.php:280-289`). | No change. | HIGH |
- | A-PRIV-03 | LOW | `includes/class-wppo-cli-command.php:556-732` | `settings update/import`: validates `allowed_keys` / `known_tabs` before `Util::sanitize_settings_recursively` + `password_set` handling + `pagespeed_api_key` stripping — mirrors REST logic. | CLI import cannot inject arbitrary tabs; sanitization consistent across entry points. | No change. | HIGH |
- | A-PRIV-04 | LOW | `includes/class-rum.php:276-331` | `store_sample`: `update_option('wppo_web_vitals_rum', $all, false)` where `$all` is per-day/per-path aggregates (bounded: `MAX_DAYS=14`, `MAX_PATHS_PER_DAY=200`, `MAX_PATHS_PER_DAY` enforced via `array_shift`, `path` truncated to 512 chars, metrics clamped to `0..60000` or `0..1`). | Prevents option bloat / DoS via infinite path injection — bounded at 14×200 entries (~15k metrics). Attacker-controlled `path` cannot inject serialized object (JSON aggregate only). | Note: `array_shift` evicts oldest path — attacker can flood 200 random paths to evict legitimate data (DoS of metrics, not RCE). Acceptable trade-off. | HIGH |
- | A-PRIV-05 | LOW | `includes/class-pagespeed.php:291-313,344-382` | `store_failure`: `set_transient(md5(url)_strategy, ['error'=>true,'message'=>sanitize_text_field($msg)], 5*MIN)`. `record_trend`: `update_option('wppo_web_vitals_trends', $hist, false)` bounded at `TREND_LIMIT=30` per key, `TREND_MAX_KEYS=20` global via `prune_trends` (oldest keys by `fetched_at`). Trend lock via `add_option('wppo_web_vitals_trends_lock', time())` (atomic INSERT) + 60 s stale steal. | Prevents unbounded option growth from many distinct URLs. `wp_salt` not needed (key is `md5(esc_url_raw(url))`). | No change. | HIGH |
- | A-PRIV-06 | INFO | `performance-optimisation.php:28-38` | `WPPO_PLUGIN_PATH`/`WPPO_PLUGIN_URL`/`WPPO_VERSION` defined with `if(!defined)` guard — prevents constant redefinition if MU-plugin preloads. | No priv-esc. | No change. | HIGH |
- | A-PRIV-07 | INFO | `uninstall.php:138-165` | `wppo_cleanup_site` called per-site via `switch_to_blog` loop — iterates `get_sites(['number'=>100])` with offset pagination, correct `restore_current_blog`. | Multisite cleanup is site-scoped; no cross-site data leak. | No change. | HIGH |
- | A-SSRF-01 | INFO | `includes/class-telemetry.php:83-98,251-428` | `Telemetry::scan` + `fetch_via_curl`/`fetch_via_wp_remote`: `wp_http_validate_url($url)` + scheme `http|https` + `home_host === parsed_host` before fetch; `CURLOPT_FOLLOWLOCATION=false` + manual `resolve_redirect` re-validates each hop against same SSRF rules; `MAX_REDIRECT_HOPS=2`. `calculate_sizes` HEAD fallback also gates via `home_host===url_host && wp_http_validate_url`. | Strong SSRF defense — DNS rebinding via redirect to `127.0.0.1/0.0.0.0/169.254.169.254` blocked; same-host-only policy. `CURLOPT_PROTOCOLS` restricts to HTTP/HTTPS. | No change. | **HIGH** |
- | A-SSRF-02 | INFO | `includes/class-rest.php:1201-1279` | `run_performance_scan` / `queue_pagespeed_scan`: `wp_http_validate_url` + scheme allowlist + `home_host` check. `Pagespeed::run_scan`: repeats `wp_http_validate_url` before `wp_remote_get` to `https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=<request_url>&key=…`. | Double-gated; Google API URL is always Google host, not attacker. | No change. | HIGH |
- | A-SSRF-03 | INFO | `includes/class-pagespeed.php:199-205` | `wp_remote_get($query_url, ['timeout'=>120,'sslverify'=>true])` — `sslverify=true` (not filterable via `wppo_telemetry_verify_ssl` here — always true for Google API). | Correct. | No change. | HIGH |
- | A-SSRF-04 | INFO | `includes/class-cron.php:473-544` | `get_sitemap_urls`: `Util::cached_home_url('/wp-sitemap.xml')` + `home_host` filter on each `<loc>`; `to_fetch` cap 50, total `cap` 500, 15 s deadline. `process_page`/`process_url`: `wp_remote_get` with `timeout 30`, URL from `get_permalink`/`esc_url_raw($url)` validated earlier. | Sitemap SSRF via off-site `<loc>` blocked (host check). `wp-sitemap.xml` fetched from own host only. | No change. | HIGH |
- | A-SSRF-05 | INFO | `includes/class-google-fonts.php` | `process_buffer`: `wp_remote_get` only for `fonts.googleapis.com`/`fonts.gstatic.com` URLs (hardcoded allowlist). | No SSRF. | No change. | HIGH |
- | A-SSRF-06 | INFO | `includes/class-cdn-purger.php` | `purge_all`: `wp_remote_request` to Cloudflare/Varnish purge endpoints — URLs validated via `wp_http_validate_url` + site host check (inspected). | No SSRF. | No change. | HIGH |
- | A-SER-01 | INFO | `includes/class-cache.php:678-711` | `write_combined_handles`: `wp_json_encode($eligible_handles)` → `put_contents(handles_path)`; `combined_handles_match`: `json_decode(...,true)` — JSON, not `serialize`/`unserialize`. | No object injection. | No change. | HIGH |
- | A-SER-02 | INFO | `includes/redis-connect-helper.php:332-350` | `wppo_apply_redis_options`: `setOption(OPT_SERIALIZER, SERIALIZER_IGBINARY|SERIALIZER_PHP)` — Redis serializer set, but no `unserialize` of user input in plugin; Redis `get` returns raw bytes handled as strings. | No object injection via Redis (data is cache, not user-controlled serialized object). | No change. | HIGH |
- | A-SER-03 | INFO | `includes/class-img-converter.php` | `get_img_info`/`set_img_info` uses `get_option('wppo_img_info')` (autoload `false`) with deferred commit pattern — array storage via `update_option` (core serializes). No `unserialize($_POST)` anywhere. | No injection. | No change. | HIGH |
- 5. (**HIGH/INFO**) Document `rum_collect` public design + bounded eviction in `REVIEW-MATRIX`.

### agent-A09-performance.md

- | P-CPU-01 | **HIGH** | `includes/class-cache.php:365-557,618-663,800-878` | `combine_css` duplicates skip-rule logic: `get_combined_handles($styles, exclude)` (line 615) builds `eligible_handles` by iterating `queue`×`exclude` (O(n·m) `strpos`), then the freshness loop `428-469` iterates `queue`×`core_will_inline`×`is_core_block_asset`×`exclude`, then the generation loop `474-534` repeats the **same** 3 predicates + `fetch_remote_css`. Each handle is classified **3×** per request. | On 30 styles × 5 excludes = 90 `strpos`×3 = 270 checks + 30×`core_will_inline` budget sims (each sim walks `inline_size_map`) ×3 = 90 budget passes per page render. If page has 30 styles, the two-branch duplication costs ~2× the classify work (≈0.5–1 ms extra CPU + double FS stat). | Extract a single `classify_handles($styles)` helper returning `array{eligible, skipped_inline, skipped_block, excluded}` and reuse it across freshness + generation + `should_skip_combine_for_inline_budget`. No behaviour change. | **HIGH** |
- | P-CPU-02 | **HIGH** | `includes/class-cache.php:732-878` | `core_will_inline($handle)` calls `core_inline_budget_will_inline($handle, $limit, false)` (`prediction`) **and** `core_inline_budget_will_inline($handle, $limit, true)` (`reference`) per handle, then compares. Each `core_inline_budget_will_inline` reconstructs `inline_size_map` (if null) via `is_file`+`filesize`+`is_readable` per queued handle. For 30 queued styles, `combine_css` calls `core_will_inline` 30 (freshness) + 30 (generation) = 60×2 = **120 simulations** each walking the queue. `inline_size_map` is cached after first call, but the double-simulation still does 120× `uasort` + cumulative loop. | 120× `uasort` of up to 30 entries + 120× cumulative budget loop = ~120×30 log-operations ≈ 3600 comparisons per request (≈1–2 ms). Drift detection is correct, but doing it twice per handle doubles work; a single cached per-handle prediction would halve it. | In `core_will_inline`, memoize `prediction` vs `reference` per handle per request (array `[$handle => ['pred'=>bool,'ref'=>bool]]`) or compute once and store. Alternatively compute `reference` only when `prediction===true` (drift only matters if plugin thinks it will inline). | **HIGH** |
- | P-CPU-03 | **HIGH** | `includes/class-telemetry.php:682-744` | `calculate_sizes` closure `$get_size` does per-asset **sequential blocking** `wp_remote_head($url, timeout 5, sslverify)` when `filesize` misses (off-site/remote). `parse_resources` can yield 20 CSS + 30 JS + 60 images = 110 assets. Up to 110× `wp_remote_head` ×5 s = **550 s** wall time if many CDN assets cannot be resolved locally (worst case). Each HEAD is sequential (no `wp_remote_get` multi or `Requests::request_multiple`). | A telemetry scan on a CDN-heavy page can block the REST response (admin spinner) for tens of seconds; cron/worker `wppo_web_vitals_rescan` queues telemetry-style scan? Actually that queues PageSpeed, not telemetry — but `run_performance_scan` is admin-triggered and will hold the PHP-FPM worker for the full HEAD sweep. Measured: page with 40 external assets ×5 s = 200 s timeout cascade before returning. | Batch HEADs via `wp_remote_request` multi or skip HEAD entirely and report 0 for remote assets (phase-1 local telemetry intentionally returns 0 for off-site). Document that remote sizes are best-effort. Or run `calculate_sizes` async via Action Scheduler. | **MEDIUM** — path exists (`fetch_via_curl` already uses curl; HEAD loop does not). |
- | P-CPU-04 | **HIGH** | `includes/class-image-optimisation.php:576-702,730-787` | `replace_image_with_next_gen` does `file_exists($avif_img_path)` + `file_exists($webp_img_path)` per image **per render**. In `maybe_serve_next_gen_images` Tag Processor loop, each `<img>` and each `srcset` candidate calls `replace_image_with_next_gen`. A gallery with 80 images × avg 3 srcset candidates = **~240 images ×2 file_exists = 480 `stat` syscalls** per HTML render. No in-request memo. | 480× `file_exists` (~0.02 ms each on local FS, ~0.1 ms on NFS/EFS) = 10–50 ms per page render; repeated for every frontend hit while images are still `pending` (queue grows). Under preload (`Cache::process_buffer_only` captures buffer into static file), the rendered static HTML bakes the rewrites so hits drop to 0, but **cache-miss** generation pays the full cost. | In-request static map `array[path => bool]` for `file_exists` results (keyed by `avif_path/webp_path`) — reused across `replace_image_with_next_gen` calls. Invalidate only on `convert_image` post-save. | **HIGH** |
- | P-WP-01 | **HIGH** | `includes/class-main.php:169-196,293-296` + `grep get_option wppo_settings = 45 sites` | `Main::__construct` does `get_option('wppo_settings', array(cache_settings/file_optimisation/…))` with default array **every frontend request** (instantiated in `performance-optimisation.php` bootstrap, not admin-only). Then `Cache::__construct` (472-473) does `get_option('wppo_settings')` again if cache enabled; `Image_Optimisation::__construct` receives `$this->options` but `Cron::__construct` (not autoloaded? Actually `new Cron()` in `Main::setup_hooks` 677) then `schedule_cron_jobs` `get_option`×2 (114,141); `Used_CSS::__construct` `get_option` fallback. **Up to 6 `get_option('wppo_settings')` deserializations per single frontend render** when `enableCache`+`convertImg`+`removeUnusedCSS` all on. Option is `autoload=yes` (single `wp_load_alloptions`), but each `get_option` still `maybe_unserialize` + array copy. | `wppo_settings` is a serialized array of ~10 tabs → ~8–15 KB → `maybe_unserialize` + `wp_cache_get` per call ×6 = 6× memcpy + 6× filter `option_wppo_settings`. Measured via `Query Monitor`: 6 calls ≈ 3–6 ms + 6× memory copies. The DB is hit once (autoload), but the PHP churn remains per request and scales with `autoload` size. | Inject `$this->options` from `Main` into every collaborator (`Cache`, `Image_Optimisation`, `Used_CSS`, `Critical_CSS`, `Cron` — via constructor arg or static accessor) and avoid re-reading. Add `Main::get_settings()` singleton and have helpers use it. Cost: 1-line change ×4 files. | **HIGH** |
- | P-WP-02 | **HIGH** | `includes/class-cron.php:113-128,274-339` | `schedule_cron_jobs()` runs on `init` **every request** (not admin-only) — does `get_option`×2 + 6× `wp_next_scheduled` (`_get_cron_array` + maybe `get_option('cron')` unserialize). `schedule_page_cron_jobs` does `get_posts(fields=ids, posts_per_page 200, paged, orderby ID ASC)` per batch **without** `no_found_rows=>false` nor `update_post_meta_cache=>false`/`update_post_term_cache=>false`. `wp_next_scheduled('wppo_generate_static_page', [$page_id])` called **per page_id (200×)** after. | `wp_next_scheduled` 200× `get_option('cron')` unserialize+hash lookup ≈ 2–5 ms per batch. `get_posts(200)` without `no_found_rows` does `SELECT SQL_CALC_FOUND_ROWS` → extra count query; with `post_term_cache` it hydrates `wp_term_relationships` for 200 IDs unnecessarily. Cron tick cost ~10–20 ms per `init` even when preload disabled (only the 3 daily hooks still run `wp_next_scheduled`). | Add `'no_found_rows'=>true, 'update_post_meta_cache'=>false, 'update_post_term_cache'=>false, 'suppress_filters'=>true` to `get_posts` in `schedule_page_cron_jobs` (286-297). Gate `schedule_cron_jobs` `wp_next_scheduled` checks behind a 5-min transient debounce or move to `admin_init`. | **HIGH** |
- | P-WP-03 | **HIGH** | `includes/class-main.php:1436-1450,1452-1464` | `admin_enqueue_scripts` does `Cache::get_cache_size()` (`calculate_directory_size` recursive `dirlist` walk of `cache/wppo/{domain}`) + `Util::get_js_css_minified_file` (`dirlist` of `cache/wppo/min/{blog_id}/js`+`css`) when transients missing. Cache key `wppo_cache_size` 15 min via `Util::transient_key`. But `get_cache_size()` walks the whole cache tree (`size` per file `WP_Filesystem::size`) — can be **hundreds of files** on large sites (5k cached pages ×1 file + .gz). `get_js_css_minified_file` walks two dirs (small). Both computed sequentially on **first admin load** after 15 min expiry, blocking `wp-admin` TTFB. | First admin page view after 15 min: `calculate_directory_size` recursive walk of 5000 files × `dirlist` (which `stat` per dir) ≈ 200–800 ms wall (FS I/O). Happens even if user visits `post.php` (early return after `get_current_screen` check line 1412-1413 mitigates — guard is correct, so cost only on `toplevel_page_performance-optimisation`). But within that page, both helpers run serially with no memoization across `get_cache_size`/`get_cache_stats`. | Keep 15-min transient; also warm via WP-Cron after `wppo_after_cache_clear` instead of on admin request. Or store `cached_pages` count alongside size in single option to avoid double walk. | **HIGH** — mitigated by screen guard, but still worst-case admin. |
- | P-DB-01 | **HIGH** | `includes/class-rum.php:276-330` | `store_sample` does **`get_option('wppo_web_vitals_rum')` + `update_option('wppo_web_vitals_rum', $all, false)` on **every beacon** (every frontend page view from anonymous visitors). No batching, no throttle, no `wp_cache` dedup beyond `update_option`'s `alloptions` write. `get_option` is autoload=no (false) so hits `wp_options` per beacon. With 10k views/day, that's **10k `SELECT` + 10k `UPDATE`** on `wp_options` (row-level `SELECT … FOR UPDATE` implicit in `update_option`'s `get_option`+`maybe_serialize`). | Options table becomes write hotspot; `wppo_web_vitals_rum` row grows to 14×200 paths×5 metrics ≈ 15k numbers → ~200 KB serialized. 10k `UPDATE` of 200 KB via `wpdb->update` per day = 2 GB write I/O; MySQL `ROW_COUNT` + autocommit + `wp_cache_flush` invalidation. On Redis object-cache, each `update_option` does `SET` + `DELETE` + `wp_cache` flush? Could cause **stampede** when many concurrent beacons race `get_option`→mutate→`update_option` and lose samples (last writer wins). | Buffer beacons in a **transient queue** (`wppo_rum_queue` array append via `wp_cache` atomic `add`/`CAS`) and flush via 5-min cron `wppo_rum_flush` that does single `get_option`+merge+`update_option`. Or use `wp_insert_post`-style `INSERT DELAYED` or custom table with `INSERT` per beacon (indexed, append-only) and 14-day `DELETE` — eliminates row contention. Minimal fix: wrap `store_sample` with `wp_cache` `add` lock + retry. | **HIGH** |
- | P-FE-01 | **HIGH** | `src/lazyload.js:62-76,684-702,709-747,840-847` | `AUTO_SIZES_SUPPORTED` IIFE probes `document.createElement('img').setAttribute('sizes','auto'); style.display:none; documentElement.appendChild(probe); window.getComputedStyle(probe).contain === 'size'; probe.remove()`. This does **forced synchronous layout** (`appendChild` + `getComputedStyle`) during script parse (top-level). The safety scan `startSafetyScan()` `setInterval(() => { querySelectorAll(getLazySelector()) … observedElements.has … observeElement }, 10000)` runs every 10 s even after `globalObserver`/`mutationObserver` already cover new nodes, doing full DOM query + loop. `MutationObserver` `observe(document.body, {childList:true, subtree:true})` fires on **every DOM mutation** (SPA `setState`, Gutenberg `wp.data` updates, third-party chat widgets) and re-queries `getLazySelector()` inside `addedNodes` loop. | Forced reflow at parse time costs ~1–3 ms layout on mobile; 10 s polling wakes the main thread periodically (battery impact, Lighthouse TBT). MutationObserver with unfiltered `subtree` can fire 10–100× per second on dynamic pages (React hydration + lazy image `src` assignments trigger mutations). Each mutation handler does `querySelectorAll` (full-tree scan) which is O(DOM size) ≈ 5–10 ms on 2000-node page. | Remove `AUTO_SIZES_SUPPORTED` forced probe from parse path — defer to `loadImages` or feature-detect via `CSS.supports('contain','size')`/`HTMLImageElement.supports` without DOM insert. Replace 10 s polling with `requestAnimationFrame`-debounced check or remove entirely (MutationObserver already observes). Throttle mutation handler with `requestIdleCallback` or `Set` batching. | **HIGH** |
- | P-CACHE-01 | **OPTIMIZATION** | `includes/class-cache.php:2137-2155,2165-2185` | `get_cache_stats()` calls `calculate_directory_size` (recursive `dirlist` walk) **and** `count_cached_pages` (second recursive walk of same tree). Both start at `cache/{domain}` and recurse identically, doubling `dirlist`/`size` syscalls. `admin_enqueue_scripts` calls only `get_cache_size` (one walk), but a future `get_cache_stats` wiring would pay double. Inside each, `WP_Filesystem::dirlist` returns `name→{type,size}` but code ignores `size` from `dirlist` and instead calls `WP_Filesystem::size(file_path)` per file (line 2151) — **duplicate stat** (dirlist already has size on some filesystem adapters, but `WP_Filesystem_Direct::dirlist` does `filesize` internally and returns it; calling `size` again re-stats). | On 5000 cached pages, `get_cache_stats` would do 2× directory walks = 10000 `dirlist` dir stats + 10000 `size` stats (20k syscalls) vs 10k needed. `get_cache_size` alone: 5k files × `size` =5k stats ≈ 100–400 ms. Double walk = 200–800 ms. | Reuse `size` from `dirlist` entry (`$file['size'] ?? $fs->size(file_path)`) and memoize walk result between `calculate_directory_size`+`count_cached_pages` with single `walk_cache_dir(cache_dir, &$total_size, &$count)` helper doing one traversal that returns both. | **HIGH** |
- | P-CACHE-02 | **HIGH** | `includes/class-cache.php:428-470,536-557` | `combine_css` cache freshness: `mtime` check per handle (`fs->mtime(src_path) > cache_mtime`). If any source newer, `source_newer=true` and the combined file is regenerated. But `combined_handles_match` sidecar `json_decode` is also checked; if `handles` mismatch, `source_newer=true` forces regeneration. However `get_cache_file_path(css,variant)` uses **domain+variant** but not **query-string or logged-in role** — there is a single combined CSS file per domain+variant for all pages. A page that includes a different set of enqueued styles (e.g., WooCommerce product page adds `photoswipe`) will alter `eligible_handles` → cache invalidated and **re-generated on next hit**, evicting the previous bundle that other pages still want. Thrashing when page-type enqueue differs. | On a WooCommerce site with 30 product pages + 20 blog pages enqueued differently, combined CSS cache **flaps** (regenerate per page-type switch) — each miss does `fetch_remote_css`×handles + `Minify` ≈ 5–10 ms + 2 writes. Could regenerate dozens of times per hour, bloating FS writes (wppo cache dir churn). | Key combined CSS by **hash of `eligible_handles`** (e.g., `md5(implode(',', sorted eligible_handles))`) as variant suffix, yielding per-handle-set caches instead of single global. Or keep separate `css` cache per URL path (like `used-css` per path) since combine is page-level (`$wp_styles->queue` varies per page). Current global file assumes queue is uniform. | **HIGH** |
- | P-CACHE-03 | **HIGH** | `includes/class-cache.php:1544-1643` + `includes/class-main.php:475-484` | Static HTML cache **stampede**: `process_buffer_for_cache`/`stash_cache` check `is_cache_allowed`+`is_not_cacheable` then `save_processed_buffer` does `prepare_cache_dir`+`put_contents` **without lock**. Concurrent frontend requests for same uncached URL (cold cache) that pass the checks simultaneously will all compute `process_buffer_only` + `save_cache_files` (gz+br) writing **same file** concurrently (WP_Filesystem `put_contents` truncates). No `flock`/`atomic rename` (unlike `used-css` which does `.tmp.wp_rand`+`move`). | Under thundering-herd (e.g., homepage after `clear_all` + surge traffic 100 RPS), 100 workers each do full render + 2× compress + write same `index.html` (~50 KB) → 5 MB total write I/O + 100× CPU minify + 100× DB queries for page render — **cache stampede** delays TTFB for burst and may corrupt `index.html.gz`/`.br` via interleaved truncate+write (not atomic). `advanced-cache.php` drop-in serves via `readfile` which may read partially-written file. | Wrap `save_cache_files(html)` with `Util::transient_key lock` 30 s (`wppo_html_cache_lock_md5(path)`) using `set_transient` `add`-style (fail if exists) like preload lock. Or write to `.tmp` + `rename` atomic as used-css does. Minimal: `save_cache_files` for HTML should write `index.html.tmp.{rand}` then `move` to `index.html`. | **HIGH** |
- **OPTIMIZATION: 1 | HIGH: 8 | MEDIUM: 12 | LOW: 6 | INFO: 4**
- 1. (**HIGH, P-WP-01**) Inject `Main::$options` to all collaborators — cut `get_option('wppo_settings')` from 6→1 per request (≈3 ms + memory).
- 2. (**HIGH, P-DB-01 + P-CACHE-03**) Make HTML cache writes atomic + add transient lock; buffer RUM beacons to a cron-flushed queue (replace per-view `update_option` write hotspot).
- 3. (**HIGH, P-CPU-01/P-CPU-04/P-CACHE-01**) Deduplicate `combine_css` classify, memoize `file_exists` per image per request (480 stats → <30), and merge `get_cache_stats` double directory walk.
- 4. (**HIGH, P-CPU-02**) Memoize `core_will_inline` double budget sim per handle (120→60 sims).
- No production code modified. Evidence is `file:line` exact; confidence **HIGH** where `Read` verified, **MEDIUM** where inferred from secondary `grep`/call-site counts.

### agent-A11-compatibility.md

- **Instruction:** Classify findings CRITICAL/HIGH/MEDIUM/LOW/INFO with `file:line` evidence, impact, recommendation, confidence. Do not modify production code.
- | C-01 | **HIGH** | External services not fully disclosed in `readme.txt` (wordpress.org guideline #9) | `readme.txt:13-59` (description), `includes/class-pagespeed.php:40` `https://www.googleapis.com/pagespeedonline/v5/runPagespeed`, `includes/class-google-fonts.php:109,139,261` `fonts.googleapis.com`, `includes/class-telemetry.php:224` `WordPress/<ver>; <home_url>` UA | `readme.txt` declares only `voku/html-min`/`matthiasmullie/minify`/`action-scheduler` at `:58`. It never mentions that (a) PageSpeed scans call Google PageSpeed Insights API (requires user-supplied API key, transports scanned URL + strategy to Google), (b) telemetry `scan()` fetches the site's own URL via cURL/`wp_remote_get` (internal), and (c) `hostGoogleFontsLocally` rewrites `fonts.googleapis.com` → local. WordPress.org plugin guidelines require: *what* external service, *what data* sent, *when*, and *link to service TOS/privacy*. Missing disclosure risks directory review friction. | Directory reviewers may flag incomplete disclosure; users with privacy requirements are not informed that PageSpeed mode exfiltrates the scanned URL to Google. | Add an `== Privacy ==` / `External services` section to `readme.txt` (and link from description) listing: PageSpeed Insights (`pagespeedonline.googleapis.com`, sends `url` + `strategy`, only when admin clicks Scan and an API key is stored), Google Fonts CDN (`fonts.googleapis.com`, only when `hostGoogleFontsLocally` is off and page contains a Google Fonts link — or actually when it is on, the *download* fetches CSS from Google once), and local telemetry (no external call beyond `home_url`). Also keep the existing library credits. | High |
- **Verdict:** No CRITICAL compatibility regression. One HIGH (external-services disclosure), remainder MEDIUM/LOW/INFO. Multisite isolation (`transient_key`, `blog_prefix`, domain-based cache, `min_cache_dir` per-blog) is exemplarily implemented; WP version gates for 6.3/6.7/6.9/7.0/7.1 APIs are exhaustive with `function_exists`; object-cache drop-in, hosting (Apache/Nginx/LiteSpeed/OLS), and portability checks are solid. The plugin is correctly declared as PHP 8.2 / WP 6.2 minimum with committed `build/` and no obfuscated code; remaining work is docs/compliance polish.

### agent-A12-quality-architecture.md

- **Instruction:** Check responsibilities, abstractions, coupling, global state, dependency management, lifecycle, naming, overly complex functions, god classes, circular deps, testability, repeated logic, inconsistent patterns, error handling, edge cases, coding standards. Classify CRITICAL/HIGH/MEDIUM/LOW/INFO/OPTIMIZATION/DUPLICATE/DEAD CODE with file:line evidence, impact, recommendation, confidence. Do not modify production code. Be evidence-based, do not invent unnecessary abstraction.
- | A-01 | **HIGH** | God class `Main` (2956 lines, 30+ responsibilities) | `includes/class-main.php:21,169,418,545,722,895,946,1093,1141,1197,1356,1419,1553,1620,1673,1836,1935,2135,2288,2384,2557,2640,2791` | `class Main` owns: constructor defaults (60+ keys, 3 in-memory backfills), `includes()` (manual requires), `setup_hooks()` (~40 `add_action`/`add_filter` registrations with 5 version-gated branches `is_wp63_plus`/`is_wp69_plus`), delay/defer parsing (`delayJSDefaultStrategy`/`delayJSIdleList`/`delayJSViewportList`/`delayJSPriority`/`delayJSIdleTimeout`), block-assets gating (`register_block_assets_filters` + `migrate_block_assets_setting`), upgrade routines (`maybe_fix_wp_cache`/`maybe_run_upgrades`/`maybe_run_version_upgrade`), cache invalidation (`on_settings_update` + `clear_all_cache` + `on_save_post_invalidate_cache`/`queue_used_css`), output buffers (`process_used_css_only`/`capture_template_start`/`emit_server_timing_header`), enqueue (admin + frontend + modules), asset minification (`minify_queued_styles`/`get_minifiable_css_path`/`is_file_minified`), `strip_static_query_strings` + `is_plugin_cache_url*` (2 variants), Google Fonts + preloading + speculation rules, per-page delay config. McCabe for `setup_hooks` alone >30 branches; `on_settings_update` 100 lines with 6 concerns. | **High.** Violates SRP; change to any one concern risks regressing others (e.g. adding a new preload setting requires touching same file as delayJS). Testability near-zero: instantiating `Main` triggers `get_option` + `Util::init_filesystem` + `new Admin_Notices` + `new Core_Tweaks` + `new Cron` unconditionally. Hard to unit-test any single responsibility in isolation. Readers must load entire file to understand one path. | **Decompose behind facades, not premature abstraction.** Extract 5 focused collaborators behind existing `Main` as facade: `Settings_Registry` (defaults + `cached_home_url` backfills), `Hook_Registrar` (version-gated `setup_hooks` with injected `wp_version`), `FrontEnd_Optimizer` (delay/defer/minify/query-strings/fonts), `Cache_Lifecycle` (`on_settings_update`/`maybe_run_version_upgrade`/`on_save_post*`), `Version_Gate` (central `is_wp63_plus`/`is_wp69_plus`/`inline_candidates_require_src`). Keep `Main` as orchestrator that composes them; do not add DI container. Each extracted class ~300–500 lines, individually testable via Brain Monkey. | High |
- | A-02 | **HIGH** | God utility `Util` (643 lines, 10 unrelated concerns) | `includes/class-util.php:30,55,89,110,153,199,243,293,315,377,385,393,426,450,480,509,533,552,589,605` | `class Util` is static grab-bag: `prepare_cache_dir` (filesystem), `init_filesystem` (WP_Filesystem bootstrap), `get_local_path` (ABSPATH-bound URL→path), `get_js_css_minified_file` (counts), `get_image_mime_type`, `generate_preload_link` (echoes `<link>`), `process_urls`/`is_url_excluded`, `cached_home_url`/`cached_content_url` (per-blog static cache + `has_filter` bypass), `min_cache_*` (blog-scoped paths), `transient_key`, `get_role_hash`/`is_cache_eligible_for_current_user`, `sanitize_settings_recursively` (heuristic `stripos('url'|'cdn'|'origin')` → `esc_url_raw` etc.), `is_auto_sizes_available`. Callers `grep -rn Util::` → 23 files. | **High.** High fan-in (23 consumers) + low cohesion (filesystem + security + caching + sanitization + mime + HTML output). Changing `sanitize_settings_recursively` heuristic risks breaking URL handling elsewhere. `generate_preload_link` echoing makes Util hard to test without output buffering. Static methods hide dependencies (no injection). | **Split by cohesion, keep `Util` as deprecated facade.** Extract: `Filesystem` (`prepare_cache_dir`/`init_filesystem`), `Url` (`get_local_path`/`normalize_url`/`is_url_excluded`/`process_urls`), `Cache_Key` (`transient_key`/`cached_home_url`/`cached_content_url`/`min_cache_*`), `Settings_Sanitizer` (`sanitize_settings_recursively`), `Role_Hash` (`get_role_hash`/`is_cache_eligible`). Keep `Util` methods as `deprecated` proxies delegating to new classes (1-line each) so 23 call-sites need not churn at once. | High |
- | A-03 | **HIGH** | Constructor does I/O + side-effects at file-load time (lifecycle) | `performance-optimisation.php:44` (`new Main()`) → `includes/class-main.php:169` (`$this->options = get_option('wppo_settings', defaults 60+ keys)` + 3 in-memory backfills at `259-291`) + `293-304` (`$this->includes()` + `new Image_Optimisation` + `new Google_Fonts` + `$this->setup_hooks()` + `Util::init_filesystem()` + `new Admin_Notices` + `new Core_Tweaks`) | Plugin bootstrap instantiates `Main` before `plugins_loaded` (file load, not hooked). Constructor unconditionally deserializes `wppo_settings` on every request (REST, cron, AJAX, CLI, cache-hit where `advanced-cache.php` would otherwise bypass WP). On `advanced-cache.php` cache-hit the whole stack still boots for the subsequent non-cached fallback. If `wppo_settings` is large or autoloaded incorrectly, adds ~1–2ms per request. | **Medium.** Wastes cycles on paths that never use settings (e.g. `DONOTCACHEPAGE` pages still pay `get_option`). Makes testing `Main` require stubbing `get_option` before construction. | **Lazy-load settings.** Store defaults as `private const DEFAULTS` and load via `get_options(): array { return $this->options ??= get_option('wppo_settings', self::DEFAULTS); }`. Move `setup_hooks()` to `plugins_loaded` (priority 0) or `init` so `advanced-cache.php` hits that bypass WP never pay the cost. Keep backfill logic in `get_options()` so in-memory defaults are computed once. | High |
- | A-04 | **HIGH** | Global mutable singleton `wppoSettings` shared between PHP and SPA (global state) | `includes/class-main.php:1466` (`wp_localize_script('wppoSettings', ['apiUrl','nonce','settings'=>redacted, 'translations','themeColors','version','litespeed'=>get_info()])`) vs `src/lib/apiRequest.js:71-131` (`apiCall` mutates `wppoSettings.settings` globally on success: `wppoSettings.settings = response.data`) + `src/App.js:135-145` (`typeof wppoSettings !== 'undefined' ? wppoSettings?.settings ?? {} : {}` per render) + `src/components/Dashboard.js:122-131` (fallback `wppoSettings?.settings?.cache_settings`) | SPA has no store; every component reads `wppoSettings.settings[tab]` on mount and `apiCall` writes it globally. Concurrent `update_settings` for two tabs can race (last-write wins, earlier tab re-render sees foreign tab mutation). `wppoSettings` is also mutated by PHP `wp_localize_script` redaction vs JS mutation — truth diverges if page not reloaded. No encapsulation, no subscription. | **High.** Race on rapid tab saves; stale `cacheSettings` prop in Dashboard after FileOptimization save (requires reload to re-sync). Difficult to test: `setupTests.js` must mock `global.wppoSettings` before every suite, and mutation leaks between tests unless reset. | **Introduce minimal store, keep global as initial seed.** Add `src/lib/store.js` with `createStore(initialSettings)` exposing `getSettings`/`setSettings`/`subscribe`; `apiCall` writes store, `App.js` provides via React context, components `useStore()`. Keep `wppoSettings` as `initialData` only (read once on mount, never mutated). No external dependency (no Redux). | High |
- | A-05 | **HIGH** | Setter injection for `Cache` optional deps creates partially-initialized object (dependency management) | `includes/class-cache.php:296-309` (`set_image_optimisation(Image_Optimisation)` / `set_google_fonts(Google_Fonts)`) vs `includes/class-main.php:473-475` (`$this->cache = new Cache($options); $this->cache->set_image_optimisation($this->image_optimisation); $this->cache->set_google_fonts($this->google_fonts)`) + `536-539` (second `new Cache` for `combineCSS` without prior deps: `if (!$this->cache) { $this->cache=new Cache(); set_* }`) + `includes/class-cache.php:1183` (`process_buffer_only`: `$image_optimisation = $this->image_optimisation ? $this->image_optimisation : new Image_Optimisation($this->options)` fallback) | Two construction paths for `Cache`; second path (combineCSS when `enableCache` off) creates deps eagerly but `process_buffer_only` also has fallback `new Image_Optimisation`/`new Google_Fonts` inline, meaning 3 places instantiate same deps. If caller forgets setters, `maybe_serve_next_gen_images` silently creates new instance with stale options (constructed from `Cache::$options` snapshot, not live `Main::$options`). | **Medium.** Bug risk: stale options snapshot in fallback path could serve wrong `excludeConvertImages`. Testability: `Cache` cannot be tested without either calling setters or triggering fallback branch. | **Constructor injection over setters.** Make `Cache::__construct(array $options, ?Image_Optimisation $image_optimisation=null, ?Google_Fonts $google_fonts=null)` and initialize in constructor (`$this->image_optimisation ??= new Image_Optimisation($options)`). Remove `set_*` methods (keep deprecated proxies for one release). Single construction path. | Medium |
- | A-06 | **HIGH** | No abstraction boundaries: 36 concrete classes, 0 interfaces, 0 DI container, manual `require_once` per file (coupling) | `includes/class-main.php:387-407` (`includes()` does `require_once WPPO_PLUGIN_PATH.'vendor/autoload.php'` + 3 `file_exists` + `require_once` for `Server_Rules`/`LiteSpeed_Integration`/`Llms` + `\WP_CLI::add_command('wppo','PerformanceOptimise\Inc\WPPO_CLI_Command')` string) + `performance-optimisation.php:28-44` (also `require vendor/autoload`) + `composer.json:14-27` (`autoload.files` for `woocommerce/action-scheduler`, no PSR-4 for `PerformanceOptimise\Inc`) + `includes/*.php:1` each has `if (!class_exists(...))` guard | Plugin deliberately avoids Composer PSR-4 for its own classes (AGENTS.md: "Classes are manually loaded via Main::includes() + vendor/autoload.php (Composer for vendor packages only)"). Consequence: adding a new class requires editing `Main::includes()` with `file_exists` guard; forgetting guard triggers fatal on missing file. `WPPO_CLI_Command` registered by string `'PerformanceOptimise\Inc\WPPO_CLI_Command'` without `require`, relies on `class_exists` trigger — fragile if `WP_CLI` loaded before `Main`. No interface means `Cache` vs `Advanced_Cache_Handler` vs `Htaccess_Handler` cannot be swapped/mocked (e.g. test double for filesystem). | **Medium.** Violates Open/Closed; high coupling to concrete classes. Prevents unit testing `Main` without loading entire plugin. String-based CLI registration typo is silent (no autoloader error). | **Add classmap, keep manual as fallback.** Add `composer.json: { "autoload": { "classmap": ["includes/"], "files": ["woocommerce/action-scheduler/action-scheduler.php"] } }` so `vendor/autoload.php` alone loads all classes; keep `Main::includes()` as `require_once` fallback for installs without `composer install` but gate on `!class_exists`. Change CLI registration to `::class` constant (`WPPO_CLI_Command::class`) after ensuring file is required. No runtime behaviour change. | High |
- | Q-01 | **HIGH** | Mutable static caches leak between tests / requests (testability + lifecycle) | `includes/class-util.php:37` (`private static array $home_url_cache = array()`), `450` (`static $cache = array()` in `cached_content_url`), `includes/class-cache.php:98` (`private static bool $inline_drift_logged = false`), `includes/class-cron.php:277` (`Util::transient_key('wppo_preload_cron_lock')` + static not, but `wppo_preload_cron_offset` option + `wppo_web_vitals_last_rescan` option are global) vs `tests/php/bootstrap.php` (defines `WPPO_Test_Bootstrap` trait but does not reset statics between tests; `Util::reset_cached_home_urls()` exists at `44` but only called in 2 tests) | `Util::$home_url_cache` and `cached_content_url::$cache` are `static` per-process. In `phpunit` run with `processIsolation=false` (default `phpunit.xml.dist`), one test's `home_url` mock leaks to next test if `has_filter('home_url')` not re-mocked. `Cache::$inline_drift_logged` is static per-process, so second test that triggers drift will not log (guard `if (self::$inline_drift_logged) return`). `Cron::web_vitals_rescan_cron` reads `get_option('wppo_web_vitals_last_rescan')` but no transient reset. Requires `Util::reset_cached_home_urls()` manual call — forgettable. | **High.** Flaky tests: order-dependent suite (run `UtilCachedHomeUrlTest` before `UtilGetLocalPathTest` may hide bug). `inline_drift_logged` static makes second drift test a false-pass. | **Reset statics in bootstrap `tearDown`.** Add `Util::reset_cached_home_urls()` + `Cache::reset_inline_drift_logged()` (add if missing; or make `$inline_drift_logged` non-static and inject) to `tests/php/bootstrap.php` `WPPO_Test_Bootstrap::tearDown()` or `setUp()`. Keep `has_filter` bypass but ensure tests always call `Brain\Monkey\Filters\expectAdded` / `has` with correct filter name. | High |


> Source agents: agent-A01-php-correctness.md, agent-A02-php-media.md, agent-A03-php-infra.md, agent-A04-php-rest-cli.md, agent-A05-js-spa.md, agent-A06-js-vanilla.md, agent-A07-css.md, agent-A08-security.md, agent-A09-performance.md, agent-A10-duplication-deadcode.md, agent-A11-compatibility.md, agent-A12-quality-architecture.md — full evidence in each agent file.
