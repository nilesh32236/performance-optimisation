# FINDINGS — DUPLICATE

_Consolidated from 12 agents (2286 lines). Each finding below is traceable to `AUDIT/AGENTS/agent-*.md` file:line evidence. This shard groups by severity DUPLICATE._

## Index

### agent-A01-php-correctness.md

- > Severities: `CRITICAL` > `HIGH` > `MEDIUM` > `LOW` > `INFO` > `OPTIMIZATION` > `DUPLICATE` > `DEAD CODE`
- | A05 | DUPLICATE | `advanced-cache-handler.php:200-274` | generated drop-in | Three identical header/304 blocks for `br`/`gzip`/`html` ( `Last-Modified`, `ETag`, `Content-Type`, `Vary`, `X-Content-*`, `Referrer-Policy`, `If-Modified-Since`/`If-None-Match`, `readfile`/`exit`). | 90 lines duplicated; maintenance burden, typo risk. | Keep duplication in string — acceptable for drop-in generation simplicity; flag as intentional. | HIGH |
- | S02 | DUPLICATE | `server-rules.php:118-134` | `Server_Rules::get_nginx_rules()` | Next-gen map fallback identical to `Htaccess_Handler` — `get_option('wppo_settings')`, `$enabled && $convert`, `apply_filters('wppo_litespeed_nextgen_rewrite')`. | Same drift risk as H01. | Centralize in `LiteSpeed_Integration`. | HIGH |
- | M01 | DUPLICATE | `class-main.php:388-391, performance-optimisation.php:41` | `Main::includes()` / bootstrap | Both bootstrap and `Main::includes()` do `require_once WPPO_PLUGIN_PATH.'vendor/autoload.php'` + Action-Scheduler include guarded by `file_exists`. | Double require is harmless (`require_once`) but hides missing-vendor failure path twice. | Keep one canonical require in bootstrap; remove from `includes()`. | HIGH |
- | M11 | DUPLICATE | `class-main.php:532-545` | `setup_hooks()` combineCSS | `if (!empty(combineCSS)) { if(!$this->cache) { $this->cache=new Cache(); ... } add_action(wp_enqueue_scripts, combine_css, PHP_INT_MAX); add_action(wp_head, maybe_preload_combine_css,1)}` — Creates Cache instance again if already created for page cache. | Duplicate construction path; two `get_option` calls. | Reuse single instance; lazy init method. | LOW |
- | `require vendor/autoload.php` duplication | `performance-optimisation.php:41` + `class-main.php:388` | **DUPLICATE** | `require_once` makes it harmless; canonicalise to bootstrap only. |
- | Vendor Action-Scheduler manual `require` | `class-main.php:389-391` | **DUPLICATE** | Autoloader already covers `woocommerce/action-scheduler`; manual include is legacy guard. |
- | Next-gen rewrite gating fallback duplication | `class-htaccess-handler.php:158-175` / `class-server-rules.php:118-134` / `class-cache.php:1595-1610` | **DUPLICATE** | Same `get_option`+`is_litespeed`+filter fallback in 3 places; centralise in `LiteSpeed_Integration`. |
- | LiteSpeed optimizer-disable guard | `class-main.php:1783,1837,1896` + `class-cache.php:367,1367` etc. (6 call-sites) | **DUPLICATE** | Repeated `class_exists(...LiteSpeed_Integration) && should_disable_wppo_optimizer()` + `has_filter(litespeed_can_optm)`; consider helper `Util::is_litespeed_optm_allowed()`. |
- | CDN gate `wppo_litespeed_can_cdn`/`litespeed_can_cdn` | `class-cache.php:1271-1295` duplicate checks inside same method | **DUPLICATE** | Two sequential `if` blocks mirror same filter; second is fallback when integration absent — correct but verbose. |
- | `maybe_store_cache` vs `is_not_cacheable` double call | `class-cache.php:1668-1707` | **DUPLICATE** | `maybe_store_cache` calls `is_not_cacheable()` which itself calls `maybe_mark_page_not_cacheable()` (side-effect). Calling twice is intentional (LS bypass path + normal guard) but obscures marker side-effect. |
- | `combine_css` freshness vs generation skip sets | `class-cache.php:404-534` + `get_combined_handles` | **DUPLICATE** | Same skip set (`core_will_inline`, `is_core_block_asset`, exclude lists, `args!='all'`) appears in both `get_combined_handles` and `combine_css` loops — intentionally kept in sync via shared helper now, but subtle drift risk remains. |
- All other findings are `LOW` (correctness/robustness), `INFO` (design notes), `OPTIMIZATION` (CPU/I/O), `DUPLICATE` (maintainability), or `DEAD CODE`.

### agent-A02-php-media.md

- Severity legend: **CRITICAL** = exploitable / data-loss; **HIGH** = correctness / security regression; **MEDIUM** = functional bug / measurable perf / a11y; **LOW** = minor edge / style; **INFO** = observation / confirmation; **OPTIMIZATION** = perf improvement opportunity; **DUPLICATE** = copy-pasted logic; **DEAD CODE** = unreachable / never-executed.
- | A02-027 | DUPLICATE | `class-image-optimisation.php:311-323` vs `1492-1504` vs `2831+` | LRU `img_size_cache` identical 12-line block duplicated 3 times | Lines 311-323 (post_process_img_dimensions closure), 1492-1504 (process_img_tag TagProcessor branch), and similar in regex branch 1703-1714 (without LRU) — same LRU eviction (`array_shift` when ≥100, `unset+reassign` for LRU promotion). Violates DRY. | Extract `private function get_cached_image_size(string $path): array|false` with static LRU. |
- | A02-028 | DUPLICATE | `class-image-optimisation.php:1368-1800` + `2831-3034` | `process_img_tag` TagProcessor vs regex fallback 350 lines duplicated | Full lazy-load logic duplicated between TagProcessor (1389-1517) and regex fallback (1518-1719). Same for `process_iframe_tag` (1845-1887) and `lazy_load_videos` (2840-3034). Any bug fix must be applied 2-3×. | Refactor shared decision helpers (`should_exclude`, `get_loading_attrs`, `apply_placeholder`) and have both paths call them. Track with tech-debt ticket. |
- | A02-029 | DUPLICATE | `class-img-converter.php:530-560` + `585-604` + `653-660` | Placeholder extraction 3× identical 6-line block | After WEBP AVIF branch (509-514), after null-image WebP decode (519-529), after GIF Imagick (587-596), after final success path (653-660) — all do `rel_path = str_replace(ABSPATH...); dominant = extract...; lqip=generate...; store...`. | Extract `store_placeholders_for_path(string $rel, GdImage $gd)`. |
- | A02-030 | DUPLICATE | `class-critical-css.php:331-418` vs `920-996` | `generate` + `inline_ccss` synchronous fallback duplicate fetch/parse/minify code | `generate()` (331-418) fetches, DOM parses, extracts CSS, calls `extract_above_fold_css` + minify. `inline_ccss` fallback (965-995) repeats `get_sample_url`, `generate`, poison check, dir+FS handling. Duplicate. | Make `inline_ccss` fallback call `generate_and_store()` directly if local-dev, not reimplement. |
- | A02-031 | DUPLICATE | `class-img-converter.php:1106-1171` vs `1253-1336` | Filesize/dimension guards duplicated between `convert_image` and `store_placeholder_data_for_upload` | Both enforce 20MB limit, 5000×5000 cap, `getimagesize` + animated WebP check, `file_get_contents` + `imagecreatefromstring`. Second copy gated on `placeholderType` but same limits. | Centralise `is_image_within_limits(string $file, int &$w, int &$h): bool` helper. |
- | A02-032 | DUPLICATE | `class-image-optimisation.php:2831-2908` | `lazy_load_videos` WP_HTML_Processor vs TagProcessor vs regex triplicate | Video lazy logic implemented thrice: Processor path (2842-2908), TagProcessor path (2910-2970), regex fallback (2972-3033). Differences in companion-video poster deferral (`autoplay+loop+muted+playsinline` check) correctly mirrored, but maintenance risk high. | Unify via shared `transform_video_tag(WP_HTML_Tag_Processor $p)` helper. |

### agent-A03-php-infra.md

- > Severity: `CRITICAL` > `HIGH` > `MEDIUM` > `LOW` > `INFO` > `OPTIMIZATION` / `DUPLICATE` / `DEAD CODE`
- | A03-027 | DUPLICATE | `class-cron.php:432-494` vs `class-llms.php:432-495` sitemap discovery | Duplicate code | Both classes implement near-identical `get_sitemap_urls`/`collect_sitemap_urls` (15 s deadline, 50 TO_FETCH_LIMIT, regex loc, home_host filter, `wp_remote_get` 5 s). Diff: Cron caps 500, LLMs caps `limit` param; Cron uppercases? Not. | Fixing SSRF/URL validation in one must be mirrored in other; drift will diverge. | Extract to `Util::discover_sitemap_urls(int $cap, int $timeoutMs = 15000, int $toFetchLimit = 50)` shared helper, unit-tested. | medium |
- | A03-028 | DUPLICATE | `class-database-cleanup.php:60-70` `METHOD_TO_TYPE` + `TABLE_MAP` | Duplicate mapping | Two maps keyed differently (`TABLE_MAP` by type, `METHOD_TO_TYPE` by method) invert each other; `clean_all` defines a third inline `$methods` array mirroring both. `auto_clean` defines fourth list (omits `unattached_media`/`oembed_cache`). | Adding a new cleanup type requires 4 edits; omission bug in `auto_clean` (intentional omission of 2 types from cron auto-clean) is implicit. | Single `CLEANUP_REGISTRY = ['revisions'=>['method'=>'clean_revisions_advanced','tables'=>…, 'auto'=>true],…]` source of truth. | medium |
- | Sitemap discovery | `class-cron.php:473-544` vs `class-llms.php:432-495` | Identical regex discovery, deadline, TO_FETCH_LIMIT, host filter. See A03-027. | DUPLICATE |
- | Batch delete template | `class-database-cleanup.php` ×5 methods | Copy-paste `do { get_col LIMIT 1000; DELETE meta; DELETE rows }`. See A03-016. | DUPLICATE |
- | Cleanup registry | `class-database-cleanup.php:42-70,788-798,843-851` | 4 parallel maps for same types. See A03-028. | DUPLICATE |
- | `wppo_settings` double fetch | `class-cron.php:114+141`, `class-system-info.php` | Same option fetched twice per request. See A03-017. | DUPLICATE |
- | Header emission double-filter | `class-litespeed-integration.php:706` + `944` | `wppo_litespeed_ttl` applied in getter and again in sender. See A03-020. | DUPLICATE |
- | LLMs high_value loop | `class-llms.php:341-372` | Two identical `high_value_urls` blocks for trends present/absent. See A03-014. | DUPLICATE |
- **Deduplication debt (DUPLICATE/DEAD):** A03-027–A03-031

### agent-A04-php-rest-cli.md

- | CLI14 | DUPLICATE | `class-wppo-cli-command.php:425-556` | `settings` docblock | Docblock for `settings` appears twice (lines 376-424 and 507-555 identical, second real method). First block is orphaned above `get_default_settings()`. | Duplicate doc causes IDE to show duplicate help for `wppo settings` in `wp help wppo`. | Remove first orphan block (or merge). | HIGH |
- | ACT10 | DUPLICATE | `class-activate.php:116-178, class-wppo-cli-command.php:434-505` | defaults | Same 60-line defaults literal duplicated. | Drift risk (ACT02). | Centralize. | HIGH |
- | AN09 | DUPLICATE | `class-admin-notices.php:84,243` | `handle_dismiss` vs `render_notices` caps | Both check `current_user_can(manage_options)` — duplicated but correct defense-in-depth. | — | Keep. | HIGH |
- | DP01 | DUPLICATE | `class-rest.php:423-434` ↔ `class-rest.php:712-728` ↔ `class-wppo-cli-command.php:627` ↔ `class-wppo-cli-command.php:640-646` | Settings allowlist `file_optimisation,preload_settings,image_optimisation,database_cleanup,object_cache,performance_audit,cache_settings,litespeed_integration,llms_txt` vs CLI `...core_tweaks` vs Activate seed `...` (7) — 3 definitions of same set, drift as in R06/ACT02. | Backup/restore parity break. | Centralize `Util::ALLOWED_TABS` constant. |
- | DP02 | DUPLICATE | `class-activate.php:116-178` ↔ `class-wppo-cli-command.php:434-505` | 60-line defaults literal duplicated. | Drift (ACT02 missing disable* keys). | Centralize. |
- | DP03 | DUPLICATE | `class-wppo-cli-command.php:425-556` | Docblock for `settings` appears twice above `get_default_settings()` and `settings()` — orphaned 24-line PHPDoc. | `wp help wppo` duplicate. | Delete first block. |
- | DP04 | DUPLICATE | `class-rest.php:553-563` ↔ `class-wppo-cli-command.php:336-339` | `realpath` + `strpos(ABSPATH)` traversal guard duplicated. | Maintenance. | Extract `Util::is_path_within_abspath($path)`. |
- | DP05 | DUPLICATE | `class-rest.php:1202-1226` ↔ `1258-1274` | SSRF checks (scheme + wp_http_validate_url + host equality) duplicated with order swap. | Drift. | Extract `Util::validate_local_url($url)` helper. |
- | DP06 | DUPLICATE | `class-rest.php:648-657` ↔ `955-970` | Pending/completed/failed count logic (`count(pending[webp])`) duplicated between `optimise_image` sync fallback and `get_image_job_status`. | Drift. | Helper `Img_Converter::get_count_summary()`. |
- | DP07 | DUPLICATE | `class-core-tweaks.php:108-127` vs `135-139` | `disable_emojis` dequeues `emoji` module then `disable_emojis_script_module` hooked separately also dequeues — double dequeue path. | Harmless defense-in-depth but confusing. | Document or keep one path with comment. |

### agent-A05-js-spa.md

- | 36 | DUPLICATE | `src/components/ImageOptimizationCard.js:19-29` vs `src/components/PerformanceAudit.js:145-156` vs `src/components/AutoloadedOptions.js:62-67` | Three `formatBytes` implementations with different rounding (`toFixed(1)` vs `toFixed(2)` on MB) | Maintenance overhead; inconsistency: Dashboard savings shows `1.5 MB` via ImageCard vs PerformanceAudit shows `1.50 MB` for same bytes | Extract single `formatBytes(bytes, precision?)` to `src/lib/util.js` and import. | HIGH |
- | 37 | DUPLICATE | `src/components/Dashboard.js:246-251` vs `src/components/DatabaseCleanup.js:279-282` | `dbOverhead` total pattern repeated: `Object.values(dbCounts).reduce((s,v)=>s+(parseInt(v,10)\|\|0),0)` | Two copies, minor | Extract helper `sumCounts(counts)` to util. | LOW |
- | DUPLICATE | `ImageOptimizationCard.js:19-29` + `PerformanceAudit.js:145-156` + `AutoloadedOptions.js:62-67` | `formatBytes` triplicated with 1 vs 2 decimal drift | Single `src/lib/util.js: formatBytes` saves maintenance; ~40 lines |
- | DUPLICATE | `Dashboard.js:246` + `DatabaseCleanup.js:279` | `reduce` parseInt summation pattern | Helper `sumCounts` |
- | DUPLICATE | `FileOptimization.js:179-215` `withNotification` vs `PluginSetting:111-161` `saveMonitoring/saveApiKey` etc. | Notification+loading boilerplate repeated >10x | Factor into `useAsyncWithNotice()` hook |
- | DUPLICATE | `App.js:159-174` `onCcssRefresh` / `onCcssRetry` identical | One handler suffices | Deduplicate |

### agent-A06-js-vanilla.md

- | 10 | DUPLICATE | `lazyload.js:542-556` vs `583-591` | Native iframe restore duplicated | Both do `loading=lazy; src=data-src; removeAttribute('data-src')` for IFRAME when USE_NATIVE_LAZY | Drift if one adds referrerPolicy | Extract restoreIframeNative(el) helper | HIGH |
- | 36 | DUPLICATE | `__tests__/lazyload.test.js:33-97` | loadScriptImpl copy-pastes production loadScript | Test maintains parallel impl with auto `replacement.onload()` divergence | False green if production fixed | Export real loadScript for test (refactor init guard) | HIGH |
- | DUPLICATE | `lazyload.js:542-556` ↔ `583-591` | Native iframe restore duplicated | Extract restoreIframeNative |
- | DUPLICATE | `lazyload.js:286-297` ↔ `202-224` | Idle query vs generic priority | Unify snapshot |
- | DUPLICATE | `lazyload.js:684-703` vs `711-747` | Safety scan vs MutationObserver both scan getLazySelector | Merge evaluate |
- | DUPLICATE | `main.js:1-101` vs `lib/apiRequest.js` | pendingRefresh/postJsonRequest 85% identical | Extract lib/nonce.js or accept |
- | DUPLICATE | `__tests__/lazyload.test.js:33-97` | loadScriptImpl copy-paste production | Export real impl |
- | DUPLICATE | `__tests__/lazyload.test.js:444-454` | restoreSizesImpl duplicates 493-504 | Same |
- | DUPLICATE | `__tests__/lazyload.test.js:295-324` | observeElement native branch reimpl | Integration gap |
- | DUPLICATE | `rum.js:29-54` | disconnectObservers 3× if/try | Helper disconnectOne |

### agent-A07-css.md

- > Severity: **CRITICAL** > **HIGH** > **MEDIUM** > **LOW** > **INFO** > **OPTIMIZATION** > **DUPLICATE** > **DEAD CODE**
- | D-05 | **DUPLICATE** | `src/css/base/_base.scss:128-136` vs `src/css/components/_fields.scss:125-135` | `.wppo-field-label` defined twice with **identical** declarations except `overflow-wrap: anywhere` added in `_fields.scss`. Base: `display:block; font-size:12px; font-weight:600; color:var(--wppo-text-muted); margin-bottom:6px; text-transform:uppercase; letter-spacing:0.06em` — Fields repeats verbatim. | Duplicate — change requires two edits; build deduplicates but keeps both selector blocks (contributes to 54 duplicates). | 99% |
- | D-06 | **DUPLICATE** | `src/css/components/_performance-audit.scss:3-7` comment + `74` comment | `// Audit controls are redefined below in the Phase 2 section.` + `// These are redefined in the Phase 2 section below.` | Stale Phase 1 placeholder — not dead CSS but dead comment/doc pointing to duplicate heading. Indicates debt from v1.5→v1.6 refactor. | 95% |
- | D-08 | **DUPLICATE** | `build/style-index.css` duplicate selectors | 54 duplicate selector groups (e.g., `.wppo-feature-card:hover` ×3, `.wppo-stats-grid` ×3, `.wppo-main` ×3). Verified via `Counter` on 457 selectors. | Build bloat; source duplication across raw `@media` vs `respond-to` (F-21) and table duplication (F-06). | 98% |

### agent-A10-duplication-deadcode.md

- **Instruction:** Find exact duplicates, near-duplicates, repeated conditions, repeated queries, repeated validation, repeated cache logic, repeated permission checks, repeated hook registrations, similar helpers, copy-pasted functionality, multiple impls of same behaviour; and unused functions/classes/methods/variables/constants/hooks/assets/dependencies/dead branches/unreachable code/commented-out. For each duplicate determine consolidate/abstract/remove/keep. For each dead trace references before declaring unused. Classify DUPLICATE/DEAD CODE/LOW/INFO with file:line evidence, impact, recommendation, confidence. Do not modify production code. Search with grep/rg for duplicates.
- | D-01 | **DUPLICATE** | `refreshNonce` + `pendingRefresh` thundering-herd guard copy-pasted between SPA and admin-bar | `src/lib/apiRequest.js:1,16-57` vs `src/main.js:6,60-101` | Both files define `let pendingRefresh = null` and an identical `refreshNonce()` (deduplicating concurrent `wppo_get_nonce` `fetch`es, `finally` clearing). `src/main.js:2` even carries comment “Keep in sync with src/lib/apiRequest.js: refreshNonce() + apiCall() retry-on-403 logic. This entry is intentionally standalone…” | **Medium.** Two implementations of same nonce-refresh semantics must be kept in sync manually. Drift risks subtle 403-retry divergence (one returns `boolean`, the other returns `string` nonce). Bundle size + maintenance cost. | **Keep with mitigation** (current design is intentional: `src/main.js` is a standalone admin-bar entry that must not import SPA bundle). Add a shared tiny helper if/when build allows code-splitting, or at minimum add a cross-file test asserting parity (already noted by comment). Do not consolidate unless entry points are merged. | High |
- | D-02 | **DUPLICATE** | `apiCall` retry-on-403 vs `postJsonRequest` retry-on-403 — two HTTP wrappers with divergent error models | `src/lib/apiRequest.js:71-131` vs `src/main.js:17-49` | SPA `apiCall` retries on JSON `code` (`rest_forbidden` / `rest_cookie_invalid_nonce`) and re-throws typed errors; admin-bar `postJsonRequest` retries on HTTP `403` status and resolves to `false` via `refreshNonce→boolean`. Same purpose (authenticated POST after nonce expiry), different contracts. `apiCall` preserves `AbortSignal`, sets `X-WP-Nonce` via arg, handles GET vs POST; `postJsonRequest` is POST-only, no signal, uses `wppoObject` vs `wppoSettings`. | **Low.** Both are correct in their context, but divergent retry predicates mean an expired-nonce response that is HTTP 200 with `code=rest_cookie_invalid_nonce` would not be retried by the admin-bar path (and vice-versa). | **Keep** (different bundles / globals). Consider unifying the retry predicate to check both `response.status===403` and `data.code∈{rest_forbidden,…}` in both places if a shared helper is ever extracted. Document the divergence. | High |
- | D-03 | **DUPLICATE** | `withNotification` helper repeated per-component vs shared `useNotice` | `src/components/FileOptimization.js:179-215` defines `withNotification`; similar inline `try/catch → notify` wrappers in `src/components/PluginSetting.js:111-160,163-206,209-251`, `src/components/DatabaseCleanup.js:150-230`, `src/components/Dashboard.js:335-380` | `FileOptimization` wraps `apiCallPromise` → `setIsLoading` → `notify(success/error)` → `catch` logging. Other tabs inline the same 10-line `try { apiCall } catch { notify(error) } finally { setIsLoading }` scaffold with only message strings differing. The project already provides `src/lib/useNotice.js:26-72` + `NoticeBanner` for presentation, but not for the loading/error plumbing. | **Medium.** 4-5 near-identical `isLoading`/`notify` wrappers bloat each settings component and invite inconsistent error messages / forgotten `dismiss()` calls. | **Abstract**: extract a `useApiCallWithNotice()` or `useSavingState()` hook (or extend `useNotice` with `wrapAsync`) that centralises `setIsLoading` / `dismiss` / `notify(success/error)` / `console.error`. Keep per-component messages as arguments. | High |
- | D-04 | **DUPLICATE** | `update_settings` validation duplicated between `Rest` and `WPPO_CLI_Command` | `includes/class-rest.php:417-481` (`allowed_tabs`, `sanitize_settings_recursively`, password→`password_set`, preservation of `pagespeed_api_key` / `server_timing_enabled` / `auto_rescan`) vs `includes/class-wppo-cli-command.php:556-680` (`settings` subcommand: same `allowed_tabs`, delegates to `Util::sanitize_settings_recursively`, same password flag logic) | Both endpoints maintain their own `allowed_tabs` whitelist and the `object_cache.password → password_set` stripping logic. Drift risk when a new tab is added (`litespeed_integration`, `llms_txt` already required two-file edits). | **Medium.** New settings tabs require two whitelists to be updated; a forgotten CLI entry would allow stale tab values via CLI while REST rejects them (or vice-versa). | **Consolidate**: extract `ALLOWED_SETTINGS_TABS` constant + `sanitize_and_normalise_settings_tab($tab,$settings)` helper to `Util` or a small `Settings_Validator` class, and have both `Rest::update_settings` and `WPPO_CLI_Command::settings` call it. | High |
- | D-05 | **DUPLICATE** | `ALLOWED_IMPORT_KEYS` / `allowed_keys` whitelists triplicated | `src/components/PluginSetting.js:22-32`, `includes/class-rest.php:713-723` (`import_settings`), `includes/class-rest.php:423-433` (`update_settings`) | Three identical allowlists of top-level settings keys (`file_optimisation`, `preload_settings`, `image_optimisation`, `database_cleanup`, `object_cache`, `performance_audit`, `cache_settings`, `litespeed_integration`, `llms_txt`). JS copy is for client-side UX, two PHP copies for two REST endpoints. | **Low.** Same drift risk as D-04; adding a new settings group requires three edits. | **Consolidate** PHP side: single `Util::ALLOWED_SETTINGS_KEYS` / `Util::get_allowed_settings_keys()` used by both REST methods; JS side keep its copy but generate from same source at build or derive from `wppoSettings` shape if feasible. | High |
- | D-06 | **DUPLICATE** | Database-cleanup method map triplicated | `includes/class-rest.php:847-857` (`$method_map` for `database_cleanup`), `includes/class-database-cleanup.php:788-798` (`$methods` in `clean_all`), `includes/class-database-cleanup.php:843-851` (`$methods` in `auto_clean`), plus `includes/class-abilities.php:466-474` | Same 9-entry map (`revisions→clean_revisions_advanced`, `auto_drafts→clean_auto_drafts`, … `oembed_cache→clean_oembed_cache`) appears in 4 places; `clean_all` vs `auto_clean` even differ in that `auto_clean` omits `unattached_media`/`oembed_cache` while `clean_all` includes them, with no comment explaining the intentional gap. | **Medium.** New cleanup types (`unattached_media`, `oembed_cache` at `NEXT`) required edits to 4 maps; omission in `auto_clean` may be intentional but is not documented. Also `Database_Cleanup::TABLE_MAP` / `METHOD_TO_TYPE` are single-source but not used by `Rest`/`Abilities`. | **Consolidate**: make `Database_Cleanup` the single source (expose `get_method_map()` or `ALL_METHODS` constant) and have `Rest` and `Abilities` and `auto_clean` derive from it; document why `auto_clean` excludes two types or include them. | High |
- | D-07 | **DUPLICATE** | Batched `DELETE … LIMIT 1000` loop copy-pasted across 5-6 `clean_*` methods | `includes/class-database-cleanup.php:86-130` (`clean_revisions`), `267-311` (`clean_auto_drafts`), `321-365` (`clean_trashed_posts`), `373-417` (`clean_spam_comments`), `425-469` (`clean_trashed_comments`) | Each method repeats the same `do { $post_ids = get_col("… LIMIT 1000"); if error return false; if empty break; $placeholders = implode(',', fill) ; delete meta; delete rows; $deleted += … } while (count>=1000)` scaffold, only the `WHERE` clause and table (`posts` vs `comments`) varies. `clean_spam/trashed_comments` additionally repeat `commentmeta` deletion. | **Medium.** ~45 lines × 5 = 225 lines of near-identical batching/error handling. Fixes to placeholder generation or error checks must be applied 5 times. | **Abstract**: extract a generic `batch_delete_by_ids(string $select_sql, string $meta_delete_sql, string $rows_delete_sql, int $batch=1000): int|false` helper and have each `clean_*` supply only its table/where fragment (or a callback). Keep `clean_revisions_advanced` and `clean_expired_transients` / `clean_orphan_postmeta` separate as they have bespoke logic. | Medium |
- | D-08 | **DUPLICATE** | CDN-URL / site-URL blank guard + normalisation repeated in `Cache::maybe_apply_cdn` vs `Cache::minify_buffer` gate | `includes/class-cache.php:1297-1299` (`$cdn_url` empty guard) + `1303-1304` (`$site_url = Util::cached_home_url(); $cdn_url=rtrim`) vs `includes/class-main.php:1369` LiteSpeed `should_disable_wppo_optimizer` + `has_filter('litespeed_can_optm')` checks in both `Cache::combine_css:367-372` and `Cache::minify_buffer:1367-1372` and `Cache::maybe_apply_cdn:1272-1295` | LiteSpeed coexistence gate (`class_exists(LiteSpeed_Integration) && should_disable_wppo_optimizer()` + `has_filter('litespeed_can_optm') && !apply_filters('litespeed_can_optm',true)`) is verbatim in 3 places in `class-cache.php`. CDN host/site-url handling also repeats `Util::cached_home_url()` + `rtrim`. | **Low.** Gate is 4 lines × 3 = 12 lines; change to LiteSpeed detection would need 3 edits. | **Abstract**: add a private `Cache::should_bypass_for_litespeed(): bool` helper and call it from all three sites. Likewise centralise CDN URL resolution behind `Util::get_effective_cdn_url()`. | Medium |
- | D-09 | **DUPLICATE** | `is_cache_eligible_for_current_user` / `is_cache_allowed_for_current_user` / `should_optimise_for_logged_in` — three identical wrappers | `includes/class-util.php:552-578` (canonical), `includes/class-cache.php:267-271`, `includes/class-main.php:320-324` | `Cache::is_cache_allowed_for_current_user` and `Main::should_optimise_for_logged_in` both delegate to `Util::is_cache_eligible_for_current_user($this->options['cache_settings']??[])`. Trivial duplication. | **Info.** Only 3-line wrappers; already DRY at logic level. | **Keep** (thin wrappers improve readability / allow per-class evolution). Optionally mark as `@see Util::is_cache_eligible_for_current_user`. | High |
- | D-10 | **DUPLICATE** | `get_role_hash` / `get_logged_in_role_hash` + `set_role_hash_cookie` hash derivation | `includes/class-util.php:533-540`, `includes/class-cache.php:280-287`, `includes/class-main.php:351-365` | Role-hash is computed via `Util::get_role_hash($user)` in both `Cache::get_logged_in_role_hash` and `Main::set_role_hash_cookie`; the cookie path/domain/ttl logic is only in `Main`. Thin but repeated concept. | **Info.** Logic is already centralised in `Util`. | **Keep**. | High |
- | D-11 | **DUPLICATE** | `Util::process_urls` called in ~25 sites; each site re-wraps with `(array)` + `?? array()` | `includes/class-main.php:571,581,610,621,646,650,654` etc., `includes/class-cache.php:395,1711`, `includes/class-image-optimisation.php:136-141` | Every call site repeats `Util::process_urls($this->options['…']['exclude…'] ?? array())` with manual `array` cast / null-coalesce. The helper already handles both `string` and `array` inputs (newline split + dedup). | **Info.** Not duplication of logic, but repetitive call-site boilerplate. | **Keep** — helper is already DRY; optional `Util::get_processed_option($options,'group','key')` could trim call-sites but is not load-bearing. | Medium |
- | D-12 | **DUPLICATE** | `get_combined_handles` vs `combine_css` generation loop — same skip rules duplicated | `includes/class-cache.php:365-557` (`combine_css` generation loop) vs `618-663` (`get_combined_handles`) | Both iterate `$styles`, check `core_will_inline`, `is_core_block_asset`, `excludeCombineCSS` (handle + `strpos(src)`), `args==='all'`. Generation loop additionally does `fetch_remote_css`, `extra['before'/'after']`, `wp_dequeue_style`; `get_combined_handles` collects handles only. Rules must stay identical or stale-cache check (`combined_handles_match`) misfires. | **Medium.** The two loops were intentionally split in 1.9.0 to fix cache-freshness, but any future rule addition (e.g. new `is_core_block_asset` condition) must be applied twice. | **Keep with mitigation** — current split is correct. Already flagged by comment “The same skip rules are applied below during generation so the two branches stay consistent”. Consider extracting an `is_handle_eligible_for_combine($handle,$exclude,$separate)` predicate used by both loops. | High |
- | D-13 | **DUPLICATE** | Responsive image `srcset` / `srcset` + `data-srcset` rewriting duplicated between `maybe_serve_next_gen_images` Tag-Processor and regex branches | `includes/class-image-optimisation.php:594-698` (two branches: `WP_HTML_Tag_Processor` vs `preg_replace_callback`) | Both branches split on `,`, `preg_split('/\s+/',…,2)`, `normalize_url`, `replace_image_with_next_gen`, and rejoin with `', '`. The Tag-Processor path additionally handles `is_valid_url` + attribute-presence guards; regex path reimplements same logic with raw string search. | **Medium.** Fixes to srcset descriptor handling must be applied twice. | **Keep** — fallback is required when `WP_HTML_Tag_Processor` is unavailable. Optionally extract a shared `rewrite_srcset_attribute(string $srcset): string` helper called by both branches. | Medium |
- | D-14 | **DUPLICATE** | `post_process_img_dimensions` `getimagesize` LRU cache keyed by `$local_path` vs `post_process_placeholders` placeholder extraction — both regex-scan `<img … data-src=` | `includes/class-image-optimisation.php:262-289` (`post_process_placeholders` regex `#<img\b[^>]*\sdata-src=…>#i`) vs `299-339` (`post_process_img_dimensions` same regex) vs `356-403` (`post_process_auto_sizes` broader `#<(img|source)\b[^>]*\s(?:data-src|data-srcset)=…>#i`) | Three `preg_replace_callback` passes scan the same serialized buffer for `data-src`/`data-srcset` images to inject placeholder `src`, `width`/`height`, and `data-sizes="auto"`. Each pass recompiles a similar regex and walks the whole buffer. | **Low.** Correct and each pass has distinct post-processing (placeholder vs dimensions vs auto-sizes). Merging into one pass would complicate logic; the three-pass cost is linear in buffer size and acceptable. | **Keep** — document that the three passes are intentionally separate stages; optionally note order dependency (dimensions must precede auto-sizes). | Medium |
- | D-15 | **DUPLICATE** | JS `handleChange(setSettings)` factory pattern repeated across all settings components | `src/lib/util.js:1-33` (single factory) used as `onChange={handleChange(setSettings)}` in `src/components/FileOptimization.js` (~15 fields), `ImageOptimization.js`, `DatabaseCleanup.js`, `PreloadSettings.js`, `ObjectCache.js`, `PluginSetting.js` | Each component repeats `import {handleChange}` + `onChange={handleChange(setSettings)}` per `<SwitchField>`/`<textarea>`. Factory itself is DRY, but wiring is repetitive. | **Info.** Factory is already abstracted; per-field wiring is idiomatic. | **Keep**. | High |
- | D-16 | **DUPLICATE** | SCSS `wppo-text-*` utilities + `wppo-mt-*` spacing helpers vs component-scoped margins | `src/css/base/_base.scss:27-43` (`wppo-mt-8`…`wppo-mb-24`, `wppo-text-small`/`wppo-text-13` both `font-size:13px`), `src/css/components/*` (ad-hoc `marginTop: 8px` etc. in JS inline styles) | `wppo-text-small` and `wppo-text-13` are exact duplicates (`font-size:13px`). Several components use inline `style={{marginBottom:'16px'}}` instead of `wppo-mb-*` utilities. `wppo-flex-gap-12` vs inline `display:flex;gap:12px`. | **Low.** Utility duplication is minor; inline styles bypass the design system. | **Consolidate**: deprecate `wppo-text-13` in favour of `wppo-text-small` (or keep alias with comment). Prefer utility classes over inline `style` margins for consistency. | Medium |
- | D-17 | **DUPLICATE** | `wppo-notice--warning/info/error/success` colour definitions duplicated via CSS variables vs hard-coded `#fef2f2` in JS | `src/css/base/_base.scss` + `src/css/components/_notices.scss` (variables `--wppo-error-bg` etc.) vs `src/components/PluginSetting.js:880` (`style={{borderLeft:'4px solid #ef4444', background:'#fef2f2'}}`) | Import-card danger zone uses hard-coded hex instead of `var(--wppo-error-bg)` / `var(--wppo-danger)`. | **Info.** One-off inline style; variables already exist. | **Abstract**: replace hard-coded `#fef2f2`/`#ef4444` with CSS class `wppo-danger-zone` (already defined in `src/css/components/_card.scss:92-98`) and remove inline style. | Medium |
- | D-18 | **DUPLICATE** | Top-level `try/catch is_multisite()` guard repeated in cleanup / Util | `includes/class-database-cleanup.php:498-503,924-931`, `includes/class-util.php:510-517`, `includes/class-cache.php` (around `get_current_blog_id`) | `try { is_multisite() } catch(Throwable) { fallback }` appears 3+ times with same comment “shared object cache backend”. | **Low.** Small guard; already centralised via `Util::transient_key` which encapsulates the try/catch. Call-sites that still inline the guard (DB cleanup) could just call `Util::transient_key` / check `is_multisite` once in `Util`. | **Consolidate**: have `clean_expired_transients` / `get_counts` delegate the multisite check to `Util::is_multisite_safe()` or rely on `Util::transient_key` prefix instead of reimplementing. | Low |
- | D-19 | **DUPLICATE** | `get_autoloadable_values` / `get_autoloaded_options` vs `write_combined_handles` JSON size-map — no duplication, noted as INFO | — | — | — | **Keep** — no action. | — |
- | D-20 | **DUPLICATE** | `register_block_assets_filters` logic mirrored in `Main::__construct` defaults | `includes/class-main.php:187` (`blockAssetsOnDemand => function_exists('wp_load_classic_theme_block_styles_on_demand')`) vs `735-753` (`register_block_assets_filters`) vs `787-819` (`migrate_block_assets_setting`) | Three places reason about the same WP 6.9 `wp_load_classic_theme_block_styles_on_demand` existence flag and the `blockAssetsOnDemand` default. Logic is intentionally layered (construct defaults vs filter registration vs one-time DB migration). | **Info.** Each layer has distinct lifecycle (in-memory default, filter side-effect, DB write). | **Keep** — duplication is lifecycle-appropriate. | High |

### agent-A12-quality-architecture.md

- **Instruction:** Check responsibilities, abstractions, coupling, global state, dependency management, lifecycle, naming, overly complex functions, god classes, circular deps, testability, repeated logic, inconsistent patterns, error handling, edge cases, coding standards. Classify CRITICAL/HIGH/MEDIUM/LOW/INFO/OPTIMIZATION/DUPLICATE/DEAD CODE with file:line evidence, impact, recommendation, confidence. Do not modify production code. Be evidence-based, do not invent unnecessary abstraction.


> Source agents: agent-A01-php-correctness.md, agent-A02-php-media.md, agent-A03-php-infra.md, agent-A04-php-rest-cli.md, agent-A05-js-spa.md, agent-A06-js-vanilla.md, agent-A07-css.md, agent-A08-security.md, agent-A09-performance.md, agent-A10-duplication-deadcode.md, agent-A11-compatibility.md, agent-A12-quality-architecture.md — full evidence in each agent file.
