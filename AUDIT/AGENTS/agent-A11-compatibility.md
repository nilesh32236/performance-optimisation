# Agent A11 — WordPress Compatibility Audit

**Scope:** exhaustive line-by-line audit of WordPress compatibility — WP version gates, PHP 8.2 declaration, multisite isolation, object-cache implementations, hosting environments (Apache/Nginx/LiteSpeed/OLS), caching-plugin coexistence, themes/plugins, plus wordpress.org compliance (readme.txt, performance-optimisation.php headers, phpcs.xml, composer.json, package.json, .distignore, build committed, no obfuscated code, external services disclosure). Cross-checks `Util::transient_key` isolation, `blog_prefix`, domain-based static cache, `wp_get_loading_optimization_attributes` back-compat, and every `function_exists` gate.  
**Date:** 2026-08-27  
**Auditor:** agent A11 (`compatibility`)  
**Workspace:** `/var/www/nileshportfolio.duckdns.org/wp-content/plugins/performance-optimisation`  
**Instruction:** Classify findings CRITICAL/HIGH/MEDIUM/LOW/INFO with `file:line` evidence, impact, recommendation, confidence. Do not modify production code.

> **Method:** Read every production file completely (`Read` with offsets) plus `performance-optimisation.php`, `readme.txt`, `composer.json`, `package.json`, `phpcs.xml`, `.distignore`, `uninstall.php`, `build/*.asset.php`, `templates/object-cache.php`, `includes/class-util.php`, `class-main.php`, `class-cache.php`, `class-advanced-cache-handler.php`, `class-htaccess-handler.php`, `class-server-rules.php`, `class-object-cache.php`, `redis-connect-helper.php`, `class-image-optimisation.php`, `class-img-converter.php`, `class-rest.php`, `class-activate.php`, `class-deactivate.php`, `class-cron.php`, `class-litespeed-integration.php`, `class-telemetry.php`, `class-pagespeed.php`, `class-rum.php`, `class-system-info.php`, `class-database-cleanup.php`, `class-core-tweaks.php`, `class-cdn-purger.php`, `class-log.php`, `class-asset-manager.php`, `class-llms.php`, `src/lazyload.js`, `src/main.js`, `src/rum.js`, `src/lib/apiRequest.js`. Ran `grep -rn` for `transient_key|get_transient|blog_prefix|is_multisite|function_exists|wp_get_loading_optimization_attributes|wp_maybe_inline_styles|wp_image_quality|wp_get_image_encode_quality|incompatible|Requires at least` and verified `build/` committed, `.distignore` anchored, `vendor/` ignored, external endpoints, obfuscation markers. Every claim below cites a `file:line`.

---

## 1. Files Reviewed (with line counts & surface)

| # | File | Lines | Surface examined |
|---|------|-------|------------------|
| 1 | `performance-optimisation.php` | 70 | Plugin header (`Requires at least`, `Requires PHP`, `Tested up to`), constants, `register_activation_hook` |
| 2 | `readme.txt` | 330 | Header block, description, installation, screenshots, changelog, FAQ, `Upgrade Notice`, disclosure statements |
| 3 | `composer.json` | 68 | `require.php >=8.2`, `config.platform.php 8.2`, `autoload.classmap includes/`, `require-dev` |
| 4 | `package.json` | 36 | `wp-scripts build` entry points, `jest.testEnvironment jsdom`, `browserslistrc`, deps |
| 5 | `phpcs.xml` | 16 | `WordPress` ruleset, `exclude-pattern` vendor/node_modules/build, `testVersion 8.2-` |
| 6 | `.distignore` | 58 | Anchored `/.git/` `/vendor/...` `/build/` keep, `/src/` exclude, `composer.json` strip |
| 7 | `uninstall.php` | 165 | Per-site cleanup, `is_multisite` + `get_sites` pagination, `switch_to_blog`, transient sweep, drop-in removal |
| 8 | `includes/class-util.php` | 643 | `transient_key()`, `cached_home_url()`, `cached_content_url()`, `min_cache_dir()`, `get_role_hash()`, `is_auto_sizes_available()`, `sanitize_settings_recursively()` |
| 9 | `includes/class-main.php` | 2956 | Constructor defaults (`wp_load_classic_theme_block_styles_on_demand` gate), `setup_hooks` version branches (6.3 defer, 6.9 template-enhancement), `register_block_assets_filters`, `migrate_block_assets_setting`, `on_settings_update`, `should_optimise_for_logged_in` |
| 10 | `includes/class-cache.php` | 2269 | `CACHE_DIR /cache/wppo`, domain validation, `block_assets_are_separate()`, `core_will_inline()` / `core_inline_budget_will_inline()`, `should_skip_combine_for_inline_budget`, `maybe_apply_cdn`, `is_not_cacheable`, `will_combine_css_inline`, `get_styles_inline_limit`, `inline_candidates_require_src/readable` |
| 11 | `templates/object-cache.php` | 1152 | `WP_Object_Cache` drop-in, `blog_prefix`, `add_salt`, `get_key` with `global_groups`/`no_mc_groups`, `flush`/`flush_group` SCAN, `wp_cache_supports` |
| 12 | `includes/class-advanced-cache-handler.php` | 324 | `DROPIN_MARKER`, `is_our_dropin`, `foreign_dropin_present`, `create()` early-boot drop-in codegen |
| 13 | `includes/class-htaccess-handler.php` | 222 | `MARKER wppo_rules`, `update_rules` `insert_with_markers`, `get_rules` next-gen block |
| 14 | `includes/class-server-rules.php` | 191 | `get_server_type`, `is_litespeed`, `get_nginx_rules`, `get_apache_rules` |
| 15 | `includes/class-object-cache.php` | 363 | `DROPIN_MARKER`, `get_status`, `ping`, `enable`/`disable` |
| 16 | `includes/redis-connect-helper.php` | 377 | `wppo_redis_connect` standalone/sentinel/cluster, `wppo_parse_nodes`, `wppo_apply_redis_options` |
| 17 | `includes/class-image-optimisation.php` | 3037 | `setup_hooks` client-side gates, `filter_client_side_supported_mime_types`, `set_loading_optimization_attributes` (WP 6.7 `wp_get_loading_optimization_attributes`), `is_auto_sizes_available`, `post_process_auto_sizes`, `process_picture_blocks_processor` |
| 18 | `includes/class-img-converter.php` | 1865 | `core_handles_next_gen` (`wp_image_quality`), `core_handles_both_next_gen`, `resolve_encode_quality` (7.1 `wp_get_image_encode_quality` → 6.7 `wp_image_quality` → fallback), `resolve_output_format` (`wp_get_image_editor_output_format`), `is_client_side_media_processing` |
| 19 | `includes/class-rest.php` | 1563 | 25 `register_rest_route`, `permission_callback`, `update_settings` preservation, `get_transient_key` via `transient_key` |
| 20 | `includes/class-activate.php` | 348 | `add_wp_cache_constant` wp-config.php walk, `maybe_run_upgrades`, `VERSION_OPTION` |
| 21 | `includes/class-deactivate.php` | 156 | `unschedule_crons`, `remove_wp_cache_constant` |
| 22 | `includes/class-cron.php` | 733 | `schedule_cron_jobs`, `schedule_page_cron_jobs`, `get_sitemap_urls`, `is_url_excluded`, transient locks |
| 23 | `includes/class-litespeed-integration.php` | 1343 | `is_litespeed`, `is_lscache_active`, `effective_mode`, `should_disable_wppo_optimizer`, `can_apply_cdn`, `is_nextgen_rewrite_enabled` |
| 24 | `includes/class-telemetry.php` | 985 | `scan`, `AUdit_SALT`, `MAX_REDIRECT_HOPS`, `transient_key`, `wp_cache_get_salted` |
| 25 | `includes/class-pagespeed.php` | 661 | `API_ENDPOINT https://www.googleapis.com/pagespeedonline/v5/runPagespeed`, `get_transient_key` via `transient_key`, `TREND_OPTION` |
| 26 | `includes/class-rum.php` | 332 | `rum_collect` public `__return_true` + token + `Util::transient_key` ratelimit, `sanitize_sample` |
| 27 | `includes/class-system-info.php` | 633 | `get_all`, `is_multisite` guards |
| 28 | `includes/class-database-cleanup.php` | 1142 | `TABLE_MAP`, `clean_*` |
| 29 | `includes/class-cdn-purger.php` | ~120 | `purge_all` edge purge |
| 30 | `includes/class-log.php` | 150 | `wppo_activity_logs` `dbDelta` |
| 31 | `includes/class-core-tweaks.php` | ~220 | Emoji/embed/dashicons/heartbeat |
| 32 | `build/index.asset.php`, `build/lazyload.asset.php`, etc. | 1 each | Committed build manifests |
| 33 | `build/index.js`, `build/lazyload.js`, `build/main.js`, `build/rum.js`, `build/tab-*.js` | — | Committed webpack output |
| 34 | `src/lazyload.js`, `src/main.js`, `src/rum.js`, `src/lib/apiRequest.js` | — | Vanilla loaders, admin-bar nonce refresh |

---

## 2. Findings

| # | Severity | Title | Files : Lines | Evidence | Impact | Recommendation | Confidence |
|---|----------|-------|---------------|----------|--------|----------------|------------|
| C-01 | **HIGH** | External services not fully disclosed in `readme.txt` (wordpress.org guideline #9) | `readme.txt:13-59` (description), `includes/class-pagespeed.php:40` `https://www.googleapis.com/pagespeedonline/v5/runPagespeed`, `includes/class-google-fonts.php:109,139,261` `fonts.googleapis.com`, `includes/class-telemetry.php:224` `WordPress/<ver>; <home_url>` UA | `readme.txt` declares only `voku/html-min`/`matthiasmullie/minify`/`action-scheduler` at `:58`. It never mentions that (a) PageSpeed scans call Google PageSpeed Insights API (requires user-supplied API key, transports scanned URL + strategy to Google), (b) telemetry `scan()` fetches the site's own URL via cURL/`wp_remote_get` (internal), and (c) `hostGoogleFontsLocally` rewrites `fonts.googleapis.com` → local. WordPress.org plugin guidelines require: *what* external service, *what data* sent, *when*, and *link to service TOS/privacy*. Missing disclosure risks directory review friction. | Directory reviewers may flag incomplete disclosure; users with privacy requirements are not informed that PageSpeed mode exfiltrates the scanned URL to Google. | Add an `== Privacy ==` / `External services` section to `readme.txt` (and link from description) listing: PageSpeed Insights (`pagespeedonline.googleapis.com`, sends `url` + `strategy`, only when admin clicks Scan and an API key is stored), Google Fonts CDN (`fonts.googleapis.com`, only when `hostGoogleFontsLocally` is off and page contains a Google Fonts link — or actually when it is on, the *download* fetches CSS from Google once), and local telemetry (no external call beyond `home_url`). Also keep the existing library credits. | High |
| C-02 | **MEDIUM** | `Tested up to: 7.1` claims compatibility with unreleased core (future-date) — may confuse `Tested up to` semantics | `performance-optimisation.php:7` `Tested up to: 7.1`, `readme.txt:6` `Tested up to: 7.1`, `composer.json:55` `platform.php 8.2` | `Tested up to` should be a *released* WordPress version that was actually tested. `7.1` is dated `2026-08-11` in `readme.txt:89-98` and the code already branches on `wp_load_classic_theme_block_styles_on_demand` (6.9), `wp_get_image_encode_quality` (7.1), `wp_is_client_side_media_processing_enabled` (7.1). If 7.1 ships with API changes, the claim is unverifiable today and WP.org's readme validator may warn. | Overstatement of testing; directory shows “untested with your version” warning is suppressed incorrectly. Low functional risk because `function_exists` gates protect runtime. | Keep `Tested up to: 7.1` only if CI actually ran against `wordpress:7.1` nightly/beta (add note). Otherwise set to the latest *released* WP (e.g. `6.9` or `6.8` whatever CI covers) and bump to `7.1` on its release. Add `Tested up to` to `RATIONALE` doc. | High |
| C-03 | **MEDIUM** | PHP 8.2 platform pin vs `composer.json` `symfony/css-selector ^7.4` and WP 6.2 floor — `composer install` on PHP 8.1 build hosts fails despite plugin header declaring 8.2 | `composer.json:13-14` `php >=8.2`, `:55` `platform.php 8.2`, `:17` `symfony/css-selector ^7.4` (requires `php ^8.2`), `:15-16` `matthiasmullie/minify ^1.3` requires `php >=5.3`, `woocommerce/action-scheduler ^4.1` requires `php >=7.4` | `platform` pin forces `composer install` to resolve as if PHP 8.2 even on PHP 8.1 CI, which is intentional for reproducibility. But `.distignore` strips `composer.json`/`composer.lock`, so WP.org ships vendor built on the packager's PHP. If the packager runs `composer release` (no `--ignore-platform-reqs` on `release`? actually `:63` does `--ignore-platform-reqs`) it's fine. However `AGENTS.md` claims `composer dev-setup` uses `--ignore-platform-reqs` — that hides actual PHP incompatibility. A host on PHP 8.1 will see `Requires PHP: 8.2` header and WP will refuse activation — *correct* per header — but if they install via zip on 8.1 and ignore header, `symfony/css-selector 7.4` will fatal on `readonly` properties. | Low for WP.org users (header blocks activation). Higher for manual installs / git checkouts without platform check. | Keep as-is but document that `composer.json` `platform` is a *resolution* hint, not a runtime check; ensure release CI runs `composer validate --strict` on PHP 8.2 (already in `psalm-wpcs-check.yml`), and that `scripts/build-release.sh` uses `composer install --no-dev --optimize-autoloader` on PHP 8.2. Optionally add `engines` note to `package.json`. | Medium |
| C-04 | **LOW** | Transient-isolation: `uninstall.php` uses inline `is_multisite() ? get_current_blog_id().'_' : ''` prefix instead of `Util::transient_key()` — consistent now but diverges from canonical helper's `try/catch` + `function_exists` hardening | `uninstall.php:93` `$transient_prefix = is_multisite() ? get_current_blog_id() . '_' : ''`, `includes/class-util.php:509-518` `transient_key()` with `!function_exists('is_multisite')` guard and `try/catch \Throwable` | `uninstall.php` runs in `WP_UNINSTALL_PLUGIN` context where `is_multisite()` may be mocked or throw under Brain Monkey / early uninstall; the inline pattern lacks the defensive `function_exists`/`try` that `Util::transient_key` provides (added for Redis shared-cache safety). Deletion set is finite (`wppo_activation_notices`, `wppo_show_welcome_notice`, `wppo_cache_size`, `wppo_total_js_css`, `wppo_wp_cache_fix_checked`) and matches `Main::maybe_fix_wp_cache` etc., so no collision today. | If a persistent object cache still holds a prefixed transient at uninstall on a flaky multisite mock, the non-prefixed delete misses it, leaving stale data. Negligible because uninstall iterates `switch_to_blog` over all sites. | Replace inline prefix in `uninstall.php:93-98` with `PerformanceOptimise\Inc\Util::transient_key()` (or duplicate its `function_exists` guard) for single-source hardening. Alternately document that inline form is intentional for uninstall without autoloader. | High |
| C-05 | **LOW** | `templates/object-cache.php:69` `blog_prefix = (is_multisite() ? get_current_blog_id() : $table_prefix).':'` — mixes `get_current_blog_id()` with `$table_prefix` on single-site, while WP core memcached uses `table_prefix` consistently on multisite too | `templates/object-cache.php:69`, `vendor/php-stubs...` `get_blog_prefix` | On single-site `$table_prefix` is typically `wp_` → prefix `wp_:`; on multisite it's `1:`. This matches WP's `WP_Object_Cache` expectation and AGENTS.md's `blog_prefix` note. However some multisite installs with custom `table_prefix` per site (HyperDB) rely on `$table_prefix` isolation; using `blog_id` alone conflates `blog_id` reuse across networks? In practice WP core also uses `blog_prefix` via `get_current_blog_id()` for cache key prefix, so this is canonical. No cross-site leak because `WP_CONTENT_DIR . '/wppo-redis-config.php'` is shared — but `blog_prefix` still isolates. | No functional leak; the `:` suffix prevents `1:` matching `10:` (`strpos` not used; prefix is concatenated). Minor documentation gap: reason for ternary not commented. | Add a one-line comment referencing `AGENTS.md` / `Multisite compatibility` explaining the ternary choice and that Redis SCAN flush at `object-cache.php:536-537` uses `$this->blog_prefix` consistently. No code change. | Medium |
| C-06 | **INFO** | Domain-based static HTML cache `wp-content/cache/wppo/{domain}/{path}/index.html` is correctly multisite-safe; minor inconsistency between `Cache` domain validation and `Advanced_Cache_Handler` drop-in domain sanitization regex | `includes/class-cache.php:200-227` `preg_match '/^[a-z0-9\.\-]+$/i'` + `..`/`/` checks + `strtolower`, `includes/class-advanced-cache-handler.php:158-165` `preg_replace '/[^a-z0-9.:-]+/i'` + `strtolower` + `..` guard | Both enforce `..` exclusion and domain character whitelist, but `Cache` strips port via `explode(':')` while the drop-in keeps `:` in `$site_domain` (`preg_replace` allows `[:-]` and `strtolower(preg_replace(...))`). Ports are rare but on `example.com:8080` the file cache directory would be `example.com:8080` (Cache after strip) vs `example.com:8080`? Wait Cache strips port → `example.com`; drop-in keeps `example.com:8080` after lower? Actually `$raw_domain` is not port-stripped in drop-in; `$site_domain` lowercases but does not `explode(':')`. On a ported host the drop-in would write to `.../example.com:8080/...` while `Cache` writes to `.../example.com/...`, causing cache miss. | Rare (non-standard port). No cache poisoning because both validate `..`. But ported hosts (e.g. `:8080` dev) would never hit the static file via drop-in. | Align the two: either keep port in both (include port in directory) or strip in both. Add a test for host-with-port. Document that `Cache::__construct` strips port intentionally and that drop-in should do the same. | High |
| C-07 | **INFO** | `wp_get_loading_optimization_attributes` back-compat is exemplary — every call-site is `function_exists`-gated with a safe fallback, but regex-fallback paths in `class-image-optimisation.php` re-invoke core for `fetchpriority` under different contexts | `includes/class-image-optimisation.php:1296` `if (function_exists('wp_get_loading_optimization_attributes'))`, `1322-1342`, `1425`, `1523,1540,1585,1611,1625`, `includes/class-util.php:589-592` `is_auto_sizes_available()` | All 7 call-sites pass `tag_name='img'`, `attr` from `WP_HTML_Tag_Processor`, `context='wp-html-tag-processor'` vs `'regex-fallback'`. Fallback correctly defaults to `fetchpriority=low`/`high` and `decoding=sync/async` when core absent (WP 6.2 floor). The context string difference is harmless but means regex path receives `'regex-fallback'` while Tag-Processor path receives `'wp-html-tag-processor'` — core's `wp_get_loading_optimization_attributes` may branch on context in future. | None today; future core adding context-specific logic could make the two paths diverge invisibly. | Keep dual-context but document why `'regex-fallback'` vs `'wp-html-tag-processor'` are used, and centralise `set_loading_optimization_attributes` so the context constant is single-sourced. | High |
| C-08 | **LOW** | `phpcs.xml` `testVersion 8.2-` vs `composer.json` `platform 8.2` is consistent, but `PHPCompatibility` standard without explicit `testVersion` in CI may not run on 7.2 to verify back-compat of header-declared `Requires at least: 6.2` | `phpcs.xml:13` `<config name="testVersion" value="8.2-"/>`, `.github/workflows/psalm-wpcs-check.yml` (not read but per AGENTS.md runs `parallel-lint` PHP 8.2-8.5) | `testVersion 8.2-` tells `PHPCompatibility` to flag usage incompatible with 8.2+. It does *not* verify compatibility with older WP/PHP — and with `Requires PHP: 8.2` the plugin intentionally drops older PHP. That's correct per declaration, but `psalm-wpcs-check.yml` running `parallel-lint` on 8.2-8.5 without also linting on `wordpress:6.2` + `php:8.2` runtime may miss that `wp_load_classic_theme_block_styles_on_demand` etc. are gated. | CI green does not guarantee WP 6.2 runtime; but manual `function_exists` gates cover it. | Keep `testVersion 8.2-` and add a CI job `wp-env` against `WP 6.2` + `PHP 8.2` (minimum matrix) to smoke-test that all 25 REST routes + cache still boot without the 6.9/7.1 APIs. | Medium |
| C-09 | **LOW** | `Util::transient_key()` `try/catch \Throwable` around `is_multisite()` + `get_current_blog_id()` is exceptionally defensive — but `get_current_blog_id()` itself can return `0` early in `wp-settings.php` before multisite is bootstrapped, causing transient key `0_wppo_...` at boot | `includes/class-util.php:513-515` `is_multisite() ? (string)get_current_blog_id().'_' : $key` inside `try` | Drop-in `templates/object-cache.php:69` runs before plugins bootstrap; `Util::transient_key` is never called that early. However `class-activate.php:70` and `class-main.php:834-841` call `transient_key` on `admin_init` / `init` where `get_current_blog_id()` is valid. Early-boot transient access via `Main::maybe_fix_wp_cache` on `admin_init` is safe. The `0_` prefix would only affect a transient written before `ms_settings.php` — not a real path. | Transient collision if a plugin called `Util::transient_key` during `muplugins_loaded` before blog ID is set — isolated to that early window. | Consider asserting `get_current_blog_id() > 0` before prefixing, falling back to unprefixed key if `0`. Or document that `transient_key` assumes `after_setup_theme`. Current `try/catch` already falls back to raw key on `Throwable`, but `0` is not a `Throwable`. | Medium |
| C-10 | **INFO** | `Util::cached_home_url()` / `cached_content_url()` per-blog static caches are correctly gated on `has_filter('home_url')` / `has_filter('content_url')` and `get_current_blog_id()` — avoids stale cache under `switch_to_blog()` | `includes/class-util.php:37` `private static array $home_url_cache`, `:450-496` `cached_content_url()`, `:480-496` `cached_home_url()` | Both bypass cache when a `home_url` / `content_url` filter is present (context-dependent output), otherwise cache per `blog_id`. The per-request static survives `switch_to_blog` because `get_current_blog_id()` is re-read on each call (see `uninstall.php:159-161` loop). `reset_cached_home_urls()` exists for tests. Excellent. | No compatibility issue; this is the reference implementation for multisite safety cited in `AGENTS.md`. | No change; keep as pattern for any future URL helpers. Optionally add `@codeCoverageIgnore` for filter-present branch. | High |
| C-11 | **LOW** | wordpress.org compliance: `build/` committed but `.distignore` anchors may silently drop future `build/tab-*.js` chunks if Webpack hashing changes | `.distignore:2-7` `/.git/` `/.github/` etc. never mention `build`, `build/index.asset.php`, `package.json:7` `wp-scripts build src/index.js src/lazyload.js src/main.js src/rum.js` | `.distignore` does *not* list `/build` — so `build/` is shipped (correct per `Build` section: “Build output is committed”). The file also excludes `vendor/squizlabs` etc. separately. Verify: `build/` appears in repo and not in `.distignore`. `scripts/build-release.sh` uses `.distignore` patterns to create zip; since `build/` is absent from `.distignore` it is included. | If a future `.distignore` adds `/build` by mistake, release would ship without JS and plugin would white-screen. Current state is correct; risk is accidental addition. | Add a comment line `# KEEP build/ — plugin ships committed build output` above `.distignore:42` `# Keep build/`, and add a CI check `test -f build/index.js` in `webpack.yml` (already does `npm run build`). | High |
| C-12 | **LOW** | `composer.json` `autoload.classmap includes/` will generate a classmap for all `includes/*.php` — but the plugin is documented as “manually loaded via `Main::includes()` + `vendor/autoload.php` (Composer for vendor packages only, no PSR-4 autoload for plugin classes)” — classmap is redundant and may autoload classes before `ABSPATH` guard runs | `composer.json:19-23` `autoload.classmap includes/`, `performance-optimisation.php:41` `require_once vendor/autoload.php`, `includes/class-main.php:388-408` `includes()` | Classmap means `new Main()` at `performance-optimisation.php:44` *would* autoload `PerformanceOptimise\Inc\Main` even without `Main::includes()` requiring the file — but `Main::includes()` still re-requires `class-server-rules.php` etc. The `if (!class_exists('PerformanceOptimise\\Inc\\*'))` guard at top of every file prevents double-declare. No functional break; but `composer dump-autoload --optimize` on 8.2 will pre-parse all 25 files — if any file uses syntax unsupported on the *build* PHP it could fatal at autoload generation (unlikely with `platform 8.2`). | Extra autoload map increases `vendor/composer/autoload_classmap.php` size (~25 entries) for no benefit; may mask a missing `require_once` during refactors. | Either remove the `classmap` entry and rely solely on manual requires (as `AGENTS.md` documents), or keep it but remove the manual `require_once` for files that are already classmapped. Document the chosen strategy in `AGENTS.md` `Architecture`. | Medium |
| C-13 | **INFO** | Hosting: `class-htaccess-handler.php:53-62` correctly uses `insert_with_markers` with `wppo_rules` only — LiteSpeed ordering invariant preserved (LSCACHE block stays above) | `includes/class-htaccess-handler.php:36-44` comment, `53-84` | The comment explicitly states “`# BEGIN LSCACHE must stay above # BEGIN WordPress and # BEGIN wppo_rules`” and the method never calls `insert_with_markers` with `wordpress`. On OLS a restart is required — correctly logged via `LiteSpeed_Integration::is_litespeed()` notice in `class-main.php:1011-1013`. Nginx rules are not auto-written; `Server_Rules::get_nginx_rules()` returns a snippet for manual copy — correct. | No compatibility regression; this is the correct LiteSpeed coexistence pattern per `docs/litespeed-*.md`. | Keep; optionally add integration test that writes a fake `.htaccess` with `LSCACHE` marker and asserts order is preserved. | High |
| C-14 | **LOW** | Caching-plugin detection only covers `advanced-cache.php` / `object-cache.php` drop-ins, not runtime object-cache collision via `wp_cache_set` — multisite with Redis `WP_Object_Cache` from another plugin (e.g. “Redis Object Cache” by Till Krüss) will be overwritten by `Object_Cache::enable()` if user enables WPPO Redis while foreign drop-in exists — but `get_status:107-110` `foreign_dropin` check blocks `enable` with `WP_Error foreign_dropin` | `includes/class-object-cache.php:257-260`, `includes/class-advanced-cache-handler.php:83-110` | `enable()` correctly returns `foreign_dropin` error instead of overwriting — safe. `uninstall.php:80-86` and `deactivate.php:50-56` guard deletion behind marker check. Runtime coexistence with full-page cache plugins (WP Rocket, LSCWP) is handled via `Admin_Notices` warning and `LiteSpeed_Integration::should_disable_wppo_optimizer()` gate that respects `litespeed_can_optm` filter — good. Missing: a warning for `WP Super Cache` / `W3TC` style `CACHE` constants beyond `WP_CACHE`. | No overwrite happens; but the admin notice for competing full-page cache may not cover `WP_CACHE` false-negative where host sets `WP_CACHE` true but serves via Varnish — not a plugin bug. | Keep gates; consider expanding `Admin_Notices` competing-cache detection to also check `defined('W3TC_DIR')` / `WP_CACHE` constant + known constants. | Medium |
| C-15 | **LOW** | `uninstall.php:142-165` multisite iteration uses `get_sites(['number'=>100,'offset'=>...])` + `switch_to_blog` — correct but `WP_CONTENT_DIR . '/cache/wppo'` and `WP_CONTENT_DIR . '/wppo'` directory deletions are not multisite-isolated by domain, so uninstall on one site deletes cache for all sites (acceptable because uninstall means plugin is removed network-wide) | `uninstall.php:57-63` `$cache_dir = WP_CONTENT_DIR.'/cache/wppo/'`, `62` `$wppo_dir = WP_CONTENT_DIR.'/wppo/'`, `138-165` loop | Domain-based static cache (`{domain}/{path}/index.html`) is shared; `wppo/` converted images are shared. Deleting them once per site would be wasteful; current code deletes them `wppo_cleanup_site()` on every blog iteration — idempotent but does repeated `scandir` + `unlink`. On 1000-site network that's 1000× directory walks. | High-site networks experience slow uninstall (timeouts). Data loss is not an issue because uninstall is intentional wipe. | Guard directory deletion to run only once (first site) or move it outside `switch_to_blog` loop. Alternatively keep as-is and document that repeated deletes are safe/idempotent. | Medium |
| C-16 | **INFO** | Themes/plugins: `is_cart` / `is_checkout` / `is_account_page` guards in `Cache::is_not_cacheable:1472-1479`, plus `DONOTCACHEPAGE` constant honored in `Cache::is_not_cacheable:1450-1453` and `LiteSpeed_Integration::is_request_cacheable` — covers WooCommerce correctly; `Yoast` / `Rank Math` (SEO) have no conflict; Elementor/Divi no special handling needed because minify excludes `jquery` only | `includes/class-cache.php:1441-1491`, `includes/class-main.php:46-48` `exclude_js jquery`, `includes/class-litespeed-integration.php:769` query-param exclude `s|ver|v` | WooCommerce cart/checkout/account bypass is both runtime (`is_cart()` etc.) and cookie-based (`woocommerce_items_in_cart` / `woocommerce_cart_hash` in `class-cache.php:1485-1486` and drop-in `:169-171`). `DONOTCACHEPAGE` covers any plugin (WooCommerce, EDD, etc.) that sets the constant. Minify defer delay excludes `wppo-lazyload` via `exclude_defer_js` default — correct. | No theme/plugin incompatibility observed; page-builders supported as declared in `readme.txt:259-260`. | Consider adding WCs `is_wc_endpoint_url` check for custom endpoints (orders/downloads) if reports arise. | High |

---

## 3. No-Issues — confirmed compliant (explicit)

1. **`Util::transient_key` isolation** `includes/class-util.php:509-518` — all 24 plugin-owned transient keys are routed through `Util::transient_key()` (see grep: `Telemetry`, `Pagespeed`, `Cron` locks, `RUM`, `LiteSpeed_Integration`). Single missed raw call is absent. Confidence: High.

2. **`blog_prefix` isolation** `templates/object-cache.php:54,69,211-218,536-537` — `blog_prefix = (is_multisite()?get_current_blog_id():$table_prefix).':'` and `get_key()` prefixes non-global groups. `flush()` SCAN uses `blog_prefix.*`; `flush_group()` scans `get_key('',group).'*'`. `WP 6.9+` `add_salt()` appends salt correctly `:60-62,208-218`. Confidence: High.

3. **Domain-based static cache** `includes/class-cache.php:34-40` `CACHE_DIR /cache/wppo`, `48 domain` + `238-246 url_path`, `484-493 is_not_cacheable` — domain sanitized (`preg_match ^[a-z0-9.-]`, `..`/`/` checks, punycode `idn_to_ascii`), path sanitized (`..` rejected). `Advanced_Cache_Handler::create:182 $file_path = WP_CONTENT_DIR.'/cache/wppo/'.$site_domain.$request_uri.'/index.html'` matches. Inherently multisite-safe. Confidence: High.

4. **`wp_get_loading_optimization_attributes` back-compat** `includes/class-image-optimisation.php:1296,1322` and `includes/class-util.php:589-592` — every `wp_get_loading_optimization_attributes` call is `function_exists`-gated; Tag Processor vs regex fallback both supply `fetchpriority`/`decoding`/`loading` defaults (`low`/`high`/`sync`/`async`). `is_auto_sizes_available()` `class-util.php:589` gates `wp_sizes_attribute_includes_valid_auto` / `wp_img_tag_add_auto_sizes`. Not called on WP 6.2. Confidence: High.

5. **Version-gated APIs exhaustive:** `wp_image_quality` / `wp_get_image_encode_quality` / `wp_get_image_editor_output_format` / `wp_is_client_side_media_processing_enabled` / `client_side_supported_mime_types` / `wp_should_load_separate_core_block_assets` / `wp_load_classic_theme_block_styles_on_demand` / `wp_maybe_inline_styles` / `wp_should_output_buffer_template_for_enhancement` all have matching `function_exists` gates at every call-site (see `class-main.php:187,259,436-443,585`, `class-cache.php:337,733,939`, `class-img-converter.php:139,193,230,1193`, `class-image-optimisation.php:178,189,1296`). Confidence: High.

6. **`min_cache_dir` blog-isolation** `includes/class-util.php:393-416` — `min_cache_base_dir = WP_CONTENT_DIR.'/cache/wppo/min'`, `min_cache_dir = base/'.get_current_blog_id()`, `min_cache_url = cached_content_url('cache/wppo/min/'.get_current_blog_id())`. Thumbnail minify per-site prevents cross-site `content_url()` poisoning. Confidence: High.

7. **`phpcs.xml` / `composer.json` PHP 8.2 alignment** — `phpcs.xml:13` `testVersion 8.2-`, `composer.json:13/55` `>=8.2` + `platform 8.2` match `performance-optimisation.php:6` `Requires PHP: 8.2`. `vendor/`/`node_modules/`/`build/` excluded. Confidence: High.

8. **No obfuscated code** — `grep` for `eval|base64_decode|gzinflate|str_rot13|create_function` shows only `base64_encode` in `class-img-converter.php:839` for LQIP `data:image/jpeg;base64,` (documented intentional, not obfuscation). `build/*.js` is Webpack minified but source `src/` is committed; `.distignore` does not ship `tests/`. Confidence: High.

9. **`.distignore` correctness** — `/.git/` `/.github/` `/.agents/` `/node_modules/` anchored; `vendor/squizlabs` etc. excluded; `composer.json`/`package.json` stripped; `/src/` `/.nvmrc` `phpcs.xml` `tests/` excluded; `build/` *kept* (not listed). Anchored paths prevent `vendor` substring false matches. Confidence: High.

10. **Build committed** — `build/index.js` (136665 bytes), `build/lazyload.js`, `build/main.js`, `build/rum.js`, `build/tab-*.js` (7 chunks), `build/style-index.css`/`-rtl.css` present and fresh (`Aug 27 17:45`). `build/*.asset.php` `version => c918…` matches `@wordpress/scripts` output. Confidence: High.

11. **Object-cache implementation compatibility** — `redis-connect-helper.php` supports `standalone`/`sentinel`/`cluster`, TLS `tls://` prefix, `persistent`, `compression` (`zstd/lz4/lzf` with `defined()` guard), `database` select, `wppo_parse_nodes` IPv6 `[host]:port`. Drop-in falls back to `WP_CONTENT_DIR.'/plugins/performance-optimisation/...'` when `WP_PLUGIN_DIR` not yet defined (WP 6.x early boot) at `templates/object-cache.php:129-130` — correct. Confidence: High.

12. **Hosting environments** — `Server_Rules::get_server_type` detects `litespeed`/`openlitespeed`/`apache`/`nginx`/`other`; `Htaccess_Handler` uses `mod_deflate` + `mod_expires` wrapped in `<IfModule>`; Nginx snippet in `class-server-rules.php:73-173` uses `gzip` + `expires 365d`. OLS restart note logged. Confidence: High.

---

## 4. Open Questions (need owner / follow-up decision)

| # | Question | Owner | Why it matters |
|---|----------|-------|----------------|
| Q-01 | Should `readme.txt` add an `External services` subsection describing `pagespeedonline.googleapis.com` (sent: `url`+`strategy` when admin has an API key) and `fonts.googleapis.com` (when `hostGoogleFontsLocally` downloads CSS) to satisfy WP.org guideline #9? | Product | Blocks directory review if missed. |
| Q-02 | Keep `Tested up to: 7.1` (future) or downgrade to latest released WP (e.g. `6.9`) until CI proves 7.1 nightly? | Release | Validator warning vs forward-looking claim. |
| Q-03 | Align `Cache` vs `Advanced_Cache_Handler` domain handling for ported hosts (`:8080`)? Strip port in both or keep in both. | Infra | Affects cache hit rate on non-standard ports. |
| Q-04 | Should `uninstall.php:93` adopt `Util::transient_key()` instead of inline prefix to pick up `try/catch` hardening, or is inline deliberate for no-autoloader uninstall? | Infra | Single-source maintenance. |
| Q-05 | Keep `composer.json` `autoload.classmap includes/` or drop it and rely solely on manual `Main::includes()` per `AGENTS.md` architecture note? | Architecture | Redundant classmap bloats `autoload_classmap.php`. |
| Q-06 | Should `class-database-cleanup.php` `auto_clean` intentionally exclude `unattached_media`/`oembed_cache` (vs `clean_all` includes them) — document intent or include? | Product | Consistency of scheduled cleanup. |

---

**Verdict:** No CRITICAL compatibility regression. One HIGH (external-services disclosure), remainder MEDIUM/LOW/INFO. Multisite isolation (`transient_key`, `blog_prefix`, domain-based cache, `min_cache_dir` per-blog) is exemplarily implemented; WP version gates for 6.3/6.7/6.9/7.0/7.1 APIs are exhaustive with `function_exists`; object-cache drop-in, hosting (Apache/Nginx/LiteSpeed/OLS), and portability checks are solid. The plugin is correctly declared as PHP 8.2 / WP 6.2 minimum with committed `build/` and no obfuscated code; remaining work is docs/compliance polish.

*Do not modify production code per audit instructions — apply recommendations via follow-up PR after audit sign-off.*
