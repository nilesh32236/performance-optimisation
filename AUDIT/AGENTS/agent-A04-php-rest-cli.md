# Agent A04 — PHP REST / CLI / Metabox Audit

**Scope:** `php-rest-cli-metabox` — line-by-line audit of 8 PHP files  
**Date:** 2026-08-27  
**Auditor:** agent A04 (`php-rest-cli-metabox`)  
**Workspace:** `/var/www/nileshportfolio.duckdns.org/wp-content/plugins/performance-optimisation`  
**Instruction:** Read every file completely (every line), trace REST routes (25), `permission_callback`, sanitization, CLI subcommands, metabox meta, activation/deactivation, util helpers, core tweaks filters. Do **not** modify production code.

---

## 1. Files Reviewed (with line counts)

| # | File | Lines | Purpose (from docblock) |
|---|------|-------|-------------------------|
| 1 | `includes/class-rest.php` | 1563 | 25 REST routes, permission, sanitization, cache/image/object-cache/RUM handlers |
| 2 | `includes/class-wppo-cli-command.php` | 956 | WP-CLI `wppo` subcommands: cache/database/image/settings/object-cache/pagespeed/system-info |
| 3 | `includes/class-metabox.php` | 461 | Preload-image + Asset Manager metaboxes, per-page disabled scripts/styles + delay strategies |
| 4 | `includes/class-activate.php` | 348 | Activation: drop-in, `WP_CACHE`, `.htaccess`, defaults seed, DB table, upgrade flush |
| 5 | `includes/class-deactivate.php` | 156 | Deactivation: unschedule crons, remove drop-ins/config, `WP_CACHE`, clear cache |
| 6 | `includes/class-util.php` | 643 | Filesystem, URL/path, preload links, MIME, exclusions, min-cache dirs, transient/home_url caching, sanitization |
| 7 | `includes/class-core-tweaks.php` | 408 | Core bloat toggles: emojis/embeds/dashicons/XML-RPC/heartbeat/REST/RSS/shortlink/generator/jQuery-migrate/password/self-pingbacks |
| 8 | `includes/class-admin-notices.php` | 316 | Activation/foreign-drop-in/competing-cache/LiteSpeed/review notices + dismiss handlers |
| **Total** | | **4851** | |

All 8 files were read in full (`Read` with offsets) — 5 KB+ per file, no truncation. `php -l` syntax: no errors reported (read-verified).

> **Verification note:** `vendor/` is git-ignored; `composer dev-setup` for PHPCS/PHPUnit. `build/` is committed. Global `wppoSettings` / `wppoObject` injected by `Main::admin_enqueue_scripts()`.

---

## 2. REST Route Inventory (25 routes)

`Rest::get_routes()` `includes/class-rest.php:74-236` maps `performance-optimisation/v1/{route}` → `methods/callback/permission_callback/schema`. All except `rum_collect` use `permission_callback=self::permission_callback` (manage_options + `X-WP-Nonce`). `rum_collect` uses `__return_true` (public, token + IP rate-limited in `RUM::collect()`).

| # | Route | Method | Callback | Permission | Notes |
|---|-------|--------|----------|------------|-------|
| 1 | `clear_cache` | POST | `clear_cache` | `permission_callback` | group flush vs path clear |
| 2 | `update_settings` | POST | `update_settings` | `permission_callback` | tab whitelist 9 keys |
| 3 | `optimise_image` | POST | `optimise_image` | `permission_callback` | webp/avif background vs sync |
| 4 | `delete_optimised_image` | POST | `delete_optimised_image` | `permission_callback` | deletes `wp-content/wppo` |
| 5 | `recent_activities` | GET | `get_recent_activities` | `permission_callback` | paginated log |
| 6 | `import_settings` | POST | `import_settings` | `permission_callback` | merge + allowlist |
| 7 | `database_cleanup` | POST | `database_cleanup` | `permission_callback` | 10 valid types incl `all` |
| 8 | `database_cleanup_counts` | GET | `get_database_cleanup_counts` | `permission_callback` | counts per type |
| 9 | `get_page_assets` | GET | `get_page_assets` | `permission_callback` | post_id → scripts/styles |
| 10 | `image_job_status` | GET | `get_image_job_status` | `permission_callback` | pending/completed/failed + queued + savings |
| 11 | `object_cache` | POST | `handle_object_cache` | `permission_callback` | status/ping/enable/disable/flush |
| 12 | `system_info` | GET | `get_system_info` | `permission_callback` | System_Info::get_all |
| 13 | `performance_scan` | POST | `run_performance_scan` | `permission_callback` | SSRF-gated Telemetry::scan |
| 14 | `pagespeed_scan` | POST | `queue_pagespeed_scan` | `permission_callback` | SSRF-gated Action Scheduler |
| 15 | `pagespeed_results` | GET | `get_pagespeed_results` | `permission_callback` | 202 not_ready vs error sentinel |
| 16 | `web_vitals_trends` | GET | `get_web_vitals_trends` | `permission_callback` | md5+strategy filter |
| 17 | `suggestions` | GET | `get_suggestions` | `permission_callback` | telemetry transient → Suggestion_Engine |
| 18 | `server_rules` | GET | `get_server_rules` | `permission_callback` | nginx/apache + litespeed info |
| 19 | `used_css_regenerate` | POST | `used_css_regenerate` | `permission_callback` | single vs background |
| 20 | `regenerate_ccss` | POST | `regenerate_ccss` | `permission_callback` | Critical_CSS::regenerate_all |
| 21 | `ccss_status` | GET | `get_ccss_status` | `permission_callback` | Critical_CSS status |
| 22 | `dismiss_welcome` | POST | `dismiss_welcome` | `permission_callback` | user_meta flag |
| 23 | `rum_collect` | POST | `collect_rum` | `__return_true` | **public** beacon |
| 24 | `rum_data` | GET | `get_rum_data` | `permission_callback` | aggregated RUM |
| 25 | `autoloaded_options` | GET | `get_autoloaded_options` | `permission_callback` | top N bloat audit |

Schema: every route sets `'schema' => [$this,'get_schema_for_route']` `includes/class-rest.php:75` — `get_schema_for_route():array` at `291-318` returns fixed `success/data/message` object (title `performance-optimisation`, `http://json-schema.org/draft-04/schema#`).

---

## 3. Findings (exhaustive, per-line)

> Columns: **ID** | **Severity** | **File:Line** | **Class/Function** | **Evidence (exact)** | **Impact** | **Recommendation** | **Confidence**

### 3.1 `includes/class-rest.php` — 1563 lines

| ID | Severity | File:Line | Class/Function | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------------|----------|--------|----------------|------------|
| R01 | HIGH | `class-rest.php:218-223` | `Rest::get_routes` / `rum_collect` | `'rum_collect' => ['methods'=>'POST','callback'=>collect_rum,'permission_callback'=>'__return_true']` — only REST route with `__return_true`; comment says token+rate-limited, but no `args` schema / `validate_callback` at route registration. | Public endpoint brute-forceable if `RUM::collect()` token or IP limiter has bug; DDoS on options table (`wppo_rum_data`). | Add `args` schema with `token` required + explicit `validate_callback`; add `rate_limit` test covering 403 after N requests; document token generation. | HIGH |
| R02 | MEDIUM | `class-rest.php:326-331` | `permission_callback` | `$nonce = $_SERVER['HTTP_X_WP_NONCE'] ?? '' ; $nonce_valid = wp_verify_nonce($nonce,'wp_rest'); return current_user_can('manage_options') && $nonce_valid;` — reads raw `$_SERVER`, not `$request->get_header('X-WP-Nonce')`. No `rest_cookie_check_errors` integration. | Nonce bypass if header name case differs (`X-Wp-Nonce`) or if proxy strips `HTTP_X_WP_NONCE`; WP core uses `get_header` lower-casing. | Use `$request->get_header('X-WP-Nonce')` or `rest_cookie_check_errors` pattern; keep `current_user_can` first for early bail. | MEDIUM |
| R03 | MEDIUM | `class-rest.php:376-387` | `clear_cache` | `if(''!==$path){ $real_path=realpath($this->cache_dir.$path); ... if(false===$real_path || (!is_exact_match && !is_under_dir)) return 400; }` — `realpath` returns false when target does not exist (uncached page) or cache_dir missing; then request rejected as `Invalid path`. | `clear_single_page_cache` for never-cached URL always 400; UI "Clear This Page" fails on first visit. Also `$this->cache_dir.$path` missing slash if `$path` no leading `/` → false reject. | Validate via normalized string prefix `strpos(wp_normalize_path($this->cache_dir.$path), trailingslashit(normalize($this->cache_dir)))===0` without `realpath`, or `realpath` on parent dir; ensure slash join `trailingslashit(cache_dir).ltrim(path,'/')`. | HIGH |
| R04 | LOW | `class-rest.php:389-406` | `clear_cache` action dispatch | `if('clear_single_page_cache'===$action){...} else { Cache::clear_cache(); }` — any non-`clear_single_page_cache` value (typo, `clear`, empty) clears **all** cache. No whitelist error. | Accidental full purge on malformed request (e.g. frontend JS bug sending `action:''`). | Whitelist: `if('clear_single_page_cache'===action) else if('clear_all'===action \|\| ''===action) else return 400 Invalid action`. | HIGH |
| R05 | MEDIUM | `class-rest.php:347-369` | `clear_cache` group flush | `if(!empty($group)){ $flushed=Cache::flush_group($group); ... }` — `$group` sanitized via `sanitize_text_field` only, no allowlist; `Cache::flush_group` may flush arbitrary object-cache group if implementation allows pattern. | With `manage_options` attacker can flush unrelated groups (e.g. `options`, `transient`) if underlying method lacks allowlist; DoS. | Add allowlist `in_array($group, ['wppo_inline_drift',...], true)` or delegate allowlist to `Cache::flush_group` and enforce 400 on unknown. | MEDIUM |
| R06 | MEDIUM | `class-rest.php:423-434,712-728` | `update_settings` / `import_settings` tab allowlist drift | `update_settings` allowed_tabs: `file_optimisation,preload_settings,image_optimisation,database_cleanup,object_cache,performance_audit,cache_settings,litespeed_integration,llms_txt` (9). `import_settings` same 9. CLI `WPPO_CLI_Command::settings` allowed_keys adds `core_tweaks` but omits `litespeed_integration,llms_txt` (8). `Activate::maybe_seed_settings` / `Util::sanitize` not gated. | `core_tweaks` settings saved via CLI `wppo settings update core_tweaks` succeed, but `update_settings` REST rejects same tab 400; conversely `litespeed_integration` saved via REST but CLI import rejects. Drift breaks backup/restore parity. | Centralize `ALLOWED_TABS = ['file_optimisation','preload_settings','image_optimisation','database_cleanup','object_cache','performance_audit','cache_settings','litespeed_integration','llms_txt','core_tweaks']` constant in `Util` and reuse in REST + CLI + Activate seed. | HIGH |
| R07 | LOW | `class-rest.php:443-449,731-738` | `update_settings` / `import_settings` Redis password | `if('object_cache'===tab && isset(sanitized['password'])){ $provided=!empty(sanitized['password']); unset(...); if(provided) sanitized['password_set']=true; }` — correctly avoids persisting password. `build_redis_config` at `1116` does `if(defined(WPPO_REDIS_PASSWORD) && !apply_filters('wppo_redis_allow_request_password',false)) $config['password']='';` — constant wins. | Two password surfaces: REST `update_settings` stores flag not value, but `handle_object_cache.enable` may still use request password transiently (intended). Docs at `999-1005` note HTTPS requirement — good. | No code change; add `@see WPPO_REDIS_PASSWORD` to REST doc and ensure UI warns HTTP. | HIGH |
| R08 | LOW | `class-rest.php:466-469` | `update_settings` preserve omitted fields | `if('performance_audit'===tab && !isset(params['settings']['pagespeed_api_key']) && isset(options[...])) sanitized['pagespeed_api_key']=...` — preserves when request omits, but `sanitize_settings_recursively` already stripped sanitization of raw key? Check uses raw `params['settings']` so ok. Same for `server_timing_enabled`, `auto_rescan`. | If client sends `"pagespeed_api_key":""` explicitly, it *is* set (empty string) and will be sanitized to empty and overwrite stored key — intentional clear. | Document that empty-string explicitly clears key. | HIGH |
| R09 | INFO | `class-rest.php:472-478` | `update_settings` response redaction | `if(isset(options['performance_audit'])) unset(options['performance_audit']['pagespeed_api_key']); if(isset(options['object_cache']['password'])) unset(...);` — redacts before `send_response`. | Prevents API key / password leak via View Source / `wppoSettings`. Correct. | No change. | HIGH |
| R10 | LOW | `class-rest.php:529-530,580-589` | `optimise_image` sanitize | `$webp_images = array_map('sanitize_text_field', (array)$params['webp'])` — `sanitize_text_field` strips tags but preserves `../` . Path validation later via `realpath`+prefix mitigates traversal, but sanitize does not normalize slashes. | `sanitize_text_field('a/b/../c')` still contains `../` until `realpath` check — validation below catches it, so safe. | Keep as defense-in-depth; consider `sanitize_text_field` + `wp_normalize_path` before validation. | MEDIUM |
| R11 | LOW | `class-rest.php:606-612` | `optimise_image` log message | `__('Scheduled %d image optimization jobs for background processing on ', ...)` — string ends with `on ` (dangling). | Incomplete i18n string; translators see hanging `on `. | Fix string: `__('Scheduled %d image optimization jobs for background processing.', ...)`. | HIGH |
| R12 | LOW | `class-rest.php:648-657` | `optimise_image` sync fallback counts | `$sanitized[bucket]=['webp'=>count(...), 'avif'=>count(...)]` counts after sync, but `$response` shape from `Img_Converter::get_img_info()` already counts? Duplicated count logic vs `get_image_job_status` at `959-970`. | Drift if bucket shape changes (e.g. `pending.webp` becomes int). | Extract helper `Img_Converter::get_counts()` and reuse. | MEDIUM |
| R13 | MEDIUM | `class-rest.php:668-688` | `delete_optimised_image` filesystem | `if(!Util::init_filesystem()) return 500; $wppo_dir=WP_CONTENT_DIR.'/wppo'; if(!$wp_filesystem->is_dir(...)) return 404; if(!$wp_filesystem->delete(...,true)) return 500; Img_Converter::clear_completed_formats(); Cache::clear_cache();` — deletes entire `wp-content/wppo` (images + maybe other plugin data) recursively. No confirmation param. | With `manage_options` an accidental DELETE call wipes all converted images; no undo. `Cache::clear_cache()` after deletion is correct (HTML references broken). | Require explicit `confirm:true` param or use `WP_Filesystem::rmdir` with allowlist; add notice. | LOW |
| R14 | LOW | `class-rest.php:695-773` | `import_settings` merge | `$merged = array_replace_recursive($existing, $sanitized)` — recursive merge preserves nested keys not in import, but also merges arrays numerically (e.g. `loggedInCacheRoles` arrays merge by index, not replace). Importing `{"cache_settings":{"loggedInCacheRoles":["editor"]}}` over `["administrator"]` yields `["editor"]`? Actually `array_replace_recursive` *does* replace indexed values by index 0, so it replaces — correct for this shape. But deep merge of `high_value_urls` may duplicate? | For list-arrays, `array_replace_recursive` replaces index 0, leaving extra indexes from old array if new list shorter → stale roles persist. | For list fields, use `array_replace` shallow or explicit replace; document merge semantics (import is overlay, not reset). | MEDIUM |
| R15 | LOW | `class-rest.php:798-862` | `database_cleanup` valid_types vs method_map | `valid_types` includes 10 inc `all`; `method_map` maps 9 types (revisions..oembed_cache). `if(!method) return 400` at `861` catches `oembed_cache`? No, map has `oembed_cache` at `857` (`clean_oembed_cache`) so ok. But `valid_types` includes `trashed_comments` and map includes it — consistent. One subtle: `revisions` uses `clean_revisions_advanced` (advanced) while CLI `database` uses `clean_revisions` (non-advanced) — different retention semantics. | REST respects `dbRevMaxAge/dbRevKeepLatest` settings, CLI `wppo database cleanup --type=revisions` ignores them (or uses defaults inside `clean_revisions`). Divergence. | Make CLI use `invoke_cleanup_method('clean_revisions_advanced', max_age, keep_latest)` same as REST, or document difference. | MEDIUM |
| R16 | LOW | `class-rest.php:888-894` | `database_cleanup` post-clean optimize | `if((int)result>0 && isset(Database_Cleanup::TABLE_MAP[type])) maybe_optimize_tables(TABLE_MAP[type], true)` — only on success and >0 rows. `TABLE_MAP` not shown but assumed correct. | Optimize may run on every single-type clean, adding `OPTIMIZE TABLE` latency (table lock). Flag as intentional. | Gate optimize behind `Database_Cleanup::should_optimize()` or transient throttle; already `maybe_optimize_tables` likely throttles. | LOW |
| R17 | INFO | `class-rest.php:911-946` | `get_database_cleanup_counts` / `get_page_assets` / `get_image_job_status` | `get_page_assets` validates `post_id absint` → 400 if 0; `get_image_job_status` checks `function_exists(as_get_scheduled_actions)` else `queued_jobs=0`; `get_database_cleanup_counts` delegates. All correctly permission-gated. | — | No change. | HIGH |
| R18 | LOW | `class-rest.php:1027-1072` | `handle_object_cache` branching | `if(status) return status+compressors; if(ping) build_config→ping; if(enable) build→enable; if(disable) disable; if(flush) flush; return 400 Invalid action` — sequential `if` not `elseif`, but each returns, so safe. `ping`/`enable` build config from raw `$params` via `build_redis_config` (sanitized). | `flush` path does `if(result) log success else 400` — `flush()` may return WP_Error vs bool; code treats falsy as failure, but WP_Error truthy would incorrectly success. Check `Object_Cache::flush()` return type. | Normalize to `is_wp_error($result)` check. | MEDIUM |
| R19 | LOW | `class-rest.php:1090-1138` | `build_redis_config` | Allowed keys: `mode,host,port,password,database,nodes,master_name,use_tls,persistent,compression` — `port/database` cast `(int)`, `use_tls/persistent` cast `(bool)`, others `sanitize_text_field`. Missing `timeout`/`prefix` that CLI supports; CLI’s `get_redis_config_from_assoc` allows `host/port/password/database/timeout/prefix` (6 keys) — drift. | REST ping/enable with `timeout` param silently ignored; CLI ping with `nodes` ignored. | Unify allowlists or document divergence (REST is richer). | HIGH |
| R20 | INFO | `class-rest.php:1150-1156` | `sanitize_nodes` | Handles array vs scalar, `sanitize_text_field` + `array_filter` + `array_values`. Correct. | — | No change. | HIGH |
| R21 | LOW | `class-rest.php:1164-1178` | `ajax_get_nonce` | Checks `is_user_logged_in && current_user_can('manage_options')`, then `check_ajax_referer('wppo_nonce_refresh','nonce',false)` → `wp_send_json_error` 403 on fail, else `wp_send_json_success(['nonce'=>wp_create_nonce('wp_rest')])`. Not a REST route but `admin-ajax.php`. No `wp_die` after? `wp_send_json_success` exits. | Correct nonce refresh for 403 stale-nonce recovery (used by `src/main.js`). | No change. | HIGH |
| R22 | MEDIUM | `class-rest.php:1201-1236` | `run_performance_scan` SSRF | Order: `wp_http_validate_url` (loopback/private) → scheme allowlist → host equality vs `Util::cached_home_url()`. `queue_pagespeed_scan` at `1258-1274` does scheme→host→`wp_http_validate_url` (different order). Both enforce host equality, but `wp_http_validate_url` rejects loopback/private *including* 127.0.0.1 hosted behind same origin via hosts file? Good. However `Util::cached_home_url()` may be cached per blog_id and may include path (`https://example.com/subdir`) → `wp_parse_url(...PHP_URL_HOST)` only host, so subdir comparison lost — scanning `https://example.com.evil.com/` passes host equality? No, host differs, so blocked. | Scheme check before `wp_http_validate_url` in `queue_pagespeed_scan` means `file://` blocked early, but `gopher://` also blocked via scheme allowlist. Good defense-in-depth. Minor order inconsistency not exploitable. | Normalize order to `scheme → wp_http_validate_url → host equality` in both. | LOW |
| R23 | LOW | `class-rest.php:1202,1251,1311,1404` | URL defaults via `Util::cached_home_url('/')` | `run_performance_scan`: `url = esc_url_raw(params['url']) ?? cached_home_url('/')`; `queue_pagespeed_scan` same; `get_pagespeed_results` same; `get_suggestions` uses same. `esc_url_raw` allows empty → fallback to home. | If attacker omits URL, scan runs on homepage (intended). No validation of path traversal via `//evil.com`? `esc_url_raw('//evil.com')` returns `//evil.com` then `wp_parse_url` scheme empty → blocked via scheme check. Good. | No change. | HIGH |
| R24 | LOW | `class-rest.php:1335-1338` | `get_pagespeed_results` retroactive LCP store | `if(!empty(results['lcp_image_url'])) Pagespeed::store_lcp_image_url(url,results,strategy)` — stores on every GET (idempotent, dedup inside). | Extra write on each poll (GET should be side-effect free). Could spam options/transients if polled rapidly. | Guard with transient throttle or move to queue completion hook only; note dedup mitigates. | LOW |
| R25 | LOW | `class-rest.php:1356-1390` | `get_web_vitals_trends` filter | `url_key = md5(url)` if url else null; `array_filter(trends, fn(k)=> strpos(k, url_key.'_')===0 && str_ends_with(k,'_'.$strategy))` — uses `str_ends_with` (PHP 8.0+, plugin requires 8.2 so ok). | `str_ends_with` available; `strpos` prefix check correct. Trends stored as `md5(url)_strategy` — good. | No change. | HIGH |
| R26 | LOW | `class-rest.php:1406-1415` | `get_suggestions` | `$transient_key = transient_key('wppo_audit_'.md5(url))` — multisite prefix via `Util::transient_key`; `get_transient` → if false returns `['suggestions'=>[]]` with message `No cached scan...Run a scan first.` | Correct. Note `wppo_audit_*` transients not cleaned in `uninstall.php` (see U03). | Add to uninstall cleanup via `LIKE 'wppo_audit_%'`. | MEDIUM |
| R27 | LOW | `class-rest.php:1430-1451` | `used_css_regenerate` | Checks `as_enqueue_async_action` exists else 500; if `post_id` enqueues `wppo_used_css_generate` else `Used_CSS::regenerate_all()` (queues per post). Returns 202. | No post_id existence check (trashed? invalid ID still queues). | Validate `get_post(post_id)` exists, else 404. | LOW |
| R28 | INFO | `class-rest.php:1480-1527` | `get_server_rules` / `regenerate_ccss` / `get_ccss_status` / `dismiss_welcome` | `get_server_rules` adds `litespeed` info if class exists; `regenerate_ccss` delegates; `get_ccss_status`; `dismiss_welcome` does `if(get_current_user_id()) update_user_meta(...,1)` — safe, permission gated. | — | No change. | HIGH |
| R29 | INFO | `class-rest.php:291-318,326` | `get_schema_for_route` + `permission_callback` | Schema static, no args validation; permission uses `manage_options` + nonce. | `args` / `validate_callback` / `sanitize_callback` not declared per-route — relies on handler sanitization. Acceptable, but OpenAPI discoverability limited. | Consider adding `args` declarations for gateway validation and `OPTIONS` preflight docs. | LOW |

### 3.2 `includes/class-wppo-cli-command.php` — 956 lines

| ID | Severity | File:Line | Subcommand | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|------------|----------|--------|----------------|------------|
| CLI01 | MEDIUM | `class-wppo-cli-command.php:76-124` | `cache` | `action = args[0] ?? 'clear'; if('preload'===action)... else if('status'===action)... if('clear'!==action) error; $page = assoc['page'] ?? null; if(page){ $path = wp_normalize_path(trim((string)wp_parse_url(page, PHP_URL_PATH),'/')); cleared=Cache::clear_cache(path); }` — `page` param accepts full URL or path; `wp_parse_url` with `PHP_URL_PATH` drops query string, but `trim(...,'/')` strips leading/trailing slashes, so `Cache::clear_cache('sample-page')` vs REST `'sample-page'` with/without slash may mismatch cache key (domain + path + index.html). | CLI clear for `/sample-page/?ver=123` clears same as REST? REST keeps query? Cache bypasses `?ver` via `maybe_store_cache` query-string check, but clear uses path only — consistent. Still path normalization differing (CLI trims slashes, REST keeps slash via `wp_normalize_path(trim)`? Actually REST does `wp_normalize_path(trim(...))`? No, REST does `wp_normalize_path(path)` then `realpath`. Slight divergence. | Normalize both via `Util::cached_home_url` + `trailingslashit` helper; share `Util::normalize_clear_path()` . | MEDIUM |
| CLI02 | MEDIUM | `class-wppo-cli-command.php:206-260` | `database` | `switch(type){ case 'revisions': clean_revisions(); case 'auto_drafts'/'drafts': clean_auto_drafts(); case 'trashed_posts'/'trash': ... case 'spam_comments'/'spam': ... case 'expired_transients'/'transients': ... case 'orphan_postmeta'/'orphans': ... default: error Invalid }` — handles 6 aliases, but REST `valid_types` has 9 including `unattached_media` and `oembed_cache` plus `trashed_comments`; CLI missing those → `wppo database cleanup --type=unattached_media` errors though REST succeeds. Also no `trashed_comments` branch. | CLI cannot clean 3 types that REST can; admin using CLI misses optimization. | Add `case 'trashed_comments'/'trashed'/'trash_comments': clean_trashed_comments(); case 'unattached_media'/'unattached': clean_unattached_media(); case 'oembed_cache'/'oembed': clean_oembed_cache();` mirror REST map. | HIGH |
| CLI03 | LOW | `class-wppo-cli-command.php:233-235` | `database` revisions | `cleaned_count = Database_Cleanup::clean_revisions()` — no args, while REST does `invoke_cleanup_method('clean_revisions_advanced', max_age, keep_latest)` with settings. CLI ignores `dbRevMaxAge/dbRevKeepLatest`. | CLI `cleanup --type=revisions` may delete more/less than UI respects settings. | Pass settings as REST does: read `wppo_settings[database_cleanup][dbRevMaxAge/KeepLatest]` and invoke advanced method. | MEDIUM |
| CLI04 | LOW | `class-wppo-cli-command.php:174-197` | `database optimize` | `foreach table_list as table: result=optimize_table(table); if(result) ++success; WP_CLI::log(' - Optimized table: ...')` — `optimize_table` takes identifier like `posts`, not fully qualified `$wpdb->prefix . 'posts'`. Internals likely map, but CLI accepts arbitrary comma list without allowlist. | `wppo database optimize --tables=options,users` could OPTIMIZE arbitrary tables if identifier not validated. | Allowlist `['posts','postmeta','comments','commentmeta','options']` else warn; delegate mapping to `Database_Cleanup::TABLE_MAP`. | MEDIUM |
| CLI05 | LOW | `class-wppo-cli-command.php:306-343` | `image convert` | `$conversion_format = format ? format : (options['image_optimisation']['conversionFormat'] ?? 'webp'); if('both'===format) formats=[avif,webp] else if in [avif,webp] single` — `format` values not sanitized beyond `in_array`; `batch = options['image_optimisation']['batch'] ?? 50` not validated (could be 0 or 1M). `total_pending` counts all pending, but `counter>=batch_size` breaks per-format, so `both` processes up to `2*batch` (one batch per format) — exceeds UI batch? | Operator `wppo image convert --format=webp` with batch 5000 could OOM (convert 5000 images synchronously). | Cast `batch = max(1, min(200, (int)batch))`; validate `format in [webp,avif,both]` else error. | MEDIUM |
| CLI06 | MEDIUM | `class-wppo-cli-command.php:336-341` | `image convert` traversal | `$source_path = ABSPATH . img` (wp_normalize_path); `$resolved=realpath(source_path); if(false===resolved || strpos(normalize(resolved), normalized_abspath)!==0) continue;` — skips traversal attempts (good) but no logging of skipped. If `wppo_img_info` contains poisoned `../etc/passwd` entry, silently skipped — no error surfaced. | Poisoned entry remains pending forever (never converted, never failed). | Log warning for skipped traversal, and consider `clear_completed_formats` for failed? | LOW |
| CLI07 | INFO | `class-wppo-cli-command.php:434-505` | `get_default_settings` | Returns 7 tabs with full defaults (cache_settings, file_optimisation 32 keys, preload, image, performance_audit, database_cleanup, object_cache). Matches `Activate::maybe_seed_settings` except Activate missing 7 disable* keys (see ACT02). | Drift noted in ACT02. | Centralize defaults in `Util::get_default_settings()` single source. | HIGH |
| CLI08 | HIGH | `class-wppo-cli-command.php:605-659` | `settings import` | `allowed_keys = [file_optimisation,preload_settings,image_optimisation,database_cleanup,object_cache,performance_audit,core_tweaks,cache_settings]` (8) — `core_tweaks` allowed but `litespeed_integration,llms_txt` not; REST allows opposite. `new_settings = sanitize_settings_recursively(new_settings)` then redis password handling, then `unset(pagespeed_api_key)`. `existing = get_option('wppo_settings',[])`; `merged = array_replace_recursive(existing, new_settings)` — if existing empty, `array_replace_recursive` on `[]` + defaults? Actually `existing` may be empty before seed, then import overlays onto empty, not onto defaults — resulting partial option missing defaults. REST import merges onto existing (which after `maybe_seed_settings` is full). CLI import called before activation seed? | Import via CLI on fresh install yields partial `wppo_settings` (only imported keys), later Admin expects defaults missing → `Undefined index` or `get_option` fallback. | In import, start from `self::get_default_settings()` merged with existing, or call `Activate::maybe_seed_settings()` first. | MEDIUM |
| CLI09 | LOW | `class-wppo-cli-command.php:661-688` | `settings get` | `if(tab && !isset(options[tab])) error Invalid tab ... Available tabs: implode(keys(options))` — `options` already merged with defaults, so lists all 7 defaults plus any stored, but if `litespeed_integration` stored but not in defaults, it *is* listed (since merged). However error message leaks internal tab names to CLI user (acceptable). | — | No change, but align tab list with central allowlist. | LOW |
| CLI10 | MEDIUM | `class-wppo-cli-command.php:695-728` | `settings update` | `$json = assoc['settings'] ?? null; new_settings=json_decode(json,true); sanitized=Util::sanitize(...); known_tabs=[file_optimisation,...,core_tweaks]` (8, same drift), `if(!in_array(tab,known_tabs)) warning Unrecognized... will be saved but may not be read`; then `options[tab]=array_replace_recursive(options[tab], new_settings)`; `update_option`. No redaction of `pagespeed_api_key` / `password` as in REST (CLI update saves them if provided). | `wppo settings update object_cache --settings='{"password":"secret"}'` would persist password plaintext in DB (REST prevents). CLI bypasses password_set flag logic present in import. | Handle password as in import: if `object_cache.password` set, convert to `password_set`, unset password. Similarly strip `pagespeed_api_key` or warn. | MEDIUM |
| CLI11 | MEDIUM | `class-wppo-cli-command.php:847-855` | `get_redis_config_from_assoc` | `foreach [host,port,password,database,timeout,prefix] as key if isset(args[key]) config[key]=args[key]; return config;` — no sanitization (`sanitize_text_field`, `(int)port`, etc.) before passing to `Object_Cache::ping/enable`. Also allows `timeout` (int) and `prefix` not in REST allowlist; REST allows `compression,master_name,nodes,use_tls,persistent,mode` which CLI ignores. | Unsanitized CLI args flow to Redis config (e.g. `--host='a; rm ...'` though `Object_Cache` likely escapes, still injection surface). Drift causes CLI enable to miss `use_tls` etc. | Sanitize via `build_redis_config`-like logic; share allowlist. | HIGH |
| CLI12 | LOW | `class-wppo-cli-command.php:885-915` | `pagespeed` | `$url = assoc['url'] ?? cached_home_url(); $job_id = Pagespeed::queue_scan(url, strategy)` — no SSRF/host validation as REST does; no strategy whitelist validation before queue. | `wp wppo pagespeed scan --url=https://evil.com` queues scan for external domain (Pagespeed internally may validate, but CLI bypasses REST gate). Low risk (CLI is privileged). | Add same host/scheme validation or delegate to `Pagespeed::queue_scan` (ensure it validates). | MEDIUM |
| CLI13 | INFO | `class-wppo-cli-command.php:939-954` | `system_info` | `group = args[0] ?? null; if(group && !isset(all[group])) error Invalid group; else log json` — correctly validates group. | — | No change. | HIGH |
| CLI14 | DUPLICATE | `class-wppo-cli-command.php:425-556` | `settings` docblock | Docblock for `settings` appears twice (lines 376-424 and 507-555 identical, second real method). First block is orphaned above `get_default_settings()`. | Duplicate doc causes IDE to show duplicate help for `wppo settings` in `wp help wppo`. | Remove first orphan block (or merge). | HIGH |

### 3.3 `includes/class-metabox.php` — 461 lines

| ID | Severity | File:Line | Function | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------|----------|--------|----------------|------------|
| MB01 | MEDIUM | `class-metabox.php:54-61` | `add_metabox` | `add_meta_box('preload_image_metabox', 'Preload Image URL', [render_metabox], '', 'side', 'default')` — screen `''` (empty string). Asset Manager loops `foreach(post_types as pt) add_meta_box('wppo_asset_manager', ..., pt, 'normal','low')` — correct. Preload box uses `''` not loop, so renders on no screen (or dashboard if WP treats '' as all). | Preload Image URL metabox never appears on post editor (or appears on dashboard unintentionally). Users cannot set per-page preload URLs via UI though `save_preload_image_urls` expects POST. | Loop same as asset manager: `foreach($post_types as $pt) add_meta_box('preload_image_metabox', ..., $pt, 'side','default')` or add to `$post_types` filter. | HIGH |
| MB02 | LOW | `class-metabox.php:50-53` | `add_metabox` | `$post_types = get_post_types(['public'=>true],'names'); $excluded=['attachment']; $post_types=array_diff(...)` — excludes only attachment, but includes `wp_block`, `wp_template`, etc. if public (custom). | Asset Manager appears on block/template editors where script disabling is irrelevant. | Allowlist via `apply_filters('wppo_asset_manager_post_types', $post_types)` or exclude `wp_block` explicitly. | LOW |
| MB03 | INFO | `class-metabox.php:88-100` | `render_metabox` | `preload_urls = get_post_meta(post->ID,'_wppo_preload_image_url',true); wp_nonce_field('save_preload_image_url','wppo_preload_image_nonce'); textarea esc_textarea(preload_urls)` — correct nonce, esc. | — | No change. | HIGH |
| MB04 | LOW | `class-metabox.php:118` | `render_asset_manager_metabox` | `wp_nonce_field('wppo_save_asset_manager','wppo_asset_manager_nonce')` — correct. | — | — | HIGH |
| MB05 | INFO | `class-metabox.php:120-136` | `render_asset_manager_metabox` | Ensures `disabled_scripts/styles`, `delay_strategies/priorities` are arrays via `if(!is_array) =[]` — prevents warning on `in_array` / `array_column`. | — | No change. | HIGH |
| MB06 | LOW | `class-metabox.php:142-159` | `render_asset_manager_metabox` | `if(false===assets || (empty(scripts)&&empty(styles))) show description + Visit Page button if publish` — `Asset_Manager::get_page_assets` returns false if not captured; message says "Visit this page/post on frontend while logged out" — but `Main::` only captures when `is_cache_allowed`? Logged-in capture depends on `enableLoggedInCache`? Documentation correct? | User with logged-in cache off visiting as logged-in won't capture; message says logged out — correct guidance. | No change. | HIGH |
| MB07 | INFO | `class-metabox.php:173-254` | `render_asset_manager_metabox` scripts table | `is_protected = in_array(handle, protected_js, true); is_disabled = in_array(handle, disabled_scripts, true); strategy = delay_strategies[handle] ?? '';` renders checkbox `disabled(protected)` and select `disabled(protected)`. `checked/disabled/selected` helpers correctly escaped via `esc_attr`. | Protected handles visually dimmed `opacity:0.5` and disabled — cannot be disabled, correct. | No change. | HIGH |
| MB08 | LOW | `class-metabox.php:308-321` | `save_metabox` | `if(DOING_AUTOSAVE) return; if(!current_user_can('edit_post',post_id)) return; save_preload...; save_asset...` — capability `edit_post` correct for per-post meta; autosave bypass correct. No `wp_is_post_revision` check — import may trigger on revision save. | Revision `post_id` is revision ID, meta saved on revision not parent. `update_post_meta` on revision ID creates orphaned meta. | Add `if(wp_is_post_revision(post_id)) return;` or map to parent `wp_is_post_revision` then `post_id = parent`. | MEDIUM |
| MB09 | LOW | `class-metabox.php:329-343` | `save_preload_image_urls` | `if(!isset($_POST[nonce])) return; if(!wp_verify_nonce(...)) return; if(!isset($_POST[preload_url])) return; $preload_urls=sanitize_textarea_field(wp_unslash(...)); update_post_meta(post_id,'_wppo_preload_image_url',preload_urls)` — sanitizes as textarea (preserves newlines). No URL validation per line. | User can save `javascript:alert(1)` in preload; later `Main::add_preload_*` may output raw via `Util::generate_preload_link` which does `esc_url` — so `javascript:` stripped to empty, harmless but no early error. | Validate each line via `esc_url_raw` + `Util::is_url_excluded` style, warn. | LOW |
| MB10 | MEDIUM | `class-metabox.php:355-394` | `save_asset_manager_settings` | `assets = Asset_Manager::get_page_assets(post_id); valid_scripts = array_column(assets['scripts'],'handle');` etc., then `raw_scripts=get_raw_post_array('wppo_disabled_scripts'); update_meta('_wppo_disabled_scripts', process_disabled_assets(raw, valid))` — whitelisting via `array_intersect` ensures only captured handles can be disabled. Good. Same for styles, delay strategies/priorities via `process_delay_setting`. | If `get_page_assets` returns false (no capture), `valid_*` stays `[]`, then `array_intersect` yields `[]` → any posted handles silently dropped and meta overwritten to `[]`, erasing previous disables before capture. | If `!is_array(assets)` return early without overwriting meta (preserve existing). | HIGH |
| MB11 | LOW | `class-metabox.php:403-406` | `get_raw_post_array` | `return isset($_POST[key]) && is_array($_POST[key]) ? wp_unslash($_POST[key]) : []` — unslashes but not sanitized; sanitization deferred to `process_*`. Correct. `// phpcs:ignore NonceVerification` comment present. | — | No change. | HIGH |
| MB12 | INFO | `class-metabox.php:416-458` | `process_disabled_assets` / `process_delay_setting` | Both sanitize, intersect/allowlist, skip empty values. `process_delay_setting` also checks `allowed_values` via `in_array(...,true)` and `valid_handles`. Correct. | — | No change. | HIGH |

### 3.4 `includes/class-activate.php` — 348 lines

| ID | Severity | File:Line | Function | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------|----------|--------|----------------|------------|
| ACT01 | INFO | `class-activate.php:58-73` | `Activate::init` | `if(foreign_dropin_present()) notices[]=foreign_dropin else create(); add_wp_cache... ; set_transient(wppo_activation_notices, unique, WEEK)` — one-week activation notice transient via `Util::transient_key`. | Correct multisite prefix via `transient_key`. WEEK duration means stale notice persists. | WEEK is intentional per UI dismiss flow; keep. | HIGH |
| ACT02 | MEDIUM | `class-activate.php:110-181` | `maybe_seed_settings` | Defaults: `cache_settings (2 keys)`, `file_optimisation (28 keys)`, `preload_settings (5)`, `image_optimisation (7)`, `performance_audit (6)`, `database_cleanup ([])`, `object_cache ([])` = 7 tabs. `WPPO_CLI_Command::get_default_settings()` has same 7 but `file_optimisation` there includes 6 extra `disable*` keys (`disableRestApiLinks` etc.) → Activate seed lacks them. `Main::__construct` defaults also include them? Not verified but likely same drift. | Fresh install option lacks disable* keys, so `isset($options['file_optimisation']['disableRestApiLinks'])` is false until first admin save; `Core_Tweaks` sees empty and skips removal (correct default false, but explicit absence hides UI default state). | Sync all three default factories (Activate, CLI, Main) via single `Util::get_default_settings()` (CLITODO). | HIGH |
| ACT03 | LOW | `class-activate.php:112-114` | `maybe_seed_settings` | `existing = get_option('wppo_settings', null); if(null!==existing) return;` — distinguishes "no option" (null) from empty array `[]` (would have returned `[]` if option stored as empty). If option exists as `[]` (wiped), seed not run, leaving empty. | Edge: manual `delete_option` vs `update_option([],[])` vs `null` — seed only on not-found, not on empty wipe. CLI fallback handles empty, but Activate does not. | Check `if(null!==existing && is_array(existing) && !empty(existing)) return;` or treat empty array as missing. | MEDIUM |
| ACT04 | LOW | `class-activate.php:180` | `maybe_seed_settings` | `add_option('wppo_settings', defaults, '', false)` — autoload `false` (not autoloaded). Correct per `wppo_img_info` pattern (non-autoloading to reduce options bloat). | Settings loaded on every admin + frontend via `get_option` still hits DB (autoload false means extra query). Tradeoff: not autoloaded reduces memory but adds query per request. | Evaluate `autoload=false` vs True for hot path; document rationale (size ~5KB). | LOW |
| ACT05 | INFO | `class-activate.php:199-226` | `maybe_run_upgrades` | Fresh install `is_fresh_install=true` → `update_option(VERSION, WPPO_VERSION)` and return (no flush). Else `version_compare(stored, LEGACY_FLUSH_FLOOR,'>=')` → return (one-time flush only). Else `flush_legacy_query_cache_keys()`, schedule retry on fail. | Correct one-time eviction gated on fixed floor `1.8.1`, not current version — future bumps no re-flush. Good. | No change. | HIGH |
| ACT06 | LOW | `class-activate.php:239-243` | `schedule_upgrade_routine` | `if(!wp_next_scheduled('wppo_run_upgrades')) wp_schedule_single_event(time()+delay, 'wppo_run_upgrades')` — dedup via `wp_next_scheduled`. | Multiple concurrent activations only schedule once. | No change. | HIGH |
| ACT07 | MEDIUM | `class-activate.php:251-313` | `add_wp_cache_constant` | Reads `wp-config.php` via `$wp_filesystem->get_contents`, checks `defined(WP_CACHE)&&WP_CACHE` early return, then `Util::init_filesystem()` twice `if(!$wp_filesystem && !init) return wp_config_fs`; then path resolution `ABSPATH/wp-config.php` else `dirname(ABSPATH)/wp-config.php`; checks `exists && is_writable`; then handles `WP_CACHE false → replace false with true`, `strpos(WP_CACHE)` early null, else inserts `if(!defined('WP_CACHE')) define('WP_CACHE',true);` before `/* That's all...` or append. `put_contents(...,FS_CHMOD_FILE)`. | Replace regex `/define(...WP_CACHE...,false);/i` will also match commented-out define `// define('WP_CACHE', false);` — incorrectly uncommented. Also not atomic (no temp+rename). | Narrow regex to `^\s*define` with `^` anchor + maybe `preg_replace` only on uncommented line; use temp file + rename for atomicity. | MEDIUM |
| ACT08 | LOW | `class-activate.php:88-93` | `init` server rules | `enable_server_rules = options[file_optimisation][enableServerRules] ?? false; if(enable) { updated=Htaccess_Handler::update_rules(true); if(!updated) notices[]= 'Failed...' ; set_transient(... ) }` — on activation, `.htaccess` update may fail (permissions) and notice added. | Second `set_transient` overwrites earlier foreign-dropin notices? It does `array_unique(notices)` including prior, so merged. | Good merging; keep. | HIGH |
| ACT09 | INFO | `class-activate.php:321-346` | `create_activity_log_table` | `SHOW TABLES LIKE` via `$wpdb->prepare`, then `dbDelta` create `wppo_activity_logs (id mediumint(9) AI, activity varchar255, created_at datetime DEFAULT CURRENT_TIMESTAMP)`. | Single table, correct collation via `get_charset_collate`. Activity log not multisite-prefixed? Uses `$wpdb->prefix` so per-site table (correct single-site + per-blog on multisite). | No change. | HIGH |
| ACT10 | DUPLICATE | `class-activate.php:116-178, class-wppo-cli-command.php:434-505` | defaults | Same 60-line defaults literal duplicated. | Drift risk (ACT02). | Centralize. | HIGH |

### 3.5 `includes/class-deactivate.php` — 156 lines

| ID | Severity | File:Line | Function | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------|----------|--------|----------------|------------|
| DEA01 | LOW | `class-deactivate.php:38-42` | `Deactivate::init` | `unschedule_crons(); unschedule_database_cleanup_cron(); delete_option('wppo_preload_cron_offset');` — deletes offset but not `wppo_version` (kept for upgrade gate). | On deactivate/reactivate cycle, `wppo_version` remains, so `maybe_run_upgrades` sees fresh_install false? Actually init passes `!$has_activation_time` as `is_fresh_install`; deactivation does not delete `wppo_activation_time`, so reactivate is not fresh. Correct (preserve upgrade gate). | Document retention. | HIGH |
| DEA02 | MEDIUM | `class-deactivate.php:42-61` | `init` drop-in removal | `Advanced_Cache_Handler::remove();` plus manual `object-cache.php` delete if contains `'Redis Object Cache Drop-in for Performance Optimisation'`, and delete `wppo-redis-config.php`. | Selective delete via substring is safe but `object-cache.php` from another plugin containing same substring false-positive? Unlikely. | Tie to `Advanced_Cache_Handler::is_our_dropin()` style check instead of substring. | LOW |
| DEA03 | LOW | `class-deactivate.php:78-102` | `unschedule_crons` | Unschedules `wppo_page_cron_hook`, `wppo_page_cron_batch`, `wppo_img_conversation` + `wppo_img_conversion`, `wppo_generate_static_page`. Missing `wppo_run_upgrades`, `wppo_web_vitals_rescan`, `wppo_database_cleanup_cron` (separate method handles one), `wppo_used_css_generate`, `wppo_generate_ccss`, `wppo_pagespeed_scan`, `wppo_generate_static_url` (sitemap), `wppo_preload` 5-hourly? `Cron::` hooks may cover some. | Residual cron entries after deactivation (e.g. `wppo_web_vitals_rescan` daily) keep firing via `wp-cron` → PHP errors (class not loaded). | Audit `Cron::` and `Pagespeed::` scheduled hooks and unschedule comprehensively or via `wp_clear_scheduled_hook` wildcard. | MEDIUM |
| DEA04 | LOW | `class-deactivate.php:126-154` | `remove_wp_cache_constant` | Pattern `/\/\*\*\s*Enables WordPress Cache\s*\*\/\s*\nif...define..true.../` else `/\n?define\(\s*['"]WP_CACHE['"]\s*,\s*true\s*\);/` → `put_contents`. Same atomicity concern as ACT07; `is_writable` checked but `get_contents` may return false on huge file → `preg_match` on false → PHP warning. | On read failure, `preg_replace` on false yields null, then `put_contents(null)` may corrupt `wp-config.php`. | Guard `if(!is_string(content)) return;` before regex. | MEDIUM |
| DEA05 | INFO | `class-deactivate.php:65-68` | `init` tail | `Log::add('Plugin deactivated'); Cache::clear_cache();` — clears HTML cache on deactivate (good). | — | No change. | HIGH |

### 3.6 `includes/class-util.php` — 643 lines

| ID | Severity | File:Line | Function | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------|----------|--------|----------------|------------|
| UTL01 | INFO | `class-util.php:37-46` | `reset_cached_home_urls` | `self::$home_url_cache=[]` testing isolator. | — | No change. | HIGH |
| UTL02 | LOW | `class-util.php:55-81` | `prepare_cache_dir` | Iteratively builds dirs `explode('/',trim(path,'/'))` → `build='/'.$part` then `if(!is_dir(build)) mkdir(build,FS_CHMOD_DIR)` — creates each ancestor with `FS_CHMOD_DIR` (typically 0755). No `is_dir` race handling; concurrent mkdir may fail harmlessly. | On race, second `mkdir` returns false and function returns false though dir now exists (another process created). | On `!mkdir` re-check `is_dir` before returning false. | LOW |
| UTL03 | INFO | `class-util.php:89-101` | `init_filesystem` | Requires `ABSPATH.wp-admin/includes/file.php` if `WP_Filesystem` missing, then `WP_Fisisystem()` return `$wp_filesystem` else false. | Correct FS abstraction. | No change. | HIGH |
| UTL04 | MEDIUM | `class-util.php:110-145` | `get_local_path` | `parsed=wp_parse_url(url); relative=normalize(parsed[path] ?? ''); if(strpos(relative,'..')!==false) return ''; home_path=normalize(parse_url(cached_home_url(), PHP_URL_PATH)??''); if(home_path && '/'!==home_path && strpos(relative,home_path)===0) relative=substr(...); full=normalize(ABSPATH.ltrim(relative,'/')); if(strpos(full, normalize(ABSPATH))!==0) return ''; return full;` — at `125` uses `cached_home_url()` (cached per blog_id, may be stale after `switch_to_blog` with `has_filter(home_url)`? But `cached_home_url` checks `has_filter` then bypass. Good. | `home_path` stripping uses `strpos` prefix check but not boundary — `/blog` vs `/blog-extra` would incorrectly strip `/blog` from `/blog-extra/path`. | Enforce `home_path.'/'` boundary or use `trailingslashit` compare. | MEDIUM |
| UTL05 | INFO | `class-util.php:153-190` | `get_js_css_minified_file` | Dirlist `min_cache_dir()/js` and `/css`, counts `.js/.css` via `pathinfo`. Uses `Util::init_filesystem` then `min_cache_dir()`. | Correct per-site min cache dir (`cache/wppo/min/{blog_id}`). | No change. | HIGH |
| UTL06 | LOW | `class-util.php:199-224` | `get_image_mime_type` | `extension=strtolower(pathinfo(parse_url(url, PHP_URL_PATH), PATHINFO_EXTENSION)); switch(jpg/jpeg→image/jpeg, png→..., webp, gif, svg, avif, heic/heif, default '')` — HEIC/HEIF not needed for preload link `as=image` but present. `default ''` so unknown extension yields empty type → caller likely skips preload type. | Good. No `jxl` / `heic` mime needed? `heic` correct. | Add `jxl`? Keep minimal. | LOW |
| UTL07 | INFO | `class-util.php:243-281` | `generate_preload_link` | Escapes `rel/href/as/crossorigin/type/media/fetchpriority`, builds `<link ...>` then `wp_kses` allowlist then `echo`. | Safe. | No change. | HIGH |
| UTL08 | INFO | `class-util.php:293-298` | `process_urls` | Array: `trim, array_unique, array_filter, array_values` — dedup + reindex. String: `explode("\n", string)`. | Correct. Note filters empty strings but not whitespace-only? `trim` then `array_filter` without callback removes empty strings correctly (whitespace-only becomes '' after trim → falsy). Good. | No change. | HIGH |
| UTL09 | LOW | `class-util.php:315-365` | `is_url_excluded` | `strip_scheme = fn(u) 0===stripos('https://')?substr(8):stripos('http://')?substr(7):u; normalized_url=strip_scheme(rtrim(url,'/')); foreach(exclude){ rtrim(..., '/'); if(''===rule) continue; if(strpos(rule,'http')!==0) rule=home_base.'/'.ltrim(...); normalized_rule=strip_scheme(rule); if(strpos(rule,'(.*)')!==false) { prefix=rtrim(str_replace('(.*)','',rule),'/').'/'; if(strpos(normalized_url.'/', prefix)===0) return true; } if(normalized_url===normalized_rule) return true; } return false;` — `strip_scheme` case-insensitive, handles `HTTP://`. `(.*)` prefix logic adds `/` boundary so `/cart` rule `https://example.com/cart(.*)` matches `/cart` and `/cart/` and `/cart/item` but not `/cartoon` (good). Root-relative rules resolved against `home_base`. | Empty rule skip handles whitespace-only? `rtrim('   ','/')` leaves `'   '` not empty, then `strip_scheme` leaves `'   '` and exact match fails — whitespace-only rules slip through as non-empty but should be ignored. Original comment says empty+whitespace-only ignored, but code only checks `''===exclude_url` after `rtrim('/'` not `trim`. | Use `trim($exclude_url)` check before `rtrim` to skip whitespace-only. | MEDIUM |
| UTL10 | LOW | `class-util.php:377-381` | `get_current_url` | `url = cached_home_url((string)add_query_arg([], $wp->request ?? '')); return untrailingslashit(esc_url_raw(url))` — strips query, normalizes via `esc_url_raw`. Uses `cached_home_url` with path = `add_query_arg` result (which encodes). Good. | `add_query_arg([], wp->request)` on multisite subdirectory may double-encode? | Acceptable; matches `store_lcp_image_url` normalization. | LOW |
| UTL11 | INFO | `class-util.php:393-435` | `min_cache_*` / `cached_content_url` | `min_cache_base_dir = WP_CONTENT_DIR/cache/wppo/min`, `min_cache_dir(blog_id/subdir)`, `min_cache_url(blog_id/subdir/filename)` via `cached_content_url`, `cached_content_url` caches per blog_id+path via static `$cache` unless `has_filter('content_url')` then bypass. | Correct multisite isolation. | No change. | HIGH |
| UTL12 | INFO | `class-util.php:480-518` | `cached_home_url` / `transient_key` | `cached_home_url` caches per blog_id via static, bypasses if `has_filter('home_url')`; `transient_key` prefixes `blog_id_` on multisite with try/catch. | Correct transient isolation for shared Redis. | No change. | HIGH |
| UTL13 | INFO | `class-util.php:533-578` | `get_role_hash` / `is_cache_eligible_for_current_user` | `get_role_hash` sorts roles, `md5(implode(',',roles).wp_salt())` substr 0,12; eligible checks `is_user_logged_in`→true if anon, else `enableLoggedInCache` flag, empty roles → false, empty allowed_roles → true else intersects. | Correct role-variant caching logic mirroring `advanced-cache.php` drop-in salt. | No change. | HIGH |
| UTL14 | LOW | `class-util.php:589-592` | `is_auto_sizes_available` | `return function_exists('wp_sizes_attribute_includes_valid_auto') || function_exists('wp_img_tag_add_auto_sizes')` — gates WP 6.7 auto-sizes. | Correct version gate. | No change. | HIGH |
| UTL15 | LOW | `class-util.php:605-641` | `sanitize_settings_recursively` | `foreach settings as key=>value: safe_key=preg_replace('/[^a-zA-Z0-9_\-]/','',key); if(''===safe_key) continue; if('mode'===safe_key && !is_array(value)) raw=sanitize_text_field((string)value) whitelist auto/wppo/litespeed/standalone; if(is_array) recurse; elseif is_bool → bool; elseif is_numeric → (int); elseif safe_key in [pagespeed_api_key,password] → sanitize_text_field; elseif stripos(url|cdn|origin) → esc_url_raw; elseif stripos(exclude|preload|delay|list) → sanitize_textarea_field; else sanitize_text_field` | Numeric cast `(int)` truncates floats (e.g. `delayJSIdleTimeout` 3000 okay, but `0.5` becomes 0). `is_numeric('3.14')` → 3 loses precision. Also `is_numeric` check before url/list checks means numeric-like url? Not. Order: `mode` allowlist before generic handling — good. `stripos` checks case-insensitive, so `CDNURL` would be caught though safe_key regex stripped non-alphanum and is case-sensitive? `safe_key` retains original case? Regex allows A-Z, so `CDNURL` stays uppercase, `stripos` case-insensitive still matches. Good. | For float settings, cast to `(float)` or keep string; currently integer truncation may silently change `auto_sizi`? No floats used. Document. Also `sanitize_settings_recursively` called on both REST + CLI, so truncation affects both. | MEDIUM |

### 3.7 `includes/class-core-tweaks.php` — 408 lines

| ID | Severity | File:Line | Function | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------|----------|--------|----------------|------------|
| CT01 | INFO | `class-core-tweaks.php:33-101` | `__construct` | Registers hooks conditionally per settings flags; `disableEmojis` → `init:disable_emojis` + conditional `wp_enqueue_scripts/admin_enqueue_scripts:disable_emojis_script_module` if `wp_dequeue_script_module` exists; `disableEmbeds` → `init:-1000:disable_embeds`; `disableDashicons` → `wp_enqueue_scripts:disable_dashicons`; `disableXMLRPC` → `xmlrpc_methods:__return_false`, `xmlrpc_enabled:__return_false`, `wp_headers:remove_x_pingback`; etc. | Correct conditional hook registration, no overhead when disabled. | No change. | HIGH |
| CT02 | LOW | `class-core-tweaks.php:108-127` | `disable_emojis` | `remove_action(wp_head,print_emoji_detection_script,7)` + `admin_print_scripts`, `wp_print_styles`, filters `the_content_feed/comment_text_rss/wp_mail`, `tiny_mce_plugins`, `wp_resource_hints`, then `wp_dequeue_script_module('emoji')` if function exists, `wp_dequeue_script('wp-emoji')`. | Duplicates `disable_emojis_script_module` hooked separately at 100; `disable_emojis` runs on `init` (before enqueue) so `wp_dequeue_script_module` there may be too early (scripts not registered yet) — dequeue no-op, but second hook at `wp_enqueue_scripts` 100 catches. Intentional defense-in-depth. | Document dual dequeue timing. | LOW |
| CT03 | INFO | `class-core-tweaks.php:135-139` | `disable_emojis_script_module` | Single `wp_dequeue_script_module('emoji')` if exists. | — | No change. | HIGH |
| CT04 | LOW | `class-core-tweaks.php:161-167` | `disable_emojis_remove_dns_prefetch` | `if('dns-prefetch'===relation && is_array(urls)) { emoji_svg_url=apply_filters('emoji_svg_url','https://s.w.org/.../svg/'); urls=array_diff(urls,[emoji_svg_url]); }` — removes only that exact URL, not host variants. | If filter changes emoji URL via `emoji_svg_url`, removal still works via filtered value. Good. | No change. | HIGH |
| CT05 | INFO | `class-core-tweaks.php:174-183` | `disable_embeds` | `remove_action(rest_api_init, wp_oembed_register_route); add_filter(embed_oembed_discover,__return_false); remove_filter(oembed_dataparse, wp_filter_oembed_result,10); remove_action(wp_head, wp_oembed_add_discovery_links/wp_oembed_add_host_js); add_filter(tiny_mce_plugins...); add_filter(rewrite_rules_array...); remove_filter(pre_oembed_result...)` | Comprehensive oembed disable. `remove_action(rest_api_init,...)` priority default 10 — WP registers at 10, so removal at init -1000 before registration? Actually `rest_api_init` action not yet fired at `init`, so `remove_action('rest_api_init', ...)` is valid to deregister before fire. | No change. | HIGH |
| CT06 | LOW | `class-core-tweaks.php:204-215` | `disable_embeds_rewrites` | `if(!is_array(rules)) return rules; foreach(rules as rule=>rewrite) if(false===strpos(rewrite,'embed=true')) new_rules[rule]=rewrite; return new_rules` — drops embed rules. | No flush rewrite rules after removal — rules stay cached until flush. | Call `flush_rewrite_rules()` on settings toggle (once) via `on_settings_update`. | MEDIUM |
| CT07 | INFO | `class-core-tweaks.php:222-226` | `disable_dashicons` | `if(!is_user_logged_in()) wp_deregister_style('dashicons')` | Logged-in users retain dashicons for admin bar icons. Correct. | No change. | HIGH |
| CT08 | INFO | `class-core-tweaks.php:234-249` | `remove_x_pingback / block_xmlrpc_methods` | `remove_x_pingback` unsets `X-Pingback`; `block_xmlrpc_methods` returns `[]`. | `xmlrpc_methods` returning empty blocks all methods, but `xmlrpc_enabled` filter already false — defense-in-depth. | No change. | HIGH |
| CT09 | MEDIUM | `class-core-tweaks.php:256-277` | `control_heartbeat / heartbeat_60s` | `heartbeatControl = settings['heartbeatControl'] ?? 'default'; if('disable_all'===c) wp_deregister_script('heartbeat'); elseif('disable_ext'===c && !is_admin() && !(DOING_AJAX)) wp_deregister; elseif('60s'===c) add_filter('heartbeat_settings', heartbeat_60s)` — `'disable_ext'` deregisters everywhere except admin and AJAX, but frontend heartbeat script may still be enqueued via other plugins' `wp_enqueue_script('heartbeat')` after `init:1`? `wp_deregister_script` at `init:1` before `wp_enqueue_scripts` runs, so later enqueue will re-register? Actually `wp_deregister_script` removes registration, but `wp_enqueue_script('heartbeat')` later will fail to find it and not load — intended. However direct `wp_print_scripts` at 100 may still try? | Heartbeat disabled on frontend via deregister works, but AJAX heartbeat still needed for post lock; `'disable_ext'` preserves AJAX per condition — correct. | No change, document that `disable_ext` keeps `admin-ajax.php` heartbeat. | LOW |
| CT10 | LOW | `class-core-tweaks.php:284-287` | `remove_rest_api_links` | `remove_action(wp_head,rest_output_link_wp_head); remove_action(wp_head,wp_oembed_add_discovery_links)` — second was already removed in `disableEmbeds`, duplicate safe. | Duplicate removal when both `disableEmbeds` and `disableRestApiLinks` enabled — harmless. | Keep. | LOW |
| CT11 | LOW | `class-core-tweaks.php:297-317` | `suppress_rest_header` | `if(WP_Error || !WP_REST_Response) return result; if(WP_REST_Response){ headers=get_headers(); foreach keys if(strtolower(key)==='link') unset; if(found) set_headers(headers); }` — case-insensitive Link header removal. `WP_Error` check first but `!WP_REST_Response` already excludes WP_Error (since WP_Error not instance of Response) — redundant check but safe. | Suppresses `Link: <...>; rel="https://api.w.org/"` header when setting enabled. Good. | No change. | HIGH |
| CT12 | INFO | `class-core-tweaks.php:324-327` | `redirect_feed_to_home` | `wp_safe_redirect(cached_home_url('/'),301); exit` | Uses `cached_home_url('/')` (blog-id cached) correct. | No change. | HIGH |
| CT13 | LOW | `class-core-tweaks.php:334-337` | `remove_feed_links` | `remove_action(wp_head,feed_links,2); remove_action(wp_head,feed_links_extra,3)` | Correct. Note must run on `wp_head` 100 (constructor hooks at 100) so after feed links registered at 2/3 — timing ok. | No change. | HIGH |
| CT14 | LOW | `class-core-tweaks.php:354-361` | `remove_jquery_migrate` | `if(!isset(scripts->registered['jquery']) || empty(deps)) return; deps=array_values(array_diff(deps,['jquery-migrate']))` | Safe. Note `wp_default_scripts` hook provides `$scripts` registry object; alternative `wp_scripts` global not needed. | No change. | HIGH |
| CT15 | LOW | `class-core-tweaks.php:368-372` | `remove_password_strength_scripts` | `if(!is_admin()) wp_deregister_script('password-strength-meter')` | Frontend only, keeps admin strength meter. Good. | No change. | HIGH |
| CT16 | HIGH | `class-core-tweaks.php:384-406` | `disable_self_pingbacks` | `home_parsed=wp_parse_url((string)get_option('home')); home_host=strtolower(home_parsed['host']??''); home_port=home_parsed['port']??null; foreach pung as key=>url { parsed=wp_parse_url(url); host=strtolower(parsed['host']??''); if(''===home_host || ''===host || host!==home_host) continue; port=parsed['port']??null; if(port!==home_port) continue; path=parsed['path']??'/'; if(''!==home_path && '/'!==home_path && strpos(path,home_path)!==0) continue; unset(pung[key]); }` — compares host+port+home_path boundary to remove self pingbacks. Prevents `https://example.com`→`http://example.com` cross-scheme self ping (same host) correctly removed, and blocks `example.com.evil` bypass via exact host equality. `home_path` check uses `strpos(path,home_path)===0` without `/` boundary — `home_path='/blog'` would match `/blogger` incorrectly removing false positive. | False positive removal blocks legitimate external pingback to `https://example.com/blogger` if home is subdirectory `/blog`. | Enforce boundary: `path===home_path || strpos(path, trailingslashit(home_path))===0`. | MEDIUM |

### 3.8 `includes/class-admin-notices.php` — 316 lines

| ID | Severity | File:Line | Function | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------|----------|--------|----------------|------------|
| AN01 | INFO | `class-admin-notices.php:22-39` | `COMPETING_CACHE_PLUGINS` | Map of 10 plugins `file=>label` including `litespeed-cache/litespeed-cache.php=>LiteSpeed Cache`. | Used to warn multiple page-cache plugins. | Keep updated as new cache plugins emerge. | HIGH |
| AN02 | LOW | `class-admin-notices.php:53-86` | `handle_dismiss` | `if(!isset($_GET[wppo_dismiss],_wpnonce)) return; if(!wp_verify_nonce(sanitize_text_field(wp_unslash(_wpnonce)),'wppo_dismiss_notice')) return; if(!current_user_can(manage_options)) return; key=sanitize_key(wp_unslash(wppo_dismiss)); switch key: activation→delete_transient, review_done→update_option 1, review_snooze→time+30DAY, litespeed→update_user_meta; wp_safe_redirect(remove_query_arg(...)); exit` — order: nonce before cap, but both checked. | Nonce check before cap prevents cap bypass via CSRF? Correct order: nonce first then cap still ok; but `sanitize_text_field` on nonce before verify is standard. Redirect via `wp_safe_redirect` + `exit` correct. | Check `current_user_can` before nonce to avoid nonce oracle for low-priv users? Order not security relevant; keep. | HIGH |
| AN03 | LOW | `class-admin-notices.php:93-102` | `render_notices` | `if(!current_user_can(manage_options)) return; maybe_activation, maybe_competing, maybe_litespeed, maybe_review` — gated. | Subscribers never see notices. Good. | No change. | HIGH |
| AN04 | LOW | `class-admin-notices.php:109-154` | `maybe_activation_notices` | `notices=get_transient(transient_key('wppo_activation_notices')); if(!is_array||empty) return; foreach notices as key switch: foreign_dropin→message, wp_cache_disabled→..., wp_config_*→..., wp_config_write_failed→..., default: break; if(empty(messages)) return; dismiss=wp_nonce_url(add_query_arg('wppo_dismiss','activation'),...); echo '<div notice-warning><p><strong>Performance Optimisation</strong></p><ul...><li>wp_kses_post(html)</li></ul><p><a href=dismiss>Dismiss</a></p></div>` — `array_unique(messages)` dedup, but `messages` not unique before loop; duplicate transient entries dedup at render. | `default: break` silently ignores unknown notice keys (future-proof). Good. | No change. | HIGH |
| AN05 | LOW | `class-admin-notices.php:166-201` | `maybe_litespeed_coexistence_notice` | `if(!class_exists(LiteSpeed_Integration)) return; if(!is_litespeed()||!is_lscache_active) return; if(get_mode()!==MODE_AUTO) return; if(effective_mode()!==MODE_LITESPEED) return; user_id=get_current_user_id(); if(user_id && get_user_meta(...,wppo_litespeed_notice_dismissed)) return; echo notice-warning is-dismissible` — `is-dismissible` class makes WP JS dismiss button work *without* server-side handler? But dismiss link explicitly via `wppo_dismiss=litespeed` nonce URL. Dual dismiss (JS vs server) may confuse — JS dismiss hides but not persistent; server link persists via user_meta. | User clicking X (JS) hides notice until reload, whereas Dismiss link persists. Acceptable. | Hook `wp_ajax_dismiss-wp-pointer`-style to persist JS dismiss? Already per-user meta check covers Dismiss link only. | LOW |
| AN06 | INFO | `class-admin-notices.php:208-232` | `maybe_competing_plugins_notice` | `if(!Advanced_Cache_Handler::is_our_dropin()) return; active=get_active_plugin_files(); found=[] foreach COMPETING as file=>label if(in_array(file,active,true)) found[file]=label; if(empty) return; names=implode(', ', map esc_html); echo notice-info is-dismissible` — only warns when *our* drop-in is active, not foreign. | When foreign drop-in present (our not active), competing notice suppressed (correct, activation notice already warns). | No change. | HIGH |
| AN07 | LOW | `class-admin-notices.php:239-286` | `maybe_review_notice` | `if(!is_admin()||!current_user_can) return; if(get_option(wppo_review_dismissed)) return; snoozed_until=(int)get_option(...0); if(snoozed>time) return; activation_time=get_option('wppo_activation_time'); if(false===activation_time) update_option(time) return; if(time-activation <7*DAY) return; echo review notice with Leave Review / Already did / Not now buttons via nonce URLs` | `get_option('wppo_activation_time')` false on first admin visit after upgrade from version lacking this option → sets time to now and returns (no notice until +7d). Correct. | No change. | HIGH |
| AN08 | INFO | `class-admin-notices.php:293-314` | `get_active_plugin_files` | `plugins=(array)get_option('active_plugins',[]); if(function_exists(is_multisite)) try is_multisite() catch → false; if(multisite) try get_site_option(active_sitewide_plugins) merge; catch→ignore; return unique values` — multisite-aware, try/catch for Brain Monkey tests. | Correct. | No change. | HIGH |
| AN09 | DUPLICATE | `class-admin-notices.php:84,243` | `handle_dismiss` vs `render_notices` caps | Both check `current_user_can(manage_options)` — duplicated but correct defense-in-depth. | — | Keep. | HIGH |

---

## 4. No-Issues (confirmed correct)

- **REST permission model**: 24 admin routes use `permission_callback` with `current_user_can('manage_options') && wp_verify_nonce('wp_rest')` (`class-rest.php:326-331`) — correct. `rum_collect` intentionally public with token+rate limit (`class-rest.php:218-223`).
- **Sensitive data redaction**: `update_settings` (`class-rest.php:472-478`), `import_settings` (`764-770`), `handle_object_cache` (`1001-1004`) and CLI `settings export/import` (`576-583,648-650`) never persist `password`/`pagespeed_api_key`, redacts from responses.
- **SSRF protection**: `run_performance_scan` (`1210-1226`) and `queue_pagespeed_scan` (`1258-1274`) both enforce scheme allowlist + `wp_http_validate_url` (loopback/private) + host equality vs `Util::cached_home_url()`.
- **Image path traversal**: `optimise_image` (`553-563`) and CLI `image convert` (`336-339`) both use `realpath` + `strpos(normalize(resolved), ABSPATH)===0`.
- **Cache path traversal**: `clear_cache` (`371-387`) uses `wp_normalize_path` + `realpath` + prefix check (though see R03 false-reject edge).
- **Filesystem abstraction**: `Util::init_filesystem()` (`89-101`) centralizes `WP_Filesystem` init; all file ops via `$wp_filesystem` (`class-rest.php:670-682`, `class-activate.php:258`, etc.) — correct.
- **Whitelisting in metabox**: `save_asset_manager_settings` (`365-394`) whitelists disabled handles and delay values against `Asset_Manager::get_page_assets()` capture (`process_disabled_assets` `417-422`, `process_delay_setting` `433-458`) — prevents arbitrary post_meta injection.
- **Nonce discipline**: Metaboxes use `wp_nonce_field` + `wp_verify_nonce(sanitize_text_field(wp_unslash()))` (`class-metabox.php:93-94,118,334-335,360-361`); `ajax_get_nonce` (`1164-1178`) and `handle_dismiss` (`58-59`) verify before state change.
- **Multisite transient isolation**: `Util::transient_key()` (`509-518`) used for `wppo_activation_notices` (`class-activate.php:70, class-admin-notices.php:110, class-rest.php:1406`), `wppo_audit_*` (`class-rest.php:1406`), CLI not needed — prevents shared Redis collisions.
- **Core_Tweaks conditional hooks**: Constructor (`33-101`) registers each tweak only when setting enabled; `disable_emojis` handles WP 6.9 script-module split (`108-139`), `disable_self_pingbacks` (`384-406`) ports/boundary aware vs string prefix.
- **Util URL helpers**: `process_urls` dedup (`293-298`), `is_url_excluded` `(.*)` prefix with `/` boundary (`352-354`), `get_local_path` ABSPATH prefix enforcement (`140-142`), `sanitize_settings_recursively` central sanitization (`605-641`).
- **Activate/Deactivate symmetry**: `activate` creates drop-in + `WP_CACHE` + seeds settings + DB table + upgrade flush; `deactivate` removes drop-in/object-cache/config/`WP_CACHE` + unschedules + clears cache — reversible.
- **CLI idempotency**: `cache status` (`85-91`), `database counts` (`195-199`), `image status` (`352-369`), `system_info` (`939-954`) are read-only; `settings get/export` (`573-602,661-688`) never mutates.

---

## 5. Duplicate / Dead Code

| ID | Severity | File:Line | Evidence | Impact | Recommendation |
|----|----------|-----------|----------|--------|----------------|
| DP01 | DUPLICATE | `class-rest.php:423-434` ↔ `class-rest.php:712-728` ↔ `class-wppo-cli-command.php:627` ↔ `class-wppo-cli-command.php:640-646` | Settings allowlist `file_optimisation,preload_settings,image_optimisation,database_cleanup,object_cache,performance_audit,cache_settings,litespeed_integration,llms_txt` vs CLI `...core_tweaks` vs Activate seed `...` (7) — 3 definitions of same set, drift as in R06/ACT02. | Backup/restore parity break. | Centralize `Util::ALLOWED_TABS` constant. |
| DP02 | DUPLICATE | `class-activate.php:116-178` ↔ `class-wppo-cli-command.php:434-505` | 60-line defaults literal duplicated. | Drift (ACT02 missing disable* keys). | Centralize. |
| DP03 | DUPLICATE | `class-wppo-cli-command.php:425-556` | Docblock for `settings` appears twice above `get_default_settings()` and `settings()` — orphaned 24-line PHPDoc. | `wp help wppo` duplicate. | Delete first block. |
| DP04 | DUPLICATE | `class-rest.php:553-563` ↔ `class-wppo-cli-command.php:336-339` | `realpath` + `strpos(ABSPATH)` traversal guard duplicated. | Maintenance. | Extract `Util::is_path_within_abspath($path)`. |
| DP05 | DUPLICATE | `class-rest.php:1202-1226` ↔ `1258-1274` | SSRF checks (scheme + wp_http_validate_url + host equality) duplicated with order swap. | Drift. | Extract `Util::validate_local_url($url)` helper. |
| DP06 | DUPLICATE | `class-rest.php:648-657` ↔ `955-970` | Pending/completed/failed count logic (`count(pending[webp])`) duplicated between `optimise_image` sync fallback and `get_image_job_status`. | Drift. | Helper `Img_Converter::get_count_summary()`. |
| DP07 | DUPLICATE | `class-core-tweaks.php:108-127` vs `135-139` | `disable_emojis` dequeues `emoji` module then `disable_emojis_script_module` hooked separately also dequeues — double dequeue path. | Harmless defense-in-depth but confusing. | Document or keep one path with comment. |
| DP08 | DEAD CODE | `class-rest.php:122` | `$schemas = [ $this, 'get_schema_for_route' ]` variable then `'schema' => $schemas` for all 25 routes — schema is identical per route but callable invoked per route; no per-route args. | Not dead but over-indirection; could be `'schema' => [$this,'get_schema_for_route']` inline. | Keep (explicit). |
| DP09 | DEAD CODE | `class-deactivate.php:78-102` vs `110-115` | `unschedule_crons` + `unschedule_database_cleanup_cron` split; second could be folded into first. | Minor. | Merge or call both from `init` as now (already). |
| DP10 | DEAD CODE | `class-util.php:524-531` | `get_role_hash` returns `''` for no roles — `is_cache_eligible_for_current_user` handles empty roles separately; hash empty case never used for cache key? Actually `Cache` uses empty hash as `''` suffix (no variant) — intentional. | Not dead, but empty hash path is tested. | No change. |

---

## 6. Open Questions (need owner decision)

1. **Q-R06 (tab allowlist)**: Should `litespeed_integration` / `llms_txt` / `core_tweaks` be first-class tabs in REST+CLI+Activate? Current UI likely has `core_tweaks` under `file_optimisation` — confirm desired allowlist and centralize.
2. **Q-R03 (clear_cache path)**: Is REST `realpath` check intended to reject non-existent cache dirs? If yes, keep 400; if no, fix to string-prefix check so "Clear This Page" works before caching.
3. **Q-CLI02 (database types)**: Are `unattached_media`, `oembed_cache`, `trashed_comments` intended to be CLI-accessible? REST allows them but CLI help lists only 6 aliases — should CLI parity be added?
4. **Q-MB01 (preload metabox screen)**: `add_meta_box(..., '', ...)` — is preload box intentionally global or missing post_type loop? Confirm expected visibility.
5. **Q-MB10 (preserve before capture)**: Should `save_asset_manager_settings` preserve existing meta when `get_page_assets()` returns `false` (no capture yet) instead of overwriting to `[]`? Current overwrites to empty on first save before frontend visit.
6. **Q-ACT02/ACT03 (defaults source)**: Should defaults live in `class-main.php`, `Activate`, and `CLI` separately or single `Util::get_default_settings()`? Proposal to single-source.
7. **Q-DEA03 (unschedule completeness)**: Which cron hooks are canonical? `Cron`, `Pagespeed`, `Used_CSS`, `Critical_CSS`, sitemap `wppo_generate_static_url` — should `Deactivate::unschedule_crons()` clear all via `wp_clear_scheduled_hook` iteration vs explicit list?
8. **Q-CT06 (rewrite flush)**: `disable_embeds` drops `embed=true` rules via `rewrite_rules_array` filter, but `flush_rewrite_rules()` not called on toggle — should `on_settings_update` flush once per disable toggle?
9. **Q-CT16 (self-pingback boundary)**: For subdirectory installs (`/blog`), should `disable_self_pingbacks` use `trailingslashit(home_path)` boundary to avoid false removal of `https://example.com/blogger`? Confirm intended semantics.
10. **Q-UTL09 (whitespace rules)**: `is_url_excluded` comment says whitespace-only rules ignored, but code only checks `''===rtrim('/' )` — should it `trim` before check? Confirm.
11. **Q-UTL15 (float sanitization)**: `sanitize_settings_recursively` casts `is_numeric` → `(int)` truncating floats — any setting expects float (e.g. timeout `1.5`)? Confirm integer truncation acceptable.
12. **Q-R13 (delete_optimised_image confirm)**: Should delete of `wp-content/wppo` require explicit `confirm` param to avoid accidental wipe via CSRF (even with nonce + cap)?
13. **Q-R01 (RUM rate limit)**: What are `RUM::collect()` token source and IP rate-limit thresholds? Audit did not inspect `class-rum.php`; need to verify persistence (options vs transient) and side-channel for public beacon.

---

## 7. Verification

- Read 8 files end-to-end (1563+956+461+348+156+643+408+316 = 4851 lines) via `Read`.
- Traced 25 REST routes in `Rest::get_routes()` (`class-rest.php:74-236`) — all 25 callbacks and permission handlers inspected.
- Traced 7 CLI commands (`cache`, `database`, `image`, `settings`, `object-cache`, `pagespeed`, `system-info`) via `WPPO_CLI_Command` (`class-wppo-cli-command.php:75-956`).
- Traced 2 metaboxes (`preload_image_metabox` empty screen, `wppo_asset_manager` per-type) + 2 save paths + whitelisting helpers (`class-metabox.php:49-460`).
- Traced activation/deactivation side-effects including `wp-config.php` regex, drop-in create/remove, cron schedule/unschedule, transient prefix (`class-activate.php:55-346`, `class-deactivate.php:37-155`).
- Did not modify production code; write is to `AUDIT/AGENTS/agent-A04-php-rest-cli.md` only.

---

*Generated by agent A04 — do not edit production files based on this audit without owner review. Update `AGENTS.md` if audit workflow changes.*
