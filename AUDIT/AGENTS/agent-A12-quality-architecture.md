# Agent A12 — Quality & Architecture Audit

**Scope:** exhaustive line-by-line audit of ALL PHP (`performance-optimisation.php`, `uninstall.php`, `templates/object-cache.php`, `includes/*.php`), JS (`src/**/*.js`), and CSS/SCSS (`src/css/**/*.scss`, `build/`)  
**Date:** 2026-08-27  
**Auditor:** agent A12 (`quality-architecture`)  
**Workspace:** `/var/www/nileshportfolio.duckdns.org/wp-content/plugins/performance-optimisation`  
**Instruction:** Check responsibilities, abstractions, coupling, global state, dependency management, lifecycle, naming, overly complex functions, god classes, circular deps, testability, repeated logic, inconsistent patterns, error handling, edge cases, coding standards. Classify CRITICAL/HIGH/MEDIUM/LOW/INFO/OPTIMIZATION/DUPLICATE/DEAD CODE with file:line evidence, impact, recommendation, confidence. Do not modify production code. Be evidence-based, do not invent unnecessary abstraction.  
**Method:** Read every production file completely (`Read` with offsets), ran `wc -l` (28,143 PHP lines), `grep -rn` for coupling/global-state/dependency patterns, inspected `composer.json`/`package.json`/`phpcs.xml`; cross-referenced each finding with all call-sites before classifying.

---

## 1. Files Reviewed (with line counts & surface)

| # | File | Lines | Surface examined |
|---|------|-------|------------------|
| 1 | `performance-optimisation.php` | 70 | Bootstrap constants, `require vendor/autoload`, `new Main()` at file load, activation/deactivation hooks |
| 2 | `uninstall.php` | 165 | Per-site `wppo_cleanup_site()` (16 option deletes, 2 dirs, drop-ins, transients with `blog_id_` prefix), `wppo_delete_directory()` recursion, multisite `get_sites` loop |
| 3 | `templates/object-cache.php` | 1152 | `WP_Object_Cache` drop-in (standalone/sentinel/cluster, TLS, compression, `blog_prefix` key namespacing) |
| 4 | `includes/class-main.php` | 2956 | Constructor defaults (60+ keys), `includes()` (manual requires), `setup_hooks()` (~40 hook registrations), `on_settings_update` (cache/htaccess/used-css gates), delay/defer parsing (strategy/priority/timeout), `block_assets_are_separate` gating, `should_optimise_for_logged_in`, `strip_static_query_strings`, `is_file_minified`, `get_minifiable_css_path`, `enqueue_scripts`/`admin_enqueue_scripts` |
| 5 | `includes/class-cache.php` | 2269 | `CACHE_DIR` + `cache_root_dir/domain/url_path` construction, `combine_css` (190 lines) + `get_combined_handles`/`combined_handles_match`, `core_will_inline`/`core_inline_budget_will_inline` (double simulation), `should_skip_combine_for_inline_budget`, `maybe_apply_cdn` (Tag Processor), `minify_buffer`, `is_not_cacheable` (DONOTCACHEPAGE + sitemap + WooCommerce), `maybe_mark_page_not_cacheable` (.wppo-no-cache marker), `save_cache_files` (gzip/br), `clear_cache` static, `count_cached_pages` |
| 6 | `includes/class-util.php` | 643 | `prepare_cache_dir`, `init_filesystem`, `get_local_path` (ABSPATH bounds), `process_urls`, `is_url_excluded` (cached `home_base`, `(.*)` prefix, scheme stripping), `cached_home_url`/`cached_content_url` (blog-id static cache + `has_filter` bypass), `min_cache_dir`/`min_cache_base_dir`/`min_cache_url`, `transient_key`, `get_role_hash`/`is_cache_eligible_for_current_user`, `sanitize_settings_recursively` (heuristic `stripos` branches), `get_image_mime_type`, `generate_preload_link` |
| 7 | `includes/class-cron.php` | 733 | `schedule_cron_jobs` (7 schedule gates), `schedule_page_cron_jobs` (200/batch, lock via transient, `preloadSitemap` branch), `get_sitemap_urls` (15s deadline, 500 cap, `TO_FETCH_LIMIT=50`), `schedule_sitemap_url_jobs`, `process_page`/`process_url`/`load_page`, `img_convert_cron` (batch + discovery), `database_cleanup_cron` (daily/weekly/monthly), `web_vitals_rescan_cron`, `used_css_cron`, `llms_txt_cron`, `clear_cron_jobs` |
| 8 | `includes/class-img-converter.php` | 1865 | `convert_image` (GD/Imagick), `get_format`/`core_handles_next_gen`, `get_img_path`/`get_img_url`, `add_img_into_queue`/`queue_unconverted_library_images`, `wppo_img_info` non-autoload + deferred shutdown commits, `clean_placeholder_on_delete` |
| 9 | `includes/class-image-optimisation.php` | 3037 | `__construct` (5 `Util::process_urls` + `exclude_sizes`), `setup_hooks` (convertImg gate), `maybe_serve_next_gen_images` (Tag Processor vs regex), `replace_image_with_next_gen` (AVIF→WebP fallback), `normalize_url`/`resolve_relative_path` (protocol-relative/root-relative/relative), `get_all_preload_data` (front/meta/post-type/LCP 3-priority), `process_picture_blocks_processor` vs `process_picture_blocks_regex`, `post_process_placeholders`/`post_process_img_dimensions`/`post_process_auto_sizes` (3 regex passes), `process_img_tag` (~250 lines), `set_loading_optimization_attributes` |
| 10 | `includes/class-database-cleanup.php` | 1142 | 9 `clean_*` (batched `LIMIT 1000` loops), `clean_all` vs `auto_clean` (2-type gap), `get_counts` (COUNT(*)), `invoke_cleanup_method`, `TABLE_MAP`/`METHOD_TO_TYPE`, `optimize_table`/`maybe_optimize_tables`, `get_autoloaded_options` |
| 11 | `includes/class-rest.php` | 1563 | 25 routes via `get_routes()`, `permission_callback` (`manage_options` + `wp_rest` nonce), `update_settings` vs `import_settings` (whitelist + sanitize + password→`password_set` + preservation of 3 keys), `optimise_image` (AS + sync, `realpath` guard), `build_redis_config`/`sanitize_nodes`, SSRF gates (scheme + `wp_http_validate_url` + `home_host`), `send_response` envelope |
| 12 | `includes/class-wppo-cli-command.php` | 956 | `cache`, `database`, `image`, `settings`, `object_cache`, `pagespeed` subcommands, mirrors REST validation (`allowed_tabs`, password flag) |
| 13 | `includes/class-abilities.php` | 583 | `register_abilities`, `permission_check`, duplicates DB-cleanup map, WordPress Abilities API integration |
| 14 | `includes/class-activate.php` | 345 | `add_wp_cache_constant` (writes `wp-config.php`), `create_activity_log_table` (`dbDelta`), `maybe_run_upgrades` (version floor + migration) |
| 15 | `includes/class-deactivate.php` | 156 | `remove_wp_cache_constant`, `clear_cron_jobs`, `maybe_remove_dropins` |
| 16 | `includes/class-advanced-cache-handler.php` | 324 | `create` (generates `advanced-cache.php` drop-in string), `is_our_dropin`/`foreign_dropin_present`, `remove`, `get_dropin_path` |
| 17 | `includes/class-htaccess-handler.php` | 222 | `update_rules` (`insert_with_markers` with `wppo_rules`), `get_rules` (gzip + expires + next-gen rewrite with AVIF→WebP order) |
| 18 | `includes/class-server-rules.php` | 191 | `get_server_type` (`SERVER_SOFTWARE` parsing), `get_nginx_rules` (gzip_types array, browser caching `expires`), `get_apache_rules` (delegates to Htaccess_Handler) |
| 19 | `includes/class-litespeed-integration.php` | 1343 | `is_litespeed`/`is_lscache_active`, `effective_mode` (auto/wppo/litespeed/standalone), `should_disable_wppo_optimizer`, `can_apply_cdn`, `get_info` (for `wppoSettings.litespeed`), `init` (purge sync hooks) |
| 20 | `includes/class-pagespeed.php` | 661 | `queue_scan` (Action Scheduler), `run_scan` (Google PSI `wp_remote_get`), `prepare_response`, `record_trend` (capped 30/URL+strategy in `wppo_web_vitals_trends`), `store_lcp_image_url` (post-meta/front-option/transient 3-tier) |
| 21 | `includes/class-telemetry.php` | 985 | `scan` (cURL fetch + `parse_resources` + `calculate_sizes`), `parse_resources` (Tag Processor), 16 metric keys |
| 22 | `includes/class-rum.php` | 332 | `collect` (public token + IP rate-limit), `is_valid_token`/`is_rate_limited`/`store_sample`, `get_data` (aggregated p75) |
| 23 | `includes/class-system-info.php` | 633 | `get_all` (8 groups), `get_php`/`get_database`/`get_wordpress`/`get_wp_constants`/`get_server`/`get_cache`/`get_infrastructure`/`get_opcache`/`get_litespeed`, `get_request_start_microtime` |
| 24 | `includes/class-object-cache.php` | 363 | `get_status`/`ping`/`enable`/`disable`/`flush`, writes `wp-content/object-cache.php` + `wppo-redis-config.php`, `password` never persisted (constant `WPPO_REDIS_PASSWORD`) |
| 25 | `includes/redis-connect-helper.php` | 377 | `wppo_redis_connect*` helpers (standalone/sentinel/cluster, TLS, retry, timeout) |
| 26 | `includes/class-used-css.php` | 1261 | `parse_css` (PurgeCSS-like), `purge_css`, `process_buffer`, `regenerate_all` (batch 20, lock transient), `process_background` (AS) |
| 27 | `includes/class-critical-css.php` | 589 | `get_ccss_dir`, `generate` (critical extraction), `inline_ccss`/`defer_stylesheets`, `background_generate` (AS) |
| 28 | `includes/class-asset-manager.php` | 245 | `capture_page_assets` (queue snapshot), `get_page_assets` (post-meta) |
| 29 | `includes/class-metabox.php` | 461 | `add_metabox`, `render` (preload URL + disabled assets), `save_metabox` (nonce + caps + sanitization) |
| 30 | `includes/class-log.php` | 150 | `add` (insert + salt `wppo_activity_log_salt` bump), `get_recent_activities` (paginated, versioned cache) |
| 31 | `includes/class-google-fonts.php` | 283 | `process_buffer`/`process_style_tag` (host Google Fonts locally, `cache/wppo/fonts`) |
| 32 | `includes/class-llms.php` | 577 | `register_rewrite` (`^llms.txt$` + `llms` query var), `serve`/`generate` (posts/pages → markdown), `emit_link_header`/`emit_head_link` |
| 33 | `includes/class-cdn-purger.php` | 124 | `purge_all` (Cloudflare `api.cloudflare.com/client/v4/zones/{id}/purge_cache` + Varnish `PURGE` requests, token via `WPPO_CLOUDFLARE_API_TOKEN` constant) |
| 34 | `includes/class-core-tweaks.php` | 408 | `disable_emojis`/`disable_embeds`/`disable_dashicons`/`disable_xmlrpc`, Heartbeat control (`wppo_heartbeat_*`), `__construct` takes `file_optimisation` slice |
| 35 | `includes/minify/class-css.php` | 311 | `Minify\CSS::minify`, `inject_font_display_swap` (`font-display: block`→`swap`) |
| 36 | `includes/minify/class-html.php` | 541 | `Minify\HTML::get_minified_html` (voku/html-min wrapper, `removeHTMLComments`, `minifyInlineCSS/JS` branches) |
| 37 | `includes/minify/class-js.php` | 169 | `Minify\JS::minify` (matthiasmullie/minify wrapper) |
| 38 | `src/App.js` | 527 | `App` orchestrator (lazy `import()` 7 tabs, `Suspense`, sidebar + focus trap + resize, theme CSS vars, 3 `useEffect` fetchers with `hasFetched*` refs + `AbortController`, `renderContent` switch) |
| 39 | `src/index.js` | 11 | `createRoot` mount guard on `#performance-optimisation` |
| 40 | `src/main.js` | 239 | Admin-bar standalone entry (`postJsonRequest` + `refreshNonce` with `pendingRefresh`, two buttons Clear All/This Page) |
| 41 | `src/lazyload.js` | 1025 | Vanilla lazy (IntersectionObserver `rootMargin: 200px`, MutationObserver, `data-src`/`data-srcset`/`data-background`, deferred script on `interaction`/`idle`/`viewport`) |
| 42 | `src/rum.js` | 195 | RUM beacon (Web Vitals `web-vitals` lib, `sendBeacon`/`fetch` with token, `wppo_rum_config` global) |
| 43 | `src/lib/apiRequest.js` | 249 | `refreshNonce` (deduped `pendingRefresh`), `apiCall` (POST/GET, `X-WP-Nonce`, retry on `rest_forbidden`/`rest_cookie_invalid_nonce`, mutates `wppoSettings.settings`, signal support), `fetchRecentActivities`/`fetchServerRules` etc. |
| 44 | `src/lib/useNotice.js` | 74 | `useNotice` hook (`notice`/`notify`/`dismiss`, auto-dismiss timer, cleanup on unmount/re-notify) |
| 45 | `src/lib/util.js` | 33 | `handleChange(setSettings)` factory (text/checkbox/number branching) |
| 46 | `src/lib/litespeed.js` | 67 | Pure helpers `isLiteSpeed`/`effectiveMode`/`bannerProps` (no WP deps, tested in isolation) |
| 47 | `src/components/Dashboard.js` | 1327 | God component: 4 stat tiles, 3 cache-setting cards (page cache, CDN purge, logged-in roles), polling `image_job_status` (5-retry), 7 sub-panels via `wppo-stacked-cards` |
| 48 | `src/components/FileOptimization.js` | 2024 | God component: 5 sub-tabs (General/Minify/Delay/Combine/CDN), 15+ fields, `withNotification` wrapper, `wppo_inline_combined_css` filter note |
| 49 | `src/components/PreloadSettings.js` | 623 | Cache warm-up (preloadCache + sitemap), preconnect/preload fonts/CSS, speculation rules (mode/eagerness/exclude) |
| 50 | `src/components/ImageOptimization.js` | 975 | Lazy (native vs JS), `<picture>` wrap, WebP/AVIF, `excludeConvertImages`/`excludeLazy`/`excludeSizes`, `prioritizeLCPImages`/`autoPreloadLCP` |
| 51 | `src/components/DatabaseCleanup.js` | 644 | 9 types + `all`, `ConfirmDialog` per type, `get_counts` polling, `clean_all` vs individual, OPTIMIZATION badge |
| 52 | `src/components/ObjectCache.js` | 902 | Redis mode toggle (standalone/sentinel/cluster), host/port/password/TLS/compression, `status`/`ping`/`enable`/`disable`/`flush` |
| 53 | `src/components/PluginSetting.js` | 968 | Activity log (paginated), PageSpeed API key (redacted export), import JSON (allowlist), welcome dismiss |
| 54 | `src/components/common/*` | 48–182 | `Tooltip` (portal), `SwitchField`, `CheckboxOption`, `ConfirmDialog` (focus trap), `LoadingSubmitButton`, `FeatureCard`/`FeatureHeader`, `StatusBadge`, `MetricCard`, `NoticeBanner` (`role="alert"`), `ErrorBoundary` |
| 55 | `src/css/**/*.scss` | 3411 | Design system (`.wppo-` BEM-like, CSS vars, `respond-to(sm/md/lg/xl)` mixin, `_variables`/`_mixins`/`_base`/`_layout`/`_components/*`) |

Total reviewed: ~28k PHP + ~13k JS + ~3.4k SCSS (build output committed but not separately audited beyond entry points).

---

## 2. Architecture Findings

| # | Severity | Title | File:Line | Evidence | Impact | Recommendation | Confidence |
|---|----------|-------|-----------|----------|--------|----------------|------------|
| A-01 | **HIGH** | God class `Main` (2956 lines, 30+ responsibilities) | `includes/class-main.php:21,169,418,545,722,895,946,1093,1141,1197,1356,1419,1553,1620,1673,1836,1935,2135,2288,2384,2557,2640,2791` | `class Main` owns: constructor defaults (60+ keys, 3 in-memory backfills), `includes()` (manual requires), `setup_hooks()` (~40 `add_action`/`add_filter` registrations with 5 version-gated branches `is_wp63_plus`/`is_wp69_plus`), delay/defer parsing (`delayJSDefaultStrategy`/`delayJSIdleList`/`delayJSViewportList`/`delayJSPriority`/`delayJSIdleTimeout`), block-assets gating (`register_block_assets_filters` + `migrate_block_assets_setting`), upgrade routines (`maybe_fix_wp_cache`/`maybe_run_upgrades`/`maybe_run_version_upgrade`), cache invalidation (`on_settings_update` + `clear_all_cache` + `on_save_post_invalidate_cache`/`queue_used_css`), output buffers (`process_used_css_only`/`capture_template_start`/`emit_server_timing_header`), enqueue (admin + frontend + modules), asset minification (`minify_queued_styles`/`get_minifiable_css_path`/`is_file_minified`), `strip_static_query_strings` + `is_plugin_cache_url*` (2 variants), Google Fonts + preloading + speculation rules, per-page delay config. McCabe for `setup_hooks` alone >30 branches; `on_settings_update` 100 lines with 6 concerns. | **High.** Violates SRP; change to any one concern risks regressing others (e.g. adding a new preload setting requires touching same file as delayJS). Testability near-zero: instantiating `Main` triggers `get_option` + `Util::init_filesystem` + `new Admin_Notices` + `new Core_Tweaks` + `new Cron` unconditionally. Hard to unit-test any single responsibility in isolation. Readers must load entire file to understand one path. | **Decompose behind facades, not premature abstraction.** Extract 5 focused collaborators behind existing `Main` as facade: `Settings_Registry` (defaults + `cached_home_url` backfills), `Hook_Registrar` (version-gated `setup_hooks` with injected `wp_version`), `FrontEnd_Optimizer` (delay/defer/minify/query-strings/fonts), `Cache_Lifecycle` (`on_settings_update`/`maybe_run_version_upgrade`/`on_save_post*`), `Version_Gate` (central `is_wp63_plus`/`is_wp69_plus`/`inline_candidates_require_src`). Keep `Main` as orchestrator that composes them; do not add DI container. Each extracted class ~300–500 lines, individually testable via Brain Monkey. | High |
| A-02 | **HIGH** | God utility `Util` (643 lines, 10 unrelated concerns) | `includes/class-util.php:30,55,89,110,153,199,243,293,315,377,385,393,426,450,480,509,533,552,589,605` | `class Util` is static grab-bag: `prepare_cache_dir` (filesystem), `init_filesystem` (WP_Filesystem bootstrap), `get_local_path` (ABSPATH-bound URL→path), `get_js_css_minified_file` (counts), `get_image_mime_type`, `generate_preload_link` (echoes `<link>`), `process_urls`/`is_url_excluded`, `cached_home_url`/`cached_content_url` (per-blog static cache + `has_filter` bypass), `min_cache_*` (blog-scoped paths), `transient_key`, `get_role_hash`/`is_cache_eligible_for_current_user`, `sanitize_settings_recursively` (heuristic `stripos('url'|'cdn'|'origin')` → `esc_url_raw` etc.), `is_auto_sizes_available`. Callers `grep -rn Util::` → 23 files. | **High.** High fan-in (23 consumers) + low cohesion (filesystem + security + caching + sanitization + mime + HTML output). Changing `sanitize_settings_recursively` heuristic risks breaking URL handling elsewhere. `generate_preload_link` echoing makes Util hard to test without output buffering. Static methods hide dependencies (no injection). | **Split by cohesion, keep `Util` as deprecated facade.** Extract: `Filesystem` (`prepare_cache_dir`/`init_filesystem`), `Url` (`get_local_path`/`normalize_url`/`is_url_excluded`/`process_urls`), `Cache_Key` (`transient_key`/`cached_home_url`/`cached_content_url`/`min_cache_*`), `Settings_Sanitizer` (`sanitize_settings_recursively`), `Role_Hash` (`get_role_hash`/`is_cache_eligible`). Keep `Util` methods as `deprecated` proxies delegating to new classes (1-line each) so 23 call-sites need not churn at once. | High |
| A-03 | **HIGH** | Constructor does I/O + side-effects at file-load time (lifecycle) | `performance-optimisation.php:44` (`new Main()`) → `includes/class-main.php:169` (`$this->options = get_option('wppo_settings', defaults 60+ keys)` + 3 in-memory backfills at `259-291`) + `293-304` (`$this->includes()` + `new Image_Optimisation` + `new Google_Fonts` + `$this->setup_hooks()` + `Util::init_filesystem()` + `new Admin_Notices` + `new Core_Tweaks`) | Plugin bootstrap instantiates `Main` before `plugins_loaded` (file load, not hooked). Constructor unconditionally deserializes `wppo_settings` on every request (REST, cron, AJAX, CLI, cache-hit where `advanced-cache.php` would otherwise bypass WP). On `advanced-cache.php` cache-hit the whole stack still boots for the subsequent non-cached fallback. If `wppo_settings` is large or autoloaded incorrectly, adds ~1–2ms per request. | **Medium.** Wastes cycles on paths that never use settings (e.g. `DONOTCACHEPAGE` pages still pay `get_option`). Makes testing `Main` require stubbing `get_option` before construction. | **Lazy-load settings.** Store defaults as `private const DEFAULTS` and load via `get_options(): array { return $this->options ??= get_option('wppo_settings', self::DEFAULTS); }`. Move `setup_hooks()` to `plugins_loaded` (priority 0) or `init` so `advanced-cache.php` hits that bypass WP never pay the cost. Keep backfill logic in `get_options()` so in-memory defaults are computed once. | High |
| A-04 | **HIGH** | Global mutable singleton `wppoSettings` shared between PHP and SPA (global state) | `includes/class-main.php:1466` (`wp_localize_script('wppoSettings', ['apiUrl','nonce','settings'=>redacted, 'translations','themeColors','version','litespeed'=>get_info()])`) vs `src/lib/apiRequest.js:71-131` (`apiCall` mutates `wppoSettings.settings` globally on success: `wppoSettings.settings = response.data`) + `src/App.js:135-145` (`typeof wppoSettings !== 'undefined' ? wppoSettings?.settings ?? {} : {}` per render) + `src/components/Dashboard.js:122-131` (fallback `wppoSettings?.settings?.cache_settings`) | SPA has no store; every component reads `wppoSettings.settings[tab]` on mount and `apiCall` writes it globally. Concurrent `update_settings` for two tabs can race (last-write wins, earlier tab re-render sees foreign tab mutation). `wppoSettings` is also mutated by PHP `wp_localize_script` redaction vs JS mutation — truth diverges if page not reloaded. No encapsulation, no subscription. | **High.** Race on rapid tab saves; stale `cacheSettings` prop in Dashboard after FileOptimization save (requires reload to re-sync). Difficult to test: `setupTests.js` must mock `global.wppoSettings` before every suite, and mutation leaks between tests unless reset. | **Introduce minimal store, keep global as initial seed.** Add `src/lib/store.js` with `createStore(initialSettings)` exposing `getSettings`/`setSettings`/`subscribe`; `apiCall` writes store, `App.js` provides via React context, components `useStore()`. Keep `wppoSettings` as `initialData` only (read once on mount, never mutated). No external dependency (no Redux). | High |
| A-05 | **HIGH** | Setter injection for `Cache` optional deps creates partially-initialized object (dependency management) | `includes/class-cache.php:296-309` (`set_image_optimisation(Image_Optimisation)` / `set_google_fonts(Google_Fonts)`) vs `includes/class-main.php:473-475` (`$this->cache = new Cache($options); $this->cache->set_image_optimisation($this->image_optimisation); $this->cache->set_google_fonts($this->google_fonts)`) + `536-539` (second `new Cache` for `combineCSS` without prior deps: `if (!$this->cache) { $this->cache=new Cache(); set_* }`) + `includes/class-cache.php:1183` (`process_buffer_only`: `$image_optimisation = $this->image_optimisation ? $this->image_optimisation : new Image_Optimisation($this->options)` fallback) | Two construction paths for `Cache`; second path (combineCSS when `enableCache` off) creates deps eagerly but `process_buffer_only` also has fallback `new Image_Optimisation`/`new Google_Fonts` inline, meaning 3 places instantiate same deps. If caller forgets setters, `maybe_serve_next_gen_images` silently creates new instance with stale options (constructed from `Cache::$options` snapshot, not live `Main::$options`). | **Medium.** Bug risk: stale options snapshot in fallback path could serve wrong `excludeConvertImages`. Testability: `Cache` cannot be tested without either calling setters or triggering fallback branch. | **Constructor injection over setters.** Make `Cache::__construct(array $options, ?Image_Optimisation $image_optimisation=null, ?Google_Fonts $google_fonts=null)` and initialize in constructor (`$this->image_optimisation ??= new Image_Optimisation($options)`). Remove `set_*` methods (keep deprecated proxies for one release). Single construction path. | Medium |
| A-06 | **HIGH** | No abstraction boundaries: 36 concrete classes, 0 interfaces, 0 DI container, manual `require_once` per file (coupling) | `includes/class-main.php:387-407` (`includes()` does `require_once WPPO_PLUGIN_PATH.'vendor/autoload.php'` + 3 `file_exists` + `require_once` for `Server_Rules`/`LiteSpeed_Integration`/`Llms` + `\WP_CLI::add_command('wppo','PerformanceOptimise\Inc\WPPO_CLI_Command')` string) + `performance-optimisation.php:28-44` (also `require vendor/autoload`) + `composer.json:14-27` (`autoload.files` for `woocommerce/action-scheduler`, no PSR-4 for `PerformanceOptimise\Inc`) + `includes/*.php:1` each has `if (!class_exists(...))` guard | Plugin deliberately avoids Composer PSR-4 for its own classes (AGENTS.md: "Classes are manually loaded via Main::includes() + vendor/autoload.php (Composer for vendor packages only)"). Consequence: adding a new class requires editing `Main::includes()` with `file_exists` guard; forgetting guard triggers fatal on missing file. `WPPO_CLI_Command` registered by string `'PerformanceOptimise\Inc\WPPO_CLI_Command'` without `require`, relies on `class_exists` trigger — fragile if `WP_CLI` loaded before `Main`. No interface means `Cache` vs `Advanced_Cache_Handler` vs `Htaccess_Handler` cannot be swapped/mocked (e.g. test double for filesystem). | **Medium.** Violates Open/Closed; high coupling to concrete classes. Prevents unit testing `Main` without loading entire plugin. String-based CLI registration typo is silent (no autoloader error). | **Add classmap, keep manual as fallback.** Add `composer.json: { "autoload": { "classmap": ["includes/"], "files": ["woocommerce/action-scheduler/action-scheduler.php"] } }` so `vendor/autoload.php` alone loads all classes; keep `Main::includes()` as `require_once` fallback for installs without `composer install` but gate on `!class_exists`. Change CLI registration to `::class` constant (`WPPO_CLI_Command::class`) after ensuring file is required. No runtime behaviour change. | High |
| A-07 | **MEDIUM** | Inconsistent filesystem abstraction (mixed `Util::init_filesystem` vs global `$wp_filesystem` vs native `is_file`/`filesize`) | `includes/class-cache.php:544` (`new CSSMinifier`), `800-816` (`is_file($path)` + `filesize($path)` + `is_readable($path)` native, not via `WP_Filesystem`), `938` (`is_file($css_file_path)` native), `1134` (`$fs->get_contents` via `WP_Filesystem`), `1414` (`$fs->put_contents`) vs `includes/class-main.php:2791` (`fopen`+`flock`+`fgets` native for `is_file_minified`), `includes/class-cron.php:594-605` (`$wp_filesystem->exists` + `delete` for `mark_page_as_processed`), `includes/class-img-converter.php:755-771` (`file_exists` native for `get_img_path`), `includes/class-util.php:89` (`WP_Filesystem()` bootstrap) | Two filesystem stacks (`WP_Filesystem` abstraction for writes, native PHP for reads) behave differently on `direct` vs `ftpext` vs `ssh2` hosts. `is_file`/`filesize` bypass `WP_Filesystem` and may fail on FTP hosts where `WP_Filesystem_FTPext::exists` would succeed. `fopen`+`flock` in `is_file_minified` uses native handle, not `$fs`. `mark_page_as_processed` uses `WP_Filesystem` but `img_convert_cron` uses `realpath`+`file_exists` native. | **Medium.** FTP/SSH hosts get inconsistent behaviour (read succeeds but write fails or vice versa). Testability: native calls cannot be mocked via `WP_Filesystem` double. | **Consolidate on `WP_Filesystem` for all paths, keep native as fast-path only when `$fs` is `WP_Filesystem_Direct`.** Add `Util::is_file($path)`/`filesize`/`is_readable` helpers that delegate to `$fs` when available, else native. Replace bare `is_file`/`filesize`/`file_exists` in `Cache`/`Main`/`Img_Converter` with helpers. | Medium |
| A-08 | **MEDIUM** | Hidden temporal coupling: `Cache::inline_size_map` cache must be invalidated after `register_combine_css_path` (lifecycle) | `includes/class-cache.php:117` (`private ?array $inline_size_map = null`), `800-816` (lazy built on first `core_inline_budget_will_inline` call, iterates `wp_styles->queue`), `965-969` (`register_combine_css_path` does `wp_style_add_data('wppo-combine-css','path',$css_file_path)` then ` $this->inline_size_map = null` with comment "size map cached … is stale; rebuild") | Correct today, but invariant is implicit: any future mutation of `wp_styles` queue after `inline_size_map` is built will use stale sizes (e.g. plugin calling `wp_dequeue_style` after `combine_css`). Reader must know to null the map after any queue mutation. No encapsulation of `wp_styles` access. | **Low.** Stale map would cause `core_will_inline` misprediction (drift detection would fire, but wasted). Fragile if new code mutates queue. | **Encapsulate queue snapshot.** Change `core_inline_budget_will_inline` to accept `array $queueSnapshot` param instead of reading `global $wp_styles` implicitly, and build `inline_size_map` from snapshot. Caller `combine_css` passes `array $styles` already available. Removes global read + invalidation need. | Medium |
| A-09 | **MEDIUM** | SPA `App.js` orchestrator mixes routing, data fetching, theming, focus-trap, resize (god component) | `src/App.js:76` (`App` 527 lines, state: `activeTab`+`transition`+`mobileMenuOpen`+`recentActivities`+`serverRules`+`serverRulesError`+`rulesRetryTrigger`+`ccssStatus`+`ccssError`+`ccssRefreshTrigger` + 3 refs), `134-195` (`renderContent` builds `components` object with 7 lazy entries + prop drilling `wppoSettings.settings[tab]` per tab), `200-210` (resize listener), `212-257` (focus trap with `sidebar.querySelectorAll`), `259-282` (theme CSS vars injection), `284-387` (`useEffect` with 3 concurrent fetchers `fetchActivities`+`fetchRules`+`fetchCcssStatus`, shared `AbortController`, `hasFetched*` refs, `rulesRetryTrigger`/`ccssRefreshTrigger` state) | 527 lines with 3 concerns (navigation, layout, data). `useEffect` deps `[activeTab, recentActivities.length, serverRules, rulesRetryTrigger, ccssRefreshTrigger]` cause refetch on every tab switch (guarded by `hasFetched*` refs — incorrect React pattern, refs don't trigger re-render but effect still runs). `renderContent` recreates `components` object on every render (no `useMemo`) so `Suspense` remounts lazies unnecessarily. | **Medium.** Hard to test routing without mocking 3 fetches + theme + focus trap. `hasFetched*` ref pattern is fragile (React strict-mode double-invoke will double-fetch). Performance: `renderContent` allocation + `fetchRules`/`fetchCcssStatus` run on every `activeTab` change even when tab is not FileOptimization. | **Extract hooks, memoize.** Move each fetcher to `useServerRules()`/`useCcssStatus()` hooks (own `AbortController`, no `hasFetched` refs, `useEffect` with empty deps + `enabled` flag). Memoize `sidebarItems` already done (`93-132`), add `useMemo` for `components` map keyed on `activeTab` only, or switch to `React.lazy` + `Routes` pattern. Keep `App` as layout shell. | Medium |
| A-10 | **MEDIUM** | Dashboard god component (1327 lines, 8 sub-panels, 3 save paths, polling) | `src/components/Dashboard.js:60` (`Dashboard` 1327 lines, imports 7 sub-panels + 6 common components), `96-120` (4 `useState` blocks for page-cache + logged-in + CDN + polling), `122-131` (prop drilling `cacheSettings` fallback to `wppoSettings`), `216-244` (`fetchDbCounts` + `useEffect`), `253-322` (`pollJobStatus` with `pollingRef` timeout + `pollRetryRef` 5-retry), `332-378` (`onClearCache`), `380-455` (`optimizeImages` branching sync vs `background`), `504-640` (3 `save*Settings` with duplicated `currentSettings = wppoSettings.settings?.cache_settings ?? cacheSettings` + `apiCall('update_settings')` + `notify`) | 1327 lines is 2× largest PHP class per file but in JS where bundle is committed; single component owns stats, cache, CDN, logged-in, polling, image, activities. Changing one stat tile risks regressing poller. `save*Settings` triplicates same `currentSettings` dance (DRY violation already noted in A10 D-04/D-05). `dbCounts` vs `totalCacheSize` vs `imageInfo` share one `state` object but also separate `useState` for cache toggles — inconsistent state shape. | **Medium.** Same SRP/testability cost as Main god. Bundle size: Dashboard chunk is largest (`build/tab-dashboard.js`) and eagerly imports 7 panels even if stat tiles are above fold only. | **Decompose, keep Dashboard as composition.** Extract: `CacheStatsCard`, `PageCacheCard`, `CdnPurgeCard`, `LoggedInCacheCard` (each owns its `useState` + `save*`), `ImagePoller` hook. Dashboard becomes ~200-line composition importing extracted cards. Already partially done (`ImageOptimizationCard`/`RecentActivityCard`/`SystemInfo` are separate) — finish the split for cache cards. | High |
| A-11 | **MEDIUM** | Circular-ish dependency via `Advanced_Cache_Handler::create()` baked state | `includes/class-advanced-cache-handler.php:142-194` (`create()` bakes `var_export(home_url(), true)` + `md5(site_url)` fallback + `COOKIEHASH` + `wppo_role_hash` logic into generated `advanced-cache.php` string), `includes/class-main.php:911` (`maybe_run_version_upgrade` calls `Advanced_Cache_Handler::create()` then `Cache::clear_cache()`), `includes/class-cache.php:1414` (`prepare_cache_dir` creates dirs that `Advanced_Cache_Handler::create()` drop-in later reads), `includes/class-cron.php:301` (`schedule_page_cron_jobs` calls `Cache::clear_cache` indirectly via `mark_page_as_processed`) | Generated drop-in is code-generation (string with `var_export` values) — state baked at `create()` time (domain, `COOKIEHASH`, `wppo_role_hash` salt via `wp_salt()` not yet? actually via `get_role_hash` salt). If `home_url` filter changes later, baked drop-in is stale until next `create()`. `Main::on_settings_update` at `978` calls `Advanced_Cache_Handler::create()` without checking return — if `create()` fails (filesystem), `wppo_version` still updated at `936`, leaving stale drop-in. | **Medium.** Stale drop-in serves wrong domain or wrong role hash after site URL change (multisite domain mapping). Not a circular *import* cycle but a temporal coupling: `create()` must be called before `clear_cache` or vice versa? Order matters. | **Make drop-in stateless where possible.** Move `home_url`/`COOKIEHASH` resolution to runtime inside drop-in (read `$_SERVER['HTTP_HOST']` + `wp_salt` file?), or at least re-bake on `update_option_home`/`update_option_siteurl`. Check `Advanced_Cache_Handler::create()` return in `maybe_run_version_upgrade` and do not bump `wppo_version` on failure (already done: `if (!create()) return` at `926` — correct, keep). Document order: `create` before `clear_cache`. | Medium |
| A-12 | **MEDIUM** | Inconsistent error handling: silent swallows vs `WP_Error` vs `notify` vs `console.error` | `includes/class-cron.php:447-458` (`process_url` swallows `WP_Error` + `>=400` behind `if (WP_DEBUG) error_log` — silent in prod), `559-571` (`load_page` same), `617-681` (`img_convert_cron` silently `continue` on `realpath` mismatch, no log), `includes/class-cache.php:1394` (`maybe_mark_page_not_cacheable` returns void even if `put_contents` fails at `1417`), `includes/class-rest.php:1030` (`handle_object_cache ping` logs but returns generic `Redis connection failed` without details), `src/lib/apiRequest.js:71-131` (`apiCall` retries nonce once then throws generic `Error` with `data.message ?? 'Request failed'`), `src/components/Dashboard.js:227` (`fetchDbCounts catch console.error + notify`), `src/App.js:301` (`fetchActivities catch console.error only`) | Three PHP error surfaces (`WP_Error`, `false`, silent) + two JS (`throw` vs `notify`). Cron callers cannot distinguish `WP_Error` vs `0` vs `false` from `Queue_Scan`. Prod operators get no signal that preload failed (requires `WP_DEBUG`). `Cache::maybe_mark_page_not_cacheable` failure is best-effort but caller `is_not_cacheable` returns `true` anyway so caller thinks page is not cacheable but marker not on disk — drop-in still serves stale. | **Medium.** Silent cron failures mask site issues (preload appears to run but pages not warmed). Inconsistent JS `notify` vs `console.error` means some failures show banner, some only console. Hides bugs during manual testing. | **Normalize.** PHP: make cron helpers return `WP_Error` and have caller decide `WP_DEBUG` logging + `Log::add` (already used for DB cleanup). JS: ensure every `catch` calls `notify(error)` not just `console.error` (App.js `fetchActivities` should `set*Error` state for banner). Add `Log::add` for `maybe_mark_page_not_cacheable` failure. | High |
| A-13 | **MEDIUM** | Version-gated branching scattered with TODO markers (inconsistent pattern) | `includes/class-main.php:435` (`$is_wp63_plus = version_compare($wp_version,'6.3-alpha','>=')` + `439` `$is_wp69_plus = version_compare($wp_version,'6.9-alpha','>=')`), `452-464` (delay vs defer vs fetchpriority), `476-484` (cache `wp_template_enhancement_output_buffer` vs `template_redirect`), `498-506` (used-CSS standalone), `509-524` (LCP prioritization), `735-753` (`register_block_assets_filters` bool param), `1158,1239,1248` (`TODO(#553)`), `511,357` (`TODO(#624)`), `includes/class-cache.php:373,593,1158` (`TODO(#553)`), `includes/class-image-optimisation.php:357,1907` (`TODO(#624)`) | Same `version_compare($GLOBALS['wp_version'],'6.9-alpha','>=')` repeated in 5 places in Main + 1 in Cache, each with slightly different guard (`function_exists('wp_should_output_buffer_template_for_enhancement') && $is_wp69_plus` vs bare `$is_wp69_plus`). Adding a new WP version gate (e.g. 7.0 for `inline_candidates_require_readable`) requires editing 6 sites. TODO comments reference issues #553/#624 but no `#406` etc. — tech debt. | **Medium.** Drift risk: new gate added in one place but not others (e.g. `emit_server_timing_header` requires `wp_should_output_buffer_template_for_enhancement` but no `is_wp69_plus` check at `492`). Scattered `version_compare` stringly-typed. | **Centralize version gate.** Add `class Wp_Version { const MIN_SUPPORTED='6.3'; public static function isAtLeast(string $v): bool { return version_compare($GLOBALS['wp_version'] ?? get_bloginfo('version'), $v, '>='); } public static function inlineRequiresSrc(): bool { return self::isAtLeast('6.3'); } public static function hasTemplateEnhancement(): bool { return function_exists('wp_should_output_buffer_template_for_enhancement') && self::isAtLeast('6.9-alpha'); } }`. Replace scattered `version_compare` with calls. Keep TODOs but reference single gate. | Medium |
| A-14 | **LOW** | Naming inconsistencies: package vs slug vs prefix vs textdomain | `composer.json:2` (`name: "performance-optimisation"` hyphen), `performance-optimisation.php:3` (`Plugin Name: Performance Optimisation`), `includes/class-main.php:13` (`package PerformanceOptimise\Inc` — missing `i`), `performance-optimisation.php:18` (`Text Domain: performance-optimisation`), JS `import PerformanceAudit from './PerformanceAudit'` (camelCase), CSS `.wppo-` (4-char), PHP `wppo_` (option `wppo_settings`, transient `wppo_cache_size`, hook `wppo_page_cron_hook`, filter `wppo_exclude_defer_js`), constants `WPPO_VERSION`/`WPPO_PLUGIN_PATH` | Three spellings: `performance-optimisation` (slug, composer, textdomain), `PerformanceOptimise` (namespace, missing `i` vs `PerformanceOptimisation`), `wppo` (prefix). Namespace typo `PerformanceOptimise` (no `i`) is inconsistent with plugin slug `performance-optimisation` (with `i`). Not a bug (autoload is manual) but confusing for contributors. JS components use PascalCase, PHP classes `Main`/`Cache` generic names that collide with common nouns. | **Low.** Confuses `grep` (must search both `performance-optimisation` and `performance-optimise`). Namespace typo would break if PSR-4 were ever enabled. Generic class names `Cache`/`Main` risk collision with other plugins in global `class_exists` check (guarded by namespace, so low risk). | **Keep, document, add alias.** Do not rename namespace (breaking). Add `composer.json: { "autoload": { "psr-4": { "PerformanceOptimise\\Inc\\": "includes/" } } }` plus `class_alias` for `PerformanceOptimisation\Inc\Main` → `PerformanceOptimise\Inc\Main` so both spellings work. Lint new classes against generic names (prefer `Wppo_Cache` if ever adding new). | High |
| A-15 | **LOW** | Overly complex functions (McCabe >15, 80+ lines) | `includes/class-cache.php:365` (`combine_css` 193 lines, 6 nested loops, 4 early returns, 2 branches for cached vs fresh, inline-budget check), `includes/class-cache.php:800` (`core_inline_budget_will_inline` 78 lines, double simulation, `uasort`, cumulative budget), `includes/class-image-optimisation.php:576` (`maybe_serve_next_gen_images` 126 lines, Tag Processor vs regex dual branches), `includes/class-image-optimisation.php:1368` (`process_img_tag` ~250 lines, Native lazy vs JS lazy vs excluded, 3 `WP_HTML_Tag_Processor` passes), `includes/class-main.php:946` (`on_settings_update` 92 lines, 6 tab comparisons + htaccess + nextgen + google-fonts), `includes/class-cron.php:473` (`get_sitemap_urls` 71 lines, `while to_fetch`, deadline, index vs leaf branching), `includes/class-util.php:605` (`sanitize_settings_recursively` 38 lines, 6 `if/elseif` heuristic branches), `src/components/FileOptimization.js:2024` (single file 2024 lines, `render` with 5 conditional sub-tabs), `src/lazyload.js:1025` (1025 lines, `initLazy` + `observerCallback` + `mutationCallback` + `delayLoad` with 3 strategies) | **Medium.** `combine_css` + `process_img_tag` are hardest to review; bug in one skip rule must be mirrored in `get_combined_handles` (already flagged D-12). `sanitize_settings_recursively` heuristic `stripos('url'|'cdn'|'origin')` could misclassify a future `excludeUrlsThatAreNotReallyUrls`. `lazyload.js` 1025 lines vanilla JS with no tests for idle/viewport branches. | Extract helpers (already partially done: `get_combined_handles`, `is_core_block_asset`, `get_styles_inline_limit`). No further split needed beyond A-01/A-02 facades. Add complexity lint (e.g. `phpmd` cyclomatic) to keep new functions <15. | Medium |

---

## 3. Quality Findings

| # | Severity | Title | File:Line | Evidence | Impact | Recommendation | Confidence |
|---|----------|-------|-----------|----------|--------|----------------|------------|
| Q-01 | **HIGH** | Mutable static caches leak between tests / requests (testability + lifecycle) | `includes/class-util.php:37` (`private static array $home_url_cache = array()`), `450` (`static $cache = array()` in `cached_content_url`), `includes/class-cache.php:98` (`private static bool $inline_drift_logged = false`), `includes/class-cron.php:277` (`Util::transient_key('wppo_preload_cron_lock')` + static not, but `wppo_preload_cron_offset` option + `wppo_web_vitals_last_rescan` option are global) vs `tests/php/bootstrap.php` (defines `WPPO_Test_Bootstrap` trait but does not reset statics between tests; `Util::reset_cached_home_urls()` exists at `44` but only called in 2 tests) | `Util::$home_url_cache` and `cached_content_url::$cache` are `static` per-process. In `phpunit` run with `processIsolation=false` (default `phpunit.xml.dist`), one test's `home_url` mock leaks to next test if `has_filter('home_url')` not re-mocked. `Cache::$inline_drift_logged` is static per-process, so second test that triggers drift will not log (guard `if (self::$inline_drift_logged) return`). `Cron::web_vitals_rescan_cron` reads `get_option('wppo_web_vitals_last_rescan')` but no transient reset. Requires `Util::reset_cached_home_urls()` manual call — forgettable. | **High.** Flaky tests: order-dependent suite (run `UtilCachedHomeUrlTest` before `UtilGetLocalPathTest` may hide bug). `inline_drift_logged` static makes second drift test a false-pass. | **Reset statics in bootstrap `tearDown`.** Add `Util::reset_cached_home_urls()` + `Cache::reset_inline_drift_logged()` (add if missing; or make `$inline_drift_logged` non-static and inject) to `tests/php/bootstrap.php` `WPPO_Test_Bootstrap::tearDown()` or `setUp()`. Keep `has_filter` bypass but ensure tests always call `Brain\Monkey\Filters\expectAdded` / `has` with correct filter name. | High |
| Q-02 | **MEDIUM** | `sanitize_settings_recursively` heuristic `stripos` over-matches (edge case) | `includes/class-util.php:632-638` (`elseif (stripos($safe_key,'url')!==false \|\| stripos($safe_key,'cdn')!==false \|\| stripos($safe_key,'origin')!==false) $sanitized[$safe_key]=esc_url_raw($value)`; next `elseif (stripos($safe_key,'exclude')!==false \|\| stripos($safe_key,'preload')!==false \|\| stripos($safe_key,'delay')!==false \|\| stripos($safe_key,'list')!==false) sanitize_textarea_field`) | New setting `excludeUrlToKeepJSCSS` matches both `url` and `exclude` — first branch wins (`esc_url_raw` on a textarea value containing `a.js\nb.js\n` → mangles newlines to `%0A`? Actually `esc_url_raw` per code at `633` would be wrong; but real key is `excludeUrlToKeepJSCSS` → `stripos('exclude')` also true, order matters. Current code orders `url/cdn` before `exclude/preload`, so `excludeUrlToKeepJSCSS` → `esc_url_raw` not `sanitize_textarea_field`. Verified: `class-main.php:1673` stores `excludeUrlToKeepJSCSS` as textarea (newline-delimited handles/URLs). Sanitizing via `esc_url_raw` collapses newlines. | **Medium.** Newline-delimited handle lists that happen to contain `url` substring would be mis-sanitized (e.g. future `delayUrlList`). Existing `excludeUrlToKeepJSCSS` is affected today but `esc_url_raw` on `a.js\nb.js` returns `a.js` (only first URL) — truncation. Check: `esc_url_raw('a.js\nb.js')` → `a.js`? Actually `esc_url_raw` strips newline then validates single URL, so multiline → truncated. | **Order heuristic by specificity.** Check `exclude/preload/delay/list` before `url/cdn/origin`, or use exact allowlist for URL keys (`cdnURL`, `preconnectOrigins`, `cloudflareZoneId` etc.) instead of substring. Add unit test for `excludeUrlToKeepJSCSS` multiline sanitization. | High |
| Q-03 | **MEDIUM** | `Cache::is_not_cacheable` couples predicate with side-effect (command-query separation) | `includes/class-cache.php:1441` (`private function is_not_cacheable(): bool { if (DONOTCACHEPAGE) { $this->maybe_mark_page_not_cacheable(); return true; } … }`) + `1394` (`maybe_mark_page_not_cacheable` writes `.wppo-no-cache` marker + deletes stale files) + doc `1428` ("Note: for pages opted out via DONOTCACHEPAGE this also records the decision on disk … This coupling is intentional") | Predicate `is_not_cacheable()` is called from 5 sites (`combine_css:377`, `start_output_buffer:1159`, `process_buffer_for_cache:1228`, `stash_cache:1248`, `maybe_store_cache:1703`). On every cache-miss render of a `DONOTCACHEPAGE` page, predicate writes marker + deletes files (I/O). Reader expects pure predicate. Violates CQS; makes testing `is_not_cacheable` require filesystem mock even when only predicate logic is under test. | **Medium.** Side-effect hidden in `if (is_not_cacheable()) return` guard; filesystem failure in `maybe_mark_page_not_cacheable` is silent (void return) so caller cannot know marker not written. Drop-in will keep serving stale file until next `is_not_cacheable` render succeeds. | **Keep coupling but make explicit.** Rename to `is_not_cacheable_and_maybe_mark(): bool` or extract `ensure_not_cacheable_marker(): void` and call explicitly at 2 render entry points (`process_buffer_for_cache` + `start_output_buffer`) instead of inside predicate. Keep doc, add return value for marker success so caller can `Log::add` on failure. | High |
| Q-04 | **MEDIUM** | `Rest::update_settings` preserves stale `pagespeed_api_key`/`server_timing_enabled`/`auto_rescan` without validation (edge case) | `includes/class-rest.php:453-466` (`if ('performance_audit'===tab && !isset($params['settings']['pagespeed_api_key']) && isset($options['performance_audit']['pagespeed_api_key'])) $sanitized_settings['pagespeed_api_key']=$options['performance_audit']['pagespeed_api_key'];` + same for `server_timing_enabled` at `459` + `auto_rescan` at `464`; preservation copies raw `options` value, not `sanitized_settings`) | Preservation copies unsanitized `options` value (e.g. if DB was corrupted with non-string, it is copied verbatim). If plugin previously stored `server_timing_enabled=true` and new tab omission preserves it, operator cannot unset it without explicitly sending `server_timing_enabled: false` — no UI toggle exists per comment "no UI toggle exists yet". Same for `auto_rescan`: omitted → preserved old value, not default `''`. | **Low.** Corrupted DB value propagated; cannot reset via UI without manual DB edit. | **Sanitize preserved values.** Apply `sanitize_text_field`/`(bool)` guard on preserved copy, or set default (`''`/`false`) when omitted. Add REST test for omission preservation. | High |
| Q-05 | **MEDIUM** | `Image_Optimisation::post_process_*` triple regex scan of same buffer (performance + fragility) | `includes/class-image-optimisation.php:262` (`post_process_placeholders` regex `#<img\b[^>]*\sdata-src=…`), `299` (`post_process_img_dimensions` same regex), `356` (`post_process_auto_sizes` broader `#<(img\|source)\b…(?:data-src\|data-srcset)=…`), each `preg_replace_callback` walks whole buffer | 3 full buffer scans for same `data-src` images to inject placeholder vs width/height vs `data-sizes="auto"`. Each recompiles regex and walks buffer linearly — 3× cost on large posts (e.g. 100-image gallery). Order dependency not enforced (dimensions must precede auto-sizes, but no check). Regex `[^>]*\sdata-src` misses `<img data-src="…">` where `data-src` is first attr (no leading `\s`? Actually `<img\b` + `[^>]*` includes `data-src` even without `\s`? Pattern `#<img\b[^>]*\sdata-src=` requires whitespace before `data-src`, so `<img data-src="x">` matches (space after `img`), ok. But `<img class="a"data-src="x">` (missing space) would not). | **Low.** Not a correctness bug today, but 3-pass cost is linear ×3; large pages pay. Missed attribute spacing edge. | **Keep 3 passes (each has distinct concern) but document order + combine placeholder+dimensions into one pass if budget.** Add comment "Order: placeholders → dimensions → auto-sizes; dimensions must precede auto-sizes so width/height present". Fix regex to `\bdata-src=` without `\s` prefix if strict HTML parsing needed, or keep Tag Processor path as primary. | Medium |
| Q-06 | **MEDIUM** | `Cron::get_sitemap_urls` uses `wp_remote_get` with 5s timeout inside `while to_fetch` loop without `wp_remote_retrieve_response_code` check (error handling) | `includes/class-cron.php:496-498` (`$response = wp_remote_get($current, ['timeout'=>5]); if (is_wp_error($response)) continue; $body = wp_remote_retrieve_body($response); if (''===$body) continue;`) — no `wp_remote_retrieve_response_code` check, so HTTP 404/500 with empty-ish body silently skipped; HTTP 200 with XML parse failure also skipped. `508` (`preg_match_all '#<loc>\s*([^<]+?)\s*</loc>#i'`) — no `wp_remote_retrieve_headers` check for `content-type: application/xml`. | **Low.** 404 sitemap index child (e.g. deleted custom post type sitemap) silently ignored, but parent already baked `to_fetch` list — missing URLs never warmed, no log. Operators see preload "enabled" but custom endpoints not warmed, no signal. | **Check status code.** Add `if (200 !== wp_remote_retrieve_response_code($response)) continue;` and `if (WP_DEBUG) error_log('WPPO sitemap fetch failed: HTTP '.code.' for '.$current)` so operators can diagnose. Already filtered `loc_host !== home_host` correctly. | High |
| Q-07 | **MEDIUM** | `Main::is_file_minified` heuristic false-positive on long data-URI URLs (edge case) | `includes/class-main.php:2791-2861` (`is_file_minified`: opens file, reads 50 lines via `fgets`, `avg_line_length > 500 css / 1000 js` → minified; caches via `wp_cache_set(cache_key, (int)is_minified, 'wppo_minify_check', HOUR)`; `filesize` already called before `fopen`) | Formatted CSS with long data URIs (e.g. `background: url(data:image/svg+xml;utf8,<svg … hundreds chars>)`) exceeds 500 avg and is misclassified as minified, skipping minify (suboptimal but not wrong). Shared `LOCK_SH` before `fclose` correct. Thresholds are heuristics; comment says "common for small lib" at `2840`. | **Low.** Suboptimal minify skip, not correctness. Cache key `md5(path)` without mtime, so file changed within hour still returns old `is_minified` from `wp_cache`. | **Document heuristic, include mtime in cache key.** Change `cache_key = md5(path . '|' . filemtime(path))` so edit invalidates. Already `filesize` called — add `filemtime` to key. | Medium |
| Q-08 | **LOW** | `Database_Cleanup` batched `LIMIT 1000` loop duplicates error handling (repeated logic) | `includes/class-database-cleanup.php:86` (`clean_revisions` 44 lines: `do { $post_ids = $wpdb->get_col("SELECT … LIMIT 1000"); if ($wpdb->last_error) return false; if (empty) break; $placeholders=implode(',',array_fill(…)); $wpdb->query("DELETE FROM $wpdb->postmeta … IN ($placeholders)"); $wpdb->query("DELETE FROM $wpdb->posts … IN ($placeholders)"); $deleted+=count } while (count>=1000)`), `267` (`clean_auto_drafts`), `321` (`clean_trashed_posts`), `373` (`clean_spam_comments`), `425` (`clean_trashed_comments`) — same 45-line scaffold, only `WHERE` varies | **Low.** 5× copy of `LIMIT 1000` batch + placeholder generation + `last_error` check. Fix to placeholder generation must be applied 5 times (already flagged as D-07 in A10). Also `clean_expired_transients`/`clean_orphan_postmeta` have bespoke logic not batched. | Keep 5 copies (each has distinct table/where) or extract `batch_delete_by_ids(string $select_sql, string $meta_delete_sql?, string $rows_delete_sql): int|false` helper that takes table/where fragment. Low priority. | Medium |
| Q-09 | **LOW** | `Main::setup_hooks` registers `wp_resource_hints` + `add_preload_prefetch_preconnect` both for preconnect (inconsistent pattern) | `includes/class-main.php:672` (`add_action('wp_head', add_preload_prefetch_preconnect,1)` + `add_action('wp_head', add_speculation_rules,0)` + `add_filter('wp_resource_hints',10,2)`) vs doc at `672-674` ("keep preload vs resource-hints separation (preload needs `as`/`type`, hints need core dedup)") | `add_preload_prefetch_preconnect` handles `preload fonts/CSS` via `Util::generate_preload_link` (echo), while `add_resource_hints` handles `preconnect`/`dns-prefetch` via `wp_resource_hints` filter. Separation is correct per doc, but reader must understand why `preconnectOrigins` are not in `Util::generate_preload_link`. `wppo_resource_hints` not used. | **Info.** No bug; pattern is intentional. Ensure future `preconnect` added via `wp_resource_hints` not via `generate_preload_link` (which lacks dedup). | Keep with doc; add comment in `add_preload_prefetch_preconnect` "preconnect here is for legacy; prefer wp_resource_hints for new preconnect". | High |
| Q-10 | **LOW** | `Rest` `VALID_TYPES` + `METHOD_MAP` + `TABLE_MAP` triplication (repeated logic / inconsistent patterns) | `includes/class-rest.php:796` (`$valid_types = ['revisions','auto_drafts','trashed_posts','spam_comments','trashed_comments','expired_transients','orphan_postmeta','unattached_media','oembed_cache','all']`), `847` (`$method_map 9 entries: revisions→clean_revisions_advanced … oembed_cache→clean_oembed_cache`), `includes/class-database-cleanup.php:788` (`$methods` in `clean_all` same 9), `843` (`$methods` in `auto_clean` 7 entries, omits `unattached_media`/`oembed_cache` without comment), `includes/class-abilities.php:466` (same map) | Adding new cleanup type (`NEXT`: `unattached_media`, `oembed_cache`) required editing 4 maps (Rest valid + Rest method + DB clean_all + Abilities). `auto_clean` omits 2 types intentionally? Not documented why daily auto-clean excludes unattached media/oembed cache while `clean_all` includes. `Database_Cleanup::TABLE_MAP`/`METHOD_TO_TYPE` are single-source but not used by Rest/Abilities. | **Low.** Drift if new type added to one map not others (CLI `database` subcommand at `class-wppo-cli-command.php:234` uses simple `clean_revisions` not `clean_revisions_advanced` — intentional?). | **Single source.** Expose `Database_Cleanup::ALL_METHODS`/`VALID_TYPES` constants and have `Rest`/`Abilities`/`WPPO_CLI_Command` derive via `Database_Cleanup::get_method_map()`; document why `auto_clean` excludes 2 types or include them. | Medium |
| Q-11 | **LOW** | `Performance-optimisation.php` hard-codes `WPPO_VERSION '1.9.0'` duplicate of header (naming / versioning) | `performance-optimisation.php:7` (`Version: 1.9.0` header) vs `28` (`define('WPPO_VERSION','1.9.0')`) + `includes/class-activate.php` re-reads plugin header via `get_file_data` for version? Actually `class-activate.php:??` uses `WPPO_VERSION` constant, not header. `class-main.php:911` (`maybe_run_version_upgrade` compares `get_option('wppo_version','')` vs `WPPO_VERSION`) | Header bump without constant bump → upgrade gate `version_compare(installed, WPPO_VERSION, '>=')` stale; `wppo_version` option never advances despite new code. Already flagged in A01 P02 as INFO. Plugin ships with `Tested up to: 7.1` non-existent WP version (A01 P01). | **Low.** Version drift not caught by CI (no check). | **Source `WPPO_VERSION` from header via `get_file_data(__FILE__)` once, or generate at `npm run build`/`composer release`. Add CI check that `WPPO_VERSION` === header `Version`. | High |
| Q-12 | **LOW** | CSS/SCSS design tokens partially duplicated (inconsistent pattern) | `src/css/base/_base.scss:27-43` (`.wppo-mt-8`…`wppo-mb-24`, `.wppo-text-small`/`wppo-text-13` both `font-size:13px`), `src/css/components/_card.scss:92` (`.wppo-danger-zone { border-left:4px solid #ef4444; background:#fef2f2 }`) vs `src/components/PluginSetting.js:880` (`style={{borderLeft:'4px solid #ef4444', background:'#fef2f2'}}` hard-coded hex instead of `var(--wppo-danger)`/`var(--wppo-error-bg)`), `src/css/abstracts/_mixins.scss:20-30` (`flex-center`/`truncate` defined but never `@include`d) | `wppo-text-small` vs `wppo-text-13` exact duplicate (13px). Inline `style` bypasses `.wppo-danger-zone` class (defined but not used where it should be). `flex-center`/`truncate` dead mixins. Already flagged X-04/X-06 in A10. Not a behaviour bug but design-system inconsistency. | Replace hard-coded `#fef2f2`/`#ef4444` with `className="wppo-danger-zone"` and remove inline `style` at `PluginSetting.js:880`; deprecate `wppo-text-13` alias or keep with comment. Remove or keep `flex-center`/`truncate` as library (document "for future use"). | Medium |
| Q-13 | **LOW** | JS `handleChange(setSettings)` factory wiring repeated per field (repeated logic) | `src/lib/util.js:1-33` (`handleChange` factory) used as `onChange={handleChange(setSettings)}` in `src/components/FileOptimization.js` (~15 fields), `ImageOptimization.js`, `DatabaseCleanup.js`, `PreloadSettings.js`, `ObjectCache.js`, `PluginSetting.js` each repeats same import + wiring | Factory is DRY, but per-field wiring `onChange={handleChange(setSettings)}` ×15 in FileOptimization is boilerplate. Not a bug. Common components `SwitchField`/`CheckboxOption` already wrap field UX. | **Info.** Keep factory; per-field wiring is idiomatic. Optional: `useFormState(initial)` hook that returns `[settings, handleChange, setSettings]` would halve boilerplate but not load-bearing. | High |
| Q-14 | **LOW** | `Cache::count_cached_pages` counts only `index.html` exact, misses variants (edge case) | `includes/class-cache.php:2165-2185` (`count_cached_pages` iterates `dirlist` and `if ('index.html' === file['name']) count++`, not `index-{hash}.html` variants nor `index.html.gz/br`) | When `enableLoggedInCache` on, `index-{hash}.html` variants not counted in `get_cache_stats()['cached_pages']` used by Dashboard stat tile (shows fewer pages than exist). Gzip/br siblings intentionally not counted (they are encodings, not pages) — correct. | **Info.** Dashboard `Cache Size` vs `Cached Pages` mismatch for logged-in cache. | Count `preg_match('/^index(-[a-f0-9]{12})?\.html$/', name)` for `index*.html`. Keep gzip/br excluded. | Medium |
| Q-15 | **LOW** | `uninstall.php` misses newer option keys (incomplete cleanup) | `uninstall.php:32-48` (deletes 16 options explicitly) but does not delete `wppo_web_vitals_trends`, `wppo_pagespeed_*` transients, `wppo_used_css_*`, `wppo_ccss_*`, `wppo_front_page_lcp_*`, `wppo_block_assets_migrated`, `wppo_web_vitals_last_rescan`, `wppo_last_db_cleanup`, `wppo_preload_cron_offset` etc. introduced in `class-pagespeed.php`/`class-used-css.php`/`class-cron.php`/`class-critical-css.php` | Leaves orphaned `wp_options` rows (autoload `no` but still bloats). `delete_option('wppo_settings')` already clears main option; remaining `wppo_*` options not deleted. Transient sweep at `93` handles transients with `blog_id_` prefix but not options. | Keep explicit list but audit `get_option`/`set_transient`/`update_option` keys across `includes/` and add to cleanup; or iterate `LIKE 'wppo_%'` (but `LIKE` delete is heavy on large multisite). Prefer explicit list + comment referencing `grep -rn wppo_ includes/`. | Medium |
| Q-16 | **INFO** | `Main::deferred_handles` O(1) map correct but `apply_module_loading_strategies` exclusion uses `process_urls` (stringly) on module ids | `includes/class-main.php:112` (`private array $deferred_handles = array()` keyed by handle for O(1) `isset`), `1620-1661` (`apply_module_loading_strategies` collects `get_print_queue`→`registered`→`all` fallback, then `set_in_footer`+`set_fetchpriority('low')` per id, skipping `excludeDeferJS` via `Util::process_urls((string)excludeDeferJS)` — but module ids are not URLs, so `process_urls` split on `\n` may mis-split ids) | Module `excludeDeferJS` entered as `wppo-lazyload` handle (not URL) but checked via URL-oriented helper; works today because `process_urls` just trims/splits, but semantic mismatch. `deferred_handles` map is correct pattern for defer. | Align: for modules, match exact id (`in_array($id, $this->exclude_defer_js, true)`) not via URL helper. Document that `excludeDeferJS` textarea accepts both handles and URL substrings. | Medium |
| Q-17 | **INFO** | `Cron::schedule_cron_jobs` reads `get_option('wppo_settings')` twice (`options` + `options_llms`) | `includes/class-cron.php:114` (`$options = get_option('wppo_settings', array())` for preload/img/database/web_vitals) + `141` (`$options_llms = get_option('wppo_settings', array())` for llms) | Harmless double read (WordPress `wp_cache_get` hits on second), but inconsistent variable naming (`options` vs `options_llms`). `schedule_cron_jobs` runs on `init` every request, so 2 `get_option` calls per request. | Single read: `$options = get_option('wppo_settings', [])` then reuse for `llms_txt` check at `142` (`$options['llms_txt']['enabled']`). | High |
| Q-18 | **INFO** | `Core_Tweaks` takes `file_optimisation` slice, not full options (abstraction) | `includes/class-main.php:306` (`new Core_Tweaks($this->options['file_optimisation'] ?? array())`) + `includes/class-core-tweaks.php:30` (`__construct(array $file_optimisation_opts)`) | Correct narrow dependency (only needs 5 keys: `disableEmojis` etc.). Private `file_optimisation` slice is stable contract. | No change — good. Keep narrow constructor for testability. | High |
| Q-19 | **INFO** | `allSuggestions` dedup by `metric` key may hide distinct telemetry vs pagespeed suggestions (edge case) | `src/components/Dashboard.js:75-88` (`useMemo` merges `pagespeedSuggestions` + `telemetrySuggestions`, `seen.has(s.metric)` dedup by `metric` only, pagespeed first) | If telemetry and PageSpeed both suggest `performance_score` but with different `message`/`severity`, telemetry suggestion is dropped (pagespeed wins). Order `pagespeed` first is intentional (PageSpeed more authoritative). No bug. | Document dedup key (`metric`) and order (pagespeed > telemetry). Keep. | High |
| Q-20 | **INFO** | `wppo_role_hash` cookie `SameSite` missing (standards) | `includes/class-main.php:361` (`setcookie('wppo_role_hash', $hash, time()+DAY_IN_SECONDS, COOKIEPATH, COOKIE_DOMAIN, is_ssl(), true)` — `httponly=true` correct, but `SameSite` not set; PHP 7.3+ `setcookie` array options `['samesite'=>'Lax']` supported, min PHP is 8.2) | Modern browsers default `SameSite=Lax` if absent, so no breakage; but cross-site `SameSite=Lax` blocks cookie on some flows (e.g. `advanced-cache.php` reads `$_COOKIE['wppo_role_hash']` server-side, so Lax is fine). Explicit `SameSite=Lax` hardens. | Add `setcookie('wppo_role_hash', $hash, ['expires'=>time()+DAY_IN_SECONDS,'path'=>COOKIEPATH,'domain'=>COOKIE_DOMAIN,'secure'=>is_ssl(),'httponly'=>true,'samesite'=>'Lax'])` (PHP 7.3+ array syntax, safe on 8.2). | High |

---

## 4. No-Issues (explicitly checked, no quality/architecture defect)

- `Util::cached_home_url` / `cached_content_url` blog-id static cache with `has_filter` bypass correctly preserves multisite safety (`class-util.php:450-496`). All 20+ call-sites reuse it.
- `Util::transient_key` single blog-ID prefix helper; `Cache`, `Cron`, `Database_Cleanup`, `Pagespeed`, `RUM`, `Advanced_Cache_Handler` all go through it (`class-util.php:509-518`).
- `Util::sanitize_settings_recursively` shared by `Rest` (`class-rest.php:494`) and CLI (`class-wppo-cli-command.php:556` delegates to `Util`), so sanitization is single-source despite heuristic branches above.
- `Rest::permission_callback` + `get_routes()` single `permission_callback` across 24 admin routes; `rum_collect` intentionally `__return_true` with token + IP rate-limit (`class-rest.php:218-222`, `class-rum.php:44-120`).
- `Rest::build_redis_config` `WPPO_REDIS_PASSWORD` constant precedence with `wppo_redis_allow_request_password` escape hatch correctly never persists password (`class-rest.php:1112-1120`, `class-object-cache.php:??` strips from drop-in).
- `Cache::should_skip_combine_for_inline_budget` block-theme `styles_inline_size_limit` 40KB gate correctly bails when total ≤ limit; `wppo_skip_combine_on_small_block_theme` filter opt-out (`class-cache.php:1070-1119`).
- `Cache::inline_drift` double simulation + conservative downgrade (`core_will_inline` `prediction !== reference` → `inline_drift_detected=true` + `Log::add` throttled via `Util::transient_key('wppo_inline_drift_…')` `DAY`) is documented and correct (`class-cache.php:732-928`).
- `Cache::maybe_apply_cdn` `WP_HTML_Tag_Processor` rewrite of `src/href/data-src/srcset` only for `wp-content|wp-includes` URLs starting with `site_url`, caches `site_url_regex` outside loop (`class-cache.php:1271-1354`).
- `Image_Optimisation::normalize_url` / `resolve_relative_path` correctly handle protocol-relative (`//`), root-relative (`/`), true relative (`../`) against `cached_home_url` + `current_url_path` (`class-image-optimisation.php:808-892`).
- `Cron::schedule_page_cron_jobs` lock via `Util::transient_key('wppo_preload_cron_lock')` 20-min + `finally delete_transient` prevents duplicate batches; `schedule_sitemap_url_jobs` caps 500 + `wp_next_scheduled` check (`class-cron.php:274-363`).
- `Cron::get_sitemap_urls` `deadline 15s` + `TO_FETCH_LIMIT 50` + `home_host` off-site filter + index child following prevents slow-index DoS (`class-cron.php:473-544`).
- `Database_Cleanup` `clean_all` vs `auto_clean` batching `LIMIT 1000` loops are correct; `invoke_cleanup_method` `false → WP_Error` branch is not dead code (`class-database-cleanup.php:86-130`).
- `LiteSpeed_Integration::effective_mode` `auto`/`wppo`/`litespeed`/`standalone` with `should_disable_wppo_optimizer` + `can_apply_cdn` + `wppo_litespeed_can_cdn` / `litespeed_can_cdn` / `litespeed_can_optm` filter layers correctly gate CDN vs minify (`class-litespeed-integration.php`, `class-cache.php:367,1303,1367`).
- `useNotice` + `NoticeBanner` shared feedback pattern adopted across SPA; `useNotice` owns auto-dismiss timer cleared on re-notify/dismiss/unmount (`src/lib/useNotice.js:26-72`).
- `ErrorBoundary` class component wraps `App.js:519` `renderContent()` so tab crash does not white-screen (`src/components/common/ErrorBoundary.js:48`).
- `src/setupTests.js` mocks `wppoSettings` translations, `window.matchMedia`, `@wordpress/components` ToggleControl correctly; global `wppoSettings` extended per-test via `global.wppoSettings = {…}`.
- `build/` output committed (`build/index.js`, `build/lazyload.js`, `build/style-index.css`) — no custom Webpack, `@wordpress/scripts` defaults (`package.json` scripts).
- `Llms::register_rewrite` / `serve` / `emit_link_header` correctly hooks `init` + `template_redirect` + `send_headers` with `query_vars` filter (`includes/class-llms.php`).
- `CDN_Purger::purge_all` on `wppo_after_cache_clear` correctly uses constants (`WPPO_CLOUDFLARE_API_TOKEN`) not DB, via `wp_remote_post` with `Zone:Cache Purge` permission (`includes/class-cdn-purger.php`).

---

## 5. Open Questions

| # | Question | Context | Owner |
|---|----------|---------|-------|
| Q-01 | Should `Main` remain a God facade or be decomposed into 5 collaborators now, given 2956 lines already exceeds any other class by 30%? Deferring split increases future change cost (every new tab/feature touches same file). | `includes/class-main.php:21` vs `includes/class-cache.php:33`/`class-image-optimisation.php:27` | Architecture |
| Q-02 | Should `Util` be split into `Filesystem`/`Url`/`Cache_Key`/`Settings_Sanitizer`/`Role_Hash` now, or keep as `deprecated` facade to avoid 23-file churn? Splitting without `class_alias` would break Brain Monkey stubs that mock `Util::` static calls. | `includes/class-util.php:30` | Architecture |
| Q-03 | Should `wppoSettings` global mutation be replaced by `src/lib/store.js` + React context now, or keep `has_filter`/`hasFetched*` ref pattern until SPA exceeds current 7 tabs? Store simplifies race on concurrent `update_settings` but adds indirection. | `src/lib/apiRequest.js:71` vs `src/App.js:135` | Frontend |
| Q-04 | Should `Cache` use constructor injection for `Image_Optimisation`/`Google_Fonts` (breaking `set_*` setters) in a minor bump, or keep setters with `deprecated` proxies for one release? Constructor injection removes partially-initialized state but requires 2 call-site edits in Main. | `includes/class-cache.php:296` vs `includes/class-main.php:473,536` | Backend |
| Q-05 | Should `version_compare` gates be centralized into `Wp_Version` now, or keep scattered `TODO(#553)` / `TODO(#624)` markers until WP 6.9 is the new minimum (expected post-7.1 bump)? Centralizing now saves 6-site edits but adds new class to load on every request. | `includes/class-main.php:435,439` vs `includes/class-cache.php:1013` | Release |
| Q-06 | Should `sanitize_settings_recursively` `url/cdn` be checked after `exclude/preload/delay/list` to fix `excludeUrlToKeepJSCSS` multiline truncation today, or keep order and document `exclude*` keys must not contain `url` substring? Fix is 2-line swap + test, low risk. | `includes/class-util.php:632` | Backend |
| Q-07 | When does the project intend to raise minimum WP to 6.9 so `TODO(#553)` legacy `template_redirect` / `script_loader_tag` fallbacks can be removed (currently ~6 branches across Main + Cache + ImageOptimisation)? | `includes/class-main.php:438,442,482,504,522` | Release |
| Q-08 | Should `Database_Cleanup::auto_clean` include `unattached_media` and `oembed_cache` (now in `clean_all`/`TABLE_MAP` at `NEXT`) or is omission intentional? Daily auto-clean on large libraries with many unattached media could be expensive; document. | `includes/class-database-cleanup.php:843` vs `788` | Backend |
| Q-09 | Should `uninstall.php` gain explicit deletes for `wppo_web_vitals_trends`, `wppo_front_page_lcp_*`, `wppo_block_assets_migrated`, `wppo_used_css_*`, `wppo_ccss_*`, `wppo_last_db_cleanup`, `wppo_preload_cron_offset` or switch to `LIKE 'wppo_%'` sweep on uninstall? `LIKE` sweep is heavier but future-proof. | `uninstall.php:32` vs `includes/class-pagespeed.php`/`class-used-css.php`/`class-cron.php` | Backend |
| Q-10 | Should JS `Dashboard` (1327) + `FileOptimization` (2024) be decomposed into `Cache*Card` pieces now, or keep as god components until bundle size forces code-split? Splitting saves render allocations and isolates save-path bugs but increases file count. | `src/components/Dashboard.js:60` vs `src/components/FileOptimization.js:1` | Frontend |

---

## 6. Recommendation Priority

| Priority | Items | Effort | Rationale |
|----------|-------|--------|-----------|
| **P1 (architecture)** | A-01 (Main facade), A-02 (Util split via deprecated proxies), A-04 (store for `wppoSettings`) | Medium — 3 new files each (~300 lines), `Main` stays as facade; `Util` proxies 1-line; `store.js` 60 lines — no breaking API | Highest coupling/global-state cost; each unlocks testability for 5+ collaborators |
| **P2 (dependencies & lifecycle)** | A-03 (lazy `get_options`), A-05 (constructor injection for Cache), A-06 (classmap + `::class` for CLI), Q-01/Q-02/Q-03 | Small — `get_options()` lazy accessor, `Cache::__construct` signature, `composer.json` classmap, `WPPO_CLI_Command::class` | Removes partially-initialized state + file-load I/O waste |
| **P3 (quality & edge cases)** | Q-01 (static reset in `tearDown`), Q-02 (`url`/`exclude` heuristic order), Q-03 (rename `is_not_cacheable` side-effect), Q-07 (`is_file_minified` mtime key) | Tiny — `bootstrap.php` 2 lines, `class-util.php` swap 2 lines + test, `class-cache.php` rename + return bool | Prevents flaky tests, data loss, silent marker failure |
| **P4 (consistency polish)** | A-07 (filesystem helper), A-13 (central `Wp_Version`), Q-10/Q-11 (cleanup maps + version constant), Q-12 (CSS `wppo-danger-zone`) | Small — `Filesystem` helpers, `Wp_Version` 30 lines, maps derived from `Database_Cleanup` | Reduces duplication + drift |
| **P5 (keep as-is, documented)** | A-08/A-09/A-10/A-11/A-12, Q-05/Q-06/Q-08/Q-14/Q-15/Q-16/Q-17 — intentional fallbacks, triple scans, cron swallows | Document only | Current trade-offs correct; no new abstraction needed |

---

*Generated without modifying production code. Evidence is `file:line` from the workspace at audit time. No unnecessary abstraction is proposed beyond facades that reduce god-class coupling while keeping `Main` + `Util` as backward-compatible entry points.*
