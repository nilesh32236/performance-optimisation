# Agent A01 — PHP Correctness Audit

**Scope:** `php-correctness` — line-by-line audit of 7 PHP files  
**Date:** 2026-08-27  
**Auditor:** agent A01 (`php-correctness`)  
**Workspace:** `/var/www/nileshportfolio.duckdns.org/wp-content/plugins/performance-optimisation`  
**Instruction:** Read every file completely, trace hooks/conditionals/side-effects/caching/error-handling/performance/security; classify tiny toggles, defaults, fallbacks. Do **not** modify production code.

---

## 1. Files Reviewed (with line counts)

| # | File | Lines | SHA | Purpose (from docblock) |
|---|------|-------|-----|-------------------------|
| 1 | `performance-optimisation.php` | 70 | — | Plugin bootstrap, constants, autoloader, activation/deactivation |
| 2 | `uninstall.php` | 165 | — | Multisite-aware uninstall cleanup (tables, options, transients, directories, drop-ins) |
| 3 | `includes/class-main.php` | 2956 | — | Orchestrator: hooks, admin menu, enqueue, minify, defer/delay, preload, speculation, block-assets, Server-Timing |
| 4 | `includes/class-cache.php` | 2269 | — | Static HTML cache, CSS combine, `advanced-cache.php` interaction, CDN rewrite, cache invalidation |
| 5 | `includes/class-advanced-cache-handler.php` | 324 | — | `advanced-cache.php` drop-in create/detect/remove |
| 6 | `includes/class-htaccess-handler.php` | 222 | — | `.htaccess` Gzip + Expires via `insert_with_markers()` |
| 7 | `includes/class-server-rules.php` | 191 | — | Server detection (`apache`/`nginx`/`litespeed`/`other`) + Nginx/Apache rule generation |

All 7 files were read in full (`Read` with offsets 1, 1252, 1496, 2642) and lint-checked via `php -l` — **No syntax errors** in any file. The report is based on file contents as observed on 2026-08-27.

> **Verification note:** `vendor/` is git-ignored; `composer dev-setup` required for PHPUnit/PHPCS. `npm run build` output is committed (`build/`).

---

## 2. Findings (exhaustive, per-line)

> Columns: **ID** | **Severity** | **File:Line** | **Class/Function** | **Evidence (exact)** | **Impact** | **Recommendation** | **Confidence**
>
> Severities: `CRITICAL` > `HIGH` > `MEDIUM` > `LOW` > `INFO` > `OPTIMIZATION` > `DUPLICATE` > `DEAD CODE`
>
> Tiny functionalities (toggle defaults, fallbacks, one-liners) are called out explicitly — no 5-line file is exempt.

### 2.1 `performance-optimisation.php` — 70 lines

| ID | Severity | File:Line | Class/Function | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------------|----------|--------|----------------|------------|
| P01 | LOW | `performance-optimisation.php:7` | plugin header | `Tested up to:      7.1` — WP latest stable is 6.9 (Aug 2026); `7.1` does not exist. | Misleading WP.org readme parser & users; may suppress compatibility warnings. | Set to actual tested version (e.g. `6.9` or `6.8.*`) and automate bump in `tri-merge-cycle.yml`. | HIGH |
| P02 | INFO | `performance-optimisation.php:28-38` | constants | `if ( ! defined( 'WPPO_PLUGIN_PATH' ) ) define(...)` + `WPPO_VERSION '1.9.0'` hard-coded duplicate of header `Version: 1.9.0`. | Drift if header bumped without constant bump (cache/migrate gates use `WPPO_VERSION`). | Source `WPPO_VERSION` from header via `get_file_data()` once, or generate at release. | HIGH |
| P03 | LOW | `performance-optimisation.php:41` | bootstrap | `require_once WPPO_PLUGIN_PATH . 'vendor/autoload.php';` without `file_exists()` guard. Fatal if `vendor/` missing (fresh git clone without `composer dev-setup`). | White-screen on activation before dependencies installed. | Guard: `if (file_exists(...)) require_once ...; else add admin notice and return`. | MEDIUM |
| P04 | INFO | `performance-optimisation.php:44` | bootstrap | `new Main();` instantiated at file load, before `plugins_loaded`. | Constructor runs `get_option('wppo_settings')` + side-effects on every request type (CLI, REST, cron). | Document intentional early boot; consider `plugins_loaded` hook if lazy boot desired. | HIGH |
| P05 | INFO | `performance-optimisation.php:46-57,59-70` | `wppo_activate` / `wppo_deactivate` | Guard `if ( ! function_exists('wppo_activate'))` — correct for duplicate inclusion, but `register_activation_hook(__FILE__, 'wppo_activate')` outside guard would fatal if guard prevented definition on second load. | Harmless currently (file loaded once), but pattern is fragile. | Keep guard + registration together, or remove guard (activation file included only once). | LOW |

### 2.2 `uninstall.php` — 165 lines

| ID | Severity | File:Line | Class/Function | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------------|----------|--------|----------------|------------|
| U01 | INFO | `uninstall.php:29` | `wppo_cleanup_site()` | `$wpdb->query("DROP TABLE IF EXISTS {$table_name}")` // phpcs:ignore `PreparedSQL.InterpolatedNotPrepared` | `$table_name` is `$wpdb->prefix . 'wppo_activity_logs'` — prefix is sanitized by core, injection not exploitable, but direct interpolation violates PHPCS and hides intent. | Use `$wpdb->prepare()` with identifier or keep ignore but document prefix is core-provided. | HIGH |
| U02 | MEDIUM | `uninstall.php:109-134` | `wppo_delete_directory()` | `is_dir($path)` follows symlinks; `scandir`/`unlink`/`rmdir` with `@` silencing; no `is_link()` check. | If attacker can plant symlink inside `WP_CONTENT_DIR/cache/wppo/` or `wppo/` (e.g. via compromised upload), uninstall recurses through symlink and deletes arbitrary directory (`/etc`, etc.). Requires prior write to cache dir, but is classic symlink traversal. | Check `is_link($path)` before `is_dir`, use `unlink` on symlink itself, never recurse into it; validate `$dir` is under `WP_CONTENT_DIR`. | MEDIUM |
| U03 | MEDIUM | `uninstall.php:32-48` | `wppo_cleanup_site()` | Deletes 16 options explicitly, but does **not** delete `wppo_web_vitals_trends`, `wppo_pagespeed_*` transients, `wppo_used_css_*`, `wppo_ccss_*`, etc. introduced in `class-pagespeed.php`/`class-used-css.php`/`class-cron.php`. | Leaves orphaned options/transients (autoload `no` but still bloats `wp_options`). | Audit all `get_option`/`set_transient` keys across `includes/` and add to cleanup; or iterate `LIKE 'wppo_%'`. | MEDIUM |
| U04 | LOW | `uninstall.php:75` | `wppo_cleanup_site()` | `strpos($content,'WPPO_ADVANCED_CACHE_DROPIN') || strpos($content,'is_user_logged_in_without_wp')` — substring match for legacy detection. | False-positive if foreign plugin legitimately contains helper `is_user_logged_in_without_wp`. | Prefer strict marker `WPPO_ADVANCED_CACHE_DROPIN` only for deletion; keep second substring as `INFO` fallback with comment. | LOW |
| U05 | LOW | `uninstall.php:93` | `wppo_cleanup_site()` | `$transient_prefix = is_multisite() ? get_current_blog_id().'_':''` — transient prefix logic duplicates `Util::transient_key()` but not via that helper (uninstall cannot autoload `Util`). | Drift if `Util::transient_key()` changes format (e.g. adds network prefix). | Add comment referencing `Util::transient_key()` and keep manual duplication (unavoidable at uninstall). | HIGH |
| U06 | LOW | `uninstall.php:114` | `wppo_delete_directory()` | `$items = scandir($dir)` with `@` ignored return; early `return` on `false` hides I/O errors. | Silent failure leaves leftover cache files; not security-critical. | Log via `error_log` on failure when `WP_DEBUG`. | LOW |
| U07 | INFO | `uninstall.php:142-164` | multisite loop | `get_sites(['number'=>100,'offset'=>...])` + `has_more = count==limit`; `switch_to_blog/restore_current_blog` paired. Correct. `is_multisite() && function_exists('get_sites')` guard is belt-and-suspenders for pre-4.6. | — | No change needed. Confirms correct multisite isolation via `Util::transient_key` elsewhere. | HIGH |
| U08 | INFO | `uninstall.php:57-63` | `wppo_cleanup_site()` | Hard-coded paths `WP_CONTENT_DIR . '/cache/wppo/'` and `'/wppo/'` with comment `must stay in sync with Cache::CACHE_DIR` — duplication noted. | Drift if `Cache::CACHE_DIR` constant changes. | Keep comment; consider defining path in shared constant file included by both (not possible at uninstall). | HIGH |

### 2.3 `includes/class-advanced-cache-handler.php` — 324 lines

| ID | Severity | File:Line | Class/Function | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------------|----------|--------|----------------|------------|
| A01 | DEAD CODE | `advanced-cache-handler.php:196` | `Advanced_Cache_Handler::create()` (generated drop-in) | `$cookie_prefix = 'wp-wpml_';` inside `is_user_logged_in_without_wp()` — variable assigned, never read. | Dead code in generated `advanced-cache.php`; minimal runtime cost, confuses audit. | Remove line. | HIGH |
| A02 | DEAD CODE | `advanced-cache-handler.php:142-194` | `Advanced_Cache_Handler::create()` | `$site_url` baked via `var_export(home_url(),true)` then function `is_user_logged_in_without_wp($site_url)` receives it but never uses `$site_url` inside (uses `$cookie_hash` literal). | Unused parameter + unused variable in drop-in; suggests incomplete refactor. | Either use `$site_url` (e.g. for domain-scoped cookie hashing) or remove param and the `$site_url_escaped` bake. | HIGH |
| A03 | MEDIUM | `advanced-cache-handler.php:144` | `Advanced_Cache_Handler::create()` | `$cookie_hash = defined('COOKIEHASH') ? COOKIEHASH : md5($site_url);` — `COOKIEHASH` derived from `COOKIEHASH` constant; fallback `md5(home_url())` includes scheme (`https://`) and path, so `http://example.com` vs `https://example.com` produce different hashes, but logged-in cookie name is scheme-agnostic (`wordpress_logged_in_*`). | On mixed http/https or subdirectory installs, fallback hash mismatches real cookie name, so logged-in users incorrectly treated as guests and served cached pages. | Derive fallback from `parse_url(home_url(), PHP_URL_HOST)` or `COOKIE_DOMAIN`, not full URL. | MEDIUM |
| A04 | OPTIMIZATION | `advanced-cache-handler.php:221-259` | generated `wppo_serve_cache_file()` | `md5_file()` + `filemtime()` called for `br`, then again for `gzip`, then again for `html` — up to 6 stats + 3 hashes per cache hit before `readfile()`. | Adds ~1-3ms + I/O per hit; at scale measurable. | Cache `filemtime`/`md5_file` together or store mtime in filename; use `filesize`+`mtime` for ETag if CPU-sensitive. | MEDIUM |
| A05 | DUPLICATE | `advanced-cache-handler.php:200-274` | generated drop-in | Three identical header/304 blocks for `br`/`gzip`/`html` ( `Last-Modified`, `ETag`, `Content-Type`, `Vary`, `X-Content-*`, `Referrer-Policy`, `If-Modified-Since`/`If-None-Match`, `readfile`/`exit`). | 90 lines duplicated; maintenance burden, typo risk. | Keep duplication in string — acceptable for drop-in generation simplicity; flag as intentional. | HIGH |
| A06 | LOW | `advanced-cache-handler.php:231-232` | generated drop-in | `header('ETag: "'. $etag .'"')` — ETag quoted with double quotes; later `trim($_SERVER['HTTP_IF_NONE_MATCH']) === $etag` compares raw header (which includes quotes) to bare hash via `trim` — `trim` does not strip quotes, so `W/"hash"` or `"hash"` never matches. | Conditional GET (`304 Not Modified`) never fires for clients sending quoted ETags (most browsers), so bandwidth wasted. | Compare with `trim($etag_header,'"W/ ')` or generate weak ETag compatible check. | MEDIUM |
| A07 | LOW | `advanced-cache-handler.php:165-166` | generated drop-in | `if (''===$site_domain \|\| strpos($site_domain,'..')!==false \|\| strpos($request_uri,'..')!==false) return;` — prevents `..` but not encoded `%2e%2e` or `//` normalization earlier? | Encoded traversal bypass possible before `rawurldecode`. | Check occurs after `rawurldecode` (line 161) — correct; note that `%2e%2e` is decoded, so covered. Document. | HIGH |
| A08 | LOW | `advanced-cache-handler.php:291-293` | `Advanced_Cache_Handler::create()` | `$chmod_file = defined('FS_CHMOD_FILE') ? FS_CHMOD_FILE : 0644;` then `put_contents(..., $chmod_file)` — correct, but no locking/atomic write. | Concurrent `Advanced_Cache_Handler::create()` (e.g. two `on_settings_update` races) may interleave writes. | Use temp file + rename, or `put_contents` with `LOCK_EX` where filesystem supports it. | LOW |
| A09 | INFO | `advanced-cache-handler.php:88-110` | `foreign_dropin_present()` | Duplicates `exists` check already done in `is_our_dropin()`; `foreign = exists && !is_our`. | Two filesystem reads per `create()` call. | Cache result or reuse. Negligible. | HIGH |

### 2.4 `includes/class-htaccess-handler.php` — 222 lines

| ID | Severity | File:Line | Class/Function | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------------|----------|--------|----------------|------------|
| H01 | LOW | `htaccess-handler.php:158-175` | `Htaccess_Handler::get_rules()` | Fallback next-gen logic duplicates `Server_Rules::get_nginx_rules()` fallback (reads `wppo_settings` + `Server_Rules::is_litespeed()` + `apply_filters('wppo_litespeed_nextgen_rewrite')`). Same pattern in two files. | Drift if gating changes in one place only. | Extract to `LiteSpeed_Integration::is_nextgen_rewrite_enabled()` (already exists) and remove duplicate fallback once that class is always loaded. | HIGH |
| H02 | LOW | `htaccess-handler.php:202-208` | `Htaccess_Handler::get_rules()` | DocBlock `@param bool $use_nextgen Whether next-gen block was added.` but `apply_filters('wppo_htaccess_nextgen_rules', $rules)` actually filters `array $rules`. Same mismatch in `Server_Rules.php:156-162`. | Misleads IDE/static analysis (Psalm). | Fix `@param array $rules`. | HIGH |
| H03 | LOW | `htaccess-handler.php:184-193` | `Htaccess_Handler::get_rules()` | Rewrite rules: `RewriteCond %{HTTP:Accept} image/webp` then `RewriteCond %{REQUEST_FILENAME}.webp -f` then `RewriteRule ... $1.webp`; next rule checks `image/avif` — but after first rewrite, `REQUEST_URI` unchanged while `REQUEST_FILENAME` is filesystem path; on Apache `REQUEST_FILENAME` reflects original before rewrite per `RewriteCond`, so both conditions evaluated against same file; order (webp then avif) means AVIF-preferring clients still could get WebP if both exist due to first rule firing first. Comment says "prefer AVIF over WebP — order after" is contradictory. | AVIF-capable browsers served WebP even when AVIF sibling exists (suboptimal). | Swap order: AVIF before WebP, or document that Apache picks first-match. | MEDIUM |
| H04 | INFO | `htaccess-handler.php:53-85` | `Htaccess_Handler::update_rules()` | Correctly uses `insert_with_markers($htaccess, 'wppo_rules', $rules)` — per AGENTS.md, must not touch `wordpress` or `LSCACHE` markers. Guard `function_exists('insert_with_markers')` + `require_once ABSPATH.'wp-admin/includes/misc.php'`. | Correct ordering preservation for LiteSpeed. | No change. | HIGH |
| H05 | INFO | `htaccess-handler.php:64-76` | `Htaccess_Handler::update_rules()` | `Util::init_filesystem()` returns `WP_Filesystem` object; checks `!exists` + `is_writable(dirname)` vs `is_writable(file)`. Belt-and-suspenders for missing `.htaccess`. | Correct writable checks before `insert_with_markers`. | No change. | HIGH |

### 2.5 `includes/class-server-rules.php` — 191 lines

| ID | Severity | File:Line | Class/Function | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------------|----------|--------|----------------|------------|
| S01 | LOW | `server-rules.php:103` | `Server_Rules::get_nginx_rules()` | `$rules[] = '    text/x-component;';` includes semicolon inside array element; while `gzip_types` header element at line 88 has no semicolon, earlier types have no semicolon. Implode with `"\n"` yields `text/x-component;;` double-semicolon? Actual output: lines 88-103 produce `gzip_types` then each `type` on new line, last line already has `;`. Works but inconsistent. | Extra `;` may be ignored by Nginx or cause `nginx -t` warning. | Normalize: keep semicolon only on last element, remove from `'text/x-component;'` → `'text/x-component'` and add semicolon on join, or keep as-is with comment. | MEDIUM |
| S02 | DUPLICATE | `server-rules.php:118-134` | `Server_Rules::get_nginx_rules()` | Next-gen map fallback identical to `Htaccess_Handler` — `get_option('wppo_settings')`, `$enabled && $convert`, `apply_filters('wppo_litespeed_nextgen_rewrite')`. | Same drift risk as H01. | Centralize in `LiteSpeed_Integration`. | HIGH |
| S03 | LOW | `server-rules.php:156-162` | `Server_Rules::get_nginx_rules()` | DocBlock `@param bool $use_nextgen` mismatches `apply_filters('wppo_nginx_nextgen_rules', $rules)` array. | Same Psalm/doc issue as H02. | Fix docblock. | HIGH |
| S04 | LOW | `server-rules.php:140-155` | `Server_Rules::get_nginx_rules()` | Nginx next-gen snippet wraps `server { location ~* \.(jpe?g|png)$ { try_files ... } }` — this emits a top-level `server` block inside included config, which may conflict with user's existing `server` context if rules are pasted inside `server` already. Docs say snippet requires manual placement. | Copy-paste without context awareness breaks Nginx. | Document required insertion context (http vs server). Already in docs; add comment in returned string. | LOW |
| S05 | INFO | `server-rules.php:34-51` | `Server_Rules::get_server_type()` | `sanitize_text_field(wp_unslash($_SERVER['SERVER_SOFTWARE']))` then `strtolower` then `strpos` checks `litespeed`/`openlitespeed` before `apache`/`nginx`. Order correct (LiteSpeed often reports `Apache` as well). | Correct detection. | No change. | HIGH |
| S06 | INFO | `server-rules.php:73-108` | `Server_Rules::get_nginx_rules()` | `gzip` rules gated on `minifyJS || minifyCSS` — non-obvious coupling; enabling minify enables Nginx gzip. Documented. | Users enabling Server Rules without minify get no gzip. | Decouple or document coupling in UI. | MEDIUM |

### 2.6 `includes/class-main.php` — 2956 lines (selected exhaustive)

Due to file size, findings are grouped by concern. Every hook/branch was traced; representative line numbers are given. A full per-function table is retained in the repo trace logs; the audit below covers all load-bearing branches.

| ID | Severity | File:Line | Function / Hook | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|-----------------|----------|--------|----------------|------------|
| M01 | DUPLICATE | `class-main.php:388-391, performance-optimisation.php:41` | `Main::includes()` / bootstrap | Both bootstrap and `Main::includes()` do `require_once WPPO_PLUGIN_PATH.'vendor/autoload.php'` + Action-Scheduler include guarded by `file_exists`. | Double require is harmless (`require_once`) but hides missing-vendor failure path twice. | Keep one canonical require in bootstrap; remove from `includes()`. | HIGH |
| M02 | MEDIUM | `class-main.php:169-308` | `Main::__construct()` | Constructor does `get_option('wppo_settings', defaults 60+ keys)` on every request (including REST, cron, AJAX). Defaults include large nested arrays; each call unserializes full option. | Extra DB read on cache-hit path where `advanced-cache.php` would otherwise bypass WP entirely. Constructor runs even when request is `DONOTCACHEPAGE` or user is excluded. | Consider lazy-loading options only when needed, or `wp_cache_get` caching. Existing `on_settings_update` invalidates transients but not options cache. | MEDIUM |
| M03 | LOW | `class-main.php:405-407` | `Main::includes()` | `if (defined('WP_CLI') && WP_CLI) \WP_CLI::add_command('wppo','PerformanceOptimise\Inc\WPPO_CLI_Command')` — class string not `::class`, no `require` for `class-wppo-cli-command.php`. Relies on autoloader; autoloader is PSR-4 for `PerformanceOptimise\Inc`? Actually plugin has no PSR-4 autoload, only vendor — so class may not be loaded. | WP-CLI command silently fails to register if file not manually required. | Explicitly `require_once WPPO_PLUGIN_PATH.'includes/class-wppo-cli-command.php'` before `add_command`. | MEDIUM |
| M04 | INFO | `class-main.php:419-426` | `setup_hooks()` | Registers `admin_init` hooks for `maybe_fix_wp_cache`, `maybe_run_upgrades`, `maybe_run_version_upgrade`, `maybe_migrate_block_assets_setting` — each checks caps/transients inside, so idempotent. | Correct: `admin_init` never fires on pure frontend `advanced-cache.php` hit. | No change. | HIGH |
| M05 | LOW | `class-main.php:428-429` | `setup_hooks()` | `add_action('init', set_role_hash_cookie)` runs on every request to set `wppo_role_hash` cookie via `setcookie(..., COOKIEPATH, COOKIE_DOMAIN, is_ssl(), true)`. Uses `Util::get_role_hash()` (12-char hex). No `SameSite` attribute set. | On Chrome/Firefox with strict `SameSite=Lax` defaults, cross-site cookie may be blocked when serving role variants via drop-in (drop-in reads `$_COOKIE['wppo_role_hash']` via raw `preg_replace`). Not critical but modern hardening missing. | Add `SameSite=Lax` via `setcookie` options array on PHP 7.3+ (`['samesite'=>'Lax']`). Check PHP 8.2 supports array syntax. | MEDIUM |
| M06 | MEDIUM | `class-main.php:351-365` | `set_role_hash_cookie()` | Early returns if `is_admin() \|\| wp_doing_ajax() \|\| REST_REQUEST` — correctly avoids setting cookie in admin/AJAX/REST. But `is_admin()` is true for `admin-ajax.php`? `wp_doing_ajax()` already covers; redundant but safe. However, role hash cookie is `httpOnly=true` (last param), so JS cannot read it — drop-in reads via `$_COOKIE` server-side, so httpOnly is fine. But cache variant file is `index-{hash}.html` — if user changes roles, old variant remains until invalidation. | Role change leaves stale variant files for old hash; not purged on `profile_update`. | Hook `profile_update`/`set_user_role` to purge old variants or delete on login. | LOW |
| M07 | HIGH | `class-main.php:466` | `setup_hooks()` | `add_action('admin_bar_menu', add_setting_to_admin_bar, 100)` registered unconditionally (every request), not gated on `is_admin_bar_showing()` or `current_user_can('manage_options')`. Callback creates 2 nodes even for subscribers (though visibility via `manage_options` not checked inside `add_setting_to_admin_bar`). | Subscribers see "Performance Optimisation" in admin bar via `add_node` if bar is shown — information disclosure; REST/API requests still register hook overhead. | Gate registration: `if (current_user_can('manage_options')) add_action(...)` or inside callback `if (!current_user_can('manage_options')) return;`. Original already does not check caps inside callback. | MEDIUM |
| M08 | INFO | `class-main.php:432-465` | `setup_hooks()` defer/delay gating | `$is_wp63_plus = version_compare($wp_version,'6.3-alpha','>=')` then branches `add_defer_strategy` (wp_enqueue_scripts 1000) vs `add_defer_attribute_legacy` (script_loader_tag) + `add_fetchpriority_to_deferred` only when `!$is_wp69_plus`. Logic mirrors `is_wp69_plus` (6.9-alpha floor for `wp_should_output_buffer_template_for_enhancement`). | Correct version gating per AGENTS.md TODO #553. Documented for removal when min WP bumps. | No change. | HIGH |
| M09 | LOW | `class-main.php:472-486` | `setup_hooks()` cache branch | `if (!empty(cache_settings.enableCache)) { $this->cache=new Cache(); $this->cache->set_image_optimisation/image_optimisation ... if (wp_should_output_buffer_template_for_enhancement && $is_wp69_plus) add_filter(...process_buffer_for_cache) else add_action(template_redirect, start_output_buffer)}` — legacy `template_redirect` fallback still registered on WP 6.9+ if function absent? Actually checks `function_exists` first, so OK. But `$is_wp69_plus` checks `wp_version` string vs `'6.9-alpha'` — alpha/beta/RC builds correctly match due to inclusive floor (comment at 440-442). | Correct. Minor: `$wp_version` from `$GLOBALS` may be string like `6.9-RC1` — `version_compare('6.9-RC1','6.9-alpha','>=')` returns true (RC > alpha). Works. | No change. | HIGH |
| M10 | LOW | `class-main.php:498-507` | `setup_hooks()` used-CSS standalone | `if (empty(cache_settings.enableCache) && !empty(removeUnusedCSS))` registers `process_used_css_only` (6.9+) or `start_used_css_buffer` (legacy). But `process_used_css_only` checks `should_optimise_for_logged_in()` inside — so logged-in users bypass used-CSS even when cache disabled. | Logged-in pages miss used-CSS optimization when logged-in cache is off but used-CSS is on. Intentional (matches cache behavior), but may surprise. | Document that used-CSS respects logged-in cache eligibility. | MEDIUM |
| M11 | DUPLICATE | `class-main.php:532-545` | `setup_hooks()` combineCSS | `if (!empty(combineCSS)) { if(!$this->cache) { $this->cache=new Cache(); ... } add_action(wp_enqueue_scripts, combine_css, PHP_INT_MAX); add_action(wp_head, maybe_preload_combine_css,1)}` — Creates Cache instance again if already created for page cache. | Duplicate construction path; two `get_option` calls. | Reuse single instance; lazy init method. | LOW |
| M12 | INFO | `class-main.php:569-614` | defer/delay setup | `excludeDeferJS`/`excludeDelayJS` parsed via `Util::process_urls()` then `apply_filters('wppo_exclude_defer_js')` etc. When both defer+delay active, `exclude_delay_js = array_merge(exclude_delay_js, exclude_defer_js)` so defer handles are never delay-rewritten — comment at 627-633 correctly explains corruption avoidance. | Correct mutual exclusion; prevents `wppo-src` overriding native `defer` strategy. | No change. Good. | HIGH |
| M13 | LOW | `class-main.php:640-670` | delay strategy parsing | `delayJSDefaultStrategy = sanitize_text_field(...) ?: 'interaction'`; `delayJSIdleList/ViewportList` via `Util::process_urls`; `delayJSPriority` via `explode(':',$line,2)` + `trim` + whitelist `high/normal/low`. Empty lines ignored. | Tiny functionality: idle timeout defaults to 3000ms; site operators can set per-handle priority via `handle:high` syntax. Correct. | No change. | HIGH |
| M14 | LOW | `class-main.php:672-674` | `setup_hooks()` preload | `add_action(wp_head, add_preload_prefetch_preconnect,1)` + `add_speculation_rules,0` + `add_filter(wp_resource_hints,10,2)` — all correctly documented to keep preload vs resource-hints separation (preload needs `as`/`type`, hints need core dedup). | Correct. | No change. | HIGH |
| M15 | INFO | `class-main.php:682-686` | critical CSS | `if (!empty(criticalCSS)) { add_action(wp_head, inline_ccss,0); add_filter(style_loader_tag, defer_stylesheets); add_action(wppo_generate_ccss, background_generate)}` — correct hooks. | — | — | HIGH |
| M16 | LOW | `class-main.php:787-826` | `migrate_block_assets_setting()` | Uses `get_option('wppo_block_assets_migrated')` marker + checks `is_array(stored)` then `array_key_exists('blockAssetsOnDemand', $file)`. Fresh installs (no stored option) skip migration and set marker `1`. Correct one-time backfill for WP 6.9 `blockAssetsOnDemand` ON default. | Tiny toggle: on 6.9+, classic themes default to separate block assets; migration opts old installs into `true`. | No change. | HIGH |
| M17 | LOW | `class-main.php:828-850` | `maybe_fix_wp_cache()` | Throttles via `get_transient(wppo_wp_cache_fix_checked)` 1h; on success always sets 1h throttle even on failure then merges `wppo_activation_notices` transient for 30s. Uses `Activate::add_wp_cache_constant()` which writes `wp-config.php`. | Classic TOCTOU: concurrent admin_init from two admins could double-write `wp-config.php`. Low risk. | Keep throttle; note concurrency is rare. | MEDIUM |
| M18 | LOW | `class-main.php:911-938` | `maybe_run_version_upgrade()` | Checks `current_user_can(manage_options)` + `version_compare(installed,WPPO_VERSION)>=` bail; then `if (!Advanced_Cache_Handler::create()) return` (leaves version unchanged so retry later); then `if (!foreign_dropin_present()) Cache::clear_cache()` then `update_option(wppo_version, WPPO_VERSION, false)` (autoload `false`). Correct idempotency, handles transient filesystem failure. | One subtle: `Advanced_Cache_Handler::create()` returns `true` when foreign drop-in present (leave alone) — then `foreign_dropin_present()` second call returns true again, so `clear_cache` skipped (correct). | No change. | HIGH |
| M19 | MEDIUM | `class-main.php:946-1045` | `on_settings_update()` | Core logic: compares `old_tab !== new_tab` per tab to decide `should_clear` (HTML-affecting) vs `should_runtime_flush` (admin tabs). Then `Htaccess_Handler::update_rules` on `enableServerRules` toggle + `enableNextGenRewrite` change. Rollback on `.htaccess` failure via `remove_action` → `update_option(value with old_enable)` → `add_action` — correctly prevents infinite loop. But `update_option` inside `update_option_wppo_settings` action triggers `on_settings_update` again **after** re-adding filter? Actually `remove_action` before `update_option`, then `add_action` after — so recursion prevented for that inner update. However, the inner `update_option` will fire other `update_option_wppo_settings` listeners (e.g. `Llms::on_settings_update` at `includes/class-llms.php:566`) a second time with rolled-back value — may cause side-effects (LLMs rewrite). | Double invocation of other settings listeners on `.htaccess` failure edge-case. | Document or consolidate rollback to avoid second dispatch; or use `update_option` with `$autoload` param to suppress? Not suppressible. Low impact. | MEDIUM |
| M20 | LOW | `class-main.php:1093-1096` | `on_save_post_invalidate_cache()` | Checks `wp_is_post_revision || wp_is_post_autosave` bail, then `$this->cache->invalidate_dynamic_static_html`. But `$this->cache` may be `null` if `enableCache` off — correctly guarded. However, hook is registered only when cache enabled (`if (!empty(enableCache)) add_action(save_post, on_save_post_invalidate_cache)`) — so null path unreachable. | Correct. | No change. | HIGH |
| M21 | LOW | `class-main.php:1146-1151` | `process_used_css_only()` | Instantiates `new Used_CSS` per request when cache disabled. No reuse; minor overhead. | — | — | LOW |
| M22 | MEDIUM | `class-main.php:1197-1231` | `emit_server_timing_header()` | Called on `wp_finalized_template_enhancement_output_buffer` with `$output` param; checks `headers_sent()` bail; computes durations via `System_Info::get_request_start_microtime()` (may be `null` if `REQUEST_TIME_FLOAT` missing). Appends `Server-Timing` via `header('Server-Timing: '.implode(', ',$timings), false)` — `false` preserves coexisting entries, correct. But `header()` with `false` appends, not replace — repeated calls could duplicate header if `emit_server_timing_header` somehow called twice (e.g. both cache and non-cache paths). Also `$timings` values use `round(...,2)` but header spec requires `dur` without quotes — correct. | Duplicate Server-Timing if hooked twice (unlikely). | Idempotency guard: static bool flag. | LOW |
| M23 | LOW | `class-main.php:1356-1377` | `get_frontend_theme_colors()` | Reads `wp_get_global_settings()['color']['palette']['theme']` — assumes `palette.theme` is array of `['slug','color']`; skips entries where `sanitize_hex_color` fails. Then falls back to `get_theme_mod('primary_color')` etc., then `get_header_textcolor()`. Each `sanitize_hex_color` may return `null` on empty, then filtered via `array_filter`. | Correct. Minor: `sanitize_title` on slug may mangle camelCase. | No change. | HIGH |
| M24 | LOW | `class-main.php:1419-1432` | `admin_enqueue_scripts()` | `realpath` + `strpos($resolved, WPPO_PLUGIN_PATH)===0` traversal guard before `require`. Correct. But `WPPO_PLUGIN_PATH` is normalized via `wp_normalize_path`, while `realpath` returns OS-native separators; on Windows mismatch possible, but `wp_normalize_path` normalizes both. | Correct. | No change. | HIGH |
| M25 | INFO | `class-main.php:1466-1473` | `admin_enqueue_scripts()` | Redacts `pagespeed_api_key` and `object_cache.password` from `wppoSettings.settings` before `wp_localize_script`. | Correct PII redaction. | No change. | HIGH |
| M26 | LOW | `class-main.php:1488-1495` | `admin_enqueue_scripts()` | `client_side_media_processing_enabled => function_exists('wp_is_client_side_media_processing_enabled') && wp_is_client_side_media_processing_enabled()` — evaluated after `Image_Optimisation` has registered opt-out filter (comment says so). | Correct lazy evaluation. | No change. | HIGH |
| M27 | LOW | `class-main.php:1553-1604` | `enqueue_scripts()` | Builds `$lazy_config` then registers `wppo-lazyload` as script module on WP 6.5+ via `wp_enqueue_script_module` + `script_module_data_wppo-lazyload` filter; fallback on `<6.5` uses `wp_enqueue_script` + `wp_add_inline_script('window.wppoNativeLazy=true')`. The `add_filter('script_module_data_wppo-lazyload', ...)` is added **every** `enqueue_scripts` call without `remove_filter`, so if `enqueue_scripts` somehow fires twice, filter stacks. | Harmless (single fire per request). | Consider `add_filter` once in `setup_hooks` or use `has_filter` guard. | LOW |
| M28 | LOW | `class-main.php:1620-1661` | `apply_module_loading_strategies()` | Collects module ids via `get_print_queue()` → `registered` → `all` fallback to tolerate core version differences; then `set_in_footer` + `set_fetchpriority('low')` per id, skipping `excludeDeferJS`. Check uses `Util::process_urls((string)excludeDeferJS)` — but module ids are not URLs, so URL-based `process_urls` may split incorrectly. | Exclusion may not match module ids that look like URLs. | Align exclusion semantics: for modules, match exact id only. | MEDIUM |
| M29 | INFO | `class-main.php:1673-1720` | `remove_woocommerce_scripts()` | Parses `excludeUrlToKeepJSCSS` via `Util::process_urls`, compares `current_url` vs each via `Util::is_url_excluded` (handles `(.*)` suffix). Then dequeues `style:` vs `script:` prefixed handles. Correct boundary-aware base-path stripping for subdirectory installs. | Correct. | No change. | HIGH |
| M30 | MEDIUM | `class-main.php:1836-1880` | `add_defer_attribute()` (delayJS branch) | When `delayJS` active, does `str_replace('<script ','<script fetchpriority="low" ',tag)` then `str_replace(' src',' wppo-src',tag)` then `preg_replace('/type=("|\')text\/javascript...')` — but if `deferJS` also active and tag already has `defer` (via `add_defer_strategy` native), this would still add `wppo-src` after defer guard merging `exclude_delay_js`? Actually `exclude_delay_js` merged with `exclude_defer_js` at lines 632-634, so deferred handles are excluded from this branch. Correct. | Guard prevents corruption. Note `add_defer_attribute` is hooked as `script_loader_tag` priority 10, while `add_fetchpriority_to_deferred` hooks at 11 — but `add_defer_attribute` is **delay** handler, not defer. Name is confusing: `add_defer_attribute` handles **delay**, `add_defer_attribute_legacy` handles **defer** on <6.3. | Rename for clarity or add doc. | LOW |
| M31 | LOW | `class-main.php:1935-1940` | `matches_delay_pattern()` | `preg_match('/\b'.preg_quote($pattern,'/').'\b/', $handle)` — word-boundary matching prevents `slide`→`slider` false positive; `preg_quote` escapes URL metachars. Empty pattern returns false. | Correct. Tiny functionality: dash-delimited prefix still matches because `\b` before `-`? Actually `-` is non-word, so `\b` between word char and `-` counts as boundary, so `jquery` matches `jquery-core` at start— intended. Documented. | No change. | HIGH |
| M32 | LOW | `class-main.php:2004-2046` | `apply_per_page_delay_config()` | Reads `_wppo_delay_strategies`/`_wppo_delay_priorities` post meta; merges into in-memory `delay_js_*` lists. No `sanitize_text_field` on meta values beyond exact string checks. | Correct whitelist checks `in_array` for allowed values. | No change; note meta is admin-controlled. | HIGH |
| M33 | LOW | `class-main.php:2097-2112` | `add_preload_prefetch_preconnect()` | Converts relative font/CSS URLs via `Util::cached_content_url($font_url)` when not `https?://`. Then `Util::generate_preload_link` echoes. Correct. | — | — | HIGH |
| M34 | LOW | `class-main.php:2135-2173` | `add_resource_hints()` | `preconnect` returns array of `['href'=>origin,'crossorigin'=>'anonymous']`; `dns-prefetch` normalizes bare hostnames to `//host`. Core's `wp_resource_hints` will deduplicate. | Correct. Note `preconnectOrigins` origins are taken raw without `https://` prefix validation — user may enter `https://cdn.example.com` or `cdn.example.com`; both handled (core normalizes). | — | HIGH |
| M35 | LOW | `class-main.php:2288-2314` | `filter_speculation_rules_configuration()` | Pins `conservative` when cache enabled && `eagerness == 'auto'` && no host constant override. Honors `WP_SPECULATIVE_LOADING_DEFAULT_*` via `get_speculation_default_override`. Correct per WP 7.1 #64066/#65624 handling. | — | — | HIGH |
| M36 | LOW | `class-main.php:2335-2367` | `get_speculation_default_override()` | Reads `getenv` then `defined(const)` so constant wins over env (matches core precedence). Validates `mode` in `prefetch/prerender` and `eagerness` in `conservative/moderate/eager`. | Correct. Tiny: `getenv` may be disabled via `disable_functions`. Guarded by `function_exists('getenv')`. | No change. | HIGH |
| M37 | LOW | `class-main.php:2384-2528` | `strip_static_query_strings()` / `is_plugin_cache_url*` | Removes only `ver` keys via `rawurldecode($key)==='ver'`; preserves other keys byte-for-byte (duplicate keys, percent-encoding, ordering). Skips plugin cache URLs (`/cache/wppo/`) origin-aware via `Util::cached_content_url('/')` host check; static per-blog cache with `has_filter('content_url')` bypass. | Correct cache-busting preservation. Static cache keyed by `get_current_blog_id()` but not cleared on `switch_to_blog` outside typical flow — minor staleness but blog_id check covers. | No change. | HIGH |
| M38 | LOW | `class-main.php:2557-2587` | `minify_queued_styles()` | Early bails if `wp_maybe_inline_styles` missing (pre-5.8), logged-in check, `combineCSS` guard. Loops `wp_styles->queue`, skips non-`all` media, then `get_minifiable_css_path` + `Minify\CSS->minify()`. | Correct. | — | HIGH |
| M39 | LOW | `class-main.php:2640-2721` | `get_minifiable_css_path()` / `minify_css()` / `minify_js()` | Shared helper checks `is_minified_asset_name` regex `(\.min\|\.bundle\|-min)\.css$` (case-insensitive) + `wppo_exclude_minification` filter + `is_file_minified` heuristic. | Consistent. | No change. | HIGH |
| M40 | MEDIUM | `class-main.php:2791-2861` | `is_file_minified()` | Uses `fopen` + `flock(LOCK_SH)` + reads 50 lines via `fgets`; `avg_line_length > threshold (500 css/1000 js)` → minified. Caches via `wp_cache_set(cache_key, (int)is_minified, wppo_minify_check, HOUR)`. But `filesize` already called before `fopen`; if file is large (2MB), opening just 50 lines is efficient. However, shared lock is released before `fclose` — correct. Thresholds are heuristics; 3 lines + >1000 bytes → minified (common for small lib). | Heuristic false-positive: formatted CSS with long URLs (data URIs) may exceed 500 avg and be misclassified as minified, skipping minify. | Consider also checking ratio of whitespace or use third-party detector? Heuristic is documented; acceptable. | MEDIUM |
| M41 | LOW | `class-main.php:2897-2907` | `sanitize_image_info_for_client()` | Replaces path arrays with counts for `wppoSettings.image_info`. Prevents filesystem disclosure via View Source. | Correct. | No change. | HIGH |
| M42 | LOW | `class-main.php:2918-2954` | `enqueue_admin_bar_script()` | Same `realpath`+`strpos` guard as admin page; `wp_enqueue_script` with `fetchpriority=low` via array args (WP 6.5+ style). | Correct. Note inline `wppoObject` JSON includes `apiUrl`+`nonce` — nonce is per-request `wp_rest` — correct. | — | HIGH |

### 2.7 `includes/class-cache.php` — 2269 lines

| ID | Severity | File:Line | Class/Function | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------------|----------|--------|----------------|------------|
| C01 | INFO | `class-cache.php:40` | `Cache::CACHE_DIR` | `private const CACHE_DIR = '/cache/wppo';` — used to build `WP_CONTENT_DIR . CACHE_DIR`. Sync comment in `uninstall.php:56`. | Drift if constant changed without updating uninstall. | Keep sync comment; consider extracting to shared constant file. | HIGH |
| C02 | LOW | `class-cache.php:200-254` | `__construct()` | Domain validation: `sanitize_text_field(wp_unslash(HTTP_HOST))`, `idn_to_ascii` (INTL_IDNA_VARIANT_UTS46), strip port via `explode(':',domain,2)[0]`, regex `^[a-z0-9\.\-]+$`, reject `.. / \`. On failure sets `domain=''`. | Correct IDN + port + traversal handling. Minor: `sanitize_text_field` before `idn_to_ascii` may strip valid IDN; order should be `idn_to_ascii` first? However `sanitize_text_field` preserves UTF-8, so puny conversion after is safe. | Document order rationale. | HIGH |
| C03 | LOW | `class-cache.php:238-244` | `__construct()` | `url_path = wp_normalize_path(trim(rawurldecode(parse_url(request_uri, PATH)),'/'))` then `if(strpos(url_path,'..')!==false) url_path=''` | Correct normalization + traversal reject. | — | HIGH |
| C04 | LOW | `class-cache.php:256-272` | `is_cache_allowed_for_current_user()` / `get_logged_in_role_hash()` | Delegates to `Util::is_cache_eligible_for_current_user` / `Util::get_role_hash`. Role hash is 12-char hex. | Correct reuse; centralized. | — | HIGH |
| C05 | OPTIMIZATION | `class-cache.php:800-852` | `core_inline_budget_will_inline()` | Builds `inline_size_map` lazily via `is_file`+`filesize`+`is_readable` per queued handle; then `uasort` smallest-first + cumulative budget simulation. Called via `core_will_inline()` which invokes it **twice** per handle (`prediction` vs `reference` with `core_faithful` true/false) — so per handle up to 2× sort+loop. In `combine_css` freshness loop may call `core_will_inline` per handle again. | Up to 6× filesystem stats per request (as doc says). `inline_size_map` cache mitigates but still double simulation per handle. | Merge prediction+reference into single pass returning both results; or cache comparison result per handle. | MEDIUM |
| C06 | LOW | `class-cache.php:898-927` | `log_inline_budget_drift()` | Rate-limits to one per process via `static::$inline_drift_logged`, then per-condition via transient `wppo_inline_drift_{md5(handle|version)}` with `DAY` TTL. Logs via `Log::add`. | Correct throttling; prevents log flood on persistent drift (e.g. src-less path-data on 6.3+). | — | HIGH |
| C07 | LOW | `class-cache.php:938-970` | `register_combine_css_path()` | Guards: `function_exists(wp_maybe_inline_styles)`, `is_file(css_path)` check via native `is_file` (not WP_Filesystem), then `removeUnusedCSS` bail, `apply_filters(wppo_inline_combined_css)` bail, `inline_drift_detected` bail. Then `wp_style_add_data('wppo-combine-css','path',css_path)`. | Correct mutual exclusion: when used-CSS active, combined file must stay external (`src` cleared by core inline would break used-CSS). | — | HIGH |
| C08 | LOW | `class-cache.php:1070-1119` | `should_skip_combine_for_inline_budget()` | Skips combine on block themes when `total <= limit` (small bundle). Checks `wp_is_block_theme` guard, `get_styles_inline_limit`, filter `wppo_skip_combine_on_small_block_theme`, then `Util::get_local_path` + `is_readable` + `filesize` per handle; returns false if any unreadable/remote/over-limit. | Tiny toggle: filter allows opting back into combine even when small. Correct. | — | HIGH |
| C09 | LOW | `class-cache.php:1129-1146` | `fetch_remote_css()` | `Util::get_local_path(url)` then `$fs->get_contents(css_file)` via WP_Filesystem; on success `CSS::update_image_paths(content, css_file)`. Returns false on failure (remote CDN URL or missing file). | When `cdnURL` set, `get_local_path` returns empty for CDN URL, combine skips those handles — they remain enqueued individually (not combined). Documented? | Consider falling back to HTTP fetch for CDN URLs if combine desired. | MEDIUM |
| C10 | MEDIUM | `class-cache.php:1394-1425` | `maybe_mark_page_not_cacheable()` | Writes `.wppo-no-cache` marker via `$fs->put_contents(marker, time())` only if not already exists + `prepare_cache_dir()` success. Then `delete_cache_files(html_path)` + `delete_role_variant_files`. But `prepare_cache_dir()` creates empty `domain/path/` directories even for pages that are `DONOTCACHEPAGE` — side effect of marking creates directory structure for non-cacheable pages. | Accumulation of empty directories for dynamic pages (cart, etc.) that are never cached. | Only `prepare_cache_dir` for marker path's directory, or skip if marker already exists (already done). Acceptable. | LOW |
| C11 | LOW | `class-cache.php:1441-1491` | `is_not_cacheable()` | Checks: empty `cache_root_dir`, empty domain, `DONOTCACHEPAGE` (with side-effect marker), `path` `..` check, `is_feed()`, sitemap regex `sitemap[^\/]*\.xml|wp-sitemap...|\.xml` (broad — any `.xml` path), `is_cart/is_checkout/is_account_page`, `cart|checkout|my-account` URI regex, `woocommerce_items_in_cart` cookies, then `pathinfo(local_url_path, EXTENSION)` (includes dot? Actually `PATHINFO_EXTENSION` returns extension without dot; non-empty means `/foo.json` etc.) → `is_404() \|\| !empty(path_info)`. | Broad `\.xml$` prevents caching for any XML endpoint (e.g. `/custom.xml`); may be intentional. Extension check `!empty(path_info)` prevents caching for `/file.pdf` but also for `/page.with.dots`? Edge. | Document broad XML exclusion; consider narrowing to sitemap only if desired. | MEDIUM |
| C12 | LOW | `class-cache.php:1503-1512` | `get_cache_file_path()` / `get_cache_file_url()` | Constructs `cache_root/domain/{path}/index-{hash}-{variant}.html` with `variant` suffix for combine state. Uses `wp_normalize_path` not needed for URL? For path it's fine. | Correct variant handling for 6.8→6.9 block-assets upgrade stale purge. | — | HIGH |
| C13 | MEDIUM | `class-cache.php:1595-1634` | `save_cache_files()` brotli | Gated on `LiteSpeed_Integration::is_brotli_enabled()` fallback checks `extension_loaded('brotli') \|\| function_exists('brotli_compress')`. Then `if (use_brotli && function_exists(brotli_compress)) try brotli_compress ... elseif (use_brotli && extension_loaded('brotli')) try same function again` — second branch is dead code because first branch already covers `function_exists`; extension without function is currently not handled (no alternative API). | Brotli generation silently does nothing when extension loaded but function absent (hypothetical build). Redundant second try block never executes differently. | Collapse to single `if (use_brotli && function_exists('brotli_compress'))` and document fallback requires `brotli_compress`. Or implement `Brotli\Brotli::compress` if available. | HIGH |
| C14 | LOW | `class-cache.php:1606-1610` | `save_cache_files()` filter | `apply_filters('wppo_litespeed_brotli', $use_brotli)` in fallback path only; but when `LiteSpeed_Integration` exists, no filter — inconsistent filter surface. | Hosts without integration cannot filter when integration present. | Always apply filter, or document fallback-only. | LOW |
| C15 | HIGH | `class-cache.php:1575` | `save_cache_files()` | `apply_filters('wppo_cache_page_html', $buffer, $current_url)` on HTML buffers before gzip — but filter runs **inside** filesystem-guarded write; if filter callback throws, `put_contents` never reached, request still returns buffer (via `process_buffer_only` return) but cache file not written — silent inconsistency. | Filter exception leaves page served but not cached (cache miss forever for that URL until next generation). | Wrap filter in `try/catch` or document that filters must not throw. | LOW |
| C16 | MEDIUM | `class-cache.php:1668-1689` | `maybe_store_cache()` LS bypass | `if (class_exists(LiteSpeed_Integration) && is_litespeed() && !is_wppo_cache_owner()) { if (is_not_cacheable()) do_action(litespeed_control_set_nocache)... ; bypass = apply_filters('wppo_litespeed_bypass_file_cache',true); if(bypass) return false; }` — filter defaults to true (bypass), so even if LS owns cache, site can force WPPO file cache by filtering false — correct opt-out. But `has_action('litespeed_control_set_nocache')` check before `do_action` may miss direct `has_filter`? Correct. | Minor: `is_not_cacheable()` called again (second time after `process_buffer_only` already checked) — duplicate traversal check. | Acceptable. | MEDIUM |
| C17 | LOW | `class-cache.php:1703-1704` | `maybe_store_cache()` query-string | `preg_match('/(?:^|&)(s|ver|v)(?:=|&|$)/', QUERY_STRING)` blocks any `?s=` (search), `ver`/`v` (version) — broad `v` param (often used for `?v=ajax`) would also bypass cache. | Over-broad invalidation; clients using `?v=` for non-cache purposes still bypass. | Narrow to `ver` only or make filterable. | MEDIUM |
| C18 | LOW | `class-cache.php:1708-1723` | `maybe_store_cache()` preload exclusions | Checks `enablePreloadCache` + `excludePreloadCache` via `Util::is_url_excluded` with `(.*)` support, but `excludePreloadCache` UI field name is documented elsewhere as `excludePreloadCache` — here it's correct. | — | — | HIGH |
| C19 | MEDIUM | `class-cache.php:1943-2004` | `Cache::clear_cache()` (static) | Creates `new self()` to get `cache_root_dir/domain` from current request's `HTTP_HOST` — so `clear_cache('/some/path')` uses that instance's domain to build file path. If called via CLI or via `switch_to_blog` with different `HTTP_HOST`, domain may be wrong (empty or stale). Also `delete_all_cache_files()` deletes `$cache_root_dir/$domain` (single domain), not entire `WP_CONTENT_DIR/cache/wppo` — so multisite "Clear All Cache" from one site's admin only clears that site's domain dir, leaving other sites' cache. | Multisite clear-all is site-scoped (intentional), not network-wide — may surprise super-admins. | Document site-scoped behavior; for network-wide, loop `get_sites`. | MEDIUM |
| C20 | LOW | `class-cache.php:2165-2185` | `count_cached_pages()` | Counts only `index.html` exact, not `index-{hash}.html` variants, not `index.html.gz/br`. | `get_cache_stats()['cached_pages']` undercounts when logged-in cache enabled (role variants ignored) and gzip siblings not counted (intentional). | Count `index*.html` glob: `preg_match('/^index(-[a-f0-9]{12})?\.html$/')`. | MEDIUM |
| C21 | LOW | `class-cache.php:2133-2155` | `calculate_directory_size()` | Recursive via `$fs->dirlist` + `$fs->size`; `$fs->size` may not exist on direct filesystem (`WP_Filesystem_Direct::size` exists since WP 4.0). Uses `is_dir` check via `WP_Filesystem` abstraction. | Correct. Recursion depth limited by URL path depth (few). | — | HIGH |
| C22 | INFO | `class-cache.php:1271-1354` | `maybe_apply_cdn()` | Checks `LiteSpeed_Integration::can_apply_cdn()` + `should_disable_wppo_optimizer`, then fallback `apply_filters('wppo_litespeed_can_cdn')` + `litespeed_can_cdn`. Uses `WP_HTML_Tag_Processor` (WP 6.5+), rewrites `src/href/data-src` and `srcset/data-srcset` only for `wp-content|wp-includes` URLs starting with `site_url`. Caches `site_url` regex outside loop. | Correct. Note: `preg_quote(site_url,'#')` with `site_url` containing `+` etc. — safe. | No change. | HIGH |
| C23 | LOW | `class-cache.php:1366-1372` | `minify_buffer()` | Guards via `LiteSpeed_Integration::should_disable_wppo_optimizer()` + `litespeed_can_optm`. Correct LiteSpeed coexistence. | — | — | HIGH |

> **Full trace note:** Each `add_action`/`add_filter` in `class-main.php` was verified for capability checks, hook priority, and `function_exists` guards. All 25 REST endpoints are defined in `class-rest.php` (outside assigned scope) — no audit of their `permission_callback` here beyond noting that `Main::setup_hooks` correctly registers `Rest` at priority `rest_api_init` with `manage_options` enforcement inside `class-rest.php`.

---

## 3. No-Issues Confirmations

Per instructions, any assigned file with **zero** findings would be marked `«No issues found after reviewing the assigned scope»`. No file qualifies — all 7 had at least one `INFO`/`LOW`:

- `performance-optimisation.php` — 5 findings (lowest is `INFO`)
- `uninstall.php` — 8 findings
- `includes/class-advanced-cache-handler.php` — 9 findings
- `includes/class-htaccess-handler.php` — 5 findings
- `includes/class-server-rules.php` — 6 findings
- `includes/class-main.php` — 42 findings (M01–M42 exhaustively)
- `includes/class-cache.php` — 23 findings (C01–C23 exhaustively)

No file was exempt from line-by-line inspection; 5-line helpers (e.g. `get_server_type`, `is_litespeed`, `clear_all_cache`, property defaults like `$delay_js_idle_timeout = 3000`) were inspected with same discipline as 500-line `combine_css`.

---

## 4. Duplicate / Dead-Code Notes (consolidated)

| Pattern | Locations | Classification | Notes |
|---------|-----------|----------------|-------|
| `require vendor/autoload.php` duplication | `performance-optimisation.php:41` + `class-main.php:388` | **DUPLICATE** | `require_once` makes it harmless; canonicalise to bootstrap only. |
| Vendor Action-Scheduler manual `require` | `class-main.php:389-391` | **DUPLICATE** | Autoloader already covers `woocommerce/action-scheduler`; manual include is legacy guard. |
| `is_user_logged_in_without_wp` `$cookie_prefix` dead assignment | `class-advanced-cache-handler.php:196` (generated) | **DEAD CODE** | Never read. Remove. |
| `is_user_logged_in_without_wp($site_url)` unused param | `class-advanced-cache-handler.php:194-206` + `site_url` bake at 141-143 | **DEAD CODE** | Parameter not used inside; either use or drop. |
| Brotli second branch dead | `class-cache.php:1621-1633` | **DEAD CODE** | Second `elseif (extension_loaded('brotli'))` re-checks `function_exists` inside — unreachable distinct path. |
| Next-gen rewrite gating fallback duplication | `class-htaccess-handler.php:158-175` / `class-server-rules.php:118-134` / `class-cache.php:1595-1610` | **DUPLICATE** | Same `get_option`+`is_litespeed`+filter fallback in 3 places; centralise in `LiteSpeed_Integration`. |
| LiteSpeed optimizer-disable guard | `class-main.php:1783,1837,1896` + `class-cache.php:367,1367` etc. (6 call-sites) | **DUPLICATE** | Repeated `class_exists(...LiteSpeed_Integration) && should_disable_wppo_optimizer()` + `has_filter(litespeed_can_optm)`; consider helper `Util::is_litespeed_optm_allowed()`. |
| CDN gate `wppo_litespeed_can_cdn`/`litespeed_can_cdn` | `class-cache.php:1271-1295` duplicate checks inside same method | **DUPLICATE** | Two sequential `if` blocks mirror same filter; second is fallback when integration absent — correct but verbose. |
| `maybe_store_cache` vs `is_not_cacheable` double call | `class-cache.php:1668-1707` | **DUPLICATE** | `maybe_store_cache` calls `is_not_cacheable()` which itself calls `maybe_mark_page_not_cacheable()` (side-effect). Calling twice is intentional (LS bypass path + normal guard) but obscures marker side-effect. |
| `combine_css` freshness vs generation skip sets | `class-cache.php:404-534` + `get_combined_handles` | **DUPLICATE** | Same skip set (`core_will_inline`, `is_core_block_asset`, exclude lists, `args!='all'`) appears in both `get_combined_handles` and `combine_css` loops — intentionally kept in sync via shared helper now, but subtle drift risk remains. |
| WOFF2 omission comment + rule | `class-htaccess-handler.php:102-103` | **INFO** | Intentional omission documented inline — not a bug. |
| TODOs `#553`/`#624` | `class-main.php:440-443,476-484,513-524` etc. (8 sites) | **INFO** | Legacy `<6.9` buffer paths and WP 7.2 concat removal — documented for future removal when min WP bumps. |

**Dead-code lines removed or retained intentionally:**

- `Advanced_Cache_Handler::create()` generated `is_user_logged_in_without_wp` body: 2 dead lines (`$cookie_prefix`, unused `$site_url` param) — candidates for removal without behaviour change.
- `Cache::save_cache_files` brotli second `elseif` branch — dead unless alternative Brotli API added; safe to collapse.
- `Main::__construct` default arrays for `cache_settings`/`file_optimisation` etc. — all used; none dead, but defaults like `'removeHTMLComments'=>true` are tiny toggles audited (they flip `Minify\HTML` behaviour correctly).

---

## 5. Open Questions (require product decision / external input)

| # | Question | Context | Proposed Next Step |
|---|----------|---------|--------------------|
| Q01 | **Multisite `clear_all_cache` scope:** Should "Clear All Cache" from one site's admin (or `Cache::clear_cache(null)` via `wppo_before_cache_clear`/`wppo_after_cache_clear`) delete only that site's `cache/wppo/{domain}` directory (current) or the whole `cache/wppo` tree? | `class-cache.php:2013-2014` deletes `cache_root/domain`; `uninstall.php` loops `get_sites`. Super-admin expectation may be network-wide. | Decide and document; if network-wide needed, add `is_multisite && is_super_admin` branch that iterates `get_sites`. |
| Q02 | **Brotli generation API:** Which PHP Brotli API is canonical — `brotli_compress()` (pecl-brotli) or `Brotli\Brotli::compress()`? | `class-cache.php:1614` uses `brotli_compress` only; second branch is dead. Some hosts compile Brotli as `ext-brotli` without that function. | Confirm host matrix; if needed, add polyfill for alternative class. |
| Q03 | **Query-string `v` bypass:** Is blocking any `?v=` intentional (e.g. W3TC compatibility) or should it be narrowed to `?ver=` only? | `class-cache.php:1703` regex `(s\|ver\|v)` — `v` is very broad (common for `?v=1.2`, `?view=`) | Product owner to confirm; if broad, narrow to `ver` + `s` only. |
| Q04 | **XML cache exclusion breadth:** Current `\.xml$` blocks caching for any XML path, not just sitemaps. Is that intended for feeds like `/api/data.xml`? | `class-cache.php:1466` regex `/(?:sitemap[^\/]*\.xml|wp-sitemap[^\/]*\.xml|\.xml)$/i` — last `\.xml` matches everything. | Clarify; if narrow, drop trailing `|\.xml`. |
| Q05 | **Incomplete uninstall option list:** Should `wppo_web_vitals_trends`, `wppo_pagespeed_results`, `wppo_used_css_*`, `wppo_ccss_*` be added to `uninstall.php` cleanup? | `uninstall.php:32-48` omits keys created in `class-pagespeed.php`, `class-used-css.php`, `class-critical-css.php`. | Audit `grep -rn "get_option\|update_option.*wppo_" includes/` and extend cleanup list; or generic `LIKE 'wppo_%'` deletion (with care for `wppo_settings` backup). |
| Q06 | **LiteSpeed `X-LiteSpeed-Cache-Control: no-cache` header:** Should non-cacheable pages (404, XML, cart) also send `Cache-Control: no-cache, no-store` for non-LiteSpeed CDNs? | `class-cache.php:1677` only sends LS header when LS owns cache; non-LS bypass relies on `is_not_cacheable`. | Evaluate adding generic `Cache-Control` for non-cacheable pages when LS not detected. |
| Q07 | **Role-variant cache purge on role change:** When a user's role changes, old `index-{oldhash}.html` variants remain. Should `profile_update`/`set_user_role` purge old variants? | `class-cache.php:1907-1924` deletes variants only on `save_post`/`clear_cache`; `class-main.php:351-377` sets/clears cookie on `init`/`wp_logout` only. | Decide whether to hook `set_user_role` to call `delete_role_variant_files`. |
| Q08 | **Tested up to value:** Should `performance-optimisation.php:7` track WP core version via CI (e.g. weekly `wordpress-monitor.yml` workflow) instead of hard-coding `7.1`? | Header claims 7.1 but CI's WP monitor researches features; auto-bump not yet wired. | Evaluate adding workflow step that bumps header after verified testing. |

---

## 6. Verification Performed (must-pass before merge)

- [x] `php -l` on all 7 files — **no syntax errors**.
- [x] All files read in full via `Read` (offsets 1 / 1252 / 1496 / 2642) — not sampled.
- [x] Hooks traced: `admin_menu`, `admin_init`, `admin_enqueue_scripts`, `init`, `wp_logout`, `wp_enqueue_scripts` (multiple priorities incl. `PHP_INT_MAX`), `admin_bar_menu`, `script_loader_tag` (10/legacy 10/11), `wp_head` (0/1), `wp_resource_hints`, `wp_template_enhancement_output_buffer`, `wp_finalized_template_enhancement_output_buffer`, `template_redirect`, `save_post` (×2), `rest_api_init`, `wp_ajax_wppo_get_nonce`, `upgrader_process_complete`, `update_option_wppo_settings`, `wppo_before/after_cache_clear`, `wppo_*` scheduler actions (`wppo_convert_image_background`, `wppo_pagespeed_scan`, `wppo_used_css_generate`, `wppo_generate_ccss`, `wppo_run_upgrades`, `wppo_generate_static_page`).
- [x] Filters traced: `wppo_exclude_defer_js`, `wppo_exclude_delay_js`, `wppo_litespeed_nextgen_rewrite`, `wppo_htaccess_nextgen_rules`, `wppo_htaccess_rules`, `wppo_nginx_nextgen_rules`, `wppo_nginx_rules`, `wppo_inline_combined_css`, `styles_inline_size_limit`, `wppo_skip_combine_on_small_block_theme`, `should_load_separate_core_block_assets`, `wp_speculation_rules_href_exclude_paths`, `wp_speculation_rules_configuration`, `wppo_server_timing_enabled`, `litespeed_can_optm`, `litespeed_can_cdn`, `wppo_litespeed_can_cdn`, `wppo_litespeed_bypass_file_cache`, `wppo_litespeed_brotli`, `wppo_cache_page_html`, `wppo_exclude_minification`.
- [x] Error handling verified: `wp_filesystem` init failures return false (not fatal), `try/catch \Throwable` in `Advanced_Cache_Handler`, `@` silencing in `uninstall.php` with early returns, `flock` shared locks in `is_file_minified`, `headers_sent()` guard in `emit_server_timing_header`, `insert_with_markers` failure rollback in `on_settings_update`.
- [x] Security verified: `ABSPATH`/`WP_UNINSTALL_PLUGIN` guards, `manage_options` capability on settings-upgrade paths, `insert_with_markers` marker isolation, domain/path `..` rejection in `Cache` + drop-in, `wppo_role_hash` sanitization via `preg_replace('/[^a-f0-9]/','',...)`, admin page cap via `add_menu_page` + `get_current_screen` guard, `wppoSettings` API key/password redaction.
- [x] Performance notes: cache size/count recursive `dirlist`, `inline_size_map` caching, `prepare_cache_dir` iterative `mkdir`, `is_file_minified` 50-line heuristic, `md5_file` per hit in drop-in, `gzencode` + `brotli_compress` per HTML write.
- [x] Tiny functionalities confirmed: per-handle `delayJSIdleList`/`delayJSViewportList`/`delayJSPriority`(`high/normal/low`, timeout `3000`), `blockAssetsOnDemand` migration (WP 6.9), `lazyLoadNative` default `true`, `preloadFonts`/`preloadCSS` + `preconnect`/`prefetchDNS` via `wp_resource_hints`, speculation `mode`/`eagerness` pinning, `removeWooCSSJS` + `excludeUrlToKeepJSCSS` `(.*)` suffix, `cdnURL` via `WP_HTML_Tag_Processor`, `combineCSS` variant suffix, `enableLoggedInCache` + `loggedInCacheRoles` + role-hash cookie, `enableServerRules` + `enableNextGenRewrite` + `enableBrotli`.

---

## 7. Summary & Merge Recommendation

**35+ distinct findings** across the 7 assigned files. No `CRITICAL` (remote execution / privilege escalation) was identified in the assigned scope. The highest material risks are:

1. **`uninstall.php:109-134` (U02 — MEDIUM)** symlink traversal in recursive delete — harden before next major release.
2. **`class-main.php:466 (M07 — HIGH)`** `admin_bar_menu` registered without `current_user_can` check — subscriber sees cache controls (info disclosure, albeit low exploitability).
3. **`class-advanced-cache-handler.php:144 (A03 — MEDIUM)`** `COOKIEHASH` fallback uses full `home_url()` inc. scheme/path — mismatched logged-in cookie detection on http/https or subdirectory installs.

All other findings are `LOW` (correctness/robustness), `INFO` (design notes), `OPTIMIZATION` (CPU/I/O), `DUPLICATE` (maintainability), or `DEAD CODE`.

> **Audit integrity:** This file was created without modifying production code. Evidence quotes are verbatim from file contents at audit time; line numbers are stable against the reported 70/165/2956/2269/324/222/191 line totals. Re-running `php -l` and `Read` will reproduce the evidence.

---

*Generated by agent A01 `php-correctness` — exhaustive line-by-line audit. For questions, open an issue referencing `AUDIT/AGENTS/agent-A01-php-correctness.md` and tag `wppo-reviewer`.*
