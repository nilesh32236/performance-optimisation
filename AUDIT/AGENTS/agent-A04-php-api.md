# Agent A04 — PHP Correctness: API/CLI/REST/RUM

**Base:** `master@31fffc61`  
**Scope:** PHP Correctness — API/CLI/REST/RUM specialist  
**Date:** 2026-08-28  
**Auditor:** Agent A04  
**Workspace:** `/var/www/nileshportfolio.duckdns.org/wp-content/plugins/performance-optimisation`  
**Instruction:** Audit-only, do NOT modify production code. Read every line, trace 28 REST endpoints (auth `manage_options` + `X-WP-Nonce`, `rum_collect` public token+rate-limit), RUM beacon, PageSpeed Action Scheduler, telemetry local cURL, system info, CLI, logging, suggestion engine.

---

## 1. Files Reviewed

| # | File | Lines | Purpose |
|---|------|-------|---------|
| 1 | `includes/class-rest.php` | 1620 | 28 REST routes, permission/middleware, sanitization, cache/image/object-cache/RUM handlers |
| 2 | `includes/class-rum.php` | 429 | Real-user Web Vitals beacon collection, token + rate-limit, bounded queue + aggregate storage |
| 3 | `includes/class-pagespeed.php` | 661 | Google PageSpeed Insights API v5 async via Action Scheduler, trends, LCP extraction |
| 4 | `includes/class-telemetry.php` | 985 | Local HTTP page analysis (cURL + wp_remote fallback), redirect SSRF hardening, HTML parsing |
| 5 | `includes/class-system-info.php` | 633 | PHP/DB/WP/server/cache/litespeed/infrastructure/opcache environment reporting |
| 6 | `includes/class-wppo-cli-command.php` | 956 | WP-CLI `wppo` subcommands: cache/database/image/settings/object-cache/pagespeed/system-info |
| 7 | `includes/class-log.php` | 150 | Activity log custom table insert + paginated fetch with salted/versioned cache |
| 8 | `includes/class-suggestion-engine.php` | 397 | Threshold-based suggestion objects from telemetry/PageSpeed/AI Adaptive |
| **Total** | | **5831** | |

All files read in full via `Read` with offsets. `php -l` not executed (audit-only, no prod change); truncated `Read` windows re-read with offset.

---

## 2. Lines Reviewed

**5831 lines** — `1620 + 429 + 661 + 985 + 633 + 956 + 150 + 397`. Each file read end-to-end (offsets `1` + `1354` for `class-rest.php`; single read for others). Grep cross-checked `permission_callback`, `manage_options`, `__return_true`, `X-WP-Nonce`, `wp_verify_nonce`, `sanitize_*`, `realpath`, `wp_normalize_path`, `wp_http_validate_url`.

---

## 3. REST Route Inventory (28 endpoints)

`Rest::get_routes()` at `includes/class-rest.php:74-261` maps `performance-optimisation/v1/{route}` → `{methods,callback,permission_callback,schema}`. Schema uniform via `$schemas = [$this,'get_schema_for_route']` at `includes/class-rest.php:75,322-349`.

| # | Route | Method | Callback `includes/class-rest.php:*` | Permission | Notes |
|---|-------|--------|--------------------------------------|------------|-------|
| 1 | `clear_cache` | POST | `clear_cache:371` | `permission_callback:81` | group flush vs path clear, path-traversal guard |
| 2 | `update_settings` | POST | `update_settings:464` | `permission_callback:87` | tab whitelist, Redis password dedup |
| 3 | `optimise_image` | POST | `optimise_image:563` | `permission_callback:93` | webp/avif queue vs sync fallback |
| 4 | `delete_optimised_image` | POST | `delete_optimised_image:705` | `permission_callback:99` | deletes `WP_CONTENT_DIR/wppo` |
| 5 | `recent_activities` | GET | `get_recent_activities:542` | `permission_callback:105` | paginated log |
| 6 | `import_settings` | POST | `import_settings:734` | `permission_callback:111` | allowlist + merge |
| 7 | `database_cleanup` | POST | `database_cleanup:819` | `permission_callback:117` | `valid_types` inc `all` |
| 8 | `database_cleanup_counts` | GET | `get_database_cleanup_counts:925` | `permission_callback:123` | counts delegating |
| 9 | `get_page_assets` | GET | `get_page_assets:937` | `permission_callback:129` | post_id → scripts/styles |
| 10 | `image_job_status` | GET | `get_image_job_status:969` | `permission_callback:135` | pending/completed/failed + scheduler |
| 11 | `object_cache` | POST | `handle_object_cache:1025` | `permission_callback:141` | status/ping/enable/disable/flush |
| 12 | `system_info` | GET | `get_system_info:1201` | `permission_callback:149` | `System_Info::get_all` |
| 13 | `performance_scan` | POST | `run_performance_scan:1215` | `permission_callback:155` | SSRF-gated telemetry |
| 14 | `pagespeed_scan` | POST | `queue_pagespeed_scan:1263` | `permission_callback:163` | SSRF-gated Action Scheduler |
| 15 | `pagespeed_results` | GET | `get_pagespeed_results:1323` | `permission_callback:169` | 202 not_ready vs error sentinel + LCP retrostore |
| 16 | `web_vitals_trends` | GET | `get_web_vitals_trends:1370` | `permission_callback:175` | md5+strategy filter |
| 17 | `suggestions` | GET | `get_suggestions:1416` | `permission_callback:181` | telemetry transient → suggestion engine + AI merge |
| 18 | `server_rules` | GET | `get_server_rules:1501` | `permission_callback:187` | nginx/apache + litespeed info |
| 19 | `used_css_regenerate` | POST | `used_css_regenerate:1451` | `permission_callback:193` | single vs background |
| 20 | `regenerate_ccss` | POST | `regenerate_ccss:1522` | `permission_callback:199` | `Critical_CSS::regenerate_all` |
| 21 | `ccss_status` | GET | `get_ccss_status:1544` | `permission_callback:205` | template status |
| 22 | `dismiss_welcome` | POST | `dismiss_welcome:1592` | `permission_callback:211` | user_meta flag |
| 23 | `rum_collect` | POST | `collect_rum:290` | `__return_true:227` | **public** beacon (token + per-IP rate limit) |
| 24 | `rum_data` | GET | `get_rum_data:307` | `permission_callback:233` | aggregated RUM (flushes queue) |
| 25 | `autoloaded_options` | GET | `get_autoloaded_options:269` | `permission_callback:239` | top N bloat audit, limit clamp |
| 26 | `ai_model` | GET | `get_ai_model:1557` | `permission_callback:245` | AI model read |
| 27 | `ai_learn` | POST | `ai_learn:1569` | `permission_callback:251` | AI learning trigger |
| 28 | `ai_suggestions` | GET | `get_ai_suggestions:1582` | `permission_callback:257` | AI adaptive suggestions |

`permission_callback()` at `includes/class-rest.php:357-362`: `current_user_can('manage_options') && wp_verify_nonce($_SERVER['HTTP_X_WP_NONCE'],'wp_rest')`. All except `rum_collect` use it. `rum_collect` doc at `includes/class-rest.php:215-229,277-289` cites compensating controls: daily rolling per-path token `wp_hash('wppo_rum_' . Ymd . '|' . path)` + `hash_equals` + 24h rotation, per-IP `120/hour` via `Util::transient_key('wppo_rum_ratelimit_' . md5(IP))`, bounded storage `14d × 200 paths/day`, metric clamping.

---

## 4. Findings

> Columns: **ID | Severity | File:Line | Category | Evidence | Impact | Recommendation | Confidence**

### 4.1 `includes/class-rest.php` — 1620 lines

| ID | Severity | File:Line | Category | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------|----------|--------|----------------|------------|
| REST-01 | HIGH | `class-rest.php:224-229` | Auth / Public endpoint | `'rum_collect' => ['methods'=>'POST','callback'=>collect_rum,'permission_callback'=>'__return_true']` — only route with `__return_true`; no `args` schema / `validate_callback` at registration, relies entirely on `RUM::collect()` token + rate limit. | If token validation has bug, public endpoint allows unauthenticated writes to `wppo_web_vitals_rum` option (option bloat). No gateway-level schema validation. | Add `args` declaration (`token` required, `path` required) with `validate_callback`; add integration test covering token replay across paths and rate-limit 429. Document token rotation in route doc. | HIGH |
| REST-02 | MEDIUM | `class-rest.php:357-361` | Auth / Nonce | `$nonce = $_SERVER['HTTP_X_WP_NONCE'] ?? sanitize_text_field(wp_unslash($_SERVER['HTTP_X_WP_NONCE'])) ; $nonce_valid = wp_verify_nonce($nonce,'wp_rest'); return current_user_can('manage_options') && $nonce_valid;` reads raw `$_SERVER`, not `$request->get_header('X-WP-Nonce')`. | Non-standard header retrieval; case-insensitivity not handled (`X-Wp-Nonce` via `get_header` lowercases). Proxy stripping `HTTP_X_WP_NONCE` edge not mitigated. | Use `$request->get_header('X-WP-Nonce')` or WP core pattern `rest_cookie_check_errors` + keep `current_user_can` first for early bail. | MEDIUM |
| REST-03 | MEDIUM | `class-rest.php:402-433` | Validation / Path Traversal | `if(''!==$path){ $candidate = trailingslashit(cache_dir).ltrim(path,'/\\'); $real=realpath(cache_dir.path); if(false!==$real){ is_exact||is_under → else 400 } else { is_exact||is_under on $candidate || strpos(candidate,'..')!==false → 400 }}` fallback validates via normalized string prefix so “Clear This Page” works before caching, but second branch checks `strpos(candidate,'..')` after normalizing `trailingslashit(cache_dir).ltrim(path)` — `..` inside encoded `%2e%2e` not caught, and `candidate` may contain `//` double-slash collapsed by `wp_normalize_path` but not fully canonicalized. | Encoded traversal `%2e%2e%2f` via `sanitize_text_field` would keep `%` then `wp_normalize_path` leaves `%2e` not `..`, later `realpath` false branch may pass prefix check if `..` encoded. Low exploit due `sanitize_text_field` + `manage_options` but inconsistent canonicalization. | Canonicalize via `sanitize_text_field` → `rawurldecode` → `wp_normalize_path` before prefix check; ensure `..` check after decode and normalization. | MEDIUM |
| REST-04 | LOW | `class-rest.php:436-455` | Logic / Action dispatch | `if('clear_single_page_cache'===action){ clear(path) } else { clear_all() }` — any non-`clear_single_page_cache` value (typo, `clear`, empty) clears **all** cache. No whitelist error. | Malformed JS request (`action:''`) triggers full purge unintentionally. | Whitelist: `if('clear_single_page_cache'===action) … elseif('clear_all'===action \|\| ''===action) … else return 400 Invalid action`. | HIGH |
| REST-05 | MEDIUM | `class-rest.php:378-400` | Validation / Group flush | `if(!empty($group)){ flushed=Cache::flush_group(group); if(!flushed) 500 … }` — `$group` sanitized via `sanitize_text_field` only, no allowlist. | `manage_options` attacker could attempt to flush arbitrary object-cache groups if underlying method lacks allowlist (DoS). | Add allowlist validation against `Cache::ALLOWED_GROUPS` or delegate and enforce 400 on unknown. | MEDIUM |
| REST-06 | MEDIUM | `class-rest.php:469-473,749-756` | Consistency / Allowlist drift | `update_settings` allowlist via `Util::ALLOWED_SETTINGS_TABS` (9 tabs), `import_settings` via `Util::ALLOWED_SETTINGS_KEYS` — REST allows `litespeed_integration, llms_txt` but CLI `WPPO_CLI_Command::settings` allowlist differs (see CLI-02). | `core_tweaks` saved via CLI rejected via REST; `litespeed_integration` saved via REST rejected via CLI import. Backup/restore parity break. | Centralize single constant in `Util` and reuse everywhere; add test asserting REST/CLI/Activate allowlists equal. | HIGH |
| REST-07 | LOW | `class-rest.php:1215-1250,1263-1310` | Security / SSRF order | `run_performance_scan` order: `wp_http_validate_url` → scheme allowlist → host equality. `queue_pagespeed_scan` order: scheme → host equality → `wp_http_validate_url`. Both enforce same checks but order differs. | Minor inconsistency; scheme before `wp_http_validate_url` in pagespeed means `file://` blocked early but `wp_http_validate_url` additionally rejects loopback/private (including IPv6, 0.0.0.0). Not exploitable but drift. | Normalize order to `scheme → wp_http_validate_url → host equality` in both; extract `Util::validate_local_url()`. | LOW |
| REST-08 | LOW | `class-rest.php:1348-1352` | Performance / Side effect | `if(!empty(results['lcp_image_url'])) Pagespeed::store_lcp_image_url(url,results,strategy)` — runs on every GET poll (side effect on read). | Rapid polling spams `update_option`/`set_transient` per request though dedup inside mitigates; violates GET idempotence principle. | Throttle or move to `Pagespeed::run_scan` completion only; keep idempotent check but document. | LOW |
| REST-09 | MEDIUM | `class-rest.php:1557-1584` | Auth / Expensive operation | `ai_learn` at `1569` does `$model = AI_Adaptive::learn()` with no input validation, no rate limiting, no capability beyond `manage_options`. Learning may be CPU-heavy. | Authenticated admin can spam `POST /ai_learn` causing repeated heavy compute (DoS via admin). No concurrency lock visible. | Add transient rate limit or lock (e.g. 5min cooldown) similar to RUM; gate on `AI_Adaptive::is_enabled()` explicitly. | MEDIUM |
| REST-10 | INFO | `class-rest.php:269-275,322-362,1609-1618` | Correctness / Redaction | `update_settings` strips `password` → `password_set` flag at `480-486`, preserves `pagespeed_api_key`/`server_timing_enabled`/`auto_rescan` when omitted, redacts before response at `509-515`; `import_settings` mirrors. `send_response` returns `{success,data,message}` at fixed 200/4xx/5xx. | Prevents credential leak via `wppoSettings` / API response. Correct. | No change — keep invariant. | HIGH |
| REST-11 | LOW | `class-rest.php:564-600` | Validation / Image path | `webp_images = array_map('sanitize_text_field', (array)$params['webp'])` then `foreach merge as img_path: source = ABSPATH.img_path; if(!file_exists) continue; $resolved=realpath(source); if(false===resolved || strpos(normalize(resolved), normalize(ABSPATH))!==0) return 400` | `sanitize_text_field` preserves `../` until `realpath` check — validation below catches it, safe. External URLs like `https://evil.com/img.jpg` → `ABSPATH . 'https://…'` file_exists false → skipped, later 0 jobs queued silently. | Add explicit allowlist: reject paths containing `://` or starting with `/`. | LOW |
| REST-12 | LOW | `class-rest.php:1451-1492` | Validation / Missing check | `used_css_regenerate` queues `wppo_used_css_generate` for `post_id` without `get_post(post_id)` existence check; enqueues even for trashed/invalid IDs. | Invalid ID queues job that will fail later; wasted scheduler slot. | Validate `get_post(post_id)` exists and is `publish` else 404. | LOW |

### 4.2 `includes/class-rum.php` — 429 lines

| ID | Severity | File:Line | Category | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------|----------|--------|----------------|------------|
| RUM-01 | MEDIUM | `class-rum.php:195-200,219-243` | Auth / Token scope | `token_for(time, path)` at `219` is `wp_hash('wppo_rum_' . Ymd . '|' . path)` where `path` from `print_config()` is `esc_url_raw(wp_unslash($_SERVER['REQUEST_URI']))` (may include query string). `is_valid_token(token,path)` checks today + yesterday per path, so token minted for `/page/?a=1` cannot be replayed on `/other/` (scoped). | Good scoping limits blast radius, but query-string variation means `/page/` vs `/page/?utm=…` have different tokens; beacon from cached HTML with stale query may fail validation (false 401). | Document that token is per exact `REQUEST_URI` including query; consider stripping query or using `PHP_URL_PATH` for token scope to reduce false negatives. | MEDIUM |
| RUM-02 | MEDIUM | `class-rum.php:251-259` | Rate limiting / Race | `is_rate_limited(ip)`: `$key=transient_key('wppo_rum_ratelimit_'.md5(ip)); $count=(int)get_transient(key); if(count>=120) return true; set_transient(key,count+1,HOUR)` — read-modify-write without atomic increment. | Concurrent beacons from same IP (burst via `navigator.sendBeacon` parallel) can race past 120 check (TOCTOU). Low abuse but public endpoint. | Use atomic increment if object cache supports `wp_cache_incr` else accept small race; document. | MEDIUM |
| RUM-03 | LOW | `class-rum.php:267-304` | Validation / Clamping | `sanitize_sample()` parses `path` via `wp_parse_url(path, PHP_URL_PATH)` fallback `/`, `substr(path,0,512)`, ranges `ttfb/fcp/lcp/inp 0-60000`, `cls 0-1`, `is_finite` check, clamps via `max(min)`. | `cls` clamped to 1 but real CLS can exceed 1 (poor pages); clamping hides severe shifts. `inp`/`lcp` 60s cap generous but ok. Non-finite returns `null` → 400 Invalid payload, correct. | Consider `cls` max 5 or no clamp; document caps. | LOW |
| RUM-04 | MEDIUM | `class-rum.php:317-341,353-427` | Performance / Persistence | `store_sample(sample)` attaches `_ts=time()`, pushes to transient queue `wppo_rum_queue` (HOUR expiry), caps `QUEUE_MAX 100` via `array_slice(-100)`, flushes at `FLUSH_THRESHOLD 20` or `rand 1/10` or schedules `wppo_rum_flush` single event in 300s. `flush_queue()` at `353` uses transient lock `wppo_rum_flush_lock` 30s, `delete_transient(queue)` before aggregation, iterates queue, builds `wppo_web_vitals_rum` option bounded `MAX_DAYS 14` + `MAX_PATHS_PER_DAY 200` via `array_shift(day)`, cutoff via `Y-m-d` string compare. | Reduces `get_option/update_option` from 1/beacon to ~1/20 beacons — good. But `array_shift` on associative `day` (path→bucket) evicts insertion-order oldest, not necessarily lowest-value path; acceptable but implicit. Option write on every flush may still contend under high traffic (lock 30s). | No urgent change; consider `wp_cache` for queue if object cache available; ensure `delete_transient(lock)` in `finally` (already). | MEDIUM |
| RUM-05 | LOW | `class-rum.php:153-158,353-427` | Correctness / Flush on read | `get_data()` opportunistically `flush_queue()` before `get_option(OPTION)`. | Admin dashboard read triggers write (lock + update_option) —GET causes write, unexpected but ensures freshness. High admin polling could cause extra writes. | Acceptable; document that `GET /rum_data` is not side-effect-free by design. | LOW |
| RUM-06 | INFO | `class-rum.php:89-92,100-144` | Correctness / Gates | `is_enabled()` checks `wppo_settings[performance_audit][rum_enabled]`; `collect()` early returns 400 if disabled, 401 if token invalid, 429 if rate-limited. | Correct gating; order token before IP avoids rate-limit oracle for invalid tokens. | No change. | HIGH |
| RUM-07 | INFO | `class-rum.php:165-180,190-205` | Correctness / Frontend | `maybe_enqueue_scripts` checks `is_enabled() && !is_admin()`, loads `build/rum.asset.php` deps/version, `wp_enqueue_script('wppo-rum', ..., true)`; `print_config()` bakes `apiUrl+token+path` into cached HTML via `wp_footer` so cache-hit still works. `echo '<script id="wppo-rum-config">window.wppoRum=' . wp_json_encode(config) . ';</script>'` — JSON-encoded, not escaped. | Correct cache-baking design; token rotation handled via daily `Ymd`. | No change. | HIGH |

### 4.3 `includes/class-pagespeed.php` — 661 lines

| ID | Severity | File:Line | Category | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------|----------|--------|----------------|------------|
| PS-01 | MEDIUM | `class-pagespeed.php:119-133` | Reliability / Scheduler | `queue_scan(url,strategy): if(!function_exists('as_enqueue_async_action')) return 0; return (int)as_enqueue_async_action(AS_HOOK, [[url,strategy]], AS_GROUP)` — returns 0 on missing Action Scheduler, no `wp_error`. REST handler at `class-rest.php:1295-1309` checks `!function_exists` before calling and returns 500; but CLI `WPPO_CLI_Command::pagespeed` at `885-895` checks `job_id<=0` and errors. Inconsistent sentinel. | Silent 0 job_id if scheduler missing but REST already guarded; CLI path correctly errors. No validation of `url`/`strategy` inside `queue_scan` itself (relies on caller). | Add validation inside `queue_scan` (host equality) or document caller responsibility; standardize error sentinel to `WP_Error`. | MEDIUM |
| PS-02 | LOW | `class-pagespeed.php:146-262` | Security / Request | `run_scan(args)` does `esc_url_raw` + `sanitize_text_field(strategy)`, validates empty url, empty api_key → `store_failure` with 5min TTL, validates `wp_http_validate_url` for SSRF, builds `query_url` via `add_query_arg` + manual `&category=` loop with `rawurlencode`, redacted log via second `add_query_arg` without key, `wp_remote_get(query_url, [timeout 120, sslverify true])`, handles `is_wp_error` with `sanitize_text_field(str_replace(ABSPATH,'',msg))`, HTTP 200 check, JSON decode, `body['error']` check, then `prepare_response`, `set_transient`, `register_transient_key`, `record_trend`, `store_lcp_image_url`. | 120s timeout may block scheduler worker; but async. Redacted log prevents key leak — correct. ABSPATH stripping from error message prevents path disclose — good. | Consider lowering timeout to 60s + retry? Already single attempt. No change. | HIGH |
| PS-03 | MEDIUM | `class-pagespeed.php:344-382,394-416` | Concurrency / Lock | `record_trend()` at `344` serializes via `acquire_trend_lock()` which does `add_option(TREND_LOCK_OPTION,time())` atomic INSERT, else checks `time-held >60` then `update_option` steal. `try{ re-read get_trends(), append snapshot, prune, update_option } finally release`. | Correct cross-request lock even without shared object cache (DB atomic). Stale lock theft after 60s prevents wedged worker. Risk: two workers both `add_option` race — one wins, other sees existing and checks staleness (not stale → returns false → skip trend). Losing one snapshot under contention but avoids lost update. Documented comment. | No change; ensure `TREND_LOCK_TTL` 60s is sufficient for `update_option` latency. | HIGH |
| PS-04 | LOW | `class-pagespeed.php:429-459` | Correctness / Pruning | `prune_trends(hist)` at `429` caps `TREND_MAX_KEYS 20` by ranking `strtotime(fetched_at)` ascending, `array_shift` oldest keys. `strtotime` on invalid `fetched_at` yields 0 → ranked oldest, pruned first — correct fallback. | Bounded option growth — good. But `end(snapshots)` to get last snapshot assumes array non-empty; if empty snapshots, `$last=0`. Edge empty array not expected but safe. | No change. | HIGH |
| PS-05 | MEDIUM | `class-pagespeed.php:578-611` | Correctness / Extraction | `extract_lcp_image_url(diagnostics)` priority 1: `prioritize-lcp-image.details.items[0].url` via `esc_url_raw`; priority 2: `largest-contentful-paint-element` url or `node.snippet` regex `/<img\s[^>]*src=["']([^"']+)["']/i`. Regex misses `srcset` or `<picture><source>` or background-image LCP, returns null → no preload. | Fallback regex brittle for malformed snippet HTML; but priority 1 covers modern audit. `esc_url_raw` may return empty for protocol-relative `//cdn/...`? It allows `//`? `esc_url_raw` with `//` may strip? Should use `esc_url_raw` with allowed protocols. | Consider `esc_url_raw` already handles `http/https` only; protocol-relative may be rejected — but LCP image from same origin likely absolute. Keep. | MEDIUM |
| PS-06 | LOW | `class-pagespeed.php:629-659` | Performance / Side effect | `store_lcp_image_url(url,prepared,strategy)` normalizes `scan_url` and `home` via `untrailingslashit(esc_url_raw(add_query_arg([],url)))`, front page → `update_option('wppo_front_page_lcp_'.strategy)`, singular → `update_post_meta(post_id,'_wppo_lcp_image_url_'.strategy)`, else transient `wppo_lcp_url_strategy_md5` DAY TTL. Called on every `GET /pagespeed_results` polling plus on `run_scan` completion. | Duplicate writes on poll (similar to REST-08) though dedup via `existing !== lcp_url` check for front page only; post_meta always `update_post_meta` even if same value (extra write). | Only write if `get_post_meta` differs or via `add/update` conditional. | LOW |
| PS-07 | INFO | `class-pagespeed.php:487-563` | Correctness / Prepare | `prepare_response(response)` extracts `lighthouseResult.categories` scores `*100`, `vitals_map` 6 vitals with `value/display_value/score`, `diagnostic_ids` 9 audits. Returns `{scores,vitals,diagnostics,strategy,fetched_at,lcp_image_url}`. Scores normalized `str_replace('-','_',key)` — correct. `fetched_at` via `current_time('mysql',true)` (GMT). | Complete Lighthouse extraction; missing `CLS` diagnostic? Covered via vitals. | No change. | HIGH |

### 4.4 `includes/class-telemetry.php` — 985 lines

| ID | Severity | File:Line | Category | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------|----------|--------|----------------|------------|
| TEL-01 | HIGH | `class-telemetry.php:204-249,367-428,442-519` | Security / SSRF | `scan()` validates `wp_http_validate_url`, scheme `http/https`, host equality via `Util::cached_home_url()` before any fetch. `fetch_via_curl` at `367` loops `MAX_REDIRECT_HOPS 2`, `execute_curl` at `204` sets `CURLOPT_FOLLOWLOCATION false`, `CURLOPT_PROTOCOLS HTTP|HTTPS`, manual hop via `resolve_redirect(location,current_url)` at `292` which resolves absolute/protocol-relative/relative then re-validates `wp_http_validate_url` + scheme + host equality. `fetch_via_wp_remote` at `442` uses `'redirection'=>0` and same `resolve_redirect` loop. | Strong SSRF defense — libcurl/persistent redirect not followed implicitly; each hop validated same-host. `CURLOPT_PROTOCOLS` prevents `file://`. 2-hop limit prevents open redirect amplification. Correct. | No change — exemplary. | HIGH |
| TEL-02 | MEDIUM | `class-telemetry.php:292-340` | Correctness / Redirect resolve | `resolve_redirect` at `292` handles `//` protocol-relative via `wp_parse_url(current_url, PHP_URL_SCHEME)`, relative via `dirname(path)` + `host+port+base_dir+'/'+location`. Uses `dirname('/')` → `'/'` then `//` double-slash? Actually `base_dir = dirname('/path/file') → '/path'`, then `resolved = scheme://host:port + base_dir + '/' + location` → `https://h/p//rel` maybe double? But `wp_http_validate_url` will reject malformed? | Relative `location="foo"` on `https://example.com/a/b/` → `dirname('/a/b/')`? `dirname('/a/b/')` in PHP returns `/a` (trailing slash trimmed), so `resolved = https://host/a/foo` correct. Edge `location` absolute `/new` handled via `strpos('/' )===0` branch correct. | Add unit test for relative resolution edge cases. | MEDIUM |
| TEL-03 | LOW | `class-telemetry.php:540-665` | Correctness / Parsing | `parse_resources(html)` uses `WP_HTML_Tag_Processor` when available, else regex fallback. Processor increments `dom_size` per tag, switches `LINK rel=stylesheet→css`, `SCRIPT src or wppo-src→js`, `IMG data-src?src + loading=lazy→lazy`. Regex fallback `preg_match_all('/<link\b(?=[^>]*\brel=["\']?stylesheet["\']?)[^>]*\bhref=["\']([^"\']+)["\'][^>]*>/i'` covers `rel` before `href` but not `rel=stylesheet` with single quotes missing? Pattern allows. JS regex `/(?:src|wppo-src)=["']([^"']+)["']/i` matches either. Image regex extracts `data-src` priority then `src`, plus `loading=lazy`. | Processor path accurate, fallback regex coverage good. `dom_size` via `preg_match_all('/<[a-zA-Z]/')` counts tags but includes comments? Slight overcount but metric not critical. | No change. | HIGH |
| TEL-04 | MEDIUM | `class-telemetry.php:682-744` | Performance / External requests | `calculate_sizes(resources)` inner `$get_size(url)` does `Util::get_local_path(url) → filesize` else if same-host and `wp_http_validate_url` then `wp_remote_head(url, [timeout 5, sslverify filterable])` → `content-length`. Loops over css+js+images sequential. | Many assets (e.g. 30 CSS/JS) → 30 synchronous HEAD requests with 5s timeout each could make scan >150s. No batch/parallel. Fallback may dominate scan time. | Cache HEAD results per URL, or limit HEAD attempts to local-path only (return 0 if not local). Document local-only intent. | MEDIUM |
| TEL-05 | LOW | `class-telemetry.php:770-820,847-881` | Correctness / Header checks | `check_compression(headers)` checks `content-encoding` for `gzip/br/zstd/deflate` via `stripos`; `get_header_value` handles object `offsetGet` vs array lowercase/Title-Case. `check_cache_control` regex `/max-age\s*=\s*(\d+)/i` then `>=604800` (1 week). | Correct handling of both header forms. `check_cache_control` only checks `max-age` directive, ignores `no-cache`/`no-store` edge but intentional. | No change. | HIGH |
| TEL-06 | INFO | `class-telemetry.php:682-744,946-983` | Correctness / Cache index | `register_transient_key(key)` at `957` uses associative `wppo_transient_index` with `expiry = now+HOUR`, prunes when `count>200` by removing expired then `asort` by expiry and `array_slice(-200)`. `scan()` registers transient key only in non-salted fallback branch (salted uses `wp_cache_get_salted` not transients). | Bounded index, reduces race impact. Salted path avoids index bloat — correct. | No change. | HIGH |
| TEL-07 | LOW | `class-telemetry.php:857-881` | Correctness / Robots | `check_robots_txt(url)` builds `scheme://host/robots.txt`, validates via `wp_http_validate_url`, `wp_remote_get` timeout 5, checks `200===code`. | Host extraction via `parsed['host']` ignores port — `https://example.com:8443/robots.txt` would check `https://example.com/robots.txt` missing port. Minor. | Include port when present. | LOW |

### 4.5 `includes/class-system-info.php` — 633 lines

| ID | Severity | File:Line | Category | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------|----------|--------|----------------|------------|
| SYS-01 | INFO | `class-system-info.php:94-113,572-617` | Security / Redaction | `get_php()` returns `PHP_MAJOR.MINOR` only (patch redacted), comment explains fingerprinting. `redact_version(version)` at `572` via `preg_match('/^(\d+\.\d+)/')` drops suffix like `-MariaDB`. `get_server()` at `211` normalizes `SERVER_SOFTWARE` via `families` map to `Apache/nginx/...` or `Unknown`, not raw banner. | Prevents version disclosure via REST/CLI. Correct. | No change. | HIGH |
| SYS-02 | LOW | `class-system-info.php:127-145,548-559` | Correctness / DB | `get_database()` sets `extension=get_class($wpdb->dbh)` if object, `client_version=null` (always null — TODO?), `server_version=redact_version($wpdb->db_version())`, `max_connections=get_mysql_var('max_connections')` via `$wpdb->prepare('SHOW VARIABLES LIKE %s')` + `get_row` → `row->Value`. | `client_version` always null, not populated (maybe intentional after redaction). `get_mysql_var` uses `->Value` (capital V) correct for `SHOW VARIABLES`. No sanitization issue as prepared. | Populate or remove `client_version` field to avoid confusion. | LOW |
| SYS-03 | INFO | `class-system-info.php:159-175,211-223` | Correctness / WP | `get_wordpress()` try/catch `is_multisite()` for Brain Monkey tests; `get_server()` null-safe for missing `SERVER_SOFTWARE`. | Robust for tests and missing server var. | No change. | HIGH |
| SYS-04 | MEDIUM | `class-system-info.php:269-356` | Performance / IO | `get_litespeed()` at `269` merges `LiteSpeed_Integration::get_info()` if exists, else `Server_Rules::get_server_type()`, then reads `advanced-cache.php` and `object-cache.php` dropin contents if `is_readable` and `filesize<1M` via `file_get_contents`. | Two file reads per `GET /system_info` (and CLI `system_info`) — okay but on every dashboard load. No caching; file read may be expensive on NFS. | Cache result in static or transient for request; already per-request but could still read twice. Add `static $cached` guard. | LOW |
| SYS-05 | LOW | `class-system-info.php:398-456` | Correctness / Opcache | `get_opcache()` checks `function_exists('opcache_get_status')`, `@opcache_get_status(false)` (silenced), formats `memory_usage`, `interned_strings`, `hit_rate` with `size_format`/`number_format_i18n`. Returns `status Disabled by configuration` if `false===opcache`. | Handles `restrict_api` blocking (returns false). `@` suppression needed. Good. | No change. | HIGH |
| SYS-06 | LOW | `class-system-info.php:506-539` | Correctness / Cache plugin detect | `get_active_cache_plugin()` merges `active_plugins` + `active_sitewide_plugins` on multisite via try/catch, `dirname(plugin_path)` to get slug, handles single-file `'.'`. Checks against `$cache_plugin_slugs` (8 entries, excludes self). | Correct multisite handling. Slug extraction may mis-handle `wp-rocket/wp-rocket.php` → `wp-rocket` correct. | Add `litespeed-cache` detection via slug? Already includes. Keep updated. | LOW |
| SYS-07 | INFO | `class-system-info.php:65-77,367-379` | Correctness / Aggregate | `get_all()` returns 7 groups: `php,database,wordpress,wp_constants,server,cache,litespeed,infrastructure,opcache` (plus `wp_constants`). Used by REST `system_info` and CLI `system-info`. | Complete. | No change. | HIGH |

### 4.6 `includes/class-wppo-cli-command.php` — 956 lines

| ID | Severity | File:Line | Category | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------|----------|--------|----------------|------------|
| CLI-01 | MEDIUM | `class-wppo-cli-command.php:75-124` | Consistency / Path | `cache` subcommand: `page = assoc['page'] ?? null; path = wp_normalize_path(trim((string)wp_parse_url(page, PHP_URL_PATH),'/')); cleared=Cache::clear_cache(path)` — strips slashes, while REST `clear_cache` uses `wp_normalize_path(path)` + `realpath` prefix. Normalization diverges. | CLI `clear --page=/sample-page/?ver=123` vs REST may map to different cache dir (trailing slash, query). | Share `Util::normalize_clear_path()` helper. | MEDIUM |
| CLI-02 | MEDIUM | `class-wppo-cli-command.php:232-260` | Completeness / Drift | `database cleanup` switch handles `revisions, auto_drafts/drafts, trashed_posts/trash, spam/spam_comments, expired_transients/transients, orphan_postmeta/orphans` (6) but REST `valid_types` includes 9 plus `trashed_comments, unattached_media, oembed_cache` (plus `all`). Missing `trashed_comments`, `unattached_media`, `oembed_cache`. | CLI cannot clean 3 types REST can — operator using CLI misses optimization. | Add `case 'trashed_comments'/'trashed'`, `unattached_media`, `oembed_cache` mirroring `Database_Cleanup::CLEANUP_METHOD_MAP`. | HIGH |
| CLI-03 | LOW | `class-wppo-cli-command.php:233-235` | Logic / Retention | `clean_revisions()` called without args; REST does `invoke_cleanup_method('clean_revisions_advanced', max_age, keep_latest)` with `dbRevMaxAge/dbRevKeepLatest`. | CLI ignores revision retention settings, may delete more. | Pass settings via `Database_Cleanup::get_revision_defaults()` same as REST. | MEDIUM |
| CLI-04 | LOW | `class-wppo-cli-command.php:177-193` | Validation / Allowlist | `database optimize` accepts arbitrary `tables` comma list, `optimize_table(table)` per entry without allowlist. | `wppo database optimize --tables=users,usermeta` could OPTIMIZE arbitrary tables if identifier not validated. | Allowlist `posts,postmeta,comments,commentmeta,options` or delegate to `TABLE_MAP`. | MEDIUM |
| CLI-05 | LOW | `class-wppo-cli-command.php:308-349` | Validation / Batch | `image convert` derives `conversionFormat` from settings or `--format`, `batch = settings[image_optimisation][batch] ?? 50` unchecked, `both` processes `2*batch`. | Batch 5000 could OOM synchronously. | Clamp `batch = max(1, min(200, (int)batch))`; validate `format in [webp,avif,both]`. | MEDIUM |
| CLI-06 | MEDIUM | `class-wppo-cli-command.php:336-341` | Security / Traversal | `source_path = ABSPATH.img; resolved=realpath(source); if(false===resolved \|\| strpos(normalize(resolved), normalize(ABSPATH))!==0) continue` — skips traversal, no logging. | Poisoned `pending` entry stays pending forever (never failed). | Log warning for skipped traversal. | LOW |
| CLI-07 | HIGH | `class-wppo-cli-command.php:605-659` | Correctness / Import | `settings import` allowlist 8 keys includes `core_tweaks` but omits `litespeed_integration,llms_txt`; REST opposite. New settings sanitized, password→`password_set`, `pagespeed_api_key` stripped, then `merged = array_replace_recursive(existing, new_settings)` where `existing=get_option('wppo_settings',[])` may be empty before seed → partial option missing defaults. | Import via CLI on fresh install yields partial `wppo_settings`; later admin expects defaults → undefined index. | Start from `get_default_settings()` merged with existing before overlay. Centralize allowlist. | MEDIUM |
| CLI-08 | MEDIUM | `class-wppo-cli-command.php:690-728` | Security / Password | `settings update` does `sanitize_recursively(new_settings)` then `options[tab]=array_replace_recursive(options[tab], new_settings)` without password→`password_set` conversion or API key stripping as in REST/import. | `wppo settings update object_cache --settings='{"password":"secret"}'` persists plaintext password in DB (REST prevents). | Handle password as in import; strip `pagespeed_api_key` or warn. | MEDIUM |
| CLI-09 | MEDIUM | `class-wppo-cli-command.php:847-855` | Validation / Sanitization | `get_redis_config_from_assoc` copies `host,port,password,database,timeout,prefix` without sanitization (`sanitize_text_field`, `(int)port`) before `Object_Cache::ping/enable`. REST `build_redis_config` sanitizes and allowlists 10 keys. | Unsanitized CLI args flow to Redis config (injection surface though privileged). Drift causes CLI enable to miss `use_tls` etc. | Reuse `build_redis_config` logic; sanitize. | HIGH |
| CLI-10 | LOW | `class-wppo-cli-command.php:885-915` | Validation / SSRF | `pagespeed scan` does `url=assoc['url'] ?? cached_home_url()` without host/scheme validation before `queue_scan`. | `wp wppo pagespeed scan --url=https://evil.com` queues external domain (internal may still validate but bypasses REST gate). Low risk (CLI privileged). | Add same host validation or delegate to `Pagespeed::queue_scan`. | MEDIUM |
| CLI-11 | INFO | `class-wppo-cli-command.php:434-505` | Duplication / Defaults | `get_default_settings()` returns 7 tabs with full defaults; duplicated from `Activate`/`Main`. | Drift risk (already `file_optimisation` 32 vs 28 keys). | Centralize `Util::get_default_settings()`. | HIGH |
| CLI-12 | INFO | `class-wppo-cli-command.php:425-556` | Docs / Duplicate | Docblock for `settings` appears twice (orphaned above `get_default_settings` + real above `settings()`). | `wp help wppo` duplicate. | Remove first orphan block. | HIGH |

### 4.7 `includes/class-log.php` — 150 lines

| ID | Severity | File:Line | Category | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------|----------|--------|----------------|------------|
| LOG-01 | LOW | `class-log.php:51-76` | Validation / Truncation | `add(activity)`: `$wpdb->insert(prefix.'wppo_activity_logs', ['activity'=>wp_kses_post(activity)], ['%s'])` — no length check; table `activity varchar(255)` via `class-activate.php:321`. `wp_kses_post` allows HTML `<a>` but varchar may truncate long URL log. | Long activity string silently truncated by DB (no error). | Enforce `substr(wp_kses_post(activity),0,255)` or change column to `text`. | LOW |
| LOG-02 | MEDIUM | `class-log.php:69-75` | Concurrency / Cache | `if(result){ if(function_exists('wp_cache_get_salted')) update_option(SALT_KEY,time()); else update_option('wppo_activity_cache_version', (int)get_option(...)+1) }` — increment via `get_option` + `update_option` not atomic. | Concurrent `Log::add` from parallel requests (e.g. batch image convert) may read same version, both write `+1` → lost increment, stale cache served. | Use `wp_cache_incr` or `update_option` with autoload false still race; acceptable low impact (HOUR TTL). Document. | MEDIUM |
| LOG-03 | LOW | `class-log.php:87-148` | Correctness / Pagination | `get_recent_activities(params)`: `page=max(1,absint(page??1))`, `per_page=min(100,max(1,absint(per_page??10)))`, `offset=(page-1)*per_page`, cache key `wppo_activity_logs_{page}_{per_page}` via salted or `wppo_activity_logs_v{version}_page_…`, `get_results(prepare("SELECT * … ORDER BY created_at DESC LIMIT %d OFFSET %d", per_page, offset), ARRAY_A)` total `COUNT(*)`. | Correct pagination, cache key scoped. `created_at DESC` may be non-deterministic if same timestamp; add `id DESC`. | Add secondary `id DESC` ordering. | LOW |
| LOG-04 | INFO | `class-log.php:37-42` | Design / Singleton | `private function __construct(){}` prevents instantiation; all methods static. | Correct utility class pattern. | No change. | HIGH |

### 4.8 `includes/class-suggestion-engine.php` — 397 lines

| ID | Severity | File:Line | Category | Evidence | Impact | Recommendation | Confidence |
|----|----------|-----------|----------|----------|--------|----------------|------------|
| SUG-01 | MEDIUM | `class-suggestion-engine.php:67-92` | Validation / AI payload | `from_ai_adaptive()` at `67` checks `class_exists(AI_Adaptive) && is_enabled()`, then `foreach suggestions as s: if(!is_array(s) \|\| empty(s['metric'])) continue; validated[]=build(metric, value??'', unit??'string', status??'needs_improvement', description??metric, fix_action??'no_action_required') + (ai_payload? ['ai_payload'=>s['ai_payload']] : [])` — `ai_payload` passed through unsanitized/unchanged. | AI-provided `ai_payload` may contain arbitrary nested data rendered by React UI → XSS if UI injects HTML without escaping. Though `build` sanitizes top-level fields, `ai_payload` bypasses `VALID_FIX_ACTIONS` and `status` invariants. | Sanitize `ai_payload` via `sanitize_text_field` or restrict to known keys; document UI must escape. | MEDIUM |
| SUG-02 | LOW | `class-suggestion-engine.php:109-170` | Correctness / Thresholds | `from_telemetry(telemetry)` creates 4 suggestions: `ttfb` ≤200 good ≤500 needs_improvement else poor, `uses_modern_image_formats` higher_is_better 95/50, boolean `gzip_brotli_compression` and `cache_control_headers` via `make_boolean`. Overrides `value` with raw header if `comp_pass && 'pass'!==value` → `value=raw header`. | Thresholds hardcoded, not filterable. Modern image 95% good may be strict for mixed content sites. Boolean `value` becomes `pass`/`fail` then optionally raw header like `gzip, br` — unit `boolean` but value now header string, inconsistent. | Either set `unit` to `string` when overriding or keep `value` as boolean and add `raw_value` field. | LOW |
| SUG-03 | LOW | `class-suggestion-engine.php:179-258` | Correctness / PageSpeed | `from_pagespeed(pagespeed)` checks `lcp` via `vitals['lcp']['value']` in ms? Actually `prepare_response` stores `numericValue` as ms? At `class-pagespeed.php:514` `lcp value` is `numericValue` (ms). `from_pagespeed` does `lcp_s = lcp_ms/1000` then `make('lcp', lcp_s, 's', {good:2.5, poor:4.0})` — converts to seconds, correct. Diagnostics `score<0.9` then `make_score(score, …)` where `make_score` maps `>=0.5 → needs_improvement` else `poor`. | LCP conversion correct (ms→s). Score threshold `0.9` matches Lighthouse `>=0.9 good`. Missing `cls`/`tbt` suggestions from PageSpeed? Only 5 diagnostics + LCP covered. | Consider adding `cls` via `vitals['cls']` similarly; document omitted metrics. | LOW |
| SUG-04 | INFO | `class-suggestion-engine.php:376-396` | Correctness / Invariants | `build(metric,value,unit,status,description,fix_action)` validates `fix_action ∈ VALID_FIX_ACTIONS` else `no_action_required`, and `good → fix_action=no_action_required`. Returns all 6 fields. | Guarantees UI contract; `VALID_FIX_ACTIONS` includes 7 values, prevents arbitrary tab injection. Correct. | No change. | HIGH |

---

## 5. No-Issues (confirmed correct)

- **REST auth model:** 27 admin routes use `permission_callback` (`manage_options && wp_verify_nonce`) at `class-rest.php:357-362`; `rum_collect` intentionally public with token+rate-limit (`class-rest.php:215-229,277-289`, `class-rum.php:219-259`). Schema uniform at `class-rest.php:322-349`.
- **Credential redaction:** `update_settings` (`class-rest.php:480-486,509-515`), `import_settings` (`759-765,791-797`), `handle_object_cache` (`1089-1150` strips password unless filter), CLI `settings export/import` (`576-589,636-650`) never persist `password`/`pagespeed_api_key`.
- **SSRF hardening:** Telemetry (`class-telemetry.php:84-97,204-340,367-428,442-519`) and REST Pagespeed (`class-rest.php:1215-1288`, `class-pagespeed.php:164-170`) enforce `wp_http_validate_url` + scheme `http/https` + host equality vs `Util::cached_home_url()` + manual redirect validation (`CURLOPT_FOLLOWLOCATION false`, `'redirection'=>0`).
- **Path traversal:** Image optimization (`class-rest.php:590-599`, `class-wppo-cli-command.php:336-341`, `class-telemetry.php:114-142`) via `realpath` + `strpos(normalize(resolved), ABSPATH)===0`; cache path (`class-rest.php:402-433`) via `realpath` + prefix fallback.
- **RUM bounding:** Queue `100` + threshold `20` + lock `30s` + `14d × 200 paths/day` + metric clamping at `class-rum.php:58-83,317-422` prevents option bloat under public beacon.
- **PageSpeed trends:** Lock via `add_option` atomic INSERT + stale steal `60s` at `class-pagespeed.php:394-416`, cap `30` per key + `20` keys via `prune_trends` (`429-459`), failure sentinel `5min TTL` at `292-301`.
- **System info redaction:** PHP `MAJOR.MINOR` only, DB ` major.minor` via `redact_version`, server family via `normalize_server_software` (`class-system-info.php:104,140,215-220,572-617`).
- **Log pagination:** `prepare(LIMIT %d OFFSET %d)` + `per_page` cap `100` + salted/versioned cache at `class-log.php:90-144`.
- **Suggestion invariants:** `VALID_FIX_ACTIONS` allowlist + `good → no_action_required` at `class-suggestion-engine.php:376-396`, locale-independent bool checks (`from_telemetry` uses native `bool/float`).
- **CLI idempotency:** `cache status`, `database counts`, `image status`, `system_info` read-only; `settings get/export` never mutates (`class-wppo-cli-command.php:85-91,195-199,352-369,573-602,939-954`).

---

## 6. Duplicate / Dead Code

| ID | File:Line | Evidence | Impact | Recommendation |
|----|-----------|----------|--------|----------------|
| DP-01 | `class-rest.php:469,749` + `class-wppo-cli-command.php:627,711` | Settings allowlist `file_optimisation,…,llms_txt` vs CLI `…,core_tweaks` — 3 definitions, drift. | Backup/restore parity break. | Centralize `Util::ALLOWED_TABS/KEYS`. |
| DP-02 | `class-rest.php:402-433` + `class-pagespeed.php:164-170` + `class-telemetry.php:84-97` | SSRF host+`wp_http_validate_url` checks repeated with order swap. | Drift. | Extract `Util::validate_local_url()`. |
| DP-03 | `class-rest.php:590-599` ↔ `class-wppo-cli-command.php:336-341` | `realpath` + ABSPATH prefix guard duplicated. | Maintenance. | `Util::is_path_within_abspath()`. |
| DP-04 | `class-rest.php:648-657` ↔ `969-986` | Pending/completed/failed count logic duplicated. | Drift. | `Img_Converter::get_count_summary()`. |
| DP-05 | `class-wppo-cli-command.php:425-556` | Docblock for `settings` appears twice (orphaned above `get_default_settings`). | `wp help wppo` duplicate. | Delete first block. |
| DP-06 | `class-system-info.php:269-356` | Two `file_get_contents` reads per `get_litespeed()` call. | IO per dashboard load. | Static cache. |
| DP-07 | `class-log.php:69-75` + `class-telemetry.php:957-983` | Version/salt bump pattern duplicated across log and telemetry index. | Drift. | Shared helper. |

---

## 7. Open Questions (need owner decision)

1. **Q-AI-01:** Should `POST /ai_learn` have rate limiting / scheduling via Action Scheduler instead of synchronous `AI_Adaptive::learn()`? Current may block REST worker.
2. **Q-RUM-01:** Should token scope strip query string (`PHP_URL_PATH` only) to reduce false 401 on cached HTML with `utm` params? Current per-`REQUEST_URI` including query.
3. **Q-REST-04:** Should `clear_cache` with unknown `action` return `400 Invalid action` instead of falling through to `clear_all`?
4. **Q-CLI-02:** Are `trashed_comments`, `unattached_media`, `oembed_cache` intended to be CLI-accessible? REST allows but CLI lists only 6 aliases.
5. **Q-TEL-04:** Should `calculate_sizes` fallback HEAD be limited or cached? Sequential HEAD for many assets may dominate scan time.
6. **Q-SYS-02:** `get_database()['client_version']` always `null` — intended to be removed or populated?
7. **Q-SUG-01:** Should `ai_payload` be sanitized / allowlisted before merging into suggestion response? Current passthrough.
8. **Q-LOG-03:** Should activity query order by `created_at DESC, id DESC` for deterministic pagination when timestamps collide?
9. **Q-PS-06:** Should `store_lcp_image_url` on `GET /pagespeed_results` polling be throttled to avoid write on read? Dedup only for front page.

---

## 8. Verification

- Read 8 files end-to-end (5831 lines) via `Read` (offsets for `class-rest.php`), Grep for `permission_callback`, `manage_options`, `__return_true`, `X-WP-Nonce`, `realpath`, `wp_http_validate_url`.
- Traced 28 REST routes in `Rest::get_routes()` (`class-rest.php:74-261`) — all callbacks and permission handlers inspected, compensating controls for `rum_collect` verified against `RUM::collect()` (`class-rum.php:100-144`).
- Traced RUM beacon lifecycle: `maybe_enqueue_scripts` → `print_config` → `collect` token/rate-limit → `sanitize_sample` → `store_sample` queue → `flush_queue` lock+aggregate+prune (`class-rum.php:165-427`).
- Traced PageSpeed: `queue_scan` → `run_scan` (API call, redacted log, error sentinel, `prepare_response`, `record_trend` lock, `prune_trends`, `extract_lcp_image_url`, `store_lcp_image_url`) at `class-pagespeed.php:119-659`.
- Traced Telemetry: `scan` cache → `fetch_via_curl`/`fetch_via_wp_remote` with `MAX_REDIRECT_HOPS 2` + `resolve_redirect` + `execute_curl`/`parse_header_block` + `parse_resources` (Tag Processor vs regex) + `calculate_sizes` + header checks (`class-telemetry.php:68-983`).
- Traced System_Info: `get_all` → `get_php/database/wordpress/wp_constants/server/cache/litespeed/infrastructure/opcache` + `redact_version`/`normalize_server_software` + `get_active_cache_plugin` multisite + `get_mysql_var` (`class-system-info.php:65-632`).
- Traced CLI 7 subcommands (`cache`, `database`, `image`, `settings`, `object-cache`, `pagespeed`, `system-info`) at `class-wppo-cli-command.php:75-956`, allowlist drift vs REST noted.
- Traced Log: `add` → `wp_kses_post` + salt/version bump, `get_recent_activities` paginated `prepare` + salted cache (`class-log.php:51-148`), schema not applicable.
- Traced Suggestion_Engine: `from_telemetry` 4 metrics, `from_pagespeed` LCP+5 diagnostics, `from_ai_adaptive` passthrough with `build` guard (`class-suggestion-engine.php:67-396`).
- Did not modify production code; write is to `AUDIT/AGENTS/agent-A04-php-api.md` only.

---

*Generated by Agent A04 — do not edit production files based on this audit without owner review. Update `AGENTS.md` if audit workflow changes.*
