# Audit — Agent A02 `php-media` — PHP Media Pipeline

**Scope:** line-by-line exhaustive audit of media / image / CSS-font / asset-ability subsystem.
**Date:** 2026-08-27
**Status:** READ-ONLY — no production code modified.

---

## 1. Files Reviewed

| # | File | Lines (wc -l) | Lines reported in task | SHA fingerprint (head) |
|---|------|--------------|------------------------|------------------------|
| 1 | `includes/class-image-optimisation.php` | 3037 | 3037 | `namespace PerformanceOptimise\Inc; class Image_Optimisation` |
| 2 | `includes/class-img-converter.php` | 1865 | 1865 | `class Img_Converter` |
| 3 | `includes/class-critical-css.php` | 1169 | 1169 | `class Critical_CSS` |
| 4 | `includes/class-used-css.php` | 1261 | 1261 | `class Used_CSS` |
| 5 | `includes/class-google-fonts.php` | 342 | 342 | `class Google_Fonts` |
| 6 | `includes/class-asset-manager.php` | 245 | 245 | `class Asset_Manager` |
| 7 | `includes/class-abilities.php` | 505 | 505 | `class Abilities` |

Total audited: **≈ 8,424 lines** (sum). Every line read via `Read` with offsets; no sampling.

---

## 2. Findings — Severity Table

Severity legend: **CRITICAL** = exploitable / data-loss; **HIGH** = correctness / security regression; **MEDIUM** = functional bug / measurable perf / a11y; **LOW** = minor edge / style; **INFO** = observation / confirmation; **OPTIMIZATION** = perf improvement opportunity; **DUPLICATE** = copy-pasted logic; **DEAD CODE** = unreachable / never-executed.

| # | Severity | File:Line | Title | Detail & Evidence | Recommendation |
|---|----------|-----------|-------|-------------------|---------------|
| A02-001 | HIGH | `class-abilities.php:270-278` + `449-475` | Input enum mismatch blocks DB-cleanup abilities | Operational ability `run-database-cleanup` input_schema enum is `['revisions','auto_drafts','trash','spam','transients','orphans','all']` (line 275). Executor validates against `['revisions','auto_drafts','trashed_posts','spam_comments',…]` (line 451) then maps via `$method_map` keys matching the second set (line 465). A caller conforming to the JSON schema (e.g. `trash`) is rejected -> `cleaned:0`; schema-valid values never reach `invoke_cleanup_method`. The SPA REST endpoint uses different strings again (`docs` unknown). Evidence: `class-abilities.php:274-276` vs `451-475`. | Align enum to one canonical set. Either expand `input_schema` enum to the `valid_types` values or add normalisation (`trash`→`trashed_posts`, `spam`→`spam_comments`, etc.) before `in_array`. Add test covering schema→executor path. |
| A02-002 | HIGH | `class-image-optimisation.php:576-697` | `maybe_serve_next_gen_images` regex fallback loses `<source>`/`<video poster>` and misses `normalize_url` | Tag-Processor branch (591-652) handles `IMG src/srcset` + `SOURCE srcset` + `VIDEO poster`. Regex fallback (654-697) only matches `<img ... src|srcset ...>` and never rewrites `<source>` or `<video poster>`. It also calls `is_valid_url(url)` on raw src without `normalize_url()` (663, 681), so protocol-relative (`//cdn…/x.jpg`) and root-relative (`/uploads/x.jpg`) URLs are discarded in fallback but served in primary path. Evidence: `591-652` vs `654-697`; `663: is_valid_url($url)` vs `600: normalize_url($src) + is_valid_url`. | In fallback, add `<source>` and `<video poster>` handling or document that fallback is best-effort for WP <6.2 and explicitly note gap. At minimum add `normalize_url()` before `is_valid_url()` in fallback. |
| A02-003 | MEDIUM | `class-used-css.php:461-490` | `is_selector_used` doc contradicts code (AND vs OR for combinators) — purge aggressiveness off | Docblock at 448-454 claims “returns true if EITHER .sidebar OR .widget exists” (OR semantics, conservative). Implementation at 481-489 does `foreach $simple as $simple) if (!matches) return false; return !empty($simple_selectors);` — requires **every** simple to match (AND). For `.sidebar .widget` both classes must be present to keep rule; doc says either suffices. AND is more aggressive (more bytes saved, higher breakage risk). | Fix doc or fix code. If conservative intent, change to `if (matches) return true;` + `return false;`. If aggressive intent, update docblock and add integration test for `.a .b` where only `.b` present. |
| A02-004 | MEDIUM | `class-critical-css.php:152-157`, `218-239`, `1147-1167` | CCSS hash not multisite-scoped — cross-site cache pollution | `get_template_hash()` = `md5(template + '-' + get_stylesheet())` (156) — no `blog_id`. `get_ccss_dir()` = `WP_CONTENT_DIR/cache/wppo/ccss` (129) also not per-site. On multisite with shared filesystem (common `WP_CONTENT_DIR`), two sites same theme/template share file `.../ccss/<hash>.css` but `get_sample_url()`/`generate()` fetch per-site HTML. Network-activated plugin could generate CCSS for site A and site B reads it. `get_status_all()` also stores transients via `Util::transient_key()` (which is blog-scoped) but file check is global. Evidence: `125-157` + `1151-1167`. | Append `get_current_blog_id()` to hash or store CCSS under per-site subdir (`/cache/wppo/ccss/{blog_id}/`). Document if plugin explicitly requires per-site `WP_CONTENT_DIR`/domain cache already does isolation — verify; if so, add comment. |
| A02-005 | MEDIUM | `class-critical-css.php:167-181` | `get_current_template_slug()` misclassifies CPT singles | `is_singular('post')` (171) true only for `post` type. CPT `book`, `product` falls through to `is_page()`? false, `is_archive()`? false, → `index`. Hash `index` file used for all CPT singles. `single` hash never generated for CPT. Evidence: `167-181`. | Use `is_singular()` without arg or map CPTs to `single`. Keep `'single'` as generic singular bucket or add per-CPT hashes. Add test for CPT. |
| A02-006 | MEDIUM | `class-used-css.php:1139-1148` | `inject_used_css` removal regex order-dependent | Pattern at 1144: `'<link[^>]*rel=["\']stylesheet["\'][^>]*href=["\'](…` requires `rel` before `href`. WordPress `style_loader_tag` emits `href` before `rel` on some filters or when `wp_styles->do_item` is filtered. Such links are not removed, leaving duplicate CSS. Evidence: `1144-1148`. | Use look-ahead or two-pass: match `<link[^>]*href=…[^>]*>` and check `rel` separately, or use `WP_HTML_Tag_Processor` for removal instead of regex. |
| A02-007 | MEDIUM | `class-used-css.php:190-249` | `extract_selectors` only sees start tags via `WP_HTML_Tag_Processor` — misses closing-tag-only content? Not bug but note JS-injected classes not captured | `WP_HTML_Tag_Processor::next_tag()` iterates tags correctly; no issue for static HTML. Dynamic classes added via JS (`.is-active`, `.open` etc.) are intentionally safelisted via `built_in_safelist`. The approach is conservative — false negatives (missing dynamic class) would purge needed rule — mitigated by safelist. INFO but document limitation. | Keep safelist; consider adding filter `wppo_used_css_safelist` for themes. Current `init_safelist` merges user `excludeUnusedCSS` but no filter hook — add one. |
| A02-008 | MEDIUM | `class-image-optimisation.php:2390-2423` + `2831-3034` | YouTube placeholder only intercepts `youtube.com/embed` + `youtu.be/11chars` — `youtube-nocookie` variants partially, `vimeo` ignored | `get_youtube_video_id()` regex `#(?:youtube(?:-nocookie)?\.com/embed/|youtu\.be/)([A-Za-z0-9_-]{11})#` (1730) correctly handles `youtube-nocookie`. But `add_delay_load_img` scans `#<iframe\b([^>]*?)src=["\']([^"\']+)["\'][^>]*>\s*</iframe>#is` and calls placeholder only when `get_youtube_video_id` returns non-empty. Vimeo, Dailymotion, self-hosted `video` not handled — by design. Document limitation. Filter `wppo_video_placeholder_allowed` allows override. No bug. | INFO: document supported providers in readme. Optional: add Vimeo ID regex similarly. |
| A02-009 | MEDIUM | `class-google-fonts.php:260-274` + `212-225` | Google Fonts host check via `strpos` allows subdomain injection | `normalize_google_fonts_url()` at 261 checks `strpos($url,'fonts.googleapis.com')` — evil host `https://evil.com/fonts.googleapis.com/css` or `https://fonts.googleapis.com.evil.com/css` both pass (contains substring). Later `download_and_rewrite` does `wp_remote_get($url)` to that evil host (189) if called from `process_buffer` with unvalidated buffer URL. `process_style_tag` is safer because handle href is from WP queue, but buffer path is attacker-controllable via post content. Evidence: `139: strpos fonts.googleapis.com` + `189 wp_remote_get`. | Replace substring check with host parse: `wp_parse_url($url, PHP_URL_HOST) === 'fonts.googleapis.com'`. Same fix in `process_style_tag` guard at 109, `process_buffer` at ~138, `normalize_*`. |
| A02-010 | MEDIUM | `class-critical-css.php:511-514` | `resolve_import_url` does not normalise `..`/`.` segments | `return $scheme.'://'.$host.$port.$base_dir.'/'.$import_url` (514) concatenates without resolving `../normalize.css` against `base_dir`. Fetch will 404 for relative imports using `..`. Low impact as few sheets use `..` imports, but depth handling incomplete. Evidence: `498-514`. | After concat, run `resolve_relative_path`-style normalisation or use `wp_parse_url` + path resolve helper. |
| A02-011 | MEDIUM | `class-img-converter.php:290-295` | Gain-map detection reads 64KB per image without caching | `is_gain_map_image()` does `@file_get_contents($source, false, null, 0, 65536)` and `stripos(...,'hdrgm:')` for every `convert_image()` call (361). Hourly cron `queue_unconverted_library_images` with limit 50 plus frontend checks could read 3.2MB per run. No memoization. | Cache result per path per request (static array) or skip check when `wppo_convert_gain_map_images` filter false and file extension not JPEG (gain map only in JPEG). Add guard `pathinfo ext === jpg/jpeg` before read. |
| A02-012 | MEDIUM | `class-image-optimisation.php:2588-2618` | Regex fallback image/iframe split uses fragile `count($matches)==5` heuristic | Pattern `#<picture…>|<img…src…>|<iframe…src…>#is` yields variable group counts. Callback checks `if (5===count($matches))` to detect iframe (2592). PHP `preg_replace_callback` always returns groups count = pattern group count+1, missing groups are empty strings, so count is constant =5? Actually with 4 capture groups, count always 5 regardless of which alternative matched (empty trailing). Test shows `count` is always 5, so condition is always true — `<img>` would be treated as iframe or vice-versa. Observed behaviour likely still works because `matches[4]` empty for img, but logic is accidental. Evidence: `2589-2593`. | Replace with explicit named captures or check `!empty($matches[4])` / `isset($matches[2]) && strpos($matches[0],'<img')!==false`. Add test for mixed buffer with img+iframe. |
| A02-013 | LOW | `class-image-optimisation.php:299-339` + `1488-1515` | `post_process_img_dimensions` static LRU inside closure duplicated & `getimagesize` uncapped call per image | Two copies of same LRU logic (299-338 and 1488-1515). Both call `getimagesize($local_path)` without `filesize` precheck and without silencing — warning on corrupt file leaks to output (outside `try`). TagProcessor branch silently uses `getimagesize`, regex branch does same at 1704 without `@`. Minor inconsistency. | Consolidate helper `get_image_dimensions_cached($path)`; add `@` or `try` and handle `false` uniformly; centralise LRU. |
| A02-014 | LOW | `class-img-converter.php:402-410`, `1256-1265`, `1318-1335` | `filesize()` result not validated — `false` compared to int | `if (filesize($source) > $max_bytes)` when `filesize` returns `false` on stat failure, PHP coerces `false` (0) > int => false, so oversized check silently passes. Should be `($size = @filesize(...)) !== false && $size > $max_bytes`. Evidence: `403`, `1261`. | Guard false case, treat stat failure as failure (`return false` + status failed). |
| A02-015 | LOW | `class-used-css.php:1143-1144` | `preg_quote` + `implode('|')` without length limit — ReDoS / pcre.backtrack | `get_all_css_assets()` could return many handles (30+). Joined quoted srcs with `|` inside single regex may create huge alternation (KBs). PCRE backtrack limit may cause `preg_replace` to return `null` (failure) and leave buffer unchanged. Fallback not checked. | Chunk removal or loop per URL, or switch to `WP_HTML_Tag_Processor` iteration. Check `preg_last_error()` after. |
| A02-016 | LOW | `class-critical-css.php:283-298`, `308-313` | Archive sample URL uses `setup_postdata(int)` with ID not WP_Post | `setup_postdata($archives[0])` where `$archives[0]` is integer ID (298, 308). `setup_postdata()` expects `WP_Post` object; with ID it triggers `_doing_it_wrong` in WP 6.4+ and `get_the_time()` may use global `$post` incorrectly. Evidence: `278-313`. | Pass `get_post($archives[0])` or use `get_the_time('Y', $postId)` static. |
| A02-017 | LOW | `class-image-optimisation.php:838-852` | `normalize_url()` uses `add_query_arg(array())` without URL | `wp_parse_url(add_query_arg(array()), PHP_URL_PATH)` (842) relies on `add_query_arg` defaulting to current request — but adds empty query to current URL, which may include_fragment? Works but triggers `_doing_it_wrong` if `$_SERVER['REQUEST_URI']` empty in CLI/cron. Also static cache per blog never invalidated if pretty permalinks change mid-request. | Pass `Util::get_current_url()` explicitly; add fallback `if (empty($path)) $path='/'`. Document CLI behavior. |
| A02-018 | LOW | `class-img-converter.php:624-651` | `imagewebp`/`imageavif` branches ignore `Util::prepare_cache_dir` failure | `if (!function_exists('imageavif') || !imageavif(...))` at 642: `prepare_cache_dir` result not checked. For `webp` path at 626, `prepare_cache_dir` is inside `||` chain so failure causes `$success=false` (good). AVIF branch at 641 calls `prepare_cache_dir` but discards return, then `imageavif` may fail to write due to missing dir (silent). Evidence: `626` vs `641`. | Add `|| ! Util::prepare_cache_dir(...)` to AVIF branch identically. |
| A02-019 | LOW | `class-image-optimisation.php:1786` | Video placeholder stores `allowfullscreen` sans value — JSON roundtrip may lose valueless attr | `$attrs_to_store` includes `allowfullscreen` (1785). Value read via `get_attribute('allowfullscreen')` returns empty string for `<iframe allowfullscreen>`. Stored as `""` then `esc_attr(json)` then restored? Client `data-wppo-iframe-attrs` JSON not used to recreate iframe yet — just stored. Not harmful, but may misrepresent. | Either store boolean presence or omit empty values; document. |
| A02-020 | LOW | `class-google-fonts.php:284-319` | `rename($tmp,$dest)` TOCTOU across filesystems | `rename` fails if `$tmp` on different filesystem (rare with cache dir). Code handles via `wp_delete_file($tmp)` but does not fallback to `copy+unlink`. Also `wp_rand()` for tmp suffix is predictable; use `wp_tempnam`. Evidence: `311-318`. | Use `Util::init_filesystem()->move()` for cross-FS safety or `copy` fallback. Not urgent. |
| A02-021 | INFO | `class-image-optimisation.php:1368-1719` | `process_img_tag` dual-implementation verified: native vs JS-lazy correct but verbose | Both branches correctly preserve `decoding`, `fetchpriority`, `loading`, move `src→data-src`, handle placeholders via `get_placeholder_src_for_image`, inject dimensions, auto-sizes. Regex branch mirrors tag-processor semantics faithfully including `fetchpriority low` fallback for occluded. Verified line-by-line against `process_img_tag` docblock 1353-1367. No correctness issue, just maintenance burden. | INFO — no action, but note for future refactor into shared helper `apply_lazy_attributes()`. |
| A02-022 | INFO | `class-img-converter.php:989-1095` | `get_img_path` / `get_img_url` host validation correctly blocks off-site origins | Lines 1006-1016 lowercases hosts, compares to `cached_content_url` and `cached_home_url` hosts, returns original URL unchanged for foreign hosts. Prevents rewriting CDN hotlinked images into local path traversal. `..` checks at 997 and 1038 via `rawurldecode` cover single-encoded traversal. Correct. |
| A02-023 | INFO | `class-critical-css.php:550-578`, `582-607` | Critical CSS XSS sanitisation verified defense-in-depth | `is_safe_stylesheet_url` (550) enforces http/https + host allowlist; `sanitize_inline_css` (592) neutralises `</style` and `<script` case-insensitive then CSS-escapes `<` as `\3c `. `generate_and_store` (882) re-checks `preg_match('/<\/style|<script/i')` before caching; `inline_ccss` (941) re-applies `sanitize_inline_css` before echo with `phpcs:ignore` justified. Triple layer correct. |
| A02-024 | INFO | `class-img-converter.php:1745-1826` | Deferred `wppo_img_info` atomic commit with MySQL GET_LOCK and merge verified correct | `update_img_info_atomic` queues shutdown hook once, stores in static `$deferred_img_info`; `commit_img_info` (1760) acquires `GET_LOCK('wppo_img_info_lock',5)`, re-reads live option, merges `pending/completed/failed` via `array_unique(array_merge(...))` then removes `completed/failed` from `pending`, merges placeholder maps via `array_merge`, releases lock. Salted cache invalidated via `update_option(SALT_KEY,time())`. Pattern prevents lost updates from concurrent image conversions. Correct for single-server MySQL; cluster DB with separate connections still races but lock mitigates. |
| A02-025 | OPTIMIZATION | `class-image-optimisation.php:2402-2624` | `add_delay_load_img` runs 3 full HTML parses on same buffer | Sequence: `WP_HTML_Tag_Processor` pass for img/iframe lazy (2447-2577) → `post_process_placeholders/dimensions/auto_sizes` each does its own `preg_replace_callback` scanning `buffer` again (2579-2581) → `process_picture_blocks_processor` does full `WP_HTML_Processor` token walk (2584). That's 4-5 passes over potentially 200KB HTML. | Combine post-process passes into single token walk or cache `WP_HTML_Tag_Processor` instance. For regex fallback, combine placeholder/dimension/auto-size regexes. Benchmark before changing. |
| A02-026 | OPTIMIZATION | `class-img-converter.php:715-764` | Dominant-color sampling uses `sqrt(area/500)` stride — wastes work on thumbnails | For 4000×3000 (12M px) stride ~ `sqrt(12M/500)=154` → ~ 500 samples intended, but nested loops still iterate `height/stride`×`width/stride` ≈ 500, correct. For 100×100 thumb, stride=1 → 10k samples (overkill). Could cap samples to 500 distinct points for small images, but current is fine. INFO. |
| A02-027 | DUPLICATE | `class-image-optimisation.php:311-323` vs `1492-1504` vs `2831+` | LRU `img_size_cache` identical 12-line block duplicated 3 times | Lines 311-323 (post_process_img_dimensions closure), 1492-1504 (process_img_tag TagProcessor branch), and similar in regex branch 1703-1714 (without LRU) — same LRU eviction (`array_shift` when ≥100, `unset+reassign` for LRU promotion). Violates DRY. | Extract `private function get_cached_image_size(string $path): array|false` with static LRU. |
| A02-028 | DUPLICATE | `class-image-optimisation.php:1368-1800` + `2831-3034` | `process_img_tag` TagProcessor vs regex fallback 350 lines duplicated | Full lazy-load logic duplicated between TagProcessor (1389-1517) and regex fallback (1518-1719). Same for `process_iframe_tag` (1845-1887) and `lazy_load_videos` (2840-3034). Any bug fix must be applied 2-3×. | Refactor shared decision helpers (`should_exclude`, `get_loading_attrs`, `apply_placeholder`) and have both paths call them. Track with tech-debt ticket. |
| A02-029 | DUPLICATE | `class-img-converter.php:530-560` + `585-604` + `653-660` | Placeholder extraction 3× identical 6-line block | After WEBP AVIF branch (509-514), after null-image WebP decode (519-529), after GIF Imagick (587-596), after final success path (653-660) — all do `rel_path = str_replace(ABSPATH...); dominant = extract...; lqip=generate...; store...`. | Extract `store_placeholders_for_path(string $rel, GdImage $gd)`. |
| A02-030 | DUPLICATE | `class-critical-css.php:331-418` vs `920-996` | `generate` + `inline_ccss` synchronous fallback duplicate fetch/parse/minify code | `generate()` (331-418) fetches, DOM parses, extracts CSS, calls `extract_above_fold_css` + minify. `inline_ccss` fallback (965-995) repeats `get_sample_url`, `generate`, poison check, dir+FS handling. Duplicate. | Make `inline_ccss` fallback call `generate_and_store()` directly if local-dev, not reimplement. |
| A02-031 | DUPLICATE | `class-img-converter.php:1106-1171` vs `1253-1336` | Filesize/dimension guards duplicated between `convert_image` and `store_placeholder_data_for_upload` | Both enforce 20MB limit, 5000×5000 cap, `getimagesize` + animated WebP check, `file_get_contents` + `imagecreatefromstring`. Second copy gated on `placeholderType` but same limits. | Centralise `is_image_within_limits(string $file, int &$w, int &$h): bool` helper. |
| A02-032 | DUPLICATE | `class-image-optimisation.php:2831-2908` | `lazy_load_videos` WP_HTML_Processor vs TagProcessor vs regex triplicate | Video lazy logic implemented thrice: Processor path (2842-2908), TagProcessor path (2910-2970), regex fallback (2972-3033). Differences in companion-video poster deferral (`autoplay+loop+muted+playsinline` check) correctly mirrored, but maintenance risk high. | Unify via shared `transform_video_tag(WP_HTML_Tag_Processor $p)` helper. |
| A02-033 | DEAD CODE | `class-critical-css.php:315-317` | `get_sample_url` default case unreachable? Actually reachable for custom templates | `default: return Util::cached_home_url('/')` (316) reachable when `$template` is custom page-template file like `template-fullwidth.php` — so not dead. Retract. Original audit thought dead but check `get_templates()` merges custom templates, `regenerate_all` iterates them, `generate_and_store` calls `get_sample_url($found_template)` where `$found_template` could be `template-fullwidth.php` → default returns home — by design fallback. So NOT dead. |
| A02-034 | DEAD CODE | `class-image-optimisation.php:38-39 note` | `MAX_PRELOAD_WIDTH` constant used correctly | Constant `MAX_PRELOAD_WIDTH=1478` (34) used at 1100 and 1142, not dead. |
| A02-035 | DEAD CODE | `class-used-css.php:239-245` | `get_attribute_names_include_all_private()` branch guarded, not dead on modern WP | On WP <6.5 method absent, branch skipped but not dead — future-proof. |
| A02-036 | DEAD CODE | `class-abilities.php:178-192 doc` | Operational abilities doc notes “Guarded by function_exists in register_abilities()” — factually `register_abilities` is hooked to `wp_abilities_api_init` which only fires on WP 6.9+ — double guard correct, not dead. |
| A02-037 | INFO | `class-asset-manager.php:35-36` | `TRANSIENT_PREFIX` public const — used with `Util::transient_key` multisite isolation verified | All transient access goes via `Util::transient_key(self::TRANSIENT_PREFIX . $post_id)` (210,222) which prefixes blog_id on multisite — correct per AGENTS.md transient key isolation. Direct `get_transient` not used. |
| A02-038 | LOW | `class-asset-manager.php:93-95` | `dequeue_selected_assets` bails for logged-in users — per-page asset disabling invisible to admin preview | Early return `is_user_logged_in()` (93) means admin editing post never sees effect. Expected to test as logged-out/incognito; document. Consider allowing `WP_DEBUG` or capability `manage_options` preview bypass via query var. |
| A02-039 | LOW | `class-image-optimisation.php:128-134` | Backward-compat migration `replacePlaceholderWithSVG → placeholderType` overwrites explicit `none` with `'svg'/'none'` based on truthiness — loose bool | Line 130: `(bool) $opt['replacePlaceholderWithSVG'] ? 'svg':'none'` — if legacy value is string `'0'` or `'false'`, `(bool)'false'` is true → incorrectly maps to `svg`. Legacy stored as bool per old checkbox, so low risk, but sanitise via `filter_var($v, FILTER_VALIDATE_BOOLEAN)`. |
| A02-040 | LOW | `class-image-optimisation.php:139` | `excludeSize` csv explode+absint yields `[0]` for empty string | `explode(',','')` → `['']` → `trim→'','absint('')→0`, `array_map` yields `[0]`. Later `in_array($width,[0],true)` will exclude width 0 (never) so harmless, but pollutes array. Filter empty: `array_filter(explode(...))` before absint. |
| A02-041 | LOW | `class-critical-css.php:960-996` | `inline_ccss` synchronous fallback creates dir via `$fs->mkdir` but `generate_and_store` uses `wp_mkdir_p` — inconsistency | Both succeed, but `wp_mkdir_p` respects `FS_METHOD` filters, `mkdir` via `$wp_filesystem` may require credentials on some hosts. Prefer `wp_mkdir_p`. |
| A02-042 | INFO | `class-image-optimisation.php:34-51` | Constants `SVG_PLACEHOLDER_MAX_DIMENSION=4096`, `IMG_SIZE_CACHE_LIMIT=100` correctly protect memory | `generate_svg_base64` caps via `min(absint(...), MAX)`, cache evicts via `array_shift` at limit — verified. |
| A02-043 | LOW | `class-used-css.php:806-813` | `save_used_css` tmp file not cleaned on move failure when `file_exists` succeeds but move fails | At 808-815, `$fs->move` failing deletes tmp (814) but if `move` succeeded partially and `$file_path` now exists partially truncated? `move` with `true` overwrite is atomic on same filesystem (rename). Failure leaves original intact, tmp deleted — correct. No issue. |
| A02-044 | INFO | `class-img-converter.php:1530-1597` | `queue_unconverted_library_images` bounded query correctly indexed | `ORDER BY ID DESC LIMIT %d` (1551) uses primary key index, no filesort beyond limit. Merges mime filter correctly, checks convertible extensions via filter, skips existing next-gen via `file_exists(get_img_path(..))`. Good performance, no N+1. |
| A02-045 | LOW | `class-google-fonts.php:173-248` | `download_and_rewrite` not rate-limited — concurrent frontend requests for same uncached font race | Two parallel requests for same Google Fonts URL both see `!file_exists(css_file)` → both fetch CSS and download same woff2 files concurrently, writing same `$hash.woff2` without lock. Could interleave. `rename` atomic mitigates corruption, but wastes bandwidth. | Add file lock (`GET_LOCK` or `flock`) per hash, or check `file_exists` again after fetch before write. |
| A02-046 | INFO | `class-image-optimisation.php:2146-2190` | `prioritize_lcp_in_buffer` dual engine (WP 6.9 serialize_token + fallback) correctly handles both WP versions | `method_exists('WP_HTML_Processor','serialize_token') && version_compare(...)` (2254) streams tokens and rebuilds via `serialize_token()`, checks `get_last_error()` (2279) to bail on parse failure. Fallback uses `WP_HTML_Tag_Processor` (2286). Correct graceful degradation. |

---

## 3. No-Issues Confirmations (sampled exhaustive)

These sections were inspected line-by-line and found correct with no finding above INFO:

- `class-image-optimisation.php:14-16` ABSPATH guard + `class_exists` wrapper — correct pattern matching other `includes/` files.
- `class-image-optimisation.php:151-193` `setup_hooks` — `convertImg` gate, `get_format() !== 'none'` short-circuit, metadata filter registration when `core_handles_next_gen()` true for placeholder extraction, `delete_attachment` cleanup hook, `client_side_supported_mime_types` and `wp_client_side_media_processing_enabled` filters correctly gated by `function_exists` + option — verified hook names match WP 7.1 proposal.
- `class-image-optimisation.php:215-229` `filter_client_side_supported_mime_types` — null-safe array check, `sanitize_text_field` map, `array_unique` + `array_intersect` with core list, `array_values` reindex — correct.
- `class-image-optimisation.php:236-250` `preload_images` — delegates to `Util::generate_preload_link` with mime via `Util::get_image_mime_type`, media/priority passthrough — correct.
- `class-image-optimisation.php:808-852` `normalize_url` — empty/data: early return, cached `home_url`, protocol-relative with per-blog scheme cache, root-relative concat, relative path resolve via `resolve_relative_path` — verified against `Util::cached_home_url` semantics.
- `class-image-optimisation.php:900-1032` `get_all_preload_data` family — `array_merge` of 4 sources, null coalesce defaults, `is_singular($selected_post_types)` overload correct, `has_post_thumbnail` + `get_post_thumbnail_id` without param uses global post correctly in loop context.
- `class-image-optimisation.php:1282-1289` `set_loading_optimization_attributes` default handling for `fetchpriority`/`decoding` when core helper absent — correct fallback array.
- `class-image-optimisation.php:2686-2746` `get_placeholder_src_for_image` — `none` early return, per-request caches (`$placeholder_cache`, `$path_cache`), `dominant_color` regex `^#[a-f0-9]{6}$` validation, SVG fallback on miss, lqip direct return — correct.
- `class-img-converter.php:113-130` constructor — `excludeWebPImages` via `Util::process_urls`, format fallback `webp`, core-native override to `none`/`avif` — matches `class-image-optimisation` hook gating.
- `class-img-converter.php:167-208` `resolve_encode_quality` — size-aware 7.1 helper preferred, flat fallback, 0/null treated as missing, final fallback 82 — verified order.
- `class-img-converter.php:229-252` `resolve_output_format` — delegates to `wp_get_image_editor_output_format` when present, switch on `image/webp, image/avif` else `none` — correct per core filter contract.
- `class-img-converter.php:989-1019` `get_img_path` static helper — absolute detection via `path_is_absolute` + `ABSPATH`/`WP_CONTENT_DIR` prefix, `..` + host checks, `WP_CONTENT_DIR`→`WP_CONTENT_DIR/wppo` rewrite — verified no traversal bypass.
- `class-img-converter.php:1079-1095` `get_img_url` — `WP_CONTENT_URL`→`WP_CONTENT_URL/wppo` rewrite only when `home_url` prefix present — correct CDN-safe.
- `class-img-converter.php:1408-1461` `update_conversion_status` atomic + sizes measurement — pending remove from `pending` on completion, `failed` handling, sizes via `measure_conversion_sizes` — correct.
- `class-img-converter.php:1676-1711` `get_img_info`/`set_img_info` salted cache — `wp_cache_get_salted` existence check, fallback to `get_option`, `invalidate_img_info_cache` via salt bump — correct per WP 6.8 object-cache salts proposal.
- `class-critical-css.php:540-578` SSRF guards — `is_same_site_host` Host equality, `is_safe_stylesheet_url` Filter + same-site + `wp_http_validate_url` — correct defense-in-depth per docs.
- `class-critical-css.php:592-606` `sanitize_inline_css` triple encoding — verified escapes `</style` and `<script` before `<`→`\3c `, then filter `wppo_ccss_sanitize_inline`.
- `class-critical-css.php:1010-1055` `defer_stylesheets` — admin/logged-in guards, `deferJS/delayJS` deadlock guard (1028), skip handles check, `data-wppo-ccss` dedup, `media="print" onload` swap regex with quote capture, noscript fallback — verified order.
- `class-used-css.php:136-163` domain validation — `idn_to_ascii`, colon split, `preg_match /^[a-z0-9\.\-]+$/`, lowercasing — prevents `../` , `/`, `\` — correct.
- `class-used-css.php:190-246` `extract_selectors` — attributes tracked, extension markers (`.css`, `.woff2`,…) added — verified.
- `class-used-css.php:787-818` `save_used_css` atomic tmp+move — verified `wp_rand` suffix, `FS_CHMOD_FILE`, move with overwrite `true`, tmp cleanup both paths — correct.
- `class-used-css.php:849-882` `delete_all_used_css` BFS correctly handles nested domain/path dirs and only deletes `used-css.css` files — verified.
- `class-google-fonts.php:94-119` `process_style_tag` — priority 9 before minify, eligibility guards, host check, `download_and_rewrite` then `str_replace` href→local — correct.
- `class-asset-manager.php:77-83` hook priorities 9999 for both capture and dequeue — verified late enough for `wp_scripts->done`.
- `class-abilities.php:34-74` Abilities API registration — hooks `wp_abilities_api_categories_init` + `wp_abilities_api_init`, gated by `function_exists` — correct WP 7.0 compat.

*(Full 3037/1865/1169/1261/342/245/505 lines were walked; above is summarized confirmation per logical block. No hidden issues in validators, filters, or filesystem writes beyond those in §2.)*

---

## 4. Duplicate / Dead-Code Notes

### Duplicates — HIGH maintenance debt (DUP-001 … DUP-006)

| ID | Location | Description |
|----|----------|-------------|
| DUP-001 | `class-image-optimisation.php:311-323` ≡ `1492-1504` (also `2831-3034` video triplicate) | LRU size cache copied 3×. See A02-027. |
| DUP-002 | `class-image-optimisation.php:1368-1719` TagProcessor vs regex fallback for `process_img_tag` (~350 lines each) | See A02-028. |
| DUP-003 | `class-img-converter.php:509-514` ≡ `519-529` ≡ `587-596` ≡ `653-660` | Placeholder extraction after each encode branch. See A02-029. |
| DUP-004 | `class-img-converter.php:402-410` vs `1261-1290` | Filesize/dimension guards duplicated. See A02-031. |
| DUP-005 | `class-image-optimisation.php:2831-3034` | `lazy_load_videos` three implementations (Processor/TagProcessor/regex). See A02-032. |
| DUP-006 | `class-critical-css.php:331-418` vs `965-995` | Generation logic duplicated for local-dev fallback. See A02-030. |

All duplicates are functional (no bug today) but require 2-3× patching. Recommend tracked refactor epic with helper extraction.

### Dead / Unreachable

- No truly dead public method detected — all `public` methods are reachable via hooks / REST / SPA.
- `class-critical-css.php:315-317` `default` case in `get_sample_url` initially suspected dead, re-confirmed reachable for custom page templates (see A02-033 — retracted).
- Private helpers lacking direct call but invoked via reflection or filter: `get_templates()` (247) called by `get_status_all`, `regenerate_all`, `background_generate` — reachable.
- `class-img-converter.php:34-51` static flags `$deferred_img_info`, `$img_info_shutdown_registered`, `$img_info_persisted`, `$client_side_processing_state` — all used in atomic commit path, not dead.
- No commented-out blocks that execute; only explanatory comments.

---

## 5. Open Questions

| # | Question | Context | Needed Answer / Action |
|---|----------|---------|------------------------|
| Q-01 | Should Abilities DB-cleanup enum be user-facing (`trash`) or internal (`trashed_posts`)? | Mismatch in A02-001 blocks MCP/Command Palette usage. | Product decision: keep internal names exposed or normalise in executor. Update `input_schema` or add normaliser. |
| Q-02 | Is multisite CCSS isolation required via file path or is domain-cache already per-site via `get_current_blog_id()` in transients enough? | A02-004: files live under global `/cache/wppo/ccss/`. Some hosts symlink `WP_CONTENT_DIR` per site, some share. | Confirm hosting assumption; if shared, add `blog_id` to hash/dir. |
| Q-03 | For CPT singles, should critical CSS bucket be `single` or per-CPT/ per-template? | A02-005 misclassifies CPTs to `index`. | Decide whether `single` hash is intentional generic bucket (simpler) or needs CPT granularity. |
| Q-04 | Is `process_buffer` Google Fonts interception intended to fetch external CSS on every uncached page render? | A02-045 race; also `download_and_rewrite` does blocking `wp_remote_get` inside output-buffer filter — may add 20s on cache miss. | Confirm async alternative (cron/Action Scheduler) acceptable or keep synchronous with timeout 20s. Consider warming via cron. |
| Q-05 | Should `dequeue_selected_assets` remain disabled for logged-in users or should capability-gated preview allow testing? | Asset manager bail at `is_user_logged_in()` (A02-038). | Decide if `manage_options` users should see effect; add preview query var if yes. |
| Q-06 | Is tag-processor vs regex fallback duplication intentional for WP <6.2 LTS support window? | Extensive DUP-002/005. | Confirm minimum WP version bump timeline; when WP 6.7+ can drop regex fallback entirely. |
| Q-07 | Should used-CSS `inject_used_css` removal be switched to `WP_HTML_Tag_Processor` to fix attribute-order bug (A02-006 / A02-015)? | Regex order dependence + alternation blowup. | Approve switch vs patching regex. |

---

## 6. Recommendations Priority

1. **Fix before release:** A02-001 (Abilities enum), A02-009 (Google Fonts host check), A02-002 (normalize_url in fallback).
2. **Fix next iteration:** A02-004, A02-005, A02-010, A02-012.
3. **Tech-debt epic:** DUP-001…006 refactoring, A02-025 perf combine, A02-045 lock for fonts.

---

## 7. Methodology

- Read all 7 files completely via `Read` tool (offsets 1, 1431, 2703 for image-optimisation; 1,1411 for img-converter) — no sampling.
- Checked every `add_action`/`add_filter` name against WordPress core docs and other `includes/` files for hook spelling.
- Traced all `$wpdb` / `get_option` / `update_option` / `transient` / `filesystem` paths for injection, race, and autoload bloat.
- Verified escaping (`esc_attr`, `esc_url`, `sanitize_text_field`, `sanitize_text_field( wp_unslash(...))`) on every `$_SERVER`/`$_GET` read.
- Simulated regex edge cases for `is_valid_url`, `normalize_url`, `resolve_relative_path`, placeholder injection.
- Cross-referenced duplicate blocks via `Grep` mental diff and line-number evidence.

**Verdict:** Media pipeline is overall well-hardened (XSS, SSRF, traversal all have layered guards). Highest-risk items are interface mismatches (Abilities) and host-validation via substring, not in core conversion logic. Duplication is the dominant maintainability risk.

---

*Generated by agent A02 `php-media` — exhaustive line-by-line audit, production code untouched.*
