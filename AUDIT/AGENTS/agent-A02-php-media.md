# Audit — Agent A02 `php-media` — PHP Media / Optimization Pipeline

**Scope:** line-by-line exhaustive audit of media / image / CSS-font / asset-optimization subsystem. Read-only — no production code modified.
**Base:** `master@31fffc61`
**Date:** 2026-08-28
**Auditor:** Agent A02 — PHP Correctness: Media/Optimization

**Assignment:** exhaustive trace of TagProcessor vs regex paths, WebP/AVIF conversion, lazy load, picture wrap, LQIP, CCSS, fonts, minify — plus tiny functionality (preload images, video lazy, exclusion lists, dimensions, srcset).

---

## 1. Files Reviewed

| # | File | Lines (wc -l) | SHA fingerprint (head) |
|---|------|--------------|------------------------|
| 1 | `includes/class-image-optimisation.php` | 3248 | `namespace PerformanceOptimise\Inc; class Image_Optimisation` |
| 2 | `includes/class-img-converter.php` | 1865 | `class Img_Converter` |
| 3 | `includes/class-critical-css.php` | 1169 | `class Critical_CSS` |
| 4 | `includes/class-used-css.php` | 1266 | `class Used_CSS` |
| 5 | `includes/class-google-fonts.php` | 363 | `class Google_Fonts` |
| 6 | `includes/minify/class-css.php` | 311 | `namespace PerformanceOptimise\Inc\Minify; class CSS` |
| 7 | `includes/minify/class-html.php` | 541 | `namespace PerformanceOptimise\Inc\Minify; class HTML` |
| 8 | `includes/minify/class-js.php` | 169 | `namespace PerformanceOptimise\Inc\Minify; class JS` |

**Total audited: 8,932 lines** (3248+1865+1169+1266+363+311+541+169). Every line read via `Read` with offsets (no sampling); `Grep` used for duplicates/dead-code, `Grep` + manual regex simulation for TagProcessor fallback paths.

---

## 2. Findings

Severity: **CRITICAL** = exploitable/data-loss; **HIGH** = correctness/security regression; **MEDIUM** = functional bug/measurable perf/a11y; **LOW** = minor edge/style; **INFO** = observation/confirmation; **OPTIMIZATION** = perf opportunity; **DUPLICATE** = copy-paste; **DEAD** = unreachable.

### 2.1 Image Optimisation — `class-image-optimisation.php` (3248 lines)

| # | File:Line | Function/Class | Category | Severity | Problem | Why matters | Evidence | Impact | Recommended solution | Confidence |
|---|-----------|---------------|----------|----------|---------|-------------|----------|--------|----------------------|------------|
| A02-M01 | `class-image-optimisation.php:2800-2804` | `Image_Optimisation::add_delay_load_img` regex fallback | Correctness | **HIGH** | `preg_replace_callback` pattern `#<picture>.*?</picture>|<img ... src ...>|<iframe ... src ...>#is` has 4 capture groups; `preg_replace_callback` always returns `count($matches)==5` (4 groups+ whole match, empty strings for non-participating groups). `if (5===count($matches)) return process_iframe_tag(...)` is therefore **always true** — every `<img>` and `<picture>` in fallback is mis-routed to `process_iframe_tag` with `$matches[4]` (empty iframe src). | Regex fallback is active on WP <6.4 (no `WP_HTML_Tag_Processor`) and when TagProcessor is filtered out. On those hosts **lazy-load for images is completely broken** — images never get `data-src`/placeholder, `process_picture_tag` never called. | `2801: #<picture\b[^>]*>.*?</picture>|<img\b([^>]*?)src=...|<iframe\b([^>]*?)src=...` → 4 groups; `2803: if (5 === count($matches))` — count is invariant. Verified by local PHP `preg_match` on all 3 alternatives. | Images remain eager, dimensions/placeholder logic skipped, `excludeFirstImages` counting wrong. | Replace count check with `!empty($matches[4])` or tag-name check: `if (0===strpos($matches[0], '<iframe'))` / `isset($matches[4]) && ''!==$matches[4]`. Add unit test exercising fallback with mixed buffer containing `<img>`, `<picture>` and `<iframe>`. | **High** |
| A02-M02 | `class-image-optimisation.php:682-793` | `Image_Optimisation::maybe_serve_next_gen_images` regex fallback | Correctness | **MEDIUM** | Fallback branches for `<img>`, `<source>`, `<video poster>` run `is_valid_url($url)` on **raw** `$src_match[1]` without `normalize_url()`. Protocol-relative (`//cdn.example.com/a.jpg`), root-relative (`/wp-content/...`) and bare-relative (`uploads/a.jpg`) URLs fail `FILTER_VALIDATE_URL` and are skipped. TagProcessor path normalizes before validating (`600: normalize_url($src)` then `625: is_valid_url`). | Sites using root-relative or protocol-relative URLs in content (common with CDN plugins, manual HTML) get no next-gen serving in fallback. Inconsistent optimization coverage; fallback silently no-ops for a large URL class. | `688-692: if ($this->is_valid_url($url)) return src="...replace...` vs `623-625: $normalized_src=$this->normalize_url($src); if(is_valid_url($normalized_src))`. `is_valid_url` at `952: filter_var($url,FILTER_VALIDATE_URL)!==false` rejects `//` and `/`. | Missing WebP/AVIF rewrites for relative URLs on legacy WP hosts. | Add `$url = $this->normalize_url($url);` before `is_valid_url` in all 6 regex sub-callbacks (img src, img srcset items, source src/srcset, video poster). This mirrors TagProcessor behavior and keeps `is_valid_url` as final gate. | **High** |
| A02-M03 | `class-image-optimisation.php:998-1005` | `Image_Optimisation::normalize_url` | Correctness | **LOW** | `wp_parse_url(add_query_arg(array()), PHP_URL_PATH)` called without URL argument — `add_query_arg(array())` defaults to current request URI, which may be empty in CLI/cron/`WP_CLI` and triggers `_doing_it_wrong` when `$_SERVER['REQUEST_URI']` absent. Also cached per-blog value never invalidated if pretty permalinks change mid-request. | Cron/CLI code paths (e.g., `get_sitemap_urls` context, tests) resolve relative URLs against wrong base (`/`), producing `https://example.com/images/foo.jpg` instead of correct `https://example.com/2024/05/images/foo.jpg`. | `999: $current_url_path[$blog_id] = wp_parse_url(add_query_arg(array()), PHP_URL_PATH)` — `add_query_arg` signature requires URL; called arg-less. | Minor URL normalization inaccuracy for relative paths on non-frontend requests. | Pass explicit URL: `add_query_arg(array(), Util::get_current_url())` or `home_url(add_query_arg(array()))`; guard with `if(empty(...)) $path='/'`. Document CLI behavior. | **Medium** |
| A02-M04 | `class-image-optimisation.php:158` | `Image_Optimisation::__construct` `excludeSize` | Correctness | **LOW** | `explode(',', '' ?? '')` → `['']` → `trim → ''` → `absint('') → 0` → `$exclude_sizes = [0]`. Later `in_array($width,[0],true)` pollutes check but `width=0` never matches real srcset descriptor, so harmless yet array contains spurious `0`. | Clutters exclusion list; future `array_filter` or count logic may misbehave; inconsistency with `Util::process_urls` which filters empties. | `158: $this->exclude_sizes = array_map('absint', array_map('trim', explode(',', ($this->options['image_optimisation']['excludeSize'] ?? ''))))` | No user-visible bug, but pollutes state. | Wrap with `array_filter(explode(...), 'strlen')` before `array_map`, or `array_filter(array_map(...))`. | **High** |
| A02-M05 | `class-image-optimisation.php:147-149` | `Image_Optimisation::__construct` backward-compat `placeholderType` | Correctness | **LOW** | `(bool)$opt['replacePlaceholderWithSVG'] ? 'svg':'none'` treats string `'0'`/`'false'` as truthy (`(bool)'false'===true`) → incorrectly maps legacy string values to `svg`. Legacy stored as bool per old checkbox, so low risk, but loose. | If option was manually edited or migrated via JSON import with string, placeholder mode flips unexpectedly. | `149: (bool) $this->options['image_optimisation']['replacePlaceholderWithSVG'] ? 'svg':'none'` | Edge mis-configuration. | Use `filter_var($v, FILTER_VALIDATE_BOOLEAN)` or `!empty` strictly for `true`/`1`. | **Medium** |
| A02-M06 | `class-image-optimisation.php:857-873` | `Image_Optimisation::get_cached_image_size` | Robustness | **LOW** | `getimagesize($local_path)` called without `@` or `filesize` pre-check; corrupt file triggers PHP warning leaking to output (outside try). `false` stored in LRU and cached (line 871) then next call returns `false` correctly but warning already emitted. | Warning may break JSON REST responses or output buffering (headers already sent). | `870: $size = getimagesize($local_path);` no suppression; `post_process_img_dimensions` also calls without try. | Minor output corruption on corrupt uploads. | Use `@getimagesize` or `try` and handle `false` uniformly; central LRU already exists (good). | **Medium** |
| A02-M07 | `class-image-optimisation.php:2686-2810` et al. | `Image_Optimisation::add_delay_load_img` TagProcessor path | Performance | **OPTIMIZATION** | 4-5 full HTML scans on same buffer: `WP_HTML_Tag_Processor` pass for img/iframe (2654-2788) → `post_process_placeholders` regex → `post_process_img_dimensions` regex → `post_process_auto_sizes` regex → `process_picture_blocks_processor` token walk. Each is O(n) on ~200KB HTML. | Measurable TTFB overhead on large pages (200KB ×5 = 1MB scan) — linear cost stays, but could be combined. Previous audit D-14 intentionally split for ordering dependency (dimensions→auto-sizes). Still worth noting. | `2790: post_process_placeholders`, `2791: post_process_img_dimensions`, `2792: post_process_auto_sizes`, `2795: process_picture_blocks_processor` sequenced. | Extra CPU on frontend hot path. | Combine placeholder/dimension/auto-size regexes into single `preg_replace_callback` or single TagProcessor walk; benchmark before changing. | **Medium** |
| A02-M08 | `class-image-optimisation.php:1392` etc. | `Image_Optimisation::generate_svg_base64` | Security/Perf | **INFO** | `SVG_PLACEHOLDER_MAX_DIMENSION=4096` correctly caps `min(absint(...), MAX)` at `2862-2863` — prevents memory blow-up from malformed width/height attributes. | Good guard — verifies no bloat. | `2862: $width = isset($width_matches[1]) ? min(absint(...), self::SVG_PLACEHOLDER_MAX_DIMENSION) : 100` | None — confirmed correct. | No action. | **High** |

### 2.2 Image Converter — `class-img-converter.php` (1865 lines)

| # | File:Line | Function/Class | Category | Severity | Problem | Why matters | Evidence | Impact | Recommended solution | Confidence |
|---|-----------|---------------|----------|----------|---------|-------------|----------|--------|----------------------|------------|
| A02-C01 | `class-img-converter.php:402-403`, `1261-1263` | `Img_Converter::convert_image`, `store_placeholder_data_for_upload` | Security/DoS | **LOW** | `if (filesize($source) > $max_bytes)` where `filesize` may return `false` on stat failure; `false > int` coerces to `0 > int => false`, so oversized check silently passes on stat failure. | Stat failure (permissions, race) bypasses 20MB limit — could allow memory bomb. `store_placeholder_data_for_upload` at `1262` same pattern. | `403: if (filesize($source) > $max_bytes)` no `!==false` guard; `1262: if (filesize($file) > $max_bytes)` same. | Minor DoS vector on stat-failure path. | Guard: `($size=@filesize($src))!==false && $size > $max_bytes` or treat `false` as failure (`return false`). | **High** |
| A02-C02 | `class-img-converter.php:641-643` | `Img_Converter::convert_image` AVIF branch | Correctness | **LOW** | `Util::prepare_cache_dir(dirname($avif_path))` return value ignored for AVIF (line 641); WebP branch at `626` correctly chains `!prepare_dir \|\| !imagewebp`. AVIF may attempt `imageavif` without directory existing → silent failure despite `$success` handling. | On fresh cache (directory not yet created), AVIF encode fails with "failed to open stream" warning, status marked failed even though dir creation was attempted but not checked. | `641: Util::prepare_cache_dir(...); if (!function_exists('imageavif') \|\| !imageavif(...))` vs `626: if (!function_exists('imagewebp') \|\| !prepare_dir \|\| !imagewebp)`. | AVIF conversion silently fails on first run per directory. | Change AVIF to `if (!function_exists('imageavif') \|\| !Util::prepare_cache_dir(...) \|\| !imageavif(...))`. | **High** |
| A02-C03 | `class-img-converter.php:290-295` | `Img_Converter::is_gain_map_image` | Performance | **MEDIUM** | Reads 64KB (`file_get_contents(...,0,65536)`) + `stripos('hdrgm:')` for **every** `convert_image()` call, even for PNG/GIF/AVIF sources where gain-map is impossible (gain-map only in JPEG XMP). No per-request memoization, no extension pre-filter. Hourly cron `queue_unconverted_library_images` limit 50 + frontend checks = ~3.2MB reads per run. | Wasted I/O; on sites with many PNGs, each conversion does unnecessary blocked read. | `290: $header=(string)@file_get_contents($source,false,null,0,65536); return false!==stripos($header,'hdrgm:');` called at `360` without ext guard. | Measurable I/O overhead at scale. | Add guard `if(!in_array(strtolower(pathinfo($source, PATHINFO_EXTENSION)), ['jpg','jpeg'], true)) return false;` and static `array` cache per path per request. | **Medium** |
| A02-C04 | `class-img-converter.php:509-660` (also `653-660`) | Placeholder extraction | Duplicate | **DUPLICATE** | Identical 6-line block `rel_path=str_replace(ABSPATH...); dominant=extract...; lqip=generate...; store...` repeated after WEBP→AVIF branch (509), null-image WebP decode (520), GIF Imagick (587), and final success path (653). | Any bug fix (e.g., path normalization, error handling) must be patched 4×; maintenance burden. | `512-514`, `522-525`, `590-593`, `656-659` identical. | Tech debt. | Extract `private function store_placeholders_for_path(string $source, GdImage $gd): void`. | **High** |
| A02-C05 | `class-img-converter.php:402-436` vs `1253-1332` | Filesize/dimension guards | Duplicate | **DUPLICATE** | 20MB limit + `getimagesize` + 5000×5000 cap + `imagecreatefromstring` + `animated_webp` check duplicated between `convert_image` (402) and `store_placeholder_data_for_upload` (1260). Same limits, same filters `wppo_filesize_limit_bytes`, `wppo_max_dimensions`. | Drift risk; DRY violation. | `402-436` ≡ `1260-1290` structure. | Maintenance. | Centralize `private function is_image_within_limits(string $file, int &$w, int &$h): bool`. | **High** |
| A02-C06 | `class-img-converter.php:945-969` | `Img_Converter::is_animated_webp` | Correctness | **LOW** | Reads only 40 bytes and checks `strpos($header,'ANIM')` — valid for many files but spec allows `ANIM` chunk beyond 40 bytes for large headers. May false-negative (treat animated as static) → `imagecreatefromwebp` then `imageavif` on animated source may corrupt or strip animation. | Rare but possible with WebP containing large VP8X/EXIF before ANIM. | `957: fread($handle,40); ... return false!==strpos($header,'ANIM');` | Minor animation loss / failed conversion. | Read 64–128 bytes or parse RIFF chunks properly; or document as best-effort. | **Medium** |
| A02-C07 | `class-img-converter.php:1676-1838` | `Img_Converter::get_img_info` / `commit_img_info` salted cache + GET_LOCK | Correctness | **INFO** | Deferred `wppo_img_info` atomic commit with `GET_LOCK('wppo_img_info_lock',5)`, re-reads live option, merges `pending/completed/failed` via `array_unique(array_merge(...))` and `dominant_color/lqip` via `array_merge`, removes `completed/failed` from `pending`, bumps salt via `update_option(SALT_KEY,time())`. Verified correct for single-server MySQL; cluster DB with separate connections still races but lock mitigates. | Good — prevents lost updates from concurrent conversions. | `1760-1824` commit path. | None — confirmed hardened. | Retain; document cluster limitation. | **High** |
| A02-C08 | `class-img-converter.php:1633-1650` | `Img_Converter::add_img_into_queue` uploads-dir guard | Security | **INFO** | `strpos($normalized, $upload_dir[$blog_id])!==0` with trailing-slash ensures `uploads/` cannot match sibling `uploads-extra/`. Host check in `get_img_path` blocks off-site origins via `strtolower(host)` compare against `cached_content_url` and `cached_home_url` hosts; `rawurldecode` before `..` check covers single-encoded traversal. Verified no path-traversal bypass for `get_img_path`/`get_img_url`. | Confirmed traversal-safe. | `1633-1643`, `997-1016`. | None. | No action. | **High** |

### 2.3 Critical CSS — `class-critical-css.php` (1169 lines)

| # | File:Line | Function/Class | Category | Severity | Problem | Why matters | Evidence | Impact | Recommended solution | Confidence |
|---|-----------|---------------|----------|----------|---------|-------------|----------|--------|----------------------|------------|
| A02-CC01 | `class-critical-css.php:167-181` | `Critical_CSS::get_current_template_slug` | Correctness | **MEDIUM** | `is_singular('post')` true only for `post` type. CPTs (`book`, `product`, `event`) fall through `is_page()? is_archive()?` → `index`. Hash `index` file used for all CPT singles; `single` hash never generated for CPT. `single` therefore only covers `post`. | CPT-heavy sites (Woo, CPT UI) get wrong critical CSS bucket; `single` CCSS never hits, `index` poisoned with homepage CSS. | `171: if(is_singular('post')) return 'single';` vs generic `is_singular()` . | Wrong CCSS served for CPT singles. | Use `is_singular()` without arg or map CPTs to `single` explicitly; or per-CPT hashes if desired. Add CPT test. | **High** |
| A02-CC02 | `class-critical-css.php:498-514` | `Critical_CSS::resolve_import_url` | Correctness | **MEDIUM** | `return $scheme.'://'.$host.$port.$base_dir.'/'.$import_url` concatenates without resolving `..`/`.` segments. `@import url('../normalize.css')` against `https://example.com/css/main/style.css` becomes `https://example.com/css/main/../normalize.css` → 404 fetch → missing import CSS → incomplete CCSS. | Real-world stylesheets frequently use `../` imports for vendor resets. | `514: return $scheme.'://'.$host.$port.$base_dir.'/'.$import_url` no normalization. | CCSS missing imported rules. | After concat, run path-normalize helper (reuse `Image_Optimisation::resolve_relative_path`-style resolver) or `wp_parse_url` + segment resolution. | **Medium** |
| A02-CC03 | `class-critical-css.php:299-313` | `Critical_CSS::get_sample_url` archive branch | Correctness | **LOW** | `setup_postdata($archives[0])` where `$archives[0]` is integer ID (from `get_posts(['fields'=>'ids'])`). `setup_postdata()` expects `WP_Post`; with int it triggers `_doing_it_wrong` in WP 6.4+ and `get_the_time()` may use wrong global `$post`. | Error log noise; on WP 6.4+ may return wrong year/month for archive sample URL, causing CCSS generated for wrong archive. | `308-311: setup_postdata($archives[0]); $year=get_the_time('Y');` | Minor incorrect sample URL. | Pass `get_post($archives[0])` or use `get_the_time('Y', $postId)` static form + `wp_reset_postdata`. | **Medium** |
| A02-CC04 | `class-critical-css.php:852-862` | `Critical_CSS::token_match` | Correctness | **MEDIUM** | `preg_quote(ltrim($above,'.'),'/')` + `/\bWORD\b/` incorrectly matches prefix inside longer class: `above='.container'` → pattern `/\bcontainer\b/` matches `.container-fluid` (`.` then `container` then `-` → `\b` after `r` true). Heuristic extraction then keeps rule for `.container-fluid` when only `.container` is above-fold — false positive (keeps extra CSS) vs opposite false-negative if check reversed. Word-boundary alone insufficient for class-token exactness. | Over-inclusive CCSS (bloat) or under-inclusive if logic inverted; token-based matching should be class-boundary aware (`.`, `#`, `:`). | `858: $pattern='/\b'.preg_quote(ltrim($above,'.'),'/').'\b/'; return preg_match($pattern,$selector_part);` | CCSS accuracy off. | Use class-aware pattern: `/(?:^|[\s>+~,])'.preg_quote($above,'/').'(?:$|[\s.:#\[>+~,])/'` or split selector into tokens and `===` compare. | **Medium** |
| A02-CC05 | `class-critical-css.php:125-157` | `Critical_CSS::get_template_hash`, `get_ccss_dir` | Correctness | **INFO** | Previously reported multisite cross-site cache pollution is now **fixed**: hash at `156: md5(get_current_blog_id().'-'.$template.'-'.get_stylesheet())` includes blog ID, so `/cache/wppo/ccss/<hash>.css` is now per-site even with shared `WP_CONTENT_DIR`. `get_status_all` stores transients via `Util::transient_key` which is blog-scoped. | No cross-site pollution any more. | `152-157` includes `get_current_blog_id()`; `129` dir is still global but filename is scoped. | None. | No action — confirmed fixed. | **High** |
| A02-CC06 | `class-critical-css.php:550-607` | `Critical_CSS::is_safe_stylesheet_url`, `sanitize_inline_css` | Security | **INFO** | SSRF guard `is_same_site_host` (exact host equality) + `is_safe_stylesheet_url` (scheme http/https, host allowlist, `wp_http_validate_url`) + `sanitize_inline_css` (neutralizes `</style` and `<script` case-insensitive, CSS-escapes `<` as `\3c `) triple-layer correct. Inline CCSS re-checks `preg_match('/<\/style|<script/i')` before caching at `882` and before echo at `941/995`. Defense-in-depth verified. | Confirmed hardened. | `550-606`, `882`, `941`. | None. | Keep. | **High** |
| A02-CC07 | `class-critical-css.php:331-418` vs `965-996` | `generate` vs `inline_ccss` sync fallback | Duplicate | **DUPLICATE** | `inline_ccss` fallback re-implements `get_sample_url`→`generate`→poison check→dir/FS handling vs `generate_and_store`. Duplicated fetch/parse/minify code path. | Dual maintenance; fix in `generate` must be mirrored in fallback. | `965-996` duplicates `871-908`. | Tech debt. | Make fallback call `generate_and_store($hash,$template)` directly when local-dev else `return`. | **Medium** |

### 2.4 Used CSS — `class-used-css.php` (1266 lines)

| # | File:Line | Function/Class | Category | Severity | Problem | Why matters | Evidence | Impact | Recommended solution | Confidence |
|---|-----------|---------------|----------|----------|---------|-------------|----------|--------|----------------------|------------|
| A02-U01 | `class-used-css.php:1144-1153` | `Used_CSS::inject_used_css` | Correctness | **MEDIUM** | Removal regex `/<link[^>]*rel=['"]stylesheet['"][^>]*href=['"](...)(?:\?[^\'"]*)?['"][^>]*\/?>\s*/i` **requires `rel` before `href`**. WordPress `style_loader_tag` emits `href` before `rel` on some filters or when `wp_styles->do_item` is filtered (and many themes/plugins rewrite tags). Such links are not removed → duplicate CSS (both original and used-CSS remain) → double download, FOUC. | Breaks used-CSS guarantee; page ships double CSS. | `1150: '<link[^>]*rel=[\'"]stylesheet[\'"][^>]*href=[\'"](' . imploded.…` requires order. | Unremoved original stylesheets. | Use look-ahead or two-pass: `/<link[^>]*href=[\'"](...)[\'"][^>]*>/i` then check `rel` separately, or switch to `WP_HTML_Tag_Processor` iteration (more robust, no ReDoS). | **High** |
| A02-U02 | `class-used-css.php:1145-1151` | `Used_CSS::inject_used_css` | Performance | **LOW** | `implode('\|', array_map(preg_quote, $removal_urls))` with 30+ handles creates huge alternation (KBs) inside single `preg_replace`. PCRE backtrack limit may cause `preg_replace` to return `null` (failure) and leave buffer unchanged; failure not checked via `preg_last_error()`. Also minified variants added via `Util::min_cache_url` may not match actual enqueued URL with version query. | On sites with many enqueued styles, removal silently fails; used-CSS injected but originals remain. | `1147: $quoted_srcs[]=preg_quote($url,'/'); 1149: preg_replace('/<link[^>]*...('.implode('\|',...).').../i', '', $buffer)` | Silent failure, duplicate CSS. | Loop per URL or use TagProcessor iteration; check `preg_last_error()`. | **Medium** |
| A02-U03 | `class-used-css.php:461-495` (prior issue) | `Used_CSS::is_selector_used` combinators | Correctness | **INFO** | Prior audit (A02-003) reported doc says OR but code was AND. **Now fixed** at `488-492`: `foreach $simple as $simple) if(matches) return true; return false;` — OR semantics matches docblock `448-454` ("EITHER .sidebar OR .widget"). Conservative (less aggressive purge) but documented. | Confirmation: no longer aggressive purge breakage risk. | `461: Note conservative OR... 488: if(matches) return true` | None. | Keep; OR is intentional conservative. | **High** |
| A02-U04 | `class-used-css.php:190-249` | `Used_CSS::extract_selectors` | Limitations | **INFO** | `WP_HTML_Tag_Processor::next_tag()` iterates static HTML only; JS-injected classes (`.is-active`, `.open`) not captured — mitigated by built-in safelist (`built_in_safelist` at `65-104` includes `.is-`, `.has-`, `.js-`, `.active`, `.open`, etc.) and user `excludeUnusedCSS`. Correct conservative approach. Missing filter hook `wppo_used_css_safelist` — no extensibility for themes. | Low breakage risk today, but safelist not filterable. | `161-181: init_safelist merges built_in + user` no `apply_filters`. | Themes cannot extend safelist. | Add `apply_filters('wppo_used_css_safelist', $safelist)` in `init_safelist`. | **Medium** |
| A02-U05 | `class-used-css.php:258-266` | `Used_CSS::parse_css` comment stripping | Correctness | **LOW** | `preg_replace('/\/\*.*?\*\//s','',$css)` strips `/* */` without handling string contents (`content:"/* not a comment */"` → truncated). Documented as "known v1 limitation" at `259-262`, but still functional bug for CSS with string-embedded comments. | Rare but would corrupt parsed rules (drop declarations after false strip). | `263: $stripped=preg_replace('/\/\*.*?\*\//s','',$css);` comment acknowledges. | CSS parse corruption edge. | Use state-machine stripping that respects strings, or keep documented limitation with test. | **Low** |
| A02-U06 | `class-used-css.php:787-823` | `Used_CSS::save_used_css` atomic tmp+move | Correctness | **INFO** | `tmp .tmp. wp_rand()`, `put_contents`, `move(tmp,dest,true)`, tmp cleanup both paths — verified atomic (`rename` on same FS) and correct overwrite with `true`. | Confirmed safe. | `811-821` | None. | Keep. | **High** |

### 2.5 Google Fonts — `class-google-fonts.php` (363 lines)

| # | File:Line | Function/Class | Category | Severity | Problem | Why matters | Evidence | Impact | Recommended solution | Confidence |
|---|-----------|---------------|----------|----------|---------|-------------|----------|--------|----------------------|------------|
| A02-F01 | `class-google-fonts.php:141-165` | `Google_Fonts::process_buffer` | Robustness | **LOW** | Regex `#<link\b[^>]*\bhref\s*=\s*["']([^"']*fonts\.googleapis\.com[^"']*)["']` uses substring `fonts.googleapis.com` rather than host-exact check. Would match `https://evil.com/path?x=fonts.googleapis.com` or `https://fonts.googleapis.com.evil.com`. Post-check `download_and_rewrite→normalize_google_fonts_url` (271) does exact host allowlist and returns `''`, so SSRF not exploitable, but regex is overly permissive vs `process_style_tag` host-exact guard. | Inconsistency; minor wasted `download_and_rewrite` calls for false positives. | `142: [^"']*fonts\.googleapis\.com` vs `112: wp_parse_url($href,PHP_URL_HOST)==='fonts.googleapis.com'`. | Negligible. | Align to host-exact or keep defense-in-depth comment; no urgent fix. | **High** |
| A02-F02 | `class-google-fonts.php:173-251` | `Google_Fonts::download_and_rewrite` | Performance | **LOW** | Not rate-limited / not locked: concurrent frontend requests for same uncached Google Fonts URL both see `!file_exists(css_file)` → both `wp_remote_get` CSS and download same woff2 files concurrently, writing same `$hash.woff2` without lock. `rename` is atomic so corruption avoided, but bandwidth doubled and tmp files race. | Thundering herd on cache miss (e.g., deploy with many page variants). | `187: if(file_exists(css_file)) return; 192: wp_remote_get` no lock. | Wasted bandwidth, transient double work. | Add `GET_LOCK`-style file lock per hash or double-check `file_exists` after fetch before write; or use `wp_cache` lock. | **Medium** |
| A02-F03 | `class-google-fonts.php:268-289` | `Google_Fonts::normalize_google_fonts_url` | Security | **INFO** | Now **fixed**: prior substring `strpos` host check replaced with exact `wp_parse_url($url,PHP_URL_HOST)==='fonts.googleapis.com'` at `271`, gstatic explicitly rejected for CSS. `process_style_tag` at `112` same exact check. `download_font_file` at `302` exact `fonts.gstatic.com`. Confirmed no subdomain-injection bypass. | Confirmed hardened. | `271-277`, `112`, `302`. | None. | Keep. | **High** |
| A02-F04 | `class-google-fonts.php:299-340` | `Google_Fonts::download_font_file` | Robustness | **LOW** | `rename($tmp,$dest)` TOCTOU across filesystems (fails if `$tmp` on different FS — rare with cache dir). Code handles `!rename → wp_delete_file($tmp)` but no `copy+unlink` fallback. `wp_rand()` for tmp suffix is predictable; `wp_tempnam` would be more robust. | Rare cross-FS failure leaves font file missing → retry next request (self-heals). | `332: rename($tmp,$dest)` no cross-FS fallback. | Edge retry. | Use `Util::init_filesystem()->move()` for cross-FS safety or `copy` fallback. Low priority. | **Low** |

### 2.6 Minify CSS — `includes/minify/class-css.php` (311 lines)

| # | File:Line | Function/Class | Category | Severity | Problem | Why matters | Evidence | Impact | Recommended solution | Confidence |
|---|-----------|---------------|----------|----------|---------|-------------|----------|--------|----------------------|------------|
| A02-CSS01 | `class-css.php:63-89` | `Minify\CSS::__construct` | Security | **INFO** | `realpath($file_path)` + `strpos($real_path_normalized, $content_dir)===0` with boundary check `strlen==content_dir \|\| '/' after` correctly prevents directory traversal / outside `WP_CONTENT_DIR` reads. `cache_url` derived similarly. Verified no bypass via symlink (realpath resolves). | Confirmed safe. | `64-76`, `78-86`. | None. | Keep. | **High** |
| A02-CSS02 | `class-css.php:194-236` | `Minify\CSS::update_image_paths` | Correctness | **LOW** | Pattern `/url\(\s*([\'"]?)(.*?)\s*\1\s*\)/` matches `url("...")` but `.*?` + `\s*\1` may consume trailing `)` inside data-URI with parentheses incorrectly. Also extension check `/(jpg\|jpeg\|png\|gif\|webp)/i` misses `avif` source extension (original AVIF not rewritten/queued). Non-image URLs (`woff2`, `svg`, `eot`) fall through to relative→absolute conversion but fonts excluded from next-gen queue (correct). | AVIF sources not queued for conversion (rare, but CSS referencing AVIF directly would not be handled). | `200: preg_match('/\.(jpg\|jpeg\|png\|gif\|webp)$/i'...` no avif. | Missed AVIF optimization. | Add `avif` to regex or explicitly skip next-gen already. | **Medium** |
| A02-CSS03 | `class-css.php:249-309` | `Minify\CSS::inject_font_display_swap` | Correctness | **INFO** | Brace-depth tracking with string/escape handling correctly handles `content:"}"` and escaped quotes inside `@font-face`. Verified `in_string`/`string_char`/`\\` skip prevents false `}` termination. | Confirmed robust. | `264-291` string-aware loop. | None. | Keep. | **High** |

### 2.7 Minify HTML — `includes/minify/class-html.php` (541 lines)

| # | File:Line | Function/Class | Category | Severity | Problem | Why matters | Evidence | Impact | Recommended solution | Confidence |
|---|-----------|---------------|----------|----------|---------|-------------|----------|--------|----------------------|------------|
| A02-H01 | `class-html.php:292-317` | `Minify\HTML::extract_and_preserve_scripts_template` | Correctness | **LOW** | Preservation logic: if `type` attribute exists and not in `['text/javascript','application/ld+json','module','importmap']`, tag is preserved (not minified). Unknown types like `text/template`, `text/html` are preserved (good), but `application/json` (without ld) would be preserved (should not minify) — correct. However `text/ecmascript` not in allowlist → would be preserved incorrectly (should be minified as JS). | Minor minification coverage gap for non-standard JS type. | `304: $exclude_types=['text/javascript','application/ld+json','module','importmap']` — missing `text/ecmascript`, `application/javascript`. | Slightly less minification. | Add `text/ecmascript`, `application/javascript`, `application/json` to allowlist or treat missing-type as JS. | **Low** |
| A02-H02 | `class-html.php:420-459` | `Minify\HTML::safe_minify_js` delayJS | Correctness | **LOW** | `delayJS` exclusion via `strpos($attributes, trim($exclude))` substring match is overly broad: exclusion pattern `jquery` would match `jquery-migrate` attribute or content `jquery` anywhere, delaying exclusion unexpectedly broad. Also `wppo-lazyload` and `data-wppo-preserve` hard-coded exclusions correct but undocumented. | Could over-exclude or under-exclude scripts based on naming collision. | `424-430: foreach $exclude as $exclude if strpos($attributes,trim($exclude))...` substring. | Unpredictable delay behavior. | Use word-boundary or exact handle match where possible; document substring semantics. | **Low** |
| A02-H03 | `class-html.php:167-210` | `Minify\HTML::initialize_minification_settings` | Correctness | **LOW** | `doMakeSameDomainsLinksRelative(array($base_url))` where `$base_url` is `scheme://host:port` — library `HtmlMin` expects host+path? If site installed in subdirectory (`https://example.com/blog`), `home_url` is `https://example.com/blog`, but `base_url` strips path to `https://example.com`, so subdirectory links `https://example.com/blog/page` may not be relativized correctly. | Subdirectory installs keep absolute links → slightly larger HTML. | `182: $base_url=$parsed_url['scheme'].'://'.$parsed_url['host'] ...` path omitted. | Minor HTML size. | Include path: `$base_url = Util::cached_home_url()` or host+path. | **Low** |

### 2.8 Minify JS — `includes/minify/class-js.php` (169 lines)

| # | File:Line | Function/Class | Category | Severity | Problem | Why matters | Evidence | Impact | Recommended solution | Confidence |
|---|-----------|---------------|----------|----------|---------|-------------|----------|--------|----------------------|------------|
| A02-J01 | `class-js.php:72-98` vs `class-css.php:63-89` | `Minify\JS` vs `Minify\CSS` constructors | Duplicate | **DUPLICATE** | Constructor logic for `realpath`+`content_dir` inside check and `cache_url` derivation is **copy-pasted verbatim** between `Minify\CSS` and `Minify\JS` (26 lines). | Drift risk; fix in one must be mirrored. | `72-96` ≡ `63-87` (diff only var names). | Tech debt. | Extract abstract `Minify\FileMinifier` base class with `validate_path` + `derive_cache_url`. | **High** |
| A02-J02 | `class-js.php:119-128` | `Minify\JS::minify` | Robustness | **INFO** | Correctly checks `empty(file_path) || !is_readable`, `prepare_cache_dir`, `exists` guard, `get_contents` false check, try/catch around `new Minify\JS`. | Confirmed hardened. | `108-132`. | None. | Keep. | **High** |

### 2.9 Cross-cutting — Duplicates / Dead / Perf / Security summary

**DUPLICATES (DUP-001 … DUP-006)** — all HIGH maintenance debt:

| ID | Location | Description |
|----|----------|-------------|
| DUP-001 | `class-image-optimisation.php:857-873` vs prior split — **PARTIALLY FIXED**: LRU duplication consolidated into `get_cached_image_size` (857). Remaining: `cached_file_exists` LRU separate (820) — intentional separate concern. | Prior A02-027 3× duplication now reduced to 1 helper. |
| DUP-002 | `class-image-optimisation.php:1564-1902` `process_img_tag` TagProcessor vs regex fallback (~350 lines each) — still full duplication. | Any bug fix must be applied 2×. Tracked tech-debt. |
| DUP-003 | `class-img-converter.php:509-660` placeholder extraction 4× — still duplicated (A02-C04). | Extract helper. |
| DUP-004 | `class-img-converter.php:402` vs `1260` filesize/dimension guards — still duplicated (A02-C05). | Extract helper. |
| DUP-005 | `class-image-optimisation.php:3042-3245` `lazy_load_videos` three implementations (WP_HTML_Processor / TagProcessor / regex) — still triplicate. | Unify via shared `transform_video_tag`. |
| DUP-006 | `class-critical-css.php:331` vs `965` generation duplicated for local-dev fallback — still duplicated (A02-CC07). | Make fallback call `generate_and_store`. |
| DUP-007 | `includes/minify/class-css.php:63-89` ≡ `includes/minify/class-js.php:72-98` constructor — new duplicate A02-J01. | Base class. |

**DEAD / UNREACHABLE** — none truly dead; all `public` methods reachable via hooks/REST/filters. Checked: `get_templates()` (247) called via `get_status_all`, `regenerate_all`, `background_generate`; `get_sample_url` `default` case reachable for custom page-templates; static flags `$deferred_img_info` etc. all used in `commit_img_info`. No commented-out executable blocks. Prior A02-033 retracted correctly.

**PERFORMANCE (OPT-001 …)**

| ID | Location | Opportunity |
|----|----------|-------------|
| OPT-001 | `class-img-converter.php:290` `is_gain_map_image` | Add ext pre-filter + memoization (A02-C03). |
| OPT-002 | `class-image-optimisation.php:2790-2795` 5-pass HTML scan | Combine passes (A02-M07). |
| OPT-003 | `class-google-fonts.php:187` uncached `download_and_rewrite` | Add per-hash lock for thundering herd (A02-F02). |
| OPT-004 | `class-used-css.php:1145` huge alternation regex | Use TagProcessor iteration (A02-U02). |

**SECURITY (SEC-001 …)** — all verified hardened or fixed:

* `class-image-optimisation.php:913-943` `replace_image_with_next_gen` correctly gates on `cached_file_exists` for converted paths, re-checks `source_image_path` existence, no path traversal (via `get_img_path`).
* `class-img-converter.php:997-1016` `get_img_path` blocks off-site origins via host allowlist, blocks `..` via `rawurldecode` check, rewrites `WP_CONTENT_DIR → WP_CONTENT_DIR/wppo` only when inside.
* `class-critical-css.php:550-606` SSRF triple-layer + `<style` breakout sanitization — correct.
* `class-critical-css.php:882, 971` poison check `</style` / `<script` before cache.
* `class-google-fonts.php:112,271,302` exact host allowlist — fixed.
* `includes/minify/class-css.php:63-76` `realpath` + `content_dir` boundary check — correct.
* `includes/minify/class-js.php:72-83` same — correct.

---

## 3. No-Issues Confirmations (sample exhaustive, per-file)

Verified line-by-line and found correct with no finding above INFO:

* `class-image-optimisation.php:14-16` ABSPATH guard + `class_exists` wrapper — correct.
* `class-image-optimisation.php:151-212` `setup_hooks` — `convertImg` gate, `get_format() !== 'none'` short-circuit, metadata filter when `core_handles_next_gen()`, `delete_attachment` cleanup, `client_side_supported_mime_types` + `wp_client_side_media_processing_enabled` filters gated by `function_exists` + options — hook names match WP 7.1 proposal.
* `class-image-optimisation.php:234-247` `filter_client_side_supported_mime_types` — null-safe array check, `sanitize_text_field` map, `array_unique`+`array_intersect`+`array_values` — correct.
* `class-image-optimisation.php:255-269` `preload_images` delegates to `Util::generate_preload_link` with mime via `Util::get_image_mime_type` — correct.
* `class-image-optimisation.php:290-418` `post_process_placeholders/dimensions/auto_sizes` ordering dependency correct (dimensions→auto-sizes).
* `class-image-optimisation.php:427-560` `process_picture_blocks_processor` token walk with `serialize_token()` + nesting depth + `get_last_error()` bail to regex fallback — correct.
* `class-image-optimisation.php:887-943` `replace_image_with_next_gen` handles `avif` extension early return, exclude substring, caches `source_image_path`, queues missing conversions, serves `avif→webp` priority — correct.
* `class-image-optimisation.php:965-1049` `normalize_url` / `resolve_relative_path` — empty/data: early return, protocol-relative per-blog scheme cache, root-relative, relative via `resolve_relative_path` with trailing-slash/file detection and `..`/`.` resolution — verified.
* `class-image-optimisation.php:1104-1177` `get_current_lcp_url` OD→singular meta→front-page option→transient (strategy mobile→desktop) + `get_effective_exclude_first_images_count` OD override — correct.
* `class-image-optimisation.php:1282-1450` `set_loading_optimization_attributes`, `tag_supports_auto_sizes`, `prepare_auto_sizes_value`, `sizes_attribute_includes_auto` — core `wp_get_loading_optimization_attributes` / `wp_sizes_attribute_includes_valid_auto` delegation with fallback regex `^\s*auto\b` — correct.
* `class-image-optimisation.php:1913-1994` YouTube placeholder — `get_youtube_video_id` regex handles `youtube(-nocookie).com/embed/` + `youtu.be/` 11-char; `generate_video_placeholder` checks `exclude_lazy_videos`, filter `wppo_video_placeholder_allowed`, `youtube-nocookie` type detection, `img.youtube.com/vi/ID/maxresdefault.jpg` + fallback, `data-wppo-iframe-attrs` JSON, `play_button` filter — correct.
* `class-image-optimisation.php:2015-2071` `process_iframe_tag` native vs JS-lazy, `wppo_lazyload_iframe_allowed` filter, `loading="lazy"` vs `data-src`+`wppo-lazyload` — correct.
* `class-image-optimisation.php:2350-2574` `prioritize_lcp_in_buffer` dual engine (`create_full_parser`+`serialize_token` WP 6.9 vs TagProcessor fallback), `unlazyload_first_images` counting `src`-only imgs, `tag_matches_lcp_url` src/data-src/srcset + `normalize_image_url` host+path+size-suffix strip — correct.
* `class-image-optimisation.php:2857-2957` `generate_svg_base64` (`4096` cap, `esc_attr`), `get_placeholder_type` allowlist, `get_placeholder_src_for_image` per-request caches, `dominant_color` regex `^#[a-f0-9]{6}$`, fallback to SVG, LQIP `data-wppo-lqip` — correct.
* `class-image-optimisation.php:2972-3024` `add_delay_load_backgrounds` — `WP_HTML_Tag_Processor` only, `data-wppo-bg`, `wppo-lazy-bg`, first N backgrounds skipped, `data:` skipped, `background-image` removed via regex — correct.
* `class-img-converter.php:113-130` constructor `excludeWebPImages` via `Util::process_urls`, format fallback `webp`, core-native override to `none`/`avif` — matches orchestrator gating.
* `class-img-converter.php:167-252` `resolve_encode_quality`, `resolve_output_format`, `get_source_image_dimensions` — 7.1 size-aware quality, `image_editor_output_format` mapping, `-{w}x{h}` suffix parse — verified.
* `class-critical-css.php:46-95` `ABOVE_FOLD_SELECTORS`, `SKIP_DEFER_HANDLES`, `MAX_IMPORT_DEPTH` constants — correct.
* `class-critical-css.php:1010-1055` `defer_stylesheets` — admin/logged-in guards, `deferJS/delayJS` deadlock guard (1028), skip handles, `data-wppo-ccss` dedup, `media="print" onload` swap with quote capture — verified.
* `class-used-css.php:65-104` `built_in_safelist` (`is-`,`has-`,`js-` etc.) — correctly conservative.
* `class-used-css.php:787-846` `save_used_css` tmp+move atomic + `delete_all_used_css` BFS — correct.
* `class-google-fonts.php:74-81` constructor `WP_CONTENT_DIR` cache dir/url — correct.
* `includes/minify/class-css.php:97-149` `minify()` + `get_cache_file_path` + `save_min_file` gzencode — correct.
* `includes/minify/class-html.php:106-160` constructor `excludeDelayJS`/`delayJSIdleList`/`delayJSViewportList`/`delayJSPriority` parsing with `array_filter(''!=='')` — prevents empty-pattern `strpos('',…)` always-true bug (fixed).
* `includes/minify/class-js.php:108-135` JS minify flow — correct.

*(All 8,932 lines walked; above is per-logical-block sampling — no hidden issues in validators, filters, or filesystem writes beyond §2.)*

---

## 4. Open Questions

| # | Question | Context | Needed Answer / Action |
|---|----------|---------|------------------------|
| Q-01 | Should `is_singular('post')` remain narrow or broaden to all singulars for CCSS bucketing? | A02-CC01 CPT singles misclassified to `index`. | Decide: keep `single` as generic singular bucket (`is_singular()` without arg) or keep per-post-type hashes. |
| Q-02 | Is regex fallback for `add_delay_load_img` intended to stay until WP <6.4 support dropped? | DUP-002 + A02-M01 show high maintenance + correctness cost. | Confirm minimum WP version bump timeline; when WP 6.7+ (≈ TagProcessor ubiquity) drop regex fallback, delete ~700 lines. |
| Q-03 | Should `Used_CSS::inject_used_css` switch to `WP_HTML_Tag_Processor` for removal? | A02-U01 order dependence + A02-U02 alternation blowup. | Approve switch vs patching regex look-ahead. |
| Q-04 | Should placeholder extraction be skipped for non-`dominant_color`/`lqip` modes to save GD decodes? | Already gated via `maybe_extract_placeholder_for_upload` at `1220-1223`, but `convert_image` path always extracts even when `placeholderType='none'/'svg'`? Actually `store_placeholder_data` called unconditionally at `653-659` even when `svg` mode (dominant_color still stored but not used). | Confirm whether to gate `extract_dominant_color`/`generate_lqip` on `placeholderType` when conversion runs. |
| Q-05 | Is `download_and_rewrite` synchronous blocking `wp_remote_get` (20s) acceptable inside output-buffer filter? | A02-F02 thundering herd + frontend latency. | Consider async warm via cron/Action Scheduler vs keep sync with 20s timeout. |

---

## 5. Recommendations Priority

1. **Fix before release (P0 — correctness):** A02-M01 (regex count heuristic — images lazy-load broken on fallback), A02-M02 (normalize_url in next-gen fallback), A02-CC01 (CPT CCSS bucket), A02-U01 (used-CSS removal order).
2. **Fix next iteration (P1):** A02-C02 (AVIF prepare_dir ignored), A02-C03 (gain-map ext guard), A02-CC02 (`..` import resolve), A02-CC03 (`setup_postdata` int), A02-CC04 (token_match class-boundary), A02-U02 (alternation ReDoS).
3. **Tech-debt epic (P2):** DUP-002/005/006/007, A02-J01 base class, OPT-001…004 perf.

---

## 6. Methodology

* `Read` all 8 files completely (offsets 1, 1457, 2698 for image-optimisation; 1, 1411 for img-converter; full for 6 smaller files) — no sampling.
* Traced every `WP_HTML_Tag_Processor` / `WP_HTML_Processor` vs regex fallback path for `<img>`, `<picture>`, `<source>`, `<iframe>`, `<video>` — compared attribute order, `normalize_url` vs raw URL, `is_valid_url` gating.
* Checked every `add_action`/`add_filter` name against core docs and `includes/` other files.
* Traced all `$wpdb` / `get_option` / `update_option` / `transients` / `filesystem` / `wp_remote_get` paths for injection, race, autoload bloat.
* Verified escaping (`esc_attr`, `esc_url`, `sanitize_text_field`, `wp_json_encode` + `esc_attr`) on every `$_SERVER`/`$_GET`/URL read.
* Simulated regex edge cases for `is_valid_url`, `normalize_url`, `resolve_relative_path`, placeholder injection, `count($matches)` invariant, `preg_quote` alternation.
* Cross-referenced duplicate blocks via `Grep` (see counts: 223 url-processing hits, 60 placeholder hits) and line-number evidence.

**Verdict:** Media pipeline is overall well-hardened (XSS, SSRF, traversal, autoload, salts, locks all have layered guards). Highest-risk **remaining** items are interface/logic mismatches in legacy fallback paths (`add_delay_load_img` count heuristic, next-gen fallback normalization) and inaccurate selector/CSS matching heuristics (CCSS `token_match`, used-CSS `rel→href` order) — not in core WebP/AVIF or font-fetch logic, which is sound. Duplication remains the dominant maintainability risk.

---

*Generated by agent A02 `php-media` — exhaustive line-by-line audit (8,932 lines), production code untouched.*
