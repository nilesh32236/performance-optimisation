{
	"id": "refactoring-07-extract",
	"area": "refactoring",
	"title": "Extract a pure helper without behavior change",
	"prompt": "Refactor duplicated retry-delay math into a single pure helper with identical behavior.",
	"language": "javascript",
	"timeoutMs": 30000,
	"pinnedInput": "const a = Math.min(1000 * 2 ** n, 30000); const b = Math.min(1000 * 2 ** m, 30000);",
	"referenceOutput": "export function retryDelayMs(attempt, baseMs = 1000, capMs = 30000) {\n\treturn Math.min(baseMs * 2 ** attempt, capMs);\n}\nconst first = retryDelayMs(n);\nconst second = retryDelayMs(m);",
	"checks": [
		{ "id": "helper", "kind": "contains", "value": "retryDelayMs" },
		{ "id": "bounded", "kind": "contains", "value": "Math.min" },
		{ "id": "no-duplication", "kind": "notContains", "value": "const b = Math.min" }
	]
}
