# IMPLEMENTATION-MATRIX.md — File-level change map (P0-3)

_Every proposed production change must appear here. Priority P0 essential, P1 high, P2 useful, P3 optional. Based on ADVERSARIAL survivors RETAIN/MODIFY._

| Priority | Area | File | Class/Function | Change | Reason | Dependencies | Tests |
|----------|------|------|----------------|--------|--------|--------------|-------|
| P0 | CLI help | `includes/class-wppo-cli-command.php:76,175,322` | `WPPO_CLI_Command::cache/database/image` synopsis | `<action>` → `[<action>]` + `options:` enum for `cache`/`database`/`image` per handbook `## OPTIONS` | `WP_CLI` synopsis pre-invoke validation fails friendly before `WP_CLI::error` | `WP_CLI::add_command` `@when after_wp_load` unchanged | `WppoCliHelpTest` asserts `--help` contains `[<action>]` |
| P0 | CLI output | `includes/class-wppo-cli-command.php:202,385,808,965` | `database:counts` + `image:status` + `object-cache:status` + `system-info` | Hardcoded `wp_json_encode(PRETTY)` → `WP_CLI\Utils\format_items`/`Formatter` `table|json` (+ `csv|yaml|count` where useful) gated `count>1`, `--format=json` passthrough, `--fields` where table | `Formatter` requires `WP_CLI` global, fallback JSON when absent | `WP_CLI\Formatter` | `WppoCliFormatTest` `table|json` + `--field` |
| P0 | CLI safety | `includes/class-wppo-cli-command.php:75,174,622,801` | `cache:clear` + `database:cleanup --type=all` + `object-cache:disable` + `settings:import` | Add `[--yes]` (`Utils\get_flag_value`) + `[--confirm]` alias via `WP_CLI::confirm("Continue?")` when `! --yes`, skip confirm when `! STDIN isatty` | Matches `search-replace --dry-run` + Rocket `--confirm` canon `ECOSYSTEM §4.5` | `WP_CLI` interactive | `WppoCliConfirmTest` `--yes` bypass, no `--yes` prompts (mock `confirm`) |
| P0 | CLI dry-run | `includes/class-wppo-cli-command.php:75,174` | `cache:clear` (`--dry-run` lists `wppo_after_cache_clear` URLs via `Cache::get_cache_stats` dry) + `database:cleanup/optimize` (`--dry-run` counts via `get_counts`/`SHOW TABLE STATUS` without `DELETE`/`OPTIMIZE`) | `--dry-run` performs no mutations, logs JSON `would_clear:200`/`would_delete:9` | `WP_CLI\Utils\get_flag_value('dry-run')` boolean | `Database_Cleanup::get_counts` | `WppoCliDryRunTest` asserts no `update_option`/`unlink`/`DELETE` when flag |
| P0 | CLI multisite | `includes/class-wppo-cli-command.php:75` | `cache:clear/status/preload` + `database:*` | Add `[--network]` paginated `get_sites(['number'=>100,'offset'=>...])` + `switch_to_blog(id)` + `try { do } finally { restore_current_blog(); }` per `uninstall.php:193-217` canonical, `--network` only when `is_multisite()` | `wppo_cache_clear` already per-site `switch_to_blog` loop `uninstall.php`; `object-cache:flush` warns `all-sites` when `--network` absent | `get_sites` + `switch_to_blog` | `WppoCliNetworkTest` 3-site fixtures `get_sites` mock + `restore` |
| P0 | Hook veto cache | `includes/class-cache.php:1505` + `:1755` | `Cache::should_cache_request()` + `Cache::generate()` early return | Add `filter wppo_should_cache_request` `apply_filters('wppo_should_cache_request', true, $request_uri, $is_mobile, $is_logged_in)` **Before** `DONOTCACHEPAGE` check, `return false` → skip `ob_start` | P0 `HOOK-GAPS G-01` `ADVERSARIAL RETAIN`, membership/Woo preview gate | `wppo_cache_page_html:1661` already mutates HTML only | `HookShouldCacheRequestTest` filter false skips `ob_start` |
| P0 | Hook user cache | `includes/class-cache.php:297` + `includes/class-main.php:369` | `Cache::is_user_excluded()` + `Main::is_user_excluded()` | Add `filter wppo_should_cache_for_user` `(bool $should_cache, int $user_id, array $user_roles)` and `wppo_should_optimise_for_user` at same point, vetted by `ADVERSARIAL` `G-28` | `loggedInCacheRoles` allowlist insufficient for membership | `Util::transient_key` unchanged | `HookShouldCacheForUserTest` |
| P0 | Hook DB per-type | `includes/class-database-cleanup.php:714` + `:935` | `Database_Cleanup::clean_all()` loop + `::clean_*()` per-type | Add `action wppo_before_database_cleanup` `(string $type, array $args)` **Before** `switch`, `action wppo_database_cleanup_completed` already only for `all` → add per-type `do_action('wppo_database_cleanup_completed', $type, $count)` + `apply_filters('wppo_database_cleanup_type_completed', $count, $type)` **After** each `clean_*` | P0 `G-15` `REST/database counts` silent for single-type | `class-rest.php:357` `manage_options` guard unchanged | `HookDatabaseCleanupPerTypeTest` |
| P1 | Hook observability | `includes/class-cache.php:1741` + `:1661` | `Cache::save_cache_files()` + `Cache::get_cache()` + `templates/advanced-cache.php` | Add `action wppo_cache_written` `($file, $size, $duration_ms)` **After** `atomic_put_contents` success, `action wppo_cache_miss` `($uri)` **Before** `ob_start` in drop-in, keep `wppo_cache_page_html` | `G-02` keep `written`+`miss` reject `hit` (drop-in no hook) | `Cache::atomic_put_contents:1572` | `HookCacheWrittenTest` |
| P1 | Hook invalidation | `includes/class-cache.php:1838` | `Cache::invalidate_post()` | Add `filter wppo_invalidation_urls` `apply_filters('wppo_invalidation_urls', $urls, $post_id)` **Before** `foreach $urls` purge, priority 10 | `G-03` `ADVERSARIAL RETAIN` | `wppo_after_cache_clear:2074` consumers `class-main.php:623,626` | `HookInvalidationUrlsTest` |
| P1 | Hook preload caps | `includes/class-cron.php:301` + `:364` + `:487` | `Cron::schedule_page_cron_jobs()` batch 200 + `Cron::get_sitemap_urls()` 500 limit + `Cron::preload()` | Add `filter wppo_preload_batch_size` `(int 200, int $offset)` **Before** `get_posts`, `filter wppo_sitemap_preload_limit` `(int 500)` **Before** `slice`, `filter wppo_sitemap_urls` `($urls, $sitemap_url)` **After** parse | `G-05` `ADVERSARIAL MODIFY` keep 3, reject deadline | `CronWebVitalsRescanTest` mocks | `HookPreloadBatchSizeTest` |
| P1 | Hook CDN | `includes/class-cache.php:1357` | `Cache::cdn_rewrite()` | Add `filter wppo_cdn_url` `apply_filters('wppo_cdn_url', $cdn_url, $src)` **During** per-asset rewrite, `return ''` → skip that asset | `G-07` narrow per-asset | `Util::is_url_excluded:214` | `HookCdnUrlTest` |
| P1 | Hook delay | `includes/class-main.php:600` | `Main::delay_js()` `wppo_exclude_delay_js:515` already | Add `filter wppo_delay_js_should_delay` `(bool $should_delay, string $handle)` **Before** `wppo_exclude_delay_js` check, priority 10 | `G-09` keep `delay` only reject `defer` (already `wppo_exclude_defer_js`) | `script_loader_tag:608` | `HookDelayShouldDelayTest` |
| P1 | Hook image veto | `includes/class-image-optimisation.php:887` | `Image_Optimisation::should_serve_next_gen()` | Add `filter wppo_should_serve_next_gen` `(bool, string $url, string $format)` **Before** `cached_home_url` check | `G-12` keep veto reject mutate | `Util::normalize_url:322` | `HookShouldServeNextGenTest` |
| P1 | Hook lazy predicate | `includes/class-image-optimisation.php:100` | `Image_Optimisation::maybe_lazy_load()` | Add `filter wppo_should_lazy_load_image` `(bool, array $img_attrs, string $src)` **Before** `exclude` list | `G-13` | `class-image-optimisation:120` exclude | `HookShouldLazyLoadTest` |
| P1 | Hook DB lifecycle extras | `includes/class-database-cleanup.php:138` + `:753` | `Database_Cleanup::delete_in_batches()` + `get_revision_defaults()` | Keep existing `delete_in_batches` `LIMIT 1000` but add `filter wppo_database_cleanup_batch_size` `(int 1000)` **Before** `array_slice`, `filter wppo_revision_defaults` `($days, $limit)` **During** | `G-16` `G-05` narrow | `CLEANUP_METHOD_MAP:81` | `HookDatabaseBatchSizeTest` |
| P1 | Hook object-cache | `includes/class-object-cache.php:67` + `includes/class-wppo-cli-command.php:864` | `Object_Cache::get_redis_config()` + `get_redis_config_from_assoc()` | Already `wppo_object_cache_dropin_path:67`; add `filter wppo_object_cache_config` `($config)` **After** `get_redis_config` merge + `filter wppo_cli_redis_config` `($config, $assoc_args)` **After** `get_redis_config_from_assoc` to converge 6 vs 10 keys (`host,port,password,database,timeout,prefix` + `tls,compression,sentinel,cluster`) | `G-19`/`G-22` `ADVERSARIAL` 6→10 gap | `Object_Cache` drop-in `templates/object-cache.php:532` | `HookRedisConfigTest` |
| P1 | Hook settings veto | `includes/class-rest.php:464` + `includes/class-util.php:464` | `Rest::update_settings()` + `Util::sanitize_settings_recursively()` | Add `filter wppo_settings_before_update` `apply_filters('wppo_settings_before_update', $new_settings, $tab, $existing)` **Before** `update_option` returning `WP_Error` aborts save, `action wppo_settings_updated` `($tab, $new, $old)` **After** | `G-20` | `ALLOWED_SETTINGS_KEYS:43` | `HookSettingsBeforeUpdateTest` |
| P1 | Hook CLI purge | `includes/class-cdn-purger.php:193` + `:212` | `CDN_Purger::purge_all()` + `Edge_Purger::purge_all()` | Add `filter wppo_purge_urls` `($urls, $purge_type)` **Before** `wp_remote_request` loop, `return []` → dry-run | `G-27` keep veto | `Cloudflare_Purger:54` | `HookPurgeUrlsTest` |
| P1 | Perf context fence | `includes/class-main.php:485-799` `setup_hooks()` | `setup_hooks()` always loads 70 hooks | Gate `is_admin()` 7 hooks (`admin_menu:486`, `Admin_Notices:495`, `Metabox:499`, `add_sites:507`, `maybe_fix_wp_cache:488`, `maybe_run_upgrades:489`, `migrate_block_assets:492`) + lazy `Util::init_filesystem:346` (`require_once` только when `is_admin()`||`WP_CLI`) + defer `Cron::schedule_cron_jobs:114` 8× `wp_next_scheduled` to `init` `did_action('init')` check | `PERF-RESEARCH` P0 saves 0.4 ms CLI | `class-main.php:472` `WP_CLI` guard already | `HookContextFenceTest` asserts `is_admin()` false → `has_action('admin_menu')` false on CLI |
| P2 | Hook RUM sampling | `includes/class-rum.php:121` | `RUM::maybe_enqueue_scripts()` | Add `filter wppo_rum_should_collect` `(bool $should, string $url, array $config)` **Before** `is_user_logged_in` check | `G-17` low but useful sampling | `RUM::store_sample:121` | `HookRumShouldCollectTest` |
| P2 | CLI progress | `includes/class-wppo-cli-command.php:321,174` | `image:convert` loop 50/batch + `database:cleanup` 9 batches | Add `WP_CLI\Utils\make_progress_bar("Converting $fmt", $totalPending)` `tick()` per `convert_image` + `finish()`; gated `if ($totalPending>10 && ! Utils\get_flag_value($assoc_args,'format',false))` | `PERF-RESEARCH` P0 CLI 0.4-0.7 ms | `WP_CLI\Utils` | `WppoCliProgressTest` mock progress |
| P2 | CLI batch-size | `includes/class-wppo-cli-command.php:321` | `image:convert` `$batch_size 50` | Add `[--batch-size=<num>]` assoc, default `50`, `absint` validation `1-500`, passed to loop `if($counter >= $batch_size)` | `W-06` `class-database-cleanup.php:138` `LIMIT 1000` precedent | `batch` option already `image_optimisation:batch` | `WppoCliBatchSizeTest` |
| P2/P3 deferred | Hook | `includes/class-cache.php:1243` buffer 6-stage | `Cache::buffer` 6-stage pipeline | **No hook** — pick `A` minimal per `BRAINSTORM` `G-04` `ADVERSARIAL REJECT` 6-stage `wppo_buffer_stage` (keep `wppo_cache_page_html` only) | Buffer coupling high | `wppo_cache_page_html:1661` sufficient | none |
| Docs | Docs | `docs/hooks.md:1-439` | 42 `wppo_*` table | Add 15 undoc public filters (`wppo_filesize_limit_bytes:402`, `wppo_cron_discovery_limit:666`, `wppo_server_timing_enabled`, etc.) + 12 new `wppo_*` with `@since NEXT`, Example, Priority | `@since NEXT` per `AGENTS.md:79` | `docs/hooks.md` generation via `grep` | `DocsHooksTest` asserts 27+ rows |

