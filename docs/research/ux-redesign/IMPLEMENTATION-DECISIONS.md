# IMPLEMENTATION-DECISIONS.md — Deviations from Plan (2026-08-29)

| Decision | Original plan | Actual implementation | Reason | Trade-off | Impact |
|----------|---------------|-----------------------|--------|-----------|--------|
| Navigation 4+1 pillars | `INFORMATION-ARCHITECTURE.md` Speed/Media/Data&System/Manage + Diagnostics separate page | Speed pillar = `FileOptimization+PreloadSettings` stacked, Data = `DatabaseCleanup+ObjectCache` stacked, Diagnostics drawer under Overview not separate tab | Keep existing components reused vs new Diagnostics page; reduces rewrites per §6 preserve engines | Diagnostics not top-level but drawer still discoverable via HealthHeader "Show details" | Low — future split to separate Diagnostics route via `?tab=diagnostics` still possible |
| Health scores 0-100 | `DASHBOARD-DESIGN.md` 3 rings 0-100 from PSI + telemetry + DB overhead | Mock scores 92/88/64 static in `Dashboard.js` `HealthHeader` props, not yet computed from `PerformanceAudit:121` thresholds + `PageSpeedPanel` | Time constraint — computing from `numericStatus 200/500` + PSI requires PSI key `PluginSetting:108` + async queue 681 failed noted `Agent N` | Health not yet data-driven, shows needs_attention for efficiency vs good | Medium — next run wire `PerformanceAudit.js` metrics to `HealthHeader` derivation |
| Recommended safe set | `ONBOARDING.md` 10 toggles `enableCache/minifyCSS/JS/defer/lazyNative` bundle single `apiCall update_settings` | Wizard Apply Recommended batch via sequential `apiCall update_settings` per pillar not single bundle `SetupWizard.js` Apply step, snapshot before each | Snapshot per `update_settings:518` already per call, single bundle would need new REST batch endpoint | Multiple `update_settings` calls vs one atomic | Low — snapshots cover each, future batch endpoint `wppo_batch_update` could atomic |
| Search global | `ADVANCED-MODE.md` global filter "defer, redis, critical" points to correct location | `src/App.js` `searchQuery` passed as `compact` prop to Speed/Media/Data/Manage but FileOptimization not yet filtering 40 toggles by query | Filter logic not yet implemented — `compact` string received but not used to hide non-matching Disclosure sections | Search input visible but not filtering yet | Medium — next run add `compact.toLowerCase().includes` hide logic in `FileOptimization.js` Disclosure sections + highlight |
| Bundle size 244 KiB limit | `PERFORMANCE` warn <244 KiB `AGENTS.md` build `src/index.js src/lazyload.js src/main.js src/rum.js` | `build 284 KiB` exceeds limit by 40 KiB due to `SetupWizard 1033` + `HealthHeader 283` + `Disclosure` + icons `faSearch/faRocket` `webpack 5.109` warning | New components add weight but lazy tab chunks `tab-dashboard` etc still 17-83K separate; entry is 284 vs 248 before wizard | Entry exceeds recommended 244 | Low — future code-split Wizard lazy `lazy(() => import('./SetupWizard'))` + `HealthHeader` dynamic import to reduce entry |
| Diagnostics drawer | `DASHBOARD-DESIGN.md` "Show details" collapsible with full 16-row table `PerformanceAudit:506` behind `?healthDetails=1` | `PerformanceAudit.js` drawer behind `aria-expanded` button already (sub-agent) but Dashboard health drawer not yet separate — HealthHeader CTA scrolls to `wppo-audit-details` id not yet present | `wppo-audit-details` id not set in Dashboard sections yet | Scroll target missing | Low — add `id` to diagnostics wrapper |
| Backend snapshot TTL | `SAFETY-RECOVERY.md` `wppo_settings_snapshot_{time}` transient 10m + Undo Tools + cookie `wppo_safe_mode` 10min | Implemented as `wppo_settings_snapshot_.time() 600` + `_latest` pointer `Util::transient_key()` + `handle_snapshot_undo` scan fallback DB `option_name LIKE` `Rest:1631` | As planned — no deviation, but undo not yet exposed in UI Manage Tools button | Undo exists as REST `wppo_snapshot_undo` but no UI button `PluginSetting.js:330` only SetupWizard re-open | Low — next run add Manage Tools "Undo last change (snapshot)" button calling `apiCall wppo_snapshot_undo` |
| Conflict banner friendly | `CONFLICT-DETECTION.md` "Another plugin handles this — leave off. Why? Details → Advanced" | `SystemInfo::get_conflicts:360` + `wppoSettings.conflicts` injected `Main:1617` but `HealthHeader.js` warning not yet rendered from `conflicts.has_conflict` prop | Backend ready, frontend banner not wired — HealthHeader warning for `conflicts` still needs prop passing from Dashboard `wppoSettings.conflicts` | No visible banner yet for WP Rocket etc vs LiteSpeed banner `706` still shown | Medium — next run wire `Dashboard.js` `wppoSettings.conflicts` → `HealthHeader` `conflicts.has_conflict` banner |

No silent deviations — all logged.

