<?xml version="1.0"?>
<ruleset name="Performance Optimisation Plugin">
	<description>WordPress Coding Standards for Performance Optimisation Plugin</description>

	<!-- Check all PHP files in the plugin -->
	<file>.</file>

	<!-- Exclude vendor and build directories -->
	<exclude-pattern>*/vendor/*</exclude-pattern>
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/build/*</exclude-pattern>
	<exclude-pattern>*/dist/*</exclude-pattern>
	<exclude-pattern>*/coverage/*</exclude-pattern>
	<exclude-pattern>*/tests/*</exclude-pattern>
	<exclude-pattern>*/admin/src/*</exclude-pattern>
	<exclude-pattern>composer.phar</exclude-pattern>
	<exclude-pattern>vendor/*</exclude-pattern>
	<exclude-pattern>node_modules/*</exclude-pattern>
	<exclude-pattern>build/*</exclude-pattern>

	<!-- Use WordPress Coding Standards -->
	<rule ref="WordPress">
		<!-- Allow short array syntax -->
		<exclude name="Generic.Arrays.DisallowShortArraySyntax"/>
		
		<!-- Allow multiple assignments in one line for constants -->
		<exclude name="Squiz.PHP.DisallowMultipleAssignments"/>
		
		<!-- Allow yoda conditions to be optional -->
		<exclude name="WordPress.PHP.YodaConditions.NotYoda"/>

		<!-- Allow PSR-4 filenames -->
		<exclude name="WordPress.Files.FileName"/>
	</rule>

	<!-- Use WordPress-Extra rules -->
	<rule ref="WordPress-Extra">
		<!-- Allow short array syntax -->
		<exclude name="Generic.Arrays.DisallowShortArraySyntax"/>
	</rule>

	<!-- Use WordPress-Docs rules -->
	<rule ref="WordPress-Docs"/>

	<!-- Check for PHP cross-version compatibility -->
	<rule ref="PHPCompatibility"/>
	
	<!-- Set minimum PHP version -->
	<config name="minimum_supported_wp_version" value="6.2"/>
	<config name="testVersion" value="7.4-"/>

	<!-- Show progress -->
	<arg value="p"/>
	
	<!-- Show sniff codes in all reports -->
	<arg value="s"/>
	
	<!-- Use colors in output -->
	<arg name="colors"/>
	
	<!-- Check up to 20 files simultaneously -->
	<arg name="parallel" value="20"/>
	
	<!-- Only check PHP files -->
	<arg name="extensions" value="php"/>

	<!-- Ignore warnings about missing textdomain in specific cases -->
	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array">
				<element value="performance-optimisation"/>
			</property>
		</properties>
	</rule>

	<!-- Allow longer lines for readability in some cases -->
	<rule ref="Generic.Files.LineLength">
		<properties>
			<property name="lineLimit" value="120"/>
			<property name="absoluteLineLimit" value="150"/>
		</properties>
	</rule>
</ruleset>
